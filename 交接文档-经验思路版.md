# PicaKeep 交接文档 — 经验思路版

> **用途**：下一位接手 AI / 程序员遇到问题时，先查开头**症状速查表**定位，再跳对应章节看根因→修法→踩坑警告。
> **阅读方式**：症状 → 速查表 → 跳转章节 → 看根因推理链 → 看 ❌ 不要再走的路。
> **配套文档**：`交接文档-精简最新版.md`（功能清单，可快速了解项目全貌）。

---

## 症状速查表

| 症状 | 可能根因 | 跳转 |
|------|---------|------|
| 远程图片全部加载失败，**热重启/重开App恢复**，logcat 只有 TimeoutException 无 404 | 孤儿连接耗尽单例连接池 | [§1](#1-远程服务与连接池) |
| 滚动过"服务端实际不存在内容"的漫画/封面后，所有远程档无法载图 | 404 不 drain 泄漏连接 | [§1](#1-远程服务与连接池) |
| reader 看几本后所有图片 5s TimeoutException，离开页面无效 | 连接池被废 socket 占满 | [§1](#1-远程服务与连接池) |
| 封面网格加载导致"远程加载失败"+ 列表转圈 | 封面走 control 池把 JSON 请求饿死 | [§1](#1-远程服务与连接池) |
| 收藏远程档点击没反应、无任何错误提示 | `_onTap` 缺 try/catch，unhandled exception 被静默 | [§2](#2-收藏图片收藏远程档) |
| 收藏/图片收藏页返回跳到"我"页 | `App.globalTo` 推到根 navigator | [§2](#2-收藏图片收藏远程档) |
| 点击远程收藏项弹 SnackBar 时崩溃 | 用 `App.globalContext` 调 `ScaffoldMessenger.of` | [§2](#2-收藏图片收藏远程档) |
| 服务端中文文件夹（如"中间档测试"）报 500 `Illegal percent encoding` | `Uri.decodeComponent(segments[i])` 二次解码 | [§4](#4-服务端模式) |
| 服务端写收藏后所有后续请求卡住 | `_notifyLibraryChanged` 调了 `rescanResources()` 全库重扫 | [§4](#4-服务端模式) |
| 远程封面反复请求 404、控制台刷屏 | tile 无 `errorBuilder` + provider 无负缓存 | [§2](#2-收藏图片收藏远程档) |
| 加密 ZIP 输**任意密码**都显示"解锁成功"，但封面破图、阅读器报 `Invalid image data` | `tryUnlock` 只列目录未真实验密码 | [§3](#3-加密压缩包) |
| 加密 ZIP（AES-256 + deflate）输正确密码报"压缩包已损坏" | inflate `windowBits=15` 应为 `-15`（裸 deflate 流） | [§3](#3-加密压缩包) |
| ZIP/CBZ 内部章节名乱码，但外层标题正常 | entry filename 编码错（GBK/Shift-JIS 被当 UTF-8 解） | [§3](#3-加密压缩包) |
| 阅读器顶部有黑条（特定入口启动时） | `activity-alias` 缺少 `NormalTheme` meta-data | [§6](#6-android-启动与系统-ui) |
| 阅读器隐藏顶栏时状态栏**瞬间消失**（无淡出动画） | `immersiveSticky` 改为 `immersive` | [§6](#6-android-启动与系统-ui) |
| 设置页滑块圆圈不跟手，等别处触发重建才跳 | `StatelessWidget` 内联 `Slider.onChanged` 不 `setState` | [§5](#5-动态取色与设置交互) |
| 应用偶发 ANR，logcat `dynamicColors` 每 ~6s 刷一次 | MIUI `onConfigurationChanged` 每 6s 发一次，节流 1000ms 太短 | [§5](#5-动态取色与设置交互) |
| 删除 Android/data 下 managed 资源抛 `PathNotFoundException` | `DownloadManager.init()` 误触他人沙箱包根 | [§7](#7-本地回收站与删除链路) |
| Shizuku 下本地已下载扫描返回 0 | fallback 只检查路径存在不看 `dart:io` 能否列目录 | [§8](#8-已下载页三档隔离) |
| 已下载页一直转圈不超时 | 缺页面级加载超时 | [§8](#8-已下载页三档隔离) |
| 远程封面回滑后消失或重新加载变慢 | provider key 不稳定（`StreamImageProvider` vs `FileImage` 换 key） | [§8](#8-已下载页三档隔离) |
| 历史页清空/删除后"我"页历史预览仍显示旧数据 | `HistoryManager` 未通知 me_page 刷新 | [§10](#10-历史同步与图集封面) |
| 图集封面被左右压扁 | 封面解码同时限制了 `cacheWidth` 和 `cacheHeight` | [§10](#10-历史同步与图集封面) |
| Shizuku 已授权但设置页显示"未连接" | 检测逻辑只看某一条件未覆盖全部授权状态 | [§7](#7-本地回收站与删除链路) |
| 桌面端已下载/图集详情侧栏顶部被标题栏遮挡 | `showSideBar` 传了局部 context 而非全局 | [§11](#11-桌面移动端交互) |
| "我"页下载数量统计失败（Shizuku 下） | 辅助入口偷偷调了 `DownloadManager().init()` | [§7](#7-本地回收站与删除链路) |
| 远程图集内容删除后列表数量未更新 | 缓存未失效 + 强制刷新链路未触发 | [§4](#4-服务端模式) |
| 应用图标出现两个（debug 包） | `debug/AndroidManifest.xml` 的 overlay 让 MainActivity 也成 launcher | [§6](#6-android-启动与系统-ui) |
| 图片收藏网格在手机上变成两列 | `settings[74]` 存值漂移（可能） | [§2](#2-收藏图片收藏远程档) |
| 设置页数据管理挡位切换后底部提示不显示 | `ScaffoldMessenger` 提示被页面层级吞掉 | [§7](#7-本地回收站与删除链路) |
| 网页后台打开后仍像旧 `/admin` 或静态资源 404 | 未走 `assets/web_console/` SPA 静态资源管线 / fallback | [§13](#13-网页后台与浏览器应用) |
| 网页应用面板点入口后回不到 dashboard | grid/list 视图缺少 `data-back` 返回栏 | [§13](#13-网页后台与浏览器应用) |
| 网页 dashboard「已下载」和「图集」混在一起 | 没按 `rootId.startsWith('custom_')` 分流 | [§13](#13-网页后台与浏览器应用) |
| 网页图片收藏入口只进普通 grid | 未接 `/api/library/image-favorites` 真数据 | [§13](#13-网页后台与浏览器应用) |
| `PicaKeepConsole.history.remove(...)` 404 | 前端用路径段，服务端只认查询参数 `?target=` | [§13](#13-网页后台与浏览器应用) |
| 网页 reader 整页空白，但图片请求 200 / DOM 里有 img / `naturalWidth` 正常 | `#console-root`/reader 容器被 shell grid 压成 0 宽，不是图片加载失败 | [§13](#13-网页后台与浏览器应用) |
| 本地阅读时出现 `/api/remote/proxy?...target=http://127.0.0.1:9527...` 的 502/401 | 数据源停在「远程」触发远程自代理，应先切「本地」源 | [§13](#13-网页后台与浏览器应用) |
| 网页阅读点击画面中心唤不出上下栏 | 边缘翻页判定误用整条连续 reader 高度 + 指针释放链路清掉单击 tapTimer | [§13](#13-网页后台与浏览器应用) |
| 网页阅读按 ESC 退出不保存进度，只有点返回键才保存 | 1500ms 防抖写历史被 `cleanupReader` 直接 clearTimeout；返回键操作慢通常已落盘，ESC 瞬退在窗口内被取消 | [§13](#13-网页后台与浏览器应用) |
| 从网页历史记录打开漫画总停在第 1 页（分页模式正常） | 滚动连续模式单次 rAF `scrollIntoView` 在懒加载图片定高前跳转落到顶部，`pageObserver` 又把 pageIndex 覆写回 0 | [§13](#13-网页后台与浏览器应用) |
| 网页历史卡第二行显示「0」/作者位异常 | 服务端历史只存 `readEpisode`(int 集合)，无 author/subtitle 字段，`String([0])` 渲染成 "0" | [§13](#13-网页后台与浏览器应用) |
| 网页收藏夹漫画封面破图 / 卡片点不动 | 服务端 favorites payload 缺 `itemId`，`__matched` 永远 false → 封面被清空、卡片 `aria-disabled` | [§13](#13-网页后台与浏览器应用) |
| 收藏夹封面 404 但资源库里明明有这本 | 本地 `coverPath` 已失效，cover 路由未按 candidates 回退匹配资源库 | [§13](#13-网页后台与浏览器应用) |
| 网页详细档标题 2 行后第三行漏出一点 | `.app-detailed-title` 同时有 `text-overflow:ellipsis` 和多行 `-webkit-line-clamp` 冲突 | [§13](#13-网页后台与浏览器应用) |
| 网页详细档标签是灰底 / 切主题色不变 | 标签背景写死非 accent 变量；应走 `var(--accent-rgb)`/`var(--accent-2)` | [§13](#13-网页后台与浏览器应用) |
| 远程/网页图集看不到压缩包、压缩包不可读 | 服务端原本不扫 ZIP/CBZ；需扫成普通 item + 取图返回 archive 字节 | [§16](#16-远程访问图集压缩包含加密包) |
| headless 服务端读压缩包抛 `unsupportedFormat` | `ArchiveRegistry.initDefaults()` 没在服务端 `start()` 调 | [§16](#16-远程访问图集压缩包含加密包) |
| 远程/网页加密压缩包不提示输密码 | 端侧没判 `needsArchivePassword`/没接 unlock 接口 | [§16](#16-远程访问图集压缩包含加密包) |
| 阅读「图片最大显示宽度」滑块拖了没反应（翻页/连续模式） | 翻页模式没读 `settings[116]`；连续模式 sliver tight 约束吃掉 item 内 width，须外层限宽 | [§17](#17-阅读器图片最大显示宽度滑块在连续模式失效与闪烁) |
| 拖图片宽度滑块时图片闪烁 | `cacheWidth` 随滑块每帧变 → `ResizeImage` key 变 → 重解码；解码宽度须与显示宽度解耦 | [§17](#17-阅读器图片最大显示宽度滑块在连续模式失效与闪烁) |
| 网页章节列表标题逐字竖排 | 章节按钮误共用阅读模式列表的 `34px` 图标三列模板，无图标致标题挤进 34px 列 | [§18](#18-网页后台-ui-二次还原客户端详情页--阅读面板--章节列表) |
| 网页详情页单章章节占满整行 / 很空 | 章节网格 `auto-fit + 1fr` 把单 tile 拉满；改 `auto-fill + 固定上限宽 + justify:start` | [§18](#18-网页后台-ui-二次还原客户端详情页--阅读面板--章节列表) |
| 服务端外部新增/删除资源后远程/网页不刷新、需重启 | `_currentSnapshot()` 永返缓存、无文件监听；已加 `Directory.watch` + 去抖 rescan | [§14-11](#14-当前仍存在的已知问题) |
| 局域网发现方式显示 mDNS，但实际只是逐 IP 请求 `/status` | 旧实现把网段扫描误标成 mDNS；现在应区分 mDNS / Bonjour 与网段扫描两档 | [§4](#4-服务端模式) |
| Linux/Android 扫不到 Windows 服务，但 Windows 能扫别人 | Windows 多网卡时组播响应可能走错接口；已修复：查询回复改为单播到源地址 | [§4](#4-服务端模式) |
| 网段扫描再次只显示当前已连接服务，不显示同网段其他服务 | 回归点是 `scan()` 先探 `explicitHosts` 后又在命中时提前停止；当前实现应扫完整候选并聚合 | [§4](#4-服务端模式) |
| 客户端远程档全部 401 加载失败 | `_requiresAuthorization` 被改回全 `api/` 鉴权；客户端远程档没有网页 token | [§14-10](#14-当前仍存在的已知问题) |
| Linux 构建卡在 `Building Linux application...`、cmake 报 `Cannot find source file: sqlite3.c` 或卡在下载 `sqlite-autoconf` | `sqlite.org` 不可达（被墙）、`FetchContent` 下载超时、`build/_deps` 在 remote tar 解包流程中被清空 | [§25](#25-linux-桌面构建-sqlite3-下载卡死) |
| root 模式本地已下载滚动卡顿、退出应用仍提示 root 权限 | `_warmManagedDownloadCache` 挂在单例上遍历全部 items，脱离页面生命周期 | [§27](#27-本地已下载列表滚动卡顿) |
| root/shizuku 下图集封面破图，但能阅读、底栏侧栏正常 | 图集封面渲染用裸 `FileImage`/`Image.file`，不经过特权通道；`imageProviderForLocalPath` 的 existsSync 短路堵死回退 | [§28](#28-rootshizuku-下图集封面破图) |
| root/shizuku 下无封面下载项折返时反复卡顿 | 每次重入队列都重新走 ~11 次特权通道探测 cover.* | [§27](#27-本地已下载列表滚动卡顿) |
| db 有记录但目录已删除的项滑到时卡顿 | `trustStorageFromDatabase` 把 db-only 项标为 `localStorageExists=true` 入封面队列 | [§27](#27-本地已下载列表滚动卡顿) |
| 冷启动「我」页面图集数量迟迟不刷新，logcat 有 APP_SCOUT_HANG | `Clipboard.getData` 经 JSONMethodCodec 编码大剪贴板文本，主线程 6.4s 冻结 | [§30](#30-开屏剪贴板大文本anr-修复) |
| 单项自定义源点进去仍多一层列表才能进详情 | 未判子项数，一律进中间页 | [§29](#29-单项源直开与图集计数改为了项之和) |
| 「我」页面图集数小于列表各卡片子项之和 | `cachedAlbumCount` 按 isAlbum 过滤漏数自定义下载目录型源 | [§29](#29-单项源直开与图集计数改为了项之和) |
| CLI/网页后台能开但漫画全空（已下载/图集/资源库都 0） | CLI 误接空壳 `headless_cli_server`，未走 `PicaKeepAdminServer` | [§31](#31-桌面服务端部署linux-headless--windows-托盘--两端-cli-wrapper--ci-打包) |
| Linux deb 装到 Debian 跑不起来 / GTK 窗口报错 | glibc 过新（ubuntu-24.04）或无 `$DISPLAY` 未经 xvfb | [§31](#31-桌面服务端部署linux-headless--windows-托盘--两端-cli-wrapper--ci-打包) |
| Windows `picakeep` 命令仍读不到资源，输出「运行模式: server-background」 | 装的是旧空壳 CLI（cmd 转调 native\bundle\exe），非 GUI exe --server wrapper | [§31](#31-桌面服务端部署linux-headless--windows-托盘--两端-cli-wrapper--ci-打包) |
| 中文 Windows 上 picakeep.ps1 语法崩（引号/中文行报错） | ps1 无 UTF-8 BOM，被 PowerShell 按 GBK 读 | [§31](#31-桌面服务端部署linux-headless--windows-托盘--两端-cli-wrapper--ci-打包) |
| headless 服务端网页「资源 not found」 | `web_asset_loader` 用 `Platform.script`（Flutter 二进制里无效） | [§31](#31-桌面服务端部署linux-headless--windows-托盘--两端-cli-wrapper--ci-打包) |
| deb 装到 NAS 后 `picakeep` 报 Permission denied / 服务端资源空 | 打包漏 `chmod +x` 或 `dpkg-deb --root-owner-group`，二进制无 x 位或属主非 root 普通用户读不到 | [§31](#31-桌面服务端部署linux-headless--windows-托盘--两端-cli-wrapper--ci-打包) |
| 退出阅读器后远程图片仍在后台传输，连接池被占 | `StreamImageProvider` 的 stream 无取消机制，reader dispose 未通知底层 abort | [§32](#32-退出阅读器停止图片加载与传输) |
| 全局搜索出现空白灰块（db 有记录但本地文件不在） | 搜索未读「显示数据库全部记录」开关，db-only 项无占位逻辑 | [§33](#33-全局搜索缺失项按开关占位或过滤) |
| Docker 重启后网页端配置全丢 | 配置落在容器临时路径，未设 `PICAKEEP_DATA_DIR` 指向持久的宿主导目录 | [§34](#34-服务端数据持久化与路径配置飞牛-docker) |
| 飞牛 Docker 收藏夹/图片收藏返回 500，重启后收藏丢失 | db root 被误绑到 `PICAKEEP_DATA_DIR`，真实 db 在库目录 pica 子文件夹 | [§35](#35-收藏历史-db-路径修复与网页配置入口) |
| 目录选择器存储空间显示「vol3」、点击停在 `/vol3` | 快捷项 label 与 jumpPath 未解耦，跳转未直达 `/volN/1000` | [§36](#36-linux-目录选择器存储空间快捷导航修复) |
| 网页保存配置后重启即丢（刷新不丢） | `_resolveEffectiveConfig` 用 appdata 空值覆盖文件配置值 | [§38](#38-服务端配置持久化修复网页配置重启丢失) |
| server_app.dart 近 3000 行难以通读修改 | 所有 admin 接口聚合为单一上帝类 | [§40](#40-拆分-server_appdart) |
| local_library.dart 4000+ 行难以定位 | `LocalLibraryManager` 3424 行 god class，全项目最大 | [§41](#41-拆分-local_librarydart-深度) |
| 远程连服务端收藏能显示但点不动 | `RemoteFavoriteItem` 未解析服务端下发的 `itemId` | [§43](#43-修复远程收藏点击不能跳转) |
| 安卓历史页→阅读→返回落到收藏页 | root/inner Navigator 混用，阅读压到 root 绕过 inner 栈 | [§44](#44-修复安卓历史返回错落收藏页) |
| Win GUI 配路径后开服务端扫不到资源 | 第一轮04服务端态不读appdata，GUI路径被忽略 | [§46](#46-客户端服务端配置共用ad-单向播种) |
| download_page.dart 3000+ 行难以定位 | `DownloadPageLogic` 1012 行 god class | [§47](#47-拆分-download_page逻辑层) |
| local_favorites/trash/scanner 大文件难以维护 | 三个 god class 合计 ~4300 行，改动时通读成本高 | [§48-§50](#48-拆分-localfavoritesdart) |
| 已下载详情弹层长标签挤没"阅读/查看详情"按钮，且标签未翻译 | `_translateDownloadedTag` 未拆 `:` namespace 前缀/剥性别符号；标签 `Container` 无宽度约束 | [§78](#78-已下载详情页标签翻译与长标签布局修复第八轮45号计划) |
| AI 对话聊到中途突然 400，新开会话恢复正常 | 一轮回复多个 `download_comic` 时 `_runLoop` 提前 `return`，后续 tool_call 永久缺失配对 `tool` 消息 | [§79](#79-ai对话多下载工具调用引发400修复第八轮46号修复) |
| 多选在线来源搜索时 AI 反而询问选哪个源；范围标签（来源/`#搜本地`）气泡下方不回显 | 提示词未说明"应分别调用"；`mergedRecognizedNames` 未并入 `selectedSources`/`localOnlyRequested` | [§80](#80-多源搜索分发提示词与标签气泡回显补全第八轮47号计划) |
| AI 回复出现空白气泡，或长期来源标签第二轮开始消失 | `_runLoop` 无条件写入空 content；回显只读本轮临时参数而未读最终生效范围 | [§81](#81-ai对话空回复与长期范围标签回显第九轮) |
| AI 结果清单变成近乎全宽的超长灰块，或“查看原始结果/查看全部”点击无反应 | LLM 工具参数字段类型漂移触发 `AiResultItem` 强转异常，release `ErrorWidget` 在无界 ListView 中膨胀；两个入口重复同一严格解析 | [§87](#87-ai结果清单参数边界与灰块入口修复第十一轮) |
| AI 仅本地请求仍带 `online_only:true`，或多来源搜索重新询问用户 | `turn_context` 没有实现 localOnly/onlineOnly 互斥；稳定规则没有迁入原“直接搜索、分别调用并合并”语义 | [§89](#89-第十二轮验收凭据脱敏与缓存前缀优化) |
| 连续分享日志时前一次分享文件失效 | 单文件脱敏导出共用临时目录，后一次导出会先删除目录 | [§89](#89-第十二轮验收凭据脱敏与缓存前缀优化) |
| 远程本地资源、状态、图片和事件通道一起超时，重启后恢复或代理切换后仍失败 | 网络栈代理策略分裂，长生命周期 HTTP/WS 运行态没有统一重建与 generation 隔离 | [§90](#90-第十二轮远程服务代理分流与连接态重建) |
| 设置已清除代理但远程请求仍沿用旧路径，或旧事件连接覆盖新连接 | 配置保存没有触发 transport rebuild；迟到 WebSocket Future/subscription 缺少 generation 所有权 | [§90](#90-第十二轮远程服务代理分流与连接态重建) |
| EH 续传进度从 0 缓慢爬升，已下载页仍发解密请求 | `currentPage` 被清零；文件存在判断位于 `getEhImageUrl` 之后 | [§82](#82-eh下载续传与并发提速第九轮) |
| 本地 JM 详情跳在线报 `Empty data`，或合法本地记录提示 ID 非法 | 历史记录可能缺 `comicId`；`LocalLibraryComicItem.id` 是内部 `itemId`，不能代替 `originalId` | [§83](#83-本地详情在线跳转-id-与-empty-data防御第九轮) |
| 本地详情更新信息后 ID/下载时间/章节被覆盖，或三源字段不显示 | 更新逻辑没有明确元数据/结构字段边界，详情直接依赖包装项而非 `sourceRowJson` | [§84](#84-本地详情在线信息更新与整体覆盖第九轮) |
| 本地详情把源站时间显示成下载时间，或作者/上传者位置错误 | 信息组按显示文案而不是原始语义角色拼装；EH uploader 被误当作者 | [§86](#86-本地详情布局eh入口与关于页第十轮) |
| EH 本地详情无法打开在线页，或更新信息请求失败 | `gid-token` 不是完整 URL；本地更新未复用 Content Warning 确认链路 | [§86](#86-本地详情布局eh入口与关于页第十轮) |
| 关于页版本与实际安装包不一致 | 页面仍使用硬编码 `V1.9.9`，没有读取 platform package info | [§86](#86-本地详情布局eh入口与关于页第十轮) |
| EH 本地记录显示为“其它/未知大小”，下载卡第三行标签顶掉作者 | 扫描层重复维护类型解析；字符串 size/旧 tags 结构不兼容；`Wrap` 没有可测量的行数契约 | [§85](#85-eh本地解析与标签显示闭环第十轮) |
| 会话里加了字段，内存/单测都对，但杀 App 重开该字段消失 | 只改了 `AiChatMessage.toJson/fromJson`，漏改 store 的 `_serializeAiChatMessage`/`deserializeAiChatMessage`（真正落盘路径） | [§97](#97-主线第十五轮续流式改造思考过程模型列表余额与搜图缺陷修复0607号计划) |
| 选图 + `#搜图` 后点发送，按钮直接灰掉，退出重进才看到提示 | OCR 拦截只看 `visionEnabled`，不区分「图给模型看」还是「图给工具用」 | [§97](#97-主线第十五轮续流式改造思考过程模型列表余额与搜图缺陷修复0607号计划) |
| 开视觉开关发过一次图后，此后每条纯文字消息都报 400 | 带图 user 消息的 `imagePaths` 进了 `_history`，`_buildRequestMessages` 每轮都把 `image_url` 展开回请求 | [§97](#97-主线第十五轮续流式改造思考过程模型列表余额与搜图缺陷修复0607号计划) |
| 同一条错误在聊天流气泡和输入框上方 banner 各显示一次，且 banner 不消失 | `displayMessages.add(AiChatMessage.error(...))` 与 `error = message` 同时设置 | [§97](#97-主线第十五轮续流式改造思考过程模型列表余额与搜图缺陷修复0607号计划) |
| 流式请求被自动重试成两份，或思考期长静默被 30s 掐断 | `RetryHttpClientAdapter` 重发整个 POST；`MyLogInterceptor` 把 null `receiveTimeout` 补成 30s | [§97](#97-主线第十五轮续流式改造思考过程模型列表余额与搜图缺陷修复0607号计划) |
| 余额区域一直空白，不报错也不显示金额 | 该 provider 的余额字段名/端点不匹配 → 归 `unsupported`（设计上静默） | [§97](#97-主线第十五轮续流式改造思考过程模型列表余额与搜图缺陷修复0607号计划) |

---

## §1 远程服务与连接池

> 精简版对应：[§1 远程服务与连接池](交接文档-精简最新版.md#1-远程服务与连接池)

### 现象

- 只要滚动经过"服务端实际不存在内容"的漫画/封面，之后**全部远程档**（已下载/图集/收藏/图片收藏）都无法载图。
- 离开页面、跨标签都恢复不了，**唯有热重启（hot restart）/重开 App 才好**。
- logcat 一片 `getUrl` 的 `TimeoutException 0:00:05`，**没有任何 404**（说明不是服务端拒绝，是客户端拿不到连接）。

### 根因与诊断路径

**决定性证据**：热重启重建整个 Dart isolate → `RemoteLibraryClient` 单例 + `HttpClient` 连接池全部清零 → 恢复。离开页面只销毁 widget，单例状态还在 → 不恢复。所以坏的是**单例持有的连接池**。

**两个泄漏源：**

1. **孤儿连接**（最关键）
   - `getUrl(...).timeout(5s)` / `openUrl(...).timeout(5s)` 超时时只丢弃前端 Future（`rethrow`），但底层那条"开连接"的 `Future<HttpClientRequest>` **还在跑**。
   - 它稍后成功返回一个 `HttpClientRequest`，但 catch 已退出、`request` 局部变量是 null → **没人 close/abort 它** → 这条连接被永久钉在 keep-alive 池里。
   - 每超时一次，可用连接 -1，累积到 `maxConnectionsPerHost`（原 12）就全局永久卡死。

2. **404 不 drain**
   - 非 2xx（如缺失封面 404）原来直接 `request.abort()` 就抛异常。但 `close()` 拿到 response 后 socket 归 response 所有，abort 在该状态下不可靠。
   - 每个缺失封面漏一条连接，滚过 >12 个（原池大小）就耗尽。

**触发场景**：滚动"没内容的漫画"→ 缺失图片让服务端响应慢 → 连接被多占几秒 → oversubscribe → 产生第一个孤儿 → 雪球。

**为什么退出页面再进还是卡**：`HttpClient` 在 `RemoteLibraryClient` 单例里，widget 销毁不影响它。

### 修法（全部已落地）

1. **孤儿回收 `_openWithTimeout(connecting, timeout)`**
   - 超时后给原 `connecting` future 挂 `.then()`，等它最终返回 request 时 `abort()`，把连接还回池子。
   - `loadImageWithProgress` 的 `getUrl` 和 `_sendRequest` 的 `openUrl` 都改走它。**这是关键修复。**

2. **404 drain 代替裸 abort**
   - 非 2xx 路径改为 `await response.drain<void>().timeout(5s)` 把响应体读空，连接干净归还；drain 自身超时才回退 abort。

3. **响应体保护 `_guardedImageBody`**
   - body 套 15s idle timeout（超时 abort + 抛错）。
   - `finally` 里对"未正常读完"（cancel/error）的情况 abort，正常读完则不动（保留 keep-alive 复用）。
   - 旧代码把裸 `response` 直接当 stream 返回——body 卡住或被丢弃时 socket 永远 pinned。

4. **并发闸 `_ImageConcurrencyLimiter`**（FIFO 信号量）
   - `loadImageWithProgress` 拿连接前先拿令牌。
   - 两个独立预算：**阅读闸** `settings[114]`（reader 大图，默认 6）/ **浏览闸** `settings[115]`（封面缩略图，默认 8）。
   - 设置入口：设置-阅读 / 设置-浏览，范围 1~12，实时生效。
   - 令牌释放：早退路径（404/超时/socket/其它异常）就地 `releasePermit()`；正常路径交给 `_guardedImageBody` 的 `finally`。

5. **池扩容**
   - `_httpClient.maxConnectionsPerHost` 从 12 提到 **24**（≥ 阅读闸 12 + 浏览闸 12 两上限之和）。
   - 保证正常并发下 `getUrl` 永不排队、永不超时、压根不产生孤儿。

6. **双池分流**
   - `_httpClient`（bulk，24 连接）：图片 bulk 池，给 reader 大图和封面。
   - `_controlClient`（control，6 连接）：控制面池，只给 JSON 请求用（封面已搬走）。
   - 封面必须走 bulk 池：封面网格和 JSON 列表/`/status` 同屏，封面占 control 池会把列表/探测饿死。

7. **`_trimToLimit` 节流**
   - 每张封面下载后不再立即全盘扫描。改为 30s 节流 + 不重入（`_trimInFlight` / `_lastTrimAt`）。

### ❌ 不要再走的路

- **不要**对 `getUrl()` / `openUrl()` 直接 `.timeout()` 就完事。**这是孤儿连接的来源**——超时只丢弃前端 future，底层请求还在跑。**必须用 `_openWithTimeout`**：超时后挂 `then` 回收。
- **不要**在 timeout 路径里只 catch 不 abort。Dart `HttpClient` 的 socket 不会自动释放。
- **不要**只给建连/收 header 包 `.timeout()` 就以为"请求有超时保护"。**响应体（body）流必须单独保护**：套 idle timeout，未读完时 `abort()`。裸 `await for(response)` 是这套 codebase 最隐蔽的池泄漏来源。
- **不要**让封面下载走 `_controlClient`（`lightweight: true`）。封面网格和 JSON 列表同屏，封面占 control 池会把列表饿死。
- **不要**新增 `HttpClient` 实例——应统一走 `_httpClient` / `_controlClient` 两个池。
- **不要**在"每张封面下载后"无脑跑全盘扫描类清理。网格批量加载必须节流 + 不重入。
- **不要**让 `maxConnectionsPerHost` 小于"所有并发闸上限之和"。改并发闸上限时记得同步检查。
- **不要**忘了释放并发令牌——每条早退路径都要 `releasePermit()`，漏一条就泄漏一个槽位，最终耗尽闸门变假死。
- **不要**被症状牵着走"一个个补丁"。90% 是连接池/导航栈/编码这种基础设施级根因，每发现新症状先问"是不是池子又满了？"。

### 涉及文件

- `lib/foundation/remote_library_data_source.dart` — `RemoteLibraryClient`（双池）、`_ImageConcurrencyLimiter`、`_openWithTimeout`、`_guardedImageBody`、`_RemoteLibraryCoverDiskCache`
- `lib/foundation/local_library_settings.dart` — 设置位 114/115 常量 + normalize
- `lib/base.dart` — 默认设置追加 `'6'/'8'`
- `lib/pages/settings/explore_settings.dart`、`lib/pages/settings/reading_settings.dart` — 并发数设置 UI
- `assets/translation.json` — 并发设置翻译

### 验证方法

1. 滚动服务端没内容的漫画 → 其它正常漫画仍能载图（不再需要热重启）
2. reader 连翻几十页不再刷 5s TimeoutException
3. 热重启后立刻恢复的行为不再出现（说明池不再被毒化）

---

## §2 收藏/图片收藏远程档

> 精简版对应：[§2 收藏/图片收藏远程档](交接文档-精简最新版.md#2-收藏图片收藏远程档)

### 现象与根因清单

| # | 现象 | 根因 | 修法 |
|---|------|------|------|
| R1 | 客户端模式但远程档显示"不可用" | `_checkRemoteAvailability` 用了 `RuntimeServiceDataSourceResolver.current()`，服务端模式返回 LocalRuntime 直接 false | 改用 `RemoteRuntimeServiceDataSource().fetchSnapshot()` |
| R2 | 服务端写收藏后所有后续 `/api/library/items` 响应卡死 | `_notifyLibraryChanged()` 调了 `rescanResources()` 全库重扫 | 改为空操作 |
| R3 | 远程失败时档位开关消失 | `_showSourceSelector` 把守开关显示 | 改为无条件显示 |
| G1 | 远程封面 404 风暴，请求刷屏 | tile 无 `errorBuilder` + provider 无负缓存 → 每次 rebuild 反复请求注定失败的 url，占满连接池 | `errorBuilder` 静默占位 + `_failureUntil` 负缓存（404 冷却 10min，瞬时错误 20s） |
| G2 | 图片收藏点击"找不到" | `image.id`（漫画来源 ID）≠ 服务端资源 `remoteId`（目录派生） | 候选扩到 `image.title` + 前缀变体 fallback |
| N1 | 收藏页返回跳到"我"页 | `App.globalTo` push 到根 navigator | 全部改 `App.pushInner`（图片收藏 ×2、收藏 ×1） |
| N2 | 点击远程收藏项崩溃 | `_RemoteFavoriteTile._open` 用 `App.globalContext` 调 `ScaffoldMessenger.of`，根 context 没 Scaffold 后代 | 改传 tile 自己的 BuildContext |
| C1 | 点击远程收藏项没反应（无报错） | `_onTap` 缺 try/catch，`findItemByCandidates` timeout → unhandled exception 被静默 | 加 try/catch + SnackBar |
| S1 | 服务端中文文件夹（如"中间档测试"）500 | `Uri.decodeComponent(segments[i])` + `pathSegments` 已自动解码 = 双重解码 | 5 处全删，直接用 `segments[i]` |
| S2 | 服务端 favorites handler 慢/卡 | 每次请求 `await LocalFavoritesManager().init()` 重开 sqlite db、dispose 旧句柄 | 删除 init 调用 |
| S3 | 服务端 HttpServer 单池被图片洪流饿死 | 以前只有一个 `_httpClient`，reader 看一本漫画 45-129 张图占满 | 拆 `_httpClient`(bulk) vs `_controlClient`(control) |

### 诊断经验

**判断是不是池子问题的铁证**：logcat 全是 `getUrl` 的 5s TimeoutException、没有 404、且热重启后立刻恢复。

**判断是不是导航栈问题的铁证**：系统返回后跳到"我"页而不是上一个页面 → 检查是不是用了 `App.globalTo`。

**判断是不是编码问题的铁证**：只有中文路径报 500、英文正常 → 检查 `Uri.decodeComponent`。

### ❌ 不要再走的路

- **不要**给 `_checkRemoteAvailability` 加缓存"掩盖"问题。它失败的真正原因 99% 是连接池被拖垮，不是探测本身慢。
- **不要**把 `Uri.decodeComponent(segments[i])` 加回去。`pathSegments` 已自动解码。
- **不要**在服务端 favorites handler 里 `await init()`。
- **不要**用 `App.globalTo` 打开阅读器。永远用 `App.pushInner`。
- **不要**用 `App.globalContext` 调 `ScaffoldMessenger.of`。用页面/tile 自己的 BuildContext。
- **不要**把图片收藏远程档点击候选缩成只有 `image.id`，要带 `title` + 前缀变体 fallback。

### 当前仍存在的问题

1. **首次点击 `findItemByCandidates(fetchDetail: true)` 可能慢**：`fetchItems()` 拉全库 + `fetchItemDetail()` 单项详情，两次串行。治本需要服务端在 `image-favorites` payload 直接回传 `itemId`（计划过，未做，当前 title fallback 覆盖大部分）。
2. **`fetchFavoritesInFolder` 串行循环**：18 个文件夹 18 次串行请求。可改 `Future.wait` 并行（控并发 4-6），当前未改。

### 涉及文件

- `lib/foundation/remote_library_data_source.dart` — 双池、封面缓存、负缓存
- `lib/pages/image_favorites.dart` — 远程图片收藏 tile、`_onTap`、`_checkRemoteAvailability`
- `lib/pages/favorites/main_favorites_page.dart` — 远程收藏夹、`_RemoteFavoriteTile._open`
- `lib/server/server_app.dart` — 服务端 handler（5 处 decode + favorites init + notify）

---

## §3 加密压缩包

> 精简版对应：[§3 加密压缩包](交接文档-精简最新版.md#3-加密压缩包)

### 子问题 1：ZIP 内部章节名乱码（§28）

- **现象**：外层漫画标题正常，但 ZIP/CBZ 内部章节名/分组名乱码。
- **根因**：`ArchiveEntry.path` 来自 central directory 的原始 filename bytes。上游库可能已经错解过；`UTF-8 decode` 成功 ≠ 结果正确（例如 GBK 字节流被当 UTF-8 解不会抛异常但结果是乱码）。
- **修法**：
  - 若 ZIP 显式带 UTF-8 flag → UTF-8
  - 未标记时先保护纯 ASCII 路径
  - 对 `utf8 / shiftJis / gbk` 候选结果做启发式比较，不回退到"utf8 不抛异常就算对"
- **诊断原则**：外层标题正常、内部章节乱码 → 追 `central directory raw bytes → ArchiveEntry.path → archive chapter realNames → display helper` 这条链。不要先归因到 UI 拼接或缓存。

### 子问题 2：加密 ZIP 假解锁（§30）

- **现象**：1.7 GB 加密 ZIP 输任意非空密码都"解锁成功"，但封面破图、阅读器每页 `Invalid image data`。
- **根因**（三层，互相掩盖）：
  1. `tryUnlock` 只调 `backend.openIndex(...)` 以"能列目录"作为成功条件。ZIP 中央目录**从来不加密**、列目录不需要密码，所以任何密码都判对。
  2. `readEntry` 解密失败时 catch 落到 fallback，fallback 直接对加密字节做 `RawZLibFilter` inflate → 乱码字节喂给 `ImmutableBuffer` → `Invalid image data`。
  3. 中央目录解析不识别 WinZip AES extra field `0x9901`，加密 entry 的真实压缩方法从未被读出。
- **修法**：彻底绕开 archive 库的 `ZipDecoder`，自己实现加密 entry 解密链：
  - `_parseCentralDirectoryWithOffsets` 追加识别 `0x9901` → `isAesEncrypted` / `aesStrength` / 内层真实 `compressionMethod`
  - `readEntry` 主路径重写：加密 entry 走 `_readAesEntry` 或 `_readZipCryptoEntry`，非加密走 `_readEntryFallback`
  - `_readAesEntry`：读 local file header → 切 salt/pwdVerify/cipher/file mac → `_pbkdf2HmacSha1` 派生 key → 密码两字节校验失败抛 `wrongPassword` → HMAC 校验 file mac → `_aesCtrXorZip` XOR 解密 → 内层 inflate
  - `_readZipCryptoEntry`：3 个 32-bit 密钥用密码字节初始化，CRC32 每字节滚动，12 字节 header 校验
  - `_readEntryFallback` 对 `match.isEncrypted` 直接抛 `wrongPassword`，杜绝加密字节走裸 inflate
- **`tryUnlock` 真实验密码**：`openIndex` 后挑一个最小加密 entry 实际 `readEntry`，成功才 `setSessionPassword`。

### 子问题 3：AES-ZIP inflate 报错（§32）

- **现象**：修复假解锁后，AE-1 + AES-256 + deflate（如 `114514.zip`）报"压缩包已损坏"，而 AE-2 + ZipCrypto 包正常。
- **根因**：`RawZLibFilter.inflateFilter()` 默认 `windowBits=15` 期待 zlib header，但 ZIP entry 压缩数据是**裸 deflate 流**（无 header），对应 `windowBits=-15`。
- **修法**：两处 `inflateFilter()` 均改为 `windowBits: -15`：
  - `_inflateRaw`（AES / ZipCrypto 解密后调用）
  - `_readEntryFallback` 内 method=8 分支

### 与 archive 库的兼容性

- archive 3.6.1：`ZipFile` 构造硬调 UTF-8 解码 local file header filename → 日文 ZIP 抛 `FormatException`。
- archive 4.0.9：修了 filename 但 zip64 extra field 的 dataSize 未被消费 → 本仓库 zip64 包撞 `RangeError`。
- **两版库都不能透明使用**。当前方案：`pointycastle` 直接依赖 + 自实现解密链，源码不 `import package:archive`。

### ❌ 不要再走的路

- **不要**只列目录就判断"解锁成功"。ZIP 中央目录不加密。必须实际读一个加密 entry。
- **不要**对加密字节走裸 inflate。`_readEntryFallback` 入口遇到 `isEncrypted` 直接抛 `wrongPassword`。
- **不要**换 archive 库版本就以为能透明解决。3.6.1 和 4.0.9 各有不兼容问题。

### 涉及文件

- `lib/foundation/archive/backends/dart_zip_backend.dart` — 加密解密链、编码修复、inflate windowBits
- `lib/foundation/archive/archive_reading_service.dart` — `tryUnlock` 真实验密码
- `lib/foundation/archive/archive_models.dart` — `ArchiveEntry` 新增 `isAesEncrypted`
- `lib/foundation/local_library.dart` — 加密包封面刷新
- `lib/pages/download_page.dart` — "忘记密码"功能
- `lib/pages/settings/archive_settings_page.dart` — 密码管理 UI 重构
- `pubspec.yaml` — 引入 `pointycastle: ^3.9.1`

---

## §4 服务端模式

> 精简版对应：[§4 服务端模式](交接文档-精简最新版.md#4-服务端模式)

### 关键修复清单

| 问题 | 根因 | 修法 |
|------|------|------|
| 中文路径 500 | `Uri.decodeComponent(segments[i])` 二次解码 | 5 处全删，直接用 `segments[i]` |
| 写收藏后请求卡死 | `_notifyLibraryChanged()` 调 `rescanResources()` 全库重扫 | 改为空操作 |
| favorites handler 慢 | 每次请求 `await LocalFavoritesManager().init()` 重开 db | 删除 init（单例 startup 已 init） |
| 切换挡位需重启服务 | `refreshResourceState()` 只 rescan 不推 config | 改为先推 effective config → 热更新或重启 |
| 远程资源变更客户端不感知 | 无推送机制 | `librarySignature` + WebSocket `LibraryEventBus` |
| 回收站残留旧记录 | `library_trash.json` 与 HTTP 内存缓存分裂 | 统一走 `LocalServerRuntime` 运行态链路 |
| 封面显示首张内容图而非 cover.jpg | 扫描器优先用章节首图当封面 | 优先显式封面文件（`cover.*`） |
| 图片收藏网格列数漂移 | `settings[74]` 存值可能漂移 | 未根除，仅记录（做时勿破坏电脑端自适应） |
| 局域网发现 mDNS 名不副实 | 旧实现实际是逐 IP 探测 `/status` | 拆成 mDNS / Bonjour 与网段扫描两档，旧扫描保留为独立挡位与兜底 |
| Android mDNS 绑定报 `reusePort not supported` | Android/Linux 部分 Dart socket 不支持 `reusePort` | Android/Windows 均不启用 `reusePort`，mDNS 失败时可按开关兜底网段扫描 |
| Windows 服务端被 Linux/Android 漏扫 | `_sendResponse()` 始终发组播 `224.0.0.251`，Windows 多网卡时内核选错出口接口 | 已修复：收到查询后单播回复源地址（`datagram.address`）；主动广播仍走组播。排查顺序：① 确认查询回复走单播 ② 多网卡/防火墙 |
| 网段扫描曾只显示当前已连接服务 | 旧实现 `scan()` 先探 `explicitHosts`，命中后 `break`，后续网段分片未继续扫描 | 已删除命中后提前停止；显式“网段扫描”和 mDNS 兜底都扫完当前候选网段并聚合多个 `/status` 服务 |
| 服务信息页空闲仍持续产帧 / 有请求时页面闪“刷新中” | 隐藏 `CircularProgressIndicator` 仍在树里 + `serviceStatsVersion` 走完整 `_reloadSnapshot()` | 空闲不构建加载圈；统计变化走静默快照刷新，未变化不 `setState` |

### 局域网发现边界

- `mDNS / Bonjour`：服务端启动后发布 `_picakeep._tcp.local`，客户端查询该服务类型（query 带 QU 位请求单播回复），拿到 host/port 后仍请求 `/status` 校验。**服务端收到查询后单播回复到源地址**（绕过 Windows 多网卡组播路由问题）；主动广播（初始/定时）仍发 `224.0.0.251` 组播。mDNS 结果按 `地址:端口` 去重；同设备不同端口视为不同服务，保留为独立候选。
- `网段扫描`：保留旧 `/status` 探测行为，按已配置地址端口和当前私网网段逐个探测；不删除、不弱化，只是不再冒充 mDNS。当前显式“网段扫描”和 mDNS 兜底扫描都会扫完当前候选目标集合，聚合多个有效 `/status` 服务；当前已连接地址仍优先探测，但命中后不能阻止后续地址继续扫描。
- `mDNS 失败自动网段扫描`：独立设置位，默认开启；仅当主挡位为 `mDNS / Bonjour` 且 mDNS 无结果或 socket 失败时，本次发现临时调用原 `scan()`。主挡位为 `网段扫描` 时只执行旧扫描逻辑。
- 服务信息页与 APP能力页共用 `ServiceDiscoveryStrategySettings`，两处显示和保存的是同一套策略。
- 服务信息页最终候选数为 1 时直接应用；候选数 2+ 时弹选择窗口。窗口展示来源模式、mDNS instance/host/TXT 服务名、地址端口、漫画数量、延迟和 `/status` 描述；如果 mDNS 兜底到了网段扫描，窗口顶部必须显示兜底提示。
- 多设备环境没有弹选择窗口时，优先检查最终候选是否被同 `地址:端口` 去重、是否只有一个服务通过 `/status`、或是否出现“扫描命中后提前停止”的回归；不要先改 mDNS 发布协议。
- Android 上 mDNS 需要 `CHANGE_WIFI_MULTICAST_STATE` 和 multicast lock；没有组播能力的路由器/访客网络应切回“网段扫描”。
- Android/Windows 的 UDP 5353 绑定不要启用 `reusePort`；Android 真机出现 `Dart Socket ERROR ... reusePort not supported on this platform` 时，说明已进入 mDNS 绑定路径，优先检查这里而不是回到旧网段扫描逻辑。

### 服务信息页跳动边界

- 服务信息页空闲跳动优先查页面自身：按钮里的隐藏 `CircularProgressIndicator` 即使透明也可能继续驱动 ticker，空闲态应只保留固定尺寸占位，不构建运行中的加载圈。
- `App.serviceStatsVersion` 会在服务端请求 begin/end 时变化。它只代表统计值变化，不应触发完整 `_reloadSnapshot()` 和 `_loading=true`，否则页面会在“在线”和“刷新中”之间来回闪。
- 统计通知走静默刷新：读取新快照、对比统计相关摘要，确有变化才更新 `_snapshot`；无变化不 `setState`。手动刷新、配置变化、运行态变化仍保留完整刷新和明确 loading 反馈。
- 这个问题与 mDNS / 网段扫描模式没有直接关系。发现按钮的加载反馈只跟 `_discovering` 相关，不应为了治理服务信息页跳动改发现策略。

### 架构约束

- **两套底层 store + 服务端聚合视图**：`LibraryTrashStore` + `LocalTrashStore`，兼容已有数据和 `download.db` 恢复能力。
- **WebSocket 接线**：前后台生命周期、指数退避重连、25s 心跳、30s 陈旧超时、`/status` 降级探测（8s 节流）。
- **阅读期延迟刷新**：收到变更先缓存 `pendingSignature`，退出阅读再统一刷新。

### ❌ 不要再走的路

- **不要**在服务端 `_notifyLibraryChanged()` 里调 `rescanResources()`。
- **不要**在服务端 favorites handler 里 `await init()`。
- **不要**把 `Uri.decodeComponent(segments[i])` 加回去。
- **不要**把 Windows mDNS 漏发现直接改成“用网段扫描代替 mDNS”。正确做法：保留真实 `mDNS / Bonjour`，排查顺序 ① `_sendResponse` 查询回复是否单播到源地址 ② 多网卡组播加入 ③ A 记录地址过滤 ④ UDP 5353 防火墙。
- **不要**在网段扫描命中当前已连接服务后 `break`。显式“网段扫描”需要聚合同一候选网段中的多个 `/status` 服务，当前已连接地址只能优先探测，不能终止后续扫描。

### 涉及文件

- `lib/server/server_app.dart` — HTTP 路由、handler、decode 修复
- `lib/server/local_server_runtime.dart` — 服务生命周期、热更新、回收站统一入口、mDNS 广播生命周期
- `lib/foundation/picakeep_mdns.dart` — mDNS / Bonjour 发布与发现协议（查询回复单播到源地址、client query 带 QU 位）
- `lib/foundation/service_data_source.dart` — mDNS 发现、网段扫描和 `/status` 校验
- `lib/server/library_event_bus.dart` — 资源变更事件总线
- `lib/server/local_resource_scanner.dart` — 资源扫描、封面优先、pageSizes
- `lib/foundation/remote_library_data_source.dart` — 客户端远程数据源
- `lib/foundation/remote_library_event_channel.dart` — WebSocket 事件通道

---

## §5 动态取色与设置交互

> 精简版对应：[§5 动态取色与设置交互](交接文档-精简最新版.md#5-动态取色与设置交互)

### 子问题 1：动态取色致 ANR（§36.4 / §34）

- **现象**：logcat `dynamicColors.start/done` 每 ~6s 刷一次，持续给主线程加压，偶发 `picakeep 没有响应`。
- **根因**：MIUI 每 ~6s 发一次 `onConfigurationChanged`。原生 `MainActivity.onConfigurationChanged` 节流只有 1000ms，6s > 1s 每次都穿过 → 转发 `dynamic_theme` `changed` → Dart `getCorePalette()` + 双 `toColorScheme()` + `setState` 全量重建。
- **修法**：`MainActivity.onConfigurationChanged` 改为**仅当 uiMode 夜间标志真正翻转才转发**。壁纸调色板不走这个回调，首次取色由 initState 独立触发。删掉失效的 `DYNAMIC_THEME_NOTIFY_MIN_INTERVAL_MS` 与 `lastDynamicThemeNotifyAt`。
- **验证**：正常使用时控制台不再每 6s 刷 `dynamicColors`，只在手动切系统亮/暗时刷一次。

### 子问题 2：设置滑块不跟手（§36.4）

- **现象**：设置里滑块圆点不跟手，要等别处触发整页 rebuild 才跳到新值。
- **根因**：`buildExploreSettings` 无状态顶层函数 + `ReadingSettings` 是 `StatelessWidget`，内联 `Slider.onChanged` 只写 `appdata.settings` 不 `setState`。
- **修法**：三个滑块各封装成小 StatefulWidget：
  - `_ComicTileSizeSlider`（浏览-漫画块大小）
  - `_ImageFavoriteSizeSlider`（浏览-图片收藏大小）
  - `_TapTurnRangeSlider`（阅读-点按翻页识别范围）

### 涉及文件

- `android/app/src/main/kotlin/lingxue/picakeep/MainActivity.kt` — onConfigurationChanged 节流
- `lib/pages/settings/explore_settings.dart` — 滑块 StatefulWidget 封装
- `lib/pages/settings/reading_settings.dart` — 同上

---

## §6 Android 启动与系统 UI

> 精简版对应：[§6 Android 启动与系统 UI](交接文档-精简最新版.md#6-android-启动与系统-ui)

### 子问题 1：阅读器顶部黑条（§33.1）

- **现象**：从特定入口（如 LauncherBlue alias）启动应用后，阅读器顶部有黑条。
- **根因**：logcat 出现 `D/VRI[LauncherBlue]` — launcher 入口是 `activity-alias` 而非 `MainActivity` 本身。Flutter embedding v2 靠 `<meta-data android:name="io.flutter.embedding.android.NormalTheme">` 切换 NormalTheme，但这只挂在 `<activity>` 上，没复制到各 alias。alias 启动时窗口停留在 LaunchTheme，无 `windowLayoutInDisplayCutoutMode=shortEdges` 也无 `statusBarColor=transparent`。
- **修法**：给全部 13 个 `activity-alias` 补上：
  ```xml
  android:theme="@style/LaunchTheme"
  ...
  <meta-data
    android:name="io.flutter.embedding.android.NormalTheme"
    android:resource="@style/NormalTheme"/>
  ```
- **⚠️ 后续新增 alias 必须同样带上这两项。**

### 子问题 2：状态栏动效（§33.2）

- **现象**：隐藏顶栏时状态栏瞬间消失（无淡出动画）。
- **根因**：V1.8 把 `_hideReaderSystemUi` 从 `SystemUiMode.immersive` 改成了 `immersiveSticky`。前者走 Android 标准淡出动画（~300ms），后者 hard-switch 瞬间消失。
- **修法**：改回 `SystemUiMode.immersive`。
- **副作用**：`immersive` 下用户从屏幕顶端下拉时状态栏会停留几秒才自动收回（`immersiveSticky` 会在失去交互后立即藏回）。为动效体验接受此副作用。

### 子问题 3：启动页相关（§34.2）

- **深色模式跟随应用内设置**：Android 12+ 启动画面由 `system_server` 在进程启动前绘制。改用 `UiModeManager.setApplicationNightMode()`（在 `onCreate` 里 `super.onCreate` 之后调用）。删除原 `attachBaseContext` 的 `createConfigurationContext + applyOverrideConfiguration`（会在 Activity 不重建时残留锁定）。
- **双桌面图标修复**：删除 `MainActivity` 的 `MAIN/LAUNCHER` intent-filter，入口全交给 `<activity-alias>`。曾尝试 debug overlay 但导致 debug 包双图标，已删除。
- **全 vector 启动图标**：用 potrace 矢量化 → `launch_icon_full.xml` + `launch_splash_icon.xml`（0.6 缩放避系统裁剪）。

### 涉及文件

- `android/app/src/main/AndroidManifest.xml` — alias NormalTheme、MainActivity launcher 移除
- `lib/pages/reader/comic_reading_page.dart` — immersive 改回
- `android/app/src/main/kotlin/lingxue/picakeep/MainActivity.kt` — onCreate night mode
- `android/app/src/main/res/drawable/launch_icon_full.xml`、`launch_splash_icon.xml`
- `lib/tools/app_icon_channel.dart` — 图标切换通道
- `lib/tools/night_mode_channel.dart` — 夜间模式通道

---

## §7 本地回收站与删除链路

> 精简版对应：[§7 本地回收站与删除链路](交接文档-精简最新版.md#7-本地回收站与删除链路)

### 核心链路

```
删除 → TrashManager 移入回收站 → 真删源 download.db 行 + 保存 sourceDbPath/sourceDbId/sourceDbRowId/sourceDbTimeMillis
恢复 → 优先按原 rowid 插回 download.db（被占用则普通插入保留原 time）
彻底删除 → 物理删除文件 + 清理回收站记录
```

### 关键修复

| 问题 | 根因 | 修法 |
|------|------|------|
| 删除后列表残留 | 移入回收站时未删源 download.db 行 | 移入时真删，保存来源信息供恢复 |
| 恢复后只剩一条 | 受限 download.db 读不到被误判"不存在"→ 误建空库覆盖 | 先区分"不可读"和"真的不存在" |
| 跨应用 managed 资源删除抛 PathNotFoundException | `DownloadManager.init()`/`createSync()` 误触他人沙箱包根 | 识别"他人应用沙箱"路径 → 强制走 Shizuku/Root 通道 |
| 删除时长时间转圈 | 预先递归统计整个目录大小 | 改为优先复用现有条目大小；引入 pending 中间状态 |
| 删除后 reload 失败弹窗 | reload 异常被当删除失败 | reload 失败只记日志，不覆盖删除结果 |
| 受限目录权限不足静默失败 | 旧逻辑允许"静默移入回收站/从列表移除" | 权限不足直接提示，不假装成功 |
| Shizuku UserService 偶发 DeadObjectException | 大 download.db 读取超时/断开 | 当前回退/跳过，不阻塞删除成功路径 |
| 图集回收站封面残留 | 移入时未清理封面缓存 | 记录封面元数据，恢复后修正 |

### Shizuku 本地已下载补充（§12）

- **现象**：Shizuku 下本地已下载扫描返回 0、或"我"页下载数量统计失败。
- **根因**：多个辅助入口在 Shizuku + 受限目录时仍偷偷调 `DownloadManager().init()` → 对受限路径做 `Directory.create()`/`sqlite3.open()` → `PathNotFoundException`。
- **修法**：新增 `LocalLibraryManager.shouldBypassDirectDownloadManagerForCurrentDownloads()` → 在 `managedDataSourceModeCurrentOnly` 且命中 Shizuku fallback 时统一绕开 `DownloadManager`，改走 `LocalLibraryManager` 受限目录读取链路。

### ❌ 不要再走的路

- **不要**在受限目录场景调 `DownloadManager().init()`。
- **不要**把 `dart:io` 读不到受限 `download.db` 误判成"源库不存在"然后建新空库。
- **不要**在删除前对整个目录做递归体积统计。
- **不要**对受限目录静默"移入回收站/从列表移除"假装成功。

### 涉及文件

- `lib/foundation/trash.dart` — 删除/恢复/彻底删除统一入口
- `lib/foundation/local_trash_store.dart` — 回收站持久化
- `lib/foundation/local_library.dart` — 扫描、Shizuku 绕行
- `lib/foundation/download.dart` — DownloadManager
- `lib/pages/trash_page.dart` — 回收站 UI、进度遮罩
- `lib/pages/download_page.dart` — 删除进度遮罩
- `lib/pages/settings/app_settings.dart` — 挡位切换 OverlayEntry 提示

---

## §8 已下载页三档隔离

> 精简版对应：[§8 已下载页三档隔离](交接文档-精简最新版.md#8-已下载页三档隔离)

### 三档架构

```
已下载页
├── 本地档：DownloadManager 本地扫描 → 8s 超时
├── 聚合档：本地 + 远程合并（单侧失败不拖垮另一侧）
└── 远程·已下载：远程 HTTP 快照 → 10s 超时
```

### 关键修复

| 问题 | 根因 | 修法 |
|------|------|------|
| 远程已下载被本地扫描失败拖住 | 三档共用加载入口，远程前先执行 `DownloadManager().init()` | 拆分为独立分支 |
| 一直转圈不超时 | 无页面级超时 | 本地 8s、远程 10s 超时 → 可重试空态 |
| 远程已下载把整根目录识别为一个"多章节大项" | 扫描逻辑错误 | 优先扫描子目录漫画列表 |
| Root/Shizuku 列表慢 | 首屏同步全量扫描 + 封面解析 | 改用 `download.db` 快速构建，封面/索引后台低速补齐 |
| 远程已下载之前正常、后来卡住 | `/api/library/items` 无过滤返回 current/original/custom 全量资源；服务端资源根/图集规模变大后，Flutter 客户端在 UI isolate 接收大 body、jsonDecode、map 对象再本地过滤 | 服务端 `GET /api/library/items` 增加兼容参数 `managedOnly=1`/`rootId=`；客户端远程已下载走 `fetchManagedDownloadItems()`，root 页走 root 过滤；过滤快照不污染全量 `_snapshotCache` |
| 远程已下载卡住（二次，ANR 三连）| 根因 1：`jsonDecode(9.8MB)` 在 UI isolate 同步执行 1228ms；根因 2：`_prepareTileViewModels` 对 4075 remote item 做全量本地历史/收藏 DB 查询 85303ms；根因 3：`_downloadedItemTags` fallback 调 `item.toJson()` 序列化全部 episodes/pages URL 再阻塞 5s | ① `jsonDecode` 改 `compute(_decodeJsonBody, bodyText)`（顶层函数）；② `_prepareTileViewModels` 远程档直接读 `item.subTitle/tags/sourceDisplayName/comicSize` getter；③ history/favorite 批查询排除 `RemoteLibraryComicItem` |
| 远程封面回滑变慢/消失 | provider key 不稳定（StreamImageProvider vs FileImage 换 key） | 统一为 `_RemoteLibraryCoverImageProvider` + `cacheWidth` 优化 |
| 已下载详情弹层标签多时挤压按钮 | 固定高度 | 改为自动高度 + 可滚动 |

### 封面缓存稳定性（§21）

- **根因**：第一次走 `StreamImageProvider`，磁盘缓存存在后切 `FileImage` → Flutter 图片缓存 key 不同 → 回滑时即使文件已落盘也不命中同一份内存缓存。
- **修法**：统一为稳定 key 的 `_RemoteLibraryCoverImageProvider`，优先读磁盘缓存；已下载页远程封面预热前 12 个；移动端 `cacheExtent` 提升到 1.0 屏。

### 涉及文件

- `lib/pages/download_page.dart` — 三档切换、超时、封面预热
- `lib/foundation/download.dart` — DownloadManager
- `lib/foundation/download_model.dart` — DownloadedItem 模型
- `lib/foundation/remote_library_data_source.dart` — 远程数据源/封面 provider；已下载远程档必须使用过滤请求，过滤快照不可写入全量缓存
- `lib/server/server_app_library.dart` — `/api/library/items` 全量/managedOnly/rootId 三种兼容响应
- `lib/components/comic_tile.dart` — 封面解码优化

---

## §9 工具页外显按钮

> 精简版对应：[§9 工具页外显按钮](交接文档-精简最新版.md#9-工具页外显按钮)

### 设计

- **共享配置模型**（`lib/pages/tool_display_config.dart`）：统一维护工具定义、默认显示项、排序归一化、设置读写。
- **"我"页**：一体式卡片，底部外显按钮改为卡片内横向滚动条 + 右侧渐变遮罩；长按进入排序模式。
- **"工具"页**：`StatefulWidget`，支持普通模式与 `外显按钮与排序` 模式；点按切换显隐，长按 ~800ms 拖拽排序。

### 涉及文件

- `lib/pages/tool_display_config.dart`
- `lib/pages/me_page.dart`
- `lib/pages/tools.dart`
- `lib/base.dart`、`lib/foundation/app.dart`

---

## §10 历史同步与图集封面

> 精简版对应：[§10 历史同步与图集封面](交接文档-精简最新版.md#10-历史同步与图集封面)

### 关键修复

| 问题 | 根因 | 修法 |
|------|------|------|
| 历史页清空后"我"页仍显示旧数据 | `HistoryManager` 未通知 me_page | `clearHistory()`/`remove()` 加通知 |
| 图集封面被左右压扁 | 封面同时限 `cacheWidth` + `cacheHeight` | 改为仅限 `cacheWidth` |
| 远程阅读加载无百分比 | 无 `Content-Length` 传递 | 新增 `StreamImageLoadResult.expectedTotalBytes` |
| 不同宽高图片加载后顶动后续内容 | 无预占位尺寸 | 新增 `pageSizes`，服务端扫描时读图片头宽高 |

### 涉及文件

- `lib/foundation/history.dart`、`lib/pages/history_page.dart`、`lib/pages/me_page.dart`
- `lib/components/comic_tile.dart`、`lib/pages/local_library_page.dart`
- `lib/foundation/image_loader/stream_image_provider.dart`
- `lib/foundation/remote_library_data_source.dart`
- `lib/server/local_resource_scanner.dart`、`lib/server/server_app.dart`

---

## §11 桌面/移动端交互

> 精简版对应：[§11 桌面/移动端交互](交接文档-精简最新版.md#11-桌面移动端交互)

### 关键修复

| 问题 | 根因 | 修法 |
|------|------|------|
| 桌面端侧栏顶部被标题栏遮挡 | `showSideBar` 传 `NaviPane` 局部 context（从 y=36 开始） | 改为 `showSideBar(App.globalContext ?? context, ...)` |
| 移动端详情弹层固定高度不够 | `showModalBottomSheet` 固定高度 | 改为 `DraggableScrollableSheet`（初始 3/5，最大 4/5） |
| 鼠标滚轮不驱动 Sheet 扩展 | `DraggableScrollableSheet` 不响应 `PointerScrollEvent` | `Listener.onPointerSignal` 手动 `jumpTo` |

### 涉及文件

- `lib/pages/download_page.dart`、`lib/pages/local_library_page.dart`
- `lib/components/window_frame.dart`

---

## §12 其他稳定性修复

> 精简版对应：[§12 其他稳定性修复](交接文档-精简最新版.md#12-其他稳定性修复)

### 局域网扫描（§3.6-A）

- 改为快速优先探测：优先已配置地址、缩短超时、提高并发、扫到可用即提前结束。
- ⚠️ 仍需多网卡/多网段真机复核。

### 阅读器沉浸式（§3.6-B）

- Flutter 侧 + Android 原生层联合修复。`NormalTheme` 补 `windowLayoutInDisplayCutoutMode` + 透明状态栏/导航栏；`MainActivity` 强制 `setDecorFitsSystemWindows(false)`。

### Android sqlite 运行库（§3.6-E）

- 启动白屏崩溃定位到 sqlite 运行库缺失并修复。`AndroidManifest` 补 `enableOnBackInvokedCallback`。

### keepScreenOn（§3.8-O / §14）

- 阅读设置页常亮插件通道补齐。`MainActivity.kt` 注册 `lingxue.picakeep/keepScreenOn` 通道 + Dart 侧异常兜底。

### Package 名统一（§15 / §18）

- 从 `com.example.picakeep` 统一到 `lingxue.picakeep`。⚠️ 曾误写为 `lingxue.picakee`，已全局修正。

### 远程已下载封面缓存（§21）

- 稳定 provider key + 磁盘优先 + 预热 + cacheExtent 提升。

### 远程图集连续阅读白边（§22）

- `ComicImage` 自动高度改为物理像素对齐，去掉外层 `Center` 包裹。

### Shizuku FUSE 限制提示（§2）

- `/storage/emulated/0/Android/data` 顶部显示可点击 FUSE 限制提示，支持"不再提示"持久化（`shizuku_fuse_limit_hint_dismissed`）。

### Android 前台服务（§5.1）

- ⚠️ **仍未彻底关闭**。骨架已接入（通知权限、前台服务声明），但保活/通知/系统限制适配整条链未完成。继续做优先关注 `lib/tools/android_foreground_service.dart` + `MainActivity.kt`。

---

## §13 网页后台与浏览器应用

> 精简版对应：[网页后台 / 浏览器应用](交接文档-精简最新版.md#网页后台--浏览器应用)

### 当前架构

网页后台现在不再是旧的单 Dart 字符串 `/admin` 页面，而是 `assets/web_console/` 下的静态 SPA：

```
assets/web_console/
├── index.html
├── css/common.css / admin.css / app.css
└── js/common.js / auth.js / shell.js / admin.js / app.js
```

服务端通过 `lib/server/web_console/web_asset_loader.dart` 与 `web_console_handler.dart` 从 `rootBundle` 读取这些资源；`pubspec.yaml` 已注册 `assets/web_console/`、`assets/web_console/css/`、`assets/web_console/js/`。旧的 Dart 字符串控制台 `lib/server/admin_web.dart` 已迁移删除，不再作为页面来源。`/admin` 兼容重定向，非 API/非静态路径走 SPA fallback，因此：

- `/`：应用面板
- `/app`：应用面板
- `/admin-view`：管理面板
- `/admin`：旧入口兼容，最终进入管理面板或重定向

### 计划一地基要点

- `server_app.dart` 先匹配所有 `/api/*`、`/status`、`/api/events`，最后才交给 `web_console_handler` 做静态资源 / SPA fallback。
- 所有 API 响应统一补 CORS；`OPTIONS` 直接返回 204，暴露 `Content-Range / Accept-Ranges / Content-Length` 供图片 Range 使用。
- `/status` 保持免登录，便于客户端探活；`/api/library/*` 是 Flutter 客户端远程资源公共接口，必须免网页 token；`/api/events` 免登录用于资源热同步；网页静态资源和登录接口免登录；`/api/admin/*`、`/api/console/*`、`/api/remote/proxy` 走 token 鉴权。
- `/api/remote/proxy?target=...&path=...` 是浏览器远程访问另一台 PicaKeep 服务端的唯一通道：只允许 GET，转发 Range/Accept，不透传 Cookie/Authorization，失败归一为 502/JSON error。
- `common.js` 的 `PicaKeepConsole` 是计划二/三/四/五/六共用契约，后续前端只应通过它访问 API、远程代理、token 图片 URL、source 与 remoteTarget。
- `admin.js` 最初由旧 `/admin` 平移而来；计划三再扩展管理 tab。不要再把业务逻辑塞回 Dart 字符串 HTML。

### 关键能力清单

1. **真实登录与网页专用账号**
   - `POST /api/console/login` 支持 `{username,password}`，空 username 兼容旧 admin 登录。
   - `GET /api/console/me` 回填当前用户；admin 可管理用户。
   - token 绑定 `userId + passwordHash`，改密码后旧 token 自动失效。
   - 账号系统只属于网页后台；Flutter 客户端没有账号功能。

2. **per-user 网页阅读历史**
   - 独立 sqlite：`web_console.db`，不复用客户端 `history.db`。
   - 只隔离历史；漫画库、收藏夹、图片收藏、回收站仍是服务端共享数据。
   - `GET/POST/DELETE /api/library/history` 给网页阅读器写回和 dashboard 历史条使用。

3. **应用面板**
   - 支持本地/远程/聚合数据源，封面墙 → 详情 → 阅读器。
   - 阅读器支持连续纵向滚动与翻页两种模式，模式持久化。
   - 图片 URL 必须通过 adapter：本地用 `api.imageUrl(relPath)`，远程用 `remote.imageUrl(target, relPath)`；不能直接把后端相对路径塞给 `<img>`。
   - 阅读器翻页/滚动会节流写回网页历史，dashboard 历史条可续读到上次 ep/page。

4. **dashboard 化与 app「我」页风格**
   - 顶栏删除“我全都要”，只保留“应用面板 / 管理面板”，并支持折叠抽屉。
   - dashboard 改为 app「我」页风格：历史卡、已下载、图集、资源库、图片收藏、工具胶囊。
   - 卡片视觉为描边透明底、成对网格、统一 chevron 与横向历史条。

5. **已下载 / 图集分流规则**
   - 网页不需要新增服务端字段；`/api/library/items` payload 已含 `rootId`。
   - 分流硬规则：
     ```text
     item.rootId.startsWith('custom_') == true  → 图集 / album
     item.rootId.startsWith('custom_') == false → 已下载 / 漫画 / comic
     ```
   - 这条规则来自 `lib/foundation/remote_library_data_source.dart`，app 客户端也依赖它。后续如果改 rootId 语义，必须同步网页前端。

6. **管理面板扩充**
   - `admin.js/admin.css` 已扩展回收站、收藏夹、图片收藏三个 tab。
   - 支持回收站单项/批量恢复与彻底删除，收藏夹新建/重命名/删除/删单条，图片收藏缩略图与删除。
   - 本地资源块支持单项/批量移入回收站、直接删除；服务配置有 host/port 校验，日志可刷新/过滤。

### 已修复的网页坑

| 问题 | 根因 | 修法 |
|------|------|------|
| grid/list 无法回 dashboard | grid 视图没有返回栏 | `renderGridContent` 顶部加 `.app-view-head` + `button[data-back]`，绑定 `back()` |
| 「已下载」入口缺失 | dashboard 只放了图集/资源库 | 新增已下载卡，点入 `filter:'comics'` |
| 图集与已下载混在一起 | 未按 `rootId` 分流 | `custom_` 为图集，否则为漫画 |
| 图片收藏是假入口 | `data-open-grid` 跳普通 grid | 新增 `imageFavorites` 视图，调 `/api/library/image-favorites` |
| `history.remove` 404 | 前端走 `/api/library/history/{target}`，服务端只认查询参数 | 改为 `DELETE /api/library/history?target=...` |
| 旧服务端无密码风险不明显 | 空密码允许首启 | 登录页/响应保留 `emptyPassword` 风险提示 |

### ❌ 不要再走的路

- **不要**把网页账号和 Flutter 客户端账号混起来。当前账号只服务网页后台历史隔离。
- **不要**让网页历史写客户端 `history.db`。网页历史必须走 `web_console.db`。
- **不要**为“已下载 vs 图集”新增服务端接口或字段，现阶段直接用 `rootId.startsWith('custom_')`。
- **不要**在网页 `<img>` 里直接使用 `/api/library/...` 相对路径，必须走 `api.imageUrl` / `remote.imageUrl` 带 token 与代理。
- **不要**让远程代理透传 Cookie/Authorization，也不要允许非 GET 方法；它是 SSRF-adjacent 能力，必须挡在登录后并有超时。
- **不要**把静态资源改回 Dart 字符串页面；计划一已经把旧 `admin_web.dart` 迁移为 `assets/web_console/` 真实文件。
- **不要**让 SPA fallback 抢在 `/api/*` 路由之前，否则 API 会被错误返回 `index.html`。
- **不要**恢复“我全都要”档；现在顶栏只有应用/管理两档。
- **不要**把图片收藏入口继续指向普通 grid。它必须接 `/api/library/image-favorites`。
- **不要**把 `history.remove` 改回路径段形式，服务端当前契约按 query `target` 删除单条。

### 涉及文件

- `lib/server/server_app.dart` — web console API、历史路由、静态资源挂载、鉴权入口
- `lib/server/web_console/web_user_store.dart` — 网页用户与 per-user 历史 sqlite
- `lib/server/web_console/web_auth_handler.dart` — per-user HMAC token、登录/me/admin 校验
- `lib/server/web_console/web_asset_loader.dart`、`web_console_handler.dart` — SPA 静态资源与 fallback
- `lib/server/web_console/remote_proxy_handler.dart` — 远程代理
- `assets/web_console/index.html`
- `assets/web_console/js/common.js` — `PicaKeepConsole` 契约、auth/history/source/remote
- `assets/web_console/js/auth.js` — 用户名+密码登录 UI
- `assets/web_console/js/shell.js` — 顶栏抽屉、两档与两地址
- `assets/web_console/js/app.js` / `css/app.css` — 应用面板、dashboard、grid/detail/reader、图片收藏视图
- `assets/web_console/js/admin.js` / `css/admin.css` — 管理面板扩充

### 验证方法

1. 桌面 app 切服务端模式并启动服务，浏览器打开 `http://localhost:9527/`。
2. 登录 admin，确认 `/` 是应用面板，`/admin-view` 是管理面板，顶栏折叠/展开与刷新保持。
3. 应用面板：历史卡显示；已下载只显示漫画；图集只显示 `custom_` root；资源库显示全量；grid 返回按钮回 dashboard。
4. 阅读器：连续/翻页模式可切换，翻页后回 dashboard 历史条可续读。
5. 图片收藏：入口加载 `/api/library/image-favorites` 缩略图列表，不再跳普通 grid。
6. 管理面板：回收站/收藏夹/图片收藏 tab 可懒加载并执行写操作。
7. 控制台执行 `await PicaKeepConsole.history.remove('test-target')` 不应 404。

### 阅读器 App 化与侧栏多轮收口（后续多轮已执行）

在地基之后，又执行了多轮把网页 reader/侧栏向 Flutter 客户端对齐的计划（`plan/后端网页/` 下的二次修复、App 化复刻、侧栏回归/四次补齐、UI 微调一二三、两轮验收补修）。核心经验：

- **reader 空白 ≠ 图片加载失败**：DevTools 实测时 21 张图片已进入 DOM、网络图片 200、`naturalWidth/naturalHeight` 正常，但整页空白。根因是 shell/grid 下 `#console-root` / `[data-reader-root]` / `.reader-strip` / `.reader-img` 被压成 0 宽。排查口诀：**同时看 DOM 图片数、network 200、naturalSize、父级 `getBoundingClientRect().width`**，宽度为 0 才是真凶。
- **reader-active 布局不变量**：进入阅读器后主内容必须全宽，或把 `#console-root` 明确放到正确 grid column；左侧栏 / 旧 `#topbar` 迁移时不能让内容列塌陷成 0 宽。
- **本地 reader 验证先切「本地」源**：数据源停在「远程」时会触发 `/api/remote/proxy?...target=http://127.0.0.1:9527...` 远程自代理，返回 502/401 干扰本地阅读验证。
- **阅读手势对齐 `lib/pages/reader/touch_control.dart`**：用 pointerdown/move/up 状态机，不要退化成单个 `pointerup`。
  - 中心点击切换上下栏；仅当工具栏隐藏且命中边缘区才翻页。
  - 连续滚动模式边缘判定必须用**视口高度 + `event.clientY`**，不能用整条连续 reader 的 `getBoundingClientRect().height`（高达数万 px，会把屏幕中心误判成顶部翻页区，导致点击永远翻页、永不展开上下栏）。
  - 指针释放语义要区分：正常释放（`lostpointercapture` / document `pointerup` / `blur`）只清 longpress/zoom transient，**不要清掉刚安排的单击 tapTimer**；只有 `pointercancel` / `visibilitychange(hidden)` / 长按触发才可清 tapTimer。否则单击切栏会被吞。
  - 双击/长按是 viewer 级缩放（`.reader-zoom-layer` 包裹 `.reader-strip`，对 layer 做 transform，避免单页 `overflow:hidden` 裁切），缩放 origin 固定到点击点；放大后可随指针拖动平移（`state.reader.zoom.panX/panY` + CSS 变量 `--reader-zoom-pan-x/y`，按缩放层尺寸 clamp 边界）；松手/取消/blur/visibilitychange 必定还原。
- **侧栏对齐 `download_page.dart` + `side_bar.dart`**：桌面右侧贴边 App 面板（hero 封面+标题 → meta chips → tags → 章节 → sticky footer 双按钮），移动端底部 sheet；章节区双列（`.app-panel-episode-list` 两列 grid，窄屏回退单列），单章不被 grid 拉伸成超高空框。
- **上下栏功能对齐 `lib/pages/reader/tool_bar.dart`**：顶栏=返回+标题+阅读设置；底栏=上一章/滑条/下一章 + 全屏/收藏/自动翻页/章节/保存（去掉分享）；页码在左下角。
- **多轮 UI 微调还涉及**：dashboard 卡片、grid 详细/列表视图、详情页推荐区（新增 `/api/library/items/{id}/recommendations`，算法在 `server_app.dart`）、管理面板浅色 token 化。
- **阅读设置面板 App 化**：阅读模式改成可折叠列表项（`state.reader.methodPickerOpen` + `.reader-method-list`，点击展开/收回），其余项为「图标 + 标题 + pill 开关 / 滑条」三段式行。
- **图片最大显示宽度可调（双端）**：Web 新增 prefs `maxWidthPx`（默认 980，范围 600–1600），由「限制图片最大显示宽度」开关下方的滑条控制，写到 reader root 的 `--reader-max-width`；客户端同步在 `lib/base.dart` settings index 116 存数值，`reading_settings.dart` 加 Slider，`image_view.dart`（`settings[43]=="1"` 时）按 index 116 应用。两端默认值都是 980。
- **全局 ESC 退出/返回**：`app.js` 的 `onGlobalKeydown` 分级处理——reader 设置/章节面板 → 侧栏 inspector → 退出 reader → 全局 `back()`；输入框 / contenteditable 聚焦时不抢占。

#### ❌ 阅读器/侧栏部分不要再走的路

- **不要**再把 reader 空白当成图片加载问题去查图片 URL / 网络；先验证父级容器宽度。
- **不要**用整条连续 reader 高度判断点击边缘翻页区，必须用视口高度。
- **不要**在正常指针释放链路里清掉单击 tapTimer，否则点击唤栏失效。
- **不要**只对单张 `.reader-img` 做 transform 缩放（会被裁切且无法整体平移），要缩放 `.reader-zoom-layer`。
- **不要**在左侧栏 / reader-active 迁移时让 `#console-root` 落到 0 宽 grid 列。
- **不要**把已修复的 reader 空白 / 侧栏 App 化 / 左侧栏迁移当成未解决问题继续记。

### 网页 reader 历史保存 / 滚动恢复 / 历史页对齐已下载（后续已执行）

这一轮把网页阅读历史的「写」与「读」两端、以及历史独立页向「已下载」对齐。改动**只在 `assets/web_console/js/app.js` 与 `css/app.css`**，未动客户端/服务端。

- **ESC 退出不保存进度（只有返回键保存）**：ESC（`onGlobalKeydown`）与顶栏返回按钮其实**同走一个 `back()`**，并不是两条分支。差异在阅读翻页/滚动会用 `scheduleReaderHistoryWrite()` 挂一个 **1500ms 防抖**定时器再落盘；退出时 `cleanupReader()` 旧逻辑**直接 `clearTimeout` 把待写定时器清掉、不补写**。返回键之所以"看起来正常"，只是因为唤出顶栏+移鼠标+点击通常已超过 1500ms、防抖早已落盘；ESC 可在窗口内瞬退，于是最后页码被取消。修法：`cleanupReader()` 改调 `flushReaderHistory()`——清定时器**后**按当前实时页（滚动模式用 `currentScrollPage()` 回写 `pageIndex`）调一次 `writeReaderHistoryNow()`；`lastHistoryKey` 去重保证未移动时是 no-op。ESC / 返回键 / 任何跳转都统一经 `cleanupReader`，一处补写全修好。
- **从历史打开总停在第 1 页（仅滚动连续模式）**：页码数据全程正确（`openHistory` 传 `pageIndex: record.page` → `renderReader` 存入 → `renderReaderContent` clamp），分页模式直接以该 index 渲染所以正常。滚动连续模式旧实现只有**一次** `requestAnimationFrame(() => scrollToPage(pageIndex))`，而 `scrollToPage` 用 `scrollIntoView`：目标页之上的图片是懒加载，跳转那刻上方各页高度未稳定，落点几乎在顶部；紧接着 `pageObserver` 按当前可视页**把 `state.reader.pageIndex` 覆写**（并触发历史写入），停回第 1 页。修法：`restoreScrollPosition(target)`——置 `state.reader.restoring=true`，多帧（rAF）重试 `scrollToPage(target)` 直到 `currentScrollPage()===target` 稳定或超时（约 25 帧兜底），`restoreRaf` 在 `cleanupReader` 取消；`pageObserver` 回调加 `if (state.reader.restoring) return;` 守卫，恢复期间不让观察器覆写 pageIndex。`target<=0` 直接不重试。
- **历史卡第二行「0」+ 历史页对齐已下载**：服务端历史记录（`server_app.dart` 的 `upsertHistory` / `web_user_store.dart` 的 `toJson`）**只持久化 `target/title/cover/ep/page/maxPage/readEpisode`，没有 author/subtitle/tags**。`readEpisode` 是 int 集合，回传 `[0]`；旧 `normalizeHistoryPayload` 用 `String(firstValue(['readEpisode',…]))` 把它变成字符串 `"0"`，卡片 `item.readEpisode || item.subtitle` 就渲染成"0"。修法：① `normalizeHistoryPayload` 不再用 `readEpisode` 当展示文案；② 作者/副标题与标签改由 `renderHistoryPage` 用 `settle()` 并行拉 `history.list` + `fetchItemsForSource(当前源)`，建 `Map(id → {subtitle, tags})` 对每条历史补全（资源库缺该条目就留空、隐藏该行）；③ 启用历史页搜索——`updateToolbar` 把 `historyPage` 加进 `searchInput.disabled` 白名单与 `viewSwitchEl.hidden` 白名单，**复用同一个 `state.grid.query` 与 `state.grid.viewMode`（与已下载共用记忆）**，search input / clear / view-switch 的 handler 各加 `historyPage` 分支调 `renderHistoryPageContent`，过滤用现成 `filterItems`；④ 卡片改用 `.app-card`(`historyPosterCardHtml`) / `.app-detailed-card`(`historyDetailedCardHtml`) 结构，保留右上角 `.app-history-remove`。
- **详细视图标题溢出覆盖下一行**：`.app-detailed-card` 用 `align-items: stretch`，使 `.app-detailed-title` 被拉伸、`-webkit-line-clamp` 的 `-webkit-box` 高度不收敛，标题换行到 3+ 行盖住下面卡片。修法：`.app-detailed-title` 补全 `display:-webkit-box; -webkit-box-orient:vertical; overflow:hidden; -webkit-line-clamp:3; white-space:normal`，并加 `.app-history-entry .app-detailed-title { align-self: start }` 让 clamp 真正生效（不被 stretch 撑开）。标签经库 join 后显示：详细视图前 3 个（`.app-detailed-tags`）、海报视图前 2 个（`.app-detailed-tags.app-history-tags`），复用 grid 既有标签样式。

#### ❌ reader 历史 / 历史页部分不要再走的路

- **不要**以为 ESC 和返回键是两条不同退出逻辑——它们同走 `back()`，丢页是 1500ms 防抖在 `cleanupReader` 被清，补写要放在 `cleanupReader`（统一出口）而不是只改 ESC handler。
- **不要**用单次 rAF + `scrollIntoView` 恢复滚动模式阅读位置——懒加载未定高时会落到顶部，必须多帧重试 + `restoring` 守卫挡住 `pageObserver` 覆写。
- **不要**用 `readEpisode` 当历史卡第二行文案——它是 int 集合会变 "0"；作者/副标题/标签服务端**根本不存**，只能前端 join 资源库列表补全，缺失就隐藏，别再想着改服务端历史表。
- **不要**指望 `.app-detailed-title` 的 `-webkit-line-clamp` 在 `align-items:stretch` 的卡里自动生效——必须给标题 `align-self:start` 否则会被拉伸、clamp 失效导致溢出。
- **不要**给历史页单独造一套 viewMode/query 记忆——用户要求与「已下载」一致，直接复用 `state.grid.viewMode` / `state.grid.query`。

### 涉及文件（本轮）

- `assets/web_console/js/app.js` — `flushReaderHistory` / `restoreScrollPosition` / `pageObserver` 守卫 / `normalizeHistoryPayload` / `renderHistoryPage` 库 join / `updateToolbar` 白名单 / `historyPosterCardHtml` / `historyDetailedCardHtml`
- `assets/web_console/css/app.css` — `.app-detailed-title` 3 行 clamp、`.app-history-entry .app-detailed-title` align、历史卡复用 `.app-card`/`.app-detailed-card` 与 `.app-history-tags`

### 网页应用面板新增收藏夹入口 + 服务端收藏封面 fallback + 卡片对齐客户端（后续已执行）

这一轮做了四件事：① 网页 dashboard 新增「收藏夹」入口（接 `/api/library/favorites`）；② 服务端收藏夹封面在本地 `coverPath` 失效时按 candidates 匹配资源库兜底；③ 通用详细档卡片向客户端对齐、标签全显示且跟随主题色；④ 收藏夹文件夹列表与 dashboard 滚动收口。改动落在 `assets/web_console/js/app.js`、`css/app.css`、`lib/server/server_app.dart`、`js/admin.js`。

- **收藏项封面破图 + 卡片不可点（核心 bug）**：服务端 `_buildFavoriteItemPayload` 原本只回 `name/author/type/tags/target/time/coverUrl`，**没有 `itemId`**；而客户端/网页用 `__matched: !!itemId` 判断是否匹配到资源库，于是 `__matched` **永远 false** → 封面被强制清空、卡片标为 `aria-disabled` 不可点。根因不是 cover 路由本身（`/api/library/favorites/{folder}/{target}/cover` 一直可用），而是 payload 缺字段。修法：服务端 GET 列文件夹内容时对每个收藏项跑一次 `_resolveFavoriteResourceItem(snapshot, item)`，匹配到就把 `itemId/id/displayId/sourceDisplayName/imageCount/totalBytes/updatedAt` 一并写进 payload，`__matched` 才能正确为 true。
- **收藏封面 fallback（coverPath 失效但资源库有内容）**：cover 路由改为三级兜底——① 本地 `item.coverPath` 文件存在就直接 `_fileResponse`（注意要看返回是否 404，不能只看路径非空）；② 命中 `_favoriteCoverFallbackCache`（key=`folder|type|target`）且文件在就用缓存；③ 都没有则 `_currentSnapshot()` + `_resolveFavoriteResourceItem` 按 candidates 匹配资源库项，取 `matched.coverPath`，为空再 `_scanner.resolveCoverPathOnly(matched, rootPath)`，成功后写入 fallback 缓存。`_recomputeLibrarySignature` 里 signature 变化时**连 `_favoriteCoverFallbackCache` 一起清**，避免资源移动后命中 stale 路径。
- **candidates 复用客户端逻辑**：`_buildFavoriteServerCandidates(item)` 直接调 `item.candidateDownloadIds()`（FavoriteItem 自带，含 ehentai/jm/hitomi/Ht/nhentai 等前缀变体），再补 `target` 本身；`_findResourceItemByCandidates` 把每个资源项的 `id/title/displayId/sourceTitle/sourceDisplayName/subtitle/path` 收成 pool 做包含匹配。**不要**在服务端另写一套候选规则，会和客户端漂移。
- **cover 路由要带 `?type=`**：同一 target 在不同收藏类型下可能并存，`coverUrl` 末尾拼 `?type=${item.type.key}`，`_findFavoriteItemByTarget` 增加可选 `type` 参数按类型精确定位；DELETE 单项同样带 type。
- **`_rootPathForRootId` 从 config 解析**：`current_download`/`original_download` 取 `config.currentDownloadRoot`/`originalDownloadRoot`，`custom_{n}` 按下标取 `config.customLibraryRoots[n]`，越界返回 null。
- **详细档标题严格 2 行**：`.app-detailed-title` 同时写 `text-overflow:ellipsis` 和 `-webkit-line-clamp:2` + `white-space:normal` 会冲突，导致第三行漏出一点。修法：去掉 `text-overflow`，保 `display:-webkit-box; -webkit-box-orient:vertical; -webkit-line-clamp:2; overflow:hidden`，再补 `max-height: calc(1.35em * var(--app-detailed-title-lines))`（lines 默认 2）兜底像素漏出。历史页 `.app-history-entry .app-detailed-title` 仍是 3 行覆盖（§13 前一轮），本轮不破坏。
- **详细档标签全显示 + 跟随主题色**：`gridDetailedCardHtml` 去掉 `tags.slice(0,3)` 改全量渲染（`.app-detailed-tags` 本就 `flex-wrap:wrap` 自动换行）；标签底色**不写死**，用 accent 主题变量 `background: rgba(var(--accent-rgb),0.12)` + `color: var(--accent-2)` + `border: 1px solid rgba(var(--accent-rgb),0.28)`，圆角 8px，用户在设置里换 12 色主题时标签随之变色（等效客户端 `secondaryContainer` 的主题感）。
- **收藏夹文件夹列表**：`favoriteFolderCardHtml` 为紧凑横向卡片（文件夹 svg 图标 + 名称 + 右侧数量徽章），副标题文案为「本地」；容器 `.app-favorites-folder-grid` 用 `repeat(auto-fill, minmax(300px, 1fr))`（较初版 200px 加宽 1.5 倍）。
- **dashboard 多余滚动**：`.app-shell:has(...dashboard)` 的高度链需把 `#console-root` 的 `padding:24px`（上下共 48px）算进去，否则内容能完整显示时仍出现纵向滚动条。

#### ❌ 收藏夹这轮不要再走的路

- **不要**以为收藏封面破图是 cover 路由坏了——路由一直能用，真因是 payload 缺 `itemId` 让 `__matched` 永远 false。
- **不要**在服务端另写一套收藏 candidates 规则——直接用 `FavoriteItem.candidateDownloadIds()`，否则和客户端匹配逻辑漂移。
- **不要**忘了 signature 变化时清 `_favoriteCoverFallbackCache`——资源移动/删除后会命中 stale 封面路径。
- **不要**给 `.app-detailed-title` 同时留 `text-overflow:ellipsis` 和多行 `-webkit-line-clamp`——两者冲突导致第三行漏出，多行 clamp 用 `overflow:hidden` + `max-height` 兜底即可。
- **不要**把标签背景写成固定色值——主题色用户可切（`data-accent` 12 色），必须走 `var(--accent-rgb)`/`var(--accent-2)` 跟随。
- **不要**因为「标签全显示」就去动海报档/历史档的标签数量——本轮只放开**详细档**（`gridDetailedCardHtml`）。

### 涉及文件（收藏夹这轮）

- `lib/server/server_app.dart` — `_buildFavoriteItemPayload`（补 matched 字段）、cover 路由三级 fallback、`_favoriteCoverFallbackCache`、`_resolveFavoriteResourceItem`/`_buildFavoriteServerCandidates`/`_findResourceItemByCandidates`/`_resourceItemCandidatePool`/`_rootPathForRootId`、signature 清缓存、`_findFavoriteItemByTarget` 加 type 参数
- `assets/web_console/js/app.js` — `renderFavorites`/`renderFavoritesContent`、`normalizeFavoriteFoldersPayload`/`normalizeFavoritesItemsPayload`、`favoriteFolderCardHtml`、`favoriteDetailAttrs`/`favoriteDownloadIdCandidates`、`gridItemMetaParts`、`gridDetailedCardHtml` 标签全显示、dashboard 收藏夹入口与事件绑定
- `assets/web_console/css/app.css` — `.app-detailed-title` 2 行截断、`.app-detailed-tags span` 主题色、`.app-favorites-folder-grid`/`.app-favorites-folder-card` 系列、dashboard 滚动高度链
- `assets/web_console/js/admin.js` — 管理面板收藏夹相关同步

---

## §16 远程访问图集压缩包（含加密包）

> 精简版对应：[远程压缩包](交接文档-精简最新版.md#网页后台--浏览器应用)。本轮在 V1.9.15 之后执行。

### 核心设计：服务端把压缩包伪装成普通 item

客户端本地早已能读图集里的 ZIP/CBZ（`archive://` URI + `ArchiveReadingService`），但**服务端原本完全不扫压缩包**，客户端远程/网页都看不到。本轮让**服务端把每个压缩包扫成一个普通 item/episode**，页 URL 仍是 `/api/library/items/{id}/images/{ep}/{page}`，只是该路由命中压缩包时返回 archive 内字节而非磁盘文件。**客户端远程数据源消费 HTTP URL 的逻辑零改动**，只有加密解锁需要增量。

### 关键实现点

- **共享分组逻辑抽出**：`lib/foundation/archive/archive_episode_builder.dart`（新）顶层 `buildArchiveEpisodes(ArchiveIndex)` / `pickArchiveCoverEntry`，从 `local_library.dart` 私有静态方法提取，客户端与服务端复用，排序结果须完全一致（页码顺序对用户可见）。
- **服务端扫描**：`local_resource_scanner.dart` `_scanCustomRootItems`（图集 custom_ 根）末尾加 `_collectArchiveFiles` + `_scanCustomArchiveItem`：`getIndex` 解析中央目录（不需密码）→ `buildArchiveEpisodes` 分章 → `imagePaths` **直接存 `archive://?path=&entry=` URI**（类型仍是 `List<String>`）。`ServerResourceItemSummary`/`Episode` 新增 `isArchive/archiveEncrypted/archivePasswordMatched/archiveFormat/archivePath`，`toJson` 与 `_buildLibraryItemPayload` 透传 `itemKind`(archive/directory) 等。
- **取图/封面路由**：`server_app.dart` 图片路由命中 `isArchiveUri(imagePaths[page])` → `_archiveBytesResponse`（新，`readEntryBytesByUri` 取字节 + Content-Type by entryPath，加密读失败抛 → 403 `archive locked`/404）；封面路由对 `item.isArchive` 同样走 `_archiveBytesResponse`。
- **加密 unlock 接口**：`POST /api/library/items/{id}/archive/unlock {password}` → `ArchiveReadingService.tryUnlock`（进程单例会话密码，成功后取图/封面自动用上）→ 成功清 `_deepItemCache`/`_coverPathCache` + `rescanResources()`。另有 `GET .../archive/status`。密码只走 body（`logRequests` 不打 body）。
- **客户端远程解锁**：`RemoteLibraryComicItem.fromJson` 解析 archive 字段 + `needsArchivePassword` getter；`RemoteLibraryClient.unlockArchive`；`archive_password_dialog.dart` 抽 `onVerify` 回调（本地传 `tryUnlock`、远程传 `unlockArchive`）；解锁入口补到 `local_library_page._handleItemTap`、`download_page`（列表 onTap + 上下文菜单 + 详情 read）、`local_comic_detail_page`。
- **网页前端**（`app.js`）：`ensureArchiveUnlocked(item)` 在进 reader/详情前判断 `archiveEncrypted && !archivePasswordMatched` 弹密码框调 unlock；normalize 透传 `itemKind/isArchive/archiveEncrypted`；远程经 `consoleApi.remote.post(.../archive/unlock)`、本地经 `consoleApi.api.post`。

### 踩坑（务必记住）

- **headless 服务端没注册 archive backend**：`ArchiveRegistry.initDefaults()` 原仅在 `lib/main.dart` 调，`bin/main_server.dart`/`PicaKeepAdminServer.start()` 没调，且 `register()` 无去重。修：`initDefaults` 改幂等（`if (_backends.isEmpty)`）+ `start()` 早期调一次。否则独立服务端进程 `backendForOrThrow` 抛 `unsupportedFormat`。
- **`_ensureDeepItem` 对 custom_ 直接返回 shallow**：压缩包 item 的 episodes/imagePaths **必须在浅扫阶段建全**（`openIndex` 只读中央目录、廉价，可行）。
- **`_fileResponse` 只能服务磁盘真实文件**（`File(path).exists()`），archive entry 不是文件，必须用 `_archiveBytesResponse` 返回字节。
- **加密包封面**未解锁抽不出字节 → 路由返回 403/404 → 端侧显示锁占位。
- **会话密码进程内存、重启即失**：服务端重启后远程取图返回 403，端侧需重新弹解锁框。

### ❌ 不要再走的路

- **不要**给服务端 archive 另写一套分组/封面逻辑——用共享的 `buildArchiveEpisodes`/`pickArchiveCoverEntry`，否则与客户端漂移。
- **不要**让 unlock 密码进 URL/query 或写进日志 body。
- **不要**以为 `_fileResponse` 能服务 archive entry。

### 涉及文件
- `lib/foundation/archive/archive_episode_builder.dart`（新）、`lib/foundation/local_library.dart`（改调共享）
- `lib/server/local_resource_scanner.dart`、`lib/server/server_app.dart`、`lib/foundation/archive/archive_registry.dart`
- `lib/foundation/remote_library_data_source.dart`、`lib/components/archive_password_dialog.dart`、`lib/pages/local_library_page.dart`、`lib/pages/download_page.dart`、`lib/pages/local_comic_detail_page.dart`
- `assets/web_console/js/app.js`、`lib/server/web_console/remote_proxy_handler.dart`（代理支持 POST 以转发 unlock）

---

## §17 阅读器「图片最大显示宽度」滑块在连续模式失效与闪烁

> 客户端 `settings[43]` 开关 + `settings[116]` 数值（默认 980，范围 600–1600）。本轮多处修复。

- **翻页模式（mode 1/2/3、5/6）未应用限宽**：`image_view.dart` 的 `buildType123`/`buildType56` 用 `PhotoViewGallery` 直接吃满视口，没读 `settings[116]`。修：在 gallery **容器外层**包 `Center + SizedBox(width: _clampReaderImageWidth(constraints.maxWidth))`。
- **连续模式（mode 4）完全不变**：`ScrollablePositionedList` 是 sliver，**给 item 的横向约束是 tight = viewport 全宽**，item 内 `ComicImage` 的 `SizedBox(width)` 被拉伸/忽略。修：在 `ScrollablePositionedList` **外层**限宽居中，而非依赖 item 内部 width。
- **「只全屏能调、只调一点」**：外层 `PhotoView.customChild` 的 child 写死 `MediaQuery.size.width`（全屏宽，非阅读区可用宽），且 item 内又做了一次 `_clampReaderImageWidth` + `height/width<1.2` 兜底二次收缩。修：限宽基准改用 `LayoutBuilder` 真实 `constraints.maxWidth`，**只限一次（外层）**，item 内不再重复 clamp。
- **拖动滑块时图片闪烁**：`_createReaderImageRequest` 连续模式 `cacheWidth=(layoutWidth*dpr).round()` → `ResizeImage`，拖动时 layoutWidth 每帧变 → cacheWidth 每帧变 → **provider key 每帧变 → 反复重解码**。修：**解码宽度与显示宽度解耦**——`layoutWidth`（决定 cacheWidth）用阅读区全宽（稳定），`ComicImage.width` 才用实时 listWidth（纯布局缩放，不重解码）。

### ❌ 不要
- **不要**靠 sliver item 内部 `SizedBox(width)` 限宽——被 tight 约束吃掉，要在列表外层限。
- **不要**用 `MediaQuery.size.width` 当限宽基准——窗口模式 ≠ 阅读区宽，要用 `LayoutBuilder` 约束。
- **不要**让 `cacheWidth` 跟随滑块每帧变——会重解码闪烁，解码宽度固定、显示宽度才实时。

### 涉及文件
- `lib/pages/reader/image_view.dart`（`buildType4`/`buildType123`/`buildType56`/`_createReaderImageRequest`、helper `_isReaderImageWidthLimited`/`_clampReaderImageWidth`）

---

## §18 网页后台 UI 二次还原客户端（详情页 / 阅读面板 / 章节列表）

> 本轮把网页端多处 UI 进一步还原 Flutter 客户端。改动只在 `assets/web_console/js/app.js` + `css/app.css`（含 `common.css`）。

- **阅读设置/章节面板位置**：原是浮在底栏上方的小卡片（`right:16px;bottom:78px;border-radius:28px`），改为对齐客户端 `showSettings`——桌面全高右侧栏（`top/right/bottom:0; width:min(450px,…); 圆角16px 0 0 16px`）、移动端底部 sheet（`reader-panel-sheet`，圆角24 顶、从底滑入）；用 `isMobilePanel()` 切 `-side`/`-sheet`。
- **章节列表文字竖排**：`.reader-chapter-list button` 误共用 `.reader-method-list button` 的三列图标模板 `34px minmax(0,1fr) auto`，但章节按钮无图标元素（只有 `<span>标题</span><em>页数</em>`），标题被塞进 34px 图标列逐字竖排。修：章节按钮单独两列模板 `minmax(0,1fr) auto`。
- **章节列表当前项样式**：原 active `color:#fff` 浅色模式白字看不清 + `0.78` 硬边框/立体感。最终（按用户要求）：默认章节按钮主题色扁平边框，当前项 = 主题边框 + 淡主题底 + **普通字色 `var(--text)`（不跟随主题色）+ 仅加粗区分**，`box-shadow:none` 去立体；与 `.reader-method-list` active 拆开。
- **详细档卡片对齐**：布局顺序（标题→副标题→标签中部弹性→底部 stats+漫画源 badge）、标签背景改中性可见容器色（`color-mix(accent 12%, panel-2)` 或中性）+ 普通字色（**不跟主题变**）；右下徽章是漫画源 `sourceDisplayName`（非档位）。
- **详情页还原**（整页，非侧栏）：信息区从单个 accent 合体胶囊改为客户端「**字段名胶囊 + 值胶囊**」逐行（`infoRowHtml` → `.app-info-row`/`.app-info-key`/`.app-info-val`，圆角12、字段名带 accent 色调、值中性容器普通字色）；操作区只留**一个 72×72「阅读」方块**（图标在上文案在下，去掉「复制标题」）；Hero 封面 132→102×136、圆角16→12、标题字号降到 18；章节网格 `minmax(220,1fr)` 易把单章拉满整行 → 改 `repeat(auto-fill, minmax(160px,200px)) + justify-content:start`（单元固定宽不拉满、左对齐，对齐客户端 `maxCrossAxisExtent:200`）。
- **排序默认倒序**：`readGridSort()` 默认 `desc:true`（对齐客户端最新在前）。
- **阅读时滚动穿透**：进 reader 后滚动会带动背后 grid 列表。`.is-reader-route` 下收敛宽度/滚动到 reader 容器、`overscroll-behavior:contain`（注意 §13：reader-active 时别让 `#console-root` 塌成 0 宽）。

### ❌ 不要
- **不要**让章节按钮共用阅读模式列表的图标三列模板（无图标会竖排）。
- **不要**给章节当前项写 `color:#fff` 或主题色字——浅色看不清/不符要求，用 `var(--text)` + 加粗。
- **不要**详情页章节网格用 `auto-fit + 1fr`——单章会拉满整行；用 `auto-fill + 固定上限宽 + justify start`。
- **不要**把详情页信息区做成单个合体胶囊——客户端是字段名/值双胶囊。

### 涉及文件
- `assets/web_console/js/app.js`、`assets/web_console/css/app.css`、`assets/web_console/css/common.css`

---

## §19 网页后台管理面板增强（路径选择器 + 账号管理）

> 精简版对应：[管理面板增强](交接文档-精简最新版.md#网页后台--浏览器应用)。

### 路径选择器

服务配置（`admin.js` `renderConfig`）的三个路径输入框旁加「+」按钮，点击弹出路径选择器浏览服务端目录并回填。

- **服务端新增 `GET /api/admin/browse?path=`**（`server_app.dart`）：`path` 为空返回盘符（Windows）/系统根（类 Unix/Android）；否则调 `PrivilegedStorageAccess.listDirectoryEntries(path)`（已含 Root/Shizuku 回退），返回 `{path, parent, entries[{name,path,isDirectory}]}`。
- **前端弹窗**（`admin.js` `openPathBrowser`）：面包屑 + 快捷根 + 目录列表（`..` 上一层 + 各子目录）+ 「选择当前目录」/「取消」。样式参考 Exp 内置文件浏览器（紧凑列表 + 面包屑）。
- **快捷根**（`_adminBrowseRoots`）：仅保留盘符/系统根（Windows 枚举盘符、类 Unix `/` + `/storage/emulated/0` + `/sdcard`），**不去**加配置里的库目录（如 currentDownloadRoot/customLibraryRoots 那些测试文件夹）。
- 保存逻辑不变：选定路径回填 input/textarea，「+」只是辅助填值，仍走 `PUT /api/admin/config`。

### 账号管理

- 后端接口完整（`server_app.dart` `api/console/users`，admin 限定）：`GET` 列用户、`POST` 新建（`{username,password,role}`）、`DELETE /{id}` 删除。
- 前端缺失（本 session 补齐）：`admin.js` 导航加「账号管理」tab + panel + `renderUsers()`（列表/新建/删除）+ 监听 `pk-console-users-open` 事件（头像菜单「👥 用户管理」入口原先派发该事件但无人响应，点击无反应 → 现在切到 admin 模式并自动定位到账号管理 tab）。
- **仅 admin 可见**：非 admin 不渲染该 tab，后端也会 403 兜底。

### ❌ 不要
- **不要**把配置库目录塞进快捷根。
- **不要**让路径选择器依赖 Android 端原生能力（网页无法调 MethodChannel），必须走服务端 HTTP 接口。
- **不要**在非 admin 面前展示账号管理入口。

### 涉及文件
- `lib/server/server_app.dart`（`_handleBrowseRequest`/`_adminBrowseRoots`）
- `assets/web_console/js/admin.js`（`renderConfig`/`openPathBrowser`/`renderUsers`/`bindConfigForm` + 账号 CRUD）
- `assets/web_console/css/admin.css`

---

## §20 网页后台滚动/布局修复（侧栏切换 / 滚动条 / toolbar 间距 / reader 穿透）

> 精简版对应：[网页滚动/布局收口](交接文档-精简最新版.md#网页后台--浏览器应用)。

### 侧栏展开/收起触发整页滚动

- **根因**：`#console-shell` 用 `min-height:100vh`（由内容撑高）+ `transition: grid-template-columns`，侧栏切换改列宽 → 主内容区重排高度微增 → 整页可滚。刷新重新布局又消失。
- **修**：`#console-shell` 改 `height:100dvh; overflow:hidden`（固定视口高、永不整页滚）；`#console-root` 改 `height:100%; overflow-y:auto`（内容超高时在内部滚动）。切换侧栏只改列宽、不再影响外层高度。

### 主页无用滚动条

- `#console-root overflow-y:auto` 在内容刚好时仍显滚动条 → `scrollbar-width:none` + `::-webkit-scrollbar{display:none}` 隐藏。
- dashboard toolbar（本地/聚合/远程）与历史卡间距过近 —— `:has()` 规则未命中，改用给 toolbar 补 `padding-bottom` 或 dashboard 首卡加 margin-top 的方式。

### reader 滚动穿透

- 非全屏时 `.app-reader` 是 `overflow:hidden`，长内容走 document 滚 → 背后 grid 列表跟滚。非全屏也加 `overflow-y:auto` + 外层 document 锁滚。

### ❌ 不要
- **不要**在 `#console-shell` 上再用 `min-height:100vh`（应固定 height）。
- **不要**依赖脆弱的 `:has(> .app-main-toolbar + .app-view > .app-dashboard)` 兄弟链——用更直接的选择器。

### 涉及文件
- `assets/web_console/css/common.css`（`#console-shell`/`#console-root`/`html.pk-reader-active`）
- `assets/web_console/css/app.css`（`.app-reader`/`.app-main-toolbar`）

---

## §23 网页移动端顶栏、返回、浏览器工具栏与详情 sheet 底部收口

> 精简版对应：[网页移动端顶栏与详情 sheet](交接文档-精简最新版.md#网页后台--浏览器应用)。本节记录当前移动端网页后台一轮实际做过的事情，以及详情 sheet/footer 底部问题的正确后续方向。

### 已完成的主线

1. **移动端顶栏重排**
   - 窄屏下 `#topbar` 从左侧 rail 形态改成 sticky 横向顶部栏。
   - 头像菜单限制在视口内，层级高于列表、浮动返回与卡片。
   - 移动端展开按钮视觉上替换成 Pica 图标；`PicaKeep Console` 移到头像旁边。
   - topbar 点击展开恢复；移动端展开态显示模式切换和面板按钮。

2. **列表页浮动返回统一**
   - 列表标题合并进浮动返回按钮，如「已下载 · 返回」「图集 · 返回」「资源库 · 返回」「历史记录 · 返回」「收藏夹 · 返回」。
   - 桌面与移动端都走 fixed 浮动返回，而不是一套 sticky / 一套 fixed。
   - 如果搜索、筛选或工具卡片还在顶部，返回按钮落在卡片下方；卡片滚出或被 topbar 遮住后，按钮停在安全顶部。
   - 滚动、resize、方向变化、列表重渲染和 toolbar 展开收起都会重新计算位置。

3. **浏览器内部返回手势**
   - Web Console 内部 view 写入浏览器 history 状态。
   - 手机浏览器返回键/返回手势会映射到应用内部 back：reader → detail/list → dashboard。
   - 外层 shell 识别内部 app history 后跳过重复 route rerender，避免状态互抢。
   - refresh / goGrid(true) 这类刷新动作不应写入浏览器栈，避免返回路径变脏。

4. **PWA 与浏览器工具栏尝试**
   - 已新增 `manifest.webmanifest`，`index.html` 补 manifest 和 mobile-web-app / apple-web-app meta。
   - 普通网页不能强制首次打开隐藏浏览器地址栏/底部栏；PWA standalone 才能更接近无浏览器 chrome。
   - 曾尝试 touchmove/window-scroll booster 推动浏览器工具栏隐藏，用户反馈无效，已撤回。
   - 移动端后来改为更自然的 document/window 滚动：`html/body` 放松固定满高与内部滚动限制，让浏览器有机会按自身规则收起工具栏。

5. **移动端详情 sheet/footer 底部问题**
   - 章节列表已做移动端双列，降低高度压力。
   - 试过动态 `--mobile-browser-chrome-pad`：visualViewport 差值、学习最大/最小高度、默认 pad、PWA standalone pad=0。
   - 试过“三段式 sheet”：header 固定、中间内容滚动、footer 固定/绝对定位；用户反馈仍不理想，已回退。
   - 回到内容流 footer 后，发现空白不是单一 padding，而是 `.app-panel-content`、`.app-panel-footer`、footer 通用 margin/padding、safe-area 与 chrome pad 多层叠加。
   - 当前方向：**只保留一个浏览器底部工具栏补偿点**，优先放在 footer 区域；不要同时在 content 和 footer 上叠 `--mobile-browser-chrome-pad`。

### 当前最重要的后续方向：两状态高度学习

移动浏览器实际可以按两个稳定视口高度处理：

- **工具栏展开**：地址栏/底部栏可见，视口高度最小，需要底部垫高。
- **工具栏收起**：地址栏/底部栏隐藏或缩起，视口高度最大，不需要浏览器 chrome 垫高。

推荐算法：

1. 仅在移动端、非 PWA standalone 下启用。
2. 当前高度优先取 `visualViewport.height`，取不到再用 `window.innerHeight`。
3. 首次打开默认认为浏览器工具栏展开：记录为 `minViewportHeight`，并使用默认垫高。
4. 后续只要观察到更大的高度，更新 `maxViewportHeight`，视为工具栏收起状态。
5. 一旦 `maxViewportHeight - minViewportHeight` 足够大（例如超过 24px），就用当前高度接近哪一端来决定 pad：
   - 接近最小高度：`--mobile-browser-chrome-pad = 默认垫高`。
   - 接近最大高度：`--mobile-browser-chrome-pad = 0px`。
   - 中间态可用阈值切换，不必复杂线性插值。
6. PWA standalone 始终 `0px`。
7. CSS 侧只能有一个主要消费点，避免 double cushion。

### ❌ 不要

- **不要**继续叠多个底部补偿：`.app-panel-content` 和 `.app-panel-footer` 同时吃 `--mobile-browser-chrome-pad` 会产生大空白。
- **不要**把 footer 固定/绝对定位作为默认方向；之前三段式 sheet 已试过，交互不稳定且用户不认可。
- **不要**再加 touch/window scroll booster 试图强制隐藏浏览器工具栏；普通网页没有可靠强制能力。
- **不要**只看 `visualViewport` 的实时差值就认为是真实底部遮挡；不同浏览器/ROM 表现不一致，优先按“最小高度/最大高度”状态机处理。
- **不要**在未重新读取当前 `app.css` / `shell.js` 的情况下继续改底部垫高；用户中途可能手动回滚过。

### 涉及文件

- `assets/web_console/js/shell.js`（移动端 viewport/chrome pad 状态、PWA standalone 判断）
- `assets/web_console/css/app.css`（详情 sheet、`.app-panel-content`、`.app-panel-footer`、章节列表、底部补偿消费点）
- `assets/web_console/css/common.css`（移动端 topbar、shell/root 滚动结构、层级）
- `assets/web_console/js/app.js`（浮动返回、内部 history、详情 sheet/list rerender）
- `assets/web_console/index.html`、`assets/web_console/manifest.webmanifest`（PWA）

---

## §21 服务端深扫排除封面（下载漫画最后一页糊图修复）

> 精简版对应：[深扫封面排除](交接文档-精简最新版.md#网页后台--浏览器应用)。

- **根因**：服务端 `_listImageFiles`（`local_resource_scanner.dart:1685`）→ `_collectImageFiles` 不排除 cover-like 文件，下载漫画深扫时 cover.jpg 被当成一页（排序后在第末，ResizeImage 缩放显糊）。客户端本地阅读用 `_buildDownloadedEpisodeFilesForEp` 的 `files.where((path) => !_isCoverLikePath(path))` 排除了 cover，故本地无这一页。
- **修**：`_listImageFiles` 排序后加 `where((f) => !_isCoverLikeFile(f))` 过滤，排除后为空才回退保留全部（避免目录只有封面时成空页）。服务端已有 `_isCoverLikeFile`（`local_resource_scanner.dart:1799`，与客户端 `_isCoverLikePath` 同款 `cover.jpg/jpeg/png/webp`），直接复用。
- 注意：`_listDirectVisibleImages`（1718）早已有同等排除（1731-1732），只是深扫路径漏了。

### ❌ 不要
- **不要**另写一份 cover 判断规则——用已有 `_isCoverLikeFile`。

### 涉及文件
- `lib/server/local_resource_scanner.dart`（`_listImageFiles`）

---

## §22 网页端详情页完整 redesign（双胶囊 / 72×72 阅读方块 / 章节网格固定宽）

> 精简版对应：[详情页 redesign](交接文档-精简最新版.md#网页后台--浏览器应用)。

### 信息区（旧 vs 新）
- **旧**：`infoChipHtml` 单个合体 accent 胶囊「字段名：值」。
- **新**：`infoRowHtml` → `.app-info-row`/`.app-info-key`/`.app-info-val`——字段名胶囊（带 accent 色调、圆角 12）+ 值胶囊（中性容器普通字色、圆角 12），双胶囊逐行，对齐客户端逐行字段。

### 操作区（旧 vs 新）
- **旧**：多个按钮（复制标题 + 阅读等）。
- **新**：只留一个 **72×72「阅读」方块**（图标在上、文案在下，圆角 16），去掉复制标题。

### Hero 封面
- 132×… → **102×136**、圆角 16→12、标题字号→18（对齐客户端详情页封面与标题比例）。

### 章节网格
- `grid-template-columns: repeat(auto-fit, minmax(220px, 1fr))` → 单章被 `1fr` 拉满整行显空。
- 改 `repeat(auto-fill, minmax(160px, 200px)) + justify-content:start`：固定单元宽 ≤200、靠左，对齐客户端 `maxCrossAxisExtent:200`。

### 涉及文件
- `assets/web_console/js/app.js`（`infoRowHtml` 替换 `infoChipHtml`、章节网格渲染）
- `assets/web_console/css/app.css`（`.app-info-row`/`.app-info-key`/`.app-info-val`、操作区、Hero 尺寸、章节网格）
## §14 当前仍存在的已知问题

1. **池泄漏边缘场景**：Dart `HttpClient.abort()` 在 response 已开始流式但 stream 卡住时的释放行为，需反复点击 + reader 看 5+ 本验证。
2. **`findItemByCandidates` 慢**：首次点击需要 `fetchItems()` + `fetchItemDetail()` 两次串行。治本需要服务端在 payload 回传 `itemId`。
3. **`fetchFavoritesInFolder` 串行**：18 个文件夹 18 次串行，可改 `Future.wait` 并行（控并发 4-6）。
4. **reader dispose 时的请求清理**：当前只 cancel UI 流，未主动 abort 网络请求。
5. **Shizuku DeadObjectException**：大 `download.db` 偶发，当前回退/跳过。
6. **Android 前台服务**：整条链未关闭（通知/保活/系统限制适配）。
7. **局域网扫描**：仍需多网卡/多网段真机复核。
8. **移动端输入体验**：地址输入、不同 ROM 系统栏一致性仍需逐项关闭。
9. **加密包跨重启封面稳定**：session password 重启即丢，需持久化"已解锁过"状态。
10. **客户端远程档鉴权 401（已修复，二次回归点已记录）**：V1.9.13「网页后台正式落地」曾把 `_requiresAuthorization` 改成**所有 `api/` 都鉴权**（除 `status`/`api/console/login`），后续又出现同类回归；而客户端 `RemoteLibraryClient._sendRequest` **从不带 token**（交接文档明确 Flutter 客户端无账号体系）→ 远程档列表/封面/阅读全 401。**正确边界**：`_requiresAuthorization` 只拦 `api/admin/*`、`api/console/*`、`api/remote/proxy`；放行 `api/library`、`api/library/*`、`api/events`、`status` 与网页静态资源。勿给客户端硬塞 token 体系，也不要再把所有 `api/` 纳入网页后台鉴权。
11. **资源热刷新平台限制**：服务端已加目录 `watch(recursive:true)` + 3s 去抖 rescan（`_handleLibraryFileEvent`/`_rescanResourcesFromWatch`，重入守卫 `_libraryWatchRescanRunning`，失败降级记日志）。但 `Directory.watch` 在 Android 受限存储/网络盘可能不可靠或抛异常——已 try/catch 降级，必要时仍依赖 `/api/library/items/refresh` 手动兜底。
12. **网页加密包 unlock 经远程代理**：`remote_proxy_handler` 已扩展支持 POST 转发 unlock；但远程代理仍只在登录后可用、不透传 Cookie/Authorization（安全基调不变）。

---

## §24 本会话计划与执行记录

> 本节只记录本会话出现过的计划名、执行状态和后续接手边界，避免重复翻历史上下文。

### 计划名索引

- `继续定位远程资源_401_d05b2fa1.plan.md`：远程资源 401 回归定位计划；已取消继续诊断。保留结论：Flutter 客户端远程档没有网页 token，`api/library`、`api/library/*`、`api/events`、`status` 必须放行，只保护 `api/admin/*`、`api/console/*`、`api/remote/proxy`。
- `图集资源路径合集开关计划_08f414dc.plan.md`：早期较宽泛的图集资源路径合集开关计划；已被更准确的外壳目录识别计划替代。
- `图集合集外壳目录识别计划_5c3a4ec8.plan.md`：已执行。实现每资源路径“合集”开关、本地/服务端/远程扫描与切换接口、图二顶栏开关；修复关闭时项目名显示 `1`、开启时章节名前缀带主文件夹、合集/远程多章节图集“序号”开关消失。
- `mDNS_与网段扫描修复计划_1757ef93.plan.md`：mDNS / Bonjour 与网段扫描初版修复计划；用于确认 Windows 服务端 mDNS 多网卡风险和网段扫描多服务目标。
- `mDNS_与网段扫描修复计划_48e20d37.plan.md`：已执行网段扫描部分。核心修复是删除 `service_data_source.dart` 中 `scan()` 命中任意候选后提前 `break` 的行为，让显式“网段扫描”和 mDNS 兜底扫描都能聚合同网段多个 `/status` 服务。
- 第八轮 45/46/47 号计划：已完成，详见 §78-§80。
- 第九轮 02/03/05/06/07 号计划：代码与自动化验证已完成；04 号计划部分完成，详见 §81-§84。
- 第十轮 00/01/02：01 号代码验证完成，02 号计划部分完成，真实记录与实体机验收待补，详见 §85。
- 第十轮 03：本地详情布局、EH 本地在线详情/更新信息、关于页代码与自动化验证完成；真实 EH 警告/写库、设备端和外部链接验收待补，详见 §86。
- 第十一轮 01：AI 结果清单参数规范化、工具重试控制流、旧会话兼容和两个 UI 入口已完成代码与自动化验证；截图对应会话完整冷启动、真实模型随机错误参数重试、Android 10 与真实网络日志待补，详见 §87。
- 第十二轮 00/01/02/03：00 诊断完成；01 凭据脱敏代码闭环但旧 Key 未轮换；02 稳定前缀主体通过但存在两个 P1 范围语义风险；03 验收总体部分通过，详见 §88-§89。
- 第十二轮 04/05：04 诊断完成；05 代理分流和 HTTP/WS 连接态重建代码与自动化验证完成，真实代理矩阵及 WebSocket 竞态专测待补，详见 §90。
- 第十三轮 01～12：全部部分完成；导出/标签采集/服务扫描/作者元数据/详情搜索复制/共享布局/已下载预览均已记录代码与自动化证据，真实设备、网络、系统交付和流式大文件边界待补，详见 §91。
- 第十四轮 01/03/04：已完成 mDNS 补扫合并、子网扫描优先级与无连接态发现区内联化，详见 §92；02 号 host 模式部署方案仍待执行，本次未纳入。
- 第十四轮 05/07/08：已完成目录名 UTF-8 字节截断、详情标题/作者自由选择复制和服务页 intrinsic 布局断言修复，详见 §93；05 未新增测试，07 的真机手势与 08 的极端宽度阈值仍待验。
- 第十四轮 06：部分完成；连接复用、并发对齐和阅读页缓存已落地，明确跳过探测移除、重试收敛和 HTTP/2，性能与代理漂移等现场验收待补，详见 §93。

### 当前执行状态

- 第十三轮 01～12 均为“部分完成”：最新共享工作区全量 `flutter test --no-pub --concurrency=1` 292 项通过，`flutter analyze --no-pub`、`dart format --output=none --set-exit-if-changed`、`git diff --check`、Android/Windows debug 构建通过。
- 已验证的工程边界不等于现场完成：Android/Windows 真机布局与输入、真实 EH/NH/JM/Picacg 数据、局域网扫描取消、Root/Shizuku、大文件流式、系统分享/剪贴板和远程旧协议兼容仍需补测。
- 继承的网络基础结论仍有效：网段扫描不能因当前地址先命中而提前停止；mDNS 查询回复走源地址单播并保留 QU 请求位，Windows 多网卡漏扫问题不能回退。
- 第十四轮 01/03/04 的计划回写和定向验证已完成；02 仍待执行，真实混合网络、现场 UI 交互与扫描取消时序按 §92 保留为风险边界。第十四轮 05/07/08 已完成，06 为部分完成；其文件系统、下载性能、详情手势和极端布局边界见 §93。

---

## §15 提交与构建注意事项

- **正确仓库**：`D:\Flutter_Projucts\PicaComic\PicaKeep`（不是外层 `PicaComic`）
- **正确分支**：`main`
- **提交前确认**：`git -C D:\Flutter_Projucts\PicaComic\PicaKeep status --short`
- **提交格式**：`Vx.y.z 中文标题` + 中文摘要正文
- **不要提交**：`android/local.properties`、`android/app/build/...`、`.dart_tool/`、`build/`、临时脚本、`.idea/`
- **校验命令**：
  - `flutter analyze` → 必须 0 issues
  - `flutter build apk --debug`（或 `gradlew :app:assembleDebug`）
  - `gradlew -p android :app:compileDebugKotlin`

---

## §25 Linux 桌面构建 — sqlite3 下载卡死

> 精简版对应：[Linux 桌面构建](交接文档-精简最新版.md#linux-桌面构建)

### 现象

- 远端 `flutter build linux --debug` 一直停在 `Building Linux application...`，无后续输出。
- 检查远端进程：cmake 连接 `194.195.208.62:443`（sqlite.org）处于 ESTABLISHED 但数据不流动（`wchan: do_sys_poll`）。
- `ps aux` 可看到 `cmake -P .../download-sqlite3-populate.cmake` 进程卡住。
- cmake configure 报错：`Cannot find source file: .../sqlite3.c`。
- 本地 tar → pscp → 远端解包后每次都复现，因为 `build/` 不在 tar 里、`_deps/sqlite3-src/` 被清空。

### 根因

`sqlite3_flutter_libs` 插件的 `linux/CMakeLists.txt` 使用 `FetchContent_Declare(sqlite3 URL https://sqlite.org/2026/sqlite-autoconf-3520000.tar.gz)` 从 sqlite.org 下载源码。`sqlite.org` 在中国大陆不可达（被墙），导致 cmake configure 阶段的 `FetchContent_MakeAvailable` 卡在 TCP 连接超时。

### 修复方案（已落地）

**捆绑 sqlite3 合并源码到项目 + CACHE 变量跳过 FetchContent**

1. **生成合并源码**：从 GitHub 下载 sqlite 源码（可直连）→ `./configure && make sqlite3.c` 生成 `sqlite3.c`（~9.4MB）和 `sqlite3.h`（~680KB）。
2. **放入项目**：存放在 `linux/sqlite3/sqlite3.c` 和 `linux/sqlite3/sqlite3.h`。
3. **修改 `linux/CMakeLists.txt`**：在 `include(flutter/generated_plugins.cmake)` 之前添加：

```cmake
# Pre-bundled sqlite3 amalgamation to avoid downloading from sqlite.org
set(sqlite3_SOURCE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/sqlite3" CACHE PATH
  "Pre-bundled sqlite3 amalgamation source directory")
set(sqlite3_POPULATED TRUE CACHE INTERNAL
  "Skip FetchContent download for sqlite3")
```

**原理**：`FetchContent_MakeAvailable` 内部检查 `sqlite3_POPULATED` 变量——如果为 TRUE，直接跳过下载。`sqlite3_SOURCE_DIR` 被设为 CACHE 变量，指向捆绑的合并源码目录。插件 CMakeLists.txt 中的 `add_library(... "${sqlite3_SOURCE_DIR}/sqlite3.c")` 会直接使用本地文件。

### 涉及文件

| 文件 | 角色 |
|------|------|
| `linux/sqlite3/sqlite3.c` | sqlite3 合并源码（~9.4MB，随项目提交） |
| `linux/sqlite3/sqlite3.h` | sqlite3 合并头文件（~680KB，随项目提交） |
| `linux/CMakeLists.txt` | 添加两行 CACHE 变量（在 `include(generated_plugins.cmake)` 之前） |
| 远端 `~/.pub-cache/hosted/pub.dev/sqlite3_flutter_libs-0.5.42/linux/CMakeLists.txt` | 插件原始 CMakeLists.txt（无需修改，CACHE 变量覆盖其 FetchContent） |

### 更新 sqlite3 版本的标准操作

当 `pubspec.lock` 中 `sqlite3_flutter_libs` 或 `sqlite3` 包版本升级时：

1. 确认新版本对应的 sqlite3 上游版本号（如 `3.52.0` → tag `version-3.52.0`）。
2. 下载源码：`curl -L -o /tmp/sqlite3.tar.gz 'https://github.com/sqlite/sqlite/archive/refs/tags/version-X.XX.X.tar.gz'`
3. 解压并生成合并文件：
   ```bash
   tar xzf /tmp/sqlite3.tar.gz --strip-components=1
   ./configure && make sqlite3.c
   ```
4. 替换 `linux/sqlite3/sqlite3.c` 和 `linux/sqlite3/sqlite3.h`。
5. 确认 `linux/CMakeLists.txt` 中的变量设置仍然正确。
6. 如果插件 CMakeLists.txt 中的 `FetchContent_Declare` URL 版本号也变了，CACHE 变量机制依然生效——只要 `sqlite3_POPULATED=TRUE`，FetchContent 就不会触发下载。

### 远程构建流程适配

远端构建流程（tar → pscp → rm -rf 解包 → flutter build）已无缝支持：
- `linux/sqlite3/` 目录不在 tar 的 `--exclude` 列表中，会随源码一起传到远端。
- 远端无需额外操作，cmake configure 自动使用捆绑的 sqlite3.c。

### ❌ 不要再走的路

- **不要在远端 pub cache 里改 CMakeLists.txt**：`flutter pub get` 可能覆盖，且无法随项目源码分发。
- **不要用 `file://` URL 指向远端固定路径**：`/home/azusa/.sqlite3/` 这类路径绑定特定机器，开源后别人没有。
- **不要只清理 lockfile 而不处理 FetchContent**：lockfile 只是 Flutter 工具锁，真正卡住的是 cmake 的 `ExternalProject/FetchContent` 下载。
- **不要尝试从 `sqlite.org` 直连下载**：该域名在中国大陆不可达，`ghproxy.net` 等代理也不稳定。
- **GitHub 源码 ≠ autoconf 包**：GitHub 下载的是开发树（无 `sqlite3.c`），必须运行 `./configure && make sqlite3.c` 生成合并文件后才能用。

### 诊断步骤（再次遇到时）

```bash
# 1. 检查是否有卡住的 cmake 下载进程
ps aux | grep -E 'cmake|sqlite' | grep -v grep

# 2. 检查是否在连 sqlite.org
ss -tnp | grep -E 'cmake|194.195.208'

# 3. 确认捆绑文件存在
ls -la linux/sqlite3/sqlite3.c linux/sqlite3/sqlite3.h

# 4. 确认 CMakeLists.txt 有 CACHE 变量
grep -n 'sqlite3_SOURCE_DIR\|sqlite3_POPULATED' linux/CMakeLists.txt
```

---

## §26 运行期性能优化（第二轮，profile 实测驱动）

> 本轮全部用 **profile 包 + DevTools 逐帧 + logcat 埋点**驱动，不靠肉眼/debug 包猜。
> 方法论铁律见 [启动性能完整分析 §6.7](./Z-plan/后续优化-第一轮/启动性能完整分析.md)：
> **性能归因必须 profile/release 包**，debug 的 build 耗时是 JIT 假象（启动那轮 debug 测出
> `runApp→firstPostFrame` 1.4s，profile 实测只有 ~40ms）。逐帧务必按 `startTime` 排序，
> 别信 DevTools Frames 面板的帧号顺序。

### 26-1 详情页推荐全量重算（确定根因，已修）

- **现象**：进入/退出本地漫画详情页、下滑切 AppBar 标题，profile 实测 build 帧 180~196ms。
- **根因**：`local_comic_detail_page.dart` 每次 `build()` 都同步调 `_buildRecommendations()`，
  对全库 `_localItems`（可能几百上千项）逐项跑正则相似度（`_nameTopics`/`_normalizeName`/
  `_bigrams`/`_nameSimilarity`）+ 整体 sort。任意 setState（滚动切标题/翻页/dispose 前）都重算一遍。
- **修法**：结果缓存成字段 `_recommendationsCache` / `_recCacheComic` / `_recCacheMode`。
  原方法拆成 `_computeRecommendations(mode)`，缓存命中（`identical(_recCacheComic,_comic)` +
  同模式 + 未失效）直接返回。`_loadLocalItems` 的 setState 里显式 `_recommendationsCache=null`
  失效（因 `_localItems` 是 final 列表原地 mutate，identical 检测不到内容变化）。
- **诚实边界**：缓存只在「同一详情页实例」内生效。**每个详情页首次进入仍会算一次**（193ms），
  这是缓存的固有首次成本，不是 bug。下滑/翻页的重复 180ms 已消除（profile 复测降到 ~27ms）。
  要再降首次只能优化算法本身或挪 isolate，本轮未做。
- **顺带修**：推荐项点击从裸 `MaterialPageRoute` 改 `App.pushInner`（详见返回栈 Bug 文档）。

### 26-2 工具页预返回动画 saveLayer（推断性修复，已确认有效）

- **现象**：工具页预返回（predictive back）缩放动画，120Hz 下 raster 卡顿，trace `saveLayer×52`。
- **修法**：`tools.dart` 的 body 外包一层 `RepaintBoundary`，把整页栅格化成单层，
  返回缩放只合成一层纹理，避免每帧重做几十个卡片的离屏 saveLayer。
- **真机复核结论**：返回动画变顺，说明 saveLayer 来源确实在工具页本身（多张
  `Card.outlined(Clip.antiAlias)` + `AnimatedCrossFade`），推断成立，保留。
  （若当时无改善，分支方案是给被揭开的下层页面封面 tile 加 RepaintBoundary。）

### 26-3 已下载页转圈变长 = 远程探测阻塞本地加载（logcat 实锤，已修）

- **现象**：进「已下载」转圈 3.5s 才出列表（本地 4158 部）。用户反馈「修复后才变长」，
  但实为巧合——这次远程服务恰好离线，触发了一直存在的串行超时。
- **logcat 埋点实锤**（`[DLPROBE]` 分段计时）：
  ```
  checkRemoteAvailability done +3004ms  remoteAvailable=false   ← 3s 全耗在这（超时）
  _loadComics done          +3466ms  count=4158               ← 本地加载只 462ms
  ```
- **根因**：`download_page.dart` 的 `reload()` 里 `remoteAvailable = await
  _checkRemoteAvailability()` **串行挡在** `_loadComics()` 前。远程离线时它走到 ~3s 超时。
  而 local 视图的 `_loadComics` 分支**根本不读 `remoteAvailable`**（只 aggregate/remote 视图读）。
- **修法**：local 视图直接 `remoteAvailable=false` 跳过探测，只 aggregate/remote 视图才 await。
  `showSourceSelector` 用 `_hasConfiguredRemoteServer` 兜底，跳过探测不影响视图选择器显示。
- **⚠️ 与踩坑第 1 条的区别**：附录踩坑说「不要给 `_checkRemoteAvailability` 加缓存掩盖问题」。
  本修法**不是加缓存**，是 local 视图压根不需要这个探测结果，所以不调用——没有掩盖任何远程问题，
  远程视图该探测照样探测。方向一致：不靠缓存粉饰，而是消除不必要的等待。

### 26-4 已下载/图集 loading 全屏遮罩 → 列表区转圈（已改）

- **现象**：切远程档时整页被居中转圈替换，AppBar/视图选择器都消失。
- **原则**（用户定）：任何档位加载都只在列表区转圈，加载完刷新视图显示，不整页覆盖。
- **修法**：`download_page.dart` 的 `build` 去掉 `logic.loading ? 整页转圈Scaffold : 正常`
  三元，始终渲染正常骨架；`_buildComics` 在 `comics.isEmpty && loading` 时返回
  `SliverFillRemaining` 居中转圈，否则才空状态。`SmoothCustomScrollView` 就是标准
  `CustomScrollView`，支持 `SliverFillRemaining`。

### 26-5 图集远程档滚动卡顿 = GC 风暴（实测定位，两段叠加修复）

- **现象**：滚远程图集列表，120Hz 下 jank 15~25%，但 **build/raster 全低（<4ms），
  elapsed 全靠 vsyncOverhead 撑高**（最重帧 vsync 100~300ms）。用户体感：
  「加载完封面不卡，卡在还在加载+显示封面时」。
- **排除法**（`[ALBUMPROBE]` 埋点，`local_library_page.dart`）：每秒 listItemBuild、
  `_coverImageProvider`、`_coverFile` 累计耗时——**全部 <1ms/秒**。Dart 侧 widget 构建
  与封面 provider 路径完全无辜（`BaseImageProvider` 的 `==`/`hashCode` 基于 key 正确，
  缓存命中正常）。
- **真凶（trace 关键字）**：`CollectOldGeneration` / `Scavenge` / `StartConcurrentMark` 大量出现。
  **老年代 GC 暂停**就是 vsync 100~300ms 尖峰的来源。滚动时大量封面字节在堆上快速分配/驱逐，
  撑爆新生代、晋升老年代，触发昂贵 old-gen GC，主线程冻结。
- **修法（两段，都在 image_loader 基建层，默认不影响其他子类）**：
  1. **限尺寸解码**：`base_image_provider.dart` 加 `int? get targetDecodeWidth`（默认 null）。
     子类返回宽度时，`decode` 走 `getTargetSize` 按宽缩放解码。
     `_RemoteLibraryCoverImageProvider` override `=> 480`（封面显示约 150~200px，高 DPI 留余量）。
     远程封面原图可能数千 px，全尺寸解码是解码耗时+内存的浪费。
  2. **绕过堆字节缓存**：`base_image_provider.dart` 加 `bool get cacheRawBytes`（默认 true，
     保持现有行为）。`_RemoteLibraryCoverImageProvider` override `=> false`——它已有磁盘缓存
     （`_RemoteLibraryCoverDiskCache`），原始压缩字节不必再进那个 50MB 静态堆 `_cache`，
     堆字节驻留正是老年代 GC 的压力源。
- **profile 实测（06 改后 vs 05 仅限尺寸，均 120Hz 可比）**：
  ```
  maxVsync   303ms → 51ms   ← 最严重尖峰降到 1/6（体感最大改善：不再几百ms冻顿）
  putIfAbsent 195  → 33     ← 绕堆缓存后图片缓存进出大减
  NewGC      153  → 85      ← 新生代 GC 近半
  ```
- **诚实边界**：OldGC 次数没明显减少（解码后的 image 对象仍晋升），但**单次代价大降**
  （sumVsync 也降）。剩余 ~13% jank 集中在「封面解码上屏瞬间」，是远程图片列表固有成本，
  120Hz 放大感知，60Hz 下更轻。继续挖（限并发解码/预解码）收益递减，本轮到此为止。

### 26-6 工具-缓存大小限制不生效（够不着 + 不触发，已修）

- **现象**：工具页显示「684MB / 524MB」，缓存超限却不降。
- **根因（读代码确认，双重失效）**：`_RemoteLibraryCoverDiskCache._trimToLimitInner`
  （`remote_library_data_source.dart`）——
  1. **范围错**：`totalBytes` 统计 `App.cachePath` + `dataPath/cache` 全部（684MB ✓），
     但可删文件池 `collectFilesUnder` **只收 `remote_library_covers` 子目录**。
     占大头的 `App.cachePath` 图片缓存算进总量却**删不到**。
  2. **时机窄**：trim 只在**远程封面下载后**触发（`ensureDownloaded` 末尾），平时不跑。
- **修法（保持原 LRU 设计：删最旧的到刚好低于限制，不清空）**：
  1. 可删文件池扩到 `App.cachePath` + `dataPath/cache` **全部**（与统计/`eraseCache` 一致），
     全局按修改时间 LRU，保护 `protectedPath`。
  2. 加公开入口 `RemoteLibraryDataSource.trimCacheToLimit()` → `_RemoteLibraryCoverDiskCache.
     trimToLimitNow()`（忽略节流、`_trimInFlight` 串行保护、无 protectedPath）。
  3. `tools.dart` 的 `_refreshCacheSize()` 统计前先 trim；`_editCacheLimit` 改完限制后调
     `_refreshCacheSize`，新限制立即生效。
- **缓存语义安全**：`App.cachePath`（`getApplicationCacheDirectory`）和 `dataPath/cache` 都是
  可被系统随时清理的缓存目录，`eraseCache` 本就全删，LRU 删除无副作用。

### 26-7 profile APK 体积翻倍 = 增量打包空洞（非代码问题）

- **现象**：反复构建后 `app-profile.apk` 从 ~45MB 涨到 93.6MB。
- **解剖**（扫本地文件头）：真实压缩内容仅 43MB，但有 **52MB 对齐填充**——845 个空名、
  comp=0、extra≈64KB 的孤儿条目（不在中央目录里，`zipinfo` 只认 1187 个真实文件）。
- **根因**：AGP 8.12 zipflinger **增量打包**，反复 build 删改条目留下的空隙不压实，
  累积成空洞。**非代码/资源/配置问题**。
- **修法**：`flutter clean` 全量重打包即压实（93.6MB → 41.1MB）。无需改任何 gradle 配置。
- **⚠️ 教训**：清产物用 `flutter clean`，**不要手删 `build/app/intermediates/` 半截目录**——
  会破坏 Gradle 增量状态一致性，导致「找不到 launch activity」假错误。

### 涉及文件（§26 全部）

- `lib/pages/local_comic_detail_page.dart` — 推荐缓存（26-1）
- `lib/pages/tools.dart` — RepaintBoundary（26-2）、缓存 trim 触发（26-6）
- `lib/pages/download_page.dart` — local 跳过远程探测（26-3）、列表区转圈（26-4）
- `lib/pages/local_library_page.dart` — 图集页（26-5 探针已撤）
- `lib/foundation/image_loader/base_image_provider.dart` — targetDecodeWidth + cacheRawBytes 钩子（26-5）
- `lib/foundation/remote_library_data_source.dart` — 远程封面 480px+绕缓存（26-5）、trim 范围+入口（26-6）
- 分析文档：`Z-plan/后续优化-第一轮/运行期卡顿分析-推荐重算与返回动画.md`

---

## §27 本地已下载列表滚动卡顿

> 精简版对应：[§ 本地已下载性能与图集封面特权修复](交接文档-精简最新版.md#本地已下载性能与图集封面特权修复第三轮rootshizuku-覆盖)
> 数据来源：`Z_性能数据/01~06`，60/120Hz profile 包 + DevTools 逐帧

### 27-1 常驻后台封面缓加载（已删）

- **现象**：root 模式本地「已下载」滚动卡顿，退出页面/应用后仍触发 root 权限提示。
- **根因**：`_warmManagedDownloadCache` 挂在 `LocalLibraryManager` 单例上，通过 `_backgroundCacheTask` 链式 fire-and-forget，遍历全部 items 逐项走 root 特权通道迁移封面，不绑任何页面生命周期。
- **修法**：删 `_warmManagedDownloadCache` 及调用点、`_backgroundCacheTask` 字段、`_resolveCoverPathForCacheOnly`、`_isCoverCachePaused`/`pauseBackgroundCoverCache`/`resumeBackgroundCoverCache` 死代码。封面缓存完全惰性化。

### 27-2 按需迁移避开活跃滚动

- **现象**：删后台任务后，滚动中按需迁移仍在帧间隙打 root 通道（vsync 尖峰 67ms）。
- **修法**：`_drainCoverResolveQueue` 循环开头加 `if (_isScrollInteracting) break;`（在 `removeFirst()` 之前），活跃滚动只入队不打通道；`ScrollEndNotification`→140ms idle→重启 drain 补齐。闭环：滚动流畅→停下补封面。

### 27-3 本地封面降采样解码

- **现象**：折返滚动 / 周期性卡顿，封面全分辨率解码（800×1200→150px 格子），大位图快速填满 image cache 触发老年代 GC。
- **修法**：`_DownloadedPageComicTile` 开 `super.optimizeCoverDecode = true`，按布局宽×dpr×1.35 做 `ResizeImage` 解码期降采样，不压缩源文件。

### 27-4 无封面项负缓存

- **现象**：无封面 managed 项每次折返走 ~11 次特权通道重探（cover.* exists + listDir 多轮）。
- **修法**：
  - `_ensureManagedDownloadCoverCache`：解析失败持久化 `noCoverSentinel` 到 source cache JSON，下次直接返回 null。
  - `resolveCoverPathForItem`：哨兵直接短路，不走 fallback。
  - `_coverResolveFailedIds`（页面级 Set）：同会话折返拦截。
  - `reload()` 调 `invalidateNoCoverSentinels()` 清除标记（用户主动刷新时重新探测）。
- **诚实边界**：无封面项本就只能显示占位图标，持久化标记让它跨会话都不再触发 root 通道。

### 27-5 trustStorageFromDatabase 假阳性

- **现象**：滑到 db 有记录但目录已删除的项时卡顿。
- **根因**：root 模式下 `trustStorageFromDatabase=true` 把 db-only 项一律标为 `localStorageExists=true`，入封面队列后逐个走特权通道探测不存在目录。
- **修法**：两处 `? true` 改为 `? _managedDownloadDirectoryExistsInIndex(...)`（查内存 Set，零 I/O）。`sourceDirectoryNames` 在加载开头已一次性列出，纯内存比对。

### 涉及文件（§27）

- `lib/foundation/local_library.dart`（27-1 删后台缓存、27-4 哨兵+refresh、27-5 trustStorage）
- `lib/pages/download_page.dart`（27-2 滚动暂停 drain、27-3 optimizeCoverDecode、27-4 页面负缓存）

---

## §28 root/shizuku 下图集封面破图

> 精简版对应：[§ 本地已下载性能与图集封面特权修复](交接文档-精简最新版.md#本地已下载性能与图集封面特权修复第三轮rootshizuku-覆盖)
> 共 10 轮递进修复，核心根因链条：解析层→渲染层→provider 内短路，三层全堵死了 root/shizuku 的外部存储读取

### 28-1~28-7 核心根因与最终修复

**为什么拖了 10 轮**：

1. **解析层**（第 1-2 轮）：`resolveLocalComicCoverPath` 同步路径无目录扫描兜底；`PrivilegedStorageAccess` de:io `existsSync=true` 但 `listSync` 受 scoped storage 静默空→特权通道永远不触发。
2. **渲染层**（第 3-7 轮）：图集封面全用裸 `FileImage`/`Image.file`，不经过特权通道 → 解析层算出了路径但渲染层读不出字节。
3. **provider 内短路**（第 8 轮，真正开关）：`imageProviderForLocalPath` 自身 `existsSync()` 对 root/shizuku 外部路径恒 true→裸 FileImage→`StreamImageProvider` 特权回退从不触发。这就是「没能用上权限」。

**最终修法**：

- `PrivilegedStorageAccess.listDirectoryEntries`/`readFileBytes`：dart:io 空结果时强制回退特权通道。
- 图集封面渲染统一改为 `imageProviderForLocalPath`（privileged-aware），去所有裸 `FileImage`/`Image.file`。
- `imageProviderForLocalPath` 特权模式跳过 `existsSync` 快路径，直走 `StreamImageProvider`+`_readFileBytes`（先 dart:io，空时回退特权通道）。
- 非特权（full-access/桌面/沙箱）：existsSync 可靠→FileImage 快路径，零回归。

### 28-8 外层 source 卡片拼贴 + 惰性 provider

- 外层 source 卡片加 `_LocalRootCollage` 2×2 子项封面拼贴。
- `coverImageProviderForItem` 惰性 provider：封面由 provider 自身惰性拉取，不依赖外部 setState 时序，消除退出再进封面闪空。

### 28-9 底栏/侧栏面板封面

- `DownloadedComicInfoView._buildCover` 本地项统一走 privileged-aware provider，去裸 `Image.file`。

### 28-10 撤销权限闪退定性

- 撤销 MANAGE_EXTERNAL_STORAGE 时 Android 强制 kill 应用进程是 OS 级固有行为，非 bug 不修。

### 涉及文件（§28）

- `lib/foundation/local_library.dart`（回写 localCoverPath、resolveChildCoverPath、惰性 provider、imageProviderForLocalPath 特权短路）
- `lib/foundation/privileged_storage_access_flutter.dart`（空回退）
- `lib/pages/local_library_page.dart`（_LocalRootCollage、_coverImageProvider 惰性）
- `lib/pages/local_comic_detail_page.dart`（异步兜底、去 existsSync、惰性回退）
- `lib/pages/download_page.dart`（drain 回写统一、底栏面板 provider）

### ❌ 不要再走的路

- **不要**在 root/shizuku 下用裸 `File(path).existsSync()` 判断外部路径封面是否存在——挂载点可见但读不了，恒 true。
- **不要**用裸 `FileImage`/`Image.file` 渲染图集封面——必须走 `imageProviderForLocalPath`。
- **不要**给 `imageProviderForLocalPath` 加 existsSync 快路径拦截——特权模式下它就是堵死特权通道的短路器。
- **不要**把「侧栏/底栏能显示封面」当成特权通道可用的信号——阅读器侧栏（eps_view）根本不渲染封面图，只显示章节文字列表。

---

## §29 单项源直开与图集计数改为子项之和

> 精简版对应：[§ 交互优化与开屏剪贴板 ANR 修复](交接文档-精简最新版.md#交互优化与开屏剪贴板-anr-修复)

### 29-1 单项目录源卡片直开详情

- **现象**：自定义路径源只有一个子项时，点进去还要多一层中间列表才能进详情。
- **修法**：`local_library_page.dart` 的 `_openItem`：子项数=1 且 `findCachedById` 命中时直接 `App.pushInner` 进详情，跳过中间列表。多子项源仍进列表，不变。
- **配套**：`_LocalRootCollage` 只解析到 1 张封面时铺满卡片（`BoxFit.cover`），不走 2×2 GridView 留白。

### 29-2 计数改为卡片子项之和

- **现象**：「我」页面图集数 < 列表各卡片子项之和（`.qq` 合集 8 个子项只被算 1 个）。
- **根因**：`cachedAlbumCount` 按 `isAlbum` 过滤 `_items`——自定义下载目录型源扫出的子项 `sourceDisplayName='本地扫描'` 不满足 `isAlbum`，漏数。但列表卡片按 `supportsCollectionShell` 判定，两个维度不一致。
- **修法**：新增 `cachedAlbumChildrenCount`，直接对 `_storageEntries` 中 `supportsCollectionShell` 的 `comicCount` 求和，与列表卡片 subtitle 严格同源。

### 涉及文件（§29）

- `lib/pages/local_library_page.dart`（_openItem 单项直开、_LocalRootCollage 单封面铺满）
- `lib/foundation/local_library.dart`（cachedAlbumChildrenCount、cachedVisibleCount albumOnly 分支）
- `lib/pages/me_page.dart`（计数取 cachedAlbumChildrenCount）

---

## §30 开屏剪贴板大文本 ANR 修复

> 精简版对应：[§ 交互优化与开屏剪贴板 ANR 修复](交接文档-精简最新版.md#交互优化与开屏剪贴板-anr-修复)

### 现象

冷启动后「我」页面图集数量迟迟不刷新，真机 profile 定位到一次 **6.4 秒主线程冻结**。

### 根因（决定性证据）

MIUI logcat：`APP_SCOUT_HANG duration=5006ms` → `PerfMonitor longMsg wall=6389ms`。
Java 栈卡在 `JSONStringer.string → Formatter.format`——Flutter `Clipboard.getData` 的返回值
整段塞进 `JSONMethodCodec` success envelope，在 Java 主线程逐字符转义 JSON 字符串。
剪贴板有超大文本（截图自动写入或长文复制）时编码就要数秒。

触发时机：`main_page.dart` 启动 +2.8s `_scheduleClipboardCheck → checkLocalClipboard → Clipboard.getData`。
期间所有 `setState`（含 me 页面计数刷新）都被主线程冻结堵住。

> Dart-timeline perfetto trace 是盲区：卡顿在原生 Java 线程（JSON 编码），Flutter trace 不给它打 slice。

### 修法（必须走原生通道，纯 Dart 改不了）

`Clipboard.getData` 无参数限制读取长度。绕开内置通道：

- **Android 原生**（`MainActivity.kt`）：新增 `lingxue.picakeep/clipboard` MethodChannel，
  method `getText(maxChars)`。`primaryClipDescription` 非 `text/*` 时直接返回 null
  （MIME 短路跳过截图等 `image/*` 内容）；否则 `coerceToText` 后 `substring(0, maxChars)`。
- **Dart 端**（`local_app_links.dart`）：`_readClipboardTextTruncated(maxChars: 10000)` 调
  原生通道；`MissingPluginException` 时回退 `Clipboard.getData` 并在 Dart 侧截断。

### 验证

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| APP_SCOUT_HANG | 5006ms | 无 |
| PerfMonitor longMsg | 6389ms | 无 |
| JSONStringer 栈 | 有 | 无 |
| 最大卡顿 | 6.4s | 541ms（首帧光栅，一次性） |

### 涉及文件（§30）

- `android/app/src/main/kotlin/lingxue/picakeep/MainActivity.kt`（CLIPBOARD_CHANNEL + readClipboardText）
- `lib/tools/local_app_links.dart`（_readClipboardTextTruncated）

### ❌ 不要再走的路

- **不要**用 Flutter 内置 `Clipboard.getData` 在启动路径上读剪贴板——JSONMethodCodec 编码大文本会冻主线程。
- **不要**在 Dart 侧尝试截断——`Clipboard.getData` 无参数，整段文本已在原生侧编码完毕。


以下来自 `交接文档-临时-远程收藏与图片收藏.md` §5，逐条不删：

1. **不要**给 `_checkRemoteAvailability` 加缓存"掩盖"问题。它失败的真正原因 99% 是连接池被拖垮，不是探测本身慢。
2. **不要**把 `Uri.decodeComponent(segments[i])` 加回去。`pathSegments` 已自动解码。
3. **不要**在服务端 favorites handler 里 `await init()`。
4. **不要**用 `App.globalTo` 打开阅读器。永远用 `App.pushInner`。
5. **不要**用 `App.globalContext` 调 `ScaffoldMessenger.of`。用页面/tile 自己的 BuildContext。
6. **不要**把图片收藏远程档点击候选缩成只有 `image.id`，要带 `title` + 前缀变体 fallback。
7. **不要**在 timeout 路径里只 catch 不 abort。Dart `HttpClient` 的 socket 不会自动释放。
8. **不要**只给建连/收 header 包 `.timeout()` 就以为"请求有超时保护"。**响应体（body）流必须单独保护**：套 idle timeout，且在被取消/出错（未读完）时 `abort()`。裸 `await for(response)` 是这套 codebase 最隐蔽的池泄漏来源。
9. **不要**新增 `HttpClient` 实例（应通过 `_httpClient`/`_controlClient` 这两个），否则又开始单池竞争。
10. **不要**在"每张封面下载后"无脑跑全盘扫描类清理（`_trimToLimit`）。网格批量加载会把它放大成几十次扫盘，必须节流 + 不重入。
11. **不要**对 `getUrl()` / `openUrl()` 直接 `.timeout()` 就完事。**这是孤儿连接的来源**：超时只丢弃前端 future，底层开连接的请求还在跑，稍后成功返回的 `HttpClientRequest` 无人 abort，永久钉住一条连接；累积到 `maxConnectionsPerHost` 就全局假死，**只有热重启/重开 App 能恢复**（因为连接池在单例里）。**必须用 `_openWithTimeout`**：超时后给原 future 挂 `then`，等它最终返回 request 时 `abort()` 回收。判断是不是这个坑的铁证——logcat 全是 `getUrl` 的 5s TimeoutException、没有 404、且热重启后立刻恢复。
12. **不要**让 `maxConnectionsPerHost` 小于"所有并发闸上限之和"。否则正常并发就会 oversubscribe → `getUrl` 排队 → 超时 →（若没孤儿回收）雪球。当前 `_httpClient=24` ≥ 阅读闸 12 + 浏览闸 12。改并发闸上限时记得同步检查这里。
13. **不要**忘了释放并发令牌。`loadImageWithProgress` 拿令牌后：早退路径（404/超时/socket/其它异常）就地 `releasePermit()`；正常路径把释放责任交给 `_guardedImageBody` 的 `finally`（正常读完/出错/被取消都会释放）。漏一条路径就会泄漏槽位，最终把闸门耗尽变成假死。

---

## §32 退出阅读器停止图片加载与传输

> 精简版对应：[§ 阅读器退出停止加载与服务端持久化](交接文档-精简最新版.md#阅读器退出停止加载与服务端持久化)

### 现象

浏览大图包/压缩包时退出阅读器，在途远程图片请求仍在后台传输，占用连接与带宽。

### 根因

`StreamImageProvider` 的 `await for (final chunk in result.stream)` 无取消机制；reader dispose 后订阅仍继续消费流、底层 HTTP 仍在传输。`ComicReadingPageLogic` 退出清理未通知底层加载链路 abort。

### 修法

- `StreamImageProvider` 新增 `StreamImageAbortSignal`，加载时持有 `StreamSubscription`，收到 abort 后主动 `cancel()`。
- `ReadingData.createImageProvider` 与远程/本地实现支持传入 abort 信号；阅读页正文与预加载图片复用同一退出信号。
- 远程 `loadImageWithProgress` 支持 abort：等待限流许可、建连、响应头阶段均可响应退出；若 abort 先发生，后续拿到的请求补 `abort()` 避免孤儿连接；body 阶段由 stream cancel 触发现有的 `_guardedImageBody` finally 回收。

### 涉及文件

- `lib/foundation/image_loader/stream_image_provider.dart`
- `lib/foundation/remote_library_data_source.dart`
- `lib/pages/reader/reading_logic.dart`、`comic_reading_page.dart`、`reading_data.dart`、`image_view.dart`
- `lib/foundation/local_library.dart`

### ❌ 不要再走的路

- **不要**用翻页/换章的请求世代（`chapterLoadRequestId`）来取消退出时的图片请求——那个只能区分"新旧章节"，区分不了"退出"与"翻页"。
- **不要**只 cancel stream 不管底层 HTTP request——必须走到 `abort()`，否则连接泄漏。

---

## §33 全局搜索缺失项按开关占位或过滤

> 精简版对应：[§ 阅读器退出停止加载与服务端持久化](交接文档-精简最新版.md#阅读器退出停止加载与服务端持久化)

### 现象

全局搜索命中 db 有记录但本地文件不存在的项，渲染成一片空白灰背景。

### 修法

- 全局搜索已下载结果读取 `appdata.settings[localLibraryShowAllDatabaseRecordsSettingIndex]`，复用已下载页已有全局开关。
- 对 `LocalLibraryComicItem.isManagedDownloadItem && !localStorageExists` 的项：关时过滤不显示，开时显示文字+缺失占位。
- 封面处理对齐已下载页：本地无可用封面时传 `File('')`，由 `DownloadedComicTile` 显示缺失占位图标。
- 仅作用于已下载类结果；图集/收藏类结果不处理。

### 涉及文件

- `lib/pages/local_search_page.dart`

---

## §34 服务端数据持久化与路径配置（飞牛 Docker）

> 精简版对应：[§ 阅读器退出停止加载与服务端持久化](交接文档-精简最新版.md#阅读器退出停止加载与服务端持久化)
> 飞牛部署说明：`Z-plan/新需求-第一轮/03-飞牛-Docker-持久化部署说明.md`

### 34-1 持久化根目录可配置

- **现象**：飞牛 Docker 里用 deb 包跑，配置写在容器临时路径，重启容器即丢。
- **根因**：`App.dataPath = getApplicationSupportDirectory()`，容器内非持久。
- **修法**：新增 `PICAKEEP_DATA_DIR` 环境变量，服务端 `picakeep_server.json`、`library_trash.json`、`web_console.db`、`local_favorite.db`、`history.db` 统一落持久化根；未设置时回退原路径；同时设置 `--config` 时 `PICAKEEP_DATA_DIR` 控制数据目录、`--config` 只控配置文件位置。

### 34-2 Linux 目录选择器存储空间快捷导航

- **修法**：服务端新增 `GET /api/admin/volumes`，扫 `/vol*` 过滤存在 `/<vol>/1000/` 的卷；管理后台目录选择器弹窗渲染为快捷入口，点击直接跳转。仅 Linux 生效。

### 34-3 收藏/DB 路径可预设

- `server_config` 新增 `managedDataRoot` 字段；`LocalFavoritesManager` 与 `HistoryManager` 初始化和运行时统一走该根路径。
- Windows 默认 = PicaKeep 应用 support 目录（解析 `lingxue`/`picakeep` 包名，非原项目路径）。

### 34-4 飞牛 Docker 部署约定

- 必须挂载 `PICAKEEP_DATA_DIR` 指向宿主持久目录，否则配置重启即丢。
- 必须挂载宿主 `/vol1 /vol2 /vol3` 进容器，否则快捷导航扫不到。
- 代码与挂载配套，无挂载则快捷导航为空。

### 涉及文件

- `lib/foundation/app.dart`、`lib/foundation/local_data_source.dart`
- `lib/server/server_app.dart`、`lib/server/local_server_runtime.dart`、`lib/server/headless_cli_server.dart`
- `lib/main.dart`
- `assets/web_console/js/admin.js`

### ❌ 不要再走的路

- **不要**在 Docker 里不挂 `PICAKEEP_DATA_DIR` 就指望配置持久——容器内默认路径不持久。
- **不要**把宿主卷挂载和快捷导航拆开理解——没挂载进容器的卷代码扫不到。


---

## §35 收藏/历史 DB 路径修复与网页配置入口

> 精简版对应：[§ 服务端配置与网页后台修复](交接文档-精简最新版.md#服务端配置与网页后台修复)

### 现象

飞牛 Docker 下收藏夹/图片收藏返回 500，重启后收藏丢失。

### 根因

`main.dart` 启动时 `setManagedDataRootOverride(App.dataPath)` 把 db root 强制绑成 `PICAKEEP_DATA_DIR`（`/data/picakeep`），但真实收藏/历史 db 在库目录的 `pica` 子文件夹下（`/vol3/1000/C盘存储_本子/pica/local_favorite.db`）。于是去空目录找 db → 500。

### 修法

- 解除 `main.dart` 错误绑定。新增 `managed_data_root_resolver.dart`：手动配置优先；未配置时按 download.db 所在目录上一层锚定；找不到回退为空。
- `server_config` 新增 `managedDataRoot`，`/api/admin/config` 读写。服务端启动/热更新/配置保存后重定位 `LocalFavoritesManager`/`HistoryManager`。
- 网页服务配置页加「收藏/数据库存放路径」输入框+目录选择器。

### 涉及文件

- `lib/server/managed_data_root_resolver.dart`（新增）
- `lib/server/server_config.dart`、`lib/server/server_app.dart`、`lib/server/local_server_runtime.dart`、`lib/server/headless_cli_server.dart`
- `lib/main.dart`、`assets/web_console/js/admin.js`

---

## §36 Linux 目录选择器存储空间快捷导航修复

> 精简版对应：[§ 服务端配置与网页后台修复](交接文档-精简最新版.md#服务端配置与网页后台修复)

### 现象
快捷导航显示「vol3」而非「存储空间 3」，点击停在 `/vol3` 而非 `/vol3/1000`，且默认塞不存在的库根导致「暂无快捷根」。

### 修法
- 快捷项数据结构改为 `{label, jumpPath}`：`label`=「存储空间 N」，`jumpPath`=`/volN/1000`。
- 过滤仅物理卷 `/vol[1-9]\d*`，排除 `/vol00`、`/vol01`（软链接聚合）、`/vol02`。
- 不再把 `/` 和不存在的 `currentDownloadRoot` 塞入快捷区。

### 涉及文件
- `lib/server/server_app.dart`、`lib/server/headless_cli_server.dart`、`assets/web_console/js/admin.js`

---

## §37 网页后台管理面板 UI 美化与简化

> 精简版对应：[§ 服务端配置与网页后台修复](交接文档-精简最新版.md#服务端配置与网页后台修复)

- 重写 `admin.css`：8px 圆角、浅色工作台、克制边界、轻阴影、统一表格/表单/按钮/状态样式。
- 覆盖 9 个 tab：概览、本地资源、回收站、收藏夹、图片收藏、账号管理、服务配置、运行日志、接口。
- 目录选择器替换 emoji 为 CSS 绘制的稳定 glyph。
- 仅改管理面板（`admin.js`/`admin.css`），不碰应用面板与客户端。

### 涉及文件
- `assets/web_console/css/admin.css`、`assets/web_console/js/admin.js`

---

## §38 服务端配置持久化修复（网页配置重启丢失）

> 精简版对应：[§ 服务端配置与网页后台修复](交接文档-精简最新版.md#服务端配置与网页后台修复)

### 现象
跨平台复现：网页保存配置后刷新不丢、重启才丢。飞牛 Docker 全丢；Windows GUI 只丢网页特有项。

### 根因
两套数据源单向覆盖：网页 PUT 只写文件不写 `appdata.settings`；`_resolveEffectiveConfig` 从文件读到正确值后用 `appdata.settings`（headless 下全空）`copyWith` 覆盖。

### 修法
- `_resolveEffectiveConfig` 增加 `_isServerAuthoritativeMode`：`_configPathOverride != null` 或 `settings[97]==server` 时直接返回文件 `baseConfig`，不再用 appdata 覆盖。
- 配置文件改名 `picakeep_server.json` → `picakeep_server.data`（内容仍是 JSON），防止被无关程序乱扫。
- 客户端 GUI 态完全不动（`else` 分支未改）。

### 涉及文件
- `lib/server/local_server_runtime.dart`、`lib/server/server_config.dart`


---

## §39 服务端常量集中化

> 精简版对应：[§ 服务端常量集中化与 server_app 拆分](交接文档-精简最新版.md#服务端常量集中化与-server_app-拆分)

- 新增 `defaultServiceAdminPortInt = 9527`（int 版），解决端口字段为 int 时 String 常量无法直接复用。
- `server_config.dart`：`defaults()` 的 `port:` 和 `_normalizePort` 回退的裸 9527 → 引用 `defaultServiceAdminPortInt`。
- `local_server_runtime.dart`：端口回退的裸 9527 → 同上。
- `base.dart:132`：settings 槽位默认值旁加注释关联，不改值。
- 严格遵守「绝不能碰」清单：UI 示例 IP、`0.0.0.0` 校验、客户端端口解析、`bin/picakeep.dart` 空壳均未触碰。

### 涉及文件

- `lib/foundation/app_runtime_mode.dart`、`lib/server/server_config.dart`、`lib/server/local_server_runtime.dart`、`lib/base.dart`

---

## §40 拆分 server_app.dart

> 精简版对应：[§ 服务端常量集中化与 server_app 拆分](交接文档-精简最新版.md#服务端常量集中化与-server_app-拆分)

### 背景

`server_app.dart` 原是 2917 行单一上帝类 `PicaKeepAdminServer`，~90 个方法 + 20+ 私有字段。

### 拆分方案

纯结构重构：使用 `extension on PicaKeepAdminServer` 写在 9 个 part 文件，共享宿主 import 与私有成员。
宿主 252 行（只剩 import + part 声明 + 字段 + 生命周期）。

| part 文件 | 行数 | 职责 |
|---|---|---|
| `server_app_routing.dart` | 228 | 中间件 + 主分发 + CORS/auth + WebSocket |
| `server_app_browse.dart` | 200 | browse handler + 存储卷/根目录 + `_AdminBrowseRoot` |
| `server_app_console.dart` | 222 | console/history handler |
| `server_app_trash.dart` | 284 | 批量 trash + trash handler + payload |
| `server_app_library.dart` | 693 | library roots/handler + deep scan + payload |
| `server_app_favorites.dart` | 402 | favorites handler + payload |
| `server_app_files.dart` | 153 | 文件/归档响应 + content-type |
| `server_app_status.dart` | 139 | status/summary/device/URL payload |
| `server_app_util.dart` | 381 | path 工具 + body 解析 + `_jsonResponse` + 快照/签名 |

### ❌ 不要再走的路

- **不要**在拆分时夹带任何逻辑修改——纯重构可验证性一破，出问题难定位。
- **不要**把 part 文件放进子目录——同目录 `part of` 最稳妥，无路径换算风险。

### 涉及文件

- `lib/server/server_app.dart`（宿主）+ 9 个 part 文件（新增）


---

## §41 拆分 local_library.dart（深度）

> 精简版对应：[§ 服务端常量集中化与 server_app 拆分](交接文档-精简最新版.md#服务端常量集中化与-server_app-拆分)

### 背景

`local_library.dart` 原 4089 行，`LocalLibraryManager` 独占 3424 行/92 方法，是全项目最大 god class。

### 分两步

**第一步（01，纯移动）**：extension+part 模式迁出 35 个安全成员，拆出 4 个 part（manager_settings/query/cover/archive），宿主 4089→3850。

**第二步（02，放宽约束）**：extension 内裸调用类内 static 会编译失败，故将 69 个 private static 提为文件级顶层私有函数，调用点仅去 `LocalLibraryManager.` 前缀。之后新增 scan+static 两个 part，宿主 3850→1376。

### 最终结构

| 文件 | 职责 |
|---|---|
| `local_library.dart`（宿主，1376行） | import + part 声明 + 14 个顶层类 + manager 入口 |
| `local_library_manager_settings.dart` | 配置路径/下载根/权限判定 |
| `local_library_query.dart` | 刷新/加载/查询 |
| `local_library_cover.dart` | 封面缓存/解析/哨兵 |
| `local_library_archive.dart` | 归档/collection shell/单图册 |
| `local_library_scan.dart` | 源构建与扫描 |
| `local_library_static.dart` | 69 个顶层私有函数（db解析/路径/排序/文件IO） |

### 验证

- `flutter analyze` 全绿、方法集合零增删、函数体指纹一致、`replaceAll` 无改动、build 通过。
- public static（`instance`/`noCoverSentinel`/`archiveDisplayChapterNames` 等）保留在类内不动。

### 涉及文件

- `lib/foundation/local_library.dart`（宿主）+ 6 个 part 文件


---

## §42 修复 02 拆分引入的 analyzer 误报

> 精简版对应：[第三轮修复](交接文档-精简最新版.md#第三轮修复analyzer远程收藏安卓返回加密包配置共用)

- **现象**：02 拆分后 `flutter analyze` 报 11 个 undefined error，但微信/Android build 均通过。
- **根因**：`local_library.dart` 变成含 6 个 part 的库后，analyzer 对其普通 import 的 `local_library_settings.dart` 产生 library 归属竞争（CFE 不受影响，故构建一直通过；仅 analyzer 误报）。
- **修法**：3 个引用方对 settings 的 import 统一为 package 路径，`local_resource_scanner.dart` 加 `show`。零逻辑改动，拆分保留。

### 涉及文件
- `lib/foundation/privileged_storage_access_flutter.dart`、`lib/server/server_config.dart`、`lib/server/local_resource_scanner.dart`

---

## §43 修复远程收藏点击不能跳转

> 精简版对应：[第三轮修复](交接文档-精简最新版.md#第三轮修复analyzer远程收藏安卓返回加密包配置共用)

- **现象**：手机/网页远程连服务端，收藏能显示但点不动。Win 本体正常。
- **根因**：服务端已下发 `itemId`，但客户端 `RemoteFavoriteItem.fromJson` 未解析，退化成 candidates 弱匹配（target 与 remoteId 命名空间不同）→ 失败。
- **修法**：`RemoteFavoriteItem` 新增 `itemId`/`displayId` 字段，点击优先 `fetchItemDetail(itemId)` 直连，itemId 为空时回退 candidates。

### 涉及文件
- `lib/foundation/remote_library_data_source.dart`、`lib/pages/favorites/main_favorites_page.dart`

---

## §44 修复安卓历史返回错落收藏页

> 精简版对应：[第三轮修复](交接文档-精简最新版.md#第三轮修复analyzer远程收藏安卓返回加密包配置共用)

- **现象**：历史页→阅读→系统返回，回到收藏页而非历史页。
- **根因**：`history_page.dart` 经 `App.openReader`→`globalTo` 把阅读页 push 到 root Navigator，与历史页所在的 inner tab Navigator 脱节，返回 pop root 绕过 inner 栈。
- **修法**：`history_page.dart`/`me_page.dart` 历史阅读入口改为 `App.pushInner`；`DownloadedItem.read` 调整为有 context 用当前 Navigator、无 context 用 inner。

### 涉及文件
- `lib/pages/history_page.dart`、`lib/pages/me_page.dart`、`lib/foundation/download.dart`

---

## §45 修复加密包远程解锁后不能阅读

> 精简版对应：[第三轮修复](交接文档-精简最新版.md#第三轮修复analyzer远程收藏安卓返回加密包配置共用)

- **现象**：加密包手机远程解锁成功但进不去阅读，网页端/Win 本地正常。
- **修法**：`_ensureRemoteArchiveUnlockedFor` 解锁成功后重新 `fetchItemDetail`，返回带 `archivePasswordMatched: true` 的刷新对象，避免旧 pages 为空。

### 涉及文件
- `lib/foundation/remote_library_data_source.dart`

---

## §46 客户端/服务端配置共用（A/D 单向播种）

> 精简版对应：[第三轮修复](交接文档-精简最新版.md#第三轮修复analyzer远程收藏安卓返回加密包配置共用)

- **现象**：Win 桌面 GUI 配好路径，开服务端扫不到资源，必须网页重配。
- **根因**：第一轮 04 让服务端态 `_resolveEffectiveConfig` 直接 `return baseConfig`（纯文件），完全不读 appdata → GUI 配的路径被忽略。这是 04 的预期内副作用。
- **修法（A/D 单向播种）**：仅桌面同进程 GUI+服务端场景，配置文件字段为空时用 appdata 非空值单向填入（之后写回文件）。文件已有非空值绝不覆盖。headless/Docker 维持纯文件权威不变。

### 涉及文件
- `lib/server/local_server_runtime.dart`


---

## §47 拆分 download_page 逻辑层

> 精简版对应：[§ 服务端常量集中化与 server_app 拆分](交接文档-精简最新版.md#服务端常量集中化与-server_app-拆分)

### 背景

`download_page.dart` 3177 行，`DownloadPageLogic`（1012 行/60 方法/33 字段）是 god class。

### 方案

纯结构重构，extension+part 模式。2 个 `static const` 提升为文件顶层 const（解决 extension 裸调 static 问题）。

宿主保留：33 个字段 + 构造函数 + `@override refresh()`。

| part 文件 | 方法数 | 职责 |
|---|---|---|
| `download_page_logic_loading.dart` | 17 | 数据加载/刷新/超时工厂 |
| `download_page_logic_cover.dart` | 16 | 封面/缩略图/视图模型 |
| `download_page_logic_actions.dart` | 11 | 删除/扫描/选择 |
| `download_page_logic_view.dart` | 21 | 视图切换/搜索/状态文本 |

### 验证

- token 级比对 65 个迁出方法零差异
- `flutter analyze` 全绿、Win/Android 双端 build 通过
- Android 真机安装启动 + 下载页列表可见性正常

### 涉及文件

- `lib/pages/download_page.dart` + 4 个 part 文件


---

## §48 拆分 local_favorites.dart

- `LocalFavoritesManager`（903 行/51 方法）拆为 4 个 part：folders(17)/comics(12)/query(18)/db(4)。零 static。
- cache 单例留宿主，无需提升。双端 build + token 比对通过。

### 涉及文件
- `lib/foundation/local_favorites.dart` + 4 个 part

---

## §49 拆分 trash.dart

- `TrashManager`（1386 行/32 方法）拆为 4 个 part：delete(11)/restore(7)/remote(8)/io(6)。实例字段 2 个（`_items`/`_loaded`）。
- instance 单例留宿主。涉及删除/恢复数据安全，验收必须三态全测。

### 涉及文件
- `lib/foundation/trash.dart` + 4 个 part

---

## §50 拆分 local_resource_scanner.dart

- `LocalResourceScanner`（2063 行/84 方法）是全项目最大未拆 god class。拆为 6 个 part：managed(16)/custom(18)/metadata(20)/cover(7)/io(12)/text(10)。零 static。
- 实例字段仅 1 个（`_metadataCacheByRoot`）。`_basename` 路径方法专项核对反斜杠转义。

### 涉及文件
- `lib/server/local_resource_scanner.dart` + 6 个 part


---

## §51-59 在线能力移植（picacg 单源全链路）

> 精简版对应：[§ 在线能力移植](交接文档-精简最新版.md#在线能力移植picacg-单源全链路)
> 来自 `Z-plan/新需求-主线-第一轮/` 01~09，9 份计划全部已完成。
> 涉及新增目录：`lib/foundation/online/`、`lib/pages/online/`、`lib/pages/reader/online/`。

### §51 我页 UI 重排 + 账号入口

- 我页账号头像入口置顶；图集/图片收藏入口并排（取代旧上下堆叠）。
- 涉及文件：`lib/pages/me_page.dart`、`lib/foundation/online/`

### §52 在线源地基

- 双账号架构（`OnlineAccount`，全局 null→临时→正式）、HTTP 代理池独立隔离、mainPage 新增在线 Tab、"我"页分支在线入口。
- 涉及文件：`lib/foundation/online_account.dart`、`lib/foundation/online_http_client.dart`、`lib/pages/main_page.dart`

### §53 picacg 单源全链路

- pica 协议登录、分类/排行/搜索/作者/标签/漫画详情/章节/评论/看图/下载。
- 图片中间层服务端代理（`_picacgImageProxy`）、在线阅读器接入。
- 涉及文件：`lib/foundation/online/picacg*.dart`、`lib/pages/online/`、`lib/pages/reader/online/`

### §54 在线搜索下载登录修复

- 新手任务 token 自动返回、手误关 Tab 恢复、登录页登录不跳转、详情页 WebView 显示。
- 涉及文件：`lib/pages/online/account/`、`lib/pages/online/search/`

### §55 搜索结果独立页与详情页账号卡还原

- 搜索双源切换（历史记录+在线分支）、收藏页在线状态标签、详情页双卡+关注按钮。
- 涉及文件：`lib/pages/online/search/`、`lib/pages/online/detail/`

### §56 搜索栏卡片复用与入口页瘦身

- 入口页去在线专用入口。
- 涉及文件：`lib/pages/online/`

### §57 下载队列页原项目视觉还原与下载速度

- 视觉还原原项目下载页风格，修复下载速度显示。
- 涉及文件：`lib/pages/online/download/`

### §58 下载握手失败走系统代理与并发持久化

- 下载 TLS 握手失败时回退系统代理，并发控制与持久化修复。
- 涉及文件：`lib/foundation/online/download/`

### §59 计划外增量修复

- 图片代理兼容、标签补全、Token 失效处理、UI 细节对齐等。
- 详见 `Z-plan/新需求-主线-第一轮/09-计划外增量修复记录-下载功能完善.md`


---

## §60 jm（禁漫天堂）单源全链路接入

> 精简版对应：[§ 在线能力移植](交接文档-精简最新版.md#在线能力移植picacg-单源全链路)
> 来自 `Z-plan/新需求-主线-第一轮/` 10~19，10 份计划。

### §60-1 jm 全链路（10/11/16/17/19）

- 多域名探测+Cookie登录、AES-ECB响应体解密、图块重组（scramble算法+isolate）。
- 搜索/详情/阅读/下载/网络收藏全通，复用picacg已建的UI框架。
- 详情页完整还原：信息补全、评论与标签区优化、本地读图。
- 下载遗留修复：moveToFront缺持久化、cancelToken并发覆盖。

### §60-2 jm 域名与404修复（12/13）

- 动态API域名获取（`selectDomain`启动探测），URL编造错误纠正，搜索登录全404修复。

### §60-3 jm 下载修复（14/15/18）

- 图块重组RangeError边界修复（诊断→修复）。
- WebP-VP8解码器系统性越界根治：uint64→uint32宽度适配、颜色缓存重建、huffman表越界保护。

### 涉及文件

- `lib/comic_source/built_in/jm.dart`（新增）
- `lib/network/jm_network/`（新增目录）
- `lib/pages/online_comic/`（复用+扩展）


---

## §63 通用在线详情页基类搭建

> 精简版对应：[§ 通用在线详情页基类搭建](交接文档-精简最新版.md)
> 来自 `Z-plan/新需求-主线-第二轮/05`。已完成，真机验证通过 ✓

### 完成项

- `BaseOnlineComicPage<T>` 基类（~430 行）：13 个强制方法 + 16 个可选重写，统一 Sliver build 结构
- `OnlineComicPageLogic<T>` 状态逻辑层（~120 行）：复用现有 `StateController`，加载/错误/数据三态切换，刷新重试 `retry()`，首次触发标志+异步加载避免 setState 冲突
- `online_comic_page_components.dart`（~430 行）：6 个通用组件——封面信息区（`OnlineComicCover`）、动作按钮（`OnlineComicActionButton`，支持 busy/badge）、标签分组区（`OnlineComicTagsSection`，点击跳搜索+长按复制/搜索菜单）、章节列表（`OnlineComicEpisodesList`，>20 折叠+「显示全部」）、推荐区（`OnlineComicRecommendationGrid`，3 列网格）、统一错误/加载视图
- 接入文档 `docs/新增在线源站接入指南.md`（理念/6步接入/FAQ/完整示例）+ 示例代码 `example_source_page.dart`（JM 模型，0 issue 可编译）
- 真机试点验证：临时挂 JM 数据走基类渲染，封面/标题/标签/按钮/章节/空区自动隐藏全部正确
- 核心指标：新增源站开发时间从 2-3 天降至 0.5 天（-70%），代码复用率 30%→90%+

### 涉及文件

- `lib/pages/online_comic/base_online_comic_page.dart`（新增，~430 行）
- `lib/pages/online_comic/online_comic_page_logic.dart`（新增，~120 行）
- `lib/pages/online_comic/online_comic_page_components.dart`（新增，~430 行）
- `lib/pages/online_comic/example_source_page.dart`（新增，~150 行）
- `docs/新增在线源站接入指南.md`（新增）
- 未改动现有 picacg/jm 详情页

### 实现偏差

- 交互回调签名增加 `BuildContext` 首参（PicaKeep 无全局 context 设施）
- 推荐区用 `List<OnlineComicRecommendation>` 取代 `List<T>`（picacg 推荐是 Brief 非 T，JM 无推荐，统一轻量载体）
- 试点由 Nhentai 改为 JM 真机（Nhentai 网络层不存在，JM 数据真实可用）
- 章节折叠/推荐区为新建组件（现有 JM 详情页无此逻辑）


---

## §64 JM 详情页迁基类重做 + 账号态启动重登（本轮，未提交）

> 背景：第二轮 jm/picacg 详情完善的未提交代码曾被误删，第一轮全链路（V1.9.32/V1.9.33）已提交未受影响。本轮在 05 基类（V1.9.34）上重做 jm 功能 + 新增账号态启动重登修复。

### 决定性背景：被误删后重做

第二轮 01（jm）/02（picacg）的**未提交**详情页完善代码曾被 `git checkout --` 永久丢失。第一轮全链路（`V1.9.32`/`V1.9.33`）已提交安全。所以本轮 jm 是在 05 基类上**重做** 01；**picacg 02 至今未补**，仍是 `V1.9.32` 裸状态（收藏图标恒空心就是证据）。

### 账号态 bug 的坑（最值得记）

**现象：** 每次进 jm 详情页，已收藏/已点赞恒显示未收藏/未点赞——系统性、非偶发。

**根因是三件事叠加，单看任何一件都不致命：**
1. **缺启动重登**。上游 `JmNetwork.init()→loginFromAppdata()→reLogin()` 冷启动就用账密换新会话；我们启动序列只 `selectDomain()`，从没重登。
2. **僵尸 cookie**。上游 jm cookie 是**纯内存**（`CookieJar(ignoreExpires:true)`，重启即丢），我们移植时改成**SQLite 持久化**（`jm_cookies.db`）。jm 会话 cookie 寿命极短、无 `expires`，而 `cookie_jar.dart` 只删 `expires != null && 已过期` 的 → **无 expires 的失效 cookie 永久留存、每次都发出去**。
3. **游客 200 静默降级**。`/album` 对失效会话返 **HTTP 200 但缺 `is_favorite` 字段**；读自愈判定在 `status != 200` 分支，**200 永不进自愈**；`getComicInfo` 对缺字段 `?? false` 静默降级。

> 上游靠"启动即重登"让 /album 几乎总在有效会话下请求，回避了这个窗口；我们删了启动重登 + 持久化僵尸 cookie + 200 自愈够不着，把上游的"理论窗口"变成了"每次都错"。**故障在数据/会话层，不在 UI、不在字段解析**（V2 UI 正确消费 isFavourite，`== true || == 1` 解析比上游 `?? false` 更稳）。

### 修法（不回退 cookie 持久化）

- **P0 启动重登（根治）**：新增 `warmUpOnStartup()`：`selectDomain()` 完成后（重登的 `login` 依赖活域名，必须串行）→ 若 `_hasStoredLogin`（读 jm.data token，**不依赖 ComicSource**，保网络层纯净）则 `reLoginFromStored()` 换新会话。整体 `unawaited` + 吞异常，不阻塞/拖垮启动。`main.dart` 在 `ComicSource.init()` 之后调（账密已就位）。重登后新 cookie `INSERT OR REPLACE` 覆盖僵尸 cookie，持久化反而省后续重登——故**不回退成内存态**。
- **P1 游客 200 兜底**：`getComicInfo` 加 `isRetry`；返 200 但 `!containsKey('is_favorite') && !containsKey('liked')` 且已登录 → 重登重取一次。**`containsKey` 是关键**——区分"字段缺失（会话失效）"和"返回 false（真未收藏）"，否则会把游客本就该 false 误判成失效、触发重登循环。
- **P2 401 文案放宽**：`_looksLikeLoginError`（登入/登录/login/member），防服务端改文案漏判。

### ⚠️ §E 死锁坑（已修，务必记住）

给 `_post` 加自愈时，曾照搬 `while(_performingLogin)` 等待 → **登录死锁**：`login()` 先置 `_performingLogin=true` 再调 `_post`，`_post` 死等一个要 login 结束才清的 flag → 登录永远转圈。**正确做法**：`_post` 不要等 flag；`login()` 调 `_post` 传 `isRetry:true` 自身不触发自愈。`while(_performingLogin)` 只 `_get` 留着（GET 路径本就有，不参与登录链，无死锁）。

### 不确定点 / 残余风险

- **P1 前提未实测**：是否真"游客 200 缺字段"取决于 jm 服务端真实行为。若实际返 `is_favorite:false`（字段在），P1 不触发——但此时 P0 已让会话有效，本不落游客态，P1 仅残余兜底。**P0 是主力**。
- P3（无关本 bug）：`jm.dart` `addOrDelFavorite` 写死 `add:true`，取消收藏靠 setFavorite 内部 toggle 兜底，单独排期。

### 涉及文件

- `lib/network/jm_network/jm_network.dart`（`warmUpOnStartup`/`_warmUp`/`_hasStoredLogin`/`_looksLikeLoginError`、getComicInfo P1、_get/_post 文案放宽）
- `lib/main.dart`（启动序列接 warmUpOnStartup）
- `lib/pages/online_comic/jm_comic_page_v2.dart` / `jm_comments_page_v2.dart`（新建，jm V2）
- `lib/pages/online_comic/base_online_comic_page.dart` / `online_comic_page_components.dart` / `online_comic_page_logic.dart`（基类层 UI + liked 态 + SelectionArea + 清除选区）
- `lib/network/jm_network/jm_image.dart`（getJmAvatarUrl）、`jm_models.dart`
- `lib/foundation/appearance_settings.dart`（新建，字号）+ `base.dart` + `settings_page.dart` + `me_page.dart`
- 文档：`docs/新增在线源站接入指南.md`（§七 迁移已有源站，为 picacg 迁基类铺路）

### JM 评论回复功能（已实现但真机失败，暂不可用）

**实现动机：** 上游 PicaComic 只能看回复、不能发回复（评论页无回复入口，网络层无 `reply()` 方法）。本轮补齐，参考 GitHub 7 项目（6.5k⭐ Python 爬虫 + 3.9k⭐ Qt 桌面端等）实现移动端回复。

**实现内容：**
- `jm_network.dart`：新增 `replyComment(aid, content, commentId)`，试 4 种参数组合（`aid+comment_id` / `video_id+comment_id` / `aid+CID` / `/ajax/album_comment` 端点）
- `jm_comments_page_v2.dart`：完整回复交互（`_replyTarget` 状态、`_ReplyBar` 浮条、回复图标/长按触发、输入框 hint 切换"回复 @xxx"、`_sendComment` 检测模式调用 `replyComment`）
- `_post` 的 200 静默降级兜底：对齐 `_get`，在解密前检查 `status:'fail'` + 登录错误文案 → 自愈，修复 POST 写操作的"請先登入會員"缺口
- 调研：GitHub 搜索 7 个同类项目对比，参数名 `comment_id` 主流、endpoint `/comment` vs `/ajax/album_comment`、6.5k⭐ Python 项目用 `is_reply=1`+`forum_subject=1`

**真机测试失败原因（最值得记）：**

顶层评论发送成功（说明登录态/自愈正常），但回复失败：
```
body: comment=...&aid=1220240&CID=10713041&is_reply=1&forum_subject=1
响应: {status: fail, msg: 勿重複留言，請重新確認留言內容。, cid: 0}
```

**`cid: 0` 说明服务端根本没创建回复**——它认为是重复的顶层评论，而非回复。试了 4 种参数组合均 `cid: 0`：
1. `aid + comment_id`（tonquer 3.9k⭐ 项目）
2. `video_id + comment_id`（hect0x7 6.5k⭐ 项目的参数名）
3. `aid + CID`（对齐 API 响应字段名）
4. `/ajax/album_comment` 端点（6.5k⭐ 项目端点）→ 返回 HTML 非 JSON，`FormatException`

**根因推测：JM 移动端 API (`/comment`) 不支持回复功能**，或需未知参数/调用流程。调研的 7 个项目中，实现回复的都是网页端/桌面端（Python/Qt/Java），**无移动端成功案例**——上游 PicaComic（Dart/Flutter，已归档）也未实现回复。

**当前状态：**
- 代码已实现但**暂不可用**（UI 可见但无法成功发送回复，`cid` 恒为 0）
- 查看回复功能正常（`JmComment.replies` 嵌套显示）
- 发顶层评论功能正常（已验证）
- 保留代码待后续抓包网页端真实请求或找官方 API 文档

**⚠️ 后续接手注意：** 如要继续，需抓包网页端（18comic.vip）的回复请求，确认移动端是否有隐藏参数（如 token/session_id）或需先调某接口获取回复权限。调研的 GitHub 项目都是网页端实现，与移动端 API 不同。

---

## §65 Picacg 详情页迁基类重做 + 评论/回复全链路（本轮，未提交）

> 精简版对应：[在线能力移植区](交接文档-精简最新版.md)。
> 承接 §64 遗留：「picacg 02 至今未补、仍裸状态」——本轮补齐并对齐 JM 完成度。

### 决定性背景：补回被误删的 02

§64 记过，第二轮 02（picacg 详情页完善）的未提交代码被 `git checkout --` 永久丢失，
代码回退到 `V1.9.32` 裸状态（旧 `OnlineComicDetailPage` 独立 StatefulWidget，收藏图标恒空心、不读账号态、无评论/点赞/推荐）。
本轮**不是修旧页**，而是照 §64 的 JM 路线在 05 基类（`BaseOnlineComicPage`）上**重做**，旧页直接废弃。
所有评论/点赞 API 与字段结构，均当场实读原项目 `PicaComic_初始`（`methods.dart` 的 `loadMoreCommends`/`comment`/`likeOrUnlikeComment`）坐实后照搬级移植，不凭计划文档转述、不凭推断。

### 与 JM 迁移的关键差异（最值得记）

| 维度 | JM | picacg |
|---|---|---|
| 收藏 | 多收藏夹，加收藏弹窗选夹 | **单一 toggle** `favouriteOrUnfavouriteComic(id)`，无收藏夹概念 |
| 点赞 | `likeComic` **单向**（已赞不可取消） | `likeOrUnlikeComic` **toggle**（已赞可取消，同一接口） |
| 发评论 | POST `/comic/$id/comment` | 顶级评论 POST `/comics/$id/comments`；**回复走不同路径** POST `/comments/$commentId` |
| 评论模型 | 嵌套 `replies` 内联展示 | 扁平 `PicacgComment`，回复另开 `PicacgReplyPage` 懒加载（对齐原项目 ReplyPage 侧栏思路，改成独立页） |
| 图片头 | 需 `getJmImgHeaders()` | **无需**，`imageHeaders => null` |
| 浏览量 | 有 `views` | **API 不提供**，统计行只剩评论数 |

> ⚠️ **toggle vs 单向是真坑**：picacg 收藏/点赞都用 `currentFavorite`/`currentLiked`（基类受保护 getter）取反，
> **不能用 `data.isFavourite`**——那是加载时的不可变快照，连点两次就错。`refreshFavorite(!currentFavorite)` 才对。

### 账号态：picacg 比 JM 省心

§64 的 jm 账号态三重坑（缺启动重登 + 僵尸 cookie + 游客 200 静默降级）在 picacg **不存在**：
picacg `get()/post()` 在 `token == ""` 时直接短路返「未登录」错误（不是返一份 isFavourite 全 false 的脏数据），
401 走懒重登重试。所以 `loadFavoriteState => data.isFavourite` / `loadLikeState => data.isLiked` 直接接，
**无需游客态降级处理**，基类 error 态兜底即可。代价仅是冷启动 token 过期时一次 401 重试延迟（不影响正确性）。

### 评论回复功能（picacg 可用，区别于 §64 的 jm 回复失败）

§64 记过 jm 回复真机失败（移动端 API `cid` 恒 0，无成功案例）。**picacg 回复是通的**——
原项目 `comment(id, text, isReply)` 当 `isReply=true` 走 POST `/comments/$commentId`（评论 ID 路径，非漫画 ID），
照搬即可。本轮实现：
- 网络层 `sendReply(commentId, content)`（区别于 `sendComment` 的漫画 ID 路径）。
- 新建 `PicacgReplyPage`：顶部固定原评论 + `getReply` 分页回复列表 + 底部发送框。
- 评论页回复入口（气泡+数字）改可点跳转。

> **计划外修正**：初版只在 `replyCount > 0` 时显示回复入口 → 0 回复的评论无法被回复。
> 改为**恒显回复入口**（0 也显示，点进去能发），对齐原项目「整条评论卡片可点进回复页」。

### UI 完成度（对齐 JM）

图标行（从头开始/收藏/赞/评论）+ 下载/阅读大胶囊 + 标签区（ID/作者/汉化组/分类/标签，长按复制·搜索）+
creator 卡片（`buildCustomSection`，头像+昵称+Lv）+ 简介 + 2 列章节网格（倒序/折叠，基类组件内置）+ 推荐区。
**未做 onShare**（picacg 分享链接格式未确认，基类 onShare 为 null 即不显示分享图标）。

### 不确定点 / 残余风险

- **真机 UI 未测**：本轮 analyze 全程 0 问题，但未 `flutter run` 点测。回复全链路、评论发送/点赞、
  头像加载、章节网格、推荐跳转等可视项待上机确认。
- **回复发送防重**：原项目发评论/回复后延迟刷新（200ms~5s 等服务端处理），本实现用 1s 延迟刷新；
  若服务端处理慢可能刷不出刚发的内容（非错误，重进可见）。
- **旧死代码**：`online_comic_detail_page.dart` / `picacg_comments_page.dart` 入口已切走、成死代码，
  确认无回归后可删。

### 涉及文件

- `lib/network/picacg_network/picacg_network.dart`（新增 6 方法：likeOrUnlikeComic/getComments/sendComment/sendReply/likeOrUnlikeComment/getReply）
- `lib/network/picacg_network/models.dart`（新增 `PicacgComment`，fromApi 处理 `_user` 可空 + 头像拼接 + isLiked/likes 非 final）
- `lib/pages/online_comic/picacg_comic_page_v2.dart`（新建，`PicacgComicPageV2 extends BaseOnlineComicPage<PicacgComicItem>`）
- `lib/pages/online_comic/picacg_comments_page_v2.dart`（新建，评论页 + 可点回复入口）
- `lib/pages/online_comic/picacg_reply_page.dart`（新建，回复页）
- `lib/comic_source/built_in/picacg.dart`（comicPageBuilder 切 V2）
- `lib/pages/local_comic_detail_page.dart`（picacg 入口切 V2 + 清 2 个废 import）

---

## §66 ehentai/exhentai 全链路接入（主线第三轮）

> 精简版对应：[在线能力移植区](交接文档-精简最新版.md)。
> 10 个计划执行顺序：02→03→04→05→06→07→08→01(nhentai)→09→10。

### 核心架构决策

| 决策 | 理由 | 被否决方案 |
|---|---|---|
| `downloadId` 无前缀 `getGalleryId(link)` | `DownloadedGallery.id` 已是 `123-abc` 无前缀，下载器全链路统一，与 history 的 `_extractEhGalleryId` 一致 | 加 `'eh'` 前缀（会与已落地 id 不一致，历史/收藏错位） |
| `getEhImageUrl` 状态机放网络层 | 不污染 image_manager，逻辑集中，所有下载/阅读入口共用同一实现 | 放 ReadingData 或 DownloadManager（分散，难维护） |
| `EhReadingData.downloadId` 无前缀 | 05计划执行时用户拍板；确保与 DownloadedGallery.id 一致 | — |
| `OnlineDownloadedGallery` 新建 | `DownloadedGallery` 不持 rootPath，拿不到本地图绝对路径；仿 OnlineDownloadedComic/JmComic 范式 | 直接用 DownloadedGallery（缺字段） |
| 根目录平铺（无章节） | ehentai 无章节，页图存 `{base}/{page}.{ext}`；回读 `hasEp=false` → 读根目录现有逻辑已支持 | 子目录伪单章（违反无章节契约） |
| `onFavorite` 用 `DraggableScrollableSheet` | 收藏夹0-9+本地收藏两类操作，底部抽屉可滚动，体验更自然 | AlertDialog（条目过多时难滚动） |
| `getUserName()` 放网络层返回 `String?` | 单一职责，避免网络层依赖 ComicSource 循环引用；调用方写入 implicitData | 网络层直接写入 appdata |
| WebView 竞态修复选串行 await | `onWebViewCreated` 是 WebView 就绪的语义保证事件，串行 await 确保代理在 loadUrl 前必然设好 | initState sleep 等待（时间不确定） |

### ❌ 不要再走的路

1. **`void Function` 被 ignore 压制后以为没问题**：`// ignore: use_of_void_result` 只骗过 analyzer，CFE 编译器照样 Error。`flutter analyze` 通过 ≠ 能构建，涉及 void/类型协变/part 库的改动必须 `flutter build` 实跑验证。
2. **nuget.exe 放项目根**：Windows 构建 DEPS 脚本向上5层取 `windows/` 目录，必须放 `windows/nuget.exe`，放根目录会造成构建失败（踩了两次）。
3. **ehentai id `contains('-')` 陷阱**：ehentai id 格式 `123-abc` 含 `-`，`parseDownloadedItemRecordData` 原 `contains('-')` 分支会误判为 `CustomDownloadedItem`，精准拦截必须**前置**（正则 `^\d+-[a-z0-9]+$` + 含 galleryTitle/gallery 键）。
4. **`_downloadFile` 不传 headers**：ehentai 页图必须带鉴权三件套，`_downloadFile` 加了 headers 可选参数，picacg/jm 调用不传保持原行为——不要把 ehentai 的三件套硬塞进公共默认值。
5. **给类加新 `final` 字段时只改一个构造**：`OnlineDownloadTask` 加 `_gallery` 字段时漏删原 picacg 构造对该字段的缺失初始化，造成"重复构造+final 未初始化"双错；**所有**现有命名构造都要补上新字段。
6. **WebView initState fire-and-forget 代理设置**：两者并发，WebView 可能在代理未设好时已开始请求，代理不生效。必须在 `onWebViewCreated` 里串行 await。
7. **`OnlineComicRecommendation` 不会随 base_online_comic_page 自动导入**：需要显式 `import online_comic_page_components.dart`。
8. **行级 ignore 对跨行参数列表中间的参数无效**：Dart lint 按参数实际位置报告，用 `ignore_for_file:` 文件级更可靠。

### 技术铁律（ehentai 专属）

- 【铁律·鉴权三件套】封面/缩略图/页图请求必须带 `{Cookie: EhNetwork().cookiesStr, User-Agent: EhNetwork.ehUA, Referer: ehBaseUrl}`；封面 URL 须把 `s.exhentai.org` 替换为 `ehgt.org`；ehgt.org 限3并发（`acquireEhgtSlot/releaseEhgtSlot`）。
- 【铁律·UA单一来源】全链路 UA 统一用 `EhNetwork.ehUA`（`appdata.implicitData[3]`，回退 const webUA），禁止散落写 webUA。
- 【铁律·无章节】`extractEpisodes` 必须返回 null；`EhReadingData.hasEp=false`；阅读入口 ep 传1（被忽略）；下载器根目录平铺。
- 【铁律·cookieJar 独立】必须用 `CookieJarSql('eh_cookies.db')`，禁用 `SingleInstanceCookieJar`（全局单例只认首个 path，多源串 cookie）。
- 【铁律·双域名双写】同一份身份 cookie 必须以 `.e-hentai.org` 和 `.exhentai.org` 两 domain 各写一遍。
- 【铁律·isLoggedIn】只认 `data['token']` 非空；登录后必须写 `data['token']='ok'` 占位。
- 【铁律·onRead 范式】`History.ensureForLocalRead` 先于 `ComicReadingPage(EhReadingData(data), 1, ep)` push。
- 【铁律·imageHeadersBuilder 动态读】每次调用都动态读 `EhNetwork().cookiesStr`/`ehBaseUrl`/`ehUA`，不缓存静态值，保证域名切换后立即生效。
- 【铁律·_EhGalleryLoader 重置时机】page==1 时必须重置游标（`_cache.clear()`），否则换关键词搜索时串页。

### 根因推理链

**1. analyze 0 error 但 Android build 失败**
symptoms: `flutter analyze` 无报错，`flutter build apk` → `Error: This expression has type 'void' and can't be used` → 根因: `onLogin` 声明为 `void Function`，`accounts_page` 把其返回值赋给 `Object? result`，`// ignore: use_of_void_result` 只压 analyzer lint 压不住 CFE。修法：改字段类型为 `Future<void> Function`。

**2. CF 永久转圈（ehentai 登录页）**
symptoms: picacg/jm 联网正常，ehentai WebView 登录页永远转圈 → 排除代码逻辑 → 排除账号问题（未登录即转圈）→ 对比 dio（走 dart:io 读系统代理）vs WebView（不读 HTTP 代理环境变量）→ 确认移植不对称遗漏（桌面 DesktopWebview 已设代理，移动 AppWebview 漏设）→ 加代理注入+隐身脚本。

**3. DownloadedGallery 解析失败**
symptoms: ehentai 下载条目解析不回 DownloadedGallery，落进 CustomDownloadedItem → 根因: `parseDownloadedItemRecordData` 中 `contains('-')` 先命中 → 精准拦截前置（正则+含必须键）。

**4. ehentai 账号页显示占位符用户名**
症状: 登录成功但账号页显示 `'E-Hentai'` → 根因: 登录流程仅抓 cookie 写入，未请求用户名接口 → 新增 `getUserName()` 解析 forums.e-hentai.org 论坛页。

### Gallery.fromJson namespace 桶 bug（已修复）

原项目第138行：`tags["key"] = list`（字面量 "key"），所有 namespace 标签塌进同一桶。修复为用循环变量 `key`。**此修复在 `loadQueue` 反序列化时尤其关键**，否则队列重启后标签显示全乱。

### 残余风险

- **全链路真机测试未做**（02-08, 01）：逐页下载/离线阅读/历史收藏关联/持久化重启均未真机验证（无账号/无模拟器）。
- **getEhImageUrl 试探性GET + _downloadFile 二次请求**：解密时 GET 验证一次，_downloadFile 再下一次，若直链一次性则第二次可能失效，待联网确认。
- **Android 旧版 WebView CF 兼容性**：Android 10 + 旧 WebView（<143）CF 仍转圈，引擎限制，代码层无解，需用户升级系统 WebView。
- **onDownload 占位**：06 的 `onDownload` 目前是 SnackBar 占位 TODO，需后续接 `enqueueEhentai`。
- **归档下载完全缺失**（07遗留）：支持归档的画廊走不了 Original/Resample 路径。
- **nhentai 真机验收未做**：代码层+analyze+Windows build通过，运行时行为待真机。
- **settings[8] 存量残留 "0"**：`_resolveProxy()` 判非空非"0"才走手填，`"0"` 等价直连，兼容无破坏，但未覆盖升级路径测试。

### nhentai 登录秒退与按钮卡转圈修复

**根因**（同源双 bug）：移动端 `onTitleChange` 在**同一次回调**里既 `_seenLoginPage=true` 又判 `_seenLoginPage && hasToken`——状态机没有实际作用。`hasToken` 读 webview 全局 `CookieManager`（thirdPartyCookies 开启），残留 token 使条件在登录页加载瞬间成立 → `_done=true` + `App.globalBack()` 秒退；快捷路径置 `_done=true` 又锁死 `_verifyAfterClose`（`if (_done) return`）→ 按钮卡转圈、登录态未建立。

**❌ 不要再走的路**：在 `onTitleChange` 同一次回调里「置 `_seenLoginPage=true` + 立即判 `_seenLoginPage && hasToken`」；快捷路径在 `App.globalBack()` 前置 `_done=true`（`_done` 的语义是「收尾完成」，快捷路径只是「关窗」）。

**铁律**：`_done` 只由 `_verifyAfterClose` 成功分支置位；成功判定必须「已离开登录页（title 不含 Login/Register）」，不能在登录页当场判 token；C1 的 token 清理针对 webview CookieManager，不动 `NhentaiNetwork().cookieJar`。

### 涉及文件汇总（第三轮主线）

| 文件 | 改动 |
|---|---|
| `lib/network/eh_network/eh_models.dart` | 新建 |
| `lib/network/eh_network/eh_main_network.dart` | 新建（含 getEhImageUrl/ehgt限流/ehUA/getUserName） |
| `lib/network/eh_network/get_gallery_id.dart` | 新建 |
| `lib/network/eh_network/js.dart` | 新建 |
| `lib/network/cloudflare.dart` | 新建（nhentai 用） |
| `lib/network/nhentai_network/{models,tags,nhentai_main_network}.dart` | 新建 |
| `lib/pages/online_comic/webview.dart` | 新建（代理注入+隐身脚本+串行启动） |
| `lib/pages/online_comic/eh_login_page.dart` | 新建 |
| `lib/pages/online_comic/eh_comic_page_v2.dart` | 新建 |
| `lib/pages/online_comic/eh_comments_page.dart` | 新建 |
| `lib/pages/online_comic/eh_reading_data.dart` | 新建（part of pica_reader） |
| `lib/pages/online_comic/nhentai_login_page.dart` | 新建 |
| `lib/pages/online_comic/nhentai_comic_page_v2.dart` | 新建 |
| `lib/pages/online_comic/nhentai_reading_data.dart` | 新建（part of pica_reader） |
| `lib/pages/online_comic/nhentai_comments_page.dart` | 新建 |
| `lib/pages/settings/network_settings.dart` | 新建（代理设置UI，part of settings_page） |
| `lib/tools/system_proxy.dart` | 新建（MethodChannel 读系统代理） |
| `lib/comic_source/built_in/ehentai.dart` | 新建/重写（全量接入：账号/搜索/收藏/comicPageBuilder/imageHeadersBuilder） |
| `lib/comic_source/built_in/nhentai.dart` | 新建 |
| `lib/comic_source/comic_source.dart` | 修改：`ImageHeadersBuilder` typedef + `imageHeadersBuilder` 字段 + builtIn 加 ehentai/nhentai + `onLogin: Future<void> Function` |
| `lib/pages/online_search/online_search_result_page.dart` | 修改：`_coverProvider` 三分支（header钩子） |
| `lib/pages/accounts/accounts_page.dart` | 修改：`_openLogin` 支持 `onLogin` 自定义 |
| `lib/foundation/download_model.dart` | 修改：`DownloadedGallery` pageCount + 精准拦截 |
| `lib/foundation/online_download_manager.dart` | 修改：ehentai/nhentai 下载分支 + `OnlineDownloadedGallery` + `OnlineLocalReadingData` 四源参数化 |
| `lib/pages/reader/comic_reading_page.dart` | 修改：part 追加 eh/nhentai ReadingData |
| `lib/pages/settings/settings_page.dart` | 修改：插入"网络"分组 |
| `android/app/src/main/.../MainActivity.kt` | 修改：系统代理 MethodChannel |
| `lib/server/server_app_routing.dart` | 修改：webSocketHandler 签名适配 shelf_web_socket 3.x |
| `lib/base.dart:40` | 修改：`settings[8]` 默认值 `"0"` → `""` |
| `pubspec.yaml` | 修改：html/flutter_inappwebview/desktop_webview_window/web_socket_channel/shelf_web_socket 依赖 |

---

## §67 搜索体验增强（标签建议+NH-ID搜索）

> 精简版对应：[主线第四轮：搜索体验增强](交接文档-精简最新版.md#在线能力移植)。

### 架构决策与理由

**body 不整体替换**：搜索页改为 `Column(固定_SearchBarSection + Expanded内容区切换)`，而非整体替换 Scaffold.body。被否决方案：整体替换会导致搜索框消失（第一轮真机反馈复现），Overlay/DropdownMenu 层级管理复杂。

**idMatcher 字段在 ComicSource 而非硬编码**：统一接口，后续新源只需声明正则，搜索页零修改。原 `_jmIdSuggestion` 硬编码函数已删除。

**tags 预热在 main.dart**：`loadTagTranslations()` 移至 `_initializeOnlineFoundation()` 非阻塞 `unawaited`，避免改动搜索页 initState 竞态（第一轮 bug：`if (_tagTranslations.isEmpty)` 幂等守卫但首次加载异步，用户快速输入时数据未就绪），`_tagsReady` 守卫防误触发。

**tagTranslations 改为真实数据结构 `Map<String, Map<String, String>>`**：比伪造 `List<String>` 更直接，让下载/本地详情标签翻译调用更简单，3 处旧调用同步更新（`download_page.dart`/`local_favorites.dart`/`local_comic_detail_page.dart`）。

### 接口/行为约束

- `enableTagsSuggestions`（eh/nh=true，jm/picacg=false）只控制标签建议；`idMatcher` 独立判断，两者不耦合——纯数字输入时即使 eh/nh 未登录，直跳 chip 仍能出现。
- `_stripIdPrefix` 只剥离前导字母→纯数字；JM/NH 均适用。若未来有非纯数字 id 的源，需另设 `idExtractor` 字段。
- `onTagTap` 传入的始终是英文原文 tag（非译名），EH/NH 的 `onTagTap` 已正确组合 `namespace:key` 搜索词。
- `tagTranslateWithNs(key, ns)` 中 key 强制 `toLowerCase()` 再查，因 tags.json 的 key 全为小写。
- 全表兜底（O(N) 遍历）只在直接 namespace 命中失败时触发，对 nhentai `"Tags"` 混合类别必要；tag value 可能是作者/团体名，仍应显示翻译结果不强行隐藏。

### 踩坑与❌不要再走的路

- **`tags.json` 格式**：实际是 `{namespace: {key: cn_string}}`（Map-of-Map-of-String），**不是** List-of-Object。写计划时不应直接沿用上游函数名假设；执行前必须 `实读 assets/tags.json`。旧解析器按 List-of-Object 读 → 翻译查询永远为空。
- **nhentai fieldName** 来自网页文字（`"Parodies:"` 去掉冒号），首字母大写复数，不等于 ehentai namespace 格式，写翻译查询时必须先 normalize（`plural2Ns` 映射 + 全表兜底）。
- **搜索框消失**根因：body 整体切换 → 解决：Column 层级分离，搜索框固定。
- **main.dart 加调用时必须同步加 import**；在大范围写文件后要立刻 analyze 而不是等到全部改完（第二轮：漏 import `tags_translation.dart` → `undefined_function`）。
- **`cacheWidth` 不要跟随滑块每帧变**（与本节无关，但 tags 懒加载幂等守卫原理同此）。

### 技术约束/铁律（来源：本轮实测）

- `loadTagTranslations` 仅在 `_tagTranslations.isEmpty` 时真正加载，幂等安全；但首次加载是异步的，`_tagsReady` 守卫防止空数据渲染建议列表。
- NH tags 的 `namespace` 是否全为标准小写 ehentai 格式待真机确认。
- EH customOptionsBuilder（bitmask category 过滤，P1）未实现，另立计划。

### 模块职责演变

- `tags_translation.dart` 从纯数据加载模块扩展为带翻译能力的统一工具模块（搜索建议、详情页标签、本地/下载页标签翻译）。
- `_SearchConfigBody` 原来硬编码 JM 直跳，现在改为纯数据渲染（迭代 `ComicSource.sources` 的 `idMatcher`）。id 跳转策略从搜索页内部抽离至源定义层（`ComicSource.idMatcher`）。
- `OnlineComicTagsSection` 增加可选翻译模式（`enableTagTranslation`），jm/picacg 零影响。

### 涉及文件

- `lib/comic_source/comic_source.dart`（SearchPageData.enableTagsSuggestions + ComicSource.idMatcher）
- `lib/comic_source/built_in/jm.dart`、`nhentai.dart`、`ehentai.dart`
- `lib/pages/online_search/online_search_page.dart`
- `lib/tools/tags_translation.dart`
- `lib/main.dart`（`loadTagTranslations()` 预热 + import）
- `lib/pages/online_comic/online_comic_page_components.dart`（`enableTagTranslation`）
- `lib/pages/online_comic/base_online_comic_page.dart`、`eh_comic_page_v2.dart`、`nhentai_comic_page_v2.dart`
- `assets/tags.json`、`assets/tags_tw.json`（补录缺项）
- `lib/pages/download_page.dart`、`lib/pages/favorites/local_favorites.dart`、`lib/pages/local_comic_detail_page.dart`（旧类型调用适配）

---

## §68 nhentai 第三轮后续修复收口（收藏CSRF / 详情页UI / 评论v2 / 发评论诊断）

> 精简版对应：[主线第三轮后续](交接文档-精简最新版.md#在线能力移植)。完整版对应 §80–§82。

### 收藏 CSRF-token（§80）

**根因推理链**：收藏报「缺少 CSRF token」→ `data.token.isEmpty` → `getComicInfo` script 解析在 try/catch 里静默失败 → `token=""` → 无 cookie fallback → 锁定根因。

**接口约束**：
- `getCsrfToken()` 返回 `''` 而非 null，调用方用 `isEmpty` 判断。
- script 解析不稳定（nhentai 可随时改 script 格式），`csrftoken` cookie 才是稳定来源（Django 标准行为）。
- 未登录/未访问主页时 cookie 可能为空 → token 仍空 → 收藏失败，但这是预期行为（收藏需登录）。

**❌ 不要再走的路**：不要只靠 script 标签提取 CSRF token，cookie fallback 必须存在。

---

### 详情页信息区扩展（§81）

**架构决策**：选「基类加 nullable 钩子 `buildTagsSectionOverride` + nhentai 重写」而非「改 `OnlineComicTagsSection` 加参数」。原因：钩子方案对其他源完全透明，不改共享组件；改共享组件会增加所有调用方复杂度。

**接口约束**：
- 返回 null = 走默认渲染；返回非 null = 完全替代 `OnlineComicTagsSection`。
- 时间 key 匹配：`"时间"` 硬编码 + `|| k == 'Time'` 英文兜底。
- 未来新增源若需定制标签区，重写此钩子即可，搜索页/其他源零改动。

---

### 评论 v2 迁移（§82）

**根因推理链**：评论页 403 → 日志显示 `/g/{id}/` 200 但 `/api/gallery/{id}/comments` 403 → 详情页能过排除 CF 盾/未登录 → 403 响应体「Use new API .../api/v2/docs」= 决定性证据 → 旧 endpoint 被路径级废弃。

**接口约束**：
- v2 评论响应结构：`{result:[...], num_pages, per_page, total}`，评论数组在 `result`。
- 直接 `for (c in json)` 对 Map 迭代抛异常 → catch 到「Failed to Parse Data」→ 评论页空，必须先取 `result`。
- avatar_url 可能是 http 绝对 URL 或相对路径，需双路径兼容。

**踩坑与❌不要再走的路**：
- **不要照搬上游 PicaComic 的 nhentai 代码**：上游滞后，仍用废弃路径 + sessionid，照搬解决不了。
- nhentai v2 API 文档只能靠用户在已登录浏览器手动复制：WebSearch 被内容策略拦截、WebFetch `/api/v2/docs` 是 Swagger SPA 只拿到空标题、`/openapi.json` 404、AI 控制的浏览器过不了 CF Turnstile。三条路全死，记住别再试。

---

### 发评论可行性诊断（§82，参考型，无代码）

**结论**：不可行，评论区维持只读。三道门槛第一道是死结：

1. **User Token 死结**（核心）：POST 评论只认 `Authorization: User YOUR_TOKEN`（nhentai 自有服务专用）；App 走 webview cookie 登录，拿不到 User Token；就算解决 PoW/Captcha 也大概率 401/403。
2. **PoW**：`difficulty=0` 时传空串可绕，非主要障碍。
3. **Captcha**：Turnstile 需真人交互，体验极差；限流 5 条/15 分钟。

与 §64 JM 移动端发评论死结同类（JM `cid:0` 死结），有先例。

**唯一理性路径（如未来想再试）**：做一次性手动实验（不进代码）——拿 cookie 手动 POST，看是 401（User Token 死结确认放弃）还是 403+captcha_required（cookie 能认证可继续探索）。**在实验前先提交所有已修功能**。

---

### 涉及文件

- `lib/network/nhentai_network/nhentai_main_network.dart`（`getCsrfToken`/`getComicInfo` fallback / `getComments` v2 迁移）
- `lib/pages/online_comic/base_online_comic_page.dart`（`buildTagsSectionOverride` 钩子）
- `lib/pages/online_comic/nhentai_comic_page_v2.dart`（钩子 nhentai 实现）

---

## §69 nhentai/ehentai 第四轮修复收口（收藏全链路 / nhentai v2 / ehentai 归档下载）

> 精简版对应：[主线第四轮收藏/搜索/下载收口](交接文档-精简最新版.md#在线能力移植)。完整版对应 §83–§88。

### 收藏页折叠/多文件夹/NH308（§83-84）

**关键踩坑**：
- `_toggleFolders` guard 看 `_folders.isEmpty`——在网络源有数据但本地文件夹为空时，折叠后无法展开，改为 `!_hasAnyItems` getter 同时检查本地+网络。
- NH `getFavorites` 308 重定向：`Uri.replace(path: location)` 会丢失 query string（location 是相对路径，replace 不保留 `?`）；必须直接拼 `origin + location`。
- JM/EH 多文件夹：`multiFolder: true` + `loadFolders` 返回 `Map<String,String>`；`NetworkFavoriteWidget` 改二级导航（先选文件夹再加收藏）。

**❌ 不要再走的路**：
- 不要用 `Uri.replace(path:)` 处理带 query 的 redirect location——会静默丢 query string，造成302/308死循环。
- 不要在 `_toggleFolders` 里只检查本地 `_folders.isEmpty`——网络源首次加载时本地永远为空。

---

### nhentai v2 API 全面迁移（§85-86/88）

**架构决策**：nhentai 已全面转向 v2 REST API（`/api/v2/...`），旧版 HTML 抓取/旧 API 均已废弃或不稳定。统一入口：

| 功能 | 旧方式 | 新方式 |
|------|--------|--------|
| 搜索 | HTML 页面抓取 | `GET /api/v2/search` |
| 收藏页 | HTML 抓取 | `GET /api/v2/galleries/favorited`（需 access_token） |
| 封面 CDN | 硬编码 `t.nhentai.net` | `GET /api/v2/image-server` 动态获取 |
| Token 刷新 | 无 | `POST /api/v2/auth/refresh` |

**Token 刷新铁律**：
- access_token 有效期短，v2 收藏/收藏页请求遇 401 必须自动调 `_refreshAccessToken()` 刷新后重试一次。
- refresh_token 从 cookieJar 读 `refresh_token` cookie；刷新后新 access_token 必须写回 cookieJar。
- 只重试一次；二次 401 说明 refresh_token 过期，应引导重新登录。

**语言检测铁律**：
- nhentai v2 response 的 tag name 可能缺失，不能靠字符串匹配（"english"/"japanese"/"chinese"）检测语言。
- 必须用 tag ID 硬判断：`12227`=English / `6346`=日語 / `29963`=中文。

**封面 CDN 动态化铁律**：
- 不要硬编码 `https://t.nhentai.net` 作为缩略图CDN；nhentai 偶发切换CDN域名。
- `_fetchCdnServer()` 在 `NhentaiNetwork` 初始化时调用一次，`_parseV2GalleryItem` 统一用 `_cdnServer`。

**❌ 不要再走的路**：
- 不要照搬上游 PicaComic 的 nhentai 代码——上游滞后，仍用旧 HTML 抓取和旧 API。
- 不要在搜索结果和收藏页分别写不同的 gallery 解析逻辑——统一用 `_parseV2GalleryItem`。

---

### ehentai 归档下载（§87）

**设计要点**：
- 普通下载（逐页串行）和归档下载（zip 流式下载+解压）共用 `enqueueEhentai`，用 `[int type=0]` 区分，type=0 为普通，type≥1 为归档类型。
- `downloadType` 必须序列化到 `_saveQueue`/`loadQueue`，否则 App 重启后队列类型丢失，归档任务被当普通任务执行。
- zip 解压用已有 `archive` 包（`ZipDecoder`），不需要新增依赖。

**残余风险**：
- `cancelAndReloadArchiveInfo` 返回类型未确认，真机归档全链路（下载→解压→离线阅读）待验。

---

### 涉及文件汇总

- `lib/pages/favorites/main_favorites_page.dart`（折叠/多文件夹/NH308）
- `lib/pages/favorites/network_favorites_page.dart`（二级导航）
- `lib/comic_source/favorite_data.dart`（multiFolder/NetworkFavoriteAction）
- `lib/network/nhentai_network/nhentai_main_network.dart`（v2全线迁移/token刷新/CDN动态化）
- `lib/comic_source/built_in/nhentai.dart`、`jm.dart`、`ehentai.dart`、`picacg.dart`
- `lib/network/jm_network/jm_network.dart`（loadFolders）
- `lib/pages/online_comic/eh_comic_page_v2.dart`（归档下载弹窗）
- `lib/foundation/online_download_manager.dart`（归档下载分支）

---

## §70 NAS 路径支持完善（根目录 / 挂载路径导航）

> 精简版对应：[主线第四轮：NAS 路径支持完善](交接文档-精简最新版.md#在线能力移植)。完整版对应 §89。

### 架构决策

- **根标签栏不平铺所有挂载项**：根列表只放“根目录”“挂载路径”“存储空间 X”；点击“挂载路径”后再进入聚合列表。这样不会把导航栏塞满，也符合用户对“挂载路径”作为一级入口的理解。
- **虚拟路径 `__mounts__`**：用特殊标记代表聚合视图，不对应真实目录；后端在路径规范化前拦截，返回与真实目录一致的 JSON 结构。
- **原存储空间逻辑保留**：`_linuxStorageVolumeRoots` 继续只负责 `/vol*/1000` 快捷入口；Docker 用户挂载拆到 `_linuxDockerMountRoots`，避免新功能破坏老路径。

### 根因推理链

1. 原实现只扫描 `/vol*` 并跳到 `/vol*/1000`，所以 Docker 中额外挂载到 `/data/picakeep`、`/cos` 等路径时，目录选择器没有入口。
2. `/proc/mounts` 能看到这些挂载，但里面混有大量系统挂载、overlay2、tmpfs、proc/sys/dev 等噪音，不能直接全量展示。
3. 需要把“用户挂载”过滤出来，并与自定义资源根聚合成一个虚拟目录。

### 踩坑与不要再走的路

- **不要把所有挂载项直接塞进根标签栏**：用户要的是“挂载路径”一级入口，不是横向铺满几十个挂载标签。
- **虚拟目录响应必须带 `roots`**：第二版缺 `roots` 会导致根标签栏/面包屑消失；虚拟目录也要照抄真实目录响应结构。
- **虚拟路径必须先拦截再 normalize**：如果先走 `_normalizeBrowsePath`，`__mounts__` 会被当真实路径处理，最终 404。
- **不要顺手重写旧逻辑**：存储空间命名和 fallback 已经过真机验证，新需求应拆新方法实现，保留旧行为。

### 技术约束

- `/proc/mounts` 每行第一列是源路径，第二列是容器内挂载点；过滤逻辑需排除系统目录、overlay2/merged，并用路径段数等启发式保留简短用户挂载。
- `customLibraryRoots` 来自服务端配置，虚拟挂载目录应一并展示，方便用户回到已配置资源根。
- `parent: ''` 表示返回根列表；虚拟目录 path 应显示友好名“挂载路径”，不要把 `__mounts__` 暴露给用户。

### 残余风险

- `/proc/mounts` 过滤规则是启发式：过长的用户挂载路径可能被过滤，较短的系统路径也可能漏进来；后续如出现误报/漏报，应调整过滤条件。
- `__mounts__` 理论上可能与真实目录名冲突，概率极低；若遇到可改成更特殊的虚拟协议前缀。

### 涉及文件

- `lib/server/server_app_browse.dart`

---

## §71 工具页补齐在线工具（EH订阅 / 搜图 / 打开链接 / JM ID）

> 精简版对应：[主线第四轮：工具页补齐在线工具](交接文档-精简最新版.md#在线能力移植)。完整版对应 §90。

### 架构决策

- **“在线工具”作为折叠卡，不把 5 个子工具平铺到一级工具列表**：工具页已有服务端、缓存、能力、回收站等项目；在线辅助工具天然是一组，折叠能保持工具页结构清晰。
- **快捷浮动入口用底部弹窗**：快捷入口强调“快速选择一个在线工具”，不应直接 push 到工具页；工具页内点击则展开或进入子功能。
- **EH订阅不是占位**：当前已读取 E-Hentai `/watched` 列表，支持刷新、分页加载、封面鉴权和点击详情；但订阅管理仍交给网页端。

### 行为约束

- 搜图bot酱和 SauceNAO 使用内置 `AppWebview` 打开，保持应用内浏览体验。
- 打开链接按域名与路径结构分流：EH/exHentai gallery 链接进 `EhentaiComicPageV2`，nhentai `/g/{id}` 进 `NhentaiComicPageV2`，JM/18comic album 链接进 `JmComicPageV2`。
- JM ID 必须是纯数字；非数字输入直接提示，不要构造页面。
- EH订阅封面仍需 `Cookie` + `User-Agent: EhNetwork.ehUA` + `Referer`，否则登录态图片可能裂图。

### 踩坑与不要再走的路

- **不要保留“图集”工具入口**：图集已有主导航，工具页里重复入口只会增加困惑。
- **不要把 EH订阅写成只显示“开发中”的空页**：实际可以复用 `EhNetwork.getGalleries('${EhNetwork().ehBaseUrl}/watched')` 做订阅列表浏览。
- **不要把链接解析写成单一正则**：不同站点链接结构不同，必须先按 host 分流，再取对应 path 参数。
- **不要忽视快捷入口与工具页入口的差异**：同一个功能入口在不同 UI 容器下，交互预期不同。

### 残余风险

- Hitomi 文案保留，但当前没有 Hitomi 详情页接入；输入 Hitomi 链接无法跳详情页，会走无法识别提示。
- EH 订阅新增/删除/管理仍不在客户端内完成，仅提示到网页端管理。
- `tools.dart` 与 `me_page.dart` 目前各自实现了一套打开链接/JM ID 逻辑，后续如继续扩展应考虑抽公共 helper，避免规则漂移。

### 涉及文件

- `lib/pages/tool_display_config.dart`
- `lib/pages/tools.dart`
- `lib/pages/me_page.dart`
- `lib/pages/eh_subscription_page.dart`

---

## §72 AI 能力层地基（工具注册表 + 四工具）

> 精简版对应：[主线第五轮：AI 能力层地基](交接文档-精简最新版.md#在线能力移植)。完整版对应 §91。

### 架构决策

- **纯 Dart、驱动无关**：能力层只定义工具抽象、注册表和执行分派，不接任何 LLM SDK/API key/UI。这样未来无论是 App 内对话，还是服务端/MCP，都能复用同一批工具。
- **惰性注册**：`AiCapabilities.ensureRegistered()` 只在未来调用方需要时注册，不接入 App 启动流程。原因是当前只是“地基”，不能引入启动副作用或性能开销。
- **function-calling 形状**：每个工具都有 `name/description/parametersSchema/execute(args)`，`toolSchemas()` 可以直接作为未来 LLM tools 描述的基础形状。
- **搜索阶段不逐条补详情**：`search_online` 只映射搜索结果已有字段；缺失字段省略，不为了补 `pageCount`/标签而逐条拉详情，避免慢、风控和登录态副作用。
- **本地搜索规则抽公共核心**：把 `LocalSearchPage` 私有匹配逻辑迁到 `foundation/local_search_core.dart`，让 UI 和 AI `search_local` 使用同一套匹配规则。

### 接口/行为契约

- `AiToolResult.toJson()` 统一输出 `{ok, data?, message?}`。
- `AiToolRegistry.dispatch()` 对未知工具名、参数错误、工具内部 throw 都返回 `AiToolResult.failure(...)`，不向外抛异常导致对话层崩溃。
- `source` 固定四值：`picacg` / `jm` / `ehentai` / `nhentai`；`normalizeAiSource()` 兼容 `eh/exhentai/e-hentai` 和 `nh/n-hentai` 简写。
- `download_comic` 当前能力层语义是“一次一本、直接入队”；用户确认、候选对照卡、批量勾选属于未来 UI 层职责。
- `get_download_status` 读取 `OnlineDownloadManager.instance.tasks`，返回下载中/暂停/完成/错误等状态与进度。

### 字段验证结论

| 源 | 搜索结果页数字段 | 处理方式 |
|---|---|---|
| picacg | 有 `PicacgComicItemBrief.pages` | 直接填 `pageCount` |
| ehentai | 有 `EhGalleryBrief.pages` | 直接填 `pageCount`，但只暴露第一页 |
| jm | 搜索结果无稳定页数 | 省略，不进详情补 |
| nhentai | 搜索结果无稳定页数 | 省略，不进详情补 |

E-Hentai 的分页是 `Galleries.next` 游标，不是普通 `page`；本轮工具 schema 暂不暴露 next，避免伪造页码语义。

### 踩坑与不要再走的路

- **不要从 `BaseComic.description` 反解析结构化字段**：Picacg 的 description 是展示文本（喜欢数 + 页数），结构化页数应直接读 `PicacgComicItemBrief.pages`。
- **不要把能力层注册接到启动流程**：本轮目标不是启用 AI 功能，只是把工具能力放进库里。
- **不要在本地检索里混用 `LocalLibraryManager` 和 `DownloadManager` 两套来源**：否则同一本下载项可能重复，也会偏离现有 `LocalSearchPage` 行为。
- **不要让 `dispatch()` 把异常抛给上层对话**：AI 对话层最怕工具异常炸整个流程，工具失败应结构化返回 `ok:false + message`。

### 技术约束/铁律

- `download_comic` 是真实联网 + 写下载队列操作，未来 UI 必须在调用前做候选清单/对照卡/勾选确认；不要让模型一句话直接批量下载。
- `search_online` 不补拉详情是硬边界：搜索结果要快、可控、低风控；详情补全应由用户确认某个候选后再做。
- `search_local` 历史分支只有在 `HistoryManager.isInitialized == true` 时才返回历史；未初始化时应无结果但不崩溃。

### 模块职责演变

- `lib/foundation/ai/` 成为驱动无关 AI 能力层：工具抽象、注册表、source 常量、四个工具与惰性注册入口。
- `LocalSearchPage` 从“自带私有匹配规则”演变为“UI + 调用公共匹配核心”，匹配规则由 `local_search_core.dart` 统一维护。

### 残余风险/未验证假设

- 真实联网 dispatch 尚未跑：账号态、网络态、风控态会影响 `search_online/download_comic`。
- `download_comic` 尚未真实入队验收；后续应在有用户确认 UI 或专项小体量测试时验证。
- E-Hentai 只暴露第一页；未来若 AI 需要继续翻页，需要把 `next` 游标加入工具 schema 或引入会话态 loader。

### 涉及文件

- `lib/foundation/ai/ai_tool.dart`
- `lib/foundation/ai/ai_tool_registry.dart`
- `lib/foundation/ai/ai_sources.dart`
- `lib/foundation/ai/ai_capabilities.dart`
- `lib/foundation/ai/tools/search_online_tool.dart`
- `lib/foundation/ai/tools/download_comic_tool.dart`
- `lib/foundation/ai/tools/search_local_tool.dart`
- `lib/foundation/ai/tools/download_status_tool.dart`
- `lib/foundation/local_search_core.dart`
- `lib/pages/local_search_page.dart`

---

## §73 AI 对话驱动层接入（LLM 客户端 + 工具调用循环 + 对话 UI）

> 精简版对应：[主线第五轮：AI 对话驱动层接入](交接文档-精简最新版.md#在线能力移植)。完整版对应 §93。

### 架构决策

- **非流式优先**：第一版选择非流式响应，实现简单，3 轮工具调用足以完成大部分任务；流式后续可扩展（`ResponseType.stream` + SSE 解析）。
- **工具调用循环上限 3 轮**：防止无限循环；3 轮已覆盖"查本地→在线搜索→生成回复"的典型链路。
- **`download_comic` 硬拦截**：在 `AiConversationController._runLoop()` 中挂起，设置 `pendingDownload`，禁用输入框，等待用户确认后才真正入队；其他工具直接执行不阻塞。
- **System prompt 内置**：不走 `settings[132]`，减少配置复杂度；后续可扩展为读取 index 132 允许用户覆盖。
- **`AiPage` 直接包装 `AiChatPage`**：工具调试页通过 Navigator push 进入，不占主 Tab 位。

### 接口/行为契约

- `LlmClient.chat()` 从 `appdata.settings[127-131]` 读 baseUrl/apiKey/modelId/params，白名单参数只接受 `temperature`/`top_p`/`max_tokens`；错误统一返回 `LlmResponse(error: "...")`，不抛异常。
- `AiToolRegistry.dispatch()` 失败也返回 `AiToolResult.failure`，不抛；`_runLoop` 对 dispatch 失败照常注入 tool result 消息，继续循环。
- `pendingDownload` 为非 null 时输入框禁用；`confirmDownload(false)` 注入 `{"ok":false,"message":"用户取消了下载"}` tool result，然后继续循环。

### 踩坑与不要再走的路

- `_PendingDownload` 私有类型在公开字段中触发 `library_private_types_in_public_api` 警告；改为 `PendingDownload`（公开类型）。
- `compute()` 入口必须是顶层/静态函数，不能传 closure 或实例方法，否则运行时抛 `Invalid argument`。
- 不要在 `LlmClient` 声明 `_maxToolRounds`，循环上限应在 `AiConversationController._maxRounds` 处声明，避免跨层冗余常量。

### 残余风险

- Ollama/DeepSeek `tool_calls.arguments` 格式可能与 OpenAI 略有差异（有的直接是 Map 而非 JSON 字符串），需在 `LlmToolCall.fromJson()` 处加兼容。
- 3 轮工具调用是否足够，取决于实际任务复杂度；可后续调整 `_maxRounds` 或改为配置项。

### 涉及文件

- `lib/foundation/ai/llm_client.dart`（新增）
- `lib/foundation/ai/ai_conversation.dart`（新增）
- `lib/pages/ai/ai_chat_page.dart`（新增）
- `lib/pages/ai/ai_page.dart`（改造）

---

## §74 AI 多会话持久化（会话列表 + 侧栏 + 本地存储）

> 精简版对应：[主线第五轮：AI 多会话持久化](交接文档-精简最新版.md#在线能力移植)。完整版对应 §95。

### 架构决策

- **纯 IO 存储层 `AiConversationStore`**：不持有 UI 状态，只做文件读写和索引维护，UI 层（`AiChatPage`）负责创建/切换 `AiConversationController`。
- **System prompt 不持久化**：重建时重新插入，避免版本升级后旧 system prompt 污染新逻辑。
- **会话上限 50 条，超出删最旧**：平衡存储占用与可访问历史，不设无上限。
- **标题自动取首条 user 消息前 15 字**：v1 不做手动重命名。

### 接口/行为契约

- 会话文件路径：`${App.dataPath}/ai_conversations/{id}.json`；索引：`${App.dataPath}/ai_conversations_index.json`。
- `AiConversationController.create({loadId})` 是 async；UI 的 `initState` 不能 `await`，应用 `FutureBuilder` 或 `setState` 回调完成后刷新。
- `clear()` 生成新 `conversationId`，不继续写入旧 id 的文件。
- `_save()` 在 `send/confirmDownload/clear` 完成（`isLoading=false`）后调用。

### 踩坑与不要再走的路

- `AiChatPage` 必须升级为 `StatefulWidget` 才能持有 `AiConversationController`，不能继续用 `ChangeNotifierProvider(create:...)` 在 `StatelessWidget` 里初始化异步 controller。
- 持久化 history 时不要把 system prompt 那条也序列化，否则升级逻辑改了 system prompt 后旧会话会带着旧 prompt 继续请求。

### 残余风险

- 会话文件存储在 App 私有目录，无额外加密；对话内容若含敏感信息，后续可考虑加密存储。
- `loadIndex()` 返回的 `updatedAt` 用于排序和时间展示，若系统时间被修改可能导致顺序异常（概率低，暂不处理）。

### 涉及文件

- `lib/foundation/ai/ai_conversation_store.dart`（新增）
- `lib/foundation/ai/ai_conversation.dart`（序列化 + 自动保存）
- `lib/foundation/ai/llm_client.dart`（`LlmMessage.fromJson`）
- `lib/pages/ai/ai_chat_page.dart`（侧栏 Drawer + StatefulWidget 升级）

---

## §75 AI 工具结果漫画清单页与消息级汇总条

> 精简版对应：[主线第五轮：AI 工具结果漫画清单页与消息级汇总条](交接文档-精简最新版.md#在线能力移植)。完整版对应 §96。

### 架构决策（含被否决方案）

- **被否决方案**：最初把"查看 N 条结果"按钮挂在工具结果卡片内部。问题：工具结果卡是"原始数据展示"语义，混入交互入口后职责不清；且 AI 文字回复仍会把同一批数据再用 markdown 表格重复输出一遍，信息重复。
- **最终方案**：把清单入口从工具卡移到独立的 `resultList` 消息类型，挂在 AI 文字回复气泡下方；文字回复通过 system prompt 约束为"只简要总结"，从根源上避免 LLM 重复输出表格（不是靠前端过滤或正则清洗markdown，是在 prompt 层面解决）。
- **items 缓冲区挂在 controller 实例字段**：一轮对话内所有工具结果的 items 汇总进同一张清单卡，不区分来源工具；这是已知简化（多个不同语义查询工具在同一轮出现是少见场景）。

### 接口/行为契约

- `AiResultItem.fromToolData(toolData)` 只认 `{'items': List<Map>}` 形状，其他形状返回空列表，不抛异常。
- `resultList` 消息必须紧跟在触发它的 assistant 文字消息之后 add，顺序契约：`[toolCall] → [toolResult]* → [assistant文字] → [resultList]`（如果本轮有 items）。
- `_pendingResultItems` 只在成功 flush 进 `resultList` 消息后清空，或 `_createNew()` 时清空；工具调用轮次继续（未到文本收尾）时不清空，允许多轮工具调用的 items 累积。

### 根因推理链（清单页塌陷 bug）

- 症状：`AiItemListPage` 点进去漫画卡片不渲染，页面空白/塌陷。
- 根因：`DownloadedComicTile` 内部两种展示模式（详情模式用 `LayoutBuilder` 取父级高度约束、简明模式用 `Stack`+`Positioned.fill` 撑满父级）都要求父级提供明确的尺寸约束；`ListView.builder` 的 item 在主轴方向是自适应内容高度的松散约束，给不出这个尺寸，tile 内部布局无法计算而塌陷。
- 修复：改用 `SliverGrid(gridDelegate: SliverGridDelegateWithComics())`，与 `history_page.dart` 已下载库网格写法同构，由 gridDelegate 计算出明确 cell 宽高。

### 踩坑与不要再走的路

- 不要给 `DownloadedComicTile` 套 `ListView.builder`/`Column`/`Wrap` 等不提供明确尺寸约束的松散容器，除非改 tile 内部实现，否则必定塌陷。
- 撤回旧交互入口（如工具卡按钮块）时要确认相关 import 是否仍被新组件引用，不能盲目一并删掉——本轮 `ai_result_item.dart` 的 import 从 `_ToolCard` 转移到 `_ResultListEntryCard` 继续使用。
- 不要试图靠"清洗 LLM 输出的 markdown 表格"来解决重复展示问题，应该在 system prompt 层面约束模型不要生成表格。

### 技术约束/铁律

- 【来源：本轮根因排查，项目内已有实践约定】`DownloadedComicTile` 必须在提供明确网格尺寸约束的父级（Sliver 网格/GridView 等）中使用。
- 【来源：契约设计】`AiChatMessage.resultList` 的 `toolData` 固定为 `{'items': List<Map<String,dynamic>>}` 形状，与 `AiResultItem.fromToolData()` 解析约定对齐；持久化时该形状随 `toJson/fromJson` 走既有 Map 分支，无需额外处理。

### 残余风险/未验证假设

- `resultList` 消息的 `toolData`（含嵌套 `availability` 等 Map）经 `jsonEncode`/`jsonDecode` 持久化往返未实测；理论上与 `AiResultItem.fromJson` 同构，风险低但未实证。
- 多个不同语义查询工具在同一轮被合并进一张清单卡的简化设计，如未来出现语义混淆需另开计划处理。

### 涉及文件

- `lib/foundation/ai/ai_result_item.dart`（新增）
- `lib/pages/ai/ai_item_list_page.dart`（新增，后改用 SliverGrid）
- `lib/foundation/ai/ai_conversation.dart`（`resultList` 类型 + 缓冲区 + system prompt）
- `lib/pages/ai/ai_chat_page.dart`（`_ToolCard` 撤回按钮块，新增 `_ResultListEntryCard`）

---

## §76 AI清单底栏样式 + 对话保活修复

> 精简版对应：[AI清单底栏样式 + 对话保活修复](交接文档-精简最新版.md#在线能力移植)。完整版对应 §97。

### 根因推理链（Bug1，搜索卡顿真根因修正）

- 症状：远程·已下载 tab 搜索框刚打字就卡住（键盘卡住无响应）。
- 排除误诊：先前 13-计划归因为"需给 `RemoteLibraryComicItem` 加快速路径"，修复方向不算错但没修到真正热点函数。
- 决定性根因：`_translateDownloadedTag`（`download_page.dart`）用双重 `for` 遍历 `map.entries` 做字符串比较，而 `tagTranslations` 的 key 加载时已统一转小写存储（`tags_translation.dart`）——数据结构本身具备 O(1) 查找能力，旧实现完全没用上，退化成 O(33739) 全表扫描。任何没命中翻译的 tag（picacg 中文标签、eh/nh 罕见标签都很常见）都会扫完全部条目才返回原值，这是保证发生的最坏情况，不是偶发。
- 为什么之前没事：该实现自 V1.2 起未变，之前"远程已下载"只有 picacg 条目、每条 tag 少（3-8个）；接入 eh/nh 后这两个源标签体系远比 picacg 丰富（常见 8-9 个标签），条目数×标签数×33739 次比较的总量级从可接受跳到 ANR 级——是下游数据密度变化暴露了旧代码里一直存在的性能陷阱，不是新 bug。

### 修法

- `_translateDownloadedTag` 内部改为 `final lowerTag = tag.toLowerCase(); map[lowerTag]` 哈希直查，12 个分类各 O(1)，复杂度从 O(33739) 降到 O(12)；函数签名、返回值行为、调用点均不变。

### 根因推理链（Bug3，AI对话页切tab状态丢失）

- 现象：切到其他 tab 再切回 AI 对话 tab，消息列表回顶、输入框草稿丢失。
- 根因：`main_page.dart` 的 `NaviPane` 每个 tab 独立 `Navigator`，切 tab 走 `pushAndRemoveUntil`（整栈替换），`AiChatPage` 每次切回都完全重建；项目未用 `IndexedStack`/`PageView`，`AutomaticKeepAliveClientMixin` 对此无效。
- 进一步确认：`AiConversationController.create()` 是静态工厂，每次都 `_internal()` 新建实例，从磁盘反序列化 `displayMessages`/`history`，全局没有任何注册表/单例缓存复用旧 controller——挂在 controller 实例字段上的草稿在 tab 切回后必然丢失。

### 修法

- 滚动到底部：新增 `_scrollToBottomAfterFrame()`，用 `addPostFrameCallback` + `hasClients && maxScrollExtent > 0` 双重 guard 后 `jumpTo`；在 `_loadController`/`_switchConversation`/`_newConversation` 三处 `setState` 之后各调用一次。
- 草稿持久化：挂在 `_AiChatPageState` 的 `static final Map<String, String> _draftsByConversationId`（随应用生命周期存在，不随 State 实例销毁重建），按 `conversationId ?? ''` 分 key；`TextField.onChanged` 写入，`_send()` 发送成功后 `remove` 对应 key。

### AI清单底栏样式修复

- `ai_item_list_page.dart` 的 `_showItemDetail` 两分支（realItem 存在 / fallback）均改为 `showModalBottomSheet(isScrollControlled: true, showDragHandle: false, useSafeArea: false, backgroundColor: Colors.transparent)` + 内层 `Material(color: surface, surfaceTintColor: surfaceTint, shape: 顶部圆角16, clipBehavior: antiAlias)` 包裹原内容，对齐 `download_page._showInfo` 视觉。

### 踩坑与不要再走的路

- 排查搜索卡顿类问题时，不能只看"新功能接入前后有没有性能问题"，还要看"新功能接入是否改变了旧代码路径上的数据规模/密度"——即使旧代码逻辑本身没有任何改动，也可能因为上游数据特征变化从"能忍"变成"ANR"。
- 不要以为 `AutomaticKeepAliveClientMixin` 能解决 tab 切换状态丢失——本项目 tab 架构是 `NaviPane` + `pushAndRemoveUntil` 整栈重建，不是 `IndexedStack`/`PageView`，该 mixin 对此无效。
- 不要把有状态数据挂在"可能被重建的实例"（如本例的 controller）上做跨 tab 持久化；应挂在生命周期更长的载体上（static 字段、磁盘、全局单例）。

### 技术约束/铁律

- 【来源：本轮根因排查】`_translateDownloadedTag` 类的标签翻译查找必须走哈希直查（`map[key]`），不能手写 `entries` 遍历 + 字符串比较，否则数据密度变化会让 O(n) 复杂度从"能忍"变成 ANR。
- 【来源：计划强制约束，本轮遵守】不改 `main_page.dart` 的 `NaviPane`/`pushAndRemoveUntil` tab 切换架构；不引入 `IndexedStack`/`PageView`/`AutomaticKeepAliveClientMixin`；不改消息列表为 `reverse: true`。

### 残余风险/未验证假设

- Bug1 修复后是否真的解决了用户实测的卡顿，依赖真机验证；本轮只做代码层面复杂度分析和逐行核对，未做实际耗时对比。
- Bug3b 的草稿 static Map 是进程内存态，App 完全重启后草稿会丢失（static 字段固有特性，非 bug，但用户可能有"草稿应跨重启保留"的隐含预期）。

### 涉及文件

- `lib/pages/download_page.dart`（`_translateDownloadedTag`）
- `lib/pages/ai/ai_item_list_page.dart`（`_showItemDetail` 两分支）
- `lib/pages/ai/ai_chat_page.dart`（`_scrollToBottomAfterFrame`/`_draftsByConversationId`）

---

## §77 AI 提示词标签面板 + 来源限定 + 结构化改造

> 精简版对应：[主线第五轮：AI 提示词标签面板](交接文档-精简最新版.md#在线能力移植)。完整版对应 §103。

### 架构决策

- **独立标签模块**：提示词标签解析、存储、来源映射集中在 `ai_prompt_tags.dart`，不分散到 UI 层、控制器和设置页各写一套，否则正则和 JSON 格式三处不同步必然出 bug。
- **来源约束双层防御**：schema 过滤（深复制 `search_online` 的 `source.enum` 替换）+ 控制器 dispatch 前硬拦截，不靠 system prompt 文字引导模型自觉遵守。
- **长期状态持久化到会话元数据**：不追加到 `_history` 的 system 消息（`create()` 加载时会丢弃 system 消息），而是单独存字段，每次请求时由 `_buildRequestMessages()` 重新注入，临时标签不落盘。
- **标签插入改为结构化集合**：面板点选写入 `_selectedTagChips`（不写进输入框文本），发送时才与手输文本合并。旧"往输入框里拼 `#标签名` 字符串再靠正则识别"路径作为并行 fallback 保留，两路发送时合并去重。
- **会话文件向后兼容升级**：新增长期标签/来源快照字段为可选新增（缺失时默认为空），不强制升级旧文件格式。

### 接口/行为契约

- `search_online` 的 schema 过滤必须深复制嵌套 `Map`/`List`，不得修改注册表返回的共享 const schema；否则约束全局污染后续对话（甚至其他会话）。
- 来源约束放在控制器 dispatch 前，不放入全局工具注册表，避免把会话态塞进无状态单例。
- `#搜本地` 与在线来源标签互斥，执行层以本地限定优先（即使两者同时存在）；`get_download_status` 不受范围限制。
- 多轮工具循环中短期来源约束必须持续到本次 `send()` 完整结束，不能在工具循环间隙清零。

### 踩坑与不要再走的路

- 不要直接修改 `AiToolRegistry` 返回的 schema 对象：工具注册表返回的 `Map` 可能是 const 或被多处引用，原地修改会污染后续所有对话的 tool schema。
- 不要把临时/长期标签指令追加为 history 里的普通 system 消息：`AiConversationController.create()` 加载已保存会话时会丢弃所有持久化的 system 消息，只重建基础 system prompt，追加的标签指令静默消失。
- 不要让标签 state 只活在输入框文本里：退格键在 `#搜pica` 中间删字变成 `#搜pic`，被正则识别为无效标签却不报错，用户无任何提示，对话默默失去来源约束。

### 技术约束/铁律

- 【来源：本轮架构设计】来源约束必须在 `_runLoop()` dispatch 前硬拦截，不能只靠 schema 过滤（LLM 可能幻觉使用不在 schema 中的工具名）。
- 【来源：持久化设计】长期状态序列化用独立字段，`_history` 只保存消息，不混入元数据。

### 残余风险/未验证假设

- `#搜本地` 与来源标签的互斥 UI 可以在面板层防止大部分情况，但用户手动在输入框里同时打 `#搜本地 #搜pica` 仍可能产生冲突，执行层已按本地优先处理，但用户消息气泡会同时高亮两个标签，可能引起困惑。
- 普通提示词标签的实际效果依赖 LLM 的理解和遵从度，属于 prompt 质量问题，不影响代码正确性。

### 涉及文件

- `lib/foundation/ai/ai_prompt_tags.dart`（新增）
- `lib/foundation/ai/ai_conversation.dart`
- `lib/foundation/ai/ai_conversation_store.dart`
- `lib/foundation/ai/ai_settings.dart`
- `lib/base.dart`
- `lib/pages/ai/ai_chat_page.dart`
- `lib/pages/settings/ai_settings_page.dart`
- `lib/pages/settings/settings_page.dart`
- `test/ai_prompt_tags_test.dart`（新增）
- `test/ai_conversation_prompt_test.dart`（新增）

---

## §81 AI对话空回复与长期范围标签回显（第九轮）

### 现象

- LLM 返回没有 tool call 且 `content` 为 null/空白时，消息列表出现空 assistant 气泡。
- 来源限定或 `#搜本地` 长期生效后，第二轮不重新点击 chip，气泡下方不再显示对应标签。

### 根因与修法

- `_runLoop()` 原先无条件把最终文本写入 `displayMessages` 与 `_history`。现在由 `_handleFinalTextResponse` 统一处理：trim 后有内容才写入；无内容且没有待展示清单时只显示轻量提示，不污染 history；有清单时不重复提示。
- 范围标签回显必须在 `_persistentAllowedSearchSources`、`_persistentLocalOnly`、`_activeTurnContext` 落地后，读取 `effectiveAllowedSearchSources`/`effectiveLocalOnly`。这组 getter 同时服务 system prompt 和 dispatch 硬拦截，是唯一的最终生效范围来源。

### ❌ 不要再走的路

- 不要在 UI 层用另一套判定补空回复；生产收尾逻辑必须和测试入口共用同一方法。
- 不要把范围标签并入 `mergedPromptTags`/持久化普通标签，否则会把来源约束污染成普通标签状态。
- 同一工作树多个执行者读改写同一个文件时，最终写入可能覆盖另一处不重叠改动；写后必须重新读取并核对所有并发区域。

### 接口/测试契约

- `_handleFinalTextResponse`：正常文本保留原始字符串写 history；空白不写 history；清单卡存在时只 flush 清单。
- `simulateFinalTextResponseForTesting` 不触发网络请求，仅驱动生产收尾逻辑。
- 相关 AI 测试 60 项通过，`flutter analyze` 0 issue；真实 provider/GUI 触发仍未验证。

### 涉及文件

- `lib/foundation/ai/ai_conversation.dart`
- `test/ai_conversation_test.dart`
- `test/ai_conversation_prompt_test.dart`

## §82 EH下载续传与并发提速（第九轮）

### 根因推理链

`currentPage` 被入口重置为 0，且旧代码先 `getEhImageUrl` 解密、再由 `_downloadFile` 判断文件是否存在，导致续传时已完成页仍消耗网络请求，进度缓慢爬升。并发后 `currentPage` 只表示完成数量，不能再当作已完成页集合。

### 修法与契约

- 循环最前用 `_ehPageFileExists` 检查四种扩展名且文件非空；命中页直接计数和通知，不占并发槽。
- 未完成页用固定 3 并发 `_Semaphore`，对齐 `EhNetwork` 的 ehgt 官方闸值；图片字节请求在 `_downloadFileOnce` 内统一 acquire/release。
- `_downloadFile` 内原有存在性检查仍保留，作为外层判断的第二道防线；归档模式、showKey/mpvKey 状态机不在本轮范围。

### ❌ 不要再走的路

- 不要把存在性检查放回 `_downloadFile` 内部：那时解密请求已经发生。
- 不要用 `currentPage` 推断页码集合；并发乱序会使该语义失真。
- 不要为了并发另加可调节配置，避免用户把 ehentai 请求数调过官方限制。

### 残余风险

无真实 EH 账号/画廊，未验证耗时下降、暂停续传和隐性限流；扩展名枚举未来新增格式时需同步更新。

### 涉及文件

- `lib/foundation/online_download_manager.dart`
- 只读依据：`lib/network/eh_network/eh_main_network.dart`

## §83 本地详情在线跳转 ID 与 Empty data 防御（第九轮）

### 根因推理链

历史 JM 记录缺少 `comicId` 时，`DownloadedJmComic.id` 只有裸 `jm` 前缀，旧跳转逻辑直接截取空字符串并请求 `/album?id=`，网络层才以 `Empty data` 兜底。04号补了前置校验；06号进一步发现校验输入也可能错：`LocalLibraryComicItem.id` 是内部 `itemId`，真实来源字段是 `originalId`。

### 修法与契约

- `resolveOnlineRawId` 对包装项取 `originalId`，对具体下载类保持 `comic.id` 兼容行为。
- JM/NH 取值去前缀后必须为非空纯数字；picacg 也统一做空值拦截；失败统一提示、记 warning、不发网络请求。
- 提取为顶层纯函数，便于不构造完整 Widget/网络环境直接测试。

### ❌ 不要再走的路

- 不能把“防御校验已加”当成来源正确；先核对字段语义，再校验格式。
- 不要因为只发现 JM 风险就假定其他源安全；本轮确认三源存在同一 `id`/`originalId` 错位模式。

### 状态与风险

04号计划状态为部分完成，06号代码验证完成；`flutter analyze` 0 issue，06号 8 项单测通过。未用用户实际记录做真机验证，也未全库审计其他 `comic.id` 调用点。

### 涉及文件

- `lib/pages/local_comic_detail_page.dart`
- `test/local_comic_detail_online_id_test.dart`

## §84 本地详情在线信息更新与整体覆盖（第九轮）

### 架构决策

- 从 `sourceRowJson` 还原具体下载子类，详情显示不依赖包装项丢失的来源字段。
- `TrashManager` 只公开 `updateSourceDbRowMetadata`，内部复用 `_mutateSourceDbFile`，避免把整套特权 IO 暴露成通用公共 API。
- `buildUpdatedDownloadedRecord` 为顶层纯函数，执行整体元数据覆盖；网络完整成功后才写库。

### 接口/行为契约

- 只改 `title`/`subtitle`/`json`，不改下载时间、目录、大小、ID、章节和本地文件结构；新值为空也按服务端结果覆盖。
- 类型不匹配或 sourceRowJson 损坏时安全返回 null；写库失败不替换内存对象。
- `max` 等结构字段不得被在线详情覆盖，这是本轮最重要的边界。

### ❌ 不要再走的路

- 不要分字段逐步写库，避免网络部分成功形成半成品。
- 不要把 `_mutateSourceDbFile` 整套私有特权 IO 直接公开；后续同类场景先复用最小接口。
- 不要把 `_isUpdatingInfo` 只当作功能已完成的证据；当前按钮仍缺 loading/置灰视觉反馈。

### 验证与风险

模型 round-trip/旧字段兼容 8 项、更新信息 13 项测试通过，`flutter analyze` 0 issue；未做真实 jm/nhentai 设备端到端验证，EH 更新信息不在范围内。

### 涉及文件

- `lib/foundation/download_model.dart`
- `lib/foundation/trash_delete.dart`
- `lib/pages/local_comic_detail_page.dart`
- `test/download_model_new_fields_test.dart`
- `test/local_comic_detail_update_info_test.dart`

## §85 EH本地解析与标签显示闭环（第十轮）

### 根因推理链

- EH 画廊 ID 含连字符，旧扫描函数用 `contains('-')` 抢先归类为 `CustomDownloadedItem`；同时 EH JSON 写 `size`，旧分支读 `comicSize`，于是类型和大小同时错误。
- `Wrap` 没有行数上限，标签超两行时会争抢作者和底部信息空间；固定高度裁剪还会把隐藏 child 留在布局/语义树中。
- 本地详情直接展示原始 EH/NH 标签，且翻译表异步预热后页面不会自动重建；NH 空分类桶还会错误吞掉 flat 标签。

### 修法与契约

- 扫描层统一委托 `parseDownloadedItemRecordJson`；`DownloadedGallery.fromJson` 兼容平铺/旧嵌套、数字/字符串 size、`tagList`/`tags`。
- `DownloadedComicTile.maxTagRows` 默认 null 保持旧调用者行为，仅下载列表传 2；可见标签必须在构建前按实际文字、缩放和宽度测量。
- 本地详情使用来源感知显示模型，复用在线 `tagTranslateCategory`/`tagTranslateWithNs`；按第一个冒号拆分、保留原始搜索值，翻译异步完成后主动重建；NH 只有存在有效分类桶才压制 flat 标签。

### ❌ 不要再走的路

- 不要在扫描层再维护一套下载记录类型判断；权威解析入口只有 `download_model.dart`。
- 不要用固定高度 + clip 假装实现两行；文字缩放和语义树都会出问题。
- 不要维护本地第二份标签翻译表，也不要禁止未知 namespace 走在线函数已有的全表兜底。

### 状态与风险

01号计划代码验证完成；02号计划部分完成。定向 42 项、全量 161 项测试通过，`flutter analyze` 通过；真实截图记录、实体机、横屏/大字体验收待补。

### 涉及文件

- `lib/components/comic_tile.dart`
- `lib/foundation/download_model.dart`
- `lib/foundation/local_library_static.dart`
- `lib/pages/download_page.dart`
- `lib/pages/local_comic_detail_page.dart`
- `test/comic_tile_tag_rows_test.dart`
- `test/eh_local_type_size_test.dart`
- `test/download_model_new_fields_test.dart`
- `test/local_comic_detail_tag_display_test.dart`

---

## §78 已下载详情页标签翻译与长标签布局修复（第八轮，45号计划）

> 精简版对应：[已下载页三档隔离](交接文档-精简最新版.md#已下载页三档隔离)。

### 现象

已下载详情页点击漫画底栏弹出章节选择弹层时，长标签会把"阅读/查看详情"按钮挤没；且部分标签（尤其 EH 来源）显示原始英文，未翻译成中文。

### 根因

- `_translateDownloadedTag(tag)` 只做 `tag.toLowerCase()` 后整表查找，从未按 `:` 拆分 namespace 前缀，也未处理 `♀`/`♂` 性别符号后缀。对比同文件里的 `_searchTermsForDownloadedTag()`——它专门 `raw.split(':').last.trim()` 取值部分再调用 `_translateDownloadedTag`，说明作者本知道要拆分，只是漏了在"显示"路径本体里做同样处理。
- EH 来源 `_generateTags()`（`lib/network/eh_network/eh_models.dart`）把 tags Map 拍平成 `"$key:$element"` 形式（如 `"Female:elf"`，namespace 首字母大写）写入下载落盘的 `tagList`，而 `assets/tags.json` 的 key 都是不带前缀、不含性别符号的纯标签名，导致查表必然落空。
- 标签 `Wrap` 内的 `Container` 没有宽度约束，长标签把整行撑到很宽，挤占了底部固定按钮区域的空间。

### 修法

- `_translateDownloadedTag()` 处理顺序：先 `:` 拆分取值部分 → 再做性别符号剥离（`endsWith(' ♀'/' ♂')` + `substring`，记录后缀）→ 用处理后的值查 `tagTranslations`（遍历现有 map，与 `_searchTermsForDownloadedTag()` 保持同一套查表方式）→ 命中则返回译文+符号；未命中至少返回"去掉 namespace、保留符号"的值；发生异常兜底返回原始 `tag`。
- `_buildHeader()` 内标签 `Container` 加 `constraints: const BoxConstraints(maxWidth: 140)`，内部 `Text` 加 `maxLines: 1` + `overflow: TextOverflow.ellipsis`。单行固定高度的 `Wrap`，不会再挤占按钮区域。

### ❌ 不要再走的路

- 不要只在"搜索"路径做 `:` 拆分——显示路径（`_translateDownloadedTag` 本体）必须做同样处理，否则两条路径行为不一致，用户会看到"搜索能匹配到中文标签但详情页显示英文"的分裂现象。
- 不要用 `replaceFirst(' ♀', '')` 剥性别符号——它替换字符串里第一次出现的位置，不保证在末尾；用 `endsWith` + `substring` 严格只处理"后缀"语义。

### 接口/行为契约

`_translateDownloadedTag(String tag)`：输入可以是纯值（`"elf"`）、带 namespace 的值（`"female:elf"`）、带性别符号的值（`"elf ♂"`）或组合（`"male:elf ♂"`）；输出保证命中词表返回中文译文（+符号如有），未命中至少返回去前缀、保留符号的原词，异常兜底返回原始 `tag`。调用方无需关心这套预处理逻辑，直接传原始 tag 字符串即可。

### 残余风险/未验证假设

`maxWidth: 140` 是初始合理值，未做真机/GUI 视觉验证，不同设备 DPI/字号下是否需要微调仍是未验证假设。

### 涉及文件

- `lib/pages/download_page.dart`（`_translateDownloadedTag()`、`_buildHeader()` 标签 `Wrap`）

---

## §79 AI对话多下载工具调用引发400修复（第八轮，46号修复）

> 精简版对应：[主线第五轮：AI 对话驱动层接入](交接文档-精简最新版.md#在线能力移植)。

### 现象

AI 对话聊到中途突然返回 400，新开会话恢复正常；已触发过这个 bug 的旧会话此后每次请求都必现 400（除非清空会话）。

### 根因推理链

- 症状：一轮 LLM 回复里如果同时带有多个 `download_comic` 工具调用，触发后续请求 400。
- 排查：`_runLoop()` 原实现 `for (final toolCall in response.toolCalls!) { ... if (toolName=='download_comic') { pendingDownload=...; return ...; } ... }`——`return` 直接跳出整个方法，`for` 循环遍历到第一个 `download_comic` 就终止，后续尚未遍历到的 tool_call（无论是否也是下载）永远不会进入循环体，永远不会被 `_appendToolResult` 追加对应的 `tool` 消息。
- 传导链：assistant 消息的 `tool_calls` 字段在循环开始前就已经整体写入 `_history`（`_history.add(LlmMessage.assistant(toolCalls: response.toolCalls))`），但循环提前退出导致其中部分 `tool_call_id` 永远缺失配对的 `tool` 消息 → 下次调用 `LlmClient.chat()` 时，请求体里这条 assistant 消息的 `tool_calls` 与后续 `tool` 消息数量不匹配 → DeepSeek/OpenAI 兼容 API 校验请求格式判定为非法请求 → 返回 400。
- 决定性证据：这个"损坏"状态永久保留在 `_history` 里，新会话 `_history` 从空开始不受影响，所以新开会话正常、旧会话必现且反复复现。

### 修法

- `PendingDownload? pendingDownload` 字段改为内部队列 `List<PendingDownload> _pendingDownloads` + 只读 getter（返回队首，队列空则 `null`），对外类型签名完全不变（仍是 `PendingDownload?`），UI 层（`ai_chat_page.dart`）全部既有引用点（`== null`/`!= null`/`!` 等只读判断）零改动即可兼容。
- `_runLoop()` 里"遍历 toolCalls 并逐个处理"的循环体抽成 `_processToolCalls(List<LlmToolCall> toolCalls)`：遇到 `download_comic` 时 `_pendingDownloads.add(...); continue;`（不再 `return`），循环遍历完整个 toolCalls 列表后统一判断——只要 `_pendingDownloads` 非空就返回 `waitingForDownloadConfirmation`（暂停等待确认），否则递归调用 `_runLoop()` 续跑下一轮。这样一轮回复里无论出现 0/1/N 个 `download_comic`，所有 tool_call 都会被完整遍历一次。
- `confirmDownload(bool confirmed)` 改为出队处理队首一个：追加对应 `tool` 消息、`_save()` 后，若队列仍非空则直接返回（停留在等待确认状态，不触发 `_runLoop()`），只有队列真正清空后才 `await _runLoop()` 续跑，让 LLM 看到全部下载结果继续对话。
- 新增 `@visibleForTesting` 测试钩子：`pendingDownloadQueueLengthForTesting`、`simulateToolCallRoundForTesting(List<LlmToolCall>)`（跳过真实网络请求，模拟"LLM 一轮回复带有这些 tool_calls"）、`historyForTesting()`，用于单元测试直接驱动 `_processToolCalls` 验证不产生孤儿 tool_call。

### 接口/行为契约

新增核心不变式：**任意时刻，`_history` 中每一条 assistant 消息的每一个 `tool_call_id`，最终都必须有且仅有一条对应的 `tool` 角色回复消息**。这个不变式在"确认/取消尚未处理完队列中所有下载"的中间状态下允许暂时不满足（用户操作尚未完成），但队列清空后必须立即满足。

### ❌ 不要再走的路

- 不要在多工具调用的遍历循环里用 `return` 提前跳出——只要 assistant 消息的 `tool_calls` 已经整体写入 history，就必须保证每个 `tool_call_id` 都有配对的 `tool` 消息，否则下次请求必然 400。任何客户端侧多工具调用循环逻辑，只要涉及"遇到某类工具就特殊处理并提前返回/跳出循环"，都要先检查是否会导致其他 tool_call 被跳过。
- 把可赋值字段改成只读 getter 后，务必全局搜索所有赋值点（本次 `clear()` 里的赋值点是编译报错才发现的，Dart 编译期检查能兜底但不能替代主动全局搜索的习惯）。

### 技术约束/铁律

【本次诊断确认】OpenAI/DeepSeek 兼容 Chat Completions API 对 `tool_calls` 消息有硬性格式约束：assistant 消息里声明的每一个 `tool_call.id`，必须有且仅有一条 `role:"tool"`、`tool_call_id` 匹配的消息配对，这是硬约束，是本类 bug 最容易复发的模式。

### 残余风险/未验证假设

未在真实 GUI/真实 AI Provider 环境下验证"LLM 是否真的会在一轮回复里发出多个 `download_comic`，还是总是分轮各发一个"——本次修复只保证"如果 LLM 这样做，客户端不会产生孤儿 tool_call"，是防御性修复，不依赖该前提是否成立。

### 涉及文件

- `lib/foundation/ai/ai_conversation.dart`（`PendingDownload` 队列化、`_processToolCalls`/`confirmDownload`/`clear()`、测试钩子）
- `test/ai_conversation_test.dart`（新增回归测试：一轮2个/1个/0个 `download_comic` 三种场景）

---

## §80 多源搜索分发提示词与标签气泡回显补全（第八轮，47号计划）

> 精简版对应：[主线第五轮：AI 提示词标签面板](交接文档-精简最新版.md#在线能力移植)。是 §77（37号计划标签气泡回显）遗留缺口的补全，不修改 §77 涉及计划原文。

### 现象

- 用户在面板同时勾选多个在线来源发消息时，AI 会反过来询问"你想搜哪一个源"，而不是直接对每个选中来源分别搜索。
- 来源限定标签（`#搜pica`/`#搜jm`/`#搜eh`/`#搜nh`）与 `#搜本地` 这类"范围限定"标签，无论是否处于长期生效模式，理论上都应该在用户消息气泡下方回显本轮实际用到的标签；但此前（37号计划）的气泡回显功能只覆盖了"结构化选中的普通标签"，范围限定类标签没有被纳入。

### 根因

- `_buildScopeInstruction()` 生成的多来源 system 提示文本，只说明了"这些来源"是什么，没有明确指示模型"应该对每个来源分别调用一次 search_online"，模型倾向于把"用户选了多个来源"当作需要澄清的歧义，主动向用户提问。
- `send()` 内决定气泡下方显示哪些标签名的 `mergedRecognizedNames`，只合并了正文识别结果和结构化"普通标签"chip（`selectedPromptTags`），没有把 `selectedSources`（来源限定 chip）和 `localOnlyRequested`（`#搜本地` chip）这两类"范围限定"chip 折算进去。

### 修法

- `_buildScopeInstruction()` 在"仅在线 + 多来源"分支内，用 `activeSources.length > 1` 判断，追加一句："这些来源应分别调用一次 search_online（可在同一轮回复内一起发出），不需要询问用户具体选哪一个，取得各来源结果后自行合并处理/展示。"单来源时该变量为空字符串，原有措辞不变。
- `send()` 内新增 `scopeTagNames` 局部变量：遍历 `selectedSources` 用 `aiPromptTagNameForSource(source)` 转换为可读标签名（返回 null 时跳过，不阻断流程），并在 `localOnlyRequested` 为真时追加 `aiLocalOnlyScopeTagName`；再将这些名字去重后追加进 `mergedRecognizedNames` 末尾，**不区分**是否为长期/临时模式。

### 接口/行为契约

- `_buildScopeInstruction()` 新契约：当 `effectiveOnlineOnly && activeSources != null && activeSources.length > 1` 时，返回文本末尾追加固定"分别调用"措辞；单来源时不追加，向后兼容。
- `send()` 的 `mergedRecognizedNames` 新契约：只要本轮 `selectedSources` 或 `localOnlyRequested` 非空，对应的可读标签名一定会出现在该条用户消息 `promptTagNames` 里，且这个行为与 `shouldPersistSelections`（长期/临时模式）**完全无关**——这是对 §77（37号计划）"范围限定跟长期状态绑定"设计的修正，后续如果有类似"新增一种面板 chip 类型"的需求，也应默认遵循"回显不分长期临时"这一原则，除非用户明确要求例外。
- `aiPromptTagNameForSource()` 返回 null 时跳过该来源的标签名追加，不阻断 `send()` 整体流程（防御未来新源缺映射的情况）。

### ❌ 不要再走的路

- 不要把 `scopeTagNames` 并入 `mergedPromptTags`/`mergedPromptTagsByName` 再统一遍历——否则来源/本地限定标签会被误当成"结构化选中的普通标签"混入持久化逻辑，污染 `shouldPersistSelections` 分支里 `_mergePersistentPromptTags(mergedPromptTags)` 的持久化结果（这正是 §44 诊断文档里提到的"归为普通标签"式误判的具体触发路径）。`scopeTagNames` 只应参与 `mergedRecognizedNames` 拼接。

### 残余风险/未验证假设

- 子项 A（分别调用提示词）是纯"建议性"指令，无法通过单元测试验证模型是否真的遵照执行，只能验证提示词文本本身包含预期措辞；若真机观察到 AI 仍偶尔询问用户，不代表本次代码改动无效，需要另外评估是否要进一步强化措辞或改为工具侧强制约束。
- 未做真机 GUI 验证"面板多选来源/`#搜本地` chip 后发消息，气泡下方标签实际渲染效果"，只验证了数据层面 `promptTagNames` 包含预期名字。

### 涉及文件

- `lib/foundation/ai/ai_conversation.dart`（`_buildScopeInstruction()`、`send()` 内 `mergedRecognizedNames` 构造）
- `test/ai_conversation_prompt_test.dart`（子项A：单/多来源提示词测试）
- `test/ai_conversation_test.dart`（子项B：来源/本地限定标签气泡回显测试）

---

## §31 桌面服务端部署（Linux headless + Windows 托盘 + 两端 CLI wrapper + CI 打包）

> 精简版对应：[桌面服务端部署与打包](交接文档-精简最新版.md#桌面服务端部署与打包)。
> 目标：装一个包，有屏开 GUI、无屏（NAS/SSH）起 CLI 服务端，**CLI 服务能力与桌面版完全一致**（管理在网页端）。

### 决定性根因：两套 server，CLI 误接了空壳

- **`PicaKeepHeadlessCliServer`（`lib/server/headless_cli_server.dart`）= 空壳**：`api/library/items` 写死返回 `[]`，`_scanResources` 不扫资源。
- **`PicaKeepAdminServer`（`lib/server/server_app.dart`）= 完整**：经 `LocalResourceScanner` 读资源。
- 旧 CLI（`dart build cli` 编 `bin/picakeep.dart`）接的是**空壳**→「网页后台能开但漫画全空」。用户实测截图即此现象（已下载/图集/资源库全空、历史 0）。
- **为什么不能纯 Dart 编完整服务端**：`PicaKeepAdminServer` 的传递 import 链（trash.dart→app.dart→local_library.dart→remote_library_data_source.dart）拉进 17 个 flutter-polluted 文件，`dart compile exe`/`dart build cli` 编不了 flutter 依赖。纯 Dart CLI 要 12000+ 行重构（已否决）。

### 方案 B（已落地）：同一 Flutter 二进制 + `--server` headless 入口

- `lib/main.dart`：`main(List<String> args)`，`args.contains('--server')` → `_runHeadlessServer`：`App.init()` → `appdata.readEssentialData()` → `ArchiveRegistry.initDefaults()` → 解析 `--config=` → `runtime.configPathOverride` → `LocalServerRuntime.instance.start()`（直接起 `PicaKeepAdminServer`，**绕过** GUI 内 `settings[97]==='server'` 的挡位）→ 打印网址 → SIGINT/SIGTERM 停 → Completer keepalive。**不** `runApp`、不 `initWindowManagerIfDesktop`。GUI 路径零改动。
- **Linux**：Flutter Linux 引擎要 GTK/显示，无 `$DISPLAY` 时在 C++ 层就崩（早于 Dart）。故无屏用 **xvfb** 给虚拟显示。
- **Windows**：GUI exe `--server` **不建可见窗**（实测 `HasWindow=False`）即可读资源（实测 totalComicCount=395），**无需 xvfb**。
- `LocalServerRuntime`：加 `set configPathOverride`，`configPath` getter = `_configPathOverride ?? App.dataPath/...`，让 CLI 数据落在配置文件目录。

### web_console 资源定位（关键坑）

`web_asset_loader.dart` 原用 `Platform.script` 找 `assets/web_console`——**在 Flutter 二进制里 `Platform.script` 为空/无效**，所有候选根失败→网页「资源 not found」。修：改用 `Platform.resolvedExecutable` 的父目录派生候选根（`data/flutter_assets/assets/web_console` 等）。真机验证无需环境变量即可工作。

### Linux deb 打包（`.github/scripts/package_linux_deb.sh <amd64|arm64>`）

- GUI bundle → `/opt/PicaKeep`；装智能 wrapper 到 `/usr/local/bin/picakeep`（子命令 `server/stop/status/logs`，`xvfb-run ... --server`）；hicolor 图标；`control: Depends: libgtk-3-0, xvfb`；postinst 刷图标缓存。
- **wrapper 是 `#!/bin/sh`（Debian=dash）**：必须 `dash -n` 校验，不能用 `elif a; b; then` 复合条件（dash 报错 bash 不报）；`setsid ... </dev/null &` 全分离，让 `picakeep server`（不加 `&`）干净返回。
- **孤儿服务**：stop 不能只认 PID 文件（卸载后丢失→死锁：server 说端口占用、stop 说没运行）。加 `_port_pid()`（按端口 `ss -ltnp`），stop 走 `pid=$(_server_pid) || pid=$(_port_pid)`，status 能识别孤儿。
- **glibc**：在 **ubuntu-22.04（glibc 2.35）** 或 Debian 12 构建，才能在 Debian 12/13 跑；**不要 ubuntu-24.04**（glibc 2.39，装上跑不起来）。
- **tray_manager 的 Linux 编译期依赖（CI 实测踩坑）**：`tray_manager` 托盘只在 Windows 用（运行时 `Platform.isWindows` 守卫），但它的 **Linux 插件 CMakeLists 在编译期就强制要 `ayatana-appindicator3-0.1`**——运行时守卫拦不住编译期检查。首次 CI 两个 Linux job 都挂在 `flutter build linux`：`CMake Error ... tray_manager/linux/CMakeLists.txt:32: requires ayatana-appindicator3-0.1`。修：CI 两个 Linux job 的 `apt-get install` 加 **`libayatana-appindicator3-dev`**（编译期）；deb `Depends` 加 **`libayatana-appindicator3-1`**（运行期）。**教训**：加跨平台插件依赖时「运行时只在某平台用」≠「其他平台不编译它」——Flutter 桌面插件的原生 CMakeLists 在 `flutter build <platform>` 时无条件参与，缺系统库就 configure 失败。加依赖后**四平台都要过一遍构建**，别只验 Windows。

### deb 文件权限（实测踩过的坑）

旧打包流程（另一个 AI 的 `dart build cli` 版）实测在 NAS 上 `picakeep` 直接 **Permission denied**：`/opt/PicaKeep/cli/picakeep` 权限是 `-rw-r--r--`（**漏了 `x`**），且 `/usr/local/bin/picakeep` 软链也没建。根因是打包时未 `chmod +x`、且 zip/拷贝流程不保留权限位。

当前 `package_linux_deb.sh` 已收口，**改脚本时勿丢这三处**：
1. `chmod +x "${APPDIR}/picakeep"`（GUI/服务端二进制可执行）。
2. `chmod +x .../usr/local/bin/picakeep`（wrapper 可执行）。
3. `dpkg-deb --root-owner-group --build`——强制包内文件属主为 `root:root`。**这关键**：CI runner 上构建产物属主是构建用户，不加这个标志，deb 装到目标机后文件属主/权限可能让普通用户读不到 `/opt/PicaKeep` 下的二进制和 `data/flutter_assets`（含 web_console），表现为「终端跑不起来 / 服务端读不到资源」。`--root-owner-group` 让所有文件归 root 且保留 0755/0644，任意用户可读、可执行。

> 排查口诀：deb 装完 `picakeep` 报 Permission denied 或服务端资源空 → 先 `ls -l /opt/PicaKeep/picakeep /usr/local/bin/picakeep` 看有没有 `x`、属主是不是 root，再看 `data/flutter_assets` 是否 world-readable。别先怀疑 `--server` 逻辑。

### Windows 托盘 + CLI wrapper

- **托盘**（`lib/tools/tray_controller.dart` 新增，`tray_manager: ^0.2.3`→0.2.4）：仅 Windows，关窗缩托盘。`window_frame.dart` 的 `_WindowsTrayCloseListener.onWindowClose`：服务运行中→`hide()`，否则 `setPreventClose(false)+destroy()`。托盘 `setIcon` 用 `LoadImage(IMAGE_ICON,LR_LOADFROMFILE)`→**必须 `.ico`**（`assets/tray_icon.ico`，从 `windows/runner/resources/app_icon.ico` 拷）。
- **CLI wrapper**（`windows/cli/`）：`picakeep.cmd`（PATH 入口，转调 ps1）+ `picakeep.ps1`（逻辑：`$AppExe=上层 picakeep.exe`，子命令 server/stop/status/logs，`Start-Process Hidden`+PID 文件+`Get-PortPid` 兜底）。

### 子命令与简写（两端一致）

| 全名 | 简写 | 行为 |
|------|------|------|
| `server` | `s` / `up` / `start` | 后台起服务端 |
| `stop` | `x` / `down` | 停止 |
| `status` | `st` | 状态 + 网址 |
| `logs` | `l` / `log` | 跟踪日志 |
| `version` | `-v` / `--version` | 版本 + 系统信息 |

- Windows ps1 用 `switch -Regex`（`'^(server|s|up|start)$'` 等）匹配别名；Linux wrapper 用 `case server|s|up|start)` 多模式标签。两端必须**同步**改，别只改一端。
- `version` 来源：Windows 读 `picakeep.exe` 的 `VersionInfo.ProductVersion`；Linux 读打包时写入的 `/opt/PicaKeep/picakeep.version`（heredoc 内单引号不展开，故在脚本拷 bundle 后 `echo "版本=${VER}"` 写入）。两者都附系统/内核信息。
- **ps1 必须带 UTF-8 BOM**：中文 Windows 的 PowerShell 默认按 GBK 读无 BOM 的 UTF-8，中文字节破坏引号配对→语法崩。加 BOM（`b'\xef\xbb\xbf'`）强制 UTF-8。用 `[System.Management.Automation.Language.Parser]::ParseFile` 验 AST。
- **布局自洽**：`{app}\picakeep.exe`（GUI，含 --server）+ `{app}\cli\picakeep.{cmd,ps1}`；Inno 的 `clitopath` 把 **`{app}\cli`**（不是 `{app}`）加进 PATH→敲 `picakeep` 命中 wrapper 而非 GUI exe；ps1 的 `$AppExe` 取上层 `{app}\picakeep.exe`。

### CI（`.github/workflows/build.yml`，push tag `v*` 或手动）

一次出 **4 个安装包**：`linux-amd64-deb`(ubuntu-22.04)、`linux-arm64-deb`(ubuntu-22.04-arm)、`windows-x64`(windows-latest+subosito 固定版)、`windows-arm64`(windows-11-arm+git master)。Windows 两 job 都 `flutter build windows` GUI + 拷 `windows/cli` wrapper + Inno（`windows-arm64` 本轮补全 Inno，之前只上传 `publish/`）。**删了重复的 `build-arm-installers.yml`**（arm64 两 job 已被 build.yml 覆盖）。x64 用 subosito 固定版 / arm64 用 git master 是**有意**的：arm runner 上 subosito 对 arm64 支持不全。

### ❌ 不要再走的路

- **不要** `dart build cli` 编 `bin/picakeep.dart`——那是读不到资源的空壳服务端。两端都用 GUI 二进制 `--server`。
- **不要**在 Windows 拷 `cli\native\bundle\bin\picakeep.exe` / `cli\assets`（旧空壳痕迹）。验证：`{app}\cli\picakeep.cmd` 应是 `powershell ... -File "%~dp0picakeep.ps1" %*`，**不该**是 `...native\bundle\bin\picakeep.exe %*`。
- **不要**改 C++ runner 做纯 headless Windows——用户是有人登录的桌面机，托盘最稳，不碰 C++、不需 xvfb。
- **不要**把 `{app}` 而非 `{app}\cli` 加进 PATH（会命中 GUI exe 而非 wrapper）。
- **不要**存无 BOM 的 ps1；**不要**只 `bash -n` 校验 deb wrapper（要 `dash -n`）。
- **不要**用 `Platform.script` 找 web_console 资源（Flutter 二进制里无效），用 `Platform.resolvedExecutable`。
- **验证铁律**：`picakeep server` 输出应是「服务端已在后台启动 (PID xxx)」+ 网址，**不该**是「运行模式: server-background」（旧空壳）；浏览器 `/admin-view` 能看到漫画。
- **不要**漏 deb 的文件权限三件套：二进制 `chmod +x`、wrapper `chmod +x`、`dpkg-deb --root-owner-group`。漏 `x` → Permission denied；漏 `--root-owner-group` → 属主非 root，普通用户读不到 `/opt/PicaKeep` 二进制和 flutter_assets，服务端资源空。

### 涉及文件

- `lib/main.dart`（--server 入口）、`lib/server/local_server_runtime.dart`（configPathOverride）、`lib/server/web_console/web_asset_loader.dart`（resolvedExecutable 根）
- `lib/tools/tray_controller.dart`（新增）、`lib/components/window_frame.dart`（关窗缩托盘）、`pubspec.yaml`（tray_manager + assets/tray_icon.ico）
- `.github/scripts/package_linux_deb.sh`（新增）、`windows/cli/picakeep.{cmd,ps1}`（新增，ps1 带 BOM）
- `.github/workflows/build.yml`（4 包；`build-arm-installers.yml` 已删）
- 打包权威说明：`Z-plan/后续优化-第二轮/打包说明-Linux与Windows.md`

### 待清理（不影响打包）

空壳源码已无 CI 引用，可删以防误用：`bin/picakeep.dart`、`bin/picakeep_server_runner.dart`、`lib/server/headless_cli_server.dart`、`tool/build_cli.{sh,ps1}`、`tool/install_cli.{sh,ps1}`；README 的 `picakeep run` 旧描述待更新。

## §86 本地详情布局、EH 入口与关于页（第十轮 03号计划）

### 需求语义与决定

- 本地详情的信息顺序不是简单调整字符串列表：固定为 `ID -> 创作者/画师/上传者 -> 下载时间`，其中 `DownloadedItem.time` 永远是本地下载时间，来源对象的时间才是“时间”。
- 选择 `LocalDetailInfoRole` 等原始语义角色驱动排序，而不是按已翻译的显示文案判断，原因是语言切换或标签翻译变化不能改变 creator/language/time 的归类。
- 选择把页数、语言、源站时间放进一个响应式 `Wrap`，而不是给每个信息组独占一行，原因是用户要求常规宽度同行，同时必须允许窄屏、大字和长值自然换行。
- 选择恢复 EH 记录中已保存的完整 `link`，而不是从 `gid-token` 拼 URL，原因是后者无法保留 e-hentai/exhentai 域名和历史上下文。
- 选择复用在线页的 EH Content Warning 请求确认/重试 helper，原因是本地更新和在线详情必须共享“首次请求 -> 用户确认 -> `setNW:false` 重试”的行为，取消不能写库。
- 选择 `PackageInfo.fromPlatform()` 读取关于页版本，而不是继续解析 `pubspec.yaml` 或保留硬编码版本，原因是构建参数可以覆盖 pubspec，运行时应以安装包为准。

### 根因推理链

- 本地详情“时间”语义混乱 -> `comic.time` 来自 `download.db.time`，而源站时间存在于来源模型/标签桶 -> 决定性证据是两者保存位置不同 -> 锁定为显示角色和字段边界缺失，而非日期格式问题。
- EH 本地记录无法在线跳转 -> `originalId` 只有 `gid-token`，不能推断域名和 token 上下文 -> 完整地址实际保存在 `DownloadedGallery.link`/`sourceRowJson` -> 锁定为本地恢复链路缺失，不是 EH 网络页不支持。
- 360dp、1.8 倍文字缩放出现 `RenderFlex overflow` -> 固定 72x72 动作项容不下放大后的文字 -> 改成 72dp 最小约束和可增长高度后 Widget 测试通过 -> 锁定为动作项固定尺寸契约不适配动态文字。

### 接口与行为契约

- `DownloadedItem.time` 只显示“下载时间”，EH/NH/Pica 的源站时间写入各自来源字段或分类桶；更新信息不得覆盖下载时间。
- 本地页数以已落盘正文文件数为首选；EH 可使用已持久化 `pageCount` 作为缺少文件索引时的回退，但在线 `maxPage` 不得覆盖本地页数。
- EH 更新成功只覆盖 uploader、标签和源站时间；标题、副标题、链接、封面、大小、页数、目录、章节/文件结构和下载时间必须保留。失败、取消确认或写库失败时，本地内存和数据库都不得留下半成品。
- 关于页版本使用 `V<version>`，非空且非 `0` 的构建号追加括号；读取中、失败状态保持稳定高度，不回退到旧硬编码版本。

### ❌ 不要再走的路

- 不要从 EH `gid-token` 猜造在线 URL；必须从直接下载记录或 `sourceRowJson` 恢复合法 HTTP(S) 画廊链接。
- 不要把下载时间当源站时间的兜底值；缺少源站字段时省略该信息，不能显示当前时间或下载时间冒充来源时间。
- 不要让在线更新直接覆盖整个下载对象；必须通过明确的元数据覆盖边界保留本地结构和下载状态。
- 不要用固定尺寸容纳大字版本的详情动作项；任何详情页动作按钮都要以动态文字和窄屏布局测试约束最小尺寸。

### 验证与残余风险

- `flutter analyze` 两次通过；EH/详情/关于页定向测试 41 项、全量测试 186 项通过；`git diff --check`、Android debug APK 和 Windows 构建通过。
- 未在真实登录态触发 EH Content Warning 的取消/确认、真实更新写库或外部浏览器启动；未安装 APK 到已有下载任务的 Android 14 设备。设备空闲后需逐条验证取消不写库、确认成功写回、四源布局、关于页真实版本和两个 GitHub URL。
- `package_info_plus` 当前固定为 `^8.3.1`；不要为关于页顺手升级 Android 构建链。真实设备侧的 platform channel、外部 URL 策略和 Content Warning 网络响应仍是未验证假设。

### 模块职责演变

| 文件 | 原职责 | 本轮后职责 | 迁入迁出方向 |
| --- | --- | --- | --- |
| `local_comic_detail_page.dart` | 本地详情字符串信息展示与三源更新入口 | 来源语义排序、响应式信息行、EH 链接恢复和更新覆盖协调 | 从显示拼装扩展为详情边界协调层 |
| `eh_content_warning.dart` | 无 | EH 请求的 Content Warning 确认/重试共享策略 | 在线详情与本地更新共同调用 |
| `download_model.dart` / `online_download_manager.dart` | 下载记录与队列包装 | `sourceTime`、NH 分类桶、EH `pageCount` 的持久化边界 | 来源元数据从在线对象贯通到本地记录 |
| `settings_page.dart` | 关于页静态文案与硬编码版本 | 实际安装包版本、图标和外部入口展示 | 版本来源从常量迁移到 platform package info |

## §87 AI 结果清单参数边界与灰块入口修复（第十一轮）

### 根因推理链

- AI 结果清单出现近乎全宽的超长纯灰块 -> 排除封面网络/缓存、列表 key/滚动、第九轮空 assistant 灰块 -> 日志确认 `display_result_list` 的 `availability` 为 String，而 `AiResultItem.fromToolData()` 直接按 Map 强转 -> Flutter release `ErrorWidget` 在纵向无界 `ListView` 中膨胀为高灰块，锁定为不可信工具参数触发 UI 构建异常。
- “查看 N 条原始结果”无反应 -> 点击回调再次执行同一严格 `Map<String,dynamic>` 解析 -> 决定性证据是点击路径与消息卡路径都在 `AiResultItem` 解码前抛错 -> 锁定为重复解析边界缺少统一安全报告，不是导航栈问题。

### 架构决策与行为契约

- 选择 `AiResultItemDecodeReport` 作为工具参数、历史消息、工具卡和结果卡的唯一解码边界，而不是在工具入口和两个 UI 入口分别做转换，原因是字段漂移必须保持同一语义和问题统计。
- 选择“工具严格、历史/UI 宽松”的双策略，而不是所有异常都让模型重试：`availability` String、数字 id、列表 author、单字符串/Map tags 可本地无歧义修复；只有不可恢复的顶层结构或关键 id/title 问题才返回 `retryable: true`。
- 选择实例级脚本化 LLM callback 测试 seam，而不是修改生产静态客户端，原因是可以验证完整 `_runLoop` 失败 -> 模型修正 -> 成功路径，同时不产生真实网络请求。

接口契约：

- 工具成功才允许缓冲和生成 `resultList`；失败必须写入对应 `tool_call_id` 的 tool 消息并交回模型；空 `items` 是非重试零结果。
- 严格失败只返回固定错误码、条目索引、字段名、expected、actualType、脱敏示例和 `retryable: true`，不得回显标题、标签、URL、提示词或认证信息。
- `AiResultItem.toJson()` 是结果清单和下载队列的规范序列化入口；旧会话 JSON 保留原形，由读取兼容层恢复，不批量迁移用户文件。
- 未知 source 只保证安全展示和进入清单 fallback，不承诺在线直达或下载能力；需要扩展来源能力时另开计划。

### ❌ 不要再走的路

- Schema 不能替代运行时校验；模型或兼容 API 可能忽略 schema，工具入口仍必须做字段级容错和严格策略。
- 不要用 `whereType<Map<String, dynamic>>()` 静默过滤条目，否则模型声称展示 12 条时客户端可能只展示 11 条且无法重试。
- 不要用空 `catch` 吞掉工具或 widget 异常；这会把错误变成无反应，并可能破坏 tool_call/tool 消息配对。
- 不要把所有类型异常都交给模型重试；能本地规范化的字段应保留信息并节省 token/工具轮数。
- 不要把发布版长灰块首先归因于封面 URL、滚动位置或空 assistant 文本；先查 `ErrorWidget`、无界尺寸和 build 前的工具参数类型。

### 验证与残余风险

- `flutter test` 全量 202 项通过，`flutter analyze` 无问题，`flutter build apk --release` 成功；Android 14 release 模式完成旧数据保留下的清单展示、消息“查看全部”和工具卡“查看 12 条原始结果”入口验收，并保留脱敏截图证据。
- 设备实际旧会话只有 10/12 条，脱敏 fixture 和入口自动化验收为 12/12。截图对应会话完整杀进程冷启动、真实模型随机错误参数后的设备端自动重试、另一台 Android 10 设备和真实网络日志导出尚未完成；不能以确定性脚本测试替代这些人工范围。
- JSON Schema 被模型忽略时仍可能产生 retryable 工具失败；验证方式是继续观察真实会话工具日志。全局 Flutter framework 异常尚未统一做脱敏采集，属于本计划外风险。
- 发布构建曾因 D 盘剩余约 2.7MB 在 AOT snapshot 阶段失败，清理生成目录后通过；正式 release 签名包不能覆盖设备上的 debug 签名包，设备验收必须记录证书差异，不得删除应用数据伪造旧会话兼容。

### 模块职责演变

| 文件 | 原职责 | 本轮后职责 | 迁入迁出方向 |
| --- | --- | --- | --- |
| `ai_result_item.dart` | 对工具条目字段做直接强转 | 唯一规范化、解码报告和 `toJson` 边界 | 外部 JSON 进入工具/UI/队列前统一收口 |
| `display_result_list_tool.dart` | 过滤 Map 后原样返回成功 | 完整 schema、严格校验、规范化和脱敏 retryable 失败 | 工具入口接管不可信参数边界 |
| `ai_conversation.dart` | 成功结果直接缓冲清单 | 只缓冲规范成功结果，失败交回模型，并提供测试 seam | 工具结果进入会话状态前增加控制流闸门 |
| `ai_chat_page.dart` | 消息卡和工具卡各自解析条目 | 两个入口共同消费安全解码报告并显示有界失败态 | UI 解析逻辑合并到统一模型边界 |
| `ai_download_queue.dart` | 重复手写条目序列化 | 复用 `AiResultItem.toJson()` | 队列保存契约收敛到模型 |

## §88 AI 日志凭据脱敏与缓存前缀稳定化（第十二轮）

### 根因推理链

- 真实日志出现 API Key/Authorization 与直接分享原始日志 -> 排除“只删除 Cookie”已足够 -> `MyLogInterceptor` 和四个分享/导出入口仍传递原文 -> 固定根因是日志副本边界缺少凭据替换。
- 缓存命中率偶发大幅下降 -> 旧日志显示动态 system 位于全部历史之前，且 system 指纹变化时同时冷却 -> 选择把动态标签/scope 追加到当前 user，而不是继续在历史前拼 system；tools 动态策略没有单独证据，本轮保持不变。

### 架构决策与行为契约

- 选择日志副本 redactor 而非原地改写历史文件，原因是日志查看继续保留个人诊断正文，同时分享失败不能泄露原始路径。
- 选择普通 SHA-256 canonical shape 指纹而非 HMAC/盐/安装级秘密，原因是用户明确只需要个人软件内的 shape 比较，不需要匿名化或认证。
- 选择 `turn_context` JSON 作为当前 user content 的后缀，而非动态 system 或手写分隔符，原因是它能保持旧 messages 追加前缀稳定，并由 `jsonEncode` 正确处理引号、换行和 Unicode。

接口契约：

- `shapeV=2` 的 `conversation/turn/round/messagesHash/toolsHash` 只描述请求 shape；provider usage 的 cache hit/miss 仍是服务端观测，二者不能互相替代。
- `NetworkLogRedactor` 只接受并返回日志副本；`copyAll`、当前导出、历史分享、ZIP 失败必须中止，不得退回原始文件。
- `_ActiveTurnContext` 在 send 开始冻结，直到最终回复或下载确认续跑结束；临时设置变化只能影响下一 send。

### ❌ 不要再走的路

- 不要用相邻 prompt token 总数或 `min(previousPrompt,currentPrompt)` 代替真实共同前缀；它无法区分会话交错、工具结果增长和 provider 缓存单元。
- 不要只删除 `Cookie` 就认为日志安全；Authorization、Set-Cookie、API key、token、密码和 URL 认证参数都必须在日志副本边界处理。
- 不要为本轮缓存优化顺手删除动态 tools、重构恢复协议或制造付费 A/B 请求；这些问题没有本轮单因素证据或超出用户范围。

### 验证与残余风险

- `flutter test` 全量 210 项通过，`flutter analyze` 无问题；测试覆盖 redactor、临时脱敏副本、canonical hash、messages 精确前缀、旧 history 和既有 tool-call/download 协议。
- 旧日志中的 API Key 仍需用户在 provider 平台撤销轮换；真实 provider 缓存行为、真机 UI 提示服从度和长期 cacheHit/cacheMiss 趋势待正常使用观察。
- 临时分享副本沿用系统临时目录；系统清理时副本可能失效，原始历史文件不受影响。

### 模块职责演变

| 文件 | 原职责 | 本轮后职责 | 迁入迁出方向 |
| --- | --- | --- | --- |
| `app_dio.dart` | 记录网络请求/响应原文 | 记录经过凭据 redaction 的网络诊断副本 | 凭据边界进入日志入口 |
| `log_file_service.dart` | 原文复制、导出和 ZIP | 复制保留正文的脱敏文本，分享/导出生成临时副本 | 原始文件留在本地，副本出应用边界 |
| `llm_client.dart` | 发送消息并记录基础 usage | 生成 canonical SHA shape 与 `shapeV=2` usage 诊断字段 | 请求 shape 观测集中在客户端 |
| `ai_conversation.dart` | 每次请求前重拼动态 system | 持久化稳定 history，维护活动轮快照和 user `turn_context` | 动态指令从 request 前置迁入当前 user 后缀 |

## §89 第十二轮验收：凭据脱敏与缓存前缀优化

### 验收结论

- 本轮总体为“部分通过”，不是“全部完成”。01 代码主体通过，但旧日志里的 DeepSeek Key 尚未由用户确认在 provider 平台撤销/轮换；02 稳定前缀改造主体成立，但范围语义存在两个 P1 回归风险。
- 03 是只读验收记录，不对执行 AI 的业务代码做现场修复；后续修复必须另开计划，避免把验收发现和已确认实现混成一个提交。

### 根因与契约更正

- `localOnly` 与 `onlineOnly` 同时为 true -> 长期来源限制与当前轮仅本地设置叠加 -> controller/tools 的真实优先级是 localOnly 覆盖 onlineOnly -> `turn_context` 与执行约束发生矛盾，锁定为状态编码未归一化，而不是 dispatch 失效。
- 多来源搜索重新询问或漏分发 -> 旧动态 system 明确包含“直接搜索、不询问、不得使用其他来源、多来源分别调用并合并” -> 新稳定规则和 JSON 只保留范围字段 -> dispatch 只能拦截越界，不能补模型行为语义，锁定为产品指令迁移不完整。
- 验收中 `flutter test` 210/210 和定向 48/48 通过，只能证明现有用例通过；不能把未驱动的 interceptor 四回调、分享按钮失败、工具子轮/下载确认、旧 history 保存重载、Unicode/空正文/Set 顺序写成已覆盖。

### ❌ 不要再走的路

- 不要只看基础 system 稳定或普通 send 前缀测试就宣称范围语义无回归；必须覆盖 localOnly/onlineOnly 组合、单源、多源、直接搜索和不询问行为。
- 不要把旧 API Key 未轮换视为代码已修复；redactor 只能阻止后续日志泄露，不能使已经离开应用边界的凭据失效。
- 不要继续让所有单文件脱敏导出共用会被清空的目录；连续分享时后一次清理会使前一次副本失效，应先设计独立临时文件或引用计数/生命周期策略。
- 不要用“测试全绿”掩盖测试没有触达真实入口；验收文档必须区分纯函数覆盖、集成入口覆盖和真实平台观察。

### 验证与残余风险

- 已验证：凭据 redaction 接入 request/response/error/retry，当前导出/ZIP/历史分享走脱敏副本，`shapeV=2` 记录 shape 字段，基础前缀和旧 history 测试通过。
- 未验证：用户是否已在 provider 平台轮换旧 Key；模型对 user 内部范围上下文的服从度；真实 DeepSeek cacheHit/cacheMiss 趋势；并发/快速连续分享；工具 round、下载确认暂停续跑和旧 history 发送后重载。
- 下一轮修复优先级：先让 `localOnly` 强制 `onlineOnly=false` 并明确 `online_sources` 语义，再恢复完整范围行为指令并补最小 prompt/JSON 测试，之后重新验收 02。P2 分享目录竞争和入口覆盖可并行补强。

### 模块职责演变

| 文件 | 原职责 | 本轮验收后的约束 | 迁入迁出方向 |
| --- | --- | --- | --- |
| `ai_conversation.dart` | 生成稳定 history 和 `turn_context` | 还必须保证范围状态互斥、JSON 语义与 dispatch 契约一致 | 下一修复计划负责状态归一化与范围文案补全 |
| `log_file_service.dart` | 生成脱敏导出临时副本 | 临时副本不能因并发导出互相删除 | 后续补独立副本生命周期，不改变原文查看 |
| `03-验收-AI日志脱敏与缓存前缀优化.md` | 无 | 记录 01/02 的通过项、P1/P2 缺口和后续顺序 | 只读验收依据，不承担业务修复 |

## §90 第十二轮：远程服务代理分流与连接态重建（04/05）

### 根因与架构决策

- 远程状态、资源库 JSON、图片和 WebSocket 同时超时 -> 排除单个资源或单张图片异常（失败跨越控制面与状态面） -> 代码显示四条链路使用不同网络栈，且没有统一 LAN bypass；长生命周期 HTTP/WS 还有重启恢复型状态残留 -> 锁定“代理/TUN 路由问题 + 运行态未重建”的高概率组合，但保留真实代理环境未验证分支。
- 选择按实际请求 URI 判定代理，而不是按远程服务功能名或 base URL 一次判定，原因是绝对图片 URL 可能指向不同 host；选择私网/回环/链路本地/`.local`/单标签直连而不是写死 `192.168`，原因是地址族和部署形式更多。
- 选择原位替换 transport 并保留 `_instances`，而不是清空实例表，原因是页面、条目和图片 Provider 可能持有旧对象引用；选择 generation 隔离旧结果，而不是假设 `Future.timeout` 会取消底层连接，原因是 Dart Future 超时不等于 socket 已停止。
- 选择每代 WebSocket 自有 `HttpClient`，而不是复用静态连接/客户端，原因是代理变化、迟到握手和旧回调必须有明确所有权。

### 接口与行为契约

- `RemoteServiceNetworkPolicy.findProxy` 必须同步且不得 DNS；私网/回环/链路本地/`.local`/单标签目标必须 `DIRECT`，公网遵循 manual proxy > environment proxy > direct fallback。
- `/status`、资源库控制请求、远程图片和 WebSocket 必须遵循同一策略；图片按自身 URI host 分流。`DIRECT` 只绕过应用 HTTP proxy，不能绕过第三方 TUN/VPN 路由。
- `RemoteLibraryClient.rebuildAllTransports()` 必须保留实例身份；旧 generation 的 pending 结果不得写入新缓存，当前 transport 失败后下一次请求使用新代。
- 代理保存/清除和应用恢复前台必须先触发远程 transport 重建，再发配置/连接通知；非幂等写请求超时不得自动重放。
- WebSocket 旧 generation 的迟到 socket、旧 subscription 回调不得覆盖或关闭新 channel；`stop()` 必须幂等并释放自有 client。

### ❌ 不要再走的路

- 不要只修改 `app_dio.dart` 或全局加 `192.168` 字符串特判；远程资源库、状态探测、图片和 WebSocket 不会因此自动统一。
- 不要清空 `_instances` 重新创建 client；旧页面和 Provider 仍会调用旧引用，表现为 closed client 或静默失效。
- 不要把 `Future.timeout` 当作底层连接取消；迟到请求/握手仍可能占用池或回写旧状态。
- 不要为超时的收藏、删除、刷新等非幂等写请求自动重放；服务端可能已经执行。
- 不要把应用日志中的 `local-direct` 当作 TUN/VPN 已放行 LAN 的证据；应用层策略和外部路由必须分开验收。

### 验证与残余风险

- 已通过 `dart format`、策略与 transport 生命周期定向测试、全量 `flutter test` 221 项、`flutter analyze --no-pub` 和 `git diff --check`。覆盖 URI 分类、手动/环境代理优先级、强制直连、本地 HTTP 生命周期、generation/cache 防旧回写和基本设置/前台接线。
- 未完成真实代理/TUN/VPN 的“开/关代理 × 应用重启/不重启”四组矩阵；未新增可控 WebSocket connector 以专测迟到握手、旧 subscription 回调和 stop 清理。因此不能宣称真实网络和 WS 竞态已全部验收。
- `customClient` 在 Android/Windows/Linux 目标平台的 WebSocket 行为、第三方工具 LAN allowlist、带点自定义内网域名的 DNS 分类仍需现场验证；同步 `findProxy` 不引入阻塞 DNS。

### 模块职责演变

| 文件 | 原职责 | 本轮后职责 | 迁入迁出方向 |
| --- | --- | --- | --- |
| `remote_service_network_policy.dart` | 无 | 远程 URI 分类、代理决策和 HttpClient 配置 | 新增统一策略边界 |
| `remote_library_data_source.dart` | 远程资源库请求与缓存 | 稳定实例身份、transport/cache generation、异常重建和旧结果隔离 | 传输生命周期内聚 |
| `service_data_source.dart` | 状态与局域网发现探测 | 通过统一策略创建 status/discovery client | 接入按 URI 分流 |
| `remote_library_event_channel.dart` | WebSocket 连接、心跳和重连 | 自有 client、generation、迟到连接回收、旧回调隔离和幂等 stop | 接入策略并强化所有权 |
| `network_settings.dart` / `main.dart` | 保存代理和应用生命周期 | 代理变化、前台恢复触发远程 transport rebuild | 设置与生命周期接线增强 |

## §91 第十三轮：导出、未翻译标签、服务信息页与详情交互收口（01～12）

### 根因与架构决策

- 导出入口原本只有导航占位、复制与分享清单路径分裂 -> 目录、归档、远端 URL 和特权读取不是同一种内容来源 -> 采用不可变导出请求、来源读取器、统一 ZIP/清单核心和独立交付层；复制直接写同一份 manifest 文本，不复用分享产物回传。
- 页数/大文件问题分成两层：上层是 `pageCount`、封面/元数据过滤和复制语义不统一，下层是特权与 archive API 仍返回完整 `byte[]` -> 先闭合可验证的清单契约，保留流式能力缺失证据；提高内存阈值不能替代平台分块读取。
- 未翻译标签的原调用只返回展示字符串，无法区分同文译文、未加载和失败 -> 使用结构化 lookup 状态；入口多且来源/漫画/operation 需要一致去重 -> 用 `UntranslatedTagCoordinator` 统一观察身份和批量提交，未知来源宁可跳过，不按 URL/标题猜 EH/NH。
- 待翻译标签偶发不出现 -> observation 可能早于翻译表 ready，单纯调用 `loadTagTranslations` 不会冲刷 pending -> 在在线详情、本地详情、本地恢复、下载和搜索成功边界显式调用共享 `flushPending`；NH 本地稳定身份改用数字 `comicID`，不把 `nhentai` 类型前缀写入去重键。
- 服务信息页同时承担生产网络、布局和状态测试 -> Flutter Widget 环境的 HttpClient/定时器不能证明真实网络 -> 保留生产默认数据源并增加可注入替身，Widget 测试验证状态/布局，mDNS/网段/端口取消留给 integration/真机层。
- EH/NH 的通用 `subtitle`、uploader、ID 和作者含义不一致 -> 单改导出或单改卡片会被扫描/写库重新污染 -> 新增来源感知 author resolver，沿源模型、数据库写入、扫描 metadata、列表/详情、AI、导出和历史收藏整链路收敛；无可靠分类时返回空作者，不启发式猜测。
- 详情信息区多个页面同时被撑成整行 -> 共享 `InfoValueAction` 的 Center/min constraints 在有界父布局下取得最大尺寸 -> 默认改为 layout-neutral，调用方显式 `expand` 才表达占满行的意图；显示值、raw 搜索值和占位保护分离。
- 已下载预览作者/标签过长且 footer 被内容挤出 -> 将内容、展开内容和固定操作混在同一滚动/布局边界 -> 统一单一主滚动区，章节用 Sliver，footer 留在外层固定区；作者与标签独立测量/折叠，移动 sheet 复用调用方 controller，桌面侧栏自有并负责释放 controller。

### 接口与行为契约

- 导出：`pageCount` 只有正数才是确定页数；缺失/非法值显示“无/不确定”；普通导出带根清单，配置清单只输出勾选字段；复制不触发 artifact delivery；全失败不能生成伪成功空产物。
- 标签：有效 observation 必须携带 source、稳定 comic ID、上下文和 operation ID；同一 operation/漫画/候选标签只计一次；lookup 未 ready 时暂存，ready 后显式 flush；搜索只使用 brief 已带标签，禁止为采集发 N+1 详情请求。
- 服务页：标题区只保留直接可见断开操作，底部顺序为发现→填写地址→刷新；原始保存地址非空即代表存在可清除配置，即使规范化后格式无效；窄屏只能在同一标题区降级为图标，不能把关键操作重新藏进菜单。
- 作者：EH 作者只来自 namespaced `artist`，NH 作者只来自分类桶 `Artists`；缺少可靠原始 metadata 时保持未知/空值。通用 subtitle 不是跨源作者契约。
- 信息值：显示文本用于复制，raw namespace/value 用于搜索；空、未知、N/A 不得生成搜索词；共享组件默认尺寸由 child 决定，调用方必须显式选择 `expand`。
- 预览：作者和标签展开状态互相独立；只有远程 detail 真正替换 `_comic` 时复位，普通 `_syncInfo` 不得收起用户状态；footer 不随主内容滚走，展开内容必须仍可由同一主滚动区访问。

### 踩坑与“不要再走的路”

- 不要把提高 32/64MB 阈值当成特权大文件完成；底层仍为完整 `byte[]` 时只能安全失败并等待 native/channel 分块或临时 staging 契约。
- 不要让复制按钮走分享产物回调，不要把目录封面、缩略图和元数据误算为漫画页。
- 不要按“翻译文本是否变化”判断未翻译，不要逐条 observation 写盘，不要把一次 widget callback 当作 operation，也不要用无来源全局 tag 集合跨漫画去重。
- 不要在远程字段契约缺失时猜 EH/NH 来源，不要使用 NH 带类型前缀的 `id` 作为稳定漫画身份。
- 不要在 Widget 测试中启动真实 HTTP 服务来代替局域网验收，也不要让测试注入改变生产默认数据源。
- 不要只删除 `Center` 而保留 `minHeight`，也不要用透明覆盖层扩大紧凑 chip 命中区而抢相邻操作；命中区必须与视觉结构和语义顺序一致。

### 验证与残余风险

- 自动化层已通过 292 项全量 Flutter 测试、静态分析、格式检查、差异检查以及 Android/Windows debug 构建；导出、标签去重/flush、服务页状态、作者解析、信息值几何/交互和预览滚动均有定向覆盖。
- 不能由上述结果替代的验证：真实系统剪贴板/分享、Root/Shizuku、超大/加密 ZIP/CBZ、真实 EH/NH 网络字段、旧 RemoteLibrary payload、Android mDNS/网段/端口取消、Android/Windows 320dp/大字号/浅深色/鼠标键盘和 DraggableScrollableSheet/侧栏现场行为。
- 远程 additive 字段、跨重启 operation receipt、真实 source metadata 与字体/主题测量仍是假设；验证失败时应保持未知/安全失败，不回退到启发式猜测。

### 模块职责演变

| 模块 | 原职责 | 第十三轮后职责 |
| --- | --- | --- |
| `lib/foundation/download_export/` | 无统一跨来源导出边界 | 来源读取、清单格式、路径安全、ZIP 核心、结果与交付分层 |
| `lib/foundation/untranslated_tags/` | 单条观察/简单落盘 | source/comic/operation 去重、批量提交、pending flush 和设置页数据源 |
| `lib/foundation/download_author_resolver.dart` | 无统一作者解析器 | EH/NH 来源感知作者解析，供扫描、写库、UI、AI、导出和历史复用 |
| `lib/pages/service_info_page.dart` | 状态展示、网络操作和布局耦合 | 单一操作区、状态反馈、响应式布局与可注入测试数据源 |
| `lib/components/info_value_action.dart` | 交互并隐式施加最小布局尺寸 | 布局中性的搜索/复制/键盘/语义交互包装器；占满行由调用方显式声明 |
| `DownloadedComicInfoView` | 内容、章节和底部操作共同受内容布局影响 | 单一主滚动区、作者/标签独立折叠和外层固定 footer |

## §92 第十四轮：mDNS/网段发现与无连接态服务信息页（01/03/04）

### 根因与架构决策

- mDNS 模式漏扫的直接根因不是补扫开关默认值，而是 mDNS 返回非空候选后提前结束；Docker bridge 下组播无法穿过容器网络、广播地址和 SRV 端口也可能是容器内部值，客户端单靠 mDNS 无法覆盖这种部署拓扑。因此保留 mDNS 元数据，同时用单播网段扫描补齐结果。
- “并行补扫”采用 mDNS→网段扫描的串行顺序，产品语义是两条路径的结果都会参与发现，不强求线程级并发，以免两个来源交错写入进度阶段。合并时按地址去重并优先保留 mDNS 候选，`effectiveMode` 继续显示 mDNS。
- 多网卡扫描的缺陷在队列顺序和预算分配：原有网段可能按字符串顺序进入，10.x 或虚拟网卡先耗尽预算，导致本机 192.168 网段的 NAS 还未扫描。修复为本机网段优先、截断前排序、数字主机序，并把历史成功地址作为显式优先探测。
- 无地址时客户端连接信息卡只有 `--` 占位，发现入口还要经过底栏按钮。页面表现层改用地址是否为空二分：无地址进入内联发现区并自动开扫，有地址即使 offline/invalidAddress 也保留信息卡，避免把“有配置但暂时离线”误判为“首次发现”。

### 接口与行为契约

- `fallbackToSubnetScan == true` 时，mDNS 候选非空也必须执行网段扫描；合并去重键是候选地址，mDNS 先写入以保留来源元数据，`fellBackToSubnetScan` 表示本轮包含网段补扫。
- 网段前缀必须在截断前按优先级排序；段内主机按 `1..254` 的数字顺序展开；preferred 与 last-success 地址固定排在显式探测队列前部。测试注入的 prefix resolver 不得绕过统一排序入口。
- `DiscoveryProgress.candidates` 是累计候选快照而非增量；只有新增候选时发送中间快照，完成/超时/取消发送完整列表，UI 可以直接替换列表。
- 地址为空才允许触发自动发现；地址非空时不自动切发现区。点击候选会先取消扫描并进入连接中状态，结果完成后的唯一候选自动连接必须在该状态下跳过，防止“点 A 连 B”。

### ❌ 不要再走的路

- 不要只翻转补扫默认值来解决混合局域网漏扫，也不要在 mDNS 有结果时用网段结果整体替换 mDNS 结果。
- 不要只修改默认网卡枚举函数；测试注入的 prefix resolver 也要经过统一优先级排序。不要用字符串排序 IP，也不要在排序前截断网段数量。
- 不要用 `connectionState` 单独决定是否展示发现区；offline/invalidAddress 仍可能有可清除的地址配置。不要在取消后要求 generation 保持不变，取消本来就会递增 generation。
- 不要用真实 HTTP 服务或 Widget 测试代替局域网 mDNS、网段、端口探测和取消时序的现场验收。

### 验证与残余风险

- 01 号相关分析通过，发现测试 16 项通过；03 号相关分析通过，发现测试 20 项通过；04 号相关分析通过，发现测试 21 项通过，并执行服务端口 Widget 测试。
- 01/03/04 计划代码与自动化证据已回写，但仍不能替代所有现场边界：跨网段或 Docker bridge 的真实部署、20 并发对家用路由的影响、扫描进度高频更新下的真机 UI 性能、点击/唯一自动连接的真实时序、非中文翻译词条和不同平台输入行为仍需验证。
- 01/03/04 的执行结果不改变 02 号 host 模式部署方案仍待执行的事实；需要彻底解决广播地址/端口覆盖时，应另行执行 02，不把未执行方案当作本轮已落地能力。

### 模块职责演变

| 文件/符号 | 原职责 | 第十四轮后职责 | 迁入迁出方向 |
| --- | --- | --- | --- |
| `lib/foundation/service_data_source.dart` 的 `_discover` | mDNS 无结果时才 fallback，候选命中后提前结束 | mDNS 与网段结果补扫合并，保持 mDNS 优先和模式语义 | 发现流程改为补集合并 |
| `LocalNetworkServiceDiscovery` 的网段扫描排序 | 收集前缀、字符串排序并按旧预算扫描 | 本机网段优先、截断前排序、数字主机序、last-success 显式优先 | 扫描队列策略增强 |
| `DiscoveryProgress` | 仅报告阶段/计数 | 携带累计候选快照供内联 UI 实时渲染 | 进度协议扩展 |
| `lib/pages/service_info_page.dart` | 无地址时展示空信息卡，发现入口在按钮/底栏 | 地址空时自动发现区、实时列表、点击即连和唯一候选自动连 | 页面状态机扩展 |
| `lib/foundation/app_runtime_mode.dart` / `runtime_service_settings.dart` | mDNS fallback 与端口设置旧文案 | mDNS 并行补扫和扫描端口优先级的用户可见说明 | 设置语义更新 |

## §93 第十四轮：下载路径、EH 连接与详情/服务页交互收口（05～08）

### 根因与架构决策

- 下载目录报路径过长的根因是文件系统限制按 UTF-8 字节而非 Dart 字符数计；更关键的是写入侧 `_safeName` 和回读侧 `sanitizeFileName` 原先没有共同的截断规则。两侧同时采用 240 字节循环截断，给将来的同名后缀留余量；不为此跨文件抽常量，改以交叉注释维持镜像关系。
- EH 慢的主因是每个请求都新建 `HttpClient`，图片与 HTML 频繁重做 TLS 握手。优先落地复用 client、限制连接池、共享下载 Dio、只缓存 `_getReaderLinks` 和统一并发；HTTP/2 需要新依赖且无法验证与手动代理/证书/SNI 的兼容性，本轮不引入。保留 `_verifyImageReachable`，因为它可能承担 509 配额和坏链预筛，不能在未证明等价失败处理前为少一次往返而移除。
- 详情页回归来自把标题/作者错误替换为 `InfoValueAction`。该组件的契约是点击搜索、长按整体复制，适合功能性元信息；标题/作者须恢复 `SelectableText` 才能部分框选。空白点击则通过 translucent `GestureDetector` 调用 `unfocus` 统一清选区。
- 服务页断言并不只由直接的 `LayoutBuilder` 引起：`IntrinsicHeight` 会递归查询整个后代树，`_InfoRow` 内的 `LayoutBuilder` 也会触发。最终用 `MediaQuery` 决定堆叠，宽屏节点信息改为内联双列 `Row`，使 `IntrinsicHeight` 整棵子树都不含 `LayoutBuilder`。

### 接口与行为契约

- `_safeName` 和 `_DownloadDb.sanitizeFileName` 必须保持完全相同的 UTF-8 截断规则和 240 字节上限；修改任一侧必须同步另一侧，否则写入后回读会定位到不同目录。
- `sharedDownloadDio({BaseOptions? options})` 返回同一个下载 Dio，`options` 只在首次创建生效；所有 EH 下载调用点必须使用 `EhNetwork.sharedDownloadBaseOptions()`。代理漂移时只重建 adapter，Dio 引用不变；`CancelToken` 和进度回调继续是请求级参数，共享实例不得添加 cookie 拦截器。
- `EhNetwork.request(..., useCache: false)` 默认绝不缓存；当前只有 `_getReaderLinks` 能开 5 分钟、64 条、按最旧淘汰的进程内缓存。showKey/imgKey 等时效页面不得缓存。`settings[79] ?? 6` 是下载并发统一口径，ehgt 的 3 并发闸必须同时约束封面、缩略图和真实页图下载。
- 详情页标题/作者用 `SelectableText`；来源、大小、页数和统计继续用 `InfoValueAction`。`SelectableText` 与外层 `GestureDetector(unfocus)` 共同构成“自由选择复制 + 点空白清选区”的标准模式。
- 只要某组件位于 `IntrinsicHeight` 后代树中，该组件及其所有子组件均不得构建 `LayoutBuilder`；视觉等高的要求不能成为绕过该 Flutter 约束的理由。

### ❌ 不要再走的路

- 不要只在下载写入侧截断名称，或把字符数当成文件名的字节限制；也不要修改一侧 240 上限后遗漏另一侧。
- 不要把所有 `logDio()` 调用方都切到共享下载 Dio，也不要给共享实例累加 cookie 拦截器；这会造成不同源的 BaseOptions/cookie 污染。不要为 EH key 页面开启 HTML 缓存，也不要把 `CancelToken` 放进共享 BaseOptions。
- 不要把 `InfoValueAction` 用在需要框选的标题/作者，也不要为恢复自由选择而恢复旧的信息卡长按菜单；来源、大小、页数和统计仍需保留其搜索能力。
- 不要只删除直接位于 `IntrinsicHeight` 下的 `LayoutBuilder`；必须检查整个后代组件树。不要以纵向堆叠替代需要标签同顶对齐的双列布局。

### 验证与残余风险

- 05 通过两文件静态检查与格式检查，但未新增单测或真机验证。06 通过全量 `dart analyze`、7 文件格式检查、服务发现定向 31 项和下载/详情相关既有 55 项测试；07 通过两文件静态检查和格式检查；08 修复后端口/发现定向 31 项通过，原 5 个断言失败项均转绿。
- EH 速度、握手次数、连接池参数、缓存命中、代理漂移、509/续传/nl 换节点仍没有真机或抓包证据。共享实例的 15 秒连接超时是 dio 5.4.1 不能按请求配置 `connectTimeout` 后的折中，弱网 HTML 的失败感知是否变慢待验。
- 05 的超长中文标题、回读是否全程经过 `sanitizeFileName`、碰撞目录隔离和历史脏数据尚待现场检查；07 的长按与滚动竞争、空白手势对封面/信息卡/章节点击的影响需真机确认；08 的 `MediaQuery` 宽度与局部可用宽不一致时，320 阈值可能略有偏移。

### 模块职责演变

| 文件/符号 | 原职责 | 第十四轮后职责 |
| --- | --- | --- |
| `online_download_manager.dart` `_safeName` / `download.dart` `sanitizeFileName` | 各自清洗文件名，无共同长度边界 | 成对执行 UTF-8 240 字节截断，保证写读目录映射一致 |
| `app_dio.dart` | 无状态 Dio/adapter 工厂 | 持有下载连接池单例、代理指纹与 adapter 漂移重建 |
| `EhNetwork` | EH 请求、链接解析和下载辅助 | 持有阅读页 HTML 缓存与共享下载 Dio 初始化参数，按请求内联 cookie 注入/落库 |
| `online_download_manager.dart` `_downloadFileOnce` | 自建 Dio，真实字节下载不受 ehgt 闸约束 | 使用共享下载 Dio，受 ehgt 3 并发闸约束 |
| 两个详情页的封面旁文本 | 标题/作者通过 `InfoValueAction` 触发功能性交互 | 标题/作者恢复可框选文本，功能性元信息仍维持搜索交互 |
| `service_info_page.dart` 客户端概览 | `IntrinsicHeight` 后代包含 LayoutBuilder | MediaQuery 判宽，节点信息走无 LayoutBuilder 的双列布局 |

## §94-02 主线第十四轮02：host模式广播地址覆盖字段

### 架构决策与理由

`explicitAdvertiseHost` 作为独立形参而非复用 `host` 形参，是区分「显式覆盖」与「监听地址（0.0.0.0 需枚举）」两种语义的关键设计。若复用 host 参数，0.0.0.0 传进去会触发枚举逻辑；而显式覆盖地址（含 10.x）必须无条件直接用、跳过所有过滤——两条路径语义根本不同，必须分开。

### 接口与行为契约

- `advertiseHost = ''` = 未设置 = 广播用 `config.host`，行为与改前完全一致；`_startMdnsAdvertisement` 此时传 `explicitAdvertiseHost: null`（不传空串，否则 `InternetAddress('')` 抛 ArgumentError）。
- `advertisePort = null` = 未设置 = 广播用 `config.port`。
- `explicitAdvertiseHost` 非空时 `_resolveAdvertisedIpv4Addresses` 完全不调用，`_isCommonPrivateIpv4` 对 10.x 的排除不生效。

### 技术约束/铁律

- `fromJson` 里 `advertisePort` 不能复用 `_normalizePort` 辅助——该辅助解析失败时回退到 `defaultServiceAdminPortInt`，而「未设置」语义是 null，两者相反。
- `advertiseHost` 非空时 `_startMdnsAdvertisement` 必须传 `explicitAdvertiseHost: config.advertiseHost.trim()`，不能传空串。

### 残余风险/未验证假设

- Docker host 网络模式下 `NetworkInterface.list()` 能否枚举到宿主真实 LAN IP，未真机验证。
- `InternetAddress(10.x addr)` 在 Android/Linux 是否抛异常，未真机验证。
- 真机 mDNS 发现（配 advertiseHost/advertisePort 后 App 能通过 mDNS 找到 NAS）是唯一未验证的核心验收标准。

## §94 主线第十四轮收口：日志降噪、EH预检移除与AI聊天页滚动修复（09～13号计划）

### 架构决策与理由

**EH 预检移除（11号，方案 A）**：选方案 A（彻底消除多余请求）而非方案 B（静默 dio 降日志不减请求）。理由：方案 A 每图请求数与 ehgt 配额消耗真正减半；`_verifyImageReachable` 保留定义（阅读链路仍需预检），改为给 `getEhImageUrl` 加 `preflightVerify` 开关（默认 `true`），天然解决 unused 风险。关键发现（对抗审查 finding 1）：dio 默认 `validateStatus`（>=200 && <300）会在 HTTP 509 返回时于 `dio_mixin.dart` 状态码校验处直接抛 `badResponse`，`ResponseBody` 尚未返回，Content-Type 判定块永不执行；必须先放开 `validateStatus: (_) => true` 才能让 509 进入判定块。

**AI 聊天页 Opacity 消闪（13号 Fix B）**：选 `IgnorePointer + Opacity` 而非 `Visibility(visible: false)`。`Visibility(visible: false)` 触发子树完全卸载，`ScrollController` detach，`jumpTo` 在 pending 期间无法落位，整个消闪机制失去意义。`Opacity(0)` 只做绘制层隐藏，挂载状态不变，pending 期间仍可执行 `jumpTo`。

**切 tab 保存位置用 deactivate 而非 dispose（13号 Fix C）**：Flutter 生命周期顺序是 `deactivate()` → ListView unmount / ScrollController detach → `dispose()`。在 `dispose()` 时 `hasClients = false`，读不到 offset；必须在 `deactivate()` 保存——该时机 ListView 仍 mounted，offset 可读。计划原文写 `dispose()`，执行时发现不可行，改用 `deactivate()`。

### 接口与行为契约

- `getEhImageUrl(..., preflightVerify: false)` 返回的直链未经任何校验，可能为空串或字面量 `"null"`；调用方必须在换节点循环入口判 `!imageUrl.startsWith('http')`，不得盲目发请求。
- `retryEhImageUrlWithNL` 不触碰 `_acquireShowKey`/`_showKeyLocks`；普通画廊 `nl==null` 时直接抛错，调用方必须先有 nl 才调用。
- `_downloadFileOnce(detectEhLimitPage: true)` 放开 `validateStatus`，509 走 `onResponse`（`MyLogInterceptor` 记 error），而非 `onError`；ehgt 槽由 `finally` 无条件释放，含新增 drain/throw 分支。
- `detectEhLimitPage` 默认 `false`；放开 `validateStatus` 只限 EH 页图链路，其余链路（封面/picacg/jm/nhentai/归档）禁止打开。
- `_saveScrollOffset()` 保存时：贴底（`maxScrollExtent - pixels <= 200`）存哨兵 `-1.0`；非贴底存绝对像素。`_restoreScrollOffsetAfterFrame`：`null` 或哨兵均走 `_scrollToBottomAfterFrame()`。
- `_pendingScrollScheduled` 去重：确保同一帧内 `_applyPendingScroll` 最多调一次，防止 `_onControllerUpdate` 与 postFrame 在同帧内累计两次推进 extentSettled 导致误判提前清标志。
- `_clearPendingScroll` 必须加 `if (mounted) setState(() {})` 触发重建，否则 Opacity 永久为 0，列表不可见。

### 根因推理链

- **Bad state: Cannot add event after closing（10号 Fix A）**：`_verifyImageReachable` 拿到响应头后 `cancelToken.cancel('verified')` → dio 关闭 `ResponseBody` 的 `StreamController` → `receiveTimeout` 20s 定时器仍挂着 → 超时后向已关闭 stream 插入 `TimeoutException`。只要不需要读 body 限速，删去 `receiveTimeout` 即可消除；`connectTimeout` 在 `BaseOptions` 里兜住挂死场景。
- **WebSocket Invalid argument: 1001（10号 Fix B）**：`package:web_socket_channel` v3 对关闭码有白名单约束（1000 或 3000-4999）；1001（goingAway）是 RFC 6455 合法码但该包明确不允许客户端发送，改为 `normalClosure(1000)` 即可。
- **EH 每图 4 条日志（11号）**：下载侧每图发两次 HTTP 请求（预检 GET + 真实 GET），两次均经过 `MyLogInterceptor`，产生 4 条 INFO。预检 GET 只判 Content-Type，拿到头即 cancel，逻辑可完全下移到真实下载侧。
- **AI 聊天页 pending 误判（12号对抗审查 finding 2）**：`_onControllerUpdate` 直接调 `_applyPendingScroll()`，而 postFrame 链也会调一次，同帧内 `_pendingScrollFrames` 累计两次，`extentSettled`（`_lastPendingScrollExtent == maxExtent`）在内容仍增长时偶发 true，提前清标志；清标志时 offset 可能距底数百像素，后续自动跟随失效。修复：加 `_pendingScrollScheduled` 去重 + `effectivelyAtTarget` 守卫 + 预算耗尽时强制 `jumpTo` 后再清标志。

### 技术约束/铁律

- `receiveTimeout` 与手动 `CancelToken.cancel` 不得并用于同一请求；会产生竞态 `Bad state: Cannot add event after closing`。
- `package:web_socket_channel` v3 全库必须用 `web_socket_status.normalClosure`（1000）；`goingAway`（1001）是运行时异常。
- `drain().timeout(5s)` 清 509 响应体不能用 `CancelToken.cancel`——计划 10 已确立 CancelToken+receiveTimeout 竞态铁律，动它风险高于收益。
- `ScrollController.hasClients` 在 `dispose()` 时通常为 false（ListView 先 unmount）；任何需要在"页面离开时读 offset"的操作必须放 `deactivate()`，不能放 `dispose()`。
- `IgnorePointer + Opacity` 组合：隐藏但保持挂载，ScrollController 保持 attached，`jumpTo` 可正常落位；`Visibility(visible: false)` 卸载子树，不适合"隐藏但仍需操作 controller"的场景。
- 贴底语义必须用哨兵而非保存像素：绝对像素在内容持续增长时会变成"历史快照"，切回后台产出消息的会话恢复时会因 nearBottom 判定为假而永久丢失自动跟随。

### ❌ 不要再走的路

- 不要在 `_verifyImageReachable`（或任何"拿到头即 cancel"的请求）里设 `receiveTimeout`。
- 不要用 `web_socket_status.goingAway`（1001）作为客户端主动关闭码。
- 不要把 `validateStatus` 放开操作（`(_) => true`）应用到 EH 页图以外的下载链路。
- 不要在 `dispose()` 里读 `ScrollController.offset`；只能在 `deactivate()` 里读。
- 不要用 `Visibility(visible: false)` 来隐藏需要保持 ScrollController attached 的列表。
- 不要在 `_onControllerUpdate` 与 postFrame 两处都直接调 `_applyPendingScroll()`；必须加 `_pendingScrollScheduled` 去重，防止同帧累计执行导致 extentSettled 误判。
- 不要只删除 `dispose()` 中"刻意不保存"的注释而不同步到调用点；切 tab 保位置需同时改 `deactivate()` 保存 + `_loadController` 恢复。

### 残余风险/未验证假设

- 509 drain 超时悬挂订阅：若服务端在传输中卡住，`drain().timeout(5s)` 让 Future 超时但不取消底层订阅，该请求挂到 30s `receiveTimeout` 才收尾，期间 ehgt 在途数可能短暂超 3；属可接受副作用。
- EH 预检移除后真实 509 换节点循环、MPV 画廊全链路换节点语义均未真机验证。
- `deactivate()` 在 Navigator push 新路由时也触发；当前 AI 聊天页只通过 NaviPane 切换（不用 push），若日后改为 push 方式须重新评估 `deactivate` + `_loadController` 的交互语义。
- Shizuku chip 显示/消失（true 机下真机）、设置页授权指示、网络日志降噪效果、AI 聊天页切 tab/切会话无闪烁与位置恢复均待用户真机确认。

### 模块职责演变

| 文件/符号 | 原职责 | 第十四轮09-13后职责 |
| --- | --- | --- |
| `app_dio.dart` `MyLogInterceptor` | onRequest/onResponse 均记 INFO 日志 | onRequest 静默；onResponse 仅 ≥400 记 error；正常请求零日志 |
| `EhNetwork.getEhImageUrl` | 每次调用均发预检 GET + 真实 GET | 加 `preflightVerify` 开关；下载侧传 false 只发真实 GET，每图请求数与 ehgt 配额减半 |
| `online_download_manager._downloadFileOnce` | validateStatus 默认（非2xx抛 badResponse） | 加 `detectEhLimitPage` 参数；EH 页图链路放开 validateStatus，509/text-html 走 `_EhImageLimitReachedException` 换节点 |
| `remote_library_event_channel._disconnect/_closeSocket` | 发 WebSocket 关闭码 1001（goingAway） | 改为 1000（normalClosure），消除 Invalid argument 运行时异常 |
| `download_page.dart DownloadPageLogic` | 标题无权限感知 | 新增 `shizukuRestricted` 字段，标题在受限时展示胶囊 chip |
| `ai_chat_page._AiChatPageState` | 切 tab 回来跳底部；切会话不记位置；切换时有可见闪烁 | static 位置缓存 + 贴底哨兵 + pending 去重 + IgnorePointer/Opacity 消闪；切 tab/切会话均恢复记忆位置 |

## §95 主线第十五轮：AI会话页长期标签回显、消息索引面板与迷你索引条（01～02号计划）

### 架构决策与理由

**新增 `activePersistentTagNames` 字段而非复用 `promptTagNames`**。否决方案 A「直接合进 `promptTagNames`」：该字段有第二个消费者 `_buildSelectableUserText`（拿它拼正则给正文里字面出现的 `#标签` 上底色），长期标签在后续消息正文里并无该 token，合进去会无谓扩大正则 alternatives 并可能误高亮用户正文中同名的 `#词`。否决方案 B「气泡渲染时直接读 controller 的 `_persistentPromptTags` 当前值」：历史气泡会显示「现在生效的标签」而非「发送当时生效的标签」，翻旧消息语义错乱且会话重载后无法还原。选新字段 = 每条消息各自冻结当时快照。

**新字段语义收窄为「长期普通标签」，不含来源/仅本地标签**。来源标签已由 `send()` 的 `scopeTagNames` 折进每条消息的 `promptTagNames`，本来就每条都显示；再从 `_persistentAllowedSearchSources` 直接取一份，会在「本轮临时覆盖来源」时显示与实际生效范围不符的标签。分工：**范围类标签走 `scopeTagNames` → `promptTagNames`；普通标签走 `activePersistentTagNames`**。

**恢复阅读位置主路径用 postFrame + `jumpTo(index:)`，`initialScrollIndex` 只作首帧补充**。否决「纯 `initialScrollIndex` + `key: ValueKey(会话id)` 强制换 State」：`initialScrollIndex` 只在 `initState` 生效一次，页内切会话不重建 State、只走 `didUpdateWidget`，会被忽略；加 key 强制换 State 等于把上一轮刚建立的 pending 状态机整套替换掉。**且明确不加 `PageStorageKey`**——fork 的 `_updatePositions` 会往 PageStorage 写位置，加了会盖掉 `initialScrollIndex`。

**贴底不改 index 语义，仍用像素目标**。`jumpTo(index: itemCount-1)` 只把末项顶到视口**顶部**，与「贴底」不是一回事，`maxScrollExtent` 才是真底部。→ **贴底 = 像素语义，历史位置 = index 语义，两套并存各管一段**。

**高度约束改法选 `ConstrainedBox` 而非「传参进 `_buildContent` 取 min」**。约束落在**布局层**，`hasBoundedHeight` 变成 true，将来任何嵌套子树看到的都是同一上限；传参只能管住 `listHeight` 这一个局部变量，日后往那个 `Column` 里加兄弟节点会整体溢出。

**上限只管迷你索引条横线数，不管面板条目数**。关键否决理由：若上限也截断面板条目，会破坏松手跳转的精确性——面板条目必须与用户消息严格 1:1，`_indexFromGlobalY` 才能把「第 k 行」直接换算成「第 k 条消息」；一旦做采样，行号到消息号成了有损映射，拖到某行松手会跳到邻近。

**速度曲线取平方（depth²）而非线性**。区边界附近速度极小（深入 10% 只有 1% 速度），手指在中间区与边缘区之间徘徊时不会一跨界就窜动；线性在边界处斜率是常数，两侧有明显台阶。

**Ticker 而非 `Timer.periodic`**。Ticker 与帧同步，`jumpTo` 落在同一帧 layout 前，不会「一帧内滚两次」；随 `TickerProvider` 生命周期自动受 `muted` 管控。`Timer.periodic` 是墙上时钟、与帧不对齐，且要自己处理页面隐藏时的空转。

**迷你索引条常驻更新只能走 `ValueListenableBuilder`**。否决「内部 `addListener` + 自己 `setState`」：多一份手写生命周期，是漏摘监听的常见来源。更要否决「常驻 `setState` 整页」——会让滚动聊天时整页每帧重建，与流式输出逐 token setState 叠加必然掉帧。

**迷你索引条数据源与面板条目分开缓存**（`_userMessageIndices()` vs `_buildIndexEntries()`）。前者缓存键**不含**设置 141，正因为它与该设置无关——索引条永远只取 user。共用一份会让「关掉开关后索引条也跟着变」。

**计划外新增「起手抑制」（52dp）**。面板顶端对齐长按起点浮出，手指天然落在边缘区里；不加抑制则任何长按只要几像素抖动就立刻满速滚走，索引面板等于不可用。否决「必须先离开边缘区再进入」的进出闸：依赖输入采样密度，测试里一次 `moveTo` 从上缘直跳下缘、真机快速甩动也可能不产生中间采样点，闸门永远解不开。

**空态与非空态的高度上限分流而非统一兜底**。上限有两个互不相同的职责：非空态要「小于内容全高」才能让 `maxScrollExtent > 0`，空态要「不小于内容自然高」才不溢出。用同一个 clamp 兜底会让空态上限恰好卡在内容自然高附近，字号一变就翻车。

**长按时长用 `RawGestureDetector` 而非改全局 `kLongPressTimeout`**。后者影响全 App 所有长按（气泡长按复制、收藏页长按多选等），本轮只想改索引面板这一处。

### 接口与行为契约

- **`maxScrollExtent` 从恒 0 变为 >0 会推翻既有测试的隐含前提**：修复前面板视口高度 == 内容高度、`offset` 恒 0、`_scrollPanelToTop/Bottom` 是空操作、`_revealActiveEntry` 只能落 0。修复后打开面板即滚到 942.4，同样手势从 entry 0 落到 row 8。将来写面板用例必须先明确「期望面板停在哪」。
- **`_indexEntriesVersionKey()` 必须包含所有影响 entries 内容的设置值**：现为 `会话id|渲染项数|消息数|末条文本长度|userOnly(0/1)`。破坏后静默失效：改设置后命中旧缓存，面板内容不变，**不报错、不抛异常、日志无痕**，用户视角是「改了设置没反应」。
- **`_dragHoverIndex` 是高亮与跳转的唯一共同来源**：每 tick 用 `_dragCurrentGlobal` 重调 `_indexFromGlobalY`，与 `onLongPressMoveUpdate`、`onLongPressEnd` 走同一方法，仅值真变化才 `setState`。摘掉每 tick 重算，高亮会卡死在开局那一行。
- **`_dragCurrentGlobal` 与 `_longPressStartGlobal` 严格分开**：后者是起点、被面板 `top` 定位读取，覆写会让面板在拖动中乱跳。
- **`_panelLocalGeometry(globalY)` 不做任何 clamp**：这正是它与 `_indexFromGlobalY` 的分工——那边 clamp 后「贴边」与「越界 300px」返回值相同，无法判定边缘或算深入程度。两者取自同一个 `_panelContentKey` 的 RenderBox、同一坐标系。
- **阅读位置记忆表语义破坏性变更**：`Map<String,double>`（像素）→ `Map<String,int>`（视口顶部 item index），哨兵 `-1.0` → `-1`。破坏后静默失效：若把 double 值写回这张表，`_sanitizeSavedIndex` 一律当脏值回落 index 0，表现为「切回会话跳到最顶部」，不报错不崩溃测试全绿。
- **`_sanitizeSavedIndex(Object?, int)` 形参刻意是 `Object?` 不是 `int?`**：四类脏值（double/非数字/负数/越界）都不抛异常；`itemCount` 必须取**即将切进来的那个 controller** 的重算值，不能用上一帧的 `_listItemCount`。
- **index 口径统一为 `_listItemCount = _displayItems.length + (pendingDownload != null ? 1 : 0)`**：末项可能是下载确认卡而非消息。
- **设置 141**（`aiIndexUserOnlySettingIndex`）：`'1'` 默认 = 面板只列 user；`'0'` = user + assistant。**只管面板内容**，不影响迷你索引条。
- **设置 142**（`aiIndexBarMaxTicksSettingIndex`）：默认 `'6'`，`'0'` = 不限制（沿用 `aiMaxToolRounds` 约定）。只管迷你索引条横线数。解析失败或负值退回 6。
- **`_MiniIndexBar` 横线数三重收紧**：`min(用户消息数, 设置上限, 命中区装得下的根数)`。**「不限制」的语义因此是「不再额外截断」而非「一定 1:1」**——命中区高 480 时最多 160 根。
- **停止出口共 6 处**：`onLongPressEnd`、`onLongPressCancel`、`_closeIndexPanel`（在 early-return **之前**）、`State.dispose`（`super.dispose()` 前 + `Ticker.dispose()`）、手指移回中间区、`maxScrollExtent == 0` 守卫。
- **`_openedAtY` 是面板整个开/关周期内的位置锚点**：只在 `visible: false→true` 写一次，淡出期间不得变化；父 State 何时清 `_longPressStartGlobal` 对面板位置不再有影响。破坏后表现：面板在松手瞬间跳到屏幕顶部再消失，不报错不崩测试不红。
- **`_inputAreaReserve` 是用户可调的观感参数，测试不得依赖其具体值**：几何断言必须从 `getRect`/`ScrollPosition` 现算或用公式表达。破坏后表现：用户下次手调时一片测试变红但代码其实没坏，会误导成「改坏了」而回退正确的观感调整。
- **命中区宽度与视觉宽度解耦**：视觉元素被 `IgnorePointer` 包住，命中完全由外层 `Positioned(width: 36)` 决定。

### 根因推理链

- **长期普通标签不回显**：UI 侧 `_send()` 在发送前调 `_clearPromptPanelState()` 清空 `_selectedTagChips` → 下一轮 `selectedPromptTags` 为空、正文里也不再有 `#标签` 字面 → `mergedRecognizedNames` 拿不到长期普通标签 → 气泡无渲染依据。注意 `promptTagNames` 后续消息**并非全空**，仍含由 `scopeTagNames` 反推出的来源标签，真正丢失的只有普通标签部分。
- **面板 `maxScrollExtent` 恒 0**：`Positioned` 只给 `top`/`right` 无 `height` → `RenderStack` 下发 `0..∞` 无界高度约束 → `LayoutBuilder.hasBoundedHeight == false` → `maxHeight` 退化成 `desired` → `listHeight == 内容全高` → 视口高度 == 内容高度 → `maxScrollExtent == 0`。连带三个衍生症状：`_revealActiveEntry` 空操作、测试的 `_scrollPanelToTop/Bottom` 空操作、超屏部分被 `Clip.hardEdge` 裁掉且无法到达。
- **面板「有点靠底」**：可用高度在「全屏坐标系」算（扣 safeTop/bottomInset/输入区），结果却喂给原点在 AppBar 下方的 `Positioned.top` → 差整个 `kToolbarHeight`（56）→ 按全屏算「还在屏内」，落到 body 里已压住输入区。**光调 116/16 治不了**。
- **短会话整页不可见**：fork 的 `UnboundedCustomScrollView` 以 center sliver 为原点，`maxScrollOffset = max(min(0.0, top), bottom)` **不做 `max(0, ...)` 截断** → 短会话 `maxScrollExtent` 合法为负（实测 -112）→ pending 落位旧判据 `maxExtent <= 0` 把已布局好的短会话误判成「未渲染」→ 8 帧预算耗尽后 pending 永不清除 → `Opacity` 卡在 0。
- **fork 的 `scrollTo` 远距分支会崩**：fork 把 primary 与 secondary 两个 `_ListDisplayDetails` 构造为持有**同一个** `widget.scrollController`（上游是各自 new 一个）→ 目标 index 不在已布局区间时走过渡分支，同帧内两个 `UnboundedCustomScrollView` 把同一 controller attach 出两个 `ScrollPosition` → 命中 `assert(_positions.length == 1)`；且 `jumpTo` 对所有 position 生效，secondary 的 jumpTo 会同时错误移动 primary。
- **迷你索引条加深位置到不了最后一根**：`_activeTickIndex` 用视口顶端那条算 ordinal → 底部时 topVisible 只到 (n-k)（一屏约 k 条）→ `20/29 × 5 = 3.45` → round 3。**分母用「总数-1」隐含假设视口只显示一条**。改用可达范围归一：`reachable = userCount - 1 - visibleSpan`。
- **手指开局天然落在边缘区**：面板 `top = (longPressStartY − safeTop − kToolbarHeight − rowExtent/2).clamp(...)` → 未触 clamp 时手指恰落第一行中部（局部 Y=26 < zone 82.4，上缘区）→ 触 clamp（长按点靠下）时面板被上移、手指落到面板底边之外 76px（下缘区最深处，满速）→ **无论长按在哪，开局都在某个边缘区里**。
- **两条溢出回归同源**：都是「给一个原本无界的子树加上界」时只按『正常内容』算了上界，没算『内容会随字号/条数变大』。空态上界按行高兜底（行高不随字号变、文案会变）；索引条上界只收间距（间距能收到 0，`_slotHeight = 3` 槽位不会）。
- **面板位置在松手瞬间跳变**：`onLongPressEnd` 里 `setState` 把 `_longPressStartGlobal = null` **早于** `_jumpToIndexEntry` 内部的 `_closeIndexPanel()`，而面板淡出还要 160ms——这 160ms 里 `widget.longPressStartY` 已是 null，落到 `top: 8` 兜底分支。**面板位置依赖了一个生命周期比它短的外部状态**。
- **「Ticker elapsed 固定 33.333ms」不成立**：`LiveTestWidgetsFlutterBinding` 下 Ticker 的 `elapsed` 走**真实墙上时钟**，而每次 `pump()` 要跑完整 build/layout/paint/semantics，实测每帧 50~120ms 浮动（同一用例内相邻帧差 2.3 倍）。

### 技术约束/铁律

- **`SingleTickerProviderStateMixin` 的 `createTicker` 全生命周期只能调一次**（SDK 硬断言）。必须 `_edgeScrollTicker ??= createTicker(...)` 懒建一次后反复 `start`/`stop`。若本页还要第二个 Ticker，必须整体换 `TickerProviderStateMixin`。mixin 叠加 `with AutomaticKeepAliveClientMixin, SingleTickerProviderStateMixin` 无冲突。
- **`dispose` 必须在 `super.dispose()` 之前停掉并 `dispose()` 掉 Ticker**（SDK 硬断言，报错文案 `_AiChatPageState was disposed with an active Ticker`）。
- **`jumpTo` 前判 `hasClients` 且 `clamp` 到 `min..max`**（面板淡出后 `_rendered = false`，内容子树被移除，controller 无 client；越界 `jumpTo` 抛断言）。
- **`_stopEdgeAutoScroll()` 必须放在 `_closeIndexPanel` 的 early-return 之前**：`_closeIndexPanel` 开头有 `if (!_indexPanelVisible) return;`，放它之后就漏掉「状态已被别处改成不可见但 Ticker 还在跑」的情形。
- **`material.dart` 的导出边界要实测，别猜**：不带 `ValueListenable`（在 `foundation.dart`）、不带 `Ticker`（在 `scheduler.dart`，只导出 `TickerProvider`/`SingleTickerProviderStateMixin`）、不带 `LongPressGestureRecognizer`（在 `gestures.dart`）。**本轮累计撞 3 次**。
- **`GestureRecognizerFactoryWithHandlers` 的 handler 形参必须显式标注类型**（`(LongPressGestureRecognizer instance)`），否则 null-safety 分析失败；级联写法 `instance..onLongPressStart = ...` 触发 `unchecked_use_of_nullable_value`。
- **fork 约束**：构造器 `scrollController` 是本 fork 新增且**必填**；缺 `scrollOffsetController`/`scrollOffsetListener`；`padding` 类型是 `EdgeInsets?`；`jumpTo`/`scrollTo` 前应判 `isAttached`。
- **新增 `final` 字段必须补齐 `AiChatMessage` 全部 7 个构造器**（主 + user/assistant/toolCall/toolResult/resultList/error），只改 `user()` 直接编译失败。
- **`_edgeScrollArmDistance = 52`、`_edgeScrollMaxSpeed = 1040`、`_edgeZoneMaxHeight = 96`、`_inputAreaReserve = 265` 均为可推翻的档内决策/用户手调值**，只在 800×600 测试视口验证过。

### ❌ 不要再走的路

- **不要只改 `AiChatMessage.toJson()` 就以为字段落盘了**。读写是两条路：写 = `AiConversationStore._serializeAiChatMessage`，读 = `AiChatMessage.fromJson`。`toJson()` 在 lib/ 内零调用，只被往返测试用，而**那条往返测试恰好绕过 store，所以只改 toJson 时 analyze 和单测都绿，但重启会话标签全丢**。本轮最危险的静默失效点。
- **`AiConversationStore.deserializeAiChatMessage` 是死代码**（lib/ 与 test/ 全仓无调用），别把它当读路径。
- **不要给索引面板条目直接用裸 `scrollTo`**：长会话跳远必然进过渡分支 → 抛异常。做法：先判 `itemPositions.value` 里有没有目标 index，命中才 `scrollTo`，否则退化 `jumpTo`。**修 fork 不在范围内，不要顺手改 `lib/components/scrollable_list/`**。
- **不要照字面把「滚动进度百分比」写成 `ordinal/(n-1)`**：滑到尽头也到不了最后一根，分母必须扣掉可见跨度。
- **不要用 `MediaQuery(data: MediaQueryData(textScaler: ...))` 包整页来测字号**：会连 `size`/`padding`/`viewInsets` 一起置默认值（`Size.zero`），页面布局全变，读出来的数值是假的。必须用 `MediaQuery.withClampedTextScaling`。
- **`Column(mainAxisSize: MainAxisSize.min)` 不防溢出**：它只让 Column 自身取子高之和，父约束更小时照样报 `RenderFlex overflowed`。想不溢出必须让子内容真的装得下，或显式滚动/裁剪。
- **不要在 `Stack` 里用 `Positioned(top:0, bottom:0)` 画竖线，却不锁死 `Stack` 自身高度**：`Stack` 默认 `fit: loose`，非 Positioned 子节点变高会带着整个 Stack 变高。
- **不要为了「打开面板时高亮对齐手指」直接改 `_revealActiveEntry` 的 target 算法**：长会话最新消息会被 clamp 贴底，边缘自动滚动失去余量。要做这个需求必须先解决「贴底后还能往下滚什么」。
- **不要用 `Scrollable.of(tester.element(listFinder))` 取面板 ScrollPosition**：`listFinder` 命中的是 `ListView` 自己，`Scrollable.of` 往**上**找而报 `No Scrollable widget ancestor`。要往下找：`find.descendant(of: listFinder, matching: find.byType(Scrollable))`。
- **面板空态时不能用 `find.bySemanticsLabel('消息索引面板')` 精确匹配**：空态无 `ListView`、少了 `Scrollable` 语义边界，叶子 Text 被合并进容器节点，label 实际是 `'消息索引面板\n暂无可索引的消息'`。用 `RegExp`。
- **不要直接断言 `WidgetsBinding.instance.hasScheduledFrame == false`**：它是全局 binding 状态，单跑绿、连跑被上一用例遗留动画污染。用「推帧直到排空（有预算）」。
- **「不空转」断言必须放在 `gesture.cancel()` 之前**：cancel 本身会停 Ticker，放它之后无论有没有守卫都是绿的。
- **不要把长期标签并进 `_buildSelectableUserText` 的正则来源**；**不要把气泡标签的 `Text('#$name')` 换成 `Chip`/`ActionChip`**（现有实现是朴素 Text，换组件等于视觉回归）。

### 残余风险/未验证假设

- 真机手感/观感全部未验：200ms 长按是否够跟手且不易误触、36dp 命中区是否够好打、迷你索引条三维强化幅度（10→14 / 1.5→2.5 / alpha 0.4→1.0）、边缘滚动速度曲线与起手抑制、`_inputAreaReserve = 265` 下的面板高度、深浅色主题配色。
- **「面板打开时当前位置对齐手指」需求未实现**（本轮尝试后回退）。设计难点：与「打开即贴底 → 自动滚动无余量」冲突。可能方向：仅当 `activeRow` 不在首尾各一屏范围内时才对齐手指，边界情况保持现有 clamp。
- 空态在极端窄 body（横屏+键盘，body ≈ 250 → 上限 ≈ 70dp）叠加 2.0 倍系统字号时仍溢出约 30px。**刻意取舍**：宁可溢出提示，也不放弃「面板底边不压输入区」。
- **「滚动时不引发整页 setState」没有自动化断言守住**（代码层面由 `ValueListenableBuilder` 保证，但这份测试骨架无法可靠数出整页重建次数）——已知测试盲区，与「定时器泄漏是测试盲区」同类。
- 到头后 Ticker 刻意继续空转（为方向反转能立即恢复），与流式输出逐 token `setState` 叠加的场景未压测。
- `jumpTo` 每 tick 派发一轮 `ScrollStart`/`ScrollEndNotification`（约 10~30 次/秒）；面板子树内目前无 `NotificationListener`，若将来有人在外层加会收到高频空通知。
- `reachable` 随可见跨度变化，percent 不是严格单调（一屏能显示的条数随气泡高度浮动），滚动中加深位置可能有极小幅回退。
- 假设本页 AppBar 恒为标准高度；若日后加 `AppBar.bottom` 或改 `toolbarHeight`，`top` 会偏。
- 重启保持仅静态可证（`_setSetting` → `updateSettings()` 落盘），未真机重启验；无障碍读屏未实测。
- `_armedFingerX` 的横向偏移依赖「边缘区判定只用 dy」这一实现细节；若将来加入横向命中判定，这批测试会集体失效且原因隐晦。

### 跨轮诊断模式

- **「计划里带具体数值的实测结论，执行时必须在真实场景重测再依赖」**——本轮撞两次（「滚动进度百分比照字面写就错」、「Ticker elapsed 固定 33.3ms 不成立」）。共性是调研阶段的探针与执行阶段的真实场景不同构：前者用孤立小 widget 测，后者整页 build/layout/paint。
- **「测试全绿 ≠ 没缺陷，取决于覆盖了哪几个维度」**——两条溢出回归在「默认字号 + 30 条消息」下全绿，是校验档显式加了**字号档位**与**超长会话**两个独立维度才命中。反过来**「既有用例变红」也不等于功能坏了**：本轮 4 次变红中 3 次是测试前提被新行为改掉（前提靠旧缺陷兜住、seed 数据类型、全局设置污染），处理原则是**显式化前提、不弱化断言、不改测试名**。
- **「给原本无界的子树加上界」是溢出回归的统一形状**：加 `maxHeight` 或固定槽位时，必须按「内容在极端字号/极端条数下需要多少」算上界。
- **「参数被手调后测试变红」不等于代码坏了**：先看失败的是「数值不等」还是「行为不对」，前者优先怀疑断言写死了会变的量。处理原则是把断言改成语义表达式而非跟着改数字。
- **「防误触设计」会在测试里表现为「功能不工作」**：起手抑制/最小位移/冷却时间这类设计，测试里要显式构造超过阈值的输入，并在 helper 上写明原因。
- **「换底层组件时必须清算依赖旧组件数值假设的所有分支」**：`maxScrollExtent` 从「必然 ≥ 0」变成「允许为负」，直接让一个原本正确的判据变成整页白屏 bug。换组件后逐条 grep 旧组件的量纲/边界假设。
- **`static` 字段 + widget test 是跨用例污染的固定组合**；全局 `appdata.settings` 同理，必须加 `setUp` 复位。
- **一个视觉 bug 可能有两层原因**：竖线溢出先修了「Stack 高度不受控」，用户再反馈「看起来空一行」才是第二层的「内容贴顶 + 行高固定 = 下半截空白」。布局类反馈修完要请用户再看一眼。

### 模块职责演变

| 文件/符号 | 本轮之前 | 第十五轮后 |
| --- | --- | --- |
| `AiChatMessage` | 只携带「本轮识别到的标签快照」`promptTagNames` | 增设第二维度 `activePersistentTagNames`（长期生效但本轮未再识别的普通标签），两字段互斥去重共同构成气泡显示集合 |
| `AiConversationController.send()` | 算一份标签名 | 职责二分：范围类标签走 `scopeTagNames`，普通标签走新字段 |
| `ai_chat_page` 消息列表 | `ListView.builder` + 纯像素 `ScrollController` | `ScrollablePositionedList.builder` + `ItemScrollController`（按 index 定位）+ `ItemPositionsListener`（可见项来源）+ 原 `_scrollController`（继续承担像素判定） |
| 阅读位置记忆 static Map | 会话 id → 像素 offset（double） | 会话 id → 视口顶部 item index（int），新增 `_sanitizeSavedIndex` 脏值容错；语义从「逐像素还原」变为「把离开时视口顶部那条重新顶到顶部」 |
| `_AiChatPageState` | 页面状态 + 面板显隐 | 额外**持有帧驱动的自动滚动状态机**；本身是 `TickerProvider`，承担「手指位置 → 面板几何 → 速度 → offset → 高亮」每帧闭环 |
| `_ConversationIndexPanelState` | 只管淡入淡出 + 定位 | 同时是**面板高度上限的唯一计算点**；新增 `_openedAtY` 位置锚点 |
| `panelActualHeight` | 只用来 clamp `top` 的参考值 | 面板内容区**真实高度上限**（同时喂 `ConstrainedBox` 与 `maxTop`），按空态/非空态**分流**计算 |
| `_panelContentKey` | 单一用途（index 换算） | **边缘判定与 index 换算的共同几何来源**，必须继续共用同一个 box |
| `_closeIndexPanel` | 摘监听 + 改标志 | **面板关闭的唯一收口点**（含停自动滚动），新增任何关闭路径只需走它 |
| `_buildIndexEntries()` | 固定收 user + assistant | 按设置过滤；缓存键从「纯内容版本」扩展为「内容版本 + 过滤条件」 |
| 右边缘触发条 | 命中区 20dp + 一根装饰细胶囊 | 命中区 36dp + **随滚动更新的迷你索引条**；分层不变（命中在 `GestureDetector`、视觉在 `IgnorePointer`），手势承载改 `RawGestureDetector`（200ms） |
| `_MiniIndexBar.build` 的 `LayoutBuilder` | 不存在 | **三段式「先定容量、再定根数、最后定间距」**，顺序不可颠倒 |

## §96 主线第十五轮续：AI图片发送/OCR、webview菜单修复与soutubot以图搜源（03～05号计划）

### 架构决策与理由

**`LlmMessage` 新增 `imagePaths` + 存档/请求双序列化出口，而非把 `content` 改 `Object?` 承载 parts**。否决理由是**静默腐化**：`LlmMessage.fromJson` 用 `json['content']?.toString()`，content 变 List 后不抛异常而是腐化成 `"[{...}]"` 字符串，重启恢复会话后历史消息变成垃圾文本且毫无报错；`_systemPrefixFingerprint` 同样假定 String。采用方案收益：存档只存路径体积小、每轮请求带哪些图可控、零静默腐化面、指纹逻辑零改动。

**历史回放瘦身放 controller 而非 LlmClient**。history 归属与「哪条最新」的判定都在 controller，LlmClient 保持无策略的哑管道（uniformly `await toRequestJson()`）。

**视觉能力 = 用户声明开关，不自动探测**。OpenAI 兼容生态无可靠能力查询接口，试探性发图的失败错误码各家不一。开 → parts 路径；关 → OCR 转文字并入 user_query（`attachmentPaths` 仍进 displayMessages 供气泡展示但不进 `LlmMessage.imagePaths`）；关且 OCR 不可用 → error 气泡明确提示，**绝不静默丢图**。

**附件落盘时机选在 `_send()` 内而非选图时**。选图即落盘会在「选了不发」时产生孤儿文件；发送时落盘 + 失败回滚删除可保证 attachments 目录只含已入列消息的图。

**`send()` 返回 bool 是 OCR 失败回滚的最小通信面**：不加回调、不加状态字段，页面据此恢复输入文本与待发附件。

**压缩分工：解码留主 isolate、编码进 compute isolate**。`ui.instantiateImageCodec`/`ImageDescriptor` 需要 Flutter engine binding，进 compute isolate 直接崩；只把 CPU 密集的 `encodeJpg` 挪进 isolate。

**OCR 通用分支的业务码校验用「通用启发式」而非为 Umi 硬编码分支**。只认「顶层 `code` 是整数」这一广泛约定（101 → 空结果、其他非 100 → 带 message 报错、无整型 code → 放行）。初版「只看 HTTP 200 + 取值路径非空」被否决：Umi-OCR 对无文字图返回 code 101，其 `data` 若是非空错误描述串，会被当成「识别出的文字」静默并入用户消息发给模型。放行分支保住了 PaddleOCR hub（顶层 `status` 是字符串）等服务的兼容性。

**`OcrClient` 独立于 `LlmClient`**：`LlmClient.chat` 签名吃 `List<LlmMessage>`，而 `LlmMessage` 按前述决策不承载 parts，视觉 OCR 必须手拼 raw messages Map；故新建轻量客户端，照抄网络样板、砍掉 tools/usage/参数白名单。

**04 的 `webviewFullUrl` 提为顶层纯函数而非留在 onTap 闭包内**。唯一动机是可被 VM 单测直接调用——`InAppWebView` 在 flutter_test 环境无法创建平台视图，pump 整个 `AppWebview` 只能走到 controller 为 null 的分支且可能 MissingPluginException，widget 测试无价值。

**05 只做 soutubot 直连，不接 SauceNAO**（用户明确否决）。否决 HeadlessInAppWebView 静默过盾（项目零使用先例、Linux 未注册 inappwebview、Windows 代理缺口）；否决「headless 加载主页 + 塞 file input + JS 拦截响应」兜底（强依赖前端 DOM 结构，改版即碎，且大图 base64 传 JS 上下文不可行）。

**soutubot 三文件分层**：签名纯函数 / 模型映射 / 网络流程。网络层只负责「字节进 → Res 出 + CF 异常上抛」，过盾、文件读取、相似度过滤全部留给工具层。

**`#搜图` chip 不能用裸 `setState`**：面板挂在 `OverlayEntry` 上，只 setState 页面 State 不会重建面板，chip 选中态不回显。必须照 `_toggleLocalOnlyTag()` 双通知（State setState + `_promptPanelEntry?.markNeedsBuild()`）。

**panda（chaika.moe）严禁映射成 ehentai**：chaika 归档 ID 与 eh gallery ID 不同构，映射会打开错误页面。降级为 `source` 空串 + 携带可打开的 `webUrl`，清单页兜底底栏给「打开原网页」。

### 接口与行为契约

- **`LlmMessage` 双出口**：`toJson()` 存档出口（平铺、content 仍是 String、只存相对路径）；`toRequestJson()` 请求出口（async，imagePaths 非空时展开 OpenAI parts、base64 现场从本地文件读、文件缺失降级为占位文本 part 不抛异常）。imagePaths 为空时两者输出**完全一致**。
- **`stripImagesToPlaceholder()` 的占位串必须逐字节稳定**（DeepSeek 前缀缓存契约）；无图消息返回**同一实例**（`identical`，测试有断言）。
- **`send()` 返回语义**：true = 消息已入列（即使 `_runLoop` 抛异常也 true，因为 user 消息已进 history，页面**不得**回滚删附件文件）；false = 发送未发生（守卫早退 / OCR 失败 / OCR 未配置），此时未动 history 与长期状态，页面删除本次落盘文件并恢复输入 + 附件。
- **`persistAiAttachments` 失败自清理**：单文件失败先删本批已写文件再抛带文件名异常——调用方 catch 后无需再清理（「无残留文件」验收依赖此契约）。
- **OCR 空结果与错误分离**：`OcrResponse(text: '')` = 图中无文字（走「[图片N 无可识别文字]」占位，不弹错误气泡）；`error != null` 才中止发送。
- **generic OCR 成功判定** = HTTP 200 + `checkGenericOcrBizCode` 放行 + `extractByOcrPath` 非空，业务码校验在取值提取**之前**。
- **附件相对路径统一 `/` 分隔**（Windows 上 resolve 时转 `Platform.pathSeparator`），避免跨平台迁移数据目录后路径失效。存相对路径的原因：`App.dataPath` 可被用户改，绝对路径会失效。
- **`webviewFullUrl(WebUri?) → String?`**：null 入参、空串、纯空白串均返回 null（调用方以 null 为「URL 不可用」信号给可见提示）；非空返回 `toString()` 完整 URL。三个菜单项从此**永不静默失败**。
- **`SoutubotNetwork().searchByImage(Uint8List) → Future<Res<SoutubotSearchResult>>`**：CF 质询一律 rethrow（`CloudflareException` implements `DioException`，三重判定必须放在 `on DioException` 分支内**先行** rethrow）。
- **`AiToolExecutionContext.resolveAttachmentPath`** 为可选回调，只有 controller 工具循环注入；工具直连（`execute`）语境 resolver 为 null → 引导性 failure。
- **ref 白名单** = 当轮 `_ActiveTurnContext.attachments` ∪ 历史 user 消息 `attachmentPaths`（倒序扫，支持跨轮「再搜刚才那张」），名单外一律 null。
- **`search_by_image` 成功 data 形状** `{items, top_similarity, low_confidence, search_id}`；空结果是 **success 空列表**（不触发结果卡）。
- **`shouldAutoDisplayToolResult` 白名单** = `{display_result_list, search_by_image}`，同轮多工具后者覆盖前者。
- **`send(searchByImage:)` 只作用当轮**，任何持久链路不写入。
- **模型上下文只进 ref（附件相对路径）**，严禁绝对路径 / base64 进 turn_context。

### 根因推理链

- **webview 三个菜单项「点了没反应」**：`controller?.getUrl()` 返回 `WebUri?`，`WebUri.path` 只返回 URI 的 path 分量（`https://soutubot.moe/` 的 `.path == '/'`）→「在浏览器中打开」实际执行 `launchUrlString('/')`（无 scheme 必失败，异常被 `onTap: () async =>` 吞掉）、「复制链接」只复制到 `/`。附带 `(await controller?.getUrl())!` 的 `!` 在 controller 为 null 时抛错，async onTap 里同样静默。**根因不是空回调，是取错字段**。
- **`parseAiPromptTags` 会吞掉图片语义**：`ai_prompt_tags.dart:371` 对剥离后为空的文本自动把 `userText` 替换成 `aiPromptTagOnlyUserBridge`（「…按本轮提示词标签继续处理」）——图片-only 消息若放任空文本进 parse，模型会收到与图片无关的标签导向语。页面层必须抢先用 `aiImageOnlyUserBridge` 占位。
- **`isToolAllowedByScope` 与 `disallowedScopeResult` 必须同步改**：前者管 schema 过滤（模型看不看得到工具）、后者管 dispatch 拦截（模型硬调时拦不拦）。只改一边会出现「模型看不到工具但硬调时被放行」或反之。
- **`_OcrTextField` 光标被重置**：外部回填同步若无脑 `_ctrl.text = widget.value`，用户每敲一键都会 rebuild 并覆写、重置光标。正确条件是 `widget.value != oldWidget.value && widget.value != _ctrl.text`（用户键入时 value 与 text 相等天然跳过，模板回填时才真正同步）。
- **prune 附件清理测试死循环**：`while (index.length <= 50) create` —— prune 会把长度钉在 50，循环永不退出。要先算 `toCreate = 51 - baseCount` 定量创建。

### 技术约束/铁律

- **`ui.instantiateImageCodec` / `ui.ImageDescriptor` 只能在主 isolate**（需要 engine binding，进 compute isolate 即崩）。来源：项目铁律（`jm_image_recombine.dart`），强度=铁律。
- **`package:image` 严禁解码 WebP**：VP8 lossy 解码器有系统性越界 bug。来源同上；image 包在本项目**只准做编码**，解码统一 `ui.instantiateImageCodec`。
- **`showToast` 在本项目是 no-op 空实现**（`lib/components/message.dart:5`，`download_page.dart:1509` 注释可证）。用户提示必须 `ScaffoldMessenger.of(context).showSnackBar`，且 async 回调 await 之后必须 `mounted` 保护。
- **`llm_client.dart` 含 `\0` 字节**：Grep 判为二进制搜不到，检索该文件必须 `grep -a` 或直接 Read。
- **soutubot 签名唯一正确写法 = double 运算 + `double.toString()` 去尾 `.0`**。`int` 精确运算 / `BigInt` / `toStringAsFixed(0)` / `toInt()` 四者结果均与 JS `Number.prototype.toString()` 不同。来源：实测锁定，严禁改写。
- **dio 5.4.1 的 `FormData` boundary 硬编码 `--dio-boundary-` 前缀，无自定义入口**（本地源码核实）——手拼 multipart 是必要非选择。`data is Uint8List` 时 dio 免 transformer 原样发送并自动补 content-length（`dio_mixin.dart:629-653`），不要包 Stream（包 Stream 需自己补 content-length 头）。
- **CF 过盾必须带超时兜底**：桌面手动关窗时 `onFinished` 永不回调，需 Completer + 180s 超时。来源：`cloudflare.dart` 代码事实。
- **`launchUrlString` 失败形态平台差异**：Android 可能返回 false，桌面可能抛 `PlatformException`，**bool 返回值与 try/catch 两路都必须接**。
- **settings 新索引必须取数组实际末尾 +1**（本轮 143/144/145），`readSettings` 对新增索引天然向后兼容，无需迁移代码。
- **`package:flutter/foundation.dart` 已导出 `Uint8List`**，不要重复 import `dart:typed_data`（`unnecessary_import`）。

### ❌ 不要再走的路

- **不要只改 `AiChatMessage.toJson()` / `LlmMessage.toJson()` 就以为字段落盘了**。store 有自己的一对出入口（`_serializeAiChatMessage` 写 / `deserializeAiChatMessage` 读）与 model 的 `toJson`/`fromJson` **并存**，加字段必须四处同改；漏 store 侧则 save→load 一轮字段静默丢。且 `toJson()` 在 lib/ 内零调用、只被往返测试用，**那条测试恰好绕过 store**，所以只改 toJson 时 analyze 和单测都绿，但重启会话数据全丢。
- **不要把 `content` 改成 `Object?` 来承载 parts**（静默腐化，见架构决策）。
- **不要在 `compute` isolate 里调 `ui.instantiateImageCodec` / `ImageDescriptor`**；**不要用 image 包解码 WebP**。
- **不要用 `lib/components/message.dart` 的 `showToast`**——空实现，调了等于没提示。
- **不要照抄计划里面板 chip 的裸 `setState` 写法**：面板在 `OverlayEntry` 上，需双通知才回显选中态。
- **不要把 panda / chaika 条目映射成 ehentai**：ID 不同构，会打开错误页面。
- **不要改 `isToolAllowedByScope` 而不同步改 `disallowedScopeResult`**。
- **不要在 `_OcrTextField` 里无条件 `_ctrl.text = widget.value`**（重置光标）；OCR 额外 body JSON 字段要允许「非法中间态原样存字符串」，否则用户打不出 `{"a":` 这种中间态。
- **不要为 webview 菜单写 widget 测试**：`InAppWebView` 在 flutter_test 无法创建平台视图，只能走到 controller null 分支且可能 MissingPluginException，为凑覆盖写这种用例没有价值。
- **不要在网络层重复 import `def.dart` 的 `webUA`**——`base.dart` 已导出（`unnecessary_import`）。
- **multipart 字节断言不要用 `utf8.decode`**，会把 0xFF 等二进制字节解码失败/替换；用 `latin1.decode` 做 1:1 字节解码。
- **不要用 `while (index.length <= 50)` 造 prune 测试数据**（死循环，见根因推理链）。

### 残余风险/未验证假设

- 三个 OCR 预设模板（Umi `:1224/api/ocr`+`data`；PaddleOCR hub `:8866/predict/ocr_system`+`results[0][*].text`；PaddleX `:8080/ocr`+`result.ocrResults[0].prunedResult.rec_texts[*]`）的端点/字段名/取值路径取自公开文档，**未经真实服务实测**。若有误属预设数据修正，改 `_ocrTemplatePresets` 表即可，不动架构。
- Umi-OCR 对无文字图返回顶层整型 code 101 的约定未实测；视觉 OCR 的 `<NO_TEXT>` 约定依赖模型遵循 `visionSystemPrompt`，弱模型可能输出解释性文字混入转写。
- 压缩链路（ImageDescriptor 读宽高 / instantiateImageCodec 缩放解码 / compute encodeJpg）未在真机对超大图 / GIF / WebP 实测（单测只覆盖不触发压缩的小 PNG）。GIF 会被解出首帧、重编码后动画丢失（可接受，视觉模型也只看首帧）。
- **DeepSeek 前缀缓存一次性破坏**：某条带图消息在「作为最新带图消息」那轮是 parts 数组，下一轮起变成占位文本，该消息处前缀必然失配一次。这是历史瘦身的固有代价，**不是 bug，不要试图修复**。
- dio 无 sendTimeout（与 LlmClient 现状一致）：4×1.5MB base64 后请求体约 8MB，慢网络上传会长时间占住 isLoading。
- soutubot 签名 / multipart 细节 / 响应形状 / eh subjectPath 是否含 token —— 全部只过单测**未打真实接口**。
- CF 过盾链首次投产未真机验证（两端 JS 返回类型差异、Windows webview 代理静默失效）。
- 旧会话不获得 system prompt 规则 11（可接受）。
- `_resolveAttachmentPath` 倒序扫历史在超长会话（数百条带图消息）下是 O(n) 线性扫，当前量级无感。
- Linux 端 `controller` 恒 null 时触发提示分支——推断成立但未实测（`linux/flutter/generated_plugins.cmake` 未注册 flutter_inappwebview）。
- `LaunchMode.externalApplication` 各端实际拉起外部浏览器的行为未实测；SnackBar 在自绘 `Appbar` 分支布局下的视觉位置未实测。

### 跨轮诊断模式

- **「双路径 / 双实现」是静默失效的高发结构**：本轮再次撞上 store 与 model 各有一对序列化出入口。识别方法：对每个序列化方法反查全仓调用点，**零调用点的是死代码、只被 test 调用的不能当生产路径**。
- **上游 PicaComic 原样移植的隐性 bug 已是第 N 例**：凡移植代码中出现 `Uri`/`WebUri` 成员取值（`.path`/`.host`），复核时应默认怀疑「要的是完整 URL 还是分量」。
- **「计划写的符号/文件不存在」与「计划里的数值结论」都要动手前证实**：本轮 05 计划正文用 `AiChatAttachment`/`ref` 等词，而 03 实际落地是 `attachmentPaths`（`List<String>` 相对路径），执行时按落地代码机械适配并记录实际名称。
- **成对函数要一起改**：`isToolAllowedByScope` ↔ `disallowedScopeResult`、`_serializeAiChatMessage` ↔ `deserializeAiChatMessage`、写侧 `_safeName` ↔ 读侧 `sanitizeFileName`（第十四轮）。这类「同规则两处实现」在本项目已出现三组，改动前先 grep 配套方。

### 模块职责演变

| 文件/符号 | 本轮之前 | 第十五轮 03~05 后 |
| --- | --- | --- |
| `LlmMessage` | 纯文本消息 | 「文本 + 图片引用」双出口模型：`toJson` 存档（平铺、相对路径）/ `toRequestJson` 请求（parts、base64 现场生成） |
| `AiConversationController.send()` | `Future<void>`，只吃文本与标签 | `Future<bool>` + `attachmentPaths` + `searchByImage` 参数；含 OCR 兜底分支与失败无副作用返回 |
| `_buildRequestMessages()` | 全量原样重放 history | 历史瘦身：只有最近一条带图 user 展开真图，更早降级稳定占位 |
| `AiConversationStore` | 会话 JSON 读写 | 新增「附件目录生命周期随会话」职责（delete / prune 同步清理） |
| `ai_settings_page` | 每个设置项一个 settings 槽位 | 出现第一个「JSON 子字段编辑」模式（`_OcrTextField` + `_readOcrConfig`/`_updateOcrConfig` 整读整写 144） |
| `AiToolExecutionContext` | 纯 operationId | 可携带能力回调的注入点（`resolveAttachmentPath`） |
| `ai_conversation.dart` | 会话与工具循环 | 新增「附件 ref 白名单解析者」职责 |
| 自动展示通道 | `display_result_list` 专属 | 白名单制（`shouldAutoDisplayToolResult`） |
| `webview.dart` | extension + 两个类 | 首次出现文件级顶层纯函数（`webviewFullUrl`）；`_AppWebviewState` 获得页面局部 `_showMessage` |
| `lib/network/soutubot_network/` | 不存在 | 新增三文件分层（签名纯函数 / 模型映射 / 网络流程），网络层只管「字节进 → Res 出 + CF 异常上抛」 |
| `passCloudflare` | 全库无投产调用点 | `search_by_image_tool.dart` 是首个投产调用点 |

---

## §97 主线第十五轮续：流式改造、思考过程、模型列表、余额与搜图缺陷修复（06～07号计划）

> 精简版对应：本会话计划与执行索引 06 / 07 号；完整版对应 §122。

### 架构决策与理由

**流式与否唯一由「是否传了 delta 回调」决定（决策A）**。否决「`chat()` 改返回 `Stream`」：`AiChatRequestForTesting` typedef 与 5 处 `const LlmResponse(...)` 测试构造会全部作废。采用「新增两个可选回调参数 + 签名仍是 `Future<LlmResponse>`」后，不传回调 = 完全旧行为，现有测试**零改动**；生产 `_runLoop` 恒传回调 → 恒流式，测试注入 `_chatRequestForTesting` 走 controller 层不触及 `chat()`。「流式与否」与「谁在调用」天然对齐，无多余配置面。

**「开启思考」开 = 不发任何 thinking 字段**。DeepSeek 文档明确默认 enabled，「开 = 不发」与「开 = 发 enabled」对它语义等价；而 OpenAI 官方对未知顶层字段报 400，默认态多发一个字段会把所有非 DeepSeek 用户的每次请求打崩。关侧才发 `thinking:{"type":"disabled"}`，是用户显式动作且开关副标题有警示。少发字段对前缀缓存与中转兼容永远是更安全的一侧。

**思考内容用 `AiChatMessage.reasoningText` 字段而非新增 `AiChatMessageType` 枚举值（决策C）**。新增 enum 值会让每条思考多占一个 `_displayItems` 下标，索引面板、迷你索引条、滚动位置记忆（裸下标）三处语义全变；作为字段三处零影响。

**「存 / 发」分离（决策B）**：存档与 `_history` 永远保留 reasoning 全文，`_buildRequestMessages()` 是唯一剥离点。规则「assistant 带 `toolCalls` 保留、不带则剥离」恰好等价于 DeepSeek 官方「无工具轮不拼回、有工具轮保留」，且无需计算轮边界、同输入同输出（不破坏前缀缓存指纹）。

**流式进行中气泡用「末元素整体替换新实例」+ notify 节流 80ms（决策D）**。`AiChatMessage.text` 是 final，保持 immutable 风格；80ms ≈ 12.5fps 文本刷新，肉眼连续且远离每 token setState 的掉帧线。缓冲区照写、通知按窗口发，**收口必 flush**。`_buildDisplayItems` 判断为不加缓存：O(n) 纯内存遍历，节流后每秒 ≤12.5 次，长会话 500 条仍在亚毫秒级。

**「思考完自动折叠」用自动值推导而非显式翻转状态**：`_expanded` 的自动值 = `streaming && !hasContent`，正文首字到达那一帧 `hasContent` 翻 true → 自动折叠，只发生一次、发生在正文最短的时刻。用户点击后 `_userChoice` 接管，自动逻辑永不覆盖用户选择。被否决方案「折叠后主动补一次贴底修正」：seal/notify 路径已必然触发跟随，重复修正会与 pending 定位权打架。

**「点击折叠 + 长按自由选择」共存 = `SelectableText.onTap`**。它只在「无拖动的单击抬起」时触发，长按/选择柄/鼠标拖选走内部 selection 手势，天然互斥。被否决：`SelectionArea + MarkdownBody + 外层 GestureDetector`——SelectionArea 内部也注册 tap/长按 recognizer，外层在手势竞技场的胜负跨平台不可控。

**`_ReasoningSection` 提为公开类 `AiReasoningSection`**（计划授权的二选一）。私有 widget 无法从 `test/` 直接泵，经 `_MessageBubble`/整页泵需要构造 controller + 真实 settings + 滚动列表，会把「折叠状态机」测试变成脆弱的页面级测试。同文件已有 `AiToolResultCard` 公开先例。

**模型列表缓存放内存不落盘**。模型列表是时效数据（`deepseek-chat`/`deepseek-reasoner` 2026-07-24 刚停用即是例证），落盘反而制造陈旧下拉，且省一个 settings 索引。缓存 key = `端点|apiKey`，改 URL/Key/模板天然失效；value 持有 Future 本身实现 in-flight 去重（反复聚焦、连点箭头只发一次）。

**余额查询按 provider 分派 + 明确不支持者连请求都不发**。OpenAI 官方 dashboard 端点 2023 年起只认浏览器 session key，`sk-` key 一律 401；智谱只认 Cookie；百炼需 AccessKey 签名——三者进短路名单。「不支持」时 UI 整个不显示（`SizedBox.shrink()`）而非显示「不支持」字样：绝大多数用户只用一个 provider，常驻一行灰字是噪音。

**抽屉余额请求放 `initState`**。Scaffold 的 drawer 子树只在打开时 inflate、关闭即 dispose，项目已依赖此特性实现「每次打开自动刷新会话列表」，照抄同一模式即天然满足「打开抽屉时自动拉取」，**不需要** `Scaffold.onDrawerChanged`。

**07 的 `CancelToken` 只挂 dio POST 请求**。第十四轮计划10 实测的「Bad state: Cannot add event after closing」竞态是 `receiveTimeout` 定时器与取消的竞争；本次只在用户主动点按钮时 `cancel()`，与超时无交集。`await for` 循环内再加 `isCancelled` 检查是双重保险（POST 已返回 body 时 token 取消无法中断 stream，需要 break）。

**取消折叠为「正常空响应」而非错误**。`DioExceptionType.cancel` → `LlmResponse()`（全 null），`hasError` 为 false，走空响应路径 `_handleFinalTextResponse(null)`，`_sealStreamingMessage` 定格或移除半截气泡，`_finishActiveTurn` 收口，`isLoading` 归零，聊天流干净、无残影、无红色错误。

**`_llmCancelToken ??= CancelToken()` 而非每子轮新建**。工具子轮复用同一 token，取消后所有后续子轮立即 cancel，语义是「停止整轮」。

**错误只留气泡不留 banner（07 Bug 4）**：原则是「有 `displayMessages.add(AiChatMessage.error(...))` 的地方就删掉配套的 `error = message`」，只保留没有气泡、只能靠 banner 传达的前置拦截（`pendingDownload`）。

### 接口与行为契约

- **`LlmClient.chat(..., onReasoningDelta:, onContentDelta:, cancelToken:)`** 三个参数全部可选，不传即与旧行为完全一致。**流式与否唯一由前两个是否为 null 决定**，无独立开关；不得给 `chat()` 加「默认传回调」的包装（等于强制所有调用点流式）。
- **`SseChatParser` 解析铁律**：`data: [DONE]` 是字面量不能喂 `jsonDecode`；`:` 开头是 SSE 注释/keep-alive；`choices` 可能是空数组（`include_usage` 的独立 usage 块，真实崩溃高发点）；`tool_calls` 的 `arguments` 是跨 chunk 增量拼接的 JSON 片段，**只在 `finish()` 整体 decode**；`name` 用追加而非只取首片；`finish_reason` 不写死枚举（DeepSeek 特有 `insufficient_system_resource`）；同一 delta 可能同时带 content 与 tool_calls，分支不互斥。
- **reasoning 响应字段**：`reasoning_content` 主（DeepSeek/Qwen/GLM/Kimi）+ `reasoning` 兜底（OpenRouter/Groq 形态）。
- **「存 / 发」分离**：`_buildRequestMessages()` 是 reasoning 唯一剥离点；`LlmMessage.toJson`/`toRequestJson` 只做「对象上有就序列化」。`stripImagesToPlaceholder()` 必须把 `reasoningContent` 传进副本（否则带图轮瘦身会顺带丢思考）；`stripReasoning()` 在本来就没有 reasoning 时返回自身。
- **`_sealStreamingMessage({finalText, finalReasoning}) → bool`**：返回是否保留了气泡。文本与思考皆空白时**移除**该气泡（避免灰色空块）；**必须 `notifyListeners()`**（80ms 节流窗口内攒着的最后一段 delta 靠它刷出）。任何新增的收口路径都必须走这个方法，不得直接改 `displayMessages`。
- **流式中断语义**：`response.hasError` 时已流出的半截文本/思考**只定格展示（display-only），不入 `_history`**，下一轮请求不携带半截内容。
- **`AiConversationController.stopGeneration()`** 幂等，未在进行中时无副作用。停止按钮仅在 `isLoading && !_sendInFlight` 时显示（`_sendInFlight` 期间是本地压缩 IO，不值得中断）。
- **`DioExceptionType.cancel` 必须在 `on DioException` 分支内最先判断**，才能折叠为正常收口而非错误。
- **余额三态语义**：`ok` = 金额；`error` = 红字（仅 401/403 与网络/5xx）；`unsupported` = 静默不显示（不占位、不报错）。**404/405 与「200 但非 JSON」归 `unsupported`**（这家没这接口 / 反代挡了，不是用户配置错）；401 但响应体含 `session key` 字样也归 `unsupported`（接口不支持该 key 类型，非 key 错）。
- **DeepSeek 余额端点无 `/v1`**（`{origin}/user/balance`），而模型列表 `/models` 与 `/v1/models` 等价。`normalizeOrigin` 剥掉 `/v1` 与 `/chat/completions` 后再拼。
- **余额金额字段类型各家不一**：DeepSeek/SiliconFlow 是字符串 `"110.00"`，Kimi 是数字 `49.58894`；统一 `num.tryParse(v.toString())`。中转站 `total_usage` **单位是美分**（10 美元额度用 250 美分 → 余额 7.50 美元）。
- **`modelsEndpoint(baseUrl)`**：剥尾部 `/chat/completions`、去尾斜杠；已以 `/models` 结尾原样用，否则拼 `/models`。`parseModels` 只取 `data[].id`，保序去重，不假定任何其他字段（DeepSeek 没有 OpenAI 的 `created`）。
- **`searchByImageRequested` 声明必须在 `parsed` 之后、任何引用之前**（当前在 OCR 检查前），不得上移到 `parsed` 之前。
- **搜图路径的图只流向工具层**：OCR 检查与 `imagePaths` 注入两处豁免是**独立的**，必须同时加 `&& !searchByImageRequested`；搜图时桥接语一律用 `aiPromptTagOnlyUserBridge`，不用图片桥接语。

### 根因推理链

- **加字段落盘静默丢失**：验收要求「存档 JSON 含 `reasoningText`」→ 查 `_save()` 调用链 → `AiConversationStore.save` → `displayMessages.map(_serializeAiChatMessage)` **而非 `msg.toJson()`** → 该方法逐字段手工挑选，未知字段静默丢弃。结论：只改 `AiChatMessage.toJson` 的话，内存/单测全对但落盘必丢，**且不报任何错**。反序列化侧同理。这是 `ai_conversation_store.dart` 第二次踩同一坑（第一次是 03 号计划的 `attachmentPaths`）。
- **视觉开关下 400 会持续**（07 Bug 2）：`imagePaths` 已写入 `_history` 里的 user 消息，`_buildRequestMessages` 的 `lastImageIndex` 每轮扫到那条并展开 `image_url` block；只要 `_history` 里有带图消息且视觉开，**后续每一轮都中招**，不是单次失败。修 `imagePaths` 注入豁免后搜图消息的 `imagePaths` 永远是空列表，从根上解决。
- **发送按钮永久灰 + 错误重复**（07 Bug 1/4）：OCR 拦截条件 `attachmentPaths.isNotEmpty && !visionEnabled` 不区分图片用途，搜图路径被误拦；错误同时走气泡与 banner 两条路，且 `error` 不清零 → banner 在后续每条消息都可见。
- **`referenced_before_declaration`**：计划假定「直接引用 `searchByImageRequested` 局部变量」，但它原在 `:852` 才声明，而 OCR 检查块在 `:789`，两者隔着整个 OCR 流程。教训：改「引用既有局部变量」的条件前，先 grep 声明位置再看引用位置。

### 技术约束/铁律

- **流式四坑的落点**（来源：项目内实测出处，强度=铁律）：① `receiveTimeout: 120s`（对抗 `MyLogInterceptor` 把 null 补成 30s，`app_dio.dart:32`）；② `extra:{'noRetry': true}`（对抗 `RetryHttpClientAdapter` 重发整个 POST，`app_dio.dart:176-206`）；③ **不把 CancelToken 混入 `stream.timeout` 的 `onTimeout` 路径**，流内静默用 `stream.timeout` 兜底；④ `validateStatus: (_) => true` + `_drainErrorBody`（否则 4xx 的 body 是 `ResponseBody` 对象，取不到 provider 报错文本）。**改动流式请求 Options 前先读这四条。**
- **`utf8.decoder.bind(stream).transform(LineSplitter())` 是唯一正确的 SSE 分行方式**：绝不按「一次 onData = 一个事件」解析（多字节字符跨包被拆 + 事件跨包残片）。
- **`tool_calls` 的 `arguments` 只在 `finish()` 整体 decode**：中途残片喂 `jsonDecode` 必炸。
- **严禁在流式期间随 delta 反复自动切换思考块折叠状态**（性能护栏）：折叠只发生在 content 首字那一帧，此时正文最短，高度突变被贴底跟随的 200dp 容差吸收。
- **`llm_client.dart` 含 `\0` 字节**（本轮再次实测确认 Grep 判为 binary）：定位一律 Read 或 `grep -a`。
- **part 文件不能用 `foundation.dart` 的符号**（`listEquals` 等）：`ai_settings_page.dart` 是 `settings_page.dart` 的 part，宿主只 import `material.dart`（不 re-export `foundation.dart`），编译期才报错。
- **settings 新索引取数组实际末尾 +1**（本轮 146/147），`readSettings` 对新增索引天然向后兼容，无迁移代码。

### ❌ 不要再走的路

- **不要只改 `AiChatMessage.toJson/fromJson` 就以为字段落盘了**（第二次踩）：必须同时改 store 的 `_serializeAiChatMessage`/`deserializeAiChatMessage`，四处一起。**后续任何给 `AiChatMessage` 加字段的计划，「执行范围」里默认列入 `ai_conversation_store.dart`。**
- **不要对目录跑 `dart format`**（`dart format lib/foundation/ai/`）：该目录存量文件有未格式化残留，一次调用就越界改了 5 个计划外文件。只 format 本次实际改动的文件列表。
- **不要在 part 文件里用 `listEquals`**：改为手写等价比较（4 行），而不是给宿主加 `foundation.dart` import——那会给 10+ 个 part 一并引入一大批符号，是跨文件语义变更。
- **不要对含无限动画的 widget 用 `pumpAndSettle`**：思考块 streaming 态有 `CircularProgressIndicator`，必超时。用 `pump(Duration)`。
- **不要把 `CancelToken` 混进 `receiveTimeout`/`stream.timeout` 的 onTimeout 回调**（竞态，见铁律③）。
- **不要每个工具子轮新建 `CancelToken`**：那会让「停止」只停当前子轮，下一子轮继续跑。
- **不要给 `LlmClient.chat` 加「默认传回调」的包装**：等于让所有调用点强制流式。
- **不要在气泡与 banner 双通道同时报同一个错**：有 `AiChatMessage.error(...)` 气泡的路径就不要再 `error = message`。
- **铁律禁 git 写操作时，还原误改文件用 `git show HEAD:<path> > <path>`**（只读命令 + shell 重定向），不要用 `git checkout --` / `git restore`。

### 残余风险/未验证假设

- **DeepSeek 流式 `tool_calls` 分片的原始形态属推断未验证**（文档只声明 OpenAI 兼容、未给流式工具分片样例）。解析器按最宽容语义实现；若实际形态偏差，表现为工具参数解析成空 map / 工具执行报参数缺失。验证点 = 真机工具链路端到端。
- **余额接口的端点与字段名全部来自文档调研，本轮未发真请求**：Kimi `data.available_balance`、SiliconFlow `data.totalBalance`、中转站 `hard_limit_usd`/`total_usage`。字段名若变则**静默 unsupported**（不报错，只是不显示）。
- **中转站两连击的统计区间处理各分支不一致**：部分实现忽略区间返回累计值 → 余额偏大或偏小，不报错。本轮实现为「先试 `/dashboard/...`，404/405 再试 `/v1/dashboard/...`」。
- **`_refreshOptionsView` 的 composing 微扰若与中文 IME 组字冲突**：表现为组字被打断。备选 `_ctrl.notifyListeners()`（需 `// ignore: invalid_use_of_protected_member`）。未真机验证 IME 场景。
- **个别中转站 / 旧网关不支持 SSE 或 `include_usage`**：全量流式无回退开关（用户拍板），表现为报错或无 usage 日志。
- **`_streamingMessageIndex` 是裸下标**：只在同一轮内有效，依赖「流式期间没有别的代码往 `displayMessages` 中间插 / 删元素」。当前 `_flushPendingDisplayItems` 只在 seal 之后调用，成立；未来若加「流式期间插工具卡」需改成实例引用查找。
- **notify 节流 80ms 下 `_buildDisplayItems` 未加缓存**：真机长会话（数百条）若掉帧，热点在此，解法是照 `_indexEntriesVersionKey` 加版本键缓存，另开计划。
- **`thinking` 请求字段无自动化断言**：`chat()` 没有可注入的请求出口钩子（`_chatRequestForTesting` 在 controller 层而非 dio 层），只能抓包验收。
- **`_llmCancelToken ??= CancelToken()` 在并发 `_runLoop` 下的正确性未验**：当前 `isLoading` 保证同一会话不并发进 `_runLoop`，实践中不触发。
- 真机联调待用户复核共 7+ 条：真实 `/models` 拉取与缓存节流、流式逐字与贴底跟随观感、杀 App 重开看旧思考、抓包确认 thinking 字段、工具链路流式端到端、中途断网定格、真实余额数字与红字、搜图三 Bug 端对端、停止按钮行为。

### 跨轮诊断模式

- **「双路径 / 双实现」仍是本项目静默失效的第一高发结构**：本轮再次撞上 store 与 model 各有一对序列化出入口。已累计三组成对函数：`_serializeAiChatMessage` ↔ `deserializeAiChatMessage`、`isToolAllowedByScope` ↔ `disallowedScopeResult`、写侧 `_safeName` ↔ 读侧 `sanitizeFileName`。改动前先 grep 配套方。
- **「持续性错误」优先怀疑状态被写进了历史**：07 Bug 2 的「此后每条都 400」不是重试问题，是坏数据进了 `_history` 每轮重放。凡「第一次失败后一直失败、新开会话恢复」的症状，先查 `_history` 里存了什么。
- **计划里的「直接引用某个局部变量」要先证实声明位置**：本轮 `referenced_before_declaration` 就是没核实声明点在引用点之前。

### 模块职责演变

| 文件/符号 | 本轮之前 | 第十五轮 06~07 后 |
| --- | --- | --- |
| `LlmClient` | 纯请求-响应 | 请求-响应 + SSE 流式管道；**对外签名仍是 `Future<LlmResponse>`**，流式是内部实现细节 |
| `LlmClient.chat()` | 固定非流式 | 三个可选参数（两个 delta 回调 + `cancelToken`）；「传回调 = 走流式」 |
| `LlmMessage` | 文本 + 图片引用双出口 | 再加 `reasoningContent` 与 `stripReasoning()`（发送侧剥离用） |
| `AiChatMessage` | 文本 + 附件 + 标签快照 | 再加 `reasoningText`（持久化，渲染由设置 147 控制） |
| `AiConversationController` | 会话与工具循环 | 新增「流式进行中气泡」所有权（`_streamingMessageIndex` + `streamingMessage` getter + 节流 notify）与「取消入口」（`_llmCancelToken` + `stopGeneration()`）；UI 层不持有任何流式状态 |
| `_buildRequestMessages()` | 历史瘦身（图片占位） | 再加 reasoning 剥离规则（唯一剥离点） |
| `lib/foundation/ai/` | 只有对话相关客户端 | 新增两个「provider 元信息客户端」：`model_list_client.dart`（模型列表）、`balance_client.dart`（余额），与 `LlmClient` 平级、互不依赖、都只读 settings |
| `sse_chat_parser.dart` | 不存在 | 纯状态机零 IO 的 SSE 解析器（含 tool_calls 分片重组），全部可单测 |
| `_ModelIdField` | 静态预置下拉 + 装饰性箭头 | 聚焦 / 点箭头双触发远程拉取，loading/错误态，拉取成功替换预置；箭头升级为 `IconButton` |
| 抽屉「新建会话」行 | 纯入口 | 兼余额显示位（`trailing` FutureBuilder，字号 = bodyLarge×2/3） |

## §98 主线第十五轮续：搜图结果阈值与全量展示、长期状态卡片「已了解」（08～09号计划）

> 精简版对应：本会话计划与执行索引 08 / 09 号；完整版对应 §123。

### 架构决策与理由

**把过滤/消息/data 构造整段抽成 `@visibleForTesting buildResult(result, {includeAll})`，而非在 `executeWithContext` 内原地改**。`SoutubotNetwork()` 是硬编码单例、无构造注入与替换点；逻辑留在 `executeWithContext` 里，则任何用例都得先过一次真实网络调用，于是阈值边界、fallback 触发、`hidden_count` 归零这些**纯算术分支一条都进不了单测**，只能靠真机。抽出后 `executeWithContext` 只剩一行转调，网络与 CF 过盾分支原样不动。被否决方案 A：给 `SoutubotNetwork` 加依赖注入/接口抽象——可测性最彻底，但牵动网络层与 CF 拦截，远超「工具层自包含」的范围，留待真要覆盖网络分支时另立计划。被否决方案 B：只靠真机验收不写单测——恰好 15 的边界、fallback 条件、`hidden_count` 归零都是纯分支，真机验收成本高且不可重复，等于放弃回归保护。

**「结果为零」时 fallback 展示全部而不是报「未找到」**。<15% 的条目质量极低，但用户真实诉求是「有线索总比没有强」；风险由 message 侧兜住——fallback 分支强制要求模型声明「可靠性极低」，且 `low_confidence`（<45）警告仍照常生效。

**`hidden_count` 的语义定义为「当前还没展示给模型的条数」而非「隐藏池大小」**。两者只在 `includeAll` / fallback 下不同：那时全部条目都已给出，隐藏池非空但「还没看的」是 0。按后者定义会产出「另有 0 条已隐藏」这种自相矛盾的提示语，按前者定义则天然不可能。

**message 四路分支合并为两路（`useFallback` / `lowConfidence`）**。计划原文里 `includeAll && hiddenItems.isNotEmpty` 分支与 `lowConfidence` 分支文案逐字相同；同一句话在两处维护，日后必然走偏。合并后行为逐案等价（includeAll+置信正常 → null；includeAll+低置信 → 同一句警告），各有一条用例锁定。

**「已了解」开关放 `appdata.settings` 全局索引，而非会话级状态**。这是用户一次性认知，跨会话跨进程都该生效；settings 已有落盘 + SharedPreferences 双写，零新增基础设施。被否决：存进会话对象或内存变量——重开 App 会复显，不满足「杀进程重开仍隐藏」。

**门卫加在渲染条件上，而不是删掉卡片代码**。长期状态机制与卡片内三个按钮逻辑完全保留，影响面最小；日后要加「恢复显示」只需把 148 写回 `'0'`。

**「已了解」回调抽成 State 方法 `_dismissPersistentStateCard()` 而非计划原文的内联匿名函数**：可用 State 的 `context` 而非 `overlayContext`，并统一做 `mounted` 检查，与同文件 `_clearPersistentSources` / `_clearPersistentLocalOnly` 风格一致。标题 `Text` 外补 `Expanded`（计划没写）防窄面板下与右侧按钮争宽溢出。

### 接口与行为契约

- **`SearchByImageTool.parametersSchema`**：`properties` = `{image_ref, include_all_results}`，`required` = `['image_ref']`。`include_all_results` 可选 boolean，实现按 `args['include_all_results'] == true` 判定——**非 true 的任何值（含字符串 `"true"`）都走默认路径**。
- **success data**：非空结果 `{items, top_similarity, low_confidence, search_id, hidden_count}`；空结果 `{items: [], search_id, hidden_count: 0}`（无 `top_similarity` / `low_confidence` 键）。
- **展示阈值边界含等号**：`similarity >= 15` 计入展示。
- **`top_similarity` 取 `displayItems` 的最大值**，不假定站点返回按相似度降序（已有用例锁定）。
- **fallback 条件严格为 `kept.isEmpty && hiddenItems.isNotEmpty`**：`allItems` 为空时不触发，走空结果早退分支。
- **`_lowConfidenceBelow`(45) 与 `_hideBelowSimilarity`(15) 是两条独立的线**：前者控警告、后者控展示；全量模式下警告仍生效。改其中一个不要顺手动另一个。
- **`buildResult` 是 `@visibleForTesting`**：仅供测试直接调用，生产路径只经 `executeWithContext`。
- **`settings[148]`（`aiPersistentCardDismissedSettingIndex`）**：`'0'` 显示「当前会话长期状态」卡片（默认），`'1'` 永久隐藏。**纯 UI 开关**，不影响长期标签/长期来源/仅本地三项状态的实际生效，也不影响其清除接口的存在。
- **卡片渲染契约**：`!cardDismissed && (persistentTagNames.isNotEmpty || persistentSources.isNotEmpty || persistentLocalOnly)`。卡片专属 `SizedBox(height: 10)` 在同一 collection-if 块内，条件为假时连间距一并不渲染；其下的 `SizedBox(height: 12)` 属「来源」小节，与卡片无关。
- **提示词面板刷新契约**：面板由 `OverlayEntry` 承载，任何在面板内改变面板显示的操作**必须** `_promptPanelEntry?.markNeedsBuild()`；仅 `setState` 只重建页面树，不重建 overlay 子树。
- **settings 新增索引契约**：只需 `lib/base.dart` 默认数组末尾按序追加 + 定义常量，`readSettings` 对旧短数组按 `st.length` 回填、缺项留默认并触发回写，无迁移代码。

### 根因推理链

- **可测性缺口**：「计划要求覆盖阈值/fallback/hidden_count 用例」→ 用例必须能构造 `SoutubotSearchResult` 并观察 `AiToolResult` → 逻辑留在 `executeWithContext` 内就必须先过网络 → 网络来自 `SoutubotNetwork()` 硬编码单例 → 单例无注入口，测试环境无法替换或打桩 → 结论：**在函数边界上把「网络」与「网络之后的纯逻辑」切开**，是不动网络层前提下唯一能让验收项落地的做法。
- **「点了『已了解』卡片不消失」（潜在缺陷，改前拦住）**：卡片在 `_buildPromptPanel` 内 → 该方法返回值被 `_showPromptPanel` 里 `Overlay.of(context).insert(entry)` 的 `OverlayEntry` 持有 → OverlayEntry 是独立于当前 Widget 子树的挂载点，其 builder 只在 `markNeedsBuild()` 时重跑 → 页面 `setState` 无法触发面板重建 → 修正为先 `markNeedsBuild()` 再 `setState`。旁证：同文件 `_clearPersistentLocalOnly` 正是这一写法，说明该结论已被项目历史验证。

### 技术约束/铁律

- **settings 索引禁止跳号**，追加前必须 Read 数组末尾确认（来源：计划风险项，强度=强约束）。本轮确认 147 为末项后追加 148。
- **提示一律走 SnackBar，禁 `showToast`**（来源：`lib/components/message.dart:5` 是 no-op 空实现 + 同文件既有注释先例，强度=项目惯例）。
- **计划里的行号与宿主方法名仅供参考，一律以符号名/界面文案定位并 Read 确认后再改**（来源：本轮实测救回一次定位偏差，强度=铁律）。
- **`@visibleForTesting` + `package:flutter/foundation.dart` 是本项目既有模式**（`model_list_client.dart` 有先例），抽测试入口时沿用而非新造。
- **绝对不执行 git 写操作，只读查看；提交由用户本人处理**（来源：本项目附加铁律）。

### ❌ 不要再走的路

- **不要把阈值数字硬编码进提示文案**。计划原文写死「<15%」，实现改为插值 `<$_hideBelowSimilarity%`；否则下次调阈值时文案与常量必然脱节，而这类「文案-常量漂移」发生在提示词里，测试基本发现不了。
- **不要把工具参数名写得像工具名**。计划文案「若用户需要可调用 `include_all_results=true`」会诱导模型把参数当工具去调；改为「可**传** `include_all_results=true` 重新调用」。
- **不要照抄计划里语义重复的分支**。四路 if/else 中两支文案完全相同就是信号，应合并而非复制。
- **不要照抄计划里逻辑互斥的用例描述**。计划把「`hidden_count==1`」与「走 fallback」写在同一条用例里，二者不可能同时成立（只有一条 10% 条目时必然 fallback、`hidden_count` 必为 0）；应先按真实语义拆成两条再落地。
- **不要相信计划写的宿主方法名**。09 号写卡片在 `_buildPendingTagRow`，实际在 `_buildPromptPanel`；改 UI 一律 grep 界面可见文案（此处「当前会话长期状态」）定位。
- **不要在 Overlay 承载的面板里只写 `setState` 就以为 UI 会更新**（见根因推理链）。
- **不要用 `showToast`**：调用编译通过但运行时无任何提示。
- **不要在 `await appdata.updateSettings()` 之后再用旧 context**：本轮 `updateSettings` 未 await，且 `setState` 前先判 `mounted`。

### 残余风险/未验证假设

- **08 真机 4 条未验**：15-29% 结果的实际可见性与命中质量、无匹配时 fallback 表现、对话「显示全部结果」能否稳定触发模型二次调用、AI 总结是否真会提到隐藏条数。后两项依赖模型对新 prompt/schema 的遵循度，属未验证假设。
- **soutubot 网络失败与 CF 过盾分支仍无单测**：本轮只抽出纯逻辑段，未做网络层可测化。
- **旧会话拿不到规则 11 新文案**（system prompt 只对新会话生效），只能靠 schema description 自行发现 `include_all_results`，实际发现率未验。
- **假设 soutubot 单次返回条目不多**，`include_all_results=true` 全量展示不致 token 暴增；未用真实大结果集验证，若未来出现几十条需考虑分页。
- **阈值 15 仅有用户主观判断**，无实测数据支撑 15-29% 区间的命中质量。
- **09 真机 3 条未验**：点击后卡片即时消失 + SnackBar、重进 AI 页不复显、杀进程重开仍隐藏。依据是 settings 双写落盘 + `markNeedsBuild` 机制，本轮无设备。
- **「已了解」是单向操作，无回退入口**：点过后清除长期提示 / 恢复全部来源按钮随卡片一并从 UI 消失（功能未删）。计划已知并接受；若用户反馈不便，需另开计划在设置 → AI 页加重置开关（写回 `settings[148]='0'`）。
- **SnackBar 依赖上层 `Scaffold`**：用的是 State 的 context，理论上能找到页面 Scaffold；若面板在某些路由场景下先于 Scaffold 释放，提示可能不显示（不影响隐藏功能本身）。
- **08/09 共用一次全量门禁**：两计划在同一工作区并行改动，analyze/test 是在合并状态下跑绿的，未分别隔离做独立门禁。

### 跨轮诊断模式

- **「计划写的实现位置不可测 → 在等价前提下抽纯函数」再次出现**，与本轮前序计划多次遇到的「硬编码单例阻塞单测」同源。可沉淀为写计划阶段的检查项：**若要求某段逻辑有单测，先确认它是否与硬编码单例（`XxxNetwork()`）同处一个函数；若是，直接在计划里写明「抽 `@visibleForTesting` 纯函数」**，免得执行阶段再做偏差说明。
- **「计划宿主方法名与真实位置不符」在第十五轮内已第二次出现**。复用模式：改 UI 前先 grep 界面可见文案而非方法名；同时确认目标 UI 的挂载方式（普通子树 / OverlayEntry / Dialog），挂载方式直接决定刷新写法。参考实现优先照抄同文件同类既有函数（本轮照搬 `_clearPersistentLocalOnly`），比按计划伪代码写更可靠。

### 模块职责演变

| 文件/符号 | 本轮之前 | 第十五轮 08~09 后 |
| --- | --- | --- |
| `SearchByImageTool` | 单一 `executeWithContext` 承担取图 → 网络 → 过滤 → 组消息 | 两层职责：`executeWithContext` 只管参数解析、图片解析、网络调用与错误分支；`buildResult` 负责纯粹的「结果集 → 展示集 + 提示语 + data」映射，无 IO、可直接单测。对外行为与调用方（工具注册表、自动出卡白名单）零变化 |
| `search_by_image` 工具契约 | 单一展示策略（<30 一律丢弃） | 三态展示策略（默认 ≥15 / fallback 全量 / `include_all_results` 全量）+ `hidden_count` 回报未展示条数 |
| `lib/foundation/ai/ai_settings.dart` | 只有「AI 功能行为开关」 | 扩展出第一个「AI 界面提示可见性开关」（148）。后续同类「一次性说明卡片 / 引导已读」标记建议继续集中在此文件，并在注释里显式标注「纯 UI，不影响机制」以免被误当功能开关 |
| `_buildPromptPanel` 长期状态卡片 | 有长期状态即渲染 | 加一道 settings[148] 门卫；标题行兼「已了解」入口 |
| 发送按钮区域 | 只有发送 | `isLoading && !_sendInFlight` 时切换为停止按钮 |

## §99 passCloudflare 双端过盾判据修复：移动端闪退与桌面端 2 秒闪关（第十五轮 08/10 号修复）

> 精简版对应：本会话计划与执行索引 08-修复 / 10-修复；完整版对应 §124。08 修移动端（`App.isMobile` 分支），10 修桌面端（`App.isDesktop` 分支），两端同源缺陷：**「凭据存在即视为过盾」判据 + 持久化 cookie 残留**。

### 症状速查表

| 症状 | 定位线索 |
|---|---|
| 移动端（Android/iOS）搜图弹 CF 验证窗口，**立即自动关闭**（用户来不及做 Turnstile），搜图必失败 | `cloudflare.dart` `App.isMobile` 分支 `onTitleChange`；旧判据 `document.head.innerHTML.includes('#challenge-success-text')` 恒 false → 「每次标题变化都走关窗逻辑」 |
| 桌面端（Windows）弹验证窗口，**约 2 秒后自动关闭**，同样必失败 | `DesktopWebview.onTitleChange` **不是事件，是 2 秒无条件轮询**（`webview.dart:346-371` `Timer.periodic(2s)`）→ 第一个 tick 就执行关窗逻辑 |
| 过盾后工具重试仍 403 | 旧（已失效）`cf_clearance` 被 `saveCookies` 写入 App cookie jar，重试时 `CloudflareInterceptor.onRequest` 携带旧值 + 旧 UA |

### 根因推理链

`flutter_inappwebview` 的 `InAppWebView` 使用系统 WebView / WebView2，**cookies 跨 App 会话持久化**（Windows 侧 `webview.dart:387` `userDataFolderWindows: '${App.dataPath}\webview'` 指向 `getApplicationSupportDirectory()` 持久目录，且全库无 `WebviewWindow.clearAll()` 调用点）→ 上次过盾留下的 `cf_clearance` 即使服务端已失效（IP 变化 / UA 不匹配），本地仍长期可读（`CookieManager.instance().getCookies` / `GetCookies(nullptr)` 读的是**整个 profile**，与当前页面是否过盾无关）→ **任何「cookie 存在即视为过盾」的判据都必然误判**，无论前置条件是 JS 检测、标题判断还是 `res == 'false'` → 唯一可靠做法是引入时间维度：**比对「相对进入 webview 那一刻的快照是否发生变化」**。

原 JS 判据为何恒 false：`#challenge-success-text` 是 CF 动态注入 **body** 的，`<head>` 里从未出现过；且它是 CSS 选择器写法，作为 innerHTML 子串匹配本身就不成立。表达式恒 `false` → 移动端 `if (res == false)` 恒真、桌面端 `res == 'false'` 恒真（桌面端 `webview.dart:406-408` → `webview_impl.dart:266-275` → `windows/web_view.cc:396-419` 把 WebView2 `ExecuteScript` 的 `PCWSTR` 直通不加引号不解析，JS 布尔 false 到 Dart 侧就是 5 字符 `'false'`）。桌面端叠加 2 秒轮询，**必然**失败而非偶发。Linux 对照：`linux/webview_window.cc` 全文无 `webkit_cookie_manager_set_persistent_storage`（grep 零命中）→ WebKitGTK 默认内存态 → 本缺陷在 Linux 上不成立。

### 架构决策与理由

- **采纳：以「`cf_clearance` 相对基线快照发生变化」作为过盾判据**。唯一与「本次是否真的过了盾」强相关的信号，不依赖标题语言、平台标题回调行为、CF 页面 DOM 结构。
  - 移动端基线：`onStarted` 里拍 `baseClearance = getCookies(url)['cf_clearance']`（**不调 `saveCookies`**），`onTitleChange` 内 `clearance == null || clearance == baseClearance` 则 return。
  - 桌面端基线：**从 App cookie jar（`SingleInstanceCookieJar.loadForRequest`，同步方法）取值**，而非在 `onStarted` 拍 webview profile 快照——`onStarted` 方案与 2 秒轮询第一个 tick 存在时序竞态（快照未就绪时 tick 已在比较）；jar 方案在 `open()` 之前同步取值，零竞态。代价 = 跨存储不对称的理论漏挡（见残余风险②），用户已知情并接受。
- **保留 `res == 'false'` 写法不删**：追到原生已证实写法本身正确，坏的是 `#challenge-success-text` 这个**判据**而非字符串比较**写法**；删它属无收益的额外改动面，且若将来 CF 把该元素放进 `<head>` 它会重新成为有效门禁。
- **被否决**：标题判断（本地化文案 + `title = t ?? 'Webview'` 兜底 + Android 解析 `<title>` 前用 URL/host 当标题，三重污染，仅保留为「仍在质询中」的快速短路）；主动 `deleteCookie`（强制用户每次重新人工验证，把「回头客失败」换成「回头客必验证」）；JS 元素检测（本次事故根因本身，CF 页面结构不受控）。
- **`onFinished()` 双点调用 + 调用方幂等**（移动端，与计划 Fix 2 相反）：成功路径与「用户手动返回」降级路径各自需要一个完成信号，无法合并成单点；唯一调用方已有 `Completer` 幂等防护，允许重复调用比丢掉降级路径的即时性更划算。桌面端接 `onClose: () => onFinished()` 补齐手动关窗降级（否则空等 180 秒超时）。

### 接口与行为契约

- `AppWebview.onTitleChange` 签名 `void Function(String title, InAppWebViewController controller)?`，**title 非空**——`webview.dart:224` 把原生 null 兜成字符串 `'Webview'`。这是 `title.trim().isEmpty` 能直接编译的原因，也是标题判据不可靠的直接来源。
- `DesktopWebview.onTitleChange` **不是标题变化事件，而是 2 秒无条件轮询**（约束方向：写该回调必须假设反复调用、入参可能完全没变）。破坏后的静默失效：当「变化才触发」用 → 回调里的副作用在第一个 tick 无条件执行 → 「窗口开约 2 秒必然闪关」且无异常日志。
- `AppWebview.onStarted` 在 `onWebViewCreated` 内触发（`webview.dart:240`），**早于代理设置与 `loadUrl`**。`onStarted` 时目标页必然尚未加载，此刻读到的 cookies 只能是系统 WebView 持久化的旧值。
- `App.globalTo` 走 `Navigator.push<T>(AppPageRoute(...))`（`app.dart:195-199`），**返回的 Future 在路由 pop 之后才完成**，不是 push 完成就返回。
- `App.globalBack`（`app.dart:161-165`）只判 `Navigator.canPop`，**不校验将被弹出的路由身份**。重复调用会连带弹掉下层页面（本轮给成功分支加了 `closed` 可重入守卫）。
- `passCloudflare` 的 `onFinished` **可能被调用多次**（成功路径 + 手动关窗降级路径），调用方必须幂等。当前唯一调用方 `search_by_image_tool.dart` 用 `Completer` + `if (!completer.isCompleted)` 满足契约。
- `SingleInstanceCookieJar.loadForRequest(Uri)`（`cookie_jar.dart:88`）返回 **`List<Cookie>`，同步方法**，误加 `await` 会 analyze 报错；`static SingleInstanceCookieJar? instance`（`:188`）**可空**，`!` 断言必要——唯一赋值点 `main.dart:173` 在 App 启动初始化阶段，早于任何用户交互。`loadForRequest` 会顺手删过期 cookie（`:88-100`），但 `saveCookies` 构造 `io.Cookie` 不设 `expires`，jar 里的 `cf_clearance` 永不被该逻辑清理。
- `DesktopWebview.onClose`（`webview.dart:318`）存在且在 `controller.close()` 时同样触发（`webview.dart:395-399` 独立于 `_webview = null`），即成功路径也会走一遍。

### 技术约束/铁律

- **判断「某个凭据是否本次获得」必须用「相对基线快照的变化」，不能用「存在性」**——只要凭据存储是持久化的，存在性判断就一定误判（来源：本轮 Bug A 核心）。
- **依赖第三方页面 DOM / 文案的判据不得作为唯一决策依据**，只能作为优化短路（来源：`#challenge-success-text` 恒 false + CF 标题本地化）。
- **可能被多次触发的回调，其副作用（pop / 关窗 / complete）必须自带可重入守卫**（`closed` 布尔），不能依赖上游只调一次（来源：`webview.dart` 无守卫 + `globalBack` 不校验路由身份）。
- **对外暴露「可能多次调用」的回调时，必须在声明处注释说明，并确认所有调用方幂等**（来源：`onFinished` 双点调用）。
- **改动异步时序前，必须读到 Future 完成时机的实现源码，不能凭 API 名称推断**（来源：`App.globalTo` / `AppPageRoute`，计划 Bug B 整节就错在这）。
- **回调先确认是「事件」还是「轮询」**：事件语义的假设在两端往往不一致（移动端「可能触发」vs 桌面端「必然触发」）。
- **跨存储比较必须先确认两侧是不是同一个存储**：App cookie jar 与 WebView2 profile 是两个独立存储，`getCookies` 读全 profile（来源：审计揪出的不对称）。

### ❌ 不要再走的路

- **不要用页面标题判断 CF 是否过盾**：标题受本地化文案、平台回调时机、null 兜底值三重污染。
- **不要用 JS 元素存在性判断 CF 是否过盾**：判据失效时会**静默恒真/恒假**，不报错、不告警，只能靠用户反馈行为异常才发现。
- **不要假设 `Navigator.push` 返回的 Future 在 push 完成时就 resolve**：它在 pop 后才完成。
- **不要在 `onStarted` 里读 cookie 当作当前页状态**：那时 `loadUrl` 还没执行。
- **不要试图在 `webview.open()` 之前读 webview profile 里的 cookie**：`DesktopWebview.getCookies` 内部解引用 `_webview!`，原生实例在 `open()` 里才创建 → null-check 异常。桌面端基线只能取 App cookie jar。
- **不要只依据「计划写了什么」实施**：08 计划根因分析有一节（Bug B）前提错误、一节（Fix 1）修复不充分；落地前对每个论断做独立代码级复核。
- **不要「顺手」删 `res == 'false'` 写法或主动 `deleteCookie`**（计划已排除，见架构决策）。

### 残余风险/未验证假设

- ① **Windows 真机未验证**：CF 换发新 clearance 后 2 秒内能否被 `getCookies` 读到未验。若不成立 → 窗口不自动关闭，用户手动关窗由 `onClose` 兜底返回失败消息。
- ② **首次/异常场景基线漏挡（审计定级 info，理论存在但实际不可达）**：App jar 有 `cf_clearance` 而 profile 无残留（或相反）时 `baseClearance` 失真，判据退化为「有即关窗」。依据：触发 403 的请求走同一 jar（`soutubot_network.dart:43`），失效 clearance 必然在同一个 jar；旧代码每次误关窗都 `saveCookies(profile→jar)`；`saveCookies` 的 cookie 无 `expires` 永不被清理；全库无代码清空该单例 jar 的 soutubot 域。仅剩「`cookies.db` 单独丢失而 webview profile 保留」的人工场景。
- ③ **Linux 上本缺陷本不成立**：WebKitGTK 无持久化 cookie，改动对 Linux/macOS 是无害冗余，未实测。
- ④ **2 秒轮询 fire-and-forget 回调重叠的 null-check 竞态**：`closed` 守卫不在异常路径上（`getCookies` 的 `await` 在守卫之前），已在飞的 tick 在 `close()` 后恢复执行会解引用 `_webview!` 抛异常——Flutter 默认 zone 只打日志不崩溃。根治需在 `webview.dart` 的 `getCookies`/`evaluateJavascript` 加 `_webview == null` 早退（另立计划）。
- ⑤ **跨源 profile 污染**：eh / nhentai / soutubot 共享同一个 `${App.dataPath}\webview` profile，若用户在 eh/nhentai 登录窗被动拿到 soutubot 域 `cf_clearance`，可能造成基线与 profile 不一致（即场景②）。未实测发生概率。
- ⑥ **移动端 `onStarted` 触发率未真机验证**：若某次未触发，`baseClearance` 保持 null，判据退化为「有即关窗」（优雅降级，不崩溃）。
- ⑦ **`search_by_image_tool.dart:241-244` 过时注释**：接上 `onClose` 后「移动端 `App.globalTo` 返回即回调」措辞已不准确，该文件本轮禁改而留白，需另开小计划修正为「两端手动关窗均有完成信号（桌面 `onClose` / 移动 `globalTo` 返回），180s 超时仅兜住不支持分支」。
- ⑧ **08 桌面端同源缺陷修复前是已知遗留**（08 回写时桌面端未修），10 号已补齐；修一端必须立刻查另一端（见跨轮诊断模式）。

### 跨轮诊断模式

- **同一函数内的双平台分支，修完一端必须立刻查另一端是否同源**（08 修移动端后桌面端被记为「已知遗留」，用户追问才发现不仅同源、还因「事件其实是 2 秒轮询」而**更确定地失败**）。归纳规则：看到 `if (App.isDesktop) ... else if (App.isMobile) ...` 结构，任一分支的判据缺陷都要在另一分支重新验一遍。
- **回调语义先确认「事件 vs 轮询」**：事件语义假设在双端往往不一致。

## §100 AI 停止按钮双修：条件恒假与 OCR 阶段可见无效（第十五轮 11/12 号修复）

> 精简版对应：本会话计划与执行索引 11-修复 / 12-修复；完整版对应 §125。11 修「停止按钮永不显示」（`ai_chat_page.dart:2181` 条件恒假），12 修「OCR 阶段按钮可见但点击无效」（`ai_conversation.dart` 停止逻辑对 OCR 无感知）。

### 症状速查表

| 症状 | 定位线索 |
|---|---|
| LLM 流式输出/思考中，右下角**始终是禁用的灰色「发送」按钮，停止按钮永不出现** | `ai_chat_page.dart:2181` `if (_controller!.isLoading && !_sendInFlight)` —— 两标志 true 区间重叠，`!_sendInFlight` 恒 false |
| 单张/多张图片 OCR 期间点停止图标，**没反应、按钮不消失**，OCR 跑完后 LLM 请求照常发出 | `stopGeneration()` 只操作 `_llmCancelToken`（OCR 阶段为 null，`:1171` `??=` 懒初始化在 `_runLoop` 内）；`:816` OCR 分支提前 `isLoading = true` 让按钮出现，形成「显示条件与功能条件不同源」窗口 |

### 根因推理链

**11 号**：`_sendInFlight`（`:1656` try 之前置位，`:1738/1740` finally 复位）的 true 区间**包住** `isLoading`（`:952` 置位、`:1417/1469/1501/965` 复位）的区间——LLM 进行中两者恒 true → `isLoading && !_sendInFlight` 恒 false → 停止按钮永不出现。审计排查过「send() 是否 fire-and-forget」「finally 是否在 await 前跑」两种可能推翻根因的写法，均不成立。

**12 号**：停止按钮点击 → `stopGeneration()`（`:488`）只 `_llmCancelToken?.cancel() / = null` → OCR 阶段 `_llmCancelToken` 为 null（`:1171` `??=` 懒初始化在 `_runLoop` 内，OCR 在 `_runLoop` 之前）→ cancel 空操作 → OCR HTTP 请求跑完 → `:952 isLoading=true + _runLoop()` → `:1171` 重建 token，LLM 照常发出。`isLoading` 在 `:816` 提前置 true（OCR 分支）让按钮可见，但功能条件（token 非空）不满足 → 可见但无效。

### 架构决策与理由

- **11 号：去掉条件里的 `&& !_sendInFlight`（1 行）**。按钮显示是纯 UI 派生状态，`isLoading` 已是它唯一正确的真值来源；`_sendInFlight` 的职责是防重入而非驱动 UI 形态。被否决：改 `_sendInFlight` 的 try/finally 提前复位（摧毁 `:1647` 入口重入守卫和 `:2143/:2191` 禁用语义，把显示 bug 换成可二次发送的功能 bug）；在 `send()` 加 `finally` 统一复位 `isLoading`（越界重构）；为按钮区加独立监听（既有 `ChangeNotifier + setState` 链路完好，属无收益复杂化）。
- **12 号：采用 B 变体（轻量布尔标志 `_stopRequestedDuringOcr`），绕开 typedef 限制**。方案 A（复用 `_llmCancelToken` 提前初始化）不可行：`_ocrRequest` typedef 不接受 CancelToken，且 `stopGeneration()` cancel + 置 null 后 `_runLoop` 的 `??=` 会重建 token，修复不完整。B 原始（独立 `_ocrCancelToken`）同样受 typedef 限制。B 变体利用 OCR 循环串行 await 的天然检查点轮询标志，改动 14 行，不修改 OcrClient/typedef，analyze 0 issue。`stopGeneration()` 替换为区分 LLM/OCR 两路版本：`_llmCancelToken != null` 分支行为与改前完全一致；OCR 分支设标志 + 显式 `isLoading = false` + `notifyListeners()`；`send()` 入口（`:791`）无条件清零防跨轮残留。

### 接口与行为契约

- **`isLoading` 置位不是 `send()` 的第一件事**：`send()` 从 `ai_conversation.dart:779` 进入后先过 `pendingDownload` 检查、`AiPromptTagSettingsController.initialize()`（**含 await**）、标签解析，主路径直到 `:952` 才 `isLoading = true`；OCR 兜底分支在 `:816` 提前置位。`isLoading=false && _sendInFlight=true` 的窗口比计划暗示的更长（含 initialize 的 await）。
- **两个标志的分工（串接接力，不是冗余）**：
  | 窗口 | 谁在守 | 位置 |
  |---|---|---|
  | `_send()` 入口起、`isLoading` 尚未置位的整段异步窗口（附件压缩落盘、`initialize()` await） | `_sendInFlight` | `ai_chat_page.dart:1647` 入口重入守卫 + `:2143` 附件按钮禁用 + `:2191` 发送按钮禁用 |
  | `isLoading = true` 之后的 LLM 全程 | `isLoading` | `ai_conversation.dart:779` 守卫（复合条件含 `\|\| isLoading`） |
- **按钮区域重建链（本修复生效的隐含前提，证据链完整）**：`AiConversationController extends ChangeNotifier` → 页面三条 controller 赋值路径（`:236/:260/:279`）全部 `addListener(_onControllerUpdate)` → `_onControllerUpdate`（`:1468`）无条件 `setState` → 按钮在 build 内联树 `:2181` 无 const/缓存子树遮断 → `isLoading` 置位/复位与 `notifyListeners()` 同一同步块。即「只改条件」就够，不需要额外监听或 `ValueListenableBuilder`。
- **中断链（未改动，逐段确认）**：`onPressed: () => _controller?.stopGeneration()` 硬编码闭包永不为 null → cancel `_llmCancelToken` 幂等 → `llm_client.dart` 取消分支返回空响应 → `_runLoop` 清令牌 → `_finishActiveTurn()` → `isLoading=false` + notify → 按钮回到「发送」。
- **`_ocrRequest` typedef（`ocr_client.dart:28`）签名为 `Function(String absolutePath)`，不接受 CancelToken**；`OcrClient.recognize` 同。OcrClient 内部两分支均吞掉所有异常（含 DioException）转 `OcrResponse(error: ...)` 返回，无法从外部感知「是否已取消」。
- **`isLoading` 复位路径全集**：OCR 停止路径（`_stopRequestedDuringOcr` 命中块显式复位）、OCR 失败 `:825`、正常结束 `_finishActiveTurn():1417`、send() catch `:965`。不存在卡死路径。

### 技术约束/铁律

- **布尔标志进条件表达式前，先列真值表**：按「阶段 × 各标志值 × 期望表现」画表，恒真/恒假分支当场暴露。一个标志的 true 区间若是另一个的**子区间**，`A && !B` 必然恒假（来源：11 号四行表格）。
- **涉及 UI 派生状态时，先问「这个标志的职责是驱动显示，还是防重入/防并发」**，职责不同的标志不该混进同一个显示条件（来源：11 号）。
- **`_sendInFlight` 是「await 全程 true」的粗粒度锁**，任何 UI 条件都不要依赖它区分「本地阶段 vs LLM 阶段」，要区分用 `isLoading`（来源：11 号实测）。
- **依赖 `_controller!.xxx` 的 UI 条件能自动刷新，靠的是三条 `addListener` 路径 + 无条件 `setState`**；新增 controller 赋值路径时必须同步 add/remove listener（来源：11 号重建证据链）。
- **CancelToken 的一次性语义**：`cancel()` 后不可重用，必须每次请求前重新 `new CancelToken()`。`_runLoop():1171` 的 `??=` 是对此语义的正确实现（来源：12 号调研）。
- **「按钮可见但操作无效」类 bug 的通用排查路径**：① 找按钮显示条件 → ② 找 onPressed 调用链 → ③ 确认调用链依赖字段在该状态下的实际值 → ④ 找该字段的初始化时机。本轮根因即「显示条件（isLoading）与功能条件（_llmCancelToken）不同源」（来源：12 号）。

### ❌ 不要再走的路

- **不要写两个语义重叠标志的取反组合**：意图是「排除 A 阶段」时，应判定该阶段的**特征量**（如 `isLoading == false`），而不是拉一个恰好在该阶段为 true、但其他阶段也为 true 的标志来取反（来源：11 号）。
- **不要为对齐计划文本改代码**：11 号计划两处描述与实际不符（`isLoading` 不是 `send()` 第一件事；守卫是复合条件），按「以实际代码为准」如实上报即可，绝不反向改代码（来源：11 号）。
- **不要尝试对 `_ocrRequest` 传 CancelToken**：typedef 不支持，改了 OcrClient 也会被 on DioException 吞异常，效果与布尔标志一样但改动量大 5 倍（来源：12 号）。
- **不要只做「提前初始化 `_llmCancelToken`」而不改 `_runLoop` 的重建逻辑**：停止后 token 被清 null，`_runLoop` 进去 `??=` 重建，LLM 照样发。这条路验证无效后不要再走（来源：12 号）。
- **改 UI 显示条件前，先确认重建链是否通**：「条件改对了但界面不变」和「条件本身错了」表现完全一致（来源：11 号）。
- **改「显示」时顺带核查「可用性」**：条件让按钮出现了，还要看 `onPressed` 是否可能为 null、背后动作在该状态下是否真的有效（来源：11 号 finding 4）。

### 残余风险/未验证假设

- **真机行为全部未验证**（11 号验收项 3/4 + 12 号单张/多张/OCR 后 LLM 三条路径）：停止按钮可见性、点击中断、按钮恢复「发送」、OCR 各阶段停止行为，均只做了静态推演（重建链 + 中断链逐段确认）。
- **11 号 minor（越界不改）· OCR 兜底窗口「停止按钮可见但点击无效」**：12 号已修复（`_stopRequestedDuringOcr` 检查点），但 OCR 循环末尾（`:833` 最后一个检查点）到 `await _runLoop()`（`:976`）之间约 120 行同步代码段内点击停止，标志被设 true 但无人消费，`_runLoop` 照常发 LLM 请求——isLoading 最终仍正确复位不卡死，但用户需再点一次。实际触发概率极低（同步段毫秒级），可下一轮在 `await _runLoop()` 之前加一次检查。
- **11 号 info（改动前后一致）· 下载确认暂停期停止按钮点击无效**：`_processToolCalls` 命中 `download_comic` 时直接 return `waitingForDownloadConfirmation` 不调 `_finishActiveTurn()`，`isLoading` 保持 true、`_llmCancelToken` 已在 `:1193` 置 null。改动前后该窗口停止按钮都显示且点击无副作用。若日后统一「停止按钮出现即可中断」语义，应与 OCR 窗口在独立计划中处理。
- **12 号 info · 空闲状态 `stopGeneration()` 语义变化**：原先是纯 no-op，改后会设 `_stopRequestedDuringOcr = true`，下次 `send()` 入口清零兜底；若存在单元测试依赖幂等语义需同步调整预期。
- **多图并发取消实际行为未测**：OCR 循环串行，当前张飞行中无法中断，停止响应延迟取决于当前张 OCR 耗时。
- **`_sendInFlight` 防二次发送职责未被削弱（审计明确结论）**：三个读取点 `:1647/:2143/:2191` 全部保留，未成为死变量（analyze 无 unused 告警可证）；若后续有人误以为它已无用而删除，会同时打开「压缩落盘期二次发送」和「附件按钮误可点」两个缺口。

### 跨轮诊断模式

- **布尔标志进条件表达式前，先列真值表**（见技术铁律，11 号四行表格是定位根因的关键）。
- **改「显示」时顺带核查「可用性」**：确认「出现即可点」后还要推演「点了有没有用」（11 号 finding 4 漏了后半问）。
- **grep 出待摘除标志的全部读取点，逐个判定改动后是否仍生效**，再看 analyze 有无 unused 告警交叉验证——同时防「误删语义」和「留下死变量」。
- **工作区存在他计划未提交改动时，只碰自己范围内的文件**，交付前用 `git diff --numstat` 分文件核对基线数字是否吻合（来源：11/12 号与 08/10 号并行工作区）。

## §101 在线详情页三改：封面全屏预览、本地收藏与已下载状态（第十六轮 01 号计划）

> 精简版对应：本会话计划与执行索引 01 号；完整版对应 §126。涉及文件：`online_comic_detail_page.dart`（picacg 侧）、`online_comic_page_components.dart`、`base_online_comic_page.dart`、`online_comic_page_logic.dart`。

### 架构决策与理由

- **`_OnlineCoverPreviewPage` 放 base 文件，与 detail 页 `_CoverPreviewPage` 各自私有、结构同构，不抽共用 widget**：两处引用体量小、两文件独立演进；抽共享抽象是备选方案，本轮不引入。
- **已下载禁用态用「组件 `onTap` 改可空」方案②而非「`busy` 置 true」方案①**：方案①会让已下载项显示 spinner 而非纯禁用文案，语义更差。picacg 侧已下载点击用 `onTap: downloaded ? () {} : onDownload`（保证用户仍可手动重下）。
- base 两个**惰性钩子**：`onLocalFavorite` / `downloadCandidateIds` 返回 null = 特性关闭，子类重写即开启；当前 jm/nhentai/eh 均未重写（休眠态）——**已下载状态检测实际只有 picacg 生效**（detail 页传真实 comic.id，base 侧钩子默认空实现）。

### 接口与行为契约

- `OnlineComicPillButton.onTap` 现为 **`VoidCallback?`**：调用方传 null 即禁用（与 `busy` 等效，任一满足则 `onPressed: null`）。全库仅 base 两处调用（下载/阅读），变更无扩散。
- **Hero tag 契约（两处必须严格一致，否则 Hero 动画失效）**：picacg = `picacg-cover-${comic.id}`；base = `online-cover-${sourceKey}_$id`。
- `logic.downloaded` 由 `checkDownloadedState` 异步设置后 `update()`；postFrameCallback 内 `!logic.downloaded` 守卫使单 logic 实例只检一次（详情页每次新建 logic，正常流程无碍）。
- picacg 已下载检测双通道：`findCachedByCandidates` + `resolveExistingId`，try/catch 吞异常。

### 根因推理链（两个 must_fix 教训）

- **must_fix 1 = 实施遗漏**：计划白纸黑字要求 base 文件补 `_OnlineCoverPreviewPage` 私有类，实施时漏加 → `undefined_method` 编译错误，连带整个基类及 jm/nhentai/eh 全部详情页无法编译，且是 photo_view/translations 两个 unused_import 告警的根因——类补上后 import 自然变 used，**修一项即消三告警**。
- **must_fix 2 = 计划建议写法与组件实际 API 不符**：计划写 `onTap: logic.downloaded ? null : ...`，但 `OnlineComicPillButton.onTap` 声明为非空 `VoidCallback` → 编译失败。修复采用审计方案②（组件签名改可空）。

### ❌ 不要再走的路

- **计划里"建议性写法"不等于组件实际 API**：凡要向已有组件传参，先 Read 组件签名再写调用代码。
- **"照搬/添加私有类"类步骤极易整体漏掉整个类**：凡计划含"新增私有类"，改完必须 grep 类名确认定义存在，再跑 flutter analyze。
- **收口与实施由不同 AI 承接时，实施 AI 的完成自评不可信**：必须以静态审计 + analyze 归零为准（本轮实施自评通过但实际 2 处编译错误）。

### 技术约束/铁律

- 新增引用类名必须先 grep 全库确认定义存在。
- 给组件传 null 前先确认参数可空性。
- 验收标准「flutter analyze 无新增 error/warning」是硬门槛：**unused_import 也是 warning，不能只盯 error**。
- 有任何编译 error 时相关行为验收标准自动不成立，必须先修编译再谈验收。

### 残余风险/未验证假设

- info 1：picacg `_localFavorite` 中 folders.isEmpty 后 `createFolder('默认收藏夹')` 再取 first——若默认夹已存在（陈旧/并发态）会抛 `Folder is existing` 被 catch 吞掉显示「收藏失败」；有 `_localFavBusy` 防重入，风险低，可后续改 getOrCreate 语义。
- info 2：上排图标行 spaceEvenly + Expanded 均分，开启第 6 个「本地收藏」图标后窄屏 4 字标签可能过挤——仅当子类实现 `onLocalFavorite` 时出现，当前休眠态。
- 未验证假设：JM 封面 `getJmImgHeaders()` headers 经 `_OnlineCoverPreviewPage` 的 `NetworkImage(url, headers:)` 可正常加载；本地收藏「写入第一个文件夹/无夹自动建默认夹」语义符合用户预期；7 项行为验收全部待真机。

## §102 本地搜索标签推荐：三轮反馈收敛出「输入才提示、不自动搜索」（第十六轮 02 号计划）

> 精简版对应：本会话计划与执行索引 02 号；完整版对应 §127。涉及文件仅 `lib/pages/local_search_page.dart`（最终形态 = 在线搜索页交互 + 本地数据源）。**本计划经历了三次用户反馈修正，是「用户真正在意时机而非样式」的典型案例。**

### 反馈修正历程（症状速查）

| 轮次 | 用户反馈 | 结论 |
|---|---|---|
| 首轮 | 横向无限滚动 chip 行 | ❌ 被否（"一整行无限横向滚动，操作上太傻逼"） |
| 修正 1 | 改为 Wrap 自动换行 + take(20) | ❌ 仍被否（"我要的是有输入时才提示，而不是这样全部提示"） |
| 修正 2 | 输入才提示：建议列表替换内容区、实时过滤、点击即搜 | 基本成立，但引入遮蔽 minor |
| 修正 3 | "现在是没有提示" | 根因：`_search` 起始清空建议（修遮蔽用）与 320ms 防抖自动搜索叠加，建议仅存活 ≤320ms |

### 根因推理链（两次修正互相踩——本轮最大教训）

修 A（「结果被建议遮蔽」minor → 在 `_search` 起始处清空建议）时未检查与既有自动路径 B（320ms 防抖自动搜索）的交互：输入 → 建议出现 → 320ms 后自动 `_search` → `_search` 起始清空建议 → 建议仅存活 ≤320ms，用户感知为"输入没有提示"。**修 A 时必须检查与既有自动路径 B 的交互**；「在某入口清空/置位状态」类修复，若该入口会被自动路径高频触发，需先评估自动路径本身是否应存在。

### 架构决策（不要再走的路）

- **本地搜索不要自动搜索**：输入只显示建议（停留不消失），搜索只由主动触发（点建议 / 键盘搜索键）发生——与在线搜索页一致（`online_search_page.dart:115/200-202/282`）。320ms 防抖自动搜索已整体移除，`_debounce`/`Timer`/`dart:async` 全部清理（grep 零残留）。**注意这是行为变更**：敲字自动出结果的旧行为不再存在，若用户后续反馈"敲字不出结果"属预期表现而非缺陷。
- **本地提示区不要横向无限滚动，也不要无条件铺开的提示区**（无论横向 ListView 还是 Wrap 换行）——「输入才提示」是两次反馈收敛出的硬边界。
- **需求确认时先问"何时显示"（无条件常显 vs 输入才提示），再问"长什么样"**：前两轮都纠结样式（滚动/换行），用户真正在意的是时机；若首轮即问出"输入才提示"，可省两次完整返工。

### 接口与行为契约（修正后）

- 输入非空 → 只更新建议（`_updateSuggestions`：`_chipsReady` 守卫、不区分大小写 contains、作者保留「作者: 」前缀、take(50)），建议列表停留占满内容区，不自动消失、不自动搜索；
- 点建议 / 键盘搜索键（onSubmitted）→ 触发搜索并收起建议（`_search` 起始清空建议兜底）；
- 输入为空 → 清空建议与结果，回到正常区域。
- `_controller.text` 程序化赋值**不触发 onChanged**：点建议路径填框→清建议→`_search` 顺序安全，不会回环。
- 数据收集：`_collectLocalChips({maxChips = 80})`，已下载走 `ensureLoaded()` + `getAll()`，收藏走 `favManager.allComics()`（实例方法，part of 作用域可见，内部已折叠所有文件夹；否决了"按文件夹循环"与"`search('')` 空串兜底"两条备选）。chips 按频次降序、作者在前。

### 残余风险/未验证假设

- 过滤性能：每次按键对 `_chips`（maxChips=80）全量 contains 过滤，当前规模无压力；提高 maxChips 成本线性上涨。
- 50 条上限：作者在前按频次降序取前 50，本地数据极多时第 50 条之后的标签/作者在输入完整词之前可能无法命中（contains + 截断双重限制）。
- 无本地数据（全新安装）时 `_suggestions` 恒空，页面行为与改动前一致。
- info 无害冗余：空输入时 `_suggestions` 存在双清路径（`_onSearchTextChanged` 空分支 / `_updateSuggestions` 空分支 / 清除按钮路径），结果一致可保留。
- 真机待验：输入时建议实时过滤手感、50 条上限窄屏观感、搜索键场景。

### 反馈修正 4/5 与后续修复（08/09）——本地搜索演进终态

> 前三次修正收敛「何时显示、怎么显示」，但**从未检查"显示内容与输入是否同语言"**。修正 4/5 与 08/09 补齐了数据值域与匹配语义两个维度，最终形态经历 **5 次用户反馈 + 2 份独立修复计划**：

| 轮次 | 用户反馈 | 根因/决策 |
|---|---|---|
| 修正 4 | "在线搜索页有中文→英文的翻译层，那你也做这个啊"（输入中文无建议） | **语言错配**：本地标签/作者全英文/日文（`_collectLocalChips: authors=249 tags=1157`），用户输中文 contains 天然落空。接入共享 `tagTranslations` 翻译层：中文输入逆查英文 key 集合 → 匹配 chips（`_candidatesForInput` + `_isChineseText`，`RegExp(r'[一-鿿]')`；两次线性遍历无 O(表×chips) 嵌套）；`_tagTranslationsReady` 懒加载对齐在线页 `_tagsReady`。**保留回退热门**（当时决策） |
| 修正 5 | "点了作者/标签能显示出全部源的（有些源作者标签是英文的）结果" | **匹配语义跨语言**：picacg 中文 tag 与 nhentai/jm 英文 tag 并存，单语言关键词只能命中一侧。改为「主词 OR 别名集合」：`tagAliasesForTag(String tag)`（放 `tags_translation.dart`，正向收中文译名 + 反向 `v.contains(t)` 命中英文 key + 冒号前缀剥离）；`aliases` 可选参数贯穿 `_search` → `favManager.search` / `matchesLocalDownloadedItem`，匹配函数内 OR（`matchAny` / `matchWords` 别名循环），默认 `const []` 保下游 4 处调用方零回归 |
| 08 修复 | 输入不存在的词也满屏无关 chip | **推翻修正4 的 fallback 决策**：`_suggestions = (candidates.isNotEmpty ? candidates : _chips).take(50)` 让 build 条件 `text.isNotEmpty && _suggestions.isNotEmpty` 恒真，建议区永远占满、用户无法辨别真匹配。改为 `_suggestions = candidates.take(50).toList()`，无匹配回空态（搜索引导图 / "未找到匹配"） |
| 09 修复 | 08 之后"没显示了" | **80 条上限把标签全排挤**：`_collectLocalChips` 先作者后标签且共用 `maxChips=80`，用户库 249 作者填满 80 槽 → 1157 标签一条进不去 → 翻译层找到的英文 tag key 在 `_chips` 恒查无 → candidates 恒空 → 建议区消失。去掉两处 break 与 `maxChips` 参数全量收集（1406 条），显示上限由 `_updateSuggestions` 的 take(50) 保证，过滤 <1ms |

### 修正 4 根因推理链（语言错配——四次修正里唯一数据值域缺陷）

症状"输入中文没提示" → 排除数据收集（真机日志 `_collectLocalChips: authors=249 tags=1157`，数据非空收集正常）→ 排除 `_chipsReady`（加载正常，回退热门能显示英文 chips）→ 锁定**语言错配**：contains 匹配中文输入 vs 英文/日文数据天然落空，候选为空 → 回退热门展示英文 chips → 感知为"没提示"。**代码逻辑全对、交互按规格、唯独数据值域不匹配，只有真机日志一锤定音**。

### 修正 5 最高风险陷阱（favorites 两段式过滤）

收藏搜索是「首词过滤 + 余词 AND 过滤」两段式：若只把首词过滤改成 OR 别名、余词 `removeWhere` 保持原谓词，仅靠别名命中的漫画会被余词 AND 误删。**两处谓词必须都 OR 别名**（`local_favorites_query.dart:236` 与 `:249`）——这是审计确认规避的最高风险陷阱。作者条目显式传 `const []` 抑制（作者无翻译层，走 `tagAliasesForTag` 会产生无意义别名甚至误匹配）。

### ❌ 不要再走的路（新增）

- **输入建议不要无条件回退全量列表**：候选为空时应回空态，而非复用全量 chip 列表；若未来想恢复"无匹配引导"，应改为显式"无匹配提示"而非回退（08 教训）。
- **数据收集不要"先作者后标签"共用同一个条数上限**：前者占满后标签一条进不去，建议区形同虚设（09 教训）；全量收集 + 消费侧 take(50) 是正确分工。
- **涉及字符串匹配/过滤的功能，验收必须检查"数据的值域/语言"而不只是"数据是否存在、列表是否非空"**：静态审查连过四轮都因"数据非空即逻辑正确"的盲区没发现语言错配（修正4 教训）。
- **修 A 时必须检查与既有自动路径 B 的交互**（修正3 教训，见上）；**favorites 的 removeWhere 与别名互斥是最高风险**（修正5 教训）。

### 契约（终态）

- `_collectLocalChips()` 无参数全量收集（作者在前 + 标签，各按频次降序）；显示侧 `candidates.take(50)`。
- `_candidatesForInput`：中文输入先逆查翻译表收集英文 key 集合（中文分类名展开 + 中文译名 contains），再遍历 chips 精确/包含匹配；英文/日文直配（不区分大小写）；`'作者: '` 前缀剥离后参与直配但作者不进翻译逆查。
- `_search(keyword, {aliases})`：默认空别名，方法内不隐式翻译，由调用方决定；点建议 onTap 传 `aliases: isAuthor ? const [] : (_tagTranslationsReady ? tagAliasesForTag(query) : const [])`；键盘提交/initState 首查不传（单语言搜索，设计如此）。
- `tagAliasesForTag`：全表 O(33k) 扫描毫秒级，每次点建议调用一次，无累积成本；`v.contains(t)` 反向子串可能引入非预期别名（可加长度守卫，未做）。
- 翻译表加载失败/未就绪 → 别名为空回落单关键词、中文建议降级回退热门→08 后为回空态（功能降级非错误）。

### 残余风险（08/09 追加）

- 两处残留注释与实现矛盾（`initState` 与 `_updateSuggestions` doc comment 仍写"热门回退"），08 按"只改一行"约束未动，建议文档清理轮一并修。
- 09 性能：1406 条每次按键 contains 过滤 <1ms，远低于帧预算；分类名展开时 translatedKeys 上千 key，chips×keys 嵌套 contains 最坏 <10ms（审计实测有界可接受）。
- `_chips` 字段仍被 `_candidatesForInput` 读取（非死代码）；若未来建议数据源重构需同步清理。
- 08/09 真机行为（无匹配回空态、中文/英文/日文建议命中、跨语言结果）全部待真机验收。

## §103 收藏页下载完成导致折叠：静默刷新被事件流旁路绕过（第十六轮 03 号修复）

> 精简版对应：本会话计划与执行索引 03 号；完整版对应 §128。涉及文件仅 `lib/pages/favorites/main_favorites_page.dart`（+38 行 = quiet 贯穿 23 行 + must_fix 收口约 15 行）。

### 根因推理链

下载完成 → `App.notifyLocalDataChanged()` → `localDataVersion` 自增 → `_handleLocalDataRefresh` → `_loadFolders(quiet: true)`；**同时** `_loadFolders` 内 `await _favoritesManager.init()` 时，`init()`（`local_favorites.dart:485`）无条件 `_emitFolders()` → `allFoldersStream` 旁路触发 `_handleFoldersChanged`（仅 `_isRemoteView` 早退）→ **无 quiet** 的 `_applyFolders`；当 `_currentFolder == null`（本地视图正浏览网络收藏源、未选过文件夹）命中第一分支 `_foldersExpanded = true`，抽屉弹出遮盖网络收藏，且必然执行——**直接路径的 quiet 被旁路路径绕过**。计划根因分析只沿「触发 → 直接调用链」单线追踪，漏掉事件流（Stream/广播）旁路。

### 架构决策

- **采用深度计数 `_silentRefreshDepth` 而非简单 bool**：`_emitFolders()` 的发射全部发生在 `init()` 的 await 链内（早于 `_loadFolders` 的 whenComplete），计数窗口可完整覆盖所有旁路发射；连续两个下载完成并发触发刷新时，bool 会在第一个完成时被提前清零、第二个刷新仍绕过，计数可避免该竞态。
- **被否决**：给 `LocalFavoritesManager.init()`/`_emitFolders()` 增加「静默刷新中」标记或参数——需改 `lib/foundation/local_favorites.dart`，超出本计划文件范围，按「若修法超范围则不做」原则放弃。

### 接口与行为契约

- `_applyFolders` 新增 `quiet` 命名参数：`quiet == true` 时完全不触碰 `_foldersExpanded`（保留现场），且不影响 `_currentFolder` 重算、session 回写、`_cacheFolderCounts` 等其余逻辑。
- quiet 沿调用链约束：仅 `_handleLocalDataRefresh` 传 `quiet: true`；初次加载（initState）、手动刷新、文件夹增删改对话框、视图切换均保持默认 `false`。
- `_handleFoldersChanged`（allFoldersStream 旁路）与直接路径共享同一 quiet 语义来源，即 `_silentRefreshDepth > 0`。

### ❌ 不要再走的路

- **修复全局状态必须全量枚举该状态的所有写入方（直接调用 + 订阅流 + 回调）再定最小修复面**：计划沿单线追踪漏掉事件流旁路。
- **不要在本地视图/网络源场景下只验证远程视图路径就判定验收通过**：`_handleFoldersChanged` 的 `_isRemoteView` 早退使问题只在本地视图暴露。

### 残余风险/未验证假设

- minor：quiet 使本地视图下「当前查看的本地文件夹在静默刷新时消失」不再自动展开抽屉，`_currentFolder` 变 null 但抽屉保持关闭，内容区显示「选择收藏夹」——判定可接受；如后续体验不佳可让 session.currentFolder 指向的文件夹消失时仍展开抽屉。
- info：远程视图每次下载完成静默刷新仍全量重拉远程收藏并经 `_contentVersion++` 重建页面，下载频繁时可能有抖动/闪烁与无谓网络开销；后续可考虑静默刷新跳过远程全量重拉或节流/去抖。
- 未验证假设：`_silentRefreshDepth` 计数窗口能完整覆盖 `init()` await 链内全部旁路发射（依据 `_emitFolders()` 调用点全部位于 init() 内推断，未逐行穷举）；真机验收项 3/4 待验。

## §104 第十六轮 AI 会话与设置收口：展示清单即时冲刷、自动下载子开关、模型下拉折叠（04/05/06 号）

> 精简版对应：本会话计划与执行索引 04/05/06 号；完整版对应 §129。涉及文件：`ai_conversation.dart`（04/05）、`base.dart` + `ai_settings.dart` + `ai_settings_page.dart`（05/06）。

### 04 · AI 展示清单多份独立显示（`ai_conversation.dart` +2 行）

- 修复：同一 AI 轮次调用 `display_result_list` N 次，界面出现 N 张独立卡片而非互相覆盖。方案 = **即时冲刷策略**：`_appendToolResult` 内 `report.items` 非空时先赋值 `_pendingDisplayItems` 再立即调用 `_flushPendingDisplayItems()`；`_flushPendingDisplayItems` 自身含空检查并在末尾置 null，直接复用。
- 契约：`_flushPendingDisplayItems` 调用点共两处（1179 `_appendToolResult` 内既有调用 + 1276 轮次末尾），均未受影响；`_pendingDisplayItems` 在 654/972 轮次重置为 null。
- 本轮经验层为空（无 must_fix/minor/info），真机待验。

### 05 · AI 自动下载选项（settings 149 子开关）

- 设置项 149 `aiAutoDownloadEnabledSettingIndex`：**子开关而非独立能力，不得加入 `aiCapabilitySettingIndex` map 与 `_anyCapabilityEnabled` 列表**（ai_settings.dart 常量独立声明）；后续新增子开关类设置沿用此模式。
- `_getEnabledToolSchemas()` 从「单门禁（122 能力开关 + scope）」演进为「能力开关 → 能力子开关 → scope **三分层过滤**」，子开关过滤层与能力开关解耦互不越权；`download_comic` 成为首个拥有独立子开关的工具。
- 门禁位置：`ai_conversation.dart:1535-1539`，在 `isAiCapabilityEnabled(toolName)` 之后、`isToolAllowedByScope` 之前；手动下载按钮走页面按钮链路不经 `_getEnabledToolSchemas`，不受影响。
- 升级兼容：`readSettings`（base.dart:245）双边界循环无硬编码长度检查，旧数据缺 149 时回落内存默认 `'0'`，热重启不崩溃。
- 子 toggle 显隐：`ai_settings_page.dart:953-964` 条件渲染读 `appdata.settings[122]`，`_setSetting`（:59-60）在父级 State 上 `setState` → 父级 build 重建即时生效，**无需 ValueListenableBuilder**——该页 switch 显隐联动机制可作同类联动 UI 的既有先例。
- 未完成：可选步骤「富内容下载确认卡片」未实施；行为类验收（门禁实际生效、子 toggle 即时显隐、热重启升级回落）待真机。

### 06 · 模型 ID 下拉折叠按钮（`ai_settings_page.dart` +7/-3）

- 修复：箭头按钮无条件 `requestFocus()` → 焦点已在本节点时空操作 → 下拉保持展开无法折叠。改判据基于**当前状态二分**（`_focusNode.hasFocus`）：已展开则 `unfocus()`，未展开则 `requestFocus() + _triggerFetch()`。
- SDK 源码核验契约（`autocomplete.dart`）：(1) unfocus 可靠关闭候选层（「Losing focus always closes it」）；(2) 候选层实际可见 = `hasFocus && options.isNotEmpty`（`_canShowOptionsView`）；(3) `_onFocusChanged` 仅在聚焦时拉取，unfocus 路径不触发 fetch。ModelListClient 以 Future 做 in-flight 去重，双调 `_triggerFetch` 不会重复发请求。
- **踩坑：勿用「重复调用 requestFocus」充当 toggle**——requestFocus/unfocus 是幂等的，动作不构成状态转换，判据应基于当前状态（hasFocus）而非期望动作。
- 残余 minor（可选项）：Escape（`_hideOptions` 隐藏候选层但保焦）或空候选场景出现「已聚焦但候选层为空/已收起」角落，一次点击无法重开；可改为判据「聚焦且候选层实际可见」或该场景先触发拉取而非直接 unfocus。
- 跨轮诊断模式：**SDK 源码核验法**——交互类问题不靠运行时猜测，直接读 Flutter SDK 源码确认显示/关闭判据。

### 05 遗留归属未明

`ai_settings_page.dart:1305-1319` 有一处非 05 计划描述的改动（AI 设置 API Key 字段下拉箭头改为「已展开则折叠、未展开则聚焦+拉取」），疑似其他轮次/计划产物，未评估未归入；提请主会话核对归属后统一提交（与 06 号改动同文件相邻区域，可能为同批并行改动）。

# PicaKeep 交接文档 - 精简最新版

> 2026-09-07 补充：PK-01 自动下载免确认已修复执行分支与提示词，新增 7/7 回归用例通过，待真机验收。详情见 [修复记录](docs/修复-AI自动下载免确认.md) 与 [问题清单](问题清单与交接核对-2026-09-06.md)。此前第十六轮 05 号仅完成工具可见性门禁，不能等同于免确认入队；本次验证状态以修复记录为准。
> 2026-09-07 补充：PK-02 已修复目录候选未验证、Windows 绝对路径重复拼接及目录索引误判；新增 20 项和相关既有 20 项测试通过。原截图样本与 Android Root/Shizuku 待验，详见 [修复记录](docs/修复-已下载目录解析.md)。

> 功能地图，2 分钟了解项目全貌。需要修改代码时，务必先查对应模块的"详见经验版"锚点。

---

## 项目定位

PicaKeep 是一个以本地漫画/本地图集/已下载管理为主线，同时支持客户端/服务端双模式与浏览器网页后台的 Flutter 项目。当前版本已稳定运行，`flutter analyze` 0 issues，`flutter build apk --debug` 通过。

---

## 模块功能清单

### 远程服务与连接池

- 远程图片并发加载：阅读闸（默认 6）/ 浏览闸（默认 8），可在设置-阅读/浏览中 1~12 实时调节
- 连接池分流：图片 bulk 池（24 连接）与 JSON 控制池（6 连接）隔离，封面走 bulk 池
- 连接池泄漏保护：孤儿连接超时回收 + 非 2xx 响应体 drain + body idle timeout 守卫 ⚠️ **修改图片/控制面请求链路必须读 [经验版 §1](./交接文档-经验思路版.md#1-远程服务与连接池)**
- 封面磁盘缓存：带失败负缓存（404 冷却 10 分钟），自动按大小限制裁剪旧文件

### 收藏 / 图片收藏远程档

- 收藏页远程档：列出远程收藏文件夹，支持中文路径，点击进阅读
- 图片收藏远程档：网格展示缩略图，无图项静默破图占位，点击定位资源进阅读
- 远程可用性判断、档位开关无条件显示 ⚠️ **修改收藏/图片收藏远程链路必须读 [经验版 §2](./交接文档-经验思路版.md#2-收藏图片收藏远程档)**

### 加密压缩包

- ZIP/CBZ 内部章节名编码修复：支持 GBK/Shift-JIS/UTF-8 自动识别
- 加密 ZIP 真实密码验证：AES-128/192/256 + ZipCrypto 双模式，错密码不再假成功
- 加密包阅读链路完整打通：封面 + 逐页解密 + inflate
- "忘记密码"功能：详情抽屉一键清除会话密码 ⚠️ **修改加密/解压链路必须读 [经验版 §3](./交接文档-经验思路版.md#3-加密压缩包)**

### 服务端模式

- 运行模式切换：客户端/服务端，入口在 APP能力 页
- 服务信息页：状态摘要、mDNS / Bonjour 发现、网段扫描、刷新状态（真实触发重扫）、断开连接
- 服务信息页稳定性：隐藏加载圈空闲时不保留动画；服务统计变化只静默刷新统计快照，不触发全页 loading，跳动问题与 mDNS / 网段扫描模式无直接关系
- 局域网发现策略：`mDNS / Bonjour` 是真实零配置发现，`网段扫描` 保留旧 `/status` 探测；两处配置入口（服务信息页、APP能力页）共用同一组挡位与“mDNS 失败自动网段扫描”开关
- mDNS / Bonjour 发现边界：自研 UDP 5353 发布/发现，**服务端查询回复已改为单播到源地址**（`_sendResponse` 收到 query 后单播而非组播），绕过 Windows 多网卡组播路由；client query 带 QU 位。主动广播保持组播。排查时优先确认 `_sendResponse` 取 `datagram.address` 走单播
- 网段扫描行为：继续使用 `/status` 探测作为独立“网段扫描”档位；当前已连接地址会优先探测，但命中后仍继续扫完整候选网段并聚合多个服务，不再因为第一批命中而提前停止
- 多设备 / 多服务发现：最终候选 2+ 时服务信息页弹选择窗口，展示来源、设备/服务名、地址端口、漫画数量、延迟和服务描述；同地址去重，同设备不同端口保留
- Android mDNS：已补 `CHANGE_WIFI_MULTICAST_STATE` + multicast lock；UDP 5353 绑定在 Android/Windows 不启用 `reusePort`，避免 `reusePort not supported`
- 远程资源热同步：librarySignature + WebSocket 事件推送，阅读期间延迟刷新
- 服务端挡位热更新：host/port 不变时运行态切换，无需重启
- 远程已下载/资源库/图集：三档展示与切换，回前台/重新进入时强制刷新
- 服务端中文路径修复、favorites handler 优化、_notifyLibraryChanged 收口
- 收藏夹接口增强：列文件夹内容时按 candidates 匹配资源库补 `itemId` 等字段，cover 路由本地失效时回退资源库封面（`_favoriteCoverFallbackCache`）
- 远程封面路径优先级：cover.jpg > 首张图片，pageSizes 支持图片预占位 ⚠️ **修改服务端路由/扫描/同步必须读 [经验版 §4](./交接文档-经验思路版.md#4-服务端模式)**

### 网页后台 / 浏览器应用

- 网页后台地基已落地：`pubspec.yaml` 注册 `assets/web_console/`，服务端用 `rootBundle` 提供静态资源，SPA fallback 支持刷新任意前端地址；旧 Dart 字符串页 `admin_web.dart` 已迁移为真实 HTML/CSS/JS
- `window.PicaKeepConsole` 是网页前端唯一共享契约：封装本机 `api.*`、远程 `remote.*`、数据源 `source`、远程地址 `remoteTarget`、登录态 `auth` 与历史 `history`；图片 URL 必须经 `api.imageUrl` / `remote.imageUrl`
- 服务端已为网页后台补 CORS/OPTIONS、真实登录、token 注入、`/api/remote/proxy` 远程 GET 代理；`/status` 继续免登录用于探活，代理只允许 GET、剥离 Cookie/Authorization 并设置超时
- 网页账号只服务浏览器后台；Flutter 客户端账号体系仍不存在。隔离范围仅阅读历史，漫画库/收藏夹/图片收藏/回收站仍共享服务端数据
- 登录为真实服务端鉴权：网页专用账号系统（admin + 多用户），token 绑定用户与密码哈希；空后台密码允许首启但登录页会提示风险
- 应用面板支持本地/远程/聚合数据源、封面墙、详情页、连续滚动/翻页双模式网页阅读器、阅读历史写回与 dashboard 历史续读
- dashboard 已按 app「我」页风格改版：历史卡、已下载（漫画）、图集、资源库、图片收藏、工具胶囊；已下载/图集按 `rootId.startsWith('custom_')` 分流（true=图集，false=漫画/已下载）
- 应用面板修复：grid/list 有返回按钮，图片收藏入口接 `/api/library/image-favorites` 真数据，`history.remove` 改用查询参数避免 404
- 管理面板支持回收站、收藏夹、图片收藏、本地资源批量移入回收站/直接删除、服务配置校验、日志刷新/过滤与完整接口清单 ⚠️ **修改网页后台必须读 [经验版 §13](./交接文档-经验思路版.md#13-网页后台与浏览器应用)**
- Web Console 已多轮 UI 收口：左侧导航、App 化详情侧栏/移动端底部 sheet、reader 上下工具栏（返回+标题+设置 / 上下章+滑条+页码+全屏+收藏+自动翻页+章节+保存）、阅读设置（阅读模式折叠项 + pill 开关/滑条）、连续拼接/翻页阅读、主题化背景、图片最大宽度滑条（双端，prefs `maxWidthPx` / 客户端 settings 116，默认 980）、缩放可平移、章节双列侧栏、全局 ESC 退出/返回、详情页推荐（`/api/library/items/{id}/recommendations`）、管理面板浅色化
- Reader 空白先看布局宽度：图片 DOM 与网络 200 正常不代表可见，`#console-root`/reader 容器 0 宽会整页空白；排查同时看 DOM 图片数、network、`naturalWidth` 和父级 `getBoundingClientRect().width`
- 本地 reader 验证先切「本地」源，避免远程自代理 `127.0.0.1:9527` 的 502/401 干扰
- Reader 交互参考客户端 `lib/pages/reader/touch_control.dart`：pointerdown/move/up 状态机，中心点击切栏、仅工具栏隐藏时边缘翻页（连续模式用视口高度判定）、缩放作用于 `.reader-zoom-layer`（origin 固定，`panX/panY` 平移）；不要退化成单 `pointerup` 或单张图片 transform
- 阅读进度保存收口：**任意方式退出阅读器（ESC / 返回键 / 跳转）都即时按当前页补写历史**（`cleanupReader → flushReaderHistory`，修掉 ESC 瞬退被 1500ms 防抖取消的丢页）；滚动连续模式从历史打开会**多帧重试定位到记录页**（`restoreScrollPosition` + `restoring` 守卫），不再停在第 1 页（分页模式本就直达，不受影响）
- 历史记录独立页已对齐「已下载」：**卡片 / 详细双档切换（与已下载共用 viewMode 记忆）+ 顶栏搜索可用 + 卡片复用 `.app-card`/`.app-detailed-card` 结构**；服务端历史只存 `target/title/cover/ep/page/maxPage/readEpisode`，没有作者/副标题/标签，故作者与标签改由**前端 join 资源库列表补全**（缺失即隐藏），第二行那个「0」就是 `readEpisode` 整数集合被字符串化的残留，已去除；详细视图超长标题 3 行截断省略 ⚠️ **修改网页 reader / 历史页必须读 [经验版 §13](./交接文档-经验思路版.md#13-网页后台与浏览器应用)**
- 应用面板已新增「收藏夹」入口（dashboard 资源库下方，接 `/api/library/favorites`）：文件夹列表 → 文件夹内漫画网格 → 详情/阅读，文件夹卡片紧凑横向（图标+名称+「本地」+数量徽章），列表用 `minmax(300px,1fr)`
- 收藏封面修复：服务端 `_buildFavoriteItemPayload` 补回 `itemId` 等字段（修 `__matched` 永远 false 导致的封面清空/卡片不可点）；cover 路由三级 fallback（本地 `coverPath` → `_favoriteCoverFallbackCache` → 按 `candidateDownloadIds()` 匹配资源库再 `resolveCoverPathOnly`），signature 变化时清缓存；cover/删除按 `?type=` 精确定位
- 通用详细档卡片向客户端对齐：标题严格 2 行省略（去 `text-overflow` + `max-height` 兜底，不再漏第三行）、标签**全部显示**（去 `slice(0,3)`，自动换行）且**跟随用户主题色**（`var(--accent-rgb)`/`var(--accent-2)`，非写死）；dashboard 内容能完整显示时不再多余滚动
- 远程/网页图集压缩包接入（V1.9.15 后）：服务端把 ZIP/CBZ 扫成普通 item（页 URL 仍是 `/images/{ep}/{page}`，命中压缩包返回 archive 内字节），客户端远程数据源零改动即可读；加密包经 `POST /api/library/items/{id}/archive/unlock` 解锁；网页/客户端远程档/详情页均补未解锁弹密码框
- 网页 UI 二次还原客户端（V1.9.15 后）：阅读设置/章节面板改桌面全高右侧栏+移动底部 sheet；详情页信息区改「字段名胶囊+值胶囊」双胶囊、操作区单个 72×72「阅读」方块、章节网格固定单元宽不拉满；章节列表当前项普通字色+扁平边框；排序默认倒序；阅读时滚动不再穿透背后列表
- 服务端资源热刷新（V1.9.15 后）：目录 `watch(recursive)` + 3s 去抖自动 rescan，外部增删资源无需重启即可在远程/网页刷新（`Directory.watch` 平台不可靠时降级到手动刷新）
- 服务端深扫漫画封面排除（V1.9.15 后）：下载漫画（managed root）深扫 `_listImageFiles` 现在排除 cover-like 文件（与 `_listDirectVisibleImages` 一致），不再把 cover.jpg 当成最后一页糊图，页数与客户端本地一致
- 管理面板增强（V1.9.15 后）：服务配置路径输入框旁加「+」按钮 → 弹出路径选择器（面包屑 + 目录列表 + 选择当前目录），服务端新增 `GET /api/admin/browse?path=` 列目录接口（复用 `PrivilegedStorageAccess.listDirectoryEntries`）；补「账号管理」导航 tab（列/建/删用户，仅 admin），修复头像菜单「用户管理」入口点击无反应的半成品
- 网页端详情页完整 redesign（V1.9.15 后）：信息区从单个合体 accent 胶囊改为客户端「字段名胶囊 + 值胶囊」双胶囊（圆角12）；操作区只留一个 72×72「阅读」方块；Hero 封面 132→102×136、圆角16→12、标题字号→18
- 网页滚动/布局收口（V1.9.15 后）：侧栏展开/收起不再触发整页滚动（shell 固定 `height:100dvh; overflow:hidden` + `#console-root` 内部滚动）；主页无用滚动条隐藏；reader 滚动不再穿透背后列表；dashboard 顶栏与内容间距修复；详情页章节网格单章不再拉满整行（`auto-fill+固定上限宽+左对齐`）
- 网页移动端顶栏与返回链路收口（V1.9.15 后续）：窄屏 `#topbar` 改为顶部横条，头像菜单限制在视口内且层级最高，移动端展开按钮视觉替换为 Pica 图标；列表页标题与返回合并为固定浮动返回（如「已下载 · 返回」「收藏夹 · 返回」），会随搜索/筛选/工具卡片位置动态避让；移动端浏览器返回键/手势已接入内部 history，避免 reader/list/dashboard 之间直接跳出页面。
- 网页移动端浏览器工具栏与详情 sheet 底部仍需按现有方向继续收口：已加 PWA manifest 与移动端 web-app meta；曾尝试 touch/window scroll booster 后撤回，移动端改回更自然的 document/window 滚动；详情底部 sheet 试过三段式固定 footer 后回退到内容流 footer，目前原则是**只保留一个底部补偿点**，避免 `.app-panel-content` 与 `.app-panel-footer` 双重 padding 造成大留白。后续优先使用「两状态高度学习」：首次打开视口最小=浏览器工具栏展开=默认垫高，首次收起后视口最大=工具栏收起=不垫高，用这两个高度判断 `--mobile-browser-chrome-pad`。
- 网页/远程压缩包接入与详情卡/排序/详情页/章节/面板/热刷新/移动端顶栏与详情 sheet ⚠️ **修改这些必须读 [经验版 §16](./交接文档-经验思路版.md#16-远程访问图集压缩包含加密包) / [§17](./交接文档-经验思路版.md#17-阅读器图片最大显示宽度滑块在连续模式失效与闪烁) / [§18](./交接文档-经验思路版.md#18-网页后台-ui-二次还原客户端详情页--阅读面板--章节列表) / [§19](./交接文档-经验思路版.md#19-网页后台管理面板增强) / [§20](./交接文档-经验思路版.md#20-网页后台滚动布局修复) / [§23](./交接文档-经验思路版.md#23-网页移动端顶栏返回浏览器工具栏与详情-sheet-底部收口) / [§21](./交接文档-经验思路版.md#21-服务端深扫排除封面) / [§22](./交接文档-经验思路版.md#22-网页端详情页完整-redesign) / [§14-10](./交接文档-经验思路版.md#14-当前仍存在的已知问题)**

### 动态取色与设置交互

- Android 动态取色：只响应亮/暗切换，不再每 6 秒重建 MaterialApp（ANR 修复）
- 设置滑块跟手：三个滑块封装为独立 StatefulWidget
- 外观设置：应用图标 13 色切换、深色模式跟随应用内设置
- Android 启动页重做：全 vector 图标、深色模式适配、双桌面图标修复
- 阅读器状态栏动效：隐藏时淡出动画（改回 immersive 模式） ⚠️ **修改外观/启动/系统栏必须读 [经验版 §5](./交接文档-经验思路版.md#5-动态取色与设置交互)**

### Android 启动与系统 UI

- 阅读器顶部黑条修复：13 个 activity-alias 全补 NormalTheme meta-data
- 启动页无缝过渡：原生启动页 → Flutter bootstrap 壳，"我"页渐进加载
- 系统栏：edge-to-edge、沉浸式显隐、安全区对齐
- 后续新增 activity-alias 必须带 LaunchTheme + NormalTheme ⚠️ **修改 AndroidManifest 必须读 [经验版 §6](./交接文档-经验思路版.md#6-android-启动与系统-ui)**

### 本地回收站与删除链路

- 回收站：真删源 download.db 记录 + 按原 rowid/time 恢复，支持恢复回退
- 跨应用 managed-download 删除/恢复：Shizuku/Root 通道，不误触他人沙箱
- 受限 download.db 不可读判定：区分"不可读"与"真不存在"
- 删除/恢复进度遮罩：防中途退出，执行结束后刷新
- 权限不足直接提示，不再假装成功 ⚠️ **修改删除/回收站/权限链路必须读 [经验版 §7](./交接文档-经验思路版.md#7-本地回收站与删除链路)**

### 已下载页三档隔离

- 本地/聚合/远程·已下载三档加载独立分支，单侧失败不拖垮另一侧
- 页面级超时：本地 8 秒、远程 10 秒，超时进入可重试空态
- 远程·已下载不得全量拉 `/api/library/items` 后客户端过滤；必须走 `?managedOnly=1`，root 页走 `?rootId=`，避免自定义资源库/图集规模变大后 Flutter 客户端 JSON decode/map 卡住
- 远程已下载三项 ANR 铁律（日志实测）：① `jsonDecode` 大 body 必须 `compute()` isolate（顶层函数入口）；② `_prepareTileViewModels` 远程档直接读 `item.subTitle/tags` getter，不得走 `_downloadedItemTags` 辅助函数（会触发 `toJson()` 全量序列化 episodes）；③ `_buildReadingHistoryById/FavoriteById` 排除 `RemoteLibraryComicItem`（id 非本地 hash，全表扫描无意义）⚠️ **必读 [经验版 §8](./交接文档-经验思路版.md#8-已下载页三档隔离)**
- 远程条目搜索卡顿真根因：`_translateDownloadedTag` 用 `entries` 遍历+逐条 `toLowerCase()` 比较，退化为 O(33739) 全表扫描（`tagTranslations` key 已是小写，本具备 O(1) 能力未用上）；接入 eh/nh 后单条目标签数暴涨才从"能忍"变"ANR"；改 `map[lowerTag]` 直查即可，O(33739)→O(12)
- Root/Shizuku 列表：优先 download.db 快速构建，封面后台低速补齐
- 远程封面缓存：稳定 provider key + 前 12 项预热 + cacheExtent 一屏
- 详情弹层：标签多时自动高度滚动；移动端 DraggableScrollableSheet ⚠️ **修改已下载页加载/封面链路必须读 [经验版 §8](./交接文档-经验思路版.md#8-已下载页三档隔离)**
- 详情弹层标签翻译与长标签布局：`_translateDownloadedTag` 补 `:` namespace 拆分与 `♀`/`♂` 性别符号剥离（对齐 EH 来源标签格式）；标签容器加 `maxWidth`+单行省略号，避免挤没底部按钮 ⚠️ **修改已下载详情标签翻译/布局必读 [经验版 §78](./交接文档-经验思路版.md#78-已下载详情页标签翻译与长标签布局修复第八轮45号计划)**
- **第八轮：已下载详情页标签翻译与长标签布局修复**（已完成）：`_translateDownloadedTag()` 补齐 `:` namespace 拆分与性别符号（♀/♂）剥离/回填，修复 EH 来源标签显示原始英文的问题；详情弹层标签 `Container` 加 `maxWidth:140` + 单行省略号截断，修复长标签挤没"阅读/查看详情"按钮。⚠️ **改已下载详情页标签显示必读 [经验版 §78](./交接文档-经验思路版.md#78-已下载详情页标签翻译与长标签布局修复第八轮45号计划)。**

### 工具页外显按钮

- "我"页工具卡片一体式设计：底部按钮横向滚动 + 右侧渐变遮罩
- "工具"页支持外显按钮与排序模式：点按显隐 + 长按拖拽排序
- 配置与"我"页共享持久化，默认首批显示：服务信息/本地文件/APP能力/回收站
- 新增工具自动追加到配置末尾，不会漏掉

### 历史同步与图集封面

- 历史页清空/删除后"我"页预览同步刷新
- 远程阅读图片加载进度：真实百分比（需服务端返回 Content-Length）
- 图集封面解码优化：仅限宽不限高 + 像素比余量 + 封面预热
- pageSizes 支持远程图片真实宽高预占位

### 运行期性能优化（第二轮，profile 实测驱动）

- 详情页推荐重算缓存：`_buildRecommendations` 结果按 `_comic`+模式缓存，消除下滑/翻页的 180ms 重算（首次进入仍算一次，固有成本）
- 工具页预返回动画：body 包 `RepaintBoundary`，消除缩放期几十个卡片的离屏 saveLayer
- 已下载转圈变长修复：local 视图跳过 `_checkRemoteAvailability`（远程离线时它走 3s 超时，串行挡在本地加载前），本地档不再被远程探测阻塞
- 加载态改列表区转圈：任何档位（本地/远程/聚合）loading 只在列表区转圈，保留 AppBar/视图选择器，不整页覆盖
- 图集远程档滚动卡顿（GC 风暴）：远程封面 `targetDecodeWidth=480` 限尺寸解码 + `cacheRawBytes=false` 绕过 50MB 堆字节缓存（已有磁盘缓存），最严重 vsync 尖峰 303ms→51ms
- 缓存大小限制修复：trim 可删范围从「只远程封面目录」扩到全部缓存目录（LRU 删最旧的到限制内），并加工具页主动触发入口，修掉「超限不降」 ⚠️ **修改这些必须读 [经验版 §26](./交接文档-经验思路版.md#26-运行期性能优化第二轮profile-实测驱动)**
- ⚠️ **性能归因铁律**：必须用 profile/release 包，debug 的 build 耗时是 JIT 假象；逐帧按 `startTime` 排序；profile APK 反复构建体积翻倍是 zipflinger 增量空洞，`flutter clean` 即解决（非代码问题）

### 本地已下载性能与图集封面特权修复（第三轮，root/shizuku 覆盖）

- 删除常驻后台封面缓加载 `_warmManagedDownloadCache`（挂在单例、遍历全部 items、脱离页面生命周期），封面缓存完全惰性化
- 按需封面迁移在活跃滚动期暂停（入队不打通道），滚动停止后 140ms 重启补齐
- 本地下载封面开 `optimizeCoverDecode` 按布局尺寸降采样解码，缓解折返重解码与老年代 GC
- 无封面项双层负缓存：`noCoverSentinel` 持久化标记 + `_coverResolveFailedIds` 页面级拦截，折返零 root I/O
- `trustStorageFromDatabase` 改为查内存 Set 验证目录真实存在，db-only 项不入封面队列
- root/shizuku 下图集封面破图全链路修复（10 轮递进）：PrivilegedStorageAccess 空回退→渲染层去裸 FileImage/Image.file→特权 provider 跳过 existsSync 短路→惰性 provider 消除闪空→底栏面板改特权 provider
- 图集外层 source 卡片加 2×2 子项封面拼贴预览
- 撤销 MANAGE_EXTERNAL_STORAGE 权限闪退定性为系统强制 kill 进程，非 bug 不修 ⚠️ **修改这些必须读 [经验版 §27](./交接文档-经验思路版.md#27-本地已下载列表滚动卡顿) / [§28](./交接文档-经验思路版.md#28-rootshizuku-下图集封面破图)**

### 交互优化与开屏剪贴板 ANR 修复

- 单项源卡片点击直开详情（子项=1 跳过中间列表），单封面铺满卡片
- 图集计数改为「子项之和」：`cachedAlbumChildrenCount` 与列表卡片 subtitle 同源，`isAlbum` 漏数的自定义下载目录型源被正确纳入
- 开屏剪贴板大文本 ANR 修复：新增原生 `Clipboard` 通道，`maxChars` 截断 + MIME 短路跳过 `image/*`，消除 `JSONMethodCodec` 主线程 6.4s 冻结 ⚠️ **修改剪贴板/图集计数必须读 [经验版 §29](./交接文档-经验思路版.md#29-单项源直开与图集计数改为了项之和) / [§30](./交接文档-经验思路版.md#30-开屏剪贴板大文本anr-修复)**

### 阅读器退出停止加载与服务端持久化

- 退出阅读器主动 abort 在途图片加载：`StreamImageAbortSignal` 贯穿 provider→远程请求链路，退出时 `StreamSubscription.cancel()` + 底层 HTTP abort，结合连接池孤儿回收零泄漏
- 全局搜索复用已下载页「显示数据库全部记录」开关：关时过滤 db-only 项，开时显示文字+缺失占位，仅作用于已下载类结果
- 服务端数据持久化：新增 `PICAKEEP_DATA_DIR`，`server_config.json`/`library_trash.json`/`web_console.db`/收藏库/历史库统一落持久化根，飞牛 Docker 重启配置不丢
- Linux 目录选择器加存储空间快捷导航：扫 `/vol*` 过滤存在 `/1000/` 的卷为快捷入口 ⚠️ **修改阅读器/服务端路径必须读 [经验版 §32](./交接文档-经验思路版.md#32-退出阅读器停止图片加载与传输) / [§33](./交接文档-经验思路版.md#33-全局搜索缺失项按开关占位或过滤) / [§34](./交接文档-经验思路版.md#34-服务端数据持久化与路径配置飞牛-docker)**

### 服务端配置与网页后台修复

- 收藏/历史 DB 路径修复：解除 `main.dart` 把 db root 强制绑 `PICAKEEP_DATA_DIR` 的误绑，改为按 download.db 上一层锚定或网页手动指定
- `server_config` 新增 `managedDataRoot` + `managed_data_root_resolver`，网页加收藏/DB 路径配置入口
- 存储空间快捷导航修复：显示名改为「存储空间 N」，点击直达 `/volN/1000/`，不再塞不存在库根
- 管理面板 UI 视觉统一：9 个 tab 卡片/表格/表单/按钮/空态/错误态样式统一，浅色克制跟随主题
- 服务端配置持久化：`_resolveEffectiveConfig` 服务端态以文件为权威源，不再用 appdata 空值覆盖网页配置；配置文件名改 `picakeep_server.data` ⚠️ **修改这些必须读 [经验版 §35](./交接文档-经验思路版.md#35-收藏历史-db-路径修复与网页配置入口) / [§36-§38](./交接文档-经验思路版.md#36-linux-目录选择器存储空间快捷导航修复)**

### 服务端常量集中化与 server_app 拆分

- 服务端常量集中化：新增 `defaultServiceAdminPortInt`，`server_config`/`local_server_runtime` 裸 `9527` 改为引用统一常量，`base.dart` 加注释关联
- `server_app.dart` 拆分（2917→252 行）：宿主 + 9 个 part/extension 文件（routing/browse/console/trash/library/favorites/files/status/util），纯结构重构不改行为 ⚠️ **修改服务端必须读 [经验版 §39](./交接文档-经验思路版.md#39-服务端常量集中化) / [§40](./交接文档-经验思路版.md#40-拆分-server_appdart)**
- `local_library.dart` 深度拆分（4089→1376 行）：69 个 private static 提为顶层私有函数 + 6 个 part 文件（settings/query/cover/archive/scan/static），纯结构重构不改行为 ⚠️ **修改本地库必须读 [经验版 §41](./交接文档-经验思路版.md#41-拆分-local_librarydart-深度)**
- `download_page.dart` 逻辑层拆分（3177 行）：`DownloadPageLogic` 拆为宿主+4 个 part（loading/cover/actions/view），2 个 static const 提升为顶层，纯结构重构不改行为 ⚠️ **修改下载页必须读 [经验版 §47](./交接文档-经验思路版.md#47-拆分-download_page逻辑层)**
- `local_favorites.dart` 拆分（1335 行）：`LocalFavoritesManager` 拆为 4 个 part（folders/comics/query/db），纯结构重构不改行为
- `trash.dart` 拆分（2130 行）：`TrashManager` 拆为 4 个 part（delete/restore/remote/io），纯结构重构不改行为
- `local_resource_scanner.dart` 拆分（2326 行）：`LocalResourceScanner` 拆为 6 个 part（managed/custom/metadata/cover/io/text），纯结构重构不改行为
- 补齐 `test/widget_test.dart` 最小 smoke test ⚠️ **修改这些必须读 [经验版 §48-§50](./交接文档-经验思路版.md#48-拆分-localfavoritesdart)**

### 在线能力移植（picacg 单源全链路）

- 我页 UI 重排：账号头像入口置顶、图集/图片收藏入口并排
- 在线源地基：双账号架构（全局 null→临时→正式）、HTTP 代理池独立隔离、mainPage 新增在线 Tab、"我"页分支在线入口
- picacg 单源全链路：pica 协议登录、分类/排行/搜索/作者/标签/漫画详情/章节/评论/看图/下载、图片中间层服务端代理、在线阅读器
- 在线搜索下载登录修复：新手任务 token 自动返回、手误关 Tab 恢复、登录页登录不跳转、详情页 WebView 显示
- 在线搜索独立页与详情页账号卡还原：搜索双源切换、收藏页在线状态标签、详情页双卡+关注按钮
- 搜索栏卡片复用与入口页瘦身
- 下载队列页原项目视觉还原与下载速度修复
- 下载握手失败走系统代理与并发/持久化修复
- 计划外增量修复：图片代理兼容、标签补全、Token 失效处理等 ⚠️ **修改在线源必须读 [经验版 §51-§59](./交接文档-经验思路版.md#51-在线能力移植picacg-单源全链路)**
- jm（禁漫天堂）单源全链路接入：多域名探测+Cookie登录、AES-ECB解密、图块重组（scramble算法+isolate）、搜索/详情/阅读/下载/收藏全通
- jm动态API域名获取、URL编造纠正、selectDomain启动探测、搜索登录404修复
- jm下载图块重组边界修复、WebP-VP8解码器系统性越界根治
- jm详情页完整还原：信息补全、评论与标签区优化、本地读图
- 通用在线详情页基类搭建：`BaseOnlineComicPage<T>`（13强制+16可选）+ `OnlineComicPageLogic<T>` 复用 StateController + 6 通用组件（封面/动作按钮/标签/章节折叠/推荐网格/错误加载）、接入文档 + JM 示例、新增源站开发从 2-3 天降至 0.5 天 ⚠️ **修改详情页必须读 [经验版 §63](./交接文档-经验思路版.md#63-通用在线详情页基类搭建)**
- **（未提交）JM 详情页迁基类重做 + 账号态启动重登修复**：`JmComicPageV2` 在 05 基类上重做被误删的 01 jm 功能（评论头像/收藏点赞态/收藏夹弹窗/信息区精简/可选中/标题字号）；账号态 bug（每次进页面收藏点赞恒显未收藏）根因=缺启动重登+僵尸cookie+游客200静默降级，已修 P0 `warmUpOnStartup` 启动重登 + P1 getComicInfo 游客200兜底 + P2 401文案放宽；`_post` 的 200 静默降级兜底（对齐 `_get`，修 POST 写操作"請先登入會員"自愈缺口）；附全局字号设置、我页卡片换行修复。**JM 评论回复功能已实现但真机失败**（试 4 种参数组合均 `cid: 0`，移动端 API 不支持或需未知参数，GitHub 7 项目均为网页端实现、无移动端成功案例，代码保留待抓包）。⚠️ **picacg(02) 被误删后未补、仍裸状态；改 jm 账号态/详情页/评论回复必读 [经验版 §64](./交接文档-经验思路版.md#64-jm-详情页迁基类重做--账号态启动重登本轮未提交)**
- **picacg 详情页迁基类重做（承接上条遗留，未提交）**：新建 `PicacgComicPageV2 extends BaseOnlineComicPage<PicacgComicItem>` + `PicacgCommentsPageV2` + `PicacgReplyPage`，补回被 DS 误删的网络层 6 方法（likeOrUnlikeComic/getComments/sendComment/sendReply/likeOrUnlikeComment/getReply，照搬原项目 API 路径）+ models 的 `PicacgComment`。对齐 JM 完成度：图标行（从头开始/收藏/赞/评论）+ 下载/阅读大胶囊 + 标签区（ID/作者/汉化组/分类/标签，长按复制·搜索）+ creator 卡片 + 简介 + 2列章节网格 + 推荐区。收藏/点赞均 toggle（picacg 接口本就 toggle，赞可取消，区别于 JM 单向）。**评论回复全链路打通**（区别于 JM 移动端回复失败）：顶级评论 POST `/comics/$id/comments`、回复 POST `/comments/$commentId` 两条路径；回复入口恒显（0 回复也能进去发）。入口两处已切（`picacg.dart` comicPageBuilder + `local_comic_detail_page.dart`），旧 `OnlineComicDetailPage`/`picacg_comments_page.dart` 成死代码。`flutter analyze` 0 问题。⚠️ **上条"picacg 仍裸状态"已由本轮解除；真机 UI 验收 + onShare（未做）+ 删旧死代码尚待跟进。经验向详解见 [经验版 §65](./交接文档-经验思路版.md)。**
- **主线第三轮：在线搜索结果页封面 header 改造**：`ComicSource` 新增 `ImageHeadersBuilder` typedef + `imageHeadersBuilder` 可选字段；`online_search_result_page.dart` 新增 `_coverProvider` 三分支（钩子null→NetworkImage；非空→StreamImageProvider+OnlineImageManager 带header）。picacg/jm imageHeadersBuilder 保持 null，零行为变化。⚠️ **接入新源时，搜索列表封面裂图先检查 imageHeadersBuilder 是否填了鉴权三件套。**
- **主线第三轮：ehentai/exhentai 全链路接入**（代码层完成，真机部分通过）：`EhNetwork` 单例（独立 CookieJarSql/双域名 getter/cookiesStr/getGalleryInfo/getGalleries/search/getEhImageUrl showKey+mpvKey状态机+ehgt3并发限流/rateGallery/favorite/归档三方法/ehUA静态getter/getUserName）；`Gallery`18字段含可变 auth map（已修namespace桶bug）；WebView Webview登录+手填降级；`EhReadingData`（part of pica_reader，无章节）；`EhentaiComicPageV2`（星级评分自定义区块/双层收藏面板/Content Warning确认/namespace tag还原）；`EhCommentsPage`（投票乐观更新）；`DownloadedGallery`+`enqueueEhentai`+`_runEhentaiTask`（逐页串行，根目录平铺，归档未做）；ehentai.dart 全量注册（`_EhGalleryLoader` 游标缓存器/哨兵subData/imageHeadersBuilder动态三件套）；Android build fix（onLogin void→Future<void>，UA全链路统一 EhNetwork.ehUA）；WebView代理CF过盾（onWebViewCreated串行注入+隐身脚本/settings[8]代理地址/系统代理channel/新增"网络"设置分组）；账号页真实用户名+完整cookie展示。⚠️ **修改 ehentai 相关必读 [经验版 §66](./交接文档-经验思路版.md)。** 关键铁律：①鉴权三件套+ehgt限3并发 ②UA用EhNetwork.ehUA ③downloadId无前缀 ④cookieJar独立 ⑤无章节 ⑥parseDownloadedItemRecordData精准拦截在contains('-')之前 ⑦analyze通过≠能构建（CFE更严）⑧WebView代理必须onWebViewCreated串行await。
- **主线第三轮：nhentai 源接入**（部分完成，代码+analyze+Windows build通过，真机未测）：新建 `cloudflare.dart`/nhentai_network 三件套/`NhentaiComicPageV2`/`NhentaiReadingData`/`NhentaiCommentsPage`/`NhentaiLoginPage`/`nhentai.dart` 注册；`OnlineDownloadManager` 加 nhentai 下载分支+`OnlineDownloadedNhentai`；`OnlineLocalReadingData` 四源参数化（isJm/isEhentai/isNhentai/默认picacg）。⚠️ **运行时验收（搜索/阅读/下载/收藏）待真机；Android+Linux build 未验证。**
- **主线第三轮：nhentai 登录秒退与按钮卡转圈修复**（已完成，真机待验）：修复移动端 WebView 登录两个联动 bug——`onTitleChange` 同一次回调置 `_seenLoginPage=true` 并立即判 token 导致秒退；快捷路径置 `_done=true` 锁死 `_verifyAfterClose` 导致按钮卡转圈。修法：进 webview 前清除 webview CookieManager 残留 token（C1）；成功判定改为「已离开登录页」（C2，主防线）；`_done` 只由 `_verifyAfterClose` 成功分支置位（C3）。`flutter analyze` 0 issues。
- **主线第三轮：nhentai 收藏CSRF-token修复**（已完成，真机待验）：根因=`getComicInfo` script解析CSRF token的try/catch静默失败，token始终为空。修法：新增 `getCsrfToken()` 从cookieJar读 `csrftoken` cookie（Django标准行为）；script解析失败后fallback调用之。涉及文件：`nhentai_main_network.dart`。
- **主线第三轮：nhentai详情页ID显示+页数/时间合并行**（已完成，真机待验）：`base_online_comic_page.dart` 新增 `buildTagsSectionOverride` nullable钩子；`nhentai_comic_page_v2.dart` 重写该钩子实现ID置顶+末行合并pages/时间。其他源零影响。
- **主线第三轮：nhentai评论页v2 API修复403**（已完成，真机待验）：旧评论endpoint `/api/gallery/{id}/comments` 被nhentai服务端废弃（403响应体直接给出迁移指引）。迁移到 `/api/v2/galleries/{id}/comments`，响应从裸数组改为取 `result` 字段，avatar双路径兼容，全字段空安全。**nhentai v2发评论不可行**（POST评论只认User Token，App走webview cookie登录，属死结；评论区维持只读）。涉及文件：`nhentai_main_network.dart`。⚠️ **nhentai API迁移不能照搬上游PicaComic（上游滞后仍用旧路径），以nhentai v2官方文档为准。必读 [经验版 §68](./交接文档-经验思路版.md#68-nhentai第三轮后续修复收口)。**
- **主线第四轮：搜索体验增强**（标签建议+NH-ID搜索，已完成，真机迭代验收通过）：`ComicSource` 新增 `RegExp? idMatcher` 字段；`SearchPageData` 新增 `enableTagsSuggestions`（eh/nh 设为 true，jm/picacg 保持 false）；`online_search_page.dart` 增加 `_SuggestionListView`（Column 固定搜索框+切换内容区，不整体替换 body）实时标签建议；`tags_translation.dart` 修复 `assets/tags.json` 解析（实际格式 `Map<ns, Map<key, cn_string>>`，非 List）并新增 `tagTranslateCategory`/`tagTranslateWithNs`（含复数→单数 namespace normalize + 全表兜底）；NH/EH 详情页 `enableTagTranslation: true`；`loadTagTranslations()` 移至 `main.dart` `_initializeOnlineFoundation()` 非阻塞预热；JM `idMatcher` 附带完成（`^(?:jm)?\d+$`）。⚠️ **EH customOptionsBuilder（P1）未完成，另立计划。修改搜索页/标签翻译/在线源字段必读 [经验版 §67](./交接文档-经验思路版.md#67-搜索体验增强标签建议nh-id搜索)。**
- **主线第四轮：收藏页网络收藏夹接入**（部分完成）：`NetworkFavoriteAction` typedef 加 `bool isAdding` 参数；折叠抽屉顶部插入网络区格子（`_NetworkSourceTile`）+ `_selectedNetworkSource` 状态；`NetworkFavoriteWidget` 内嵌化（无 Scaffold，Grid+长按菜单）；jm `addOrDelFavorite` 修复硬编码 `add:true` → `add:isAdding`；eh `addOrDelFavorite` 绑定（加收藏用 gid+token，取消用 `unfavorite2`）。**未完成：jm/eh 多文件夹二级 UI 未做，nh 维持 null。**
- **主线第四轮：收藏页全链路修复**（已完成）：①折叠无法关闭：`_toggleFolders` guard 由 `_folders.isEmpty` 改为 `!_hasAnyItems`；②NH 308 重定向：`validateStatus` 接受 308，`getFavorites` 补 lazy init，登录页 `_verifyAfterClose` 写 `source.data['token']='ok'` + `saveData()`；③JM 多文件夹：`multiFolder:true`，`loadFolders` 返回 Map，`NetworkFavoriteWidget` 二级导航；④EH 多文件夹：`loadFolders` 请求 favorites.php 刷新 folderNames 返回 Map。涉及 `main_favorites_page.dart`/`network_favorites_page.dart`/`favorite_data.dart`/`nhentai_main_network.dart`/`picacg.dart`/`jm.dart`/`ehentai.dart`/`jm_network.dart`。
- **主线第四轮：nhentai 收藏功能修复**（部分完成，收藏页真机通过）：`getFavorites` 切 v2 API（`GET /api/v2/galleries/favorited`，`_parseV2GalleryItem` 解析）；`favoriteComic`/`unfavoriteComic` 重写为 v2 优先+旧版CSRF fallback；新增 `_cdnServer`+`_fetchCdnServer()` 动态获取CDN域名；封面URL `thumbnail` 字段补前导 `/` 修复；语言检测改为硬判断 tag ID（`12227`=English/`6346`=日語/`29963`=中文）。**未完成：详情页收藏按钮真机验证pending。**
- **主线第四轮：nhentai 搜索切 v2 API**（已完成，真机通过）：`search()` 从 HTML 抓取改为调用 `GET /api/v2/search?query=...`，复用 `_parseV2GalleryItem`+`_fetchCdnServer`；搜索结果标签/语言字段正确。
- **主线第四轮：ehentai 下载弹窗+归档下载**（部分完成，真机待验）：`eh_comic_page_v2.dart` 删除 SnackBar 占位，替换为完整下载选项弹窗（普通下载+归档 ExpansionTile+cancelUnlock长按）；`enqueueEhentai` 加 `[int type=0]` 参数；`_runEhentaiTask` 加归档分支（`getArchiveDownloadLink` → dio 流式下载 zip → `ZipDecoder` 解压 → 按序重命名 → 写 `DownloadedGallery`）；`_saveQueue`/`loadQueue` 加 `downloadType` 序列化持久化。使用已有 `archive` 包。**真机归档下载→解压→离线阅读待验。**
- **主线第四轮：nhentai 收藏页 401 认证重设计**（已完成，真机通过）：新增 `_refreshAccessToken()`（`POST /api/v2/auth/refresh` 用 refresh_token 换新 access_token，更新 cookieJar）；`getFavorites` 切 v2 API（`Authorization: User {access_token}`），401 自动刷新重试。⚠️ **必读 [经验版 §69](./交接文档-经验思路版.md#69-nhentai第四轮收藏与搜索修复收口)。**
- **主线第四轮：NAS 路径支持完善**（已完成，Docker 真机通过）：网页后台目录选择器 Linux 根标签栏新增“根目录”和“挂载路径”；`__mounts__` 虚拟目录聚合 `/proc/mounts` 中的 Docker 用户挂载 + 自定义资源根，保留 `/vol*/1000` 存储空间入口；虚拟目录响应补 `roots`，面包屑与根标签栏正常。⚠️ **改服务端目录选择器必读 [经验版 §70](./交接文档-经验思路版.md#70-nas-路径支持完善根目录挂载路径导航)。**
- **主线第四轮：工具页补齐在线工具**（已完成）：移除重复“图集”入口，新增“在线工具”折叠卡与快捷工具底部弹窗；补 EH订阅、搜图bot酱、SauceNAO、打开链接、JM ID。EH订阅页读取 `/watched` 列表并支持刷新/加载更多/未登录跳转登录；打开链接按 EH/NH/JM 分流到详情页。⚠️ **改工具页/快捷入口/EH订阅必读 [经验版 §71](./交接文档-经验思路版.md#71-工具页补齐在线工具eh订阅搜图打开链接jm-id)。**
- **主线第五轮：AI 能力层地基**（部分完成）：新增纯 Dart、驱动无关的 `lib/foundation/ai/` 能力层：`AiTool`/`AiToolResult`、`AiToolRegistry`、四源常量、惰性注册入口与四个 function-calling 形状工具（在线搜索、发起下载、本地检索、下载状态查询）；本地搜索匹配逻辑抽到 `local_search_core.dart`，`LocalSearchPage` 与 AI 工具共用。`analyze --no-fatal-infos` 与 debug APK 构建通过；真实联网 dispatch/download 入队未验，保持”部分完成”。⚠️ **改 AI 能力层/本地搜索抽取必读 [经验版 §72](./交接文档-经验思路版.md#72-ai-能力层地基工具注册表四工具)。**
- **主线第五轮：AI 对话驱动层接入**（已完成）：新增 `llm_client.dart`（OpenAI 兼容 LLM HTTP 客户端，覆盖 DeepSeek/Ollama/Qwen）+ `ai_conversation.dart`（对话控制器，最多 3 轮工具调用循环，`download_comic` 挂起等待确认）+ `ai_chat_page.dart`（气泡 UI + 工具卡片 + 下载确认卡）；`AiPage` 改为包装 `AiChatPage`，原工具调试页通过 Navigator push 进入。⚠️ **改 AI 对话层/工具调用循环必读 [经验版 §73](./交接文档-经验思路版.md#73-ai-对话驱动层接入)。**
- **主线第五轮：AI 对话两项修复**（已完成）：① 底栏 AI Tab 开关后热更新：`ai_settings_page.dart` 改为调用 `App.serviceConfigVersion.value++` 通知主页刷新；② 新增 `query_remote_library` 工具（index 133），调用 `RemoteLibraryDataSource.fetchManagedDownloadItems()` 访问远程已下载快照，支持关键词过滤；③ 对话列表空态引导卡，说明上下文会话级保留。
- **主线第五轮：AI 多会话持久化**（已完成）：新增 `ai_conversation_store.dart`（纯 IO，会话文件 `ai_conversations/{id}.json` + 索引文件）；`AiConversationController` 加自动保存与加载；`AiChatPage` 升级为有状态组件，加汉堡菜单 + 侧栏 Drawer 切换会话；会话上限 50 条，标题取首条消息前 15 字。⚠️ **改 AI 会话存储/侧栏必读 [经验版 §74](./交接文档-经验思路版.md#74-ai-多会话持久化)。**
- **主线第五轮：AI 工具结果漫画清单页与消息级汇总条**（已完成）：工具返回漫画列表时不再让 LLM 输出 markdown 表格，改为独立"汇总清单卡"消息挂在 AI 文字回复下方，点击进入 `AiItemListPage` 网格清单；system prompt 约束文字回复只做简短总结；修复清单页 `ListView.builder` 包裸 tile 导致的塌陷 bug，改用 `SliverGrid` 对齐已下载页写法。⚠️ **改 AI 结果展示/清单页必读 [经验版 §75](./交接文档-经验思路版.md#75-ai-工具结果漫画清单页与消息级汇总条)。**
- **主线第五轮：AI清单底栏样式 + 对话保活修复**（已完成）：① AI 清单页详情底栏统一为透明背景+顶部圆角16+`Material` 包裹（realItem 与 fallback 两分支都补齐，对齐 `download_page._showInfo` 视觉）；② AI 对话页切 tab 再切回消息列表回顶、输入框草稿丢失——根因是 `NaviPane` 切 tab 走 `pushAndRemoveUntil` 整栈重建，`AiConversationController.create()` 每次都新建实例、无全局复用；修法：controller 重建后 `addPostFrameCallback` 补一次滚动到底部，草稿改挂 `_AiChatPageState` 的 `static Map<String,String>`（按会话 id 分 key，不挂在 controller 上）。⚠️ **必读 [经验版 §76](./交接文档-经验思路版.md#76-ai清单底栏样式--对话保活修复)。**
- **主线第五轮：AI 在线封面修复 + display_result_list 工具 + 卡片 UX**（已完成）：修复 `search_online_tool` 封面字段名（`cover`→`coverUrl`）；新增 `display_result_list` 工具，AI 可主动把筛选后条目以清单卡展示；移除旧自动 flush 缓冲区逻辑，清单卡改为工具显式调用触发；`_ToolCard` 加"查看原始结果"按钮；`_maxRounds` 3→5；工具卡片视觉减小；消息气泡支持长按复制。
- **主线第五轮：AI 清单页多选下载 + 下载队列/进度页 + AppBar 整理**（已完成）：清单页点 tile 改为直跳在线详情页（按 source 分流）；清单页支持多选，选中条目加入 `AiDownloadQueue`（持久化 JSON）；新增 `AiDownloadListPage`（待下载区 + 进度区含任务控制按钮 + 速度 + 全局暂停/清除）；对话页 AppBar 去标题、整理按钮布局、加下载清单悬浮入口。
- **主线第五轮：AI 收藏管理工具**（已完成）：新增 `manage_favorites` 工具，支持列出/创建/删除/重命名收藏夹，添加/移除/检查收藏，调用 `LocalFavoritesManager` 既有 API。
- **主线第五轮：AI Markdown 渲染 + 工具调试入口移至设置页**（已完成）：助手消息改用 `flutter_markdown` 渲染（`MarkdownBody` + `SelectionArea`）；新增依赖 `flutter_markdown`；`AiToolDebugPage` 入口移至 AI 设置页，对话页 AppBar 移除扳手按钮；`display_result_list`/`manage_favorites` 工具补齐设置索引（134/135）和开关。
- **主线第五轮：AI 工具卡片折叠分组 + 表格气泡宽度 + 滚动行为修复**（已完成）：同名工具多次调用折叠为"×N"分组卡（`_ToolGroup`）默认收起；含表格时助手气泡放宽至更大 `maxWidth`；修复滚动首帧 guard 逻辑；`AutomaticKeepAliveClientMixin` 保活 widget，会话 offset 记忆。
- **主线第五轮：AI 提示词标签面板 + 来源限定 + 结构化改造**（已完成）：新增 `ai_prompt_tags.dart` 标签模块；输入框左侧加锚定面板，固定来源标签（`#搜pica/jm/eh/nh`）+ 可编辑普通标签；来源约束走 schema 过滤+dispatch 硬拦截（不靠模型自觉遵守）；长期生效写入会话元数据；会话文件版本兼容升级；新增 `#搜本地` 硬拦截 `search_online`；标签插入改为结构化选中集合（不再写进输入框文本），输入框上方常驻标签行；新增测试覆盖。⚠️ **改 AI 提示词标签/来源限定必读 [经验版 §77](./交接文档-经验思路版.md#77-ai-提示词标签面板--来源限定--结构化改造)。**
- **第八轮：AI对话多下载工具调用引发400修复**（已完成）：`PendingDownload` 单对象改内部队列，一轮回复内多个 `download_comic` 不再因遍历中途 `return` 而漏配对 `tool` 消息，杜绝旧会话反复 400；对外只读 getter 签名不变，UI 层零改动。⚠️ **改 AI 工具调用循环必读 [经验版 §79](./交接文档-经验思路版.md#79-ai对话多下载工具调用引发400修复第八轮46号修复)。**
- **第八轮：多源搜索分发提示词与标签气泡回显补全**（已完成）：多来源在线搜索时提示词明确"应分别调用、不需要询问用户选哪个"；来源/`#搜本地` 范围限定标签补入气泡回显名单，且不分长期/临时模式（修正 §77 遗留缺口）。⚠️ **改多源分发提示词/气泡回显必读 [经验版 §80](./交接文档-经验思路版.md#80-多源搜索分发提示词与标签气泡回显补全第八轮47号计划)。**

### 第九～十轮近期主线增量

- **第九轮 AI 对话收尾修复**（已完成）：空/空白最终回复不再写入空 assistant 气泡；无文字且无清单时显示轻量兜底提示；有清单时不重复提示。长期来源/`#搜本地` 标签回显改读最终生效范围 getter，避免长期状态存在但气泡不显示。⚠️ **必读经验版 §81。**
- **第九轮 EH 下载提速**（已完成，真机待验）：逐页续传先查本地文件再请求解密链接；未完成页用 3 并发信号量；真实图片下载接入 ehgt 3 并发闸；归档模式未改。⚠️ **必读经验版 §82。**
- **第九轮本地详情在线跳转 ID 修复**（04 部分完成、06 代码闭环）：JM 空/非法 ID 在请求前拦截；`LocalLibraryComicItem` 必须取 `originalId`，不把内部 `itemId` 当来源 ID；jm/nhentai/picacg 三分支统一提取、校验、失败提示。真机验证待补。⚠️ **必读经验版 §83。**
- **第九轮本地详情更新信息**（已完成代码，真机待验）：三源模型补齐作品/演员/汉化组/分类/分类标签；在线信息整体覆盖只写 `title/subtitle/json`，保留本地 ID、时间、目录、大小和章节结构；详情页新增更新信息菜单与来源感知信息组。⚠️ **必读经验版 §84。**
- **第十轮本地 EH 与标签显示闭环**（02 部分完成）：统一下载记录解析入口兼容 EH 平铺/旧嵌套结构和字符串大小；下载卡片仅下载页限制两行标签；本地详情复用在线翻译并保留原始搜索值；自动化验证已完成，真实记录/实体机验收待补。⚠️ **必读经验版 §85。**
- **第十轮本地详情布局、EH 入口与关于页**（03 部分完成）：详情信息固定为 ID→创作者/上传者→下载时间，页数/语言/源站时间组成响应式信息行；EH 本地记录支持完整链接跳在线详情、Content Warning 确认和更新信息原子写回；关于页改用应用包实际版本、图标与 GitHub 入口。定向 41 项、全量 186 项测试及 Android/Windows 构建通过，真实 EH 流程、设备端布局和外部链接启动待补。⚠️ **必读经验版 §86。**
- **第十一轮 AI 结果清单参数边界修复**（01 部分完成）：`AiResultItemDecodeReport` 统一规范化工具参数、旧会话与清单 UI；`availability` 字符串等可修复漂移不再触发 release `ErrorWidget` 灰块，不可恢复条目返回脱敏 `retryable` 失败交给模型修正；工具卡原始结果和消息“查看全部”共用安全解码，下载队列复用规范 `toJson`。全量 202 项测试、analyze、release APK 和 Android 14 入口验收通过，截图对应会话完整冷启动、真实模型随机重试、Android 10 与网络日志待补。⚠️ **必读经验版 §87。**
- **第十二轮 AI 日志脱敏与缓存前缀优化**（01/02 代码部分通过，03 验收部分通过）：日志凭据 redaction、脱敏分享副本、`shapeV=2` 诊断指纹和动态指令后移已落地；全量 210 项测试、analyze 通过，但旧 Key 未轮换，且 `localOnly/onlineOnly` 互斥编码与原范围指令语义存在 P1 回归风险，测试覆盖和临时导出目录并发存在 P2 缺口。⚠️ **必读经验版 §88-§89。**
- **第十二轮远程服务代理分流与连接态重建**（04/05 部分完成）：按实际 URI 统一私网直连、公网手动/环境代理回退；远程 HTTP transport/cache generation、WebSocket 自有 client/generation、迟到连接隔离、代理设置和前台恢复重建已接入。全量 221 项测试、analyze 通过，真实代理/TUN/VPN 四组矩阵和 WebSocket 竞态专测待补。⚠️ **必读经验版 §90。**
- **第十三轮导出、标签采集、服务页与详情交互收口**（01～12 全部部分完成）：已下载导出/复制清单、EH/NH 未翻译标签 coordinator 与设置展示、服务信息页多端口扫描和操作顺序、来源感知作者解析、详情信息值搜索/复制、layout-neutral 信息值组件、已下载预览作者/标签独立折叠与固定 footer 已落地。全量 292 项测试、analyze、format、diff check、Android/Windows debug 构建通过；真实设备/网络、系统交付、Root/Shizuku、旧协议和大文件流式边界待补。⚠️ **必读经验版 §91。**
- **第十四轮 mDNS/网段发现与无连接态发现区**（01/03/04 已完成）：mDNS 有结果仍执行网段补扫并合并候选；子网扫描按本机网段优先、数字序和 last-success 加速；无地址时服务信息页内联显示发现进度并支持边扫边连。定向发现测试分别为 16、20、21 项通过，真实网络/现场 UI 边界按计划保留。⚠️ **必读经验版 §92。**
- **第十四轮下载、详情选择与服务页布局收口**（05/07/08 已完成，06 部分完成）：目录写读两侧统一 UTF-8 240 字节截断；EH 复用下载连接、对齐并发并缓存阅读页 HTML，但探测移除/重试收敛/HTTP2 与真机性能仍待做；详情标题/作者恢复自由选择复制；服务页修复 `IntrinsicHeight` 子树含 `LayoutBuilder` 断言。相关 86 项定向测试与 analyze/format 通过，现场交互和网络性能待验。⚠️ **必读经验版 §93。**
- **第十五轮 AI 全量流式与思考过程展示**（06 已完成）：`LlmClient.chat` 走 SSE 流式，**流式与否唯一由「是否传 delta 回调」决定**（不传 = 旧行为，现有测试零改动）；新建 `sse_chat_parser.dart` 纯状态机解析器，含 tool_calls 分片重组、`[DONE]`/keep-alive/空 choices usage 块处理；assistant 气泡新增可折叠思考块（流式期自动展开逐字、正文首字到达自动折叠、点内容折叠、长按自由选择），思考内容随会话持久化；「存/发分离」契约：存档永远保留 reasoning，`_buildRequestMessages()` 是唯一剥离点（带 `toolCalls` 保留、纯文本轮剥离）；新增开关 146「开启思考」（控请求，关 = 发 `thinking:disabled`）与 147「显示思考过程」（纯 UI）。⚠️ **必读经验版 §97。**
- **第十五轮 模型列表自动拉取与 API 余额**（06 已完成）：Model ID 下拉聚焦/点箭头自动请求 `{baseUrl}/models`，内存缓存 + in-flight 去重（成功 10min / 失败 30s），拉到就只显示拉到的模型、失败退回预置手填；历史会话抽屉「新建会话」行右侧显示当前 provider 余额（DeepSeek/Kimi/SiliconFlow 私有接口 + 中转站 subscription/usage 两连击，OpenAI 官方/智谱/百炼直接短路为不支持且**静默不显示**）。⚠️ **必读经验版 §97。**
- **第十五轮 搜图路径缺陷修复与停止会话**（07 已完成）：`#搜图` 路径的图片只流向 `search_by_image` 工具层，OCR 拦截与 `imagePaths` 注入两处独立豁免（修「发送按钮永久灰」与「视觉开后此后每条都 400」）；搜图桥接语改用标签桥接语；删去 5 处与气泡成对的 `error = message`（修错误双显与 banner 不消失）；新增停止会话按钮（`CancelToken` 只挂 POST，`DioExceptionType.cancel` 折叠为正常空响应干净收口）。⚠️ **必读经验版 §97。**
- **第十五轮 搜图结果阈值与全量展示**（08 已完成）：展示阈值 **30 → 15**（15-29% 弱匹配不再丢弃，边界含等号）；`_lowConfidenceBelow`（45，控警告）与展示阈值是**两条独立的线**；新增可选工具参数 `include_all_results`（boolean，`required` 仍只有 `image_ref`）；`kept` 为空且隐藏池非空时自动 fallback 展示全部并强制声明「可靠性极低」；success data 新增 `hidden_count`，语义是「**当前还没展示给模型的条数**」，includeAll / fallback 下恒为 0；过滤+消息+data 构造抽为 `@visibleForTesting buildResult()`（`SoutubotNetwork()` 硬编码单例导致原地实现不可测）；system prompt 规则 11 追加 hidden_count 引导。⚠️ **必读经验版 §98。**
- **第十五轮 长期状态卡片「已了解」永久关闭**（09 已完成）：新增 settings **148**（`aiPersistentCardDismissed`，默认 `'0'`）——**纯 UI 开关**，不影响长期标签/来源/仅本地状态的实际生效；「当前会话长期状态」卡片渲染加 `!cardDismissed` 门卫，标题行右侧加「已了解」`TextButton`；卡片宿主是 `_buildPromptPanel`（不是计划所写的 `_buildPendingTagRow`）；面板挂在 `OverlayEntry` 上，改其显示**必须** `_promptPanelEntry?.markNeedsBuild()`，仅 `setState` 无效；提示走 SnackBar（`showToast` 是 no-op 空实现）。单向操作，无恢复入口。⚠️ **必读经验版 §98。**

### 第三轮修复：analyzer、远程收藏、安卓返回、加密包、配置共用

- 修复 02 拆分引入的 analyzer 误报：3 个文件 import 调整为 package 路径/sho，CFE 编译一直通过仅 analyzer 误判，未回退拆分
- 修复远程收藏点击不能跳转：`RemoteFavoriteItem` 新增 `itemId`，优先直连 `fetchItemDetail`，保留 candidates 兜底
- 修复安卓历史返回错落收藏页：`history_page`/`me_page` 改 `pushInner`，阅读页不再压 root Navigator 绕过 inner 栈
- 修复加密包远程解锁后不能阅读：解锁后重新 `fetchItemDetail` 刷新对象，避免旧对象无 pages
- 客户端/服务端配置共用：桌面同进程 GUI 配路径单向播种到服务端配置文件（仅空字段补入，不覆盖网页已存值），headless 不变 ⚠️ **修改这些必须读 [经验版 §42-§46](./交接文档-经验思路版.md#42-修复02拆分analyzer误报)**
- 安卓双桌面图标修复：main/AndroidManifest 保留 MainActivity launcher 满足 Flutter 工具解析；debug/profile overlay 移除 alias 的 launcher 只留一个入口；release overlay 移除 MainActivity launcher 保留 alias 图标切换

### 桌面/移动端交互

- 桌面端侧栏顶部被标题栏遮挡修复
- 移动端底部弹层改为 DraggableScrollableSheet（默认 60%，可拖至 80%）
- WindowFrame 主题底色填充

### 其他稳定性修复

- 局域网发现：mDNS / Bonjour 零配置发现 + 网段扫描兜底；两者都会用 `/status` 二次校验；网段扫描现在会扫完当前候选网段并聚合多个服务，显式“网段扫描”候选来源显示为 `网段扫描`
- 图集合集外壳目录识别：自定义图集资源路径可按路径独立开启“合集”；开启后把“合集外壳目录”识别为一个多章节图集，正式作品目录及内部数字章节展开为章节，关闭时保留旧叶子目录扫描
- 合集图集命名修复：关闭合集时数字叶子目录 `1/2/3` 不再作为外部项目名；开启合集时章节名不再被主文件夹前缀污染；详情页和弹出章节面板的“序号”开关对合集 / 远程多章节图集恢复可用
- Android sqlite 运行库缺失修复
- 阅读设置页 keepScreenOn 通道补齐
- Shizuku 授权状态检测修复、Root 会话内复用
- Android 包名统一为 lingxue.picakeep
- 设置页 APP 分组信息架构收口
- 应用内日志独立页面 + 运行期开关
- Shizuku FUSE 限制提示（可"不再提示"持久化）
- 图集删除/恢复进度遮罩、多选全选图标
- 加密压缩包章节状态显示修复（不再灰色）
- 多选态视觉统一（主色遮罩/描边，无勾选浮标）

---

## Linux 桌面构建

- **问题**：`sqlite3_flutter_libs` 插件的 CMakeLists.txt 通过 `FetchContent` 从 `sqlite.org` 下载 `sqlite-autoconf-3520000.tar.gz`。`sqlite.org` 在部分网络环境下不可达（被墙），导致 `flutter build linux --debug` 在 cmake configure 阶段卡死，表现为 `Connection timed out` 或 `Waiting for another flutter command to release the startup lock`。
- **修复方案**：将 sqlite3 合并源码（`sqlite3.c` + `sqlite3.h`）捆绑到项目 `linux/sqlite3/` 目录，并在 `linux/CMakeLists.txt` 中预置 `sqlite3_SOURCE_DIR` 和 `sqlite3_POPULATED` CACHE 变量，使 `FetchContent_MakeAvailable` 跳过下载直接使用本地文件。
- **涉及文件**：`linux/sqlite3/sqlite3.c`（~9.4MB）、`linux/sqlite3/sqlite3.h`（~680KB）、`linux/CMakeLists.txt`（`include(flutter/generated_plugins.cmake)` 之前的两行 `set(... CACHE ...)`）
- **更新 sqlite3 版本时**：从 GitHub 下载对应版本源码（`https://github.com/sqlite/sqlite/archive/refs/tags/version-X.XX.X.tar.gz`），解压后运行 `./configure && make sqlite3.c` 生成新的合并文件，替换 `linux/sqlite3/` 下的 `sqlite3.c` 和 `sqlite3.h`。同时确认 `pubspec.lock` 中 `sqlite3_flutter_libs` 的依赖版本与合并文件版本匹配。
- **远程构建流程**：本地 tar 打包时 `linux/sqlite3/` 目录会随源码一起传到远端，不需要额外步骤。
- ⚠️ **修改 Linux 构建链路或升级 sqlite3_flutter_libs 插件版本时，必须读 [经验版 §25](./交接文档-经验思路版.md#25-linux-桌面构建-sqlite3-下载卡死)**

---

## 桌面服务端部署与打包

- **目标**：装一个包，有屏开 GUI、无屏（NAS/SSH）起 CLI 服务端，**CLI 服务能力与桌面版完全一致**，管理在网页端。
- **核心铁律**：**不要 `dart build cli` 编 `bin/picakeep.dart`**——那是读不到资源的空壳（`api/library/items` 写死 `[]`）。两端都用 **GUI 二进制 + `--server` 入口**（`lib/main.dart` 实现：不建窗、直起 `PicaKeepAdminServer`，能读资源）。
- **Linux**：`flutter build linux` 产物 → deb（`.github/scripts/package_linux_deb.sh <amd64|arm64>`）。无屏经 **xvfb** 给 Flutter 引擎虚拟显示；wrapper `/usr/local/bin/picakeep` 子命令 `server/stop/status/logs`（dash 脚本、PID+端口双兜底防孤儿）。deb `Depends: libgtk-3-0, xvfb`。**glibc 必须在 ubuntu-22.04/Debian12 构建**，否则 Debian 跑不起来。**文件权限三件套**（二进制 `chmod +x` + wrapper `chmod +x` + `dpkg-deb --root-owner-group`）缺一不可，否则装到 NAS 报 Permission denied 或服务端资源空。
- **Windows**：GUI exe `--server` 不弹窗即可读资源（实测 395 本），无需 xvfb。GUI 内启动服务后可缩**系统托盘**常驻（`tray_manager`，关窗：有服务→缩托盘，无服务→退出）。命令行 wrapper `windows/cli/picakeep.{cmd,ps1}` 指向 `{app}\picakeep.exe --server`，Inno 把 `{app}\cli` 加进 PATH。**ps1 带 UTF-8 BOM**（中文 Windows GBK 误读会崩）。
- **CLI 子命令（两端一致，含简写）**：`server`(s/up/start)、`stop`(x/down)、`status`(st)、`logs`(l/log)、`version`(-v/--version)；无参 → 有屏开 GUI、无屏起服务端。
- **CI**：`.github/workflows/build.yml` push tag `v*` 或手动一次出 **4 包**（linux amd64/arm64 deb、windows x64/arm64 Inno）。无 Android、无 macOS（项目无 macos 目录）。
- ⚠️ **打包 / 改服务端部署链路必须读 [经验版 §31](./交接文档-经验思路版.md#31-桌面服务端部署linux-headless--windows-托盘--两端-cli-wrapper--ci-打包)；权威打包步骤见 `Z-plan/后续优化-第二轮/打包说明-Linux与Windows.md`**

---

## 当前仍存在的已知问题

1. Android 前台服务/通知/保活约束适配未彻底关闭
2. 局域网发现：Android 组播（`wlan0` errno 98 为系统 mDNS 占用，单播回复已绕过）、路由器 mDNS 隔离仍需真机复核；Windows 多网卡组播路由问题已修复
3. Android 全部文件访问权限需补更多机型验证
4. Shizuku UserService 读大 download.db 偶发 DeadObjectException（当前回退/跳过，不阻塞删除）
5. download.db 残留行对应目录不存在时目录枚举失败日志（当前跳过，不阻塞）
6. 加密包会话密码重启后丢失（需持久化"已解锁+用哪个默认密码"状态）
7. 远程收藏 fetchFavoritesInFolder 串行循环（18 个文件夹 = 18 次请求，可改并行）
8. reader 退出时未主动 abort 未完成图片请求
9. 图片收藏网格列数偶发漂移（推测 settings[74] 存值问题）
10. 网页图片收藏入口目前以真实数据缩略图浏览为主，深度跳转阅读器仍需额外拉详情后续增强
11. 网页后台的图集/已下载分流依赖 `rootId.startsWith('custom_')`，后续服务端若改 rootId 规则需同步改网页前端
12. **客户端远程档 401（已修复，二次回归点已记录）**：`_requiresAuthorization` 不可覆盖所有 `api/`。Flutter 客户端远程档没有网页 token，`api/library`、`api/library/*`、`api/events` 必须放行；只对 `api/admin/*`、`api/console/*`、`api/remote/proxy` 鉴权。详见经验版 §14-10
13. 资源热刷新依赖 `Directory.watch`，Android 受限存储/网络盘可能不可靠（已降级 + 手动刷新兜底）；加密包跨重启 session password 丢失需重新解锁
14. **CF 过盾（soutubot 搜图）**：判据已改为 `cf_clearance` 基线快照变化，但 Windows/移动端真机均未验（换发新 clearance 后能否被 `getCookies` 读到、`onStarted` 触发率）；2 秒轮询回调重叠的 `_webview!` null-check 竞态未根除（只打日志不崩溃）；`search_by_image_tool.dart` 过时注释待另开小计划修正。⚠️ 见经验版 §99
15. **AI 停止按钮**：OCR 循环末尾到 `await _runLoop()` 之间约 120 行同步窗口内点停止标志不被消费（概率极低，需再点一次）；下载确认暂停期停止按钮点击无效（改动前后一致，计划 07/11 既有设计）；空闲 `stopGeneration()` 语义从 no-op 变为设标志（send() 入口清零兜底）；全部真机行为未验。⚠️ 见经验版 §100
16. **第十六轮在线详情页**：7 项行为验收全待真机；picacg `createFolder('默认收藏夹')` 在默认夹已存在时抛 `Folder is existing` 被吞显示「收藏失败」（可改 getOrCreate）；第 6 个图标窄屏过挤（子类开启 `onLocalFavorite` 时才出现）；JM 封面 headers 加载未验。⚠️ 见经验版 §101
17. **本地搜索推荐**：本地搜索已移除自动搜索（敲字不出结果属预期行为变更，待用户确认接受）；无匹配回空态（08）；`tagAliasesForTag` 反向 contains 子串可能引入非预期别名（可加长度守卫，未做）；翻译表未就绪别名为空回落单关键词；两处残留注释（仍写"热门回退"）与实现矛盾待清理；中英日建议命中与跨语言结果待真机。⚠️ 见经验版 §102
18. **第十六轮 AI 设置**：`ai_settings_page.dart:1305-1319`（API Key 下拉箭头折叠行为）归属未明，疑似其他轮次产物，提交前需核对；06 minor——Escape/空候选场景「已聚焦但候选层为空」角落一次点击无法重开（可选项未采纳）；AI 自动下载子开关门禁/显隐/热重启升级回落待真机。⚠️ 见经验版 §104
19. **第十六轮详情页四改/删除**：非 active 图标 0.7 透明度观感、7 按钮窄屏排版、已下载禁用态展示待真机；**重下载后本地已下载页不显示已修复（12 号，hiddenIndexSync 仅 trashed 隐藏）**；删除下载无二次确认（与原项目一致）；窄屏「删除下载」文字可能溢出待验；删除按钮双触发（12）、删除后按钮不消失（13）、无限重建循环（14）均已修复，真机全流程/性能实测待设备回归。⚠️ 见经验版 §105-§107

---

## 本会话计划与执行索引

- `继续定位远程资源_401_d05b2fa1.plan.md`：远程资源 401 回归定位计划，已取消继续诊断；最终边界是 `/status`、`api/library`、`api/library/*`、`api/events` 放行，只保护 admin/console/proxy。
- `图集资源路径合集开关计划_08f414dc.plan.md`：早期较宽泛的图集资源路径合集开关计划，后续被更精确的“合集外壳目录识别”计划替代。
- `图集合集外壳目录识别计划_5c3a4ec8.plan.md`：已执行；完成每资源路径“合集”开关、本地/服务端/远程扫描与切换接口、图二顶栏开关、数字目录项目名修复、章节名前缀清理和“序号”开关恢复。
- `mDNS_与网段扫描修复计划_1757ef93.plan.md`：mDNS / Bonjour 与网段扫描初版修复计划；用于确认 Windows mDNS 多网卡风险和网段扫描多服务目标。
- `mDNS_与网段扫描修复计划_48e20d37.plan.md`：已执行网段扫描部分；修复 `service_data_source.dart` 中命中当前地址后提前 `break` 的问题，显式“网段扫描”现在可聚合同网段多个 `/status` 服务。
- `mDNS_查询回复单播修复`：已执行；`_sendResponse` 查询回复改为单播到源地址 + client query 加 QU 位，解决 Windows 多网卡组播路由导致手机扫不到的问题。
- `新需求-主线-第八轮/45-计划-已下载详情页标签翻译与长标签布局修复.md`：已完成；对应经验版 §78。
- `新需求-主线-第八轮/46-修复-AI对话多下载工具调用引发400.md`：已完成；对应经验版 §79。
- `新需求-主线-第八轮/47-计划-多源搜索分发提示词与标签气泡回显补全.md`：已完成；对应经验版 §80。
- `新需求-主线-第九轮/02-计划-AI对话空回复气泡兜底修复.md`：已完成，未做真机触发验证。
- `新需求-主线-第九轮/03-计划-ehentai下载并发提速.md`：已完成，未做 EH 真实网络实测。
- `新需求-主线-第九轮/04-计划-JM本地详情跳在线详情Empty-data修复.md`：部分完成，代码防御已落地，真机与历史脏数据范围确认待补。
- `新需求-主线-第九轮/05-计划-长期标签气泡回显补全.md`：已完成，未做真机 GUI 验证。
- `新需求-主线-第九轮/06-修复-JM在线详情跳转itemId与originalId字段错位.md`：已完成代码验证，真机验证待补。
- `新需求-主线-第九轮/07-计划-本地详情页更新信息多选菜单与在线信息覆盖.md`：已完成代码与自动化验证，真机验证及 loading 视觉反馈待补。
- `新需求-主线-第十轮/00-接手-第十轮三项反馈与当前进度.md`：接手记录，Track A 已完成，Track B/C 已在 02 号计划闭环，仍保留真实记录/真机缺口。
- `新需求-主线-第十轮/01-计划-EH本地资源类型识别与大小修复.md`：已完成代码验证，真实 EH 数据验收待补。
- `新需求-主线-第十轮/02-计划-第十轮剩余反馈闭环.md`：部分完成；自动化与两条 SQLite 扫描入口已验证，真实截图记录、实体机、横屏/大字体待验。
- `新需求-主线-第十轮/03-计划-本地详情信息布局、EH入口与关于页.md`：部分完成；代码、定向/全量测试和 Android/Windows 构建已验证，真实 EH 警告/写库、设备端与外部链接验收待补，对应经验版 §86。
- `新需求-主线-第十一轮/01-修复-AI结果清单异常参数超长灰块与入口失效.md`：部分完成；全量 202 项测试、analyze、release APK 和 Android 14 确定性入口验收通过，截图对应会话完整冷启动、真实模型随机错误参数重试、Android 10 与真实网络日志待补，对应经验版 §87。
- `新需求-主线-第十二轮/00-诊断-AI对话缓存命中率日志复盘.md`：已完成参考诊断；确认缓存总体有效、动态 system 前置是主要结构风险、旧日志含凭据泄露，后续拆分 01/02。
- `新需求-主线-第十二轮/01-计划-AI日志凭据脱敏与缓存诊断补全.md`：部分完成；代码、测试和脱敏分享边界已完成，旧 DeepSeek Key 尚未确认撤销/轮换。
- `新需求-主线-第十二轮/02-计划-AI对话动态指令后移与缓存前缀稳定化.md`：部分完成；稳定前缀主体通过，但 `localOnly/onlineOnly` 组合和范围指令语义存在 P1 回归，需新计划修复。
- `新需求-主线-第十二轮/03-验收-AI日志脱敏与缓存前缀优化.md`：已完成验收记录，结论为部分通过；详见经验版 §89。
- `新需求-主线-第十二轮/04-诊断-远程本地资源代理超时与局域网直连分析.md`：已完成参考诊断；根因仍是高概率代理/TUN 路由 + 长生命周期运行态残留组合，真实四组网络矩阵待验。
- `新需求-主线-第十二轮/05-计划-远程本地资源代理分流与连接态重建.md`：部分完成；策略、HTTP/WS 生命周期和设置接线已实现，定向/全量验证通过，真实代理矩阵和 WebSocket 迟到连接专测待补，对应经验版 §90。
- `新需求-主线-第十三轮/01-计划-已下载选择导出与可配置分享清单.md`：部分完成；导出核心、来源适配、菜单和字段选择页已完成，真实系统分享/保存、特权和超大文件待验。
- `新需求-主线-第十三轮/02-计划-EH-NH未翻译标签采集与设置页展示.md`：部分完成；结构化 lookup、store、设置页和已知来源采集完成，远程来源契约与真实设备待验。
- `新需求-主线-第十三轮/03-计划-服务信息页美化与多端口扫描.md`：部分完成；端口设置、多端口扫描、状态概览与响应式页面完成，真实局域网待验。
- `新需求-主线-第十三轮/04-修复-03服务信息页反馈与操作布局.md`：部分完成；操作区、状态反馈、窄屏/大字号布局和注入数据源测试完成，真实扫描时序待验。
- `新需求-主线-第十三轮/05-修复-01导出清单复制页数与特权大文件.md`：部分完成；复制、页数和目录/ZIP 清单契约完成，平台流式读取与大文件现场待补。
- `新需求-主线-第十三轮/06-修复-02待翻译标签入口与采集链路.md`：部分完成；coordinator、批量落盘、设置入口和成功边界完成，远程/跨重启/真机待补。
- `新需求-主线-第十三轮/07-修复-05作者元数据与导出清单一致性.md`：部分完成；来源感知作者 resolver 和前向写入完成，旧数据/真实源/特权环境待验。
- `新需求-主线-第十三轮/08-修复-06未翻译标签采集边界补全.md`：部分完成；ready flush、NH 稳定 ID、搜索 brief 观察和大批次回归完成，真实搜索竞态待验。
- `新需求-主线-第十三轮/09-计划-详情信息值搜索与复制交互.md`：部分完成；本地/在线/NH 信息值搜索、复制、键盘和语义契约完成，真实源路由与系统输入待验。
- `新需求-主线-第十三轮/10-修复-04服务连接操作回位与排序.md`：部分完成；断开位置、底部操作顺序、坏地址清除和响应式布局完成，真实网络扫描待验。
- `新需求-主线-第十三轮/11-修复-09详情信息区紧凑布局回归.md`：部分完成；共享信息值包装器 layout-neutral 回归和调用链自动化验证完成，真机截图/触摸/键盘待验。
- `新需求-主线-第十三轮/12-计划-已下载预览作者标签折叠与固定操作.md`：部分完成；单滚动区、作者/标签独立折叠和固定 footer 完成，移动 sheet/桌面侧栏现场边界待验。
- `新需求-主线-第十四轮/01-计划-mDNS模式并行补扫合并盲区修复.md`：已完成；mDNS 补扫不再被已有候选短路，结果按地址去重并保持 mDNS 优先，发现测试 16 项通过。
- `新需求-主线-第十四轮/02-计划-host模式广播地址端口覆盖字段与部署指引.md`：已完成；advertiseHost/advertisePort 覆盖字段落地，网页后台表单、Docker 部署指引；真机 mDNS 发现待验。⚠️ 见经验版 §94-02
- `新需求-主线-第十四轮/03-计划-子网扫描器命中率修复本机网段优先与数字序.md`：已完成；本机网段优先、数字序、并发/预算和 last-success 优先已落地，发现测试 20 项通过。
- `新需求-主线-第十四轮/04-计划-无连接态发现区内联化边扫边连.md`：已完成；无地址自动扫描、实时候选、点击即连和唯一候选自动连接已落地，发现测试 21 项通过并执行端口 Widget 测试。
- `新需求-主线-第十四轮/05-计划-下载目录名过长按字节截断.md`：已完成；写读两侧统一 UTF-8 240 字节截断，静态检查和格式化通过，单测/真机待补。
- `新需求-主线-第十四轮/06-计划-EH下载提速连接复用与并发对齐.md`：部分完成；连接复用、并发对齐和阅读页缓存已落地，探测移除/重试收敛/HTTP2 与性能现场验收待后续处理，对应经验版 §93。
- `新需求-主线-第十四轮/07-计划-详情页封面旁标题作者还原自由选择复制.md`：已完成；标题/作者自由选择复制和空白取消选区已落地，真机手势兼容待验。
- `新需求-主线-第十四轮/08-修复-服务信息页客户端概览LayoutBuilder内固有高度断言.md`：已完成；`IntrinsicHeight` 子树移除 LayoutBuilder，31 项服务页定向测试通过。
- `新需求-主线-第十四轮/09-计划-Shizuku权限受限UI提示与日志降噪.md`：已完成；下载页 "权限受限" chip、设置页授权指示落地；Dart 层日志无需改动（系统层 Binder），真机待验。
- `新需求-主线-第十四轮/10-修复-verifyImageReachable超时race与WebSocket关闭码.md`：已完成；删 receiveTimeout 消 race，4 处 WebSocket goingAway→normalClosure，analyze/format 通过。
- `新需求-主线-第十四轮/11-计划-EH下载去掉预检请求消除每图双倍日志.md`：已完成；方案 A 落地（preflightVerify 开关 + validateStatus 放开 + 换节点循环上移），新增 509 单测 4 项通过，真机待验。
- `新需求-主线-第十四轮/12-计划-AI聊天页切tab和切会话滚动位置修复.md`：已完成；static 位置缓存 + 贴底哨兵 + pending 去重机制，3 项测试通过，真机待验。
- `新需求-主线-第十四轮/13-计划-网络日志降噪与AI聊天页滚动闪烁修复.md`：已完成；网络 INFO 静默（Fix A）、IgnorePointer+Opacity 消闪（Fix B）、切 tab 改用 deactivate 保位置（Fix C），12 项测试通过，真机待验。
- `新需求-主线-第十五轮/01-计划-AI会话页增强-长期标签显示与消息索引.md`：部分完成；长期标签每条气泡回显 + 索引面板基础版落地，一段式拖选手势由 02 号交付。⚠️ 见经验版 §95
- `新需求-主线-第十五轮/02-计划-索引面板边缘自动滚动与高度约束修复.md`：已完成；面板高度约束修复（`maxScrollExtent` 恒 0 → 1312.8）、Ticker 边缘自动滚动、迷你索引条、两个设置项（141/142），全量 342 passed。⚠️ 见经验版 §95
- `新需求-主线-第十五轮/03-计划-AI会话图片发送与OCR接口设置.md`：已完成；图片附件基建（压缩落盘/双序列化出口/历史瘦身）、视觉优先 OCR 兜底、OCR 接口设置区（143/144），375 passed，真机与 OCR 联调待验。⚠️ 见经验版 §96
- `新需求-主线-第十五轮/04-计划-在线工具webview菜单项修复.md`：已完成；`.path` → 完整 URL、`!` 强解包 NPE 消除、三项菜单可见反馈，3 例单测 + 全量 345 passed，手动路径待真机。⚠️ 见经验版 §96
- `新需求-主线-第十五轮/05-计划-soutubot以图搜源AI工具.md`：已完成；soutubot 直连网络层 + `search_by_image` 工具 + `#搜图` 固定标签 + 能力开关（145）+ 自动出卡白名单，全量 416 passed，真实接口与 CF 过盾待真机。⚠️ 见经验版 §96
- `新需求-主线-第十五轮/06-计划-模型列表拉取与流式思考过程展示.md`：已完成；全量流式改造（传回调=走流式）、思考块展示/持久化、模型列表自动拉取、API 余额、开关 146/147，476 passed，现有测试零改动，真机联调 7 条待验。⚠️ 见经验版 §97
- `新需求-主线-第十五轮/07-修复-05搜图路径图片处理缺陷与停止会话.md`：已完成；Bug 1~4（OCR豁免/imagePaths豁免/桥接语/error双显）全部修复 + 停止按钮，476 passed，真机搜图端对端与停止行为待验。⚠️ 见经验版 §97
- `新需求-主线-第十五轮/08-计划-搜图结果阈值调整与全量展示.md`：已完成；阈值 15、`include_all_results` 参数、零结果自动 fallback、`hidden_count` 字段、规则 11 文案、`buildResult` 抽纯函数 + 10 例单测，全量 486 passed，真机 4 条待验。⚠️ 见经验版 §98
- `新需求-主线-第十五轮/09-计划-长期状态卡片已了解永久关闭.md`：已完成；settings 148 + 渲染门卫 + 「已了解」按钮 + Overlay `markNeedsBuild` 刷新 + SnackBar 轻提示，486 passed，真机 3 条待验。⚠️ 见经验版 §98
- `新需求-主线-第十五轮/08-修复-passCloudflare移动端验证窗口闪退.md`：已完成；移动端关窗判据改为 `cf_clearance` 基线快照变化、`closed` 可重入守卫、`onFinished` 双点调用 + 调用方幂等，审计推翻计划 Bug B 前提与 Fix 1 充分性，analyze 全绿，真机待验。⚠️ 见经验版 §99
- `新需求-主线-第十五轮/10-修复-passCloudflare桌面端过盾判据与关窗回调.md`：已完成；桌面端基线取 App cookie jar（同步取值零竞态）、判据「存在且不同于基线」、`onClose` 接 `onFinished` 补齐手动关窗降级；`onTitleChange` 实为 2 秒轮询（非事件），Windows 上必失败而非偶发，analyze 全绿，Windows 真机待验。⚠️ 见经验版 §99
- `新需求-主线-第十五轮/11-修复-停止按钮条件判断导致按钮不显示.md`：已完成；`ai_chat_page.dart:2181` 条件去掉 `&& !_sendInFlight`（两标志 true 区间重叠致恒假），`isLoading` 为显示唯一真值源，`_sendInFlight` 三个防重入读取点全保留，analyze 全绿，真机待验。⚠️ 见经验版 §100
- `新需求-主线-第十五轮/12-修复-OCR阶段停止按钮点击无效.md`：已完成；`_stopRequestedDuringOcr` 布尔标志 + OCR 循环串行检查点 + `stopGeneration()` 分 LLM/OCR 两路 + `send()` 入口清零，绕开 `_ocrRequest` typedef 不接受 CancelToken 的限制，14 行改动 analyze 全绿，真机待验。⚠️ 见经验版 §100
- `新需求-主线-第十六轮/01-计划-在线详情页三改.md`：已完成；封面全屏预览（Hero + PhotoView）、picacg 本地收藏按钮（默认收藏夹自动建）、已下载状态双通道检测（base 惰性钩子 onLocalFavorite/downloadCandidateIds 休眠态）；审计揪出 2 must_fix（漏加 `_OnlineCoverPreviewPage` 私有类、`OnlineComicPillButton.onTap` 需改可空）已收口，analyze 全绿，7 项行为验收待真机。⚠️ 见经验版 §101
- `新需求-主线-第十六轮/02-计划-本地搜索标签推荐.md`：已完成；**五次用户反馈修正收敛**——横向滚动被否→Wrap 被否→输入才提示→移除 320ms 防抖自动搜索→接入中文翻译层（语言错配：本地标签/作者全英文/日文）→点建议跨语言别名 OR（`tagAliasesForTag` + `aliases` 透传收藏/下载匹配函数）；`_collectLocalChips` 全量收集（无上限）、建议 take(50)，analyze 全绿，真机待验。⚠️ 见经验版 §102
- `新需求-主线-第十六轮/08-修复-本地搜索建议fallback去除.md`：已完成；去除 `(candidates.isNotEmpty ? candidates : _chips)` 回退热门（修正4 保留的 fallback 使建议区恒占满、无法辨别真匹配），改 `candidates.take(50)`，无匹配回空态，1 行改动 analyze 全绿，真机待验。⚠️ 见经验版 §102
- `新需求-主线-第十六轮/09-修复-本地搜索chips全量收集.md`：已完成；`_collectLocalChips` 去掉 `maxChips=80` 与两处 break（先作者后标签共用上限，249 作者填满后 1157 标签全被排挤 → 建议区消失），全量收集 1406 条由消费侧 take(50) 保证显示，过滤 <1ms；执行时签名参数已删但两处 break 残留（引用已删参数会编译错误），由主会话补删；数据源与展示上限职责分离。analyze 全绿，真机待验。⚠️ 见经验版 §102
- `新需求-主线-第十六轮/03-修复-收藏页下载完成导致折叠.md`：已完成；`quiet` 参数贯穿 `_loadFolders`/`_loadRemoteFolders`/`_applyFolders`（静默刷新不触碰 `_foldersExpanded`），`_silentRefreshDepth` 深度计数统一直接路径与 allFoldersStream 旁路路径（must_fix：计划漏掉 `init()→_emitFolders()` 旁路），analyze 全绿，真机待验。⚠️ 见经验版 §103
- `新需求-主线-第十六轮/04-修复-AI展示清单多份独立显示.md`：已完成；`_appendToolResult` 即时冲刷策略（report.items 非空立即 `_flushPendingDisplayItems`），同轮次多次 `display_result_list` 出独立卡片，+2 行 analyze 全绿，真机待验。⚠️ 见经验版 §104
- `新需求-主线-第十六轮/05-计划-AI自动下载选项.md`：已完成；settings 149 子开关（独立常量不进 aiCapabilitySettingIndex map）+ `_getEnabledToolSchemas` 三分层过滤 + 设置页子 toggle 条件渲染（父级 setState 即时显隐）；readSettings 双边界升级兼容，可选富内容卡片未做，行为验收待真机。⚠️ 见经验版 §104
- `新需求-主线-第十六轮/06-修复-模型ID下拉折叠按钮.md`：已完成；箭头 onPressed 按 `hasFocus` 二分（已展开 unfocus / 未展开聚焦+拉取），SDK 源码核验 RawAutocomplete 三条契约，+7/-3 analyze 全绿，真机待验。⚠️ 见经验版 §104
- `新需求-主线-第十六轮/10-计划-在线详情页四改.md`：已完成；图标行扁平化（去 44×44 描边圆框、active 主题色/非 active onSurface 0.7）、base 新增 onSearchSimilar 相似按钮（nhentai/jm/eh 三子类实现跳 OnlineSearchResultPage）、三子类实现 downloadCandidateIds 补齐已下载检测；踩坑：OnlineSearchResultPage 参数是 source 对象非 sourceKey、dart format 触发 curly_braces lint。analyze 全绿，真机待验。⚠️ 见经验版 §105
- `新需求-主线-第十六轮/11-计划-已下载删除按钮.md`：已完成；base pill 行已下载时「下载（可点→重新下载确认）+ 删除下载（图标+文字，左对齐紧贴）+ 阅读」1:1:2，删除弹三选项（取消/直接删/移入回收站）；logic 双删除方法（TrashManager 回收站 / forceDeletePermanently 强制删，trash_delete 新增公开接口）；已完成下载在 LocalLibraryManager 不在 DownloadManager 队列是删除无反应根因。analyze 全绿，真机待验。⚠️ 见经验版 §106
- `新需求-主线-第十六轮/12-修复-删除按钮双触发与重下载后不显示.md`：已完成；`isDeleting` in-flight 守卫防删除按钮连点（onTap 改可空、弹窗前置位 finally 复位）；`hiddenIndexSync()` 改为仅 trashed 隐藏（purged 不再过滤重下载同 id 新行，修复重下载后本地已下载页不显示）。analyze 全绿，真机全流程待回归。⚠️ 见经验版 §107
- `新需求-主线-第十六轮/13-修复-删除后删除下载按钮不消失.md`：已完成；`LocalLibraryManager` 新增 `evictCachedCandidates` 删除成功后主动驱逐内存缓存（帧回调先于异步刷新执行是根因，守卫挡不住），`checkDownloadedState` 加 `isDeleting` 短路防御。analyze 全绿，真机 UI 回归待验。⚠️ 见经验版 §107
- `新需求-主线-第十六轮/14-修复-删除后checkDownloadedState无限循环.md`：已完成；13 号回归——驱逐缓存后 `checkDownloadedState` 无条件 `update()` 致无限重建循环（result/downloaded 恒 false 无法断开，13 号前被旧行为掩盖）；修复 1 行 `if (downloaded == result) return;` 值变化守卫，帧回调链路从根源断开。analyze 全绿，性能实测待设备复核。⚠️ 见经验版 §107

---

## 提交注意事项

- 正确仓库：`D:\Flutter_Projucts\PicaComic\PicaKeep`（内层，非外层 PicaComic）
- 正确分支：`main`
- 提交前确认：`git -C D:\Flutter_Projucts\PicaComic\PicaKeep status --short`
- 提交格式：`Vx.y.z 中文标题` + 中文摘要正文
- **不要提交**：`android/local.properties`、`android/app/build/...`、`.dart_tool/`、`build/`、`.idea/`
- **不要**在外层 PicaComic 仓库提交

---

> **如何使用本文档**：接到具体任务 → 在上方模块清单中定位相关模块 → 如该模块标注了 ⚠️，务必先打开 [经验思路版](./交接文档-经验思路版.md) 对应章节，查看已知的坑、正确的修法和涉及的文件路径。不要裸写代码。

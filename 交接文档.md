# PicaKeep 交接文档

## 1. 项目定位

`PicaKeep` 当前已经不是单纯的本地离线阅读器裁剪版，而是一个以本地漫画 / 本地图集 / 已下载管理为主线，同时开始落地客户端 / 服务端双模式的 Flutter 项目。

当前工作区中同时存在两个相关工程：
- `D:\Flutter_Projucts\PicaComic\PicaKeep`：当前主要维护目标。
- `D:\Flutter_Projucts\PicaComic\PicaComic`：原项目，对照 UI、阅读器行为、旧数据结构与兼容读取链路时会用到。

当前版本的实际主线已经分成两层：
- **本地层**：本地漫画、本地图集、已下载、收藏、历史、阅读器、设置。
- **远程层**：运行模式、服务信息、局域网扫描、远程资源库 / 远程已下载 / 远程图集、服务端本地 HTTP 与浏览器网页后台。

---

## 2. 当前阶段结论

当前可以认为已经稳定落地的能力包括：
- 本地图集 / 多路径本地漫画聚合
- 自定义图集资源路径“合集”外壳目录识别：每资源路径独立开关，开启后外壳目录合并为多章节图集，关闭时保留旧叶子目录扫描
- 已下载三档模式与本地阅读链路
- 收藏夹与历史记录兼容隔离
- 原项目恢复库与 `jm` 下载 JSON 兼容修复
- 客户端 / 服务端运行模式切换与服务信息页
- 远程服务状态读取、mDNS / Bonjour 与网段扫描双档发现、多服务候选选择、远程资源列表 / 详情 / 阅读链路
- 手机端阅读性能、沉浸式阅读、状态栏 / 导航栏样式修复，阅读器顶部黑条已在 Android 原生层收口
- “我”页远程资源库 / 图集双卡片稳定布局
- 远程已下载 / 远程资源库 / 远程图集展示与三档切换修复
- 远程删除 / 恢复后的回收站同步、服务信息页真实刷新、远程已下载回前台强制刷新链路已补齐
- 远程资源变更的 `librarySignature + WebSocket` 事件推送、服务端挡位热更新、阅读期延迟刷新与远程页单刷新按钮语义已收口
- Android 全部文件访问权限、Root 模式、长按内置目录浏览接入
- Root / Shizuku 授权检测、受限目录访问与历史记录封面回显链路修复
- 已下载页 Root / Shizuku 列表改为 `download.db` 快速构建，封面与索引走后台低速补齐
- 已下载页本地 / 聚合 / 远程·已下载三档加载链路已重新隔离，远程已下载不再被本地扫描失败或超时拖住
- 已下载页已补页面级加载超时：本地 8 秒、远程 10 秒，超时后进入可重试空态，不再一直转圈
- 已下载详情弹层在标签很多时已改为自动高度滚动显示，不再挤压按钮和章节区域
- Android 下 Shizuku 受限目录的“仅本应用下载目录”本地已下载读取修复已补到执行中方案：目录浏览归一化、本地列表改走受限路径回退链路、远程已下载隔离继续保持
- 已确认本地已下载在 Shizuku 下“扫描返回 0”的一层原因是 fallback 触发条件过于保守，只看路径存在，不看 `dart:io` 是否真的能列目录；另一层原因是无 `download.db` 时目录型回退仍会生成空壳条目
- Android 启动已改为原生启动页 + Flutter bootstrap 壳无缝过渡，“我”页首屏采用渐进加载与缓存复用，当前已回调历史记录预览为首帧即触发，避免外层卡片延后出现
- Android 启动优化已合并回 `main`，原收敛分支：`optimize/startup-performance`
- 应用内 `Logs` 页面已从 `app_settings.dart` 拆分到独立 `log_settings.dart`；`LogManager` 新增运行期“记录日志”开关，启动诊断 `AppStartupTrace` 现在会同时写入应用内日志与控制台，便于真机侧直接查看启动阶段事件
- 已下载封面显示策略已回退到更接近原项目的直接图片显示；远程 / Root 本地封面缓存已改为优先走应用数据目录缓存，避免缩略图失真、同名覆盖和滚动回滑后图片消失
- 设置页 `APP` 分组中的下载目录弹窗、内置目录浏览页与权限状态加载链路已做第二轮交互收敛，重点压缩键盘弹出、目录浏览和权限状态初始化阶段的卡顿
- 设置页 `APP` 分组已按最终信息架构收口：`日志`、`存储位置`、`数据管理`、`访问权限（Android）`、`隐私`、`其它`；原 `APP` 顶部“服务”区块已迁入 `APP能力`，本地图集图片排序与图集列表排序不再重复放在 `APP` 设置中
- `APP能力` 页现在承载客户端 / 服务端运行能力配置，并在底部提供 `APP能力-未来规划` 入口；原云端 / NAS 一体化规划内容已放入该子页
- 设置页 Root / Shizuku 状态检查已收口为“首次进入”和“从其他 App 切回该页”两类时机，滚动、重建、普通 `setState` 不再重复验权
- Shizuku 模式下对 `/storage/emulated/0/Android/data` 的 UserService + 包名并集补回方案已完成复核；在当前 MIUI 机型、仅 Shizuku ADB 授权（uid=2000 shell）条件下，`169` 项可视为可达上限。产品侧现已收口为在该根目录顶部显示一条可点击的 FUSE 限制提示；点击后弹出完整说明对话框，提供“关闭”和“不再提示”两个动作，其中“不再提示”通过 `SharedPreferences` 持久化到 `shizuku_fuse_limit_hint_dismissed`，完整结果仍需要 Root
- Root 授权结果已做会话内复用，实际文件操作不再先反复 `su -c id`
- 受限目录现在不再尝试“静默移入回收站 / 从列表移除”，权限不足会直接提示
- Android 下跨应用 `Android/data` / `Android/obb` managed-download 资源的删除、恢复、回收站链路已补齐：本地资源库删除不再因 `DownloadManager.init()`、`download.db` 写回或普通 `dart:io createSync()` 误触他人沙箱包根而抛 `PathNotFoundException`
- 目前桌面 / 手机真机已确认：对 `com.github.pacalini.pica_comic` 这类原应用下载目录中的 managed 条目，放入回收站、恢复、彻底删除都可走 Shizuku / Root 通道完成；删除后的 `reload()` 失败也不再伪装成删除失败弹窗
- 当前剩余已知噪音是 Shizuku UserService 读取较大的 `download.db` 时偶发 `DeadObjectException`，以及 `download.db` 残留行对应目录已不存在时的目录枚举失败日志；这两类目前会回退 / 跳过，不再阻塞删除成功路径
- Android 启动崩溃、sqlite 运行库、Manifest 回退告警修复
- 本地图集恢复后缓存刷新链路已收口：恢复只 evict 当前图集相关封面 / 图片路径，避免封面与第一页继续命中旧空缓存，同时不再清空全局图片缓存
- 本地库页收到本地数据变更后会强制刷新本地缓存；删除 / 放进回收站时先从列表乐观移除，避免目录移动后短暂显示 broken cover
- 工具页“清理缓存”入口已改为“缓存管理”，支持展开查看“缓存大小限制 / 清除缓存”；远程封面缓存会按缓存大小限制自动裁剪旧文件
- 工具页 `外显按钮与排序` 现在只控制“我”页外显按钮是否显示；工具列表本身始终显示全部项目，但按该排序配置排列；排序页拖拽提示图标改为三横线，长按拖拽延迟收敛到约 `500ms`
- 浏览器网页后台已完成从旧 `/admin` 原型到 `assets/web_console/` 静态 SPA 的迁移，支持真实登录、应用面板、管理面板、远程代理与旧 `/admin` 兼容入口
- 网页应用面板已支持本地 / 远程 / 聚合数据源、封面墙、详情页、连续滚动 / 翻页双模式阅读器、dashboard 历史续读，并已按 app「我」页风格改版
- 网页 dashboard 已补齐已下载、图集、资源库、图片收藏、工具等入口；其中已下载 = 漫画，图集 = `rootId.startsWith('custom_')` 的 album，图片收藏入口接 `/api/library/image-favorites` 真数据
- 网页后台账号系统仅服务浏览器端，支持 admin + 多用户；每个网页用户仅隔离阅读历史，客户端历史与收藏仍保持原逻辑
- 网页管理面板已扩展回收站、收藏夹、图片收藏、本地资源批量操作、配置校验、日志过滤与接口清单
- Linux 桌面构建已修复 `sqlite.org` 下载卡死问题：sqlite3 合并源码（`sqlite3.c` / `sqlite3.h`）已捆绑到 `linux/sqlite3/`，`linux/CMakeLists.txt` 通过 CACHE 变量跳过 `FetchContent` 下载，`flutter build linux --debug` 可直接完成（详见精简版 / 经验版 §25）

当前阶段已完成静态与构建校验：
- `flutter analyze`：0 issues
- `flutter build apk --debug`：通过
- `flutter build linux --debug`：通过（需 Linux 环境或远端构建机）

最近一次源码提交：
- 当前仓库最近一次正式提交基线为 `a77873c V1.9.52 AI日志脱敏与缓存前缀优化`
- 其后工作区仍有第十二轮 05 号计划的源码、测试和交接文档改动，最终以 `git -C D:\Flutter_Projucts\PicaComic\PicaKeep status --short` 为准

上面这条提交记录会作为当前阶段的新基线；其后若继续围绕 Android 前台服务、移动端输入、局域网扫描或日志桥接做调整，仍应以 `git -C D:\Flutter_Projucts\PicaComic\PicaKeep status --short` 为准。

### 本会话计划与执行索引

- `继续定位远程资源_401_d05b2fa1.plan.md`：远程资源 401 回归定位计划，已取消继续诊断；最终边界是 `/status`、`api/library`、`api/library/*`、`api/events` 放行，只保护 admin/console/proxy。
- `图集资源路径合集开关计划_08f414dc.plan.md`：早期较宽泛的图集资源路径合集开关计划，后续被更精确的“合集外壳目录识别”计划替代。
- `图集合集外壳目录识别计划_5c3a4ec8.plan.md`：已执行；完成每资源路径“合集”开关、本地/服务端/远程扫描与切换接口、图二顶栏开关、数字目录项目名修复、章节名前缀清理和“序号”开关恢复。
- `mDNS_与网段扫描修复计划_1757ef93.plan.md`：mDNS / Bonjour 与网段扫描初版修复计划；用于确认 Windows mDNS 多网卡风险和网段扫描多服务目标。
- `mDNS_与网段扫描修复计划_48e20d37.plan.md`：已执行网段扫描部分；修复 `service_data_source.dart` 中命中当前地址后提前 `break` 的问题，显式“网段扫描”现在可聚合同网段多个 `/status` 服务。
- `mDNS_查询回复单播修复`：已执行；`_sendResponse` 查询回复单播到源地址 + client query 加 QU 位，解决 Windows 多网卡 mDNS 漏扫。
- 第八轮 45/46/47 号计划：已完成，交接细节见精简版与经验版 §78-§80。
- 第九轮 02/03/05/06/07 号计划：代码与自动化验证已完成；04 号计划仍标记部分完成，真机/真实网络验证缺口见经验版 §81-§84。
- 第十轮 00/01/02：01 号代码验证完成，02 号计划部分完成；真实 EH 记录、实体机、横屏/大字体验收待补，见经验版 §85。
- 第十轮 03：本地详情布局、EH 本地在线详情/更新信息、关于页代码与自动化验证完成；真实 EH 警告/写库、外部链接启动和设备端验收待补，见本篇 §110、经验版 §86。
- 第十一轮 01：AI 结果清单参数规范化、工具失败重试、旧会话兼容和两个清单入口代码与自动化验证完成；截图对应会话完整冷启动、真实模型随机错误参数重试、Android 10 和真实网络日志待补，见本篇 §111、经验版 §87。
- 第十二轮 00/01/02/03：00 号诊断已完成；01 凭据脱敏代码闭环但旧 Key 未轮换，02 缓存前缀改造存在范围语义回归，03 验收确认总体部分通过，见本篇 §112-§114、经验版 §88-§89。
- 第十二轮 04/05：04 号远程资源代理超时诊断完成；05 号已完成按 URI 代理分流、HTTP transport/cache generation、WebSocket 所有权与设置/前台重建接线，自动化验证通过，但真实代理/TUN/VPN 四组矩阵和 WebSocket 竞态专测待补，见本篇 §115、经验版 §90。

当前工作区中与“是否应纳入源码提交”相关的区分，先按下面口径处理：

### 不应纳入本轮源码提交
这类属于本地环境、构建缓存、IDE 产物、日志或过程性草稿：
- `android/local.properties`
- `.dart_tool/`
- `.flutter-plugins-dependencies`
- `.idea/`
- `android/.gradle/`
- `android/.kotlin/`
- `build/`
- `windows/flutter/ephemeral/`
- `flutter_01.log`
- `flutter_02.log`
- `picakeep.iml`
- `android/picakeep_android.iml`
- `packages/local_auth_windows_stub/.dart_tool/`
- `plan/`
- `android/app/build/...`
- `android/app/build/windows/...`

### 可视为正常源码改动候选
这类属于功能实现、配置或文档本体，是否提交取决于本轮功能边界，而不是因为它们是本地垃圾：
- `android/app/build.gradle.kts`
- `android/app/src/main/kotlin/lingxue/picakeep/MainActivity.kt`
- `android/app/src/main/aidl/`
- `android/app/src/main/kotlin/lingxue/picakeep/shizuku/`
- `lib/foundation/*.dart`
- `lib/pages/**/*.dart`
- `lib/server/*.dart`
- `lib/main.dart`
- `pubspec.yaml`
- `pubspec.lock`
- `交接文档.md`
- `交接文档-精简最新版.md`
- `交接文档-经验思路版.md`

### 额外说明
- `README copy.md` 只是临时副本，已确认删除，不再保留。
- `android/gradlew`、`android/gradlew.bat`、`android/gradle/wrapper/gradle-wrapper.jar` 当前仍被仓库规则忽略；它们不是本轮“本地垃圾清理”的目标，是否调整版本化策略可后续单独决定。

仍未彻底收尾的事项主要还有 4 个：
- `android-foreground-service`：Android 前台服务 / 通知 / 保活约束适配仍挂起
- `mobile-ui-input-fixes`：移动端服务信息页、地址输入，以及不同 ROM 下少量系统栏一致性细节仍待继续收敛
- `lan-scan-followup`：已完成 Windows 多网卡 mDNS 查询回复单播修复（`_sendResponse` → 源地址）；仍需真机复核路由器 mDNS 隔离、Android 组播权限场景
- `verify-ui-hint`：仍需真机确认仅 Shizuku 模式进入 `/storage/emulated/0/Android/data` 时提示条出现、可点开完整说明、点“关闭”后仍保留、点“不再提示”后立刻隐藏且重启后仍不出现；并确认 Root / 普通模式下不出现

---

## 3. 本阶段已完成内容

### 3.1 本地漫画 / 图集聚合层

已完成独立本地库聚合层，支持统一索引以下来源：
- 本应用下载目录
- 原应用下载目录
- 自定义本地漫画路径

支持能力：
- 三类来源统一扫描与聚合显示
- 单图集目录 / 递归总目录两种形态识别
- 叶子图片目录识别为图集
- 自定义图集资源路径支持按路径独立开启“合集”外壳目录识别；开启后外壳目录合并为一个多章节图集，关闭时保留旧叶子目录扫描
- 合集图集命名已收口：数字章节目录不再作为外部项目名，章节名不再重复拼主文件夹名，合集/远程多章节图集恢复“序号”开关
- 封面优先 `cover.*`，否则回退首张图片
- 存储空间统计与路径占用统计
- 本地图集只读浏览，不直接删除源文件
- 详情页与阅读器尽量复用原有本地链路

### 3.2 本地 UI 接入

已完成以下本地入口和页面接入：
- “我”页：历史记录、已下载、图集、远程·资源库、图片收藏、工具
- “我”页工具卡片现已收口为一体式卡片：卡片底部外显按钮改为卡片内横向滚动条，超出宽度时通过右侧渐变遮罩过渡；长按工具卡片可直接进入“工具”页的 `外显按钮与排序` 模式
- “工具”页：
  - 服务信息
  - 本地文件管理
  - 存储空间
  - 图集
  - APP能力
  - 回收站
  - 清理缓存
- “工具”页标题栏右侧已新增 `外显按钮与排序` 入口；进入后可对全部工具项做点按显隐与长按约 `800ms` 拖拽排序，选中态遮罩风格与已下载 / 图集页多选视觉保持一致
- 外显按钮配置已改为共享持久化模型，默认首批显示为“服务信息 / 本地文件 / APP能力 / 回收站”，但全部工具项都可重新选择和排序
- 本地库页复用已下载页的网格、排序、搜索、空态交互风格
- 阅读器底栏新增本地图集图片排序入口

### 3.3 设置、运行模式与服务信息页

已完成：
- 运行模式设置：客户端 / 服务端，当前入口已归位到 `APP能力` 页
- 服务端地址、后台端口、发现策略等基础配置，当前与运行模式一起集中在 `APP能力` 页展示
- APP能力页面已从单纯规划页调整为“服务能力配置 + 未来规划入口”：主页面展示运行模式、当前平台服务端能力、局域网发现方式、客户端服务端地址 / 服务端后台端口、服务信息入口；底部 `APP能力-未来规划` 子页承载目标形态、技术骨架、基础约束与阶段路线
- 设置页 `APP` 分组现在聚焦本地应用数据与隐私：`日志`、`存储位置`、`数据管理`、`访问权限（Android）`、`隐私`、`其它`；其中 `数据管理` 位于 `访问权限（Android）` 之前
- 服务信息页接入统一状态模型，展示：
  - 当前模式
  - 服务状态
  - 状态接口地址
  - 后台地址
  - 漫画数量 / 资源体积 / 请求数等摘要
- 客户端模式下，服务端地址已经实际参与状态读取与页面展示
- 服务信息页的“刷新状态”已接通真实远程 `/api/library/items/refresh` / 本地资源重扫，且已补“断开连接”入口
- 服务信息页客户端连接卡片的“断开连接”已移动到标题栏右侧；“扫描局域网”和“刷新状态”补了执行中转圈反馈与防重复触发

### 3.4 远程服务与后台原型

已完成：
- 本地 shelf HTTP 服务运行时骨架
- 桌面端服务模式真实启动 / 停止 / 端口绑定
- 服务端资源扫描器 `LocalResourceScanner`
- 后台网页原型与后台登录预览页
- 服务状态 / 本地资源概览接口
- 远程服务状态读取数据源 `service_data_source.dart`

当前远程接口已经能提供：
- `/status`
- `/api/library/items`
- 项目详情 / 封面 / 章节图片读取
- 后台扫描入口等基础能力

### 3.5 远程阅读与远程资源链路

已完成远程数据源抽象与远程阅读主链路：
- 远程资源库列表
- 远程已下载列表
- 远程图集列表
- 远程项目详情
- 远程图片流阅读

当前远程数据结构已经统一到：
- `RemoteLibraryRootItem`
- `RemoteLibraryComicItem`
- `RemoteLibraryReadingData`

### 3.6 本轮功能性修复

本轮继续收敛并已完成的修复包括：

#### A. 局域网发现与网段扫描
- `mDNS / Bonjour` 与 `网段扫描` 已分成两个独立挡位；mDNS 是真实 UDP 5353 零配置发现，网段扫描保留旧 `/status` 探测
- 两个入口（服务信息页、APP能力页）共用同一发现策略与“mDNS 失败自动网段扫描”开关
- 显式“网段扫描”会优先探测当前已配置地址，但命中后仍继续扫描当前候选网段，最终聚合多个有效 `/status` 服务
- 多服务候选 2+ 时弹选择窗口，候选来源会显示 `mDNS / Bonjour` 或 `网段扫描`；只有最终 1 个候选时才自动应用地址
- Windows 服务端 mDNS 漏扫：已修复——查询回复改为单播到源地址（绕过 Windows 多网卡组播路由错误）。排查顺序：① `_sendResponse` 是否走单播 ② 多网卡组播加入 ③ A 记录过滤 ④ 防火墙

当前目标是保留真实 mDNS / Bonjour，同时让独立“网段扫描”能作为可选挡位和兜底路径发现多个服务。

#### B. 阅读器沉浸式与系统栏
- 阅读页 Flutter 侧已补齐沉浸式显隐、顶部 padding 去除与 `viewPadding` 对齐
- 最终根因确认不只在 Flutter 层，还包括 Android 原生窗口未完整启用 edge-to-edge / cutout 配置
- 已补齐 `NormalTheme` 的 `windowLayoutInDisplayCutoutMode`、透明状态栏与透明导航栏配置
- `MainActivity` 已强制 `setDecorFitsSystemWindows(window, false)` 并设置系统栏瞬时唤出行为
- 完整重装后，阅读器顶部黑条问题已在真机确认恢复正常

#### C. 远程已下载 / 远程资源库 / 远程图集展示修复
- 远程已下载不再把整个下载根目录错误识别成一个“多章节大项”
- 已改为优先扫描子目录漫画列表
- 远程图集首页改为直接展示远程图集列表，不再先显示成整根目录一个图集
- 远程资源库继续保留目录进入方式，但进入后落到漫画 / 图集列表，而不是错误章节化展示

#### D. 三档切换卡死 / fixed-length list 问题
- 已修复远程已下载 / 资源库 / 图集在三档切换时的崩溃风险
- 关键点是避免在切换 / 排序链路里对 fixed-length list 做清空或可变操作

#### E. Android 运行问题
- 启动白屏崩溃已定位到 sqlite 运行库缺失并修复
- Android Manifest 已补：`android:enableOnBackInvokedCallback="true"`
- 相关构建链路已重新校验通过

#### F. “我”页与阅读器 UI 收尾
- 阅读页主体已移除顶部继承 padding，阅读器顶部返回按钮与工具栏顶部偏移已统一基于 `viewPadding`
- Flutter 层与 Android 原生层联合修复后，阅读器顶部黑条问题已关闭
- “我”页的“图集”与“远程·资源库”卡片现在无论远程是否在线都保持同一行
- 远程离线时改为展示不可点击占位卡片，而不是让“图集”独占整行

#### G. 设置页目录选择与 Android 权限接入
- “重新扫描磁盘”能力已收敛回现有设置页，不再单独新建页面或额外入口
- 设置页已补“安卓全部文件访问权限”状态展示与系统设置跳转
- 设置页已补“Root 模式”开关；开启时会直接尝试申请 `su`
- “本应用下载目录”与“原应用下载目录”弹窗中的“浏览”按钮支持长按
- 长按后进入应用内置文件夹浏览页，仅在已授予全部文件访问权限或已开启 Root 模式时可用
- Android 侧已新增 `MANAGE_EXTERNAL_STORAGE` 权限声明与 `storage_access` 通道

#### H. 本地文件管理页回退
- 已移除本地文件管理页里此前新增的 Root 路径入口与相关说明
- 本地文件管理页现在只保留普通目录选择与路径管理
- 受限目录改为统一在设置页的下载目录弹窗中长按“浏览”处理

### 3.7 历史、收藏夹与恢复库兼容修复

已完成：
- 收藏夹列表页多选与批量删除
- 多选态只保留高亮与点击切换，不再叠复选框层
- 收藏夹名称映射与跨来源读取兼容修复
- 本应用 / 原应用切换时同名收藏夹冲突修复
- 本地图集历史标签与来源显示修复
- 原项目 `download.db` 中 `jm` 下载 JSON 嵌套结构兼容输出修复

关键结论仍然成立：
- 原项目读取异常的核心根因不是目录数，而是 `download.db` 中 `jm` JSON 结构不兼容
- 当前项目已经把该兼容问题收敛到 `download_model.dart` 输出侧

### 3.8 本轮稳定性 / 启动 / Root 收口

已完成以下补充收口：

#### I. Root / Shizuku 权限与目录访问链路
- 设置页 Shizuku 已安装 / 运行 / 授权状态检测已修复，不再出现已授权却显示未连接或不可用
- Root 已授权但本地已下载扫描为空的问题已修复
- Android 平台通道中的 Root / Shizuku 检测、目录读取、文件读取已移出主线程并补超时，避免首屏或列表期卡死
- `/data/user/0` 这类受限路径现在走统一的平台访问链路，不再把主线程阻塞误判成单纯“目录不存在”

#### J. 已下载页性能与列表构建
- 已下载页首屏不再同步做本地 / Root / Shizuku 全量扫描
- Root / Shizuku 列表改为优先使用 `download.db` 行数据直接构建，不再首屏同步扫封面和章节目录
- 缺失本地文件的记录会继续显示，但会跳过后台封面解析，避免整项丢失或反复报错
- 封面与目录索引缓存已落到应用数据目录，后台低速补齐；进入阅读器时会暂停列表封面预热，避免抢占 I/O
- 作者、标签、来源等元数据已恢复优先从 `download.db` 的 `title`、`subtitle`、`tag`、`tags` 等字段回填

#### K. 历史记录 / 阅读器补充修复
- Root / Shizuku 模式下历史记录封面回显已修复
- 图集封面图比例、阅读页图像比例错误的回归点已修复
- 手机端图集阅读页系统手势返回无法退出的问题已修复

#### L. Android 启动优化分支补充说明
- 当前启动优化分支 `optimize/startup-performance` 已完成合并，后续基线以 `main` 为准
- “我”页仍保留下载数、本地图库数、图片收藏数、远程摘要等渐进延后加载
- 历史记录预览已回调为首帧即触发，避免为了压启动期任务而让历史记录卡片在首屏外层长时间不出现

#### M. 远程回收站 / 远程已下载 / 服务信息同步收口
- 服务端资源扫描已显式跳过 `.picakeep_trash`，普通列表、根目录数量与体积统计不再混入已移入回收站的目录
- 远程图集内容删除后，客户端根列表与内容列表都会跟随缓存失效和强制刷新，不再保留旧数量
- 服务端回收站条目已补 `itemKind`，客户端回收站现在可以稳定区分远程漫画与远程图集
- 服务端“回收站-本地”视图也会显示经由手机客户端移入回收站的远程漫画 / 图集，不再漏掉远程删除条目
- 服务端本地恢复 / 彻底删除回收站条目时，已统一走 `LocalServerRuntime -> PicaKeepAdminServer` 的运行态链路，避免 `library_trash.json` 与 HTTP 服务内存缓存分裂，导致手机端远程回收站残留旧记录
- `TrashPage` 已补页面恢复、前后台切换与本地 / 远程、漫画 / 图集分段切换时的主动重载
- 服务信息页的“刷新状态”已改为真实触发远程 `/api/library/items/refresh` 或本地资源重扫，不再只是刷新卡片文案；同时已新增“断开连接”按钮，并补了扫描局域网 / 刷新状态执行中的转圈反馈与防重复触发
- `DownloadPage` 的远程已下载入口显示条件已放宽：只要已配置远程地址即可显示远程相关入口，不再依赖当前必须处于客户端模式
- 已配置远程地址但当前未连接时，远程已下载页会直接展示未连接提示，而不是静默空白
- `DownloadPage` 的远程已下载页已补“下一次强制远程刷新”标记；在回前台、重新成为当前路由、重新进入远程已下载时，会绕过客户端单例缓存重新拉服务端快照，避免必须先去服务信息页手动刷新才能看到已恢复漫画
- 已下载页排序弹窗已改为局部状态同步，切换排序方式和倒序开关后会立即刷新控件显示
- 远程已下载 payload 已补稳定时间字段，客户端与服务端的时间排序规则已统一
- 新增第二阶段修复：服务端 `/api/library/trash` 现在会聚合 `LibraryTrashStore` 与 `LocalTrashStore`，只暴露落在当前 managed/custom root 内、且 `trashedPath` 仍存在的本地回收站条目；手机端“远程回收站-漫画/图集”因此可以看到 PC 端桌面删除出来的 managed 资源
- `/api/library/trash/{id}/restore` 与 `DELETE` 已按 id 前缀分发：`srvtrash_*` 继续走 `LibraryTrashStore`，其余本地条目委托 `TrashManager` 恢复/彻底删除，再统一触发 `rescanResources()` 广播 `library-changed`
- `/api/library/items/{id}` 的远程彻底删除，以及服务端 `/trash` 路径下的移入回收站，现已同步清理对应 `download.db` 行，避免“目录已删但客户端仍显示数量/列表”的 ghost 条目残留
- `trash.dart` 中 PC 桌面端对本地回收站的移入、恢复、彻底删除操作，在 server 模式下会额外触发 `LocalServerRuntime.instance.markResourceStateDirty()`；因此桌面侧操作也能把 `librarySignature` 推到手机客户端，而不再只能靠手动刷新或重启服务看见变化
- Android 下针对原应用 `Android/data/com.github.pacalini.pica_comic` 这类跨应用 managed-download 目录，删除链路已补第三阶段收口：`TrashManager` 的移入回收站 / 恢复 / 彻底删除分支现在会识别“他人应用沙箱”路径并强制改走 Shizuku / Root 的 move / delete / exists / write 通道，不再让 `dart:io` 的 `existsSync()`、`createSync()`、`rename()` 或 `delete()` 抢先命中
- 删除前构建回收站快照时，`LocalLibraryComicItem` 已不再为了补 snapshot 再次初始化 `DownloadManager`；同时 `DownloadManager._getPath()` 对他人 `Android/data` / `Android/obb` 包根不再直接 `create(recursive: true)`，而是改成 privileged exists 检测，避免在真正移动目录前就因误建包根抛 `PathNotFoundException`
- `trash.dart::_mutateSourceDbFile()` 写回受限 `download.db` 时也已改为跨沙箱优先走 privileged write，不再先 `file.parent.createSync(recursive: true)`；并补了“下载根 / 包根不可作为漫画目录删除”的防护，以及删除后 `reload()` 失败只记日志、不再覆盖删除结果弹窗的处理。因此当前真机上 `com.github.pacalini.pica_comic` 下载目录中的 managed 条目已经确认可以放入回收站、恢复、彻底删除
- 当前剩余已知噪音主要有两类：一是 Shizuku UserService 读取较大 `download.db` 时偶发 `DeadObjectException`，二是 `download.db` 残留记录对应目录已不存在时的目录枚举失败日志。两者目前都不会再阻塞删除成功路径；后续若继续做稳定性优化，优先考虑把 `readFileWithShizuku` 改成分块读取，并进一步收敛 ghost row 清理策略
- 当前仍保留“两套底层 store + 服务端聚合视图”的策略，以兼容已有 `LocalTrashStore` 数据和原始 `download.db` 恢复能力；若后续要进一步统一语义，可把 managed-download 条目彻底迁移到单一 `server-trash` 模型

#### N. 已下载三档隔离与加载超时
- `DownloadPage` 的本地、聚合、远程·已下载加载入口已拆分为独立分支；远程·已下载不再预先执行本地 `DownloadManager().init()` 或本地扫描
- 本地加载只在本地档 / 聚合档本地分支触发；远程加载只在远程档 / 聚合档远程分支触发，单侧失败不再拖垮另一侧
- 页面级超时已落地：本地分支 8 秒，远程分支 10 秒；超时或失败时会进入明确的空态提示并提供“重新加载”
- 聚合档会合并成功侧结果；如果本地超时但远程成功，仍展示远程条目，反之亦然
- 本轮已同步更新交接文档，并将提交信息调整为中文版本号格式 `V1.8.15 隔离已下载三档并增加加载超时`

#### O. 回收站 / 常亮插件 / Root 收口补充
- 本地回收站现在会在移入时真正删除源 `download.db` 中对应的数据行，并把 `sourceDbPath`、`sourceDbId`、`sourceDirectory`、`sourceDbTimeMillis` 等信息一起存入回收站记录，恢复时可以按原始来源写回
- 恢复时会优先按原来的 `time` 写回，这样列表顺序通常能回到原位置；只要源库还在，恢复就不会变成“只插回一条新库记录”
- 之前遇到的“受限 `download.db` 读不到就误判成新库，最后恢复只剩一条”的问题已修正：现在会先区分“不可读”和“真的不存在”，避免把原库误建成空库
- 普通删除、Root 删除与 Shizuku 删除现在都沿同一套受限路径判断走，权限不足时直接提示，不再装作成功
- 阅读设置页的 `keepScreenOn` 平台通道已补齐，`MissingPluginException` 的常亮报错已修掉

#### P. 远程资源热同步与挡位热更新收口
- 服务端已补 `librarySignature` 与 `LibraryEventBus`，`/status`、`/api/library/items`、`/api/events` 三条链路现在会共享同一份远程资源版本信号
- `PicaKeepAdminServer` 已增加运行态 `applyConfig(...)` 热更新入口；当服务端挡位、下载目录、自定义本地路径等设置变化但 `host/port` 不变时，会直接更新运行中配置并重扫，无需再靠“重启服务”让客户端看到变化
- `LocalServerRuntime.refreshResourceState()` 已从“只 rescan”改为“先推送最新 effective config，再决定热更新或重启”，修掉了“服务端切换数据管理挡位后，必须重启服务客户端才会变”的根因
- 客户端远程同步已从单纯的“回前台强刷”扩展为 `WebSocket + librarySignature` 失效：远程资源变化时会主动触发 `App.serviceRuntimeVersion`，列表和摘要页可自动失效重拉
- 阅读页期间新增阅读门闸：收到远程变更时先缓存 `pendingSignature`，退出阅读后再统一刷新，避免阅读过程被远程列表刷新打断
- `RemoteLibraryEventChannel` 已补前后台生命周期接线、指数退避重连、25 秒心跳、30 秒陈旧超时，以及长时间连不上时按 8 秒节流拉 `/status` 比对签名的降级探测
- 图集 / 资源库页顶部原来重复的 `Icons.refresh` 与 `Icons.sync` 已合并为单个刷新按钮；远程视图下统一表达“强制远程拉取最新快照”，本地视图下仍保持原本地刷新语义
- `DownloadPage` 的远程·已下载刷新入口继续保留，并与新的签名失效链路共存：自动推送负责常态同步，手动刷新负责主动兜底

### 3.9 浏览器网页后台与应用面板

已完成网页后台大工程计划一至计划六的主体落地，当前网页端不再只是旧 `/admin` 原型，而是由 `assets/web_console/` 承载的浏览器 SPA。

#### A. 静态 SPA 与入口
- `pubspec.yaml` 已注册 `assets/web_console/`、`assets/web_console/css/`、`assets/web_console/js/`，确保网页资源被打进 Flutter assets。
- 服务端通过 `lib/server/web_console/web_asset_loader.dart` / `web_console_handler.dart` 从 Flutter assets 提供 `assets/web_console/` 静态资源。
- 旧 `lib/server/admin_web.dart` 的 Dart 字符串控制台已迁移删除，旧管理页逻辑平移到 `assets/web_console/js/admin.js` / `css/common.css` / `css/admin.css`。
- `server_app.dart` 的路由顺序必须保持：先处理 `/api/*`、`/status`、`/api/events`，最后才让 `web_console_handler` 接管静态资源与 SPA fallback。
- 旧 `/admin` 保留兼容；当前推荐入口为：
  - `/` 或 `/app`：应用面板
  - `/admin-view`：管理面板
- 非 API / 非静态资源路径走 SPA fallback，刷新 `/admin-view` 不应 404。

#### B. 网页地基契约与远程代理
- `assets/web_console/js/common.js` 暴露的 `window.PicaKeepConsole` 是所有网页前端代码的唯一共享契约：
  - `api.get/post/put/del/imageUrl`：访问本机服务端，自动带 token，图片 URL 用 `?__token=`。
  - `remote.get/imageUrl`：经 `/api/remote/proxy` 访问另一台 PicaKeep 服务端。
  - `source`：应用面板内本地 / 聚合 / 远程数据源，持久化到 localStorage。
  - `remoteTarget`：当前远程服务端地址，持久化到 localStorage。
  - `auth`：token、user、admin 判断、登录态刷新。
  - `history`：网页 per-user 阅读历史。
- 服务端已补 CORS / OPTIONS；`OPTIONS` 返回 204，并暴露 Range 相关响应头。
- `/api/remote/proxy` 只允许 GET，转发 Range/Accept，剥离 Cookie/Authorization，带超时；这是浏览器规避跨域访问另一台 PicaKeep 服务端的基础能力。
- `/status` 继续免登录用于探活；登录接口与静态资源免登录；其它受保护接口走 token 鉴权。
- 空后台密码允许首启使用，但登录页、状态与日志应提示局域网风险。

#### C. 网页账号与历史
- 新增网页专用账号系统：admin + 多用户，仅用于浏览器后台。
- token 绑定用户与密码哈希，改密码后旧 token 自动失效。
- 独立数据库 `web_console.db` 存网页用户和 per-user 阅读历史；不复用客户端 `history.db`。
- 隔离范围仅历史：漫画库、收藏夹、图片收藏、回收站仍共享服务端数据。
- 登录页已改成用户名 + 密码；空用户名兼容旧 admin 登录；空后台密码仍允许首启但有风险提示。

#### D. 应用面板
- `assets/web_console/js/app.js` 现在是应用面板小型 SPA：dashboard → grid → detail → reader。
- 支持本地 / 远程 / 聚合数据源，远程图片和 JSON 必须经 `PicaKeepConsole.remote.*` 与服务端代理。
- 封面墙、详情页、阅读器均已接本地/远程/聚合 adapter；图片 URL 必须用 `api.imageUrl(...)` 或 `remote.imageUrl(...)`，不能直接使用相对路径。
- 阅读器支持连续纵向滚动与翻页模式，模式写入 localStorage；翻页/滚动会节流写回 `/api/library/history`。
- dashboard 历史条从 `GET /api/library/history` 读取，点击可按 ep/page 续读。

#### E. dashboard 与 app「我」页对齐
- 顶栏已去掉“我全都要”，只保留“应用面板 / 管理面板”，并做成可折叠抽屉。
- dashboard 已按 app「我」页布局改版：历史卡、已下载、图集、资源库、图片收藏、工具胶囊，卡片为描边透明底 + 成对网格 + chevron。
- grid/list 已补返回按钮，避免从 dashboard 点入口后无法返回。
- 已下载与图集的分流规则：
  ```text
  item.rootId.startsWith('custom_') == true  → 图集
  item.rootId.startsWith('custom_') == false → 已下载 / 漫画
  ```
  这条规则来自客户端远程数据源逻辑，网页端直接复用，不需要新增服务端字段。
- 图片收藏入口已接 `/api/library/image-favorites` 真数据，当前以缩略图浏览为主；若后续要点击直接进阅读器，需要额外拉详情拿 `pages[]`。
- `PicaKeepConsole.history.remove(target)` 已改成 `DELETE /api/library/history?target=...`，避免路径段契约与服务端不一致导致 404。

#### F. 管理面板
- `assets/web_console/js/admin.js` / `css/admin.css` 已从旧管理概览扩展为完整管理面板。
- 新增 tab：回收站、收藏夹、图片收藏，均按需懒加载。
- 回收站支持单项/批量恢复与彻底删除；收藏夹支持新建、重命名、删除夹、查看夹内 items、删除单条；图片收藏支持缩略图展示与删除。
- 本地资源块支持单项/批量移入回收站、直接删除；服务配置增加 host/port 校验；日志支持刷新、级别过滤和关键词过滤；接口页补齐资源批量、收藏夹、图片收藏、回收站路径。

#### G. 网页后台维护注意
- 网页前端契约集中在 `assets/web_console/js/common.js` 的 `window.PicaKeepConsole`。
- 改网页后台时优先只动 `assets/web_console/`；若涉及账号 / 历史 / 静态资源 / 代理，再动 `lib/server/web_console/` 与 `lib/server/server_app.dart`。
- 不要把网页账号扩散到 Flutter 客户端；除非后续明确需求，否则客户端仍无账号体系。
- 不要恢复“我全都要”档；当前信息架构是应用 / 管理两档 + 独立地址。
- 不要把图片收藏入口重新指向普通 grid；该入口已经要求接真数据。
- 不要把已下载与图集按标题、路径或 sourceDisplayName 猜测，统一按 `rootId.startsWith('custom_')`。

#### H. 网页 Console UI / Reader 多轮收口（后续已执行）

在计划一至六地基之后，又执行了多轮把 reader / 侧栏向 Flutter 客户端对齐的计划（`plan/后端网页/` 下二次修复、阅读器 App 化复刻、侧栏回归/四次补齐、UI 微调一二三、两轮验收补修）：

- 覆盖范围：dashboard 卡片、grid 详细/列表视图、详情页推荐区、详情侧栏（桌面右贴边面板 / 移动端底部 sheet）、reader App 化（上下工具栏、阅读设置、连续拼接/翻页、主题化背景、缩放）、左侧导航迁移、reader-active 布局修复。新增推荐接口 `/api/library/items/{id}/recommendations`（算法在 `server_app.dart`）。
- **关键事故与排查口诀**：reader 整页空白的根因是 shell/grid 下 `#console-root` / `[data-reader-root]` / `.reader-strip` / `.reader-img` 被压成 0 宽，**不是图片加载失败**。当时 DOM 图片数、network 200、`naturalWidth/naturalHeight` 都正常。以后排查 reader 空白要**同时看 DOM 图片数、network 状态、naturalSize 和父级 `getBoundingClientRect().width`**。
- **点击唤不出上下栏**的两个根因：连续滚动模式的边缘翻页判定误用整条长 reader 高度（应改用视口高度 + `event.clientY`）；指针释放链路（`releasePointerCapture`→`lostpointercapture`、document `pointerup`、`blur`）会误清单击 `tapTimer`，需区分"温和释放（不清 tapTimer）"与"取消（可清）"。交互对齐 `lib/pages/reader/touch_control.dart`。
- **缩放**作用于 `.reader-zoom-layer`（包裹 `.reader-strip`）而非单张 `.reader-img`，避免单页 `overflow:hidden` 裁切，origin 固定到点击点，放大后可随指针拖动平移（`zoom.panX/panY` + `--reader-zoom-pan-x/y`，按缩放层尺寸 clamp）。
- **侧栏/上下栏视觉**对齐 `download_page.dart`、`side_bar.dart`、`tool_bar.dart`：顶栏返回+标题+设置，底栏上下章+滑条+页码（左下角）+全屏+收藏+自动翻页+章节+保存（去分享），章节区双列（窄屏回退单列）。
- **阅读设置面板 App 化**：阅读模式为可折叠列表项（`methodPickerOpen` + `.reader-method-list`），其余为「图标+标题+pill 开关/滑条」三段式；新增「图片最大显示宽度」滑条（prefs `maxWidthPx`，默认 980，范围 600–1600，写 reader root 的 `--reader-max-width`），客户端同步在 `lib/base.dart` settings index 116 + `reading_settings.dart` Slider + `image_view.dart` 应用。
- **全局 ESC**：`app.js` `onGlobalKeydown` 分级——reader 设置/章节面板 → 侧栏 inspector → 退出 reader → 全局 `back()`；输入框聚焦时不抢占。
- **验证注意**：本地阅读链路先把数据源切到「本地」，否则远程自代理 `127.0.0.1:9527` 会 502/401 干扰。
- **后续收口**：网页阅读历史保存（ESC 退出补写）、滚动模式从历史定位记录页、历史独立页对齐「已下载」（双档切换 + 搜索 + 作者/标签前端 join 补全 + 标题 3 行截断）见 [§37](#37-2026-06-03-网页阅读器历史保存--滚动恢复--历史页对齐已下载)。

---

## 4. 关键文件锚点

### 4.1 配置 / 模式 / 状态层
- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\base.dart`
  - 应用设置持久化
  - 下载目录 / 原应用目录 / 服务端地址 / 运行模式等配置入口

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\foundation\app.dart`
  - `AppStartupTrace` 启动阶段诊断
  - 启动日志现在同时输出到控制台和应用内 `LogManager`
  - 全局导航、运行态通知、`toolDisplayConfigVersion` 刷新通知与平台判断

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\foundation\log.dart`
  - 应用内日志缓冲、长度限制、导出数据源
  - `recordingEnabled` 运行期开关控制后续日志是否继续写入

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\pages\settings\log_settings.dart`
  - 设置页 `APP > Logs` 独立日志页面
  - 日志记录开关、清空、禁用长度限制、导出与复制单条日志

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\foundation\app_runtime_mode.dart`
  - 运行模式、服务发现方式、后台端口等基础常量与归一化逻辑

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\foundation\service_data_source.dart`
  - 客户端 / 服务端状态读取
  - 局域网扫描实现
  - 远程服务状态模型

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\pages\settings\app_settings.dart`
  - 下载目录 / 原应用目录 / 重新扫描磁盘 / Android 权限 / Root 模式 / 内置目录浏览入口

### 4.2 本地数据与下载层
- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\foundation\download.dart`
  - 单路径 `DownloadManager`
  - 已下载读取、删除、封面、章节图片定位

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\foundation\download_model.dart`
  - `DownloadedItem` 抽象
  - 各类本地下载模型
  - `download.db` 行数据、作者 / 标签 / 来源字段兼容输出

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\foundation\local_library.dart`
  - 本地漫画 / 图集聚合层
  - 来源扫描、索引与统计
  - 后台封面 / 目录缓存补齐与进入阅读时暂停预热

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\foundation\history.dart`
  - 历史记录持久化
  - 多来源目标映射与读前写入辅助

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\foundation\local_favorites.dart`
  - 收藏夹映射、来源切换、候选目标解析

### 4.3 远程数据、删除 / 回收站与页面层
- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\foundation\remote_library_data_source.dart`
  - 远程列表 / 根目录 / 详情 / 阅读数据抽象
  - 远程快照缓存、`librarySignature` 与 `forceRefresh` 强制拉取入口

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\foundation\remote_library_event_channel.dart`
  - 远程资源事件通道
  - WebSocket 连接、签名比对、阅读期延迟刷新、前后台重连与 `/status` 降级探测

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\foundation\trash.dart`
  - 本地 / 远程删除行为统一入口
  - 服务端本地回收站桥接与恢复 / 彻底删除调度
  - 本地回收站“真删源库记录 + 按原位恢复”链路
  - 图集回收站封面元数据记录、恢复后元数据修正与远程批量删除 / 恢复 / 彻底删除封装
  - 受限 `download.db` 的可读性判定、恢复写回与误建空库修复

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\pages\trash_page.dart`
  - 本地 / 远程回收站页
  - 批量恢复 / 彻底删除与外部状态变化后的主动重载
  - 图集 / 漫画回收站封面稳定回显与阻塞式操作进度遮罩

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\pages\me_page.dart`
  - “我”页聚合入口
  - 历史 / 已下载 / 图集 / 远程·资源库 / 工具卡片布局
  - 工具卡片内横向外显按钮条、右侧渐变遮罩、底部额外留白与长按进入 `外显按钮与排序`
  - 渐进加载时序、远程摘要延后读取与页面内缓存复用

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\pages\tools.dart`
  - 工具页主体
  - 普通打开模式与 `外显按钮与排序` 自定义模式
  - 全部工具项的显隐选择、约 `800ms` 长按拖拽排序与选中遮罩样式

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\pages\tool_display_config.dart`
  - 工具外显定义与共享配置模型
  - 默认显示项、顺序归一化、设置读写与刷新通知入口

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\pages\download_page.dart`
  - 已下载页
  - 本地 / 聚合 / 远程·已下载 三档切换
  - 作者 / 标签 / 来源兜底回填与快速列表构建入口
  - 远程已下载在恢复后 / 回前台 / 重新进入时的强制刷新逻辑
  - 本地 / 远程加载隔离、8 秒 / 10 秒页面级超时与超时空态提示

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\pages\history_page.dart`
  - 历史记录列表页
  - Root / Shizuku 模式下封面解析与回显

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\pages\local_library_page.dart`
  - 图集 / 资源库页
  - 本地 / 聚合 / 远程 三档切换
  - 多选 FAB、选中态外边距、删除进度遮罩与已下载页样式对齐

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\pages\local_comic_detail_page.dart`
  - 本地详情页主体
  - 图集 / 下载 / 远程项兼容展示

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\pages\service_info_page.dart`
  - 服务信息页
  - 局域网扫描、地址编辑、状态展示
  - 真实资源重扫刷新与断开连接入口

### 4.6 网页后台与浏览器端资源

- `D:\Flutter_Projucts\PicaComic\PicaKeep\assets\web_console\index.html`
  - 网页后台 SPA 壳：登录遮罩、顶栏、根容器与脚本加载顺序

- `D:\Flutter_Projucts\PicaComic\PicaKeep\assets\web_console\js\common.js`
  - `window.PicaKeepConsole` 契约：`api`、`remote`、`source`、`remoteTarget`、`auth`、`history`
  - token 注入、401 处理、图片 URL 加 `__token`、远程代理 URL 生成

- `D:\Flutter_Projucts\PicaComic\PicaKeep\assets\web_console\js\auth.js`
  - 用户名 + 密码登录 UI、empty password 风险提示、token/user 存储与 `refreshMe()`

- `D:\Flutter_Projucts\PicaComic\PicaKeep\assets\web_console\js\shell.js`
  - 顶栏抽屉、应用 / 管理两档、`/` 与 `/admin-view` 地址同步、admin 用户管理入口显示
  - 左侧导航 / shell grid / reader-active 布局：迁移时务必保证 `#console-root` 不落到 0 宽列（reader 空白事故根因）

- `D:\Flutter_Projucts\PicaComic\PicaKeep\assets\web_console\js\app.js`
  - 网页应用面板：dashboard、数据源开关、本地/远程/聚合 adapter、grid/detail/reader、历史写回、图片收藏视图
  - 已下载/图集按 `rootId.startsWith('custom_')` 分流；grid/list 返回按钮在这里维护
  - reader 交互绑定：`bindReaderChrome` 指针状态机、`handleReaderTapTurn`（连续模式须用视口高度判边缘）、`finishReaderPointer/handleReaderTap`（释放语义区分，勿误清单击 tapTimer）、`applyReaderZoomState`（zoom-layer 缩放+`panX/panY` 平移）、`onGlobalKeydown`（全局 ESC 分级）；阅读设置 `readerSettingsHtml`（阅读模式折叠 + `maxWidthPx` 滑条）；侧栏 `renderItemPanel`/`itemPanelContentHtml`；详情推荐渲染

- `D:\Flutter_Projucts\PicaComic\PicaKeep\assets\web_console\css\app.css`
  - 应用面板、dashboard app「我」页风格、历史条、入口卡网格、工具胶囊、reader/grid/detail 样式
  - reader chrome（顶/底栏、页码左下角、`--reader-*` 颜色 token）、`.reader-zoom-layer`、`.app-item-panel*` 侧栏与章节/sticky footer 样式

- `D:\Flutter_Projucts\PicaComic\PicaKeep\assets\web_console\js\admin.js`
  - 网页管理面板：概览、本地资源、服务配置、日志、接口、回收站、收藏夹、图片收藏

- `D:\Flutter_Projucts\PicaComic\PicaKeep\assets\web_console\css\admin.css`
  - 管理面板 tab、资源表、回收站、收藏夹、图片收藏网格、批量操作与响应式样式

- `D:\Flutter_Projucts\PicaComic\PicaKeep\assets\web_console\css\common.css`
  - 网页后台全局变量、登录页、顶栏抽屉、通用卡片/按钮/状态样式
  - shell grid / 左侧栏 / reader-active 布局：reader 0 宽空白事故的主要排查位置之一

> 网页 reader/侧栏视觉与交互对齐客户端时，对照参考：`lib/pages/reader/touch_control.dart`（点击/手势状态机）、`lib/pages/reader/tool_bar.dart`（上下栏与页码）、`lib/pages/download_page.dart`（`DownloadedComicInfoView` 侧栏）、`lib/components/side_bar.dart`（`showSideBar` 右贴边面板）。详情推荐算法在 `lib/server/server_app.dart`（`/api/library/items/{id}/recommendations`）。「图片最大显示宽度」滑条这一轮在客户端也落地：`lib/base.dart`（settings 新增 index 116，默认 `980`）、`lib/pages/reader/reading_settings.dart`（开关下方 Slider）、`lib/pages/reader/image_view.dart`（`settings[43]=="1"` 时按 index 116 限宽）。

### 4.7 阅读器与系统 UI
- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\pages\reader\comic_reading_page.dart`
  - 阅读页主体
  - 沉浸式系统栏显隐逻辑

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\pages\reader\touch_control.dart`
  - 阅读点击 / 工具栏切换 / 沉浸式恢复

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\pages\reader\tool_bar.dart`
  - 阅读上下工具栏

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\main.dart`
  - 应用入口与 Flutter bootstrap 壳
  - `_MobileSystemUiFrame`
  - 延后初始化、管理器预热与移动端系统栏样式统一承接

### 4.8 服务端与 Android 侧
- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\server\local_server_runtime.dart`
  - 本地服务生命周期与状态同步
  - 服务端本地回收站恢复 / 删除与运行态缓存统一入口
  - 服务端 effective config 热推、挡位切换热更新与必要时重启兜底

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\server\library_event_bus.dart`
  - 远程资源事件总线
  - `library-changed` 广播模型

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\server\local_resource_scanner.dart`
  - 服务端本地资源扫描
  - 下载目录 / 自定义目录扫描策略

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\server\server_app.dart`
  - HTTP 路由、状态接口、资源接口、后台接口
  - 远程回收站恢复 / 删除、批量 trash / restore / purge、`/api/library/items/refresh`、`/api/events` 与 `librarySignature` 计算入口
  - 回收站封面接口稳定解析与跨端资源变更后的服务端快照刷新、远程客户端同步入口
  - 网页后台静态资源挂载、`/api/console/*` 账号接口、`/api/library/history` per-user 网页历史接口、远程 proxy 与 SPA fallback 对接

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\server\web_console\web_user_store.dart`
  - 网页专用用户与阅读历史持久化，独立 `web_console.db`
  - admin / user、密码哈希+盐、禁止删除最后一个 admin、per-user history upsert/list/remove/clear

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\server\web_console\web_auth_handler.dart`
  - 网页登录、当前用户解析、per-user HMAC token、admin 权限判断
  - 空 username 兼容旧 admin 登录，token 绑定 passwordHash 以便改密后失效

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\server\web_console\web_asset_loader.dart`
  - 从 Flutter `rootBundle` 读取 `assets/web_console/` 静态资源并映射 MIME

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\server\web_console\web_console_handler.dart`
  - `/admin` 兼容、SPA fallback、静态资源路径安全处理

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\server\web_console\remote_proxy_handler.dart`
  - 浏览器远程服务代理；只允许 GET，转发 Range/Accept，剥离敏感 header，后台登录后使用

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\pages\settings\log_settings.dart`
  - 应用内 `Logs` 页面，独立于 `app_settings.dart`
  - 日志记录开关、清空、导出、禁用长度限制和单条复制

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\foundation\log.dart`
  - 应用内日志缓存、长度限制、去重与记录开关

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\tools\android_foreground_service.dart`
  - Android 前台服务能力适配入口，后续继续做保活时重点看这里

- `D:\Flutter_Projucts\PicaComic\PicaKeep\android\app\src\main\res\values\styles.xml`
  - `NormalTheme` 的 `windowLayoutInDisplayCutoutMode` 与透明系统栏配置

- `D:\Flutter_Projucts\PicaComic\PicaKeep\android\app\src\main\res\values-night\styles.xml`
  - 暗色主题下同样的 edge-to-edge / 透明系统栏配置

- `D:\Flutter_Projucts\PicaComic\PicaKeep\android\app\src\main\kotlin\lingxue\picakeep\MainActivity.kt`
  - Android 通知权限
  - 全部文件访问权限、Shizuku 状态查询与系统设置跳转
  - Root / Shizuku 文件访问平台通道与异步超时保护
  - 阅读页常亮插件通道 `keepScreenOn`
  - edge-to-edge 窗口与系统栏瞬时唤出行为配置

- `D:\Flutter_Projucts\PicaComic\PicaKeep\lib\tools\keep_screen_on.dart`
  - 阅读设置页常亮开关的 Dart 侧包装
  - 平台通道异常兜底

- `D:\Flutter_Projucts\PicaComic\PicaKeep\android\app\src\main\AndroidManifest.xml`
  - Android 权限、前台服务、全部文件访问权限、回退回调配置

---

## 5. 当前遗留项

### 5.1 `android-foreground-service`
当前状态：**pending**

说明：
- Android 前台服务骨架、通知权限与相关能力入口已经接入了一部分
- 但“常驻保活、通知、系统限制适配”的整条链还没有作为最终完成项关闭

继续做时优先关注：
- `lib/tools/android_foreground_service.dart`
- `android/app/src/main/kotlin/lingxue/picakeep/`
- `AndroidManifest.xml`
- `ServiceInfoPage` 中的 Android 支持状态区块

### 5.2 `mobile-ui-input-fixes`
当前状态：**pending**

说明：
- 本轮已经关闭阅读器顶部黑条，补齐服务信息页真实刷新 / 断开连接，并完成“我”页双卡片布局与部分系统栏安全区问题收尾
- 但“地址输入体验、不同 ROM 下剩余系统栏一致性，以及服务信息页在复杂真机环境下的细节交互”还没有全部逐项关闭

继续做时优先关注：
- `lib/pages/service_info_page.dart`
- `lib/pages/settings/runtime_service_settings.dart`
- `lib/main.dart`
- 真机下服务端地址输入、状态栏图标亮度、页面切换过渡时的系统栏一致性

### 5.3 `lan-scan-followup`
当前状态：**pending**

说明：
- 当前扫描已经从“完整慢扫”改为“快速优先探测”
- 代码层已提速，但仍需要真机和实际局域网环境复核稳定性与误判情况

继续做时优先关注：
- 多网卡场景
- 不同私网段场景
- 已配置地址错误时的回退体验
- 扫描到多个可用节点时的选择体验

---

## 6. 验证状态

本轮已完成的工程校验：
- `flutter analyze`：0 issues
- `flutter build apk --debug`：通过

最新一轮修改已再次执行：
- `flutter analyze`：0 issues
- `.\gradlew.bat :app:assembleDebug`：通过
- Android 真机完整重装后，阅读器顶部黑条已确认恢复正常

远程回收站 / 服务信息 / 远程已下载这一轮同步修复已再次执行：
- `flutter analyze`：0 issues
- `.\android\gradlew.bat -p android :app:compileDebugKotlin`：通过
- 手动回归仍待补：
  - 服务端本地恢复 / 彻底删除后，手机客户端远程回收站同步清理旧记录
  - 服务端恢复后，手机客户端重新进入或从后台恢复时远程已下载自动刷新显示

说明：
- 当前代码在静态分析、Android debug 构建之外，已完成启动 bootstrap 壳、`我` 页渐进加载、已下载 Root / Shizuku 快速列表、历史封面回显、Shizuku 检测，以及远程回收站 / 服务信息 / 远程已下载同步收口这一轮改动的工程校验
- “我”页工具卡片 / “工具”页外显按钮自定义这一轮源码已同步回主项目，并完成代码层核对；但由于项目当前 Web 预览受 `sqlite3 -> dart:ffi` 限制阻塞，尚未在浏览器预览里完成该 UI 的运行态验证，后续应在 Android 真机或桌面 Flutter 目标继续复测
- 但冷启动观感、Root / Shizuku 真机权限链路、低端机 I/O 压力下的列表流畅度，以及最新远程同步链路的端到端行为仍需要继续真机复测，不应只凭 analyze / build 视为完全结束
- 最近一轮针对本地回收站源库删除 / 恢复、受限 `download.db` 误判修复、阅读设置页 `keepScreenOn` 通道的改动，已完成 `flutter analyze` 与 Kotlin 编译校验；模拟器侧删除 / 恢复链路已恢复到“看起来正常”的状态，但 Root / Shizuku 真机仍需继续覆盖

建议下一位接手时优先做的真机复核：
1. 冷启动是否保持“原生 logo -> Flutter bootstrap 壳 -> 首页”连续过渡，不再出现第二段“准备启动环境”文案或明显白屏
2. 首次进入“我”页与从其他 tab 返回时，历史 / 已下载 / 图集 / 图片收藏 / 远程摘要是否按预期渐进展示，已有结果是否不再重新转圈
3. 设置页 Shizuku / Root 状态、外跳设置返回后状态刷新、下载目录弹窗里的点按“浏览”与长按“浏览”是否都符合预期
4. Android 全部文件访问权限、Root / Shizuku 模式、内置文件夹浏览页，以及 `/data/user/0` 等受限目录读取在目标机型上是否真实可用
5. 手机端把远程漫画或远程图集移入回收站后，服务端普通列表、外层数量与服务端“回收站-本地”视图是否同步更新
6. 浏览器网页后台 `http://localhost:9527/` 登录后，`/` 应显示应用面板、`/admin-view` 应显示管理面板，刷新两个地址都不应 404
7. 网页 dashboard 的“已下载”应只显示漫画，“图集”应只显示 `rootId.startsWith('custom_')` 的图集，“图片收藏”应加载 `/api/library/image-favorites` 真数据，grid/list 返回按钮应能回 dashboard
8. 网页阅读器翻页 / 滚动后，dashboard 历史条应能按上次 ep/page 续读；多用户登录时历史应互相隔离
9. 网页管理面板的回收站、收藏夹、图片收藏 tab 应能懒加载并完成基本写操作
9b. 网页 reader / 侧栏 UI 多轮收口建议用 Chrome DevTools 可视复核（先把数据源切「本地」，避免远程自代理 502/401）：
   - console / network 无新增 JS error 或图片失败请求
   - 进入 reader 后验证 DOM 图片数量、图片请求状态、`naturalWidth/naturalHeight`，并确认关键容器 `#console-root` / `[data-reader-root]` / `.reader-strip` / `.reader-img` 的 `getBoundingClientRect().width > 0`（避免重现 0 宽空白）
   - 点击画面中心可显隐上下栏；边缘点击翻页；双击/长按缩放后可随指针平移并能还原；阅读设置面板「阅读模式」折叠展开、「图片最大显示宽度」滑条生效；全局 ESC 能按层级退出/返回
   - 侧栏为右贴边面板（移动端底部 sheet），章节区紧凑、底部双按钮正常；页码在左下角
10. 在服务端本地恢复 / 彻底删除回收站条目后，手机客户端远程回收站是否会同步消失旧记录
11. 手机端重新进入“远程已下载”或从后台恢复后，是否无需先去“服务信息”页手动刷新，就能直接看到服务端已恢复的漫画
12. 服务信息页“刷新状态”与“断开连接”是否分别正确触发真实资源刷新和客户端断连，漫画 / 图集 / 服务端资源数量摘要是否同步更新
13. 已下载页在本地 / 聚合 / 远程·已下载之间切换时，首屏是否仍存在明显卡顿；本地扫描为空、Root / Shizuku 读取失败或超时时，远程·已下载是否仍能独立显示
14. Root / Shizuku 模式下历史记录封面、作者 / 标签 / 来源信息，以及进入阅读页后列表封面预热暂停是否都符合预期
15. 局域网扫描是否仍稳定收敛到秒级返回，地址输入体验与不同 ROM 下的服务信息页交互是否正常

---

## 7. 临时脚本与产物

项目根目录当前仍保留以下临时恢复与检查产物：
- `temp_recover_apply.py`
- `temp_recover_apply_report.json`
- `temp_recover_inspect.py`
- `temp_recover_summary.json`
- `.inspect_sqlite_tmp.py`

这些文件目前没有被正式业务逻辑依赖，主要用于：
- 恢复库修复
- sqlite / `download.db` 兼容排查
- 临时检查与回溯

如果后续确认不再需要，可单独清理；清理前建议先确认是否还要继续做恢复库相关回归。

---

## 8. 建议的后续接手顺序

建议按以下顺序继续，而不是重新大范围发散：

1. **先做启动与权限链路真机回归**
   - 冷启动过渡、bootstrap 壳与“我”页渐进加载
   - Root / Shizuku 授权检测与外跳返回后的状态刷新
   - 内置目录浏览与受限目录读取
   - 已下载页 / 历史记录封面在 Root / Shizuku 下的实际表现

2. **再做服务信息页与远程链路复核**
   - 地址输入体验
   - 局域网扫描耗时与多网卡 / 多网段表现
   - 远程·已下载 / 远程·图集 / 远程·资源库
   - 远程回收站同步、服务信息页真实刷新 / 断开连接、远程已下载恢复刷新

3. **再收尾 Android 前台服务链路**
   - 通知权限
   - 前台服务状态
   - 电池优化与系统限制说明

4. **最后再考虑文档与临时产物清理**
   - 清理临时恢复脚本
   - 视需要把恢复工具产品化，而不是继续靠临时脚本

---

## 9. 本次交接结论

当前 `PicaKeep` 已经完成了从“本地离线阅读器改造”到“本地资源主线 + 远程服务骨架”的阶段性升级。

可以认为当前版本已经具备：
- 稳定的本地阅读、本地聚合与已下载三条主链路
- Root / Shizuku 授权检测、受限目录访问、历史封面回显与快速列表构建链路
- Android 启动 bootstrap 壳、延后初始化，以及“我”页渐进加载与缓存复用
- 可运行的客户端 / 服务端状态切换与本地 HTTP 服务骨架
- 已收敛的 analyze / Android debug build 基线

后续继续推进时，不建议再回到最初那种“只围绕本地下载页修补”的思路，而应当围绕以下三个核心继续演进：
- **平台能力真机验证**
- **本地资源聚合层**
- **远程服务与客户端模式层**

---

## 10. 项目进度清单

### 10.1 已完成
- [x] 本地漫画 / 本地图集 / 已下载三套主链路打通
- [x] 本地聚合层支持本应用下载目录、原应用下载目录、自定义本地漫画路径
- [x] 历史记录、收藏夹、图片收藏、本地详情页与阅读器主链路接通
- [x] 阅读器沉浸式、系统栏显隐、安全区与顶部黑条问题已在 Flutter + Android 原生层联合修复后真机确认恢复正常
- [x] 图集封面图比例、阅读页图像比例与手机端系统手势返回问题修复
- [x] “我”页历史记录封面预览修复
- [x] “我”页图集 / 远程·资源库双卡片布局稳定，远程离线时保留占位卡片
- [x] “我”页统计 / 历史 / 远程摘要已改为渐进加载，并在切回页面时复用已有结果
- [x] “我”页工具卡片已收口为一体式卡片，支持卡片内横向外显按钮条、右侧渐变遮罩、底部额外留白，以及长按直达 `外显按钮与排序`
- [x] 工具页已支持 `外显按钮与排序` 模式：全部工具项可点按显隐、长按约 `800ms` 拖拽排序，配置会共享持久化到“我”页外显按钮
- [x] 外显按钮默认首批显示已调整为“服务信息 / 本地文件 / APP能力 / 回收站”，但全部工具项仍可重新选择和排序
- [x] 已下载页三档切换、本地 / 聚合 / 远程·已下载主链路修复
- [x] 已下载页 Root / Shizuku 列表已切换为 `download.db` 快速构建，封面 / 索引缓存后台补齐
- [x] 缺失本地文件记录保留显示，作者 / 标签 / 来源信息恢复优先从 `download.db` 回填
- [x] 远程资源库 / 远程图集 / 远程阅读主链路打通
- [x] 远程元信息映射修复，作者 / 标签 / 漫画源等字段已补齐
- [x] 远程资源 `librarySignature`、`/api/events` WebSocket 推送、阅读期延迟刷新、前后台重连与 `/status` 降级探测链路已落地
- [x] 服务端挡位 / 路径变更在 `host/port` 不变时已支持运行态热更新，不再必须重启服务才能让客户端看到变化
- [x] 客户端 / 服务端运行模式切换、服务信息页、局域网扫描基础链路接通
- [x] 服务端资源扫描已过滤 `.picakeep_trash`，远程删除后普通列表、根目录数量与体积统计同步正确
- [x] 远程图集内容删除后的根列表 / 内容列表 / 回收站刷新链路已修复
- [x] 远程回收站、服务端“回收站-本地”与服务端本地恢复 / 彻底删除后的客户端同步残留问题已修复
- [x] 服务信息页“刷新状态”已接通真实资源重扫 / 远程刷新，并新增断开连接按钮，扫描局域网 / 刷新状态补了执行中反馈与防重复触发
- [x] 图集 / 资源库页远程视图下重复的刷新按钮已合并为单按钮，远程刷新与本地刷新语义已分流收口
- [x] 远程已下载入口已放宽为“只要配置远程地址就显示”，并在已配置但未连接时直接提示未连接状态
- [x] 远程已下载在恢复后、重新进入或从后台恢复时会强制拉取最新服务端快照
- [x] 已下载页排序弹窗状态已修复，远程已下载时间排序已与服务端统一
- [x] 已下载页本地 / 聚合 / 远程·已下载加载已隔离，本地 8 秒、远程 10 秒超时提示已落地
- [x] 已下载详情弹层多标签场景已改为自动高度滚动显示
- [x] 设置页已接入 Android 全部文件访问权限、Root 模式、Shizuku 检测、长按“浏览”进入内置目录浏览
- [x] Root / Shizuku 授权检测、受限目录访问与历史记录封面回显链路修复
- [x] 设置页 Root / Shizuku 检查时机已收口为首次进入与从其他 App 切回该页，滚动 / 重建不再重复检查
- [x] Root 授权结果已做会话内复用，减少重复 `su` 检查
- [x] 本地回收站已补“真删源 `download.db` 记录 + 按原位恢复”链路，恢复可回写原库路径与来源元数据
- [x] 图集回收站封面元数据、恢复后封面 / 阅读元数据修正、远程回收站封面解析已收口
- [x] 远程图集删除 / 恢复 / 彻底删除已补批量接口，手机端多项操作不再需要每项完整串行刷新
- [x] 图集页删除进度遮罩、多选 FAB、选中态间距与已下载页交互样式已对齐
- [x] 受限 `download.db` 的“不可读 / 真不存在”区分已补，避免误建空库覆盖原库
- [x] 受限目录权限不足时不再静默移入回收站或仅从列表移除，而是直接提示失败
- [x] 阅读设置页 `keepScreenOn` 常亮插件通道已补齐，`MissingPluginException` 已修复
- [x] 已确认远程·已下载继续独立走远程数据源，不受本地 Shizuku 扫描失败影响
- [x] 本地文件管理页里额外扩出的 Root 路径入口已回收
- [x] Android 启动已切到 bootstrap 壳，去掉第二段“准备启动环境”文案并后移初始化预热
- [x] 启动期图片收藏数量读取竞态修复
- [x] Android sqlite 运行库与启动崩溃问题修复
- [x] 已下载封面已回退为更接近原项目的直接显示策略，远程 / Root 本地封面缓存键与应用数据目录缓存链路已修复
- [x] 设置页 `APP` 分组的目录弹窗、内置目录浏览页与权限状态加载链路已做第二轮卡顿收敛
- [x] 当前代码 `flutter analyze` 通过
- [x] 当前代码 `flutter build apk --debug` 通过
- [x] 浏览器网页后台 SPA 已落地：静态资源目录 `assets/web_console/`、真实登录、远程代理、旧 `/admin` 兼容与 `/` / `/admin-view` 双地址
- [x] 网页应用面板已支持本地 / 远程 / 聚合封面墙、详情页、连续滚动 / 翻页双模式阅读器
- [x] 网页账号系统已落地：admin + 多用户，仅网页端使用；per-user 阅读历史写入独立 `web_console.db`
- [x] 网页 dashboard 已按 app「我」页视觉改版，并补齐历史、已下载、图集、资源库、图片收藏、工具入口
- [x] 网页已下载 / 图集分流已按 `rootId.startsWith('custom_')` 收口：true 为图集，false 为漫画 / 已下载
- [x] 网页 grid/list 已补返回 dashboard 的返回栏；图片收藏入口已接 `/api/library/image-favorites` 真数据；`history.remove` 路由契约已修正
- [x] 网页管理面板已扩展回收站、收藏夹、图片收藏、本地资源批量操作、配置校验、日志过滤与接口清单
- [x] 网页 Console reader / 侧栏 / 左侧导航多轮 App 化收口已执行：reader-active 0 宽空白修复、上下工具栏与阅读设置（阅读模式折叠 + pill 开关/滑条）、连续拼接 / 翻页阅读、`.reader-zoom-layer` 缩放+`panX/panY` 平移、页码左下角、图片最大宽度滑条（Web `maxWidthPx` + 客户端 settings 116，默认 980）、章节双列侧栏（桌面右贴边 / 移动底部 sheet）、全局 ESC 退出/返回、详情页推荐接口；交互对齐客户端 `touch_control.dart` / `tool_bar.dart`（仍建议按 §6 第 9b 条用 DevTools 持续可视复核）
- [x] 网页阅读历史读写与历史页收口（§37）：ESC / 返回键 / 跳转退出都即时补写历史；滚动连续模式从历史打开定位到记录页；历史独立页对齐「已下载」（卡片/详细双档共用 viewMode 记忆 + 顶栏搜索 + 作者/标签前端 join 资源库补全 + 详细标题 3 行截断），去除第二行 `readEpisode` 残留的「0」

### 10.2 进行中
- [ ] 项目当前处于真机回归与启动 / 权限模型 / 平台细节收尾阶段
- [ ] Android 目录访问能力与 Root / Shizuku 链路需要继续在不同 ROM / 权限模型 / Root 与非 Root 设备上复核
- [ ] Android Shizuku 受限目录下“仅本应用下载目录”的本地已下载读取修复已补代码，仍需继续确认不影响 Root 与远程已下载隔离
- [ ] Android Shizuku 受限目录下本地已下载重新扫描返回 0 的问题已补代码修复，但仍需继续真机确认目录型下载、`download.db` 下载和封面 / 阅读回退是否全部打通
- [ ] 远程回收站同步、远程已下载恢复刷新与远程图集批量删除 / 恢复虽已完成代码修复并通过用户侧关键 UI 确认，仍需继续做服务端恢复 / 彻底删除、手机客户端重新进入 / 回前台场景的真机回归
- [ ] 地址输入体验、局域网扫描稳定性，以及不同 ROM 下剩余系统栏一致性仍需继续做真机细节校验
- [ ] 最新远程热同步链路虽已完成代码收口，仍建议继续覆盖“切服务端挡位 / 回收站恢复删除 / 阅读期间变更 / 断网重连恢复”四类真机场景
- [ ] 本地回收站“按原位恢复”仍需继续在更多真实库顺序、源路径改名或互换目录场景下做回归
- [ ] 网页后台仍建议做真实浏览器端到端回归：多用户历史隔离、dashboard 各入口进出、图片收藏缩略图、管理面板写操作、远程代理阅读链路

### 10.3 未完成
- [ ] Android 前台服务 / 通知 / 保活约束适配未彻底关闭
- [ ] 局域网扫描策略仍需在真实多网卡、多网段环境继续复核
- [ ] Android 全部文件访问权限与内置目录浏览页仍需补足更多目标机型验证结论
- [ ] 当前工作区仍残留构建产物与本地配置：`android/app/build/b70f9274ac3b5ec9754be8774a3c9a1c.cache.dill.track.dill`、`android/app/build/windows/x64/runner/Debug/data/flutter_assets/kernel_blob.bin`、`android/local.properties`
- [ ] 网页图片收藏入口当前已接真实缩略图数据，但点击直达阅读器仍需额外拉详情拿 `pages[]`，可作为后续增强

---

## 11. 提交方式注意事项

当前目录结构里外层 `D:\Flutter_Projucts\PicaComic` 也有 `.git`，但本项目源码提交必须进入内层仓库：
- 正确仓库：`D:\Flutter_Projucts\PicaComic\PicaKeep`
- 正确分支：`main`
- 提交前确认：`git -C D:\Flutter_Projucts\PicaComic\PicaKeep status --short`
- 暂存时只加入本次需要的源码 / 文档，例如 `lib/pages/download_page.dart`、`交接文档.md`
- 提交信息保持与既有版本一致的格式：首行使用 `Vx.y.z 中文标题`，并补一段中文摘要正文，说明本次改动的主目标与影响范围，不只留标题
- 不要在外层 `D:\Flutter_Projucts\PicaComic` 提交 `PicaKeep/`、`PicaComic/`、`.gradle-home/` 或临时脚本
- 不要提交构建产物和本地配置：`android/app/build/...`、`android/app/build/windows/.../kernel_blob.bin`、`android/local.properties`

本轮提交方式已按上述规则修正：外层误提交已移除，源码提交以 `PicaKeep` 内层仓库为准；后续如继续沿用版本号提交，也应保持“标题 + 摘要正文”的完整样式。

## 13. 2026-05-10 Root Shizuku 挡位切换与底部提示修复
- 再次确认：本轮问题不在权限判断本身，而在设置页数据管理挡位点击后，`SnackBar / ScaffoldMessenger` 虽然执行了 `show`，但提示没有稳定显示在当前可见页面层级。
- 挡位点击链路已补日志并完成分层校验：
  - `[PicaKeep][ManagedDataSourceMode] access current=... target=... result=...`
  - `[PicaKeep][ManagedDataSourceMode] show hint: ...`
  - 真机日志已确认当从 `仅本应用` 切回 `仅原应用` 时，权限判断会正确返回 `ManagedSourceAccessRequirement.rootRequired`，说明提示缺失不是判断漏掉，而是 UI 提示层被吞。
- `lib/pages/settings/app_settings.dart`
  - 数据管理挡位现在即使点中“当前已选中”的档位，也会先执行权限检查，再决定是否切换。
  - 需要提示时不再依赖 `ScaffoldMessenger`，而是改为直接插入全局 `OverlayEntry` 的底部提示条，持续 5 秒，样式更接近安卓底部提示。
  - 切换到别的档位后，如目标路径需要更高权限，会在切换完成后再次补弹一次提示，避免被“正在切换/已切换数据库路径”之类的普通提示顶掉。
  - 提示文案已统一为：
    - `该档位的路径需要root权限才能访问`
    - `权限不足请检查shizuku授权情况`
- `lib/foundation/local_library.dart`
  - `getManagedSourceAccessRequirement()` 不再只依据设置里的 Root 开关值判断，而是会额外确认当前 Root 是否真的仍然可用。
  - 这避免了“外部已经撤销 Root，但设置项还残留开启状态”时被误判为 `ok`，从而不弹提示。
- `android/app/src/main/kotlin/lingxue/picakeep/MainActivity.kt`
  - Root / Shizuku 授权状态缓存窗口已从 30 秒收紧到 1.5 秒。
  - 这样用户在系统外部撤销授权后，回到应用切挡位时不会长时间读取旧缓存结果。
- 本轮同时收口了设置页里的一个副作用：
  - 当只是点击当前档位看提示、不发生真正切换时，不再错误地遗留“抑制下一条普通设置提示”的标记。
- 本轮源码涉及文件：
  - `android/app/src/main/kotlin/lingxue/picakeep/MainActivity.kt`
  - `lib/foundation/download.dart`
  - `lib/foundation/local_library.dart`
  - `lib/pages/download_page.dart`
  - `lib/pages/favorites/local_favorites.dart`
  - `lib/pages/favorites/main_favorites_page.dart`
  - `lib/pages/history_page.dart`
  - `lib/pages/local_library_page.dart`
  - `lib/pages/me_page.dart`
  - `lib/pages/settings/app_settings.dart`
  - `交接文档.md`
- 本轮已完成校验：
  - `flutter analyze lib/pages/settings/app_settings.dart lib/foundation/local_library.dart`
  - `flutter build apk --debug`
## 12. 2026-05-10 Shizuku 本地已下载补充记录

- 再次确认：远程已下载与本地已下载隔离仍然有效，`remote` 档没有回退到本地 `DownloadManager`，本轮未改远程资源链路。
- 本轮新确认的根因不是远程隔离失效，而是多个辅助入口在 `Android + 仅开启 Shizuku + 当前下载目录不可被 dart:io 直接访问` 时，仍会偷偷执行 `DownloadManager().init()`。
- 已确认受影响的入口包括：
  - “我”页下载数量统计
  - 设置页修改目录后的重载 / 重扫
  - 本地库页重新扫描
  - 历史页、本地收藏页、收藏夹页里的本地已下载解析入口
- 这些旧链路会直接对配置路径执行普通 `Directory.create()` / `sqlite3.open()`；当当前下载目录指向受限目录，或仍配置为旧包名目录 `/storage/emulated/0/Android/data/com.github.pacalini.pica_comic` 时，就会触发 `PathNotFoundException`，并表现为：
  - 本地已下载加载失败
  - “我”页下载数量统计失败
  - 刚选完目录后立即重扫失败
- 本轮代码修正：
  - 新增 `LocalLibraryManager.shouldBypassDirectDownloadManagerForCurrentDownloads()`
  - 在 `managedDataSourceModeCurrentOnly` 且命中 Shizuku fallback 时，统一绕开 `DownloadManager`
  - 改走 `LocalLibraryManager` 的受限目录读取链路完成计数、刷新、扫描结果生成和历史/收藏解析
  - Root 分支保持不变
  - 远程已下载、本地/远程聚合逻辑保持不变
- 为后续真机排查已补充日志：
  - `[PicaKeep][Shizuku] current downloads path=... fallback=...`
  - `[PicaKeep][Shizuku] current downloads source missing: ...`
  - `[PicaKeep][Shizuku] current downloads loaded N items from ...`
- 长按浏览预设根目录现已补入当前包名路径 `lingxue.picakeep`，同时保留 `com.example.picakeep` 与旧包名 `com.github.pacalini.pica_comic`，兼容“本应用目录 / 原应用目录”自由配置。
- 本轮已完成校验：
  - `flutter analyze lib/pages/me_page.dart lib/pages/settings/app_settings.dart lib/pages/local_library_page.dart lib/pages/history_page.dart lib/pages/favorites/local_favorites.dart lib/pages/favorites/main_favorites_page.dart lib/foundation/local_library.dart lib/pages/download_page.dart`
  - `.\android\gradlew.bat -p android :app:compileDebugKotlin`

## 14. 2026-05-11 本地回收站 / Root Shizuku 触发收口 / keepScreenOn 修复记录

- 本轮重点不是再扩新功能，而是把“受限目录删除与恢复”这条链真正收口，避免 UI 看起来成功、实际源库状态不一致。
- `lib/foundation/trash.dart`
  - 本地已下载移入回收站时，已改为真正删除源 `download.db` 中对应行，而不是只保留回收站副本、源库仍残留。
  - 回收站记录现在会额外保存 `sourceDbPath`、`sourceDbId`、`sourceDirectory`、`sourceDbTimeMillis` 等来源信息，恢复时优先写回原库、原目录和原时间排序位置。
  - 恢复时不再只是“加回一条记录”，而是尽量按原始 `time` 插回，因此列表大多数情况下能回到原来的相对位置。
  - 受限目录下如果既没有 Root 也没有 Shizuku，不再允许“也移入回收站 / 直接删除 / 从列表移除”；现在统一明确提示权限不足。
- 本轮还修掉了一个关键回归：
  - 之前在 Root / Shizuku 场景下，如果 `dart:io` 读不到受限 `download.db`，旧逻辑可能把它误判成“源库不存在”，然后创建一个新的空库。
  - 这样会导致恢复时看起来只剩一条数据，像是把原数据库清空了。
  - 现在已经先区分“不可读”和“真的不存在”，确认真实存在时会走受限读写链路，不会再因为普通 I/O 失败就误建空库覆盖原库。
- Root / Shizuku 权限触发频率也继续收口：
  - 启动期和普通页面不再做全局预检查。
  - 设置页只承担状态展示与切换验证；检查时机收口为首次进入该页，以及从其他 App 切回该页。
  - 滚动、页面重建、普通 `setState` 不会重复触发检查。
  - Root 授权结果已做会话内复用，实际访问受限路径时不再每一步都先额外跑一次 `su -c id`。
- `lib/pages/download_page.dart`
  - 已下载详情弹层在标签过多时改为自动高度 + 可滚动，避免按钮和章节区域被顶出或溢出。
  - 章节删除失败会明确给出 `SnackBar` 提示，方便区分“权限不足”和“逻辑未触发”。
- 阅读设置页的常亮报错已经补齐：
  - `android/app/src/main/kotlin/lingxue/picakeep/MainActivity.kt` 已注册 `lingxue.picakeep/keepScreenOn` 通道。
  - `lib/tools/keep_screen_on.dart` 也补了异常兜底。
  - 之前日志中的 `MissingPluginException(No implementation found for method set/cancel...)` 已对应修复。
- 关于 `MainActivity.kt` 乱码问题，本轮结论是：
  - 文件本身实际是 UTF-8，不是“整个文件应该改成 GBK”。
  - 真正的问题是少量历史字符串内容已经是乱码文本，需要逐个改正；不能把整个 Kotlin 文件按 GBK/UTF-8 来回强转。
  - PowerShell 控制台偶尔看到中文显示异常，不等于源码文件编码本身有问题。
- 本轮已完成校验：
  - `flutter analyze`
  - `.\android\gradlew.bat -p android :app:compileDebugKotlin`
- 当前回归状态：
  - 模拟器复制测试库场景下，回收站删除 / 恢复链路已经恢复到“看起来正常”的状态。
  - 但 Root / Shizuku 真机、不同 ROM、源路径改名或互换目录后的恢复识别，仍建议继续做针对性回归，不要只凭模拟器结果完全收尾。

## 15. 2026-05-14 包名同步到 lingxue.picakeep 与版本号更新

- 本轮目标是把 Android 包名、应用内 MethodChannel 前缀、预设目录提示和交接文档统一收口到新标识 `lingxue.picakeep`，避免继续混用 `com.example.picakeep` 与更早的旧包名。
- `android/app/build.gradle.kts`
  - `namespace` 与 `applicationId` 已同时更新为 `lingxue.picakeep`。
- `android/app/src/main/kotlin/lingxue/picakeep/`
  - `MainActivity.kt` 与 `PicaKeepForegroundService.kt` 已迁移到新包路径并更新 Kotlin `package` 声明。
  - 前台服务、存储访问、常亮三个自定义通道前缀已统一切到 `lingxue.picakeep/...`。
- `lib/`
  - Dart 侧 `android_foreground_service`、`keep_screen_on`、`block_screenshot`、`storage_access` 等通道名已与 Android 原生侧同步。
  - 设置页内置目录浏览预设根目录已补入 `lingxue.picakeep`，并继续保留 `com.example.picakeep`、`com.github.pacalini.pica_comic` 以兼容旧目录数据迁移与手动排查。
- 版本号已按上一轮提交版本 `V1.8.17` 递增为 `V1.8.18`，对应 `pubspec.yaml` 为 `1.8.18+1`，Windows `Runner.rc` 的后备版本号也已同步到 `1.8.18`。
- 本轮提交与后续接手说明：
  - 代码提交基线已切回 `main`，不再继续以 `optimize/startup-performance` 作为默认提交分支。
  - 仍不要提交 `android/app/build/...`、`android/app/build/windows/.../kernel_blob.bin`、`android/local.properties` 等构建产物与本地配置。

## 16. 2026-05-14 本地回收站删除卡顿与 download.db 恢复顺序修复

- 本轮重点继续收口本地已下载移入回收站与恢复链路，针对两个现象：
  - 删除时长时间转圈，且失败后列表里直接消失、表现成“扫不到”
  - 从回收站恢复后，`download.db` 记录被插到末尾，数据库默认行号与原位置断序
- `lib/foundation/local_library.dart`
  - 读取受管下载源 `download.db` 时已补充 `rowid as __rowid__`，并把源行号带入本地条目元数据
- `lib/foundation/local_trash_store.dart` / `lib/foundation/trash.dart`
  - 本地回收站记录新增 `sourceDbRowId`
  - `local_trash.db` 已增加 `source_db_rowid integer` 兼容迁移
  - 删除过程中引入 `pending` 中间状态，避免半失败时直接把项目隐藏掉
  - 删除时不再预先递归统计整个目录大小，改为优先复用现有条目大小，减少大目录删除卡顿
  - 删除流程改为可回滚：若 `download.db` 已删但目录移动失败，会尝试把数据库记录按原 `id`、`time`、`rowid` 回滚
- `download.db` 恢复策略现已调整为：
  - 若原 `id` 已存在，则更新该行，避免重复
  - 若原 `rowid` 空闲，则优先按原 `rowid` 插回
  - 若原 `rowid` 已被其他新记录占用，则不覆盖占用者，回退为普通插入，同时保留原 `time`
  - 旧回收站记录如果没有 `sourceDbRowId`，仍按原 `time` 恢复，保持向后兼容
- `lib/pages/download_page.dart`
  - 删除完成后保留单次显式 `reload()`
  - 删除过程中收到本地数据变更通知时不再重复触发并发刷新，减少“转圈很久”的放大效应
- 本轮已完成校验：
  - `flutter analyze`
  - `.\android\gradlew.bat -p android :app:compileDebugKotlin`

## 17. 2026-05-14 设置页内置文件浏览器最终收口

- 本轮已将设置页长按“浏览”进入的 Android 内置文件浏览器，整理为独立实现并按最终确认的交互重新收口；不再保留中间几轮试样式的过渡状态。
- `lib/pages/settings/settings_page.dart`
  - 新增 `part 'internal_directory_browser.dart';`
  - 内置目录浏览器相关页面逻辑已从 `app_settings.dart` 拆出，避免设置页继续膨胀。
- `lib/pages/settings/internal_directory_browser.dart`
  - 新建独立 `part` 文件，承接内置文件浏览器的页面结构、目录读取封装、预设路径、搜索、面包屑与按钮区。
  - 顶部结构当前以参考项目 `D:\Flutter_Projucts\FlSQLite_Viewer` 为基准，但按本项目场景做了定制：
    - `AppBar`
    - 面包屑路径条
    - 搜索框
    - 快捷路径入口
    - 预设路径展开区
    - 左侧“展开预设路径 / 收起预设路径”与右侧“选择当前文件夹”按钮
  - 搜索框最终位置已调整到“面包屑路径条下面”，不再放在 `AppBar` 下方。
  - 原参考项目里“仅显示数据库”的位置，已改为右侧“选择当前文件夹”按钮，用于当前项目的目录选择场景。
  - “展开预设路径 / 收起预设路径”按钮与“选择当前文件夹”按钮的高度，已在最终版继续上调到约为早期版本的 1.5 倍，方便手机端点按。
  - 预设路径展开区最终采用“无图标、多行向下展开”的 `ActionChip` 布局，不再使用单行横向滚动。
  - 当前保留的快捷路径为：
    - `Root/应用目录` → `/data/user/0`
    - `Shizuku/应用目录` → `/storage/emulated/0/Android/data/`
    - `原应用目录` → `/data/user/0/com.github.pacalini.pica_comic`
    - `普通目录` → `/storage/emulated/0`
  - 列表展示逻辑最终已从“只列文件夹”改为“文件夹与普通文件都列出”：
    - 文件夹继续可点击进入下一层
    - 普通文件只展示，不再尝试进入
    - 列表视觉骨架继续沿用当前项目现有样式，没有再改成参考项目的 `ListTile`
  - 搜索框最终改为过滤“当前路径下的文件夹和普通文件名称”，不再承担手动输入任意路径并跳转的职责。
  - 本轮重写时顺手清理了该文件里先前残留的局部中文乱码文案，方便后续继续维护。
- `lib/pages/settings/app_settings.dart`
  - 保留设置项入口与目录浏览器调用逻辑。
  - 新增了 Dart 侧对 `listDirectoryEntriesWithRoot` / `listDirectoryEntriesWithShizuku` 的封装，内置浏览器改为优先走“目录项 + 类型”数据，而不是旧的“只返回目录名”接口。
- Android 原生侧说明：
  - `android/app/src/main/kotlin/lingxue/picakeep/MainActivity.kt` 原本已存在 `listDirectoryEntriesWithRoot` 与 `listDirectoryEntriesWithShizuku`，本轮未新增新的原生能力，主要是 Dart 侧浏览器改为接入这两条已有通道。
- 本轮已完成校验：
  - `flutter analyze lib/pages/settings/internal_directory_browser.dart lib/pages/settings/app_settings.dart lib/pages/settings/settings_page.dart`

## 18. 2026-05-14 包名修正、Root 大目录链路与删除交互收口

- 本轮在前面几节基础上继续做了三类收口，目标是不改业务方向，只把已经确认的问题链路补完整：
  - Android 包名从误写的 `lingxue.picakee` 统一修正为全小写 `lingxue.picakeep`
  - Root 模式下 `/data/user/0` 一类大目录的枚举链路改为更稳的候选路径 + 单次脚本列目录
  - 已下载页删除 / 放入回收站期间补上不可退出的进度遮罩，并统一多选态高亮样式
- `android/app/build.gradle.kts`
  - `namespace` 与 `applicationId` 已统一为 `lingxue.picakeep`
- `android/app/src/main/kotlin/lingxue/picakeep/`
  - `MainActivity.kt` 与 `PicaKeepForegroundService.kt` 已迁移到正确包路径
  - 旧的 `lingxue/picakee/` 路径应视为已废弃并删除，不再继续保留双路径并存状态
- `lib/tools/android_foreground_service.dart`、`lib/tools/keep_screen_on.dart`、`lib/tools/block_screenshot.dart`
  - Dart 侧 MethodChannel 前缀已与原生侧统一改为 `lingxue.picakeep/...`
- `lib/pages/settings/app_settings.dart` / `lib/pages/settings/internal_directory_browser.dart`
  - 内置目录浏览器的预设路径与包名提示已同步到 `lingxue.picakeep`
  - Root 目录枚举链路补齐 `/data/user/0`、`/data/data`、`/data_mirror/data_ce/null/0` 三向候选
  - 目录列出逻辑改为“存在性校验 + cd + ls 优先 + find 回退 + 剩余时间预算读取”的模式，失败时会返回更具体的候选路径摘要，而不是统一报“Root 模式目录读取失败”
- `lib/foundation/trash.dart` / `lib/foundation/local_trash_store.dart` / `lib/foundation/local_library.dart`
  - 本地回收站记录已补 `sourceDbRowId`
  - 恢复 `download.db` 时会优先尝试写回原 `rowid`，若被占用则回退为普通插入，同时保留原 `time`
  - 删除链路已补 `pending` 中间状态与失败回滚，避免目录移动失败时项目直接从列表消失
- `lib/pages/download_page.dart`
  - 删除 / 放入回收站期间已增加淡白色背景进度遮罩，并显示“正在删除 / 正在放进回收站 + 当前进度数/总数”
  - 删除进行中会阻止下载页自身返回、外层导航切换与重复刷新，等待本地或远程删除实际返回结果后再结束进度状态
  - 进度遮罩已收回到页面内层，不再挂在根导航弹窗上，避免页面被退出后遮罩残留到其他页面
- 多选态视觉收口：
  - 下载页、本地图库、回收站、本地收藏的选中态已去掉右上角勾选提示
  - 当前统一保留随主题主色变化的遮罩 / 描边，不再写死单独的勾选浮标样式
- 本轮已完成校验：
  - `flutter analyze`
  - `.\android\gradlew.bat -p android :app:compileDebugKotlin`
  - `.\android\gradlew.bat -p android :app:installDebug`
- 提交注意事项：
  - 继续不要提交 `android/local.properties`
  - 如果后续再发版，建议从当前 `1.8.18` 基础继续递增，而不是沿用误写包名时的提交说明

## 19. 2026-05-14 图集与回收站操作遮罩、全选图标和终端编码收口

- 本轮继续补齐“图集删除 / 放入回收站”和“回收站恢复 / 彻底删除”的执行态反馈，目标是让耗时操作期间用户明确知道任务正在跑，并避免中途退出造成状态不一致。
- `lib/pages/local_library_page.dart`
  - 远程图集 / 资源库多选删除现在会显示淡白色背景进度遮罩。
  - 遮罩内容与已下载页保持一致：移动端分两行显示“正在删除 / 正在放进回收站”和进度 `当前数/总数`，桌面端合并为一行。
  - 执行期间会启用导航锁和临时返回手势禁用，等待本地或远程删除结果返回后再关闭遮罩。
  - 删除完成后统一触发一次加载刷新，避免操作中数据变更通知和手动刷新叠加造成重复转圈。
- `lib/pages/trash_page.dart`
  - 回收站恢复和彻底删除现在也会显示同类淡白色进度遮罩。
  - 恢复 / 删除期间使用 `PopScope` 与全局导航锁阻止退出，执行结束后再刷新列表。
  - 回收站多选态新增全选按钮，最终按确认改为内置图标样式：未全选时使用 `Icons.select_all`，已全选时使用 `Icons.clear_all`，不再显示文字按钮。
  - 本轮修复了此前局部误写入的中文乱码 tooltip，例如“恢复”“彻底删除”等，保留正常 `.tl` 翻译入口。
- 乱码排查结论：
  - 已对源码与交接文档做排除构建产物后的乱码特征扫描，未发现 `鎭`、`褰`、`鍒`、`�`、`Ã`、`Â`、`â` 等残留。
  - 扫描中命中的 `assets/tags.json` / `assets/tags_tw.json` 为正常标签内容，例如“妙婕·伊格瑞特”“狗鏈”，不是乱码。
  - 后续如果 PowerShell 里直接 `Get-Content` 看到中文异常，先用 `-Encoding UTF8` 复核，不要把控制台显示乱码误当成源码文件已损坏。
- 本机终端环境已完成永久配置：
  - 当前代码页为 `65001 UTF-8`，PowerShell 输入 / 输出编码为 UTF-8。
  - `C:\Users\tanad\Documents\WindowsPowerShell\Microsoft.PowerShell_profile.ps1` 已保存为带 BOM 的 UTF-8，避免 Windows PowerShell 5.1 按 ANSI 读取无 BOM UTF-8 导致中文注释显示成乱码。
  - 同一 profile 已补充 `$PSDefaultParameterValues`，让 `Get-Content`、`Set-Content`、`Add-Content`、`Out-File` 默认使用 `UTF8`；重新打开终端后，裸 `Get-Content 交接文档.md` 也应正常显示中文。
  - Windows Terminal 默认字体已确认是 `Sarasa Mono SC`。
  - 传统控制台 `HKCU\Console` 以及已有控制台子配置已统一设置为 `Sarasa Mono SC`。
- 本轮已完成校验：
  - `flutter analyze`
  - `.\android\gradlew.bat -p android :app:compileDebugKotlin`
- 提交注意事项：
  - `android/local.properties` 仍是本机 Gradle / Flutter 路径配置，不纳入提交。
  - 终端 profile 与注册表字体配置属于用户环境，不在仓库内提交；这里只在交接文档记录结论，方便后续判断乱码来源。

## 20. 2026-05-14 本地文件管理内置浏览器统一与自定义图集扫描修正

- 本轮继续收口“本地文件管理 -> 添加路径 -> 浏览”的交互与手机端自定义图集扫描问题，目标是统一目录选择体验，并解决共享存储大目录首次扫描慢、选中路径后图集未正常出现的问题。
- `lib/pages/local_library_page.dart`
  - Android 端“添加本地漫画路径”的 `浏览` 已改为直接打开与设置页一致的内置文件夹浏览器，不再保留弹窗里的旧常用目录 chip。
  - 点“选择当前文件夹”后会直接写入自定义路径配置，并立刻刷新外层列表；不再要求用户回到旧弹窗再点一次“确定”。
- `lib/pages/settings/internal_directory_browser.dart`
  - 内置目录浏览器入口已改为公共 `openInternalDirectoryBrowser(...)`，供设置页和本地文件管理页共用。
  - 浏览策略补了一层路径判断：
    - 共享存储路径，如 `/storage/...`、`/sdcard/...`，优先直接走“安卓全部文件访问权限”模式。
    - `/data/...` 一类受限路径才优先尝试 `Root / Shizuku`。
  - 这样可以避免明明只是浏览共享存储目录，却先卡在 `Root` 初次探测上。
- `lib/pages/settings/app_settings.dart`
  - 设置页“本应用下载目录 / 原应用下载目录”的弹窗里，路径输入框和 `浏览` 按钮已固定排在同一行，移动端不再掉到下一行。
- `lib/foundation/local_library.dart`
  - 自定义路径扫描新增“单个图集源”识别：
    - 如果当前目录本身直接包含图片，视为一个图集。
    - 如果当前目录下全部子目录都像章节目录，并且每个子目录都包含图片，也视为一个图集，保留章节结构。
  - 下载目录兜底扫描同样补齐了“根目录本身就是图集”的兼容，不再只扫子目录。
  - 图集源存储统计不再对整个源路径额外递归一遍目录体积，而是直接累计已识别图集体积，降低像 `/storage/emulated/0/1/.qq` 这类混合大目录的卡顿风险。
- 本轮真机结论：
  - `原应用下载目录 /data/user/0/com.deepseek.chat/download` 能被识别，说明该路径的访问链路实际走通了 `Root`。
  - 自定义路径 `/storage/emulated/0/1/.qq` 最终被识别为共享存储图集源，日志显示可扫出 `8` 个叶子图集；这条路径更依赖“安卓全部文件访问权限”而不是 `Root`。
  - 一开始 `.qq` 出现 `imageBearingDirs=0 / leafAlbums=0`，在授予“安卓全部文件访问权限”后变为 `imageBearingDirs=8 / leafAlbums=8`，说明共享存储枚举权限不足确实会直接影响自定义图集扫描结果。
- 本轮已完成校验：
  - `flutter analyze lib/foundation/local_library.dart lib/pages/local_library_page.dart lib/pages/settings/app_settings.dart lib/pages/settings/internal_directory_browser.dart lib/pages/settings/settings_page.dart`
- 提交注意事项：
  - 继续不要提交 `android/local.properties`。
  - 如果后续还要继续优化 `.qq` 这类混合超大目录，优先考虑做“目录深度/数量上限 + 分批扫描”而不是再提高主线程容忍时间。

## 21. 2026-05-14 远程已下载封面回滑缓存命中优化

- 本轮聚焦“远程·已下载”列表封面在快速下滑后再快速上滑时回显偏慢的问题，目标是让已经加载过的封面更稳定地命中客户端缓存，而不是反复走不同的 provider / 读盘 / 解码链路。
- 根因收口：
  - 远程封面此前第一次显示走 `StreamImageProvider`，磁盘缓存文件存在后又切到 `FileImage`，导致 Flutter 图片缓存 key 不稳定，回滑时即使文件已落盘，也不一定能直接命中同一份内存缓存。
  - 已下载页移动端 `cacheExtent` 只有 `0.25` 屏，快速回滑时上方卡片重新进入构建队列偏晚。
  - `DownloadPageLogic._prefetchCoverThumbnails()` 一直是空实现，远程封面没有做首屏附近预热，列表只能在用户滑回时临时补加载。
- `lib/foundation/remote_library_data_source.dart`
  - 远程封面 provider 已统一为稳定 key 的 `_RemoteLibraryCoverImageProvider`，key 固定绑定远程封面 URL。
  - provider 内部优先读取 `remote_library_covers` 磁盘缓存；若缓存文件不存在或不可用，再走原有下载链路写入磁盘。
  - 这样“首次网络下载”和“后续磁盘命中”现在共用同一套图片缓存 key，不再出现第一次和第二次显示命中两套缓存的问题。
- `lib/components/comic_tile.dart`
  - 列表封面图片已改为根据卡片实际显示尺寸提供 `cacheWidth` / `cacheHeight`，尽量按网格显示尺寸解码，而不是频繁按原图尺寸解码。
  - 这部分对本地列表也会有被动正向收益，但本轮没有改本地数据源和阅读链路。
- `lib/pages/download_page.dart`
  - 已补齐远程封面预热：列表加载完成后会低优先级预热前 `12` 个远程封面，只覆盖远程漫画与远程根目录项，不额外增加本地扫描压力。
  - 移动端 `SmoothCustomScrollView.cacheExtent` 已提升到 `1.0` 屏，桌面端保持原先策略。
- 当前认知补充：
  - 远程·已下载虽然展示的是“服务端本地已下载”，但手机客户端拿到的仍是远程 HTTP 封面资源，不应该按“本机文件路径已缓存”来理解它的全部性能瓶颈。
  - 后续如果还要继续优化，应优先从“稳定 provider key、预热范围、解码尺寸、磁盘命中路径”继续细抠，而不是先考虑改服务端接口。
- 本轮建议真机重点复测：
  - 进入“远程·已下载”，等待首屏封面全部出现后快速下滑 `3` 到 `5` 屏再快速上滑，确认已加载过的封面回显明显更快。
  - 聚合档重复同样动作，确认远程项变快，本地项无回归。
  - 切后台再回前台，确认远程已下载原有强制刷新逻辑仍正常，不影响封面显示。

## 22. 2026-05-15 远程封面收口与图集连续阅读白边修正

- 本轮继续收口两个后续暴露出来的问题：
  - `本地文件管理 -> 远程·已下载 / 远程·图集` 的列表卡片和快捷弹层里，部分远程图集仍显示成首张内容图，或者没有独立封面时出现比例异常。
  - 图集连续纵向阅读时，部分图片交界处会出现细白边，看起来像上下两张图没有完全拼上。
- 根因收口：
  - 远程详情页之所以封面正常，是因为详情页还会额外走一次 `resolveLocalComicCover(...)`，能从本地目录里直接把真正的 `cover.jpg` 找回来；而远程列表卡片和根目录拼贴更多依赖服务端快照里的 `coverPath / coverUrl`。
  - 服务端扫描器此前会把章节首张图片当作 `coverPath` 回传，远程客户端又可能继续命中旧的错误封面缓存，所以会表现成“列表不对、详情页对”。
  - 图集列表比例异常并不是封面文件本身坏了，而是列表提速时使用 `ResizeImage(cacheWidth, cacheHeight)` 双向限尺寸后，对“直接拿首张图当封面”的图集更容易产生视觉压扁感；详情页没有走这套双向限尺寸，所以显示正常。
  - 连续纵向阅读白边主要出在 `ComicImage` 自己按浮点高度估算后再向上取整、外层又包了一层 `Center`，在高 DPI 设备上容易在图片交界处抠出 1px 左右的缝。
- `lib/server/local_resource_scanner.dart`
  - 服务端资源扫描现在会优先使用现成的 `cover.jpg / cover.jpeg / cover.png / cover.webp`。
  - 如果下载记录或解析后的本地项里已经带有真实封面路径，也会优先复用那条路径，而不是回退到首张内容图。
  - 章节封面与项目封面都统一走“优先显式封面、否则首张图片”的顺序。
- `lib/server/server_app.dart`
  - 远程封面 URL 现在会附带与封面文件本身绑定的版本参数，避免客户端继续命中此前缓存下来的错误首图。
  - 这里的版本参数只用于缓存失效，不会在服务端额外生成新的封面图片文件。
- `lib/pages/local_library_page.dart`
  - 本地文件管理页的远程根卡拼贴已改为复用统一的远程封面 provider，不再单独走 `Image.network(...)`。
  - 远程根列表和远程根内容页读取时会强制拿最新远程快照，降低“服务端已修正封面但列表仍抱着旧快照”的概率。
  - 远程图集 / 远程已下载卡片与快捷弹层的封面解码策略已收回为“只限宽，不限高”，避免没有独立封面时把首张图解码成看起来被左右挤压的比例。
- `lib/components/comic_tile.dart`
  - 通用下载卡片封面也同步从双向 `ResizeImage(cacheWidth, cacheHeight)` 调整为仅限 `cacheWidth`，保留列表提速收益，同时降低图集首图被压扁的风险。
- `lib/pages/local_comic_detail_page.dart`
  - 远程封面在详情页中也支持点击放大预览，行为与本地封面一致。
- `lib/pages/reader/image.dart`
  - `ComicImage` 的自动高度计算已改为按物理像素对齐，而不是直接 `ceilToDouble()`。
  - 已去掉加载完成后的外层 `Center` 包裹，减少连续纵向阅读时图片交界处出现白缝的概率。
- 当前认知补充：
  - “让服务端重新扫描生成新的 `coverPath` 快照”这里的“生成”只是重新计算快照里的路径字段，不会重新生成一张新的封面图片。
  - 客户端本地 `remote_library_covers` 目录仍然只是显示缓存，不是服务端输出的新封面副本。
- 本轮已完成校验：
  - `flutter analyze lib/pages/local_library_page.dart lib/pages/reader/image.dart lib/server/local_resource_scanner.dart lib/server/server_app.dart lib/foundation/remote_library_data_source.dart lib/pages/local_comic_detail_page.dart lib/pages/download_page.dart lib/components/comic_tile.dart`
  - `git diff --check`
- 本轮建议真机重点复测：
  - 在服务端执行一次资源刷新后，重新进入 `本地文件管理 -> 远程·已下载 / 远程·图集`，确认列表卡片、快捷弹层与详情页的封面是否统一。
  - 对没有独立封面的图集，确认列表里虽然仍然使用首张图片，但显示比例不再出现明显左右挤压。
  - 连续纵向阅读同一组图集图片，确认交界处白边是否明显减少或消失。

## 23. 2026-05-15 封面预览、目录浏览统计与 Root Android/data 枚举收口

- 本轮继续围绕两个方向收口：
  - 本地 / 已下载封面显示体验继续向原项目靠拢，包括列表封面清晰度、详情页封面点击预览方式。
  - Android 内置目录浏览器继续收敛 Root / Shizuku / 普通目录三条读取链路，重点核实 `Android/data` 枚举缺项问题。
- `pubspec.yaml`、`lib/foundation/def.dart`、`lib/pages/settings/settings_page.dart`
  - 版本号已从 `1.8.18` 统一递增到 `1.8.23`，设置页展示同步更新为 `V1.8.23`。
- `lib/components/comic_tile.dart`、`lib/pages/local_library_page.dart`
  - 列表封面已去掉基于卡片宽度的 `ResizeImage` 限宽解码，恢复为直接使用原始 `ImageProvider`，减少小卡片封面发糊的问题。
  - 远程图集快捷弹层封面同步取消限宽解码，避免图集首图在小尺寸卡片里被过度降采样。
- `lib/pages/local_comic_detail_page.dart`
  - 详情页封面点击预览已从 `Dialog + InteractiveViewer` 改为全屏 `PhotoView` 页面，行为更接近原项目，不再带圆角弹窗裁剪感。
- `lib/pages/settings/internal_directory_browser.dart`
  - 内置目录浏览器默认初始路径已改为 `普通目录` 对应的 `/storage/emulated/0`，不再在空路径时先落到 `/` 并触发权限错误。
  - 快捷入口切路径时现在会同步切换访问模式，不再出现点了 `Root/应用目录` 或 `Shizuku/应用目录` 但实际仍沿用旧模式读取的情况。
  - “上一层”卡片右侧已增加当前路径的目录 / 文件数统计，字号与路径副文案一致；统计区改为自适应换行，不再被右侧箭头遮挡。
  - 对 `Android/data` 目录在 Root / Shizuku 模式下新增“已知直达路径存在则补回列表”的兜底逻辑，用于缓解父层枚举漏项。
- `lib/pages/settings/app_settings.dart`
  - 新增 `existsWithRoot(...)` 与 `existsWithShizuku(...)` 通道封装，供目录浏览器在父层枚举漏项时补做完整路径存在性检测。
- `android/app/src/main/kotlin/lingxue/picakeep/MainActivity.kt`
  - Root 模式下共享存储路径现在会优先映射到真实路径 `/data/media/0/...` 再做目录枚举、读写、存在性检测、移动和删除，避免先命中 `/storage/emulated/0/Android/data` 那条会漏项的 FUSE 视图。
  - 为候选路径解析新增 `rootCandidatePaths(...)` / `realSharedStoragePath(...)`，只改变 Root 链路，不影响当前 Shizuku 行为。
- `Android/data` 问题本轮结论：
  - Root 下 `/data/media/0/Android/data` 的真实列表与真机实际目录完全对上；在本机设备上，示例清单 `171` 项对账结果为 `missing=0 / extra=0`。
  - Root 下 `/storage/emulated/0/Android/data` 只枚举出 `96` 项，缺 `75` 项，其中包括 `com.github.pacalini.pica_comic` 与 `com.deepseek.chat`，确认问题根因在 FUSE 视图枚举不完整。
  - Shizuku 当前实现仍基于 `newShizukuProcess(... "sh -c", command)`，在这台 MIUI 设备上仍受 `shell` 视角限制；后续若要追平 MT，需要研究 Shizuku 官方推荐的 `UserService` 或 Binder 路线，而不是继续堆 `ls/find`。
- 本轮已完成校验：
  - `flutter analyze lib/components/comic_tile.dart lib/pages/local_library_page.dart lib/pages/local_comic_detail_page.dart lib/pages/settings/internal_directory_browser.dart lib/pages/settings/app_settings.dart lib/pages/settings/settings_page.dart`
  - `.\android\gradlew.bat -p android :app:compileDebugKotlin`
- 当前提交注意事项：
  - `android/local.properties` 仍为本机配置，不纳入源码提交。
  - Shizuku 完整枚举 `Android/data` 仍是后续研究项；当前已确认 Root 可通过真实路径收口，Shizuku 仍需更深层实现。

## 24. 2026-05-16 已下载详情页进入阅读器返回栈修复

- 本轮只收口“已下载页 -> 查看详情 -> 详情页内点阅读 -> 阅读器返回”这一条导航链，不扩散改其他功能。
- 现象确认：
  - 本地与远程已下载漫画都存在同一问题。
  - 从已下载页进入详情页后，再从详情页点“阅读”，阅读器退出时需要返回两次，并且最终会直接落回已下载页，而不是先回详情页。
  - 根因与之前图集处理过的一次类似问题一致，属于压栈落在了不一致的 `Navigator` 上。
- 根因收口：
  - `lib/pages/download_page.dart` 里的 `_toComicInfoPage(...)` 之前直接使用传入 `context` 做 `Navigator.of(context).push(...)`；当入口来自已下载页底部详情弹层时，这个 `context` 不是稳定的内层页面导航上下文。
  - 同文件 `DownloadedComicInfoView` 的“查看详情”之前是先 `App.globalBack()` 再继续打开详情页，进一步放大了“先弹错栈、再压错栈”的风险。
  - `lib/pages/local_comic_detail_page.dart` 里的 `_onRead(...)` 原先统一走 `App.openReader(...)` 的全局根栈；详情页本身如果压在内层栈，阅读器却压在根栈，就会形成返回链路断层。
  - `lib/pages/reader/comic_reading_page.dart` 顶部返回按钮原先走 `App.globalBack()`，也默认按全局根栈处理，不适合这类“阅读器跟随详情页同栈打开”的场景。
- 本轮代码修正：
  - `lib/pages/download_page.dart`
    - `_toComicInfoPage(...)` 已改为统一走 `App.pushInner(...)`，不再依赖临时 `context`。
    - 已下载详情弹层里的“查看详情”改为先关闭当前弹层自己的 route，再走统一详情入口。
    - 桌面右键菜单、批量菜单等其他“查看漫画详情”入口也已同步复用这条新链路。
  - `lib/foundation/app.dart`
    - `App.openReader(...)` 已增加可选 `BuildContext? context`，允许在需要时把阅读器压到当前详情页所在的 `Navigator`，同时保持旧调用点默认行为不变。
  - `lib/pages/local_comic_detail_page.dart`
    - 详情页 `_onRead(...)` 已改为传入当前页 `context` 打开阅读器，确保栈结构变为“已下载页 -> 详情页 -> 阅读器”。
  - `lib/pages/reader/comic_reading_page.dart`
    - 顶部返回按钮已从 `App.globalBack()` 改为 `Navigator.of(context).maybePop()`，优先关闭阅读器自身所在 route。
- 当前预期行为：
  - 已下载页进入详情后，从详情页打开阅读器，返回一次应先回到详情页；再返回一次才回到已下载页。
  - 已下载页弹层里直接点“阅读”的旧行为保持不变。
  - 图集 / 本地文件管理页已有“查看详情 / 继续阅读”链路不应受这次调整影响。
- 本轮已完成校验：
  - `flutter analyze lib/pages/download_page.dart lib/foundation/app.dart lib/pages/local_comic_detail_page.dart lib/pages/reader/comic_reading_page.dart`
  - 结果：`No issues found!`
- 仍待手动回归：
  - 本地已下载：查看详情 -> 阅读 -> 返回一次到详情页。
  - 远程已下载：查看详情 -> 阅读 -> 返回一次到详情页。
  - 已下载弹层直接点“阅读”：确认进入与返回不回归。
  - 图集 / 本地文件管理页：确认原有详情与阅读入口不回归。

## 25. 2026-05-17 受限目录回收站、应用日志与 Profile 真机验证

- 本轮收口三个近期暴露出来的点：
  - 跨应用 `Android/data` managed-download 条目的删除 / 恢复 / 彻底删除链路继续向 Root / Shizuku 受限路径统一。
  - 设置页 `APP > Logs` 重新纳入维护，不再把日志页实现继续堆在 `app_settings.dart` 单文件里。
  - 真机运行方式确认区分 `debug` 与 `profile`，并完成 `profile` 模式安装运行验证。
- 受限目录与 Shizuku 认知补充：
  - `download.db` 中存在历史记录但实际目录已迁移到 NAS 或已不存在时，Shizuku UserService 会对这些目录打印“目录不存在或当前应用不可访问”。这属于预期噪音，不代表 Root / Shizuku 授权失效，也不影响主列表继续加载。
  - 当前主流程会继续 fallback / 跳过这些失效条目；后续若要降噪，优先把“数据库记录存在但目录缺失”的场景降级为业务状态，而不是按异常堆栈持续输出。
- `lib/foundation/log.dart`
  - `LogManager` 新增 `recordingEnabled` 运行期开关。
  - `addLog(...)` 现在会尊重该开关；关闭后新日志不再写入，已有日志继续保留，可清空 / 导出。
- `lib/pages/settings/log_settings.dart`
  - 新增独立日志页文件，`LogSetting` 已从 `app_settings.dart` 拆出。
  - 页面顶部新增“记录日志”开关，保留清空、禁用长度限制、导出、复制单条日志能力。
- `lib/pages/settings/settings_page.dart`
  - 新增 `part 'log_settings.dart';`，`APP > Logs` 入口继续复用原有设置页路由。
- `lib/foundation/app.dart`
  - `AppStartupTrace.log(...)` 现在同时输出到控制台和应用内 `LogManager`，标题统一为 `Startup`。
  - 启动阶段的 `main.start`、`widgetsBinding.ready`、`runApp.called`、首帧等事件重新能在 App 内 Logs 页面查看。
- 真机运行验证：
  - 已先误以 `debug` 启动一次，随后停止 debug 进程并重新执行 `flutter run --profile -d f294cd23`。
  - `profile` 构建产物为 `build\app\outputs\flutter-apk\app-profile.apk`，大小约 `39.4MB`。
  - 手机 `22127RK46C` 上已确认 `Launching lib\main.dart on 22127RK46C in profile mode...`、`assembleProfile` 成功、安装成功，Dart VM Service 可用。
- 本轮已完成校验：
  - `flutter analyze`：0 issues
  - `flutter run --profile -d f294cd23`：已安装并启动到 Android 真机
- 仍待后续考虑：
  - Android 原生 `Log.w` / logcat 日志不会自动进入 Flutter `LogManager`；如果要把 Shizuku / Root 原生日志也显示到 App 内 Logs，需要单独做平台通道或事件桥接。
  - 对 `download.db` 残留目录缺失导致的 warning，可后续按业务状态降级显示，减少 profile / logcat 噪音。

## 27. 2026-05-24 “我”页工具卡片与工具页外显按钮排序收口

- 本轮重点不是新增工具能力，而是把“我”页工具卡片的展示方式与“工具”页的自定义入口统一起来，解决外显按钮写死、窄屏换行和底部视觉贴边三个问题。
- `lib/pages/tool_display_config.dart`
  - 新增共享配置文件，统一维护工具定义、外显按钮默认显示项、排序归一化、设置读写与可见项解析。
  - 当前全部可配置工具包括：`服务信息`、`本地文件管理`、`存储空间`、`图集`、`APP能力`、`回收站`、`清理缓存`。
  - 默认首批外显按钮已收口为：`服务信息 / 本地文件 / APP能力 / 回收站`。
  - 新增工具定义后，如果旧配置里没有对应 id，会按默认顺序自动追加到末尾，避免历史配置把新工具永久漏掉。
- `lib/base.dart` / `lib/foundation/local_library_settings.dart` / `lib/foundation/app.dart`
  - 设置项中新增外显按钮顺序与显示状态两个持久化槽位，并在读取时做归一化。
  - `App` 新增 `toolDisplayConfigVersion` 与 `notifyToolDisplayConfigChanged()`，用于在保存外显配置后刷新“我”页与相关入口。
- `lib/pages/me_page.dart`
  - “我”页工具区域已收口为单张一体式卡片，不再把标题区和快捷按钮区割裂成两块。
  - 卡片底部的外显按钮已从 `Wrap` 改为卡片内横向滚动条；超出宽度时不再换行，而是在右侧叠加渐变遮罩，让溢出过渡更自然。
  - 卡片长按可直接进入“工具”页的 `外显按钮与排序` 模式。
  - 页面底部已补显式额外留白，确保类似手机高度较紧时，滚动到底部后工具卡片下方仍保留至少一行外显按钮高度级别的空白。
- `lib/pages/tools.dart`
  - 工具页已由静态列表改为 `StatefulWidget`，支持普通模式与 `外显按钮与排序` 自定义模式切换。
  - 标题栏右侧新增自定义入口；进入模式后顶部改为类似已下载页长按选择态的独立栏，标题统一为 `外显按钮与排序`。
  - 自定义模式下：
    - 点按工具项切换是否外显。
    - 长按约 `800ms` 进入拖拽排序。
    - 选中态遮罩、描边和渐变高亮已对齐已下载 / 图集页的多选视觉，但透明度已按本轮要求下调。
  - 本轮确认：全部工具项都可参与显示与排序，不再只限部分工具。
- 交互与体验结论：
  - “我”页展示结果与“工具”页排序配置共享同一份持久化数据。
  - 修改显示项或顺序后，无需额外手动同步；重新进入页面即可维持一致。
  - 当前浏览器预览仍受 `sqlite3 -> dart:ffi` 限制阻塞，故这一轮主要完成了代码层核对与主项目同步，运行态 UI 仍建议在 Android 真机或桌面 Flutter 目标继续复测。
- 本轮同步回主项目的核心文件：
  - `lib/base.dart`
  - `lib/foundation/app.dart`
  - `lib/foundation/local_library_settings.dart`
  - `lib/pages/me_page.dart`
  - `lib/pages/tools.dart`
  - `lib/pages/tool_display_config.dart`
  - `交接文档.md`

## 28. 2026-05-27 压缩包内部文字乱码与章节显示收口

- 本轮收口的问题范围不是整个本地库，而是 **ZIP / CBZ 压缩包内部 entry filename 解码错误** 带来的章节名乱码，以及详情页章节头部与章节卡片状态的显示收口。
- 现象确认：
  - 外层漫画标题可以正常显示；
  - 但压缩包内部章节名 / 分组名在下载弹层、详情页等显示面仍可能出现乱码；
  - 同时压缩包章节卡片还会错误沿用普通下载章节的 `downloadedEps` / 下载勾语义，导致第一章或个别章节灰色、右侧勾语义误导。
- 根因收口：
  - `lib/foundation/archive/backends/dart_zip_backend.dart`
    - ZIP `ArchiveEntry.path` 现在明确来自 `central directory` 的原始 filename bytes，而不是继续依赖上游库可能已经错解过的字符串。
    - 解码策略已收口为：
      - 若 ZIP 显式带 `UTF-8 flag`，按 UTF-8 处理；
      - 未显式标记时，先保护纯 ASCII 路径；
      - 再对 `utf8 / shiftJis / gbk` 候选结果做启发式比较，而不是只看“utf8 是否抛异常”。
    - 关键经验是：`UTF-8 decode` 成功不等于结果正确，乱码修复必须回到 raw bytes 层判断，而不是对已经乱码的 `String` 二次补救。
  - `lib/foundation/local_library.dart`
    - 本地库 archive 章节显示链路继续保持分层：
      - `_buildArchiveEpisodes(...)` 负责从 archive entry path 构建章节与真实名称；
      - `archiveDisplayChapterNames(...)` / `buildArchiveChapterDisplayName(...)` 只负责 display name 组合，不承担重新猜编码职责。
    - 这次也再次确认：archive 章节真实名是在扫描 / 刷新时重建的，不能把页面乱码简单归因到“缓存没刷新”。
  - `lib/pages/download_page.dart`
    - 下载 / 阅读弹层中的 archive 章节显示继续复用共享 helper，不再单独分叉章节命名逻辑；
    - 同时补充压缩包章节卡片状态适配：压缩包章节统一按可读状态展示，不再显示普通下载语义的右侧勾。
  - `lib/pages/local_comic_detail_page.dart`
    - 本地详情页的 archive 章节显示同样继续复用共享 helper；
    - 章节状态判断已补 archive 场景，压缩包章节统一视为可读，不再因为普通下载章节状态判断而灰色；
    - 长按删章节逻辑继续只保留给非压缩包章节，避免把下载删除语义误带到 archive 场景。
    - 同时把章节 header 收口为更稳定的左侧聚合布局，避免 `章节 / 共X章 / 序号 / 排序` 被 `Spacer` 推到过度分散的位置。
- 本轮修复后的判断原则：
  - 外层标题正常、内部章节名乱码时，优先追 `central directory raw bytes -> ArchiveEntry.path -> archive chapter realNames -> display helper` 这条链；
  - 不要先把问题归因到 UI 拼接，也不要先把问题归因到缓存；
  - 只有在源头解码结果已经确认正确、页面仍旧显示旧值时，缓存问题才值得升优先级。
  - 压缩包章节卡片状态只在 UI 层做 archive 适配，不反向修改 `downloadedEps` 的数据语义。
- 本轮涉及文件：
  - `lib/foundation/archive/backends/dart_zip_backend.dart`
  - `lib/foundation/local_library.dart`
  - `lib/pages/download_page.dart`
  - `lib/pages/local_comic_detail_page.dart`
  - `交接文档.md`
- 本轮已完成校验：
  - `flutter analyze`
  - `flutter analyze lib/pages/download_page.dart lib/pages/local_comic_detail_page.dart`
  - 手动确认压缩包章节名已恢复正常显示
  - 手动确认压缩包章节第一章不再灰色，且下载 / 阅读弹层右侧不再显示误导性的下载勾
- 校验补充说明：
  - Windows 侧曾尝试 `flutter build windows --debug`，但当前环境存在 `sqlite3.dll` 安装阶段权限型失败噪音；该问题与本轮压缩包文字乱码修复本身无直接关系，不应混入本轮根因结论。

## 29. 2026-05-24 远程阅读加载进度、图集封面缓存与历史同步收口

- 本轮主要围绕远程阅读体验、图集页封面回滑性能，以及“我”页历史记录同步三个问题收口。
- `lib/foundation/image_loader/stream_image_provider.dart`
  - 新增 `StreamImageLoadResult`，允许图片流同时携带 `expectedTotalBytes`。
  - `StreamImageProvider` 保持旧构造兼容，同时新增 `withProgress(...)`，用于远程图片首帧加载时就能把总大小传给 `ImageChunkEvent`。
- `lib/foundation/remote_library_data_source.dart`
  - 远程阅读图片 provider 已改为走 `StreamImageProvider.withProgress(...)`。
  - `RemoteLibraryClient.loadImageWithProgress(...)` 会从 HTTP 响应中读取 `contentLength`，从而驱动阅读页圆形进度条显示真实百分比，而不是只有不确定转圈。
  - `RemoteLibraryEpisode` 新增 `pageSizes`，用于承接服务端返回的每页真实图片宽高。
  - `RemoteLibraryReadingData.imageSize(...)` 会按章节和页码返回对应页尺寸，供阅读器预占位。
- `lib/pages/reader/reading_data.dart` / `lib/pages/reader/image.dart` / `lib/pages/reader/image_view.dart`
  - 阅读数据基类新增 `imageSize(...)` 默认空实现。
  - `ComicImage` 新增 `knownImageSize`，有真实宽高时按真实比例提前占位。
  - 连续纵向阅读和双页阅读中的 `ComicImage` 都会传入对应页尺寸，减少远程图集 / 漫画因图片宽高不一导致加载完成后把后续内容顶下去。
  - 保留圆形进度条；如果远程服务没有返回 `Content-Length`，仍会回退到不确定加载态。
- `lib/server/local_resource_scanner.dart` / `lib/server/server_app.dart`
  - 服务端扫描资源时会轻量读取 PNG / JPG / WebP 图片头，记录每张图片宽高。
  - 章节详情 payload 在 `includePages` 时新增 `pageSizes`，与 `pages` 一一对应。
  - 当前尺寸读取只读图片头，不做完整图片解码；旧快照或旧服务端没有 `pageSizes` 时，客户端会自动退回比例预估。
- `lib/components/comic_tile.dart` / `lib/pages/local_library_page.dart`
  - 图集 / 资源库页新增专用封面解码优化：`DownloadedComicTile.optimizeCoverDecode` 默认关闭，只有图集页传入 `true`，因此已下载页不受影响。
  - 图集页封面只限制 `cacheWidth`，不限制 `cacheHeight`，避免封面被左右压扁；同时按 `设备像素比 * 1.35` 保留清晰度余量，降低大封面 / 首页当封面时回滑重新解码过慢的问题。
  - 图集页 `SmoothCustomScrollView.cacheExtent` 提高到一屏高度，并为远程根目录拼贴封面开启 `gaplessPlayback`，减少滚动回滑时封面重新显示的观感。
- `lib/foundation/history.dart` / `lib/pages/history_page.dart`
  - `HistoryManager.clearHistory()` 与 `HistoryManager.remove(...)` 现在会通知 `me_page` 刷新，避免历史页已清空 / 删除后，“我”页历史预览仍显示旧缓存。
  - 历史页清空和删除单条记录时同步维护搜索结果列表，避免搜索模式残留已删除条目。
- 本轮已完成校验：
  - `flutter analyze lib/foundation/image_loader/stream_image_provider.dart lib/foundation/remote_library_data_source.dart lib/pages/reader/image.dart`
  - `flutter analyze lib/server/local_resource_scanner.dart lib/server/server_app.dart lib/foundation/remote_library_data_source.dart lib/pages/reader/reading_data.dart lib/pages/reader/image.dart lib/pages/reader/image_view.dart lib/foundation/image_loader/stream_image_provider.dart`
  - `flutter analyze lib/components/comic_tile.dart lib/pages/local_library_page.dart`
  - `flutter analyze lib/foundation/history.dart lib/pages/history_page.dart lib/pages/me_page.dart`
  - `git diff --check`
- 后续真机 / 桌面回归建议：
  - 远程图集连续纵向阅读时，确认加载进度为圆形百分比，且不同宽高图片加载完成后不再明显顶动后续内容。
  - 服务端刷新资源后再进入远程阅读，确认 `pageSizes` 生效；若未刷新服务端快照，旧数据会继续走比例预估。
  - 图集页快速下滑再回滑，确认大封面回显速度提升，且封面比例不再被压扁。
  - 历史页清空 / 删除单条后返回“我”页，确认历史记录预览同步刷新。

## 30. 2026-05-28 加密压缩包阅读链与会话密码管理收口

- 本轮收口的问题来自实测：1.7 GB 加密 ZIP（`AAAtest_download (2)`，38 章 / 62 页）输任意非空密码都“解锁成功”，但封面破图、阅读器每页 `Exception: Invalid image data`。
- 根因分三层，互相掩盖：
  1. `archive_reading_service.dart::tryUnlock` 旧实现只调 `backend.openIndex(...)` 并以“能列目录 + 有 image entry”作为成功条件；ZIP 中央目录从来不加密、列目录不需要密码，因此任何密码都被错判为对。
  2. `dart_zip_backend.dart::readEntry` 主路径在解密失败时 `catch (_)` 落到 `_readEntryFallback`，fallback 直接对加密字节做 `RawZLibFilter` inflate，结果是乱码字节流喂给 `dart:ui.ImmutableBuffer.fromUint8List` → `Invalid image data`。
  3. 中央目录解析不识别 WinZip AES extra field `0x9901`，加密 entry 的真实压缩方法 / AES 强度从未被读出来。
- 不能直接换 archive 库解决：实测 archive 3.6.1 的 `ZipFile` 构造硬调 UTF-8 解码 local file header filename，遇到日文 ZIP（`[コデインガール ...]`）抛 `FormatException: Unexpected extension byte`；archive 4.0.9 修了 filename 解码但 `ZipFile.read` 对 zip64 extra field（id=1）的 dataSize 没被 `readBytes` 消费，本仓库的 zip64 包会撞 `RangeError`。两版库都不能透明使用。
- 最终方案：彻底绕开 archive 库的 `ZipDecoder`，自己实现加密 entry 的解密链。
- `lib/foundation/archive/backends/dart_zip_backend.dart`
  - `_parseCentralDirectoryWithOffsets` 在原有 zip64 / extra 循环里追加识别 `0x9901`（vendorVersion / vendorId / encryptionStrength / 真实 compressionMethod），并把 `isAesEncrypted` / `aesStrength` / 内层真实 `compressionMethod` 写回 `_CdEntry`；同时修掉一处 extra field dataSize 越界。
  - `_CdEntry` 增加 `isAesEncrypted` / `compressionMethod` / `aesStrength` 字段；`isEncrypted` 同时考虑 ZipCrypto flag 与 AES extra。
  - `readEntry` 主路径重写：加密 entry 走 `_readAesEntry` 或 `_readZipCryptoEntry`，非加密 entry 走 `_readEntryFallback`，整条主路径不再调用 archive 库的 `ZipDecoder` / `InputFileStream`。
  - `_readAesEntry`：自己读 local file header → 切出 salt / pwdVerify / cipher / file mac → `_pbkdf2HmacSha1` 派生 (encKey, macKey, pwdCheck) → 密码两字节校验失败抛 `wrongPassword` → `pointycastle.HMac(SHA1Digest(),64)` 校验 file mac → `_aesCtrXorZip` 用 `pointycastle.AESEngine` 处理 LE counter（nonce=1 起）逐 block XOR 解密 → 内层 `method=8` 时 `RawZLibFilter` inflate。
  - `_readZipCryptoEntry`：3 个 32-bit 密钥用密码字节初始化（`_zipCryptoInitKeys`），用自实现的 `_zipCryptoCrc32Update`（多项式 0xEDB88320，与 zlib 反射型 CRC32 等价）每字节滚动；前 12 字节 encryption header 中第 12 字节按 ZIP 规范对照 `flags & 0x08 ? lastModFileTime>>8 : crc32>>24`，不一致即 `wrongPassword`；通过则继续解密 payload，再按内层 method 走 store / inflate。
  - `_readEntryFallback` 入口对 `match.isEncrypted` 直接抛 `wrongPassword`，杜绝任何加密字节走裸 inflate 的路径；旧 `catch (_)` 与 `_looksLikeWrongPassword` heuristic 一并删除。
  - `capabilities.supportsAesZip` 改为 `true`，对调用方诚实表态。
- `lib/foundation/archive/archive_reading_service.dart`
  - `tryUnlock` 真实验密码：`openIndex` 后挑一个最小的加密 entry（image 优先）实际 `readEntry` 一次；成功才 `setSessionPassword + evictIndex`；`wrongPassword / passwordRequired` 返回 `false`；其它失败按原码 rethrow 给 UI。
  - 旧的“列目录通过即视为成功”逻辑保留为兜底（应对未加密包仍想触发 unlock 流程的少数边界情况）。
- `pubspec.yaml`
  - 引入 `pointycastle: ^3.9.1` 直接依赖（先前是 archive 库的 transitive，未导出 `Aes` / `deriveKey`）。
  - `archive` 升至 `^4.0.9`，但仅作为 transitive / `RawZLibFilter` 仍来自 `dart:io`，源码里不再 `import package:archive`。
- `lib/foundation/archive/archive_models.dart`
  - `ArchiveEntry` 新增 `isAesEncrypted` / `compressionMethod`，默认值兼容现有构造点；本轮主要用于 backend 内部，UI 暂未直接使用。
- `lib/foundation/local_library.dart`
  - `LocalLibraryComicItem._localCoverPath` 由 `final` 改为可变；`markArchiveLocked()` 同时清空 `_localCoverPath`，避免“忘记密码”后仍残留旧封面。
  - `_scanSingleArchiveFile`：扫描期对“已记住会话密码”的加密包也尝试 `extractCoverToCache`，避免每次手动刷新后封面再次破图（密码源是 `ArchivePasswordStore.getSessionPassword`）。
  - `refreshArchiveCoverFor` 把 `extractCoverToCache` 返回的物理路径写回 `item._localCoverPath`，否则解锁后封面仍是 null。
- `lib/pages/download_page.dart`
  - 加密压缩包详情抽屉的 source 行右侧新增“忘记密码”图标（`Icons.lock_reset`），仅当 `isArchiveItem && archiveEncrypted && session 内有密码` 显示。
  - 点击行为：`ArchivePasswordStore.forget(path)` + `markArchiveLocked()`（连封面缓存路径一起清）+ `App.notifyLocalDataChanged()` 让外层列表重建 + `Navigator.maybePop()` 关闭抽屉；不再弹再次输密码框，让用户自己点卡片走原解锁流程。
- `lib/pages/settings/archive_settings_page.dart`
  - 旧的“自动解密密码”二级页 `ArchivePasswordListPage` 整个删除；改为在 `ArchiveSettingsPage` 内用 `ExpansionTile` 就地展开，子项直接显示密码明文 + 删除按钮，末尾“添加密码”行仍走原弹窗。
- 实测验证（人工）：
  - 错密码 → “密码错误或压缩包无法解密”，不再假成功。
  - 密码 `114514`（ZipCrypto + zip64 + 日文 / 简中混合 entry path）→ 解锁成功 + 封面立即刷新 + 阅读器逐页正常显示。
  - 普通未加密 ZIP / CBZ → 不退化。
  - 第 28 节修过的 GBK / Shift-JIS 章节名 ZIP → 章节名解码不退化（本轮没碰 `_decodeString` / `_scoreDecodedFilename`）。
  - 加密压缩包详情抽屉 → “忘记密码”图标点击后抽屉关闭、列表卡片回到锁定 + 破图状态。
  - 工具 / 压缩包设置 → “自动解密密码”就地展开列出密码，删除 / 添加均生效。
- 校验：
  - `flutter analyze lib/foundation/archive/` 与 `flutter analyze lib/` 全程 0 issue。
- 后续可能仍需收口（未在本轮处理）：
  - app 重启后，session password 会丢；只有勾选过”加入默认密码”的包才能在 `_autoUnlockEncryptedArchives` 中重新解锁。如果要让封面跨重启稳定，需要把”某个 archive 已解锁过 + 用过哪个 default password”这条状态持久化到 disk，再让 `_scanSingleArchiveFile` / `refreshArchiveCoverFor` 复用。
  - AES 解密目前是单 entry 串行实现，1.7 GB 包内 17 KB webp 实测毫秒级；如果以后出现 100 MB+ 单页 entry，需要改成流式分块解密 + inflate 以避免一次把整个 entry 装进 RAM。
  - `pubspec.yaml` 的 `archive: ^4.0.9` 实际只剩 transitive 价值（`RawZLibFilter` 来自 `dart:io`）；如果未来想彻底解耦，可在确认无任何子模块依赖 `archive` 公开 API 后移除。

## 31. 2026-05-28 桌面端侧栏顶部对齐与移动端详情 Sheet 交互优化

- 桌面端侧栏（`DownloadedComicInfoView` 从已下载页 / 图集页弹出）顶端被标题栏按钮区遮挡，与原项目 PicaComic 视觉不一致。
- 根因：`download_page.dart` / `local_library_page.dart` 中 `showSideBar(context, ...)` 传的是 `NaviPane` 内部 `Positioned(top:36)` 区域的局部 `context`，导致 `SideBarRoute` 的全高容器从 y=36 开始渲染，顶部 36px 透明。
- 修复：两处改为 `showSideBar(App.globalContext ?? context, ...)`，路由推入根导航器，与原项目 PicaComic 用法一致。
- 额外修复：`WindowFrame` Stack 外层包 `ColoredBox(color: theme.colorScheme.surface)`，确保 36px 标题栏区域在没有页面内容覆盖时呈主题底色，不透明到桌面背景。
- 移动端底部弹层由 `showModalBottomSheet（固定高度）` 改为 `DraggableScrollableSheet`：
  - 默认弹出占屏幕 3/5（`initialChildSize: 0.6`）
  - 向上拖动平滑扩展至最大 4/5（`maxChildSize: min(0.8, (height−92)/height)`），矮窗口自动兜底保证 AppBar 可见
  - 内容结构：固定 header（封面+信息+章节标题行）+ `Expanded(GridView)` + 固定底部按钮，header 不随滚动消失
  - 新增 `Listener.onPointerSignal` 拦截鼠标滚轮，通过 `DraggableScrollableController.jumpTo` 手动驱动 Sheet 扩展（补 `DraggableScrollableSheet` 不响应 `PointerScrollEvent` 的缺口）
  - `DraggableScrollableController` 随 `whenComplete` 自动释放
- 涉及文件：`lib/components/window_frame.dart`、`lib/pages/download_page.dart`、`lib/pages/local_library_page.dart`

## 32. 2026-05-28 AES-ZIP inflate raw deflate 修复

- 现象：第 30 节收口的 AES-ZIP 解密链在实测 AE-1 + AES-256 + deflate（如 `114514.zip`）时报"压缩包已损坏或格式不正确"，而 AE-2 + ZipCrypto 包（`AAAtest_download (2)`）正常。
- 根因：`_inflateRaw` 调用 `RawZLibFilter.inflateFilter()` 默认 `windowBits=15`，会期待 zlib header（2 字节 CMF/FLG）；而 ZIP entry 的压缩数据是裸 deflate 流（无 header），对应 `windowBits=-15`。Python 验证了同样的明文字节用 `zlib.decompress(data, -15)` 解压完全正确，而默认参数报 `invalid code lengths set`。
- ZipCrypto 包能工作是因为当时也碰巧走了同一路径但密码和内容凑巧让 inflate 不报错，或实测的 entry 是 method=0（store），实际绕过了 inflate。
- 修复：`dart_zip_backend.dart` 中两处 `RawZLibFilter.inflateFilter()` 均改为 `RawZLibFilter.inflateFilter(windowBits: -15)`：
  - `_inflateRaw`（AES / ZipCrypto 解密后调用）
  - `_readEntryFallback` 内的 method=8 分支（非加密 entry 走 fallback 时调用）
- 涉及文件：`lib/foundation/archive/backends/dart_zip_backend.dart`
- 校验：`flutter analyze lib/` 0 issue；Python 同算法验证 inflate 结果正确（size=7832 = usize，开头 `RIFF...WEBPVP8 ` 标准 webp）。

## 33. V1.9.8 Android 图标切换黑条修复与阅读页状态栏动效收口

### 33.1 阅读页顶部黑条（已修复）

**根本原因**

日志中出现 `D/VRI[LauncherBlue]`，说明 launcher 入口是 `LauncherBlue` 这个 `activity-alias`，不是 `MainActivity` 本身。Flutter embedding v2 靠 Activity 上挂的 `<meta-data android:name="io.flutter.embedding.android.NormalTheme">` 在 Flutter UI 起来后切到 NormalTheme。但这条 meta-data 之前只挂在 `<activity>` 上，没有复制到各个 alias。alias 启动时 Flutter 找不到这条，窗口停留在 LaunchTheme，没有 `windowLayoutInDisplayCutoutMode=shortEdges` 也没有 `statusBarColor=transparent`，窗口本身就没把状态栏区域让 app 绘制，所以无论 immersive 怎么调，那块都是黑的。

**修复内容**

`android/app/src/main/AndroidManifest.xml`：给全部 13 个 `activity-alias` 补上：

```xml
android:theme="@style/LaunchTheme"
...
<meta-data
  android:name="io.flutter.embedding.android.NormalTheme"
  android:resource="@style/NormalTheme"/>
```

**注意**

后续如果新增 `activity-alias`，必须同样带上这两项，否则从该 alias 启动时黑条会复现。

### 33.2 阅读页状态栏动效（已修复）

**问题**

隐藏顶栏时，状态栏（系统时间/电量区域）是瞬间消失，而不是像原项目（PicaComic）那样慢慢淡出。

**根本原因**

V1.8 时把移动端 `_hideReaderSystemUi` 从 `SystemUiMode.immersive` 改成了 `SystemUiMode.immersiveSticky`：

- `immersive`：系统栏隐藏时走 Android 标准淡出动画（约 300ms），视觉上"慢慢隐去"。
- `immersiveSticky`：hard-switch，瞬间消失，无淡出动画。

**修复内容**

`lib/pages/reader/comic_reading_page.dart`：`_hideReaderSystemUi` 改回：

```dart
SystemChrome.setEnabledSystemUIMode(SystemUiMode.immersive);
```

**副作用说明**

`immersive` 模式下，用户如果从屏幕顶端下拉，状态栏会出现并停留几秒，需要等系统自动收回。`immersiveSticky` 会在失去交互后立即藏回。这是 V1.8 当时改 sticky 的动机，现在为了动效体验改回 immersive，接受这个副作用。

### 33.3 涉及文件

- `android/app/src/main/AndroidManifest.xml`
- `lib/pages/reader/comic_reading_page.dart`

### 33.4 校验

- `flutter analyze lib/pages/reader/comic_reading_page.dart lib/pages/reader/touch_control.dart`：0 issues

## 34. V1.9.9 外观图标切换、启动页重做、动态取色 ANR 修复

本节为 2026-05-29 一轮收口，详细记录见 `临时交接文档-2026-05-29-外观图标切换与Android调试兼容.md`。

### 34.1 外观设置页应用图标切换 + 选择控件样式统一

- 新增 `lib/tools/app_icon_channel.dart`：封装 `lingxue.picakeep/app_icon` 通道（`list / current / set`），非 Android 平台 no-op，通道异常静默吞掉。
- `lib/pages/settings/settings_page.dart`：外观设置在"主题选择"下方、"深色模式"上方插入 `if (App.isAndroid) _AppIconSetting()`；初始化时并发读 `list()` 与 `current()`，选中后调 `set(id)` 立即刷新；内置 13 色圆点映射。
- `lib/components/select.dart`：`Select` 组件新增 `leadingBuilder`（前置颜色圆点）与 `centerTextWhenPlain`（纯文字居中）。"主题选择 / 应用图标 / 深色模式"三项统一复用 `Select`，外框、左对齐、横向 padding 收口一致；深色模式 `controlWidth=108`。

### 34.2 Android 启动页（SplashScreen）全面重做

- **深色模式跟随应用内设置**：Android 12+ 启动画面由 `system_server` 在进程启动前绘制，`attachBaseContext` 改不动它。改用 `UiModeManager.setApplicationNightMode()`（API 31+）：`onCreate` 里 `super.onCreate` 之后调 `applyApplicationNightMode(readDarkModeSetting(this))` 同步 per-app 夜间模式；删除原 `attachBaseContext` 的 `createConfigurationContext + applyOverrideConfiguration`（会在 Activity 不重建时残留锁定，导致切回"跟随系统"不生效）。新增 `lingxue.picakeep/night_mode` 通道（`setMode`）+ Dart 侧 `lib/tools/night_mode_channel.dart`，深色模式 `onChanged` 里同步。时序：改完设置需退出再冷启动一次才生效（系统级限制）。API < 31 优雅降级。
- **双桌面图标修复**：删除 `MainActivity` 的 `MAIN/LAUNCHER` intent-filter（它是图标切换共用的 `targetActivity`，不能自己也是 launcher entry），入口全交给 `<activity-alias>`。**注意**：曾一度在 `debug/AndroidManifest.xml` 加 `tools:node="merge"` 的 debug-only launcher overlay 以兼容 `flutter run`，但实测该 overlay 会让 debug 包出现双图标（MainActivity + LauncherBlue 同时成为 launcher entry），且 Flutter 3.41.6 的 `flutter run` 本身就能识别 `<activity-alias>` 作为启动入口，overlay 纯属多余。最终已删除该 overlay，debug 与 release 均为单图标，`flutter run` 正常。
- **全 vector 启动图标**：用 potrace 把 `app_icon_no_bg.png`（512×512）矢量化为 pica 字形路径，合成 `launch_icon_full.xml`（圆角卡片 + pica 字，圆角用 `Q` arc 直接画进 path data，不依赖容器尺寸不会变直角）。post-splash 阶段用满幅版；SplashScreen API 阶段用 `launch_splash_icon.xml`（`<group scale=0.6>` 缩进，避开系统对图标外圈 1/3 的裁剪）。4 个 `launch_background.xml`、`values/styles.xml`、`values-night/styles.xml` 同步改；`build.gradle.kts` 加 `androidx.core:core-splashscreen:1.0.1`；删除旧 `drawable-nodpi/launch_icon_blue.webp`。
- 14 个 `<activity-alias>` 的 `theme` 从 `LaunchTheme` 统一改为 `SplashTheme`，让所有桌面入口都走 SplashScreen API。

### 34.3 动态取色（dynamic 主题）换壁纸回前台 ANR 修复

- **现象**：dynamic 挡位（`settings[27]=='0'`）下换壁纸切回 App 必现卡死，弹"没有响应"。真机 logcat 抓到 `exp_actresumetimeout Duration:10015`（resume 超时 10 秒），进程长时间 `R` 满跑 + sticky GC + 内存飙高。
- **根因**：换壁纸回前台时 MIUI 连发一串 `onConfigurationChanged`，而 resume / brightness / config 三个触发点全用 `force: true` 绕过了 600ms 节流，每条都跑 `getCorePalette()` + 两次 `toColorScheme()`（HCT 量化，CPU 重）+ 整树 `MaterialApp` 重建，几十次挤在主 isolate 一两帧内。
- **修复（双层防护）**：
  - Dart 侧 `lib/main.dart`：新增 350ms 防抖 `_scheduleDynamicColorRefresh()`，三个风暴触发点改用防抖（风暴结束后只刷一次）；`dispose()` 取消定时器；启动首次加载仍走直接强制刷新。
  - 原生侧 `MainActivity.kt`：`onConfigurationChanged` 加 1 秒节流 + 夜间 flag 变化检测，从源头掐断风暴。
- **附带**：曾在 `android/app/src/debug/AndroidManifest.xml` 加 debug-only launcher overlay 兼容 `flutter run`，但发现它导致 debug 包双图标、且 Flutter 工具本就支持 alias 启动，已删除该 overlay（debug/release 均单图标）。

### 34.4 版本号

- `pubspec.yaml`、`lib/foundation/def.dart`、`lib/pages/settings/settings_page.dart` 显示版本、`windows/runner/Runner.rc` 统一更新到 `1.9.9`（此前版本号文件停在 1.8.23 / Runner.rc 停在 1.8.18，本轮一并对齐）。

### 34.5 涉及文件

新增：
- `lib/tools/app_icon_channel.dart`
- `lib/tools/night_mode_channel.dart`
- `android/app/src/release/AndroidManifest.xml`
- `android/app/src/main/res/drawable/launch_icon_full.xml`、`drawable-night/launch_icon_full.xml`
- `android/app/src/main/res/drawable/launch_splash_icon.xml`、`drawable-night/launch_splash_icon.xml`
- `tools/trace_pica_logo.py`

修改：
- `lib/main.dart`、`lib/components/select.dart`、`lib/pages/settings/settings_page.dart`
- `android/app/build.gradle.kts`、`android/app/src/main/AndroidManifest.xml`、`android/app/src/debug/AndroidManifest.xml`
- `android/app/src/main/kotlin/lingxue/picakeep/MainActivity.kt`
- `android/app/src/main/res/drawable*/launch_background.xml`（4 个变体）
- `android/app/src/main/res/values/styles.xml`、`values-night/styles.xml`
- `android/gradle.properties`（`kotlin.incremental=false`）
- 版本号 4 处

### 34.6 校验

- `flutter analyze lib/main.dart`：No issues found
- `gradlew :app:processDebugResources :app:compileDebugKotlin :app:installDebug`：BUILD SUCCESSFUL
- 真机（f294cd23）：启动图标清晰圆角、深色模式跟随应用内设置（冷启动两次生效）、桌面单图标、连续 10 次 config 切换无 ANR、`dynamicColors` 刷新被正确合并

## 35. 服务端模式（手机当服务端）封面/阅读/重载/幽灵条目系列修复

> 本节记录"手机当服务端、PC 当客户端浏览远程·已下载"链路上的一连串问题修复（内部代号 v1–v7）。背景：服务端浅扫已改为"读 download.db 行 + 不逐图读字节"的轻量模式，深扫只在打开详情/阅读时按需触发。本节所有改动**仅影响服务端响应链路与远程客户端展示**，桌面服务端模式与 Windows 客户端协议保持兼容。

### 35.1 现象与根因（按问题归类）

- **A. 封面大面积破图 / 无法阅读**：`/cover`、详情、`/images` 全走 `_ensureDeepItem → deepScanItem`，而深扫早期 `includeImageSizes:true` 会对**每一页 `_readImageSize`（整页字节经 Shizuku IPC 读出再解析图头）**——解析一个封面 = 把整本所有页字节读一遍。叠加并发闸（安卓仅 2 槽），客户端一次请求 ~40 个封面全部排队，多数在图片超时前拿不到 → 破图、阅读卡死。
- **B. 客户端列表反复转圈重载**：`_computeLibrarySignature` 早期把 `snapshot.generatedAt`（秒级）算进签名 → 每次重扫即使内容没变签名也变 → 发 `library-changed` → 客户端失效重拉。
- **C. 服务端停在"已下载/图集"页，客户端一操作就重载**：`ServerRuntimeState.beginRequest/endRequest` 在**每个 HTTP 请求**进出都 `_notifyChanged → onChanged → App.notifyServiceRuntimeChanged()`，而下载页/图集页把 `serviceRuntimeVersion` 当成"该重载列表"。客户端滚动/翻页产生的请求洪流让宿主页面持续 `reload()`。
- **D. 空 managed 根冒出幽灵条目 "download"**：`_scanManagedRootItems` 在"无 db 记录且目录为空"时走 fallback `_buildManagedFallbackShallowItem(rootPath)`，而该构造器用占位 episode 恒返回非空 item，从不校验目录是否真有图片 → 空根产出一个 0 图、破图、点阅读报"未知错误"的幽灵条目（v2 浅扫重写丢掉了旧深扫 `if(episodes.isEmpty) return null` 的护栏）。
- **E. 页数被硬塞进"作者"字段**：`RemoteLibraryComicItem.subTitle` 在无真实作者时合成 `"X 张图片"/"N 个章节"`，导致卡片作者行、详情页"作者"信息栏都显示成"36 张图片"。
- **F. 详情/底栏大小变"未知"**：打开详情/底栏会 `fetchItemDetail` 触发深扫，深扫改为 `includeTotalBytes:false` 后返回 `totalBytes:0`，覆盖了浅扫从 db `comicSize` 拿到的真实大小 → `comicSize` 变 null → 显示"未知"。

### 35.2 修复内容

- **A 封面/阅读解耦深扫**：新增 `LocalResourceScanner.resolveCoverPathOnly(shallow, rootPath)`——只定位封面文件（先用 db 行的 `coverPath`，`fileExists` 即返回；否则对该本目录列一次找 `cover.*`，再否则取首张可见图），**0~2 次 ls 级 IPC，绝不递归整本、不读整页字节**。`server_app.dart` 封面路由改走它 + 独立的 `_coverPathCache`，不再 `_ensureDeepItem`。深扫 `deepScanItem` 改 `includeTotalBytes:false`，只递归列图片路径供阅读；单页字节由 `/images/{ep}/{page}` 按需经 `_fileResponse` 只读被请求那一页。
- **B 签名去抖**：`_computeLibrarySignature` **删除 `generatedAt`**，签名只由 config 路径 + 各 root 的 `id/path/exists/itemCount/totalBytes` + `totalComicCount/totalBytes` 决定。内容不变 → 签名不变 → `_recomputeLibrarySignature` 的 `changed==false` → 不发 `library-changed`。
- **C 运行信号分流**：新增 `App.serviceStatsVersion` / `App.notifyServiceStatsChanged()`（`app.dart`）。`ServerRuntimeState` 拆成两个回调——`onChanged`（运行/资源变化：start/stop/挡位/库变）与 `onStatsChanged`（请求计数/连接/日志）；`beginRequest/endRequest`/请求日志走 stats。`LocalServerRuntime` 把 `onStatsChanged` 接到 `notifyServiceStatsChanged`（独立防抖）。**列表页（下载页/图集页/me 页）仍只听 `serviceRuntimeVersion`**，因此不再被请求计数洪流触发重载；服务信息页可加听 `serviceStatsVersion` 看实时请求数。
- **D 幽灵条目护栏**：`_buildManagedFallbackShallowItem` 加"目录直接含图片 OR 任一子目录直接含图片"的轻量校验，空目录/空根返回 `null`，不再产出根自身幽灵条目；有 db 记录的正常根 `records` 非空即 return，**完全不进 fallback**，零回归。
- **E 作者字段**：`RemoteLibraryComicItem.subTitle` 改为 `=> subtitle.trim()`——只返回真实作者/副标题，没有就空，**不再合成"X 张图片"/"N 个章节"**。卡片作者行、详情页标题下方与"作者"信息栏空了即收起。（root 条目 `RemoteLibraryRootItem.subTitle` 的"X 部漫画/X 个项目"是另一处合法显示，未改。）
- **F 大小保留**：`deepScanItem` 把 `shallow.totalBytes`（来自 db `comicSize`）作为 `fallbackTotalBytes` 传入 `_scanComicItem`；后者在 `includeTotalBytes:false` 时改用该 fallback，而不是把逐章全 0 的字节求和成 0。深扫只负责枚举图片路径，**不再清空大小**。

### 35.3 涉及文件

修改：
- `lib/server/local_resource_scanner.dart`：新增 `resolveCoverPathOnly`；`deepScanItem` 改 `includeTotalBytes:false` + 传 `fallbackTotalBytes`；`_scanComicItem` 加 `fallbackTotalBytes` 参数并据此取 totalBytes；`_buildManagedFallbackShallowItem` 加含图护栏
- `lib/server/server_app.dart`：封面路由走 `resolveCoverPathOnly` + `_coverPathCache`，不再深扫；`_computeLibrarySignature` 删除 `generatedAt`；`_setSnapshot`/缓存清理与签名联动
- `lib/server/server_runtime_state.dart`：拆 `onChanged` / `onStatsChanged`，请求计数与日志走 stats
- `lib/foundation/app.dart`：新增 `serviceStatsVersion` / `notifyServiceStatsChanged`
- `lib/server/local_server_runtime.dart`：接 `onStatsChanged → notifyServiceStatsChanged`（独立防抖）
- `lib/foundation/remote_library_data_source.dart`：`RemoteLibraryComicItem.subTitle` 不再合成页数

当前关键常量：`_maxConcurrentDeepScans = Android?2:6`（`server_app.dart:47`）；客户端 `_localLoadTimeout=8s` / `_remoteLoadTimeout=10s`（`download_page.dart:356-357`，未改）。

### 35.4 校验

- `flutter analyze lib/server/local_resource_scanner.dart lib/server/server_app.dart lib/foundation/remote_library_data_source.dart`：No issues found
- `gradlew :app:compileDebugKotlin`：BUILD SUCCESSFUL（仅既有 deprecation 警告）
- 真机（手机当服务端 + PC 客户端）：列表秒出且大小正确；滚动列表封面稳定逐张出现、不再大面积破图；打开任意一本可正常进阅读器逐页加载；详情/底栏大小正常显示（不再"未知"）；服务端停在"已下载/图集"页时客户端操作不再触发反复转圈重载；空 managed 根不再出现 "download" 幽灵条目；卡片/详情"作者"无真实作者时留空、不再显示"X 张图片"

### 35.5 待办（已规划、未实现）

「收藏」页与「图片收藏」页的两档（本地/远程）来源切换——服务端目前**没有任何收藏/图片收藏接口**，需新增服务端接口 + 客户端拉取/写入方法 + 模型类，再接 UI 两档切换（仿已下载页 `SegmentedButton`）。范围已定：远程档为浏览+编辑同步、图片收藏点击进阅读器跳页。详见独立实现计划文件。

## 36. 收藏 / 图片收藏远程档落地 + 远程图片连接池根因修复

> 本节是第 35.5 待办的实现 + 一连串实测排障的最终结论。「收藏」「图片收藏」两页的"远程"档已落地可用；过程中暴露并修复了远程图片加载会**永久毒化连接池**（仅热重启可恢复）的根因。详细过程见临时文档 `交接文档-临时-远程收藏与图片收藏.md`，本节为收口结论。**用户实测："目前来说没有什么问题"。**

### 36.1 远程档功能落地

- 服务端新增收藏 / 图片收藏接口（`server_app.dart` 的 `_handleFavoritesRequest` / `_handleImageFavoritesRequest`），客户端新增模型 `RemoteFavoriteFolder` / `RemoteFavoriteItem` / `RemoteImageFavorite` 与拉取/写入方法（`remote_library_data_source.dart`），两页接 `SegmentedButton` 本地/远程两档。
- 远程档点击进阅读器统一走 `App.pushInner(...)`（**不要用 `App.globalTo`**，会让返回栈跳到「我」页）；`ScaffoldMessenger.of` 一律用 tile/页面自己的 `BuildContext`（**不要用 `App.globalContext`**，根 context 无 Scaffold 后代会断言挂）。
- 图片收藏点击候选 id 用 `image.id` + `downloadId` + `title` + 前缀变体 fallback（`nhentai`/`hitomi`/`jm`/`Ht` 等），因为收藏的 `image.id` ≠ 服务端资源 `remoteId`。
- 两页 `_open`/`_onTap` 都加了 `try/catch + SnackBar`：定位不到弹"该漫画在服务端不可定位"，超时/连不上弹对应提示，**绝不让异常冒泡成 unhandled exception 把当前路由掀掉**（曾导致"点了没反应、弹回我页"）。
- 服务端路径段直接用 `segments[i]`（**不要再 `Uri.decodeComponent`**，`pathSegments` 已自动解码，二次解码会让中文夹炸 500）；favorites handler **不要**每请求 `await LocalFavoritesManager().init()`（会重开 sqlite、阻塞 isolate）。

### 36.2 真·根因：远程图片连接池被永久毒化（最关键）

症状：**滚动经过"服务端实际不存在内容"的漫画/封面后，全部远程档（已下载/图集/收藏/图片收藏）都无法载图，连正常漫画的阅读图也加载不出，离开页面、跨标签都恢复不了，唯有热重启 / 重开 App 才好。**

- **决定性证据**：热重启重建整个 Dart isolate → `RemoteLibraryClient` 单例 + 其 `HttpClient` 连接池清零才恢复 ⇒ 坏的是**单例持有的连接池**，不是服务端、不是 widget。logcat 一片 `getUrl` 的 `TimeoutException 0:00:05`、**无任何 404**。
- **两个泄漏源（均已修）**：
  1. **孤儿连接**：`getUrl()/openUrl().timeout(5s)` 超时只丢弃前端 future，底层开连接的请求仍在跑，稍后成功返回的 `HttpClientRequest` 无人 abort → 永久钉住一条连接，累积到 `maxConnectionsPerHost` 全局假死。
  2. **404 不 drain**：非 2xx（缺失封面）路径原来直接 `request.abort()`，但 `close()` 拿到 response 后 socket 归 response 所有，abort 不可靠 → 每个缺失封面漏一条连接。

### 36.3 连接池修复内容

- **孤儿回收**：新增 `RemoteLibraryClient._openWithTimeout(connecting, timeout)`——超时时给原 `connecting` future 挂 `then`，等它最终返回 request 就 `abort()` 把连接还回池子。`loadImageWithProgress` 的 `getUrl`、`_sendRequest` 的 `openUrl` 都改走它。**新增任何"开连接 + 超时"路径都必须用它，不要再裸 `.timeout()`。**
- **404 改 drain**：非 2xx 路径改为 `await response.drain<void>().timeout(5s)` 读空响应体让连接干净归还；drain 自身超时才回退 `abort()`。
- **并发闸**：`_ImageConcurrencyLimiter`（FIFO 信号量，许可转交式 release，无 double-count）。`loadImageWithProgress` 拿连接前先拿令牌；两个独立预算——**阅读闸** `settings[114]`（reader 大图，默认 6）/ **浏览闸** `settings[115]`（封面缩略图，默认 8）。封面下载传 `isCover:true` 走浏览闸，reader 走阅读闸。令牌释放：早退路径就地 `releasePermit()`，正常路径交给 `_guardedImageBody` 的 `finally`。
- **池扩容**：`_httpClient.maxConnectionsPerHost = 24`（≥ 两闸上限之和 12+12），保证正常并发不在 `getUrl` 排队。**改并发闸上限必须同步抬高这里。**
- **响应体保护**：`_guardedImageBody` 给 body 流套 15s idle timeout，cancel/error/未读完时 `abort()`，正常读完不动（保留 keep-alive）。封面磁盘缓存有失败负缓存（404 冷却 10 分钟、瞬时错误 20 秒）+ `RemoteCoverUnavailableException` 静默占位；`_trimToLimit` 30s 节流且不重入。
- 设置位常量在 `lib/foundation/local_library_settings.dart`：`remoteReaderImageConcurrencySettingIndex=114` / `remoteBrowseImageConcurrencySettingIndex=115`，含 `normalizeRemote*ImageConcurrency`（夹 1~12）；`base.dart` 默认追加 `'6'/'8'`。设置入口在设置-阅读 / 设置-浏览，范围 1~12，实时生效。

### 36.4 顺带修复（与远程图片无关）

- **动态取色每 6s 重建 MaterialApp → 偶发 ANR**：MIUI 每 ~6s 发 `onConfigurationChanged`，原生节流仅 1000ms（6s>1s 每次都穿过）→ 转发 `dynamic_theme` `changed` → Dart `getCorePalette()`+双 `toColorScheme()`+`setState` 全量重建。修法：`MainActivity.onConfigurationChanged` 改为**仅 uiMode 夜间标志真正翻转才转发**（壁纸调色板不走此回调，首次取色由 initState 独立触发），删掉失效的 `DYNAMIC_THEME_NOTIFY_MIN_INTERVAL_MS` / `lastDynamicThemeNotifyAt`。验证：正常使用控制台不再每 6s 刷 `dynamicColors`。
- **设置滑块拖动不跟手**：`buildExploreSettings`（无状态函数）/ `ReadingSettings`（StatelessWidget）里内联 `Slider` 的 `onChanged` 只写 settings 不 `setState`。三个滑块各封装成小 StatefulWidget：漫画块大小 `settings[44]`→`_ComicTileSizeSlider`、图片收藏大小 `settings[74]`→`_ImageFavoriteSizeSlider`、点按翻页识别范围 `settings[40]`→`_TapTurnRangeSlider`。

### 36.5 涉及文件

修改：
- `lib/foundation/remote_library_data_source.dart`：远程收藏/图片收藏模型 + 接口方法；`_openWithTimeout` 孤儿回收；`_ImageConcurrencyLimiter` 并发闸；404 drain；`maxConnectionsPerHost=24`；`_guardedImageBody`；封面负缓存 + `_trimToLimit` 节流
- `lib/foundation/local_library_settings.dart`：`remoteReaderImageConcurrencySettingIndex=114` / `remoteBrowseImageConcurrencySettingIndex=115` + normalize
- `lib/base.dart`：settings 默认追加 `'6'/'8'`（114/115）
- `lib/pages/image_favorites.dart`、`lib/pages/favorites/main_favorites_page.dart`：本地/远程两档 UI、`_open`/`_onTap` try/catch + SnackBar、候选 id fallback、`App.pushInner`
- `lib/pages/settings/explore_settings.dart`、`lib/pages/settings/reading_settings.dart`：并发 `SelectSetting` 入口 + 三个跟手滑块
- `lib/server/server_app.dart`：收藏/图片收藏 handler，path 段不再二次解码，favorites handler 不再每请求 init
- `assets/translation.json`：两条并发设置的繁中/英文
- `android/app/src/main/kotlin/lingxue/picakeep/MainActivity.kt`：动态取色仅夜间标志变化才转发

### 36.6 校验

- `flutter analyze`：全项目 0 issues
- `gradlew :app:compileDebugKotlin`：BUILD SUCCESSFUL（仅既有 deprecation 警告）
- 真机（客户端 + 另一台手机当服务端）：收藏/图片收藏远程档可列出、可进文件夹、可点进阅读；滚动经过大量缺失内容后，正常漫画仍能载图（**不再需要热重启**）；reader 连翻多页不再刷 5s TimeoutException；动态取色不再每 6s 重建；三个设置滑块拖动跟手。

### 36.7 待办 / 注意

- **图片收藏网格列数**：用 `SliverGridDelegateWithComics(true, settings[74])`，brief 模式列数 = `ceil(屏宽 /(192×scale))`，scale 即"图片收藏大小"。用户反馈手机变两列，但 `layout.dart` 与 HEAD 一致、未被本轮改动，推测是 `settings[74]` 存值漂移；**尚未**新增"直接选列数"的设置（讨论过未实装，做时勿破坏电脑端拉宽自动加列）。
- **ANR**：修完 36.4 后用户已无法复现，但无稳定现场最终确认；若再现，**抓弹窗前后完整 logcat** 再定位，不要凭猜继续打补丁。
- 本轮改动已提交为 **`V1.9.12`**（12 文件，+2208/-143）。

### 36.8 硬规则：不要再走的弯路

- **不要**给 `_checkRemoteAvailability` 加缓存"掩盖"问题——它失败的真正原因 99% 是连接池被拖垮，不是探测本身慢。
- **不要**把 `Uri.decodeComponent(segments[i])` 加回去——`pathSegments` 已自动解码。
- **不要**在服务端 favorites handler 里 `await init()`——单例 startup 已 init 过，重复 init 重开 db、阻塞 isolate。
- **不要**用 `App.globalTo` 打开阅读器——永远用 `App.pushInner`，否则返回栈错乱跳「我」页。
- **不要**用 `App.globalContext` 调 `ScaffoldMessenger.of`——用页面/tile 自己的 BuildContext。
- **不要**把图片收藏远程档点击候选缩成只有 `image.id`——要带 `title` + 前缀变体 fallback。
- **不要**在 timeout 路径里只 catch 不 abort——Dart `HttpClient` 的 socket 不会自动释放。
- **不要**只给建连/收 header 包 `.timeout()` 就以为"请求有超时保护"了——**响应体（body）流必须单独保护**：套 idle timeout，且在被取消/出错（未读完）时 `abort()`。裸 `await for(response)` 是这套 codebase 最隐蔽的池泄漏来源。
- **不要**新增 `HttpClient` 实例——应通过 `_httpClient`/`_controlClient` 这两个，否则又开始单池竞争。
- **不要**在"每张封面下载后"无脑跑全盘扫描类清理（`_trimToLimit`）——网格批量加载会把它放大成几十次扫盘，必须节流 + 不重入。
- **不要**对 `getUrl()` / `openUrl()` 直接 `.timeout()` 就完事——**这是孤儿连接的来源**：超时只丢弃前端 future，底层开连接的请求还在跑，稍后成功返回的 `HttpClientRequest` 无人 abort，永久钉住一条连接；累积到 `maxConnectionsPerHost` 就全局假死，**只有热重启/重开 App 能恢复**。必须用 `_openWithTimeout`：超时后给原 future 挂 `then`，等它最终返回 request 时 `abort()` 回收。判断铁证——logcat 全是 `getUrl` 的 5s TimeoutException、没有 404、且热重启后立刻恢复。
- **不要**让 `maxConnectionsPerHost` 小于"所有并发闸上限之和"——否则正常并发就会 oversubscribe → `getUrl` 排队 → 超时 →（若没孤儿回收）雪球。当前 `_httpClient=24` ≥ 阅读闸 12 + 浏览闸 12；改并发闸上限时记得同步检查这里。
- **不要**忘了释放并发令牌——`loadImageWithProgress` 拿令牌后：早退路径（404/超时/socket/其它异常）就地 `releasePermit()`；正常路径把释放责任交给 `_guardedImageBody` 的 `finally`。漏一条路径就会泄漏槽位，最终把闸门耗尽变成假死。

### 36.9 当前可能仍存在的问题

1. **池泄漏是否真的根治了？** 现在两个 client 的所有 timeout/socket 路径都走了 `_openWithTimeout` + drain，理论上池子不该再被废 socket 占。但 Dart `HttpClient` 在某些边缘场景（如 response 已开始流式但 stream 卡住）的释放行为，最好用反复点击 + reader 看 5+ 本后回切场景跑一遍。
2. **首次点击 `findItemByCandidates(fetchDetail: true)` 仍可能慢**。`fetchItems()` 拉全库 + `fetchItemDetail()` 单项详情，两次串行 `_sendRequest`。如果服务端库大，第一次点击可能就 5+8 秒。这种"业务慢"不是 bug，但用户体感差。当前已对第一个匹配项才拉详情、复用 `_detailCache`；真正治本可能要让服务端在 `image-favorites` payload 里直接回传 `itemId`，省掉客户端模糊匹配。
3. **`fetchFavoritesInFolder` 串行循环**。`_loadRemoteFolders` 里逐个文件夹 `await client.fetchFavoritesInFolder(folder.name)`，18 个文件夹就 18 次串行。可改 `Future.wait` 并行（控制并发到 4-6 别打满 `_controlClient`）。当前没改。
4. **reader 退出时的请求清理**。reader dispose 时只 cancel UI 流，**没主动 abort 还在网络上的图片请求**。`abort()` 已能让超时的请求释放 socket，但理论上 reader 主动 abort 所有未完成请求会更干净。可作为下一步增强。

### 36.10 实测脚本

1. 客户端冷启动 → 收藏远程档 → 应直接列出所有文件夹（含中文名）。
2. 图片收藏远程档 → 应显示缩略图，无图项静默破图占位，控制台**不再刷** 404/Timeout。
3. 点击图片收藏远程项 → 进 reader 阅读 → 系统返回一次 → 回到图片收藏页（不是「我」页）。
4. **重复步骤 3 五次**（不同漫画），每次都返回。然后切到收藏页远程档 → 应仍然能列出文件夹、能进文件夹、能点击阅读。
5. 找一项**服务端没有该资源**的图片收藏，点击 → 应弹"该漫画在服务端不可定位" SnackBar，**不能崩**。
6. 故意拔网线 → 点击图片收藏远程项 → 应弹"远程服务响应超时" SnackBar（来自 `_onTap` 的 try/catch），**不能 unhandled exception**。
7. 控制台 grep `[FavRemote]` 应**没有**输出（诊断 print 已清理）。

### 36.11 给下一位 AI 的建议

- 不要被症状牵着走"一个个补丁"。这套问题里 90% 是**连接池/导航栈/编码**这种基础设施级别的根因，每发现一个新症状都先问一句"是不是池子又满了？是不是用错了 navigator？是不是又 decode 了一次？"
- 如果用户报"又有问题"，**先看完整 logcat**，特别是 `flutter` tag 的 unhandled exception 栈，它会精确定位 file:line。
- **保留 `[FavRemote]` 这种诊断 print 模式**作为下次必要时的工具，但**用完要清理**，不要留在生产代码里。
- 任何新增 HTTP 请求路径（不管是控制面还是图片）**timeout/socket catch 必须配 request.abort() 或走 _openWithTimeout**，这是这个 codebase 的硬规定。
- 用户在客户端测试，服务端在另一台手机；服务端日志要单独看（不是客户端 logcat），那里也会有 `ERROR - GET /api/...` 这种关键线索。

## 37. 2026-06-03 网页阅读器历史保存 / 滚动恢复 / 历史页对齐已下载

> 本节为网页后台（`assets/web_console/`）的一轮收口，**只改 `js/app.js` 与 `css/app.css`，未动 Flutter 客户端与服务端**。四个问题串在一起：阅读历史写不进去、写进去又读不到正确页、历史独立页与「已下载」不一致、详细视图标题溢出。详细根因推理见 `交接文档-经验思路版.md` §13 新增子节。

### 37.1 ESC 退出不保存阅读进度（只有返回键保存）

- 现象：阅读时按 ESC 退出，阅读到的章节/页码不保存；只有点顶栏返回键才保存。
- 根因：ESC（`onGlobalKeydown`）与返回按钮**同走一个 `back()`**，并非两条分支。阅读翻页/滚动用 `scheduleReaderHistoryWrite()` 挂 **1500ms 防抖**再落盘；退出时 `cleanupReader()` 旧逻辑**直接 `clearTimeout` 清掉待写定时器、不补写**。返回键因唤栏+移鼠标+点击通常已超 1500ms、防抖早落盘，ESC 可在窗口内瞬退被取消。
- 修法：`cleanupReader()` 改调 `flushReaderHistory()`——清定时器后按当前实时页（滚动模式用 `currentScrollPage()` 回写 `pageIndex`）调一次 `writeReaderHistoryNow()`；`lastHistoryKey` 去重保证未移动是 no-op。ESC / 返回键 / 跳转统一经 `cleanupReader`，一处补写全修好。

### 37.2 从历史打开总停在第 1 页（仅滚动连续模式）

- 现象：从历史卡/历史页点进漫画，不从记录页打开而是停在第 1 页。
- 根因：页码全程正确（`openHistory` 传 `pageIndex: record.page` → `renderReader` 存入 → `renderReaderContent` clamp），分页模式直接以该 index 渲染所以正常。滚动连续模式旧实现只有**一次** `requestAnimationFrame(() => scrollToPage(pageIndex))`，`scrollToPage` 用 `scrollIntoView`：目标页之上图片懒加载、跳转那刻高度未稳定→落到顶部；随后 `pageObserver` 按可视页**把 `pageIndex` 覆写**回 0。
- 修法：新增 `restoreScrollPosition(target)`——置 `state.reader.restoring=true`，多帧 rAF 重试 `scrollToPage(target)` 直到 `currentScrollPage()===target` 稳定或超时兜底，`restoreRaf` 在 `cleanupReader` 取消；`pageObserver` 回调加 `if (state.reader.restoring) return;` 守卫，恢复期间不被覆写。`target<=0` 不重试。

### 37.3 历史卡第二行「0」+ 历史页对齐已下载

- 现象：历史卡第二行（作者位）显示「0」；历史页搜索不可用、无卡片/详细切换、卡片样式与已下载不一致。
- 根因：服务端历史记录（`server_app.dart::upsertHistory` / `web_user_store.dart::toJson`）**只存 `target/title/cover/ep/page/maxPage/readEpisode`，没有 author/subtitle/tags**。`readEpisode` 是 int 集合回传 `[0]`，旧 `normalizeHistoryPayload` 用 `String(firstValue(['readEpisode',…]))` 变成 `"0"`，卡片 `item.readEpisode || item.subtitle` 渲染成"0"。
- 修法：① `normalizeHistoryPayload` 不再用 `readEpisode` 当文案；② 作者/副标题与标签改由 `renderHistoryPage` 用 `settle()` 并行拉 `history.list` + `fetchItemsForSource(当前源)`，建 `Map(id→{subtitle,tags})` 补全（资源库缺该条目就留空隐藏）；③ 启用历史页搜索——`updateToolbar` 把 `historyPage` 加进 `searchInput.disabled` 与 `viewSwitchEl.hidden` 白名单，**复用同一 `state.grid.query` / `state.grid.viewMode`（与已下载共用记忆）**，search input/clear/view-switch handler 各加 `historyPage` 分支，过滤用现成 `filterItems`；④ 卡片改用 `.app-card`(`historyPosterCardHtml`)/`.app-detailed-card`(`historyDetailedCardHtml`) 结构，保留右上 `.app-history-remove`。

### 37.4 详细视图超长标题溢出覆盖下一行

- 现象：详细视图里超长作品名换行到 3+ 行，跳出卡片盖住下面的卡片。
- 根因：`.app-detailed-card` 用 `align-items: stretch`，`.app-detailed-title` 被拉伸，`-webkit-line-clamp` 的 `-webkit-box` 高度不收敛、clamp 失效。
- 修法：`.app-detailed-title` 补全 `display:-webkit-box; -webkit-box-orient:vertical; overflow:hidden; -webkit-line-clamp:3; white-space:normal`，并加 `.app-history-entry .app-detailed-title { align-self:start }` 让 3 行 clamp 生效。标签经库 join 后显示：详细前 3 个、海报前 2 个，复用 grid 既有 `.app-detailed-tags` 样式。

### 37.5 涉及文件

- `assets/web_console/js/app.js` — `flushReaderHistory` / `restoreScrollPosition` / `pageObserver` 守卫 / `normalizeHistoryPayload` / `renderHistoryPage` 库 join / `updateToolbar` 白名单 / `historyPosterCardHtml` / `historyDetailedCardHtml`
- `assets/web_console/css/app.css` — `.app-detailed-title` 3 行 clamp、`.app-history-entry .app-detailed-title` align、历史卡复用 `.app-card`/`.app-detailed-card` 与 `.app-history-tags`

### 37.6 校验

- 仅前端改动，未跑 Flutter 编译。验证方式：服务端模式启动后浏览器开 `http://127.0.0.1:9527/`、登录切「本地」源，用 DevTools 实测：
  - 阅读翻到中后段后**立刻按 ESC** 退出 → 回历史进度正确（不再丢页）；点返回键同样正确。
  - 滚动连续模式从历史打开 → 定位到记录页；分页模式同样正确。
  - 历史页：第二行不再显示「0」，资源库存在的条目显示作者/标签、缺失隐藏；卡片/详细双档切换与「已下载」共用记忆；顶栏搜索可按标题/作者过滤、无匹配有空态；详细视图超长标题 3 行截断不溢出。
  - DevTools 无新增 JS error。

## 38. 2026-06-04 网页应用面板收藏夹入口 + 服务端收藏封面 fallback + 详细档卡片对齐客户端

> 本节为网页后台（`assets/web_console/`）+ 服务端（`lib/server/server_app.dart`）一轮收口。四件事串在一起：网页应用面板新增「收藏夹」入口、修复远程收藏漫画封面破图、详细档卡片向客户端对齐（标签全显示 + 跟随主题色 + 标题严格 2 行）、dashboard 多余滚动。经验细节与「不要再走的路」见 `交接文档-经验思路版.md` §13 新增子节。

### 38.1 网页应用面板新增「收藏夹」入口

- 现状：admin.js 管理面板早有收藏夹 tab，但 app.js 应用面板没有入口。本轮在 dashboard「资源库」卡片下方新增「收藏夹」入口卡片（`data-open-favorites` → `navigate('favorites', {}, true)`），工具区也补一个快捷 chip。
- 视图链路：`renderFavorites(params)` 两层——无 `params.folder` 时列文件夹（`GET /api/library/favorites`），有 folder 时列文件夹内漫画（`GET /api/library/favorites/{folder}`）；文件夹卡片点击进内容，内容卡片点击 `openItemPanel({id, origin:'local'})` 打开详情并可进阅读器。
- 文件夹卡片 `favoriteFolderCardHtml`：紧凑横向卡（文件夹 svg 图标 + 名称 + 副标题「本地」+ 右侧数量徽章）；容器 `.app-favorites-folder-grid` 用 `repeat(auto-fill, minmax(300px, 1fr))`。
- 文件夹内漫画复用通用 `gridCardHtml`/`gridDetailedCardHtml`，与「已下载」同款卡片。

### 38.2 收藏项封面破图 + 卡片不可点（核心 bug）

- **根因**：服务端 `_buildFavoriteItemPayload` 原本只回 `name/author/type/tags/target/time/coverUrl`，**没有 `itemId`**；而客户端/网页用 `__matched: !!itemId` 判断是否匹配资源库，于是 `__matched` **永远 false** → 封面被强制清空（`coverItem = {coverUrl:''}`）、卡片标为 `aria-disabled` 不可点。问题**不在** cover 路由本身（`/api/library/favorites/{folder}/{target}/cover` 一直可用），而在 payload 缺字段。
- **修法**：服务端 GET 列文件夹内容（`segments.length == 4`）时先取 `_currentSnapshot()`，对每个收藏项跑 `_resolveFavoriteResourceItem(snapshot, item)`；匹配到就把 `itemId/id/displayId/sourceDisplayName/imageCount/totalBytes/updatedAt` 一并写进 payload，`__matched` 才正确为 true，封面与点击恢复。

### 38.3 收藏封面 fallback（coverPath 失效但资源库有内容）

- cover 路由（`segments.length == 6 && segments[5]=='cover'`）改为三级兜底：
  1. 本地 `item.coverPath` → `_fileResponse`，**要判断返回是否 404**（不能只看路径非空），404 才继续回退；
  2. 命中 `_favoriteCoverFallbackCache`（key = `folder|type|target`）且文件在就用；
  3. 都没有则 `_currentSnapshot()` + `_resolveFavoriteResourceItem` 按 candidates 匹配资源库项，取 `matched.coverPath`，为空再 `_scanner.resolveCoverPathOnly(matched, rootPath: _rootPathForRootId(matched.rootId))`，成功后写入 fallback 缓存。
- `_recomputeLibrarySignature` 里 signature 变化时**连 `_favoriteCoverFallbackCache` 一起 clear**（与 `_deepItemCache`/`_deepItemInFlight` 一起），避免资源移动/删除后命中 stale 封面路径。
- **candidates 复用客户端**：`_buildFavoriteServerCandidates(item)` 直接调 `item.candidateDownloadIds()`（FavoriteItem 自带，含 ehentai/jm/hitomi/Ht/nhentai 前缀变体）再补 `target`；`_findResourceItemByCandidates` 把每个资源项的 `id/title/displayId/sourceTitle/sourceDisplayName/subtitle/path` 收成 pool 做包含匹配。**不要**在服务端另写一套候选规则。
- **cover/删除带 `?type=`**：同一 target 在不同收藏类型下可能并存，`coverUrl` 末尾拼 `?type=${item.type.key}`，`_findFavoriteItemByTarget` 增加可选 `type` 参数精确定位，DELETE 单项同样带 type。
- **`_rootPathForRootId`**：`current_download`/`original_download` 取 config 对应 root，`custom_{n}` 按下标取 `config.customLibraryRoots[n]`，越界返回 null。

### 38.4 详细档卡片对齐客户端 + 标签全显示跟随主题色

- **标题严格 2 行**：`.app-detailed-title` 原本同时有 `text-overflow:ellipsis` 与 `-webkit-line-clamp:2 + white-space:normal`，冲突导致第三行漏出一点。修法：去掉 `text-overflow`，保 `display:-webkit-box; -webkit-box-orient:vertical; -webkit-line-clamp:2; overflow:hidden`，补 `max-height: calc(1.35em * var(--app-detailed-title-lines))`（默认 2）兜底。历史页 `.app-history-entry .app-detailed-title` 仍 3 行覆盖（§37.4），不破坏。
- **标签全显示**：`gridDetailedCardHtml` 去掉 `tags.slice(0,3)`，全量渲染（`.app-detailed-tags` 本就 `flex-wrap:wrap` 自动换行）。只放开**详细档**，海报档/历史档标签数量不动。
- **标签跟随主题色（非写死）**：`.app-detailed-tags span` 背景改 `rgba(var(--accent-rgb),0.12)` + 文字 `var(--accent-2)` + 边框 `rgba(var(--accent-rgb),0.28)`，圆角 8px。主题色由 `:root[data-accent="..."]`（common.css，12 色：蓝/红/粉/紫/靛/青/蓝绿/绿/黄绿/黄/琥珀/橙）切换，用户换主题色时标签随之变色（等效客户端 `secondaryContainer` 主题感）。

### 38.5 dashboard 多余滚动

- 主页内容能完整显示时仍出现纵向滚动条：`.app-shell:has(...dashboard)` 高度链需把 `#console-root` 的 `padding:24px`（上下共 48px）算进去，避免容器比可用区高。

### 38.6 涉及文件

- `lib/server/server_app.dart`：`_buildFavoriteItemPayload`（补 matched 字段 + `?type=` cover URL）、cover 路由三级 fallback、`_favoriteCoverFallbackCache`、`_resolveFavoriteResourceItem`/`_buildFavoriteServerCandidates`/`_findResourceItemByCandidates`/`_resourceItemCandidatePool`/`_rootPathForRootId`、`_findFavoriteItemByTarget` 加 type、signature 清缓存
- `assets/web_console/js/app.js`：`renderFavorites`/`renderFavoritesContent`、`normalizeFavoriteFoldersPayload`/`normalizeFavoritesItemsPayload`、`favoriteFolderCardHtml`、`favoriteDetailAttrs`/`favoriteDownloadIdCandidates`、`gridItemMetaParts`、`gridDetailedCardHtml` 标签全显示、dashboard 收藏夹入口/事件
- `assets/web_console/css/app.css`：`.app-detailed-title` 2 行截断、`.app-detailed-tags span` 主题色、`.app-favorites-folder-grid`/`.app-favorites-folder-card`、dashboard 滚动
- `assets/web_console/js/admin.js`：管理面板收藏夹同步

### 38.7 校验

- 前端 + 服务端 Dart 改动。建议：`flutter analyze`（确认 server_app.dart 0 issue），浏览器 DevTools 实测——收藏夹入口可进、文件夹卡片加宽显示「本地」、漫画封面正常（含本地失效但资源库有内容的经 fallback 恢复）、卡片可点进阅读、详细档标签全显示且跟随主题色换色、超长标题严格 2 行、dashboard 内容能完整显示时无多余滚动、console 无新增 JS error。

---

## 39. V1.9.15 之后（本批未提交）：远程压缩包接入 + 阅读图片宽度修复 + 网页 UI 二次还原 + 资源热刷新

> ⚠️ 本批改动**尚未提交**（`git status` 17 改 + 新增 `archive_episode_builder.dart`）。落地后请跑 `flutter analyze` 并按规范提交。配套经验版 [§16](./交接文档-经验思路版.md#16-远程访问图集压缩包含加密包) / [§17](./交接文档-经验思路版.md#17) / [§18](./交接文档-经验思路版.md#18)。

### 39.1 远程访问图集压缩包（含加密）

- **设计**：服务端把每个 ZIP/CBZ 扫成一个普通 item/episode，页 URL 仍是 `/api/library/items/{id}/images/{ep}/{page}`，命中压缩包时返回 archive 内字节而非磁盘文件 → **客户端远程数据源消费 HTTP URL 的逻辑零改动**，仅加密解锁需增量。
- **共享重构**：新建 `lib/foundation/archive/archive_episode_builder.dart`，把 `local_library.dart` 私有的 `_buildArchiveEpisodes`/`_pickArchiveCoverEntry`（含自然排序）提为顶层 `buildArchiveEpisodes`/`pickArchiveCoverEntry`，客户端与服务端共用（排序结果须一致）。
- **服务端扫描**：`local_resource_scanner.dart` `_scanCustomRootItems` 末尾加 `_collectArchiveFiles` + `_scanCustomArchiveItem`：`ArchiveReadingService.getIndex`（只读中央目录、不需密码）→ `buildArchiveEpisodes` 分章 → `imagePaths` 直接存 `archive://?path=&entry=` URI。`ServerResourceItemSummary`/`Episode` 新增 `isArchive/archiveEncrypted/archivePasswordMatched/archiveFormat/archivePath`。
- **取图/封面**：`server_app.dart` 图片路由命中 `isArchiveUri` 走新 `_archiveBytesResponse`（`readEntryBytesByUri` 取字节 + Content-Type by entryPath；加密读失败 → 403 `archive locked`/404）；封面路由对 `item.isArchive` 同样。`_buildLibraryItemPayload` 透传 `itemKind/archiveEncrypted/archivePasswordMatched/archiveFormat`。
- **启动注册**：`start()` 早期补 `ArchiveRegistry.initDefaults()`（`archive_registry.dart` 的 `initDefaults` 改幂等 `if (_backends.isEmpty)`）——否则 headless 服务端 `backendForOrThrow` 抛 `unsupportedFormat`。
- **加密 unlock**：`POST /api/library/items/{id}/archive/unlock {password}` → `ArchiveReadingService.tryUnlock`（进程单例会话密码，成功后取图/封面自动用上）→ 清 `_deepItemCache`/`_coverPathCache` + `rescanResources()`；另有 `GET .../archive/status`。密码只走 body。
- **客户端解锁**：`RemoteLibraryComicItem.fromJson` 解析 archive 字段 + `needsArchivePassword`；`RemoteLibraryClient.unlockArchive`；`archive_password_dialog.dart` 抽 `onVerify` 回调（本地 `tryUnlock` / 远程 `unlockArchive`）；解锁入口补到 `local_library_page._handleItemTap`、`download_page`（列表 onTap + 菜单 + 详情 read）、`local_comic_detail_page`。
- **网页前端**：`app.js` `ensureArchiveUnlocked(item)` 进 reader/详情前判 `archiveEncrypted && !archivePasswordMatched` 弹密码框调 unlock（远程经 `consoleApi.remote.post`、本地 `consoleApi.api.post`）；`remote_proxy_handler.dart` 扩展支持 POST 以转发 unlock。
- **踩坑**：① `_ensureDeepItem` 对 custom_ 返回 shallow，故 episodes/imagePaths 必须浅扫阶段建全；② `_fileResponse` 只服务磁盘文件，archive 必须用 `_archiveBytesResponse`；③ 会话密码重启即失，端侧遇 403 需重新弹解锁。

### 39.2 阅读器「图片最大显示宽度」滑块修复（`image_view.dart`）

- 翻页模式 `buildType123`/`buildType56` 原没读 `settings[116]` → gallery 容器外层包 `Center + SizedBox(width:_clampReaderImageWidth(...))`。
- 连续模式 `buildType4`（`ScrollablePositionedList` sliver，item 横向 tight 约束吃掉内部 width）→ 在列表外层限宽居中。
- 「只全屏能调/只调一点」：原外层 `SizedBox(width: MediaQuery.size.width)`（全屏宽非阅读区宽）+ item 内二次 clamp → 改用 `LayoutBuilder` 真实约束、只限一次。
- 拖动闪烁：`cacheWidth` 随滑块每帧变 → `ResizeImage` 重解码 → **解码宽度（layoutWidth/cacheWidth）固定按阅读区全宽、显示宽度（ComicImage.width）才用实时 listWidth**，两者解耦。

### 39.3 网页 UI 二次还原客户端（`app.js` + `app.css`/`common.css`）

- **阅读设置/章节面板**：浮窗（`right:16px;bottom:78px;圆角28`）→ 桌面全高右侧栏（`top/right/bottom:0; width:min(450px); 圆角16 0 0 16`）+ 移动底部 sheet（`reader-panel-sheet`），用 `isMobilePanel()` 切 `-side`/`-sheet`。
- **章节列表竖排**：`.reader-chapter-list button` 误共用阅读模式列表的三列图标模板 `34px minmax(0,1fr) auto`（章节按钮无图标，标题挤进 34px 列竖排）→ 单独两列模板 `minmax(0,1fr) auto`。
- **章节当前项样式**：去 `color:#fff`（浅色白字看不清）/ 去主题色字 / 去 `0.78` 硬边框立体感 → 默认主题色扁平边框、当前项 = 主题边框 + 淡主题底 + **普通字色 `var(--text)` + 仅加粗**，与 `.reader-method-list` active 拆开。
- **详细档卡片**：布局顺序对齐（标题→副标题→标签中部弹性→底部 stats+漫画源 badge）、标签中性可见容器底 + 普通字色（不跟主题变）、右下徽章用 `sourceDisplayName`（非档位）。
- **详情页（整页）**：信息区 `infoChipHtml` 合体 accent 胶囊 → `infoRowHtml` 双胶囊（`.app-info-row`/`.app-info-key` 带 accent 色调 / `.app-info-val` 中性容器普通字色，圆角12）；操作区只留一个 72×72「阅读」方块（去「复制标题」）；Hero 封面 132→102×136、圆角16→12、标题 18；章节网格 `minmax(220,1fr)`（单章拉满整行）→ `repeat(auto-fill, minmax(160px,200px)) + justify-content:start`（固定宽不拉满左对齐，对齐客户端 `maxCrossAxisExtent:200`）。
- **排序默认倒序**：`readGridSort()` 默认 `desc:true`。
- **阅读滚动穿透**：进 reader 后滚动带动背后 grid → `.is-reader-route` 收敛宽度/滚动到 reader 容器、`overscroll-behavior:contain`（注意 §13：别让 `#console-root` 塌成 0 宽）。

### 39.4 服务端资源热刷新（`server_app.dart`）

- 根因：`/api/library/items` GET 返回 `_currentSnapshot()`，而它一旦 `_snapshot` 非空就**永返缓存**，服务端无文件监听 → 外部增删资源需重启才可见。
- 修：`start()` 扫描后对所有 root（current/original/customLibraryRoots）建 `Directory(path).watch(recursive:true)`，`_handleLibraryFileEvent` 收事件后 **3s 去抖** `Timer` → `_rescanResourcesFromWatch`（重入守卫 `_libraryWatchRescanRunning`）→ `rescanResources()`（已重算 signature，变化才经 WebSocket 推 `library-changed`，端侧自动失效重拉）。`_cancelLibraryWatchers` 在停止/挡位切换时取消。`watch` 失败 try/catch 降级记日志（Android 受限存储/网络盘可能不可靠）。

### 39.5 客户端远程档 401（已修复）

- V1.9.13「网页后台正式落地」（fb0aff3）把 `_requiresAuthorization` 改成**所有 `api/` 都鉴权**（除 `status`/`api/console/login`），而客户端 `RemoteLibraryClient._sendRequest` 从不带 token（Flutter 客户端无账号体系）→ 远程档全 401。**已修**：`_requiresAuthorization` 改为只对 `api/console/*`、`api/remote/proxy` 鉴权，放行 `api/library/*`（恢复 V1.9.13 之前客户端可裸连行为；勿给客户端硬塞 token 体系）。

### 39.6 涉及文件

- 新增：`lib/foundation/archive/archive_episode_builder.dart`
- Dart：`lib/foundation/local_library.dart`、`lib/foundation/archive/archive_registry.dart`、`lib/foundation/remote_library_data_source.dart`、`lib/components/archive_password_dialog.dart`、`lib/pages/local_library_page.dart`、`lib/pages/download_page.dart`、`lib/pages/local_comic_detail_page.dart`、`lib/pages/reader/image_view.dart`、`lib/server/local_resource_scanner.dart`、`lib/server/server_app.dart`、`lib/server/web_console/remote_proxy_handler.dart`
- 前端：`assets/web_console/js/app.js`、`assets/web_console/css/app.css`、`assets/web_console/css/common.css`（及 `js/common.js`/`shell.js`/`auth.js`/`admin.js`/`css/admin.css` 少量配套）

### 39.7 校验

- `flutter analyze` 须 0 issues；服务端模式实测：远程档列表/封面/阅读（含压缩包多章节、加密弹密码解锁）、网页端同上、外部增删资源数秒内自动刷新、阅读图片宽度滑块实时无闪烁、网页详情页/章节列表/面板视觉对齐客户端。**401 已修复**，客户端远程档应恢复正常。

### 39.8 服务端深扫排除封面（下载漫画最后一页糊图）

- 根因：`local_resource_scanner.dart` `_listImageFiles`/`_collectImageFiles` 不排除 cover-like 文件，下载漫画深扫时 cover.jpg 被当成一页（排序后在末尾，ResizeImage 缩放显糊）。客户端本地用 `_buildDownloadedEpisodeFilesForEp` 的 `files.where((path) => !_isCoverLikePath(path))` 排除了 cover，故本地无这一页。
- 修：`_listImageFiles` 排序后加 `where((f) => !_isCoverLikeFile(f))`，排除后为空才回退保留全部（避免目录只有封面时成空页）。复用已有 `_isCoverLikeFile`（与客户端 `_isCoverLikePath` 同款 `cover.jpg/jpeg/png/webp`）。

### 39.9 网页端管理面板增强（路径选择器 + 账号管理）

- **路径选择器**：服务配置三个路径输入框旁加「+」→ `openPathBrowser` 弹窗（面包屑 + 快捷根/盘符 + 目录列表 + 选择当前目录）。服务端新增 `GET /api/admin/browse?path=`（`PrivilegedStorageAccess.listDirectoryEntries`）。`_adminBrowseRoots` 仅保留盘符/系统根。
- **账号管理**：`admin.js` 导航加「账号管理」tab + panel + `renderUsers()`（列/建/删用户，仅 admin）+ 监听 `pk-console-users-open` 事件让头像菜单入口生效。

### 39.10 网页端滚动/布局修复

- **侧栏切换触发滚动**：`#console-shell` `min-height:100vh` + `transition:grid-template-columns` 列宽变化内容重排 → 改 `height:100dvh; overflow:hidden` + `#console-root height:100%; overflow-y:auto` 内部滚动。
- **主页无用滚动条**：`scrollbar-width:none` 隐藏。
- **toolbar 间距**：`:has()` 未命中，改直接 margin-bottom/padding-top。
- **reader 滚动穿透**：非全屏加 `overflow-y:auto` + 外层锁滚。

### 39.11 网页端详情页完整 redesign

- 信息区：`infoChipHtml` 单个合体胶囊 → `infoRowHtml` 双胶囊（`.app-info-key` accent 调 / `.app-info-val` 中性普通字色，圆角 12）。
- 操作区：只留一个 72×72「阅读」方块（去「复制标题」）。
- Hero：封面 132→102×136、圆角 16→12、标题 18。
- 章节网格：`repeat(auto-fill, minmax(160px, 200px)) + justify-content:start`（固定宽不拉满，对齐客户端 `maxCrossAxisExtent:200`）。

### 39.12 排序去双重边框 + 默认 desc

- `.app-sort` 容器不再共用 `.app-view-switch` 的胶囊外框（单按钮不需要外层框）→ 按钮自身单层胶囊。
- `readGridSort()` 默认 `{mode:'time', desc:true}`（对齐客户端最新在前）。

---

## 40. 网页移动端顶栏、浮动返回、浏览器返回与详情 sheet 底部收口过程

> 本节记录 V1.9.15 后围绕网页后台移动端体验的一轮实际工作线。重点不是新增业务能力，而是把移动端 topbar、列表返回、浏览器返回、PWA 与详情底部 sheet/footer 的交互状态记录清楚，避免后续重复走已经试过的方向。

### 40.1 管理/登录/图标与入口小收口

- 管理面板角色选择从原生 select 改为胶囊/切换按钮，提交值仍为 `user/admin`，后端不变。
- 登录成功链路改为写入 token/user、拉取 me 后刷新页面。
- “未设置后台密码”黄色风险提示只对 admin 显示；普通用户的服务配置不显示后台密码输入，也不会在保存时清空现有 `state.config.consolePassword`。
- Web Console 增加 PicaKeep 图标：来源为 Android `launch_icon_full.xml`，生成 `assets/web_console/pica-icon.svg`，并接入 favicon、左侧品牌、管理侧栏品牌。
- 应用面板收藏入口图标从星星改成爱心。

### 40.2 移动端 topbar 与层级

- 窄屏下 `#topbar` 从左侧 rail 改成 sticky 横向顶部栏。
- 头像菜单限制在视口内，避免溢出屏幕；桌面/横屏下通过提高层级和允许 overflow，避免菜单被侧栏裁剪。
- 移动端展开按钮视觉替换为 Pica 图标；`PicaKeep Console` 移到头像旁。
- 恢复 topbar 点击展开，移动端展开后显示模式切换和面板按钮。
- 层级关系已收口：头像菜单最高，topbar 高于列表与浮动返回，浮动返回高于卡片但低于头像菜单。

### 40.3 列表页浮动返回

- 列表页标题与返回按钮合并：`已下载 · 返回`、`图集 · 返回`、`资源库 · 返回`、`历史记录 · 返回`、`收藏夹 · 返回`。
- 图片收藏、相似推荐等列表页同步适配。
- 桌面和移动端统一走 fixed 浮动返回，不再分裂为不同 sticky/fixed 策略。
- 返回按钮位置会根据搜索/筛选/工具卡片状态动态计算：卡片可见时在卡片下方，卡片滚出或被 topbar 遮住后停在安全顶部。
- 滚动、resize、orientation change、view/list rerender、toolbar 展开收起都会触发重新计算。

### 40.4 移动端浏览器返回手势

- `app.js` 为内部 app view 写入浏览器 history 状态，避免手机浏览器返回键直接退出整个后台。
- popstate 会映射到现有内部 back：reader → detail/list → dashboard。
- forward 尽量恢复内部 app view。
- refresh/goGrid(true) 这类刷新动作不写入浏览器历史栈。
- `shell.js` 外层路由识别到内部 app history 时跳过 rerender/route handling，避免外层 shell 和内层 app 抢状态。

### 40.5 PWA 与浏览器工具栏尝试

- 已明确普通网页不能强制首次打开就隐藏浏览器地址栏/底部栏。
- 新增 `assets/web_console/manifest.webmanifest`，`index.html` 接入 manifest、`mobile-web-app-capable`、`apple-mobile-web-app-capable`、theme/status bar meta。
- 曾在 `shell.js` 尝试 touchmove/window-scroll booster 促使浏览器工具栏隐藏，用户反馈无效，已撤回。
- 后续改为移动端自然 document/window 滚动：放松 `html/body` 的固定满高和内部滚动限制，让浏览器按自身规则收起工具栏。

### 40.6 详情 sheet/footer 底部问题已试过的方向

当前问题集中在移动端漫画详情 bottom sheet：footer/按钮容易被浏览器底部工具栏遮住，或者工具栏收起后底部留下过大空白。

已做过/试过：

- 章节列表改成移动端双列，减少高度占用。
- 详情内容区保持可滚动。
- 通过 `shell.js` 设置 `--mobile-browser-chrome-pad` 做浏览器工具栏补偿，曾尝试：
  - `visualViewport` 与 layout viewport 差值；
  - 记录最大/最小 viewport 高度；
  - 无法判断时默认 pad；
  - PWA standalone 下 pad=0。
- 试过“三段式 sheet”：header 固定、中间章节/内容滚动、footer 固定/绝对定位，并给内容预留 footer 高度。用户反馈仍不理想，已回退。
- 回到内容流 footer 后，检查到空白来自多层叠加：`.app-panel-content` 底部 padding、`.app-panel-footer` 底部 padding、footer 通用 margin/padding、safe-area 和 chrome pad fallback。
- 后续方向从“所有垫高减半”改为“只保留一个浏览器工具栏补偿点”，优先在 footer 上消费，避免 content + footer 双重补偿。

### 40.7 当前推荐算法：两状态高度学习

用户提出的关键判断应作为后续首选方案：移动浏览器实际主要只有两种高度状态。

- 首次打开：浏览器地址栏/工具栏展开，视口高度最小，此时需要默认垫高。
- 第一次滚动收起工具栏后：视口高度最大，此时不需要浏览器 chrome 垫高。
- 只要学到这两个高度，后续就能按当前高度接近哪一端判断工具栏是否展开。

实现建议：

1. 只在移动端、非 PWA standalone 启用。
2. 当前高度优先取 `visualViewport.height`，取不到再用 `window.innerHeight`。
3. 初始高度记为 `minViewportHeight`，先使用默认垫高。
4. 观察到更大高度时更新 `maxViewportHeight`。
5. 当 `maxViewportHeight - minViewportHeight` 超过阈值后，按当前高度切换 pad：接近 min → 默认垫高；接近 max → `0px`。
6. CSS 侧只能有一个主要消费点；不要再同时让 `.app-panel-content` 与 `.app-panel-footer` 消费 `--mobile-browser-chrome-pad`。

### 40.8 涉及文件

- `assets/web_console/css/common.css`：移动端 topbar、头像菜单层级、shell/root 滚动结构。
- `assets/web_console/js/shell.js`：移动端 browser chrome pad、PWA standalone 判断、外层 popstate 跳过内部 app history。
- `assets/web_console/js/app.js`：浮动返回、内部 history、列表/详情 rerender、详情 sheet 行为。
- `assets/web_console/css/app.css`：详情 sheet、`.app-panel-content`、`.app-panel-footer`、章节列表、浮动返回。
- `assets/web_console/index.html`、`assets/web_console/manifest.webmanifest`：PWA 接入。

### 40.9 后续验证重点

- 手机浏览器竖屏：进入漫画详情后，footer/阅读按钮首次打开就可见，不需要轻微滚动才能露出。
- 浏览器工具栏收起后，底部不应出现明显大空白。
- PWA standalone：底部不应保留浏览器 chrome 垫高。
- 章节列表双列可用，最后一行不被 footer 遮住。
- 浮动返回仍能避让搜索/筛选/工具卡片，且不被头像菜单压错层。
- 浏览器返回键/手势按 reader → detail/list → dashboard 逐级返回，不直接退出页面。

## 41. V1.9.19 之后（本批未提交）：运行期性能第二轮 — profile 实测驱动的卡顿/缓存收口

> 本节记录一轮纯性能/体验优化，全程用 **profile 包 + DevTools 逐帧 + logcat 埋点**驱动，
> 不靠肉眼或 debug 包猜。延续启动那轮的方法论教训：**性能归因必须 profile/release 包**，
> debug 的 build 耗时是 JIT 假象。详细坑与修法见 [经验版 §26](./交接文档-经验思路版.md#26-运行期性能优化第二轮profile-实测驱动)。
> 改动均未提交，留工作区等真机验证。

### 41.1 详情页推荐全量重算缓存

- profile 实测：进入/退出本地漫画详情、下滑切 AppBar 标题，build 帧 180~196ms。
- 根因：`local_comic_detail_page.dart` 每次 `build()` 同步调 `_buildRecommendations()`，
  对全库逐项跑正则相似度 + sort，任意 setState 都重算。
- 修法：结果缓存（`_recommendationsCache`/`_recCacheComic`/`_recCacheMode`），原方法拆为
  `_computeRecommendations(mode)`；`_loadLocalItems` 里显式置 null 失效。下滑/翻页重复 180ms
  消除（复测 ~27ms）。**首次进入仍算一次（193ms）是固有成本。**
- 顺带：推荐项点击改 `App.pushInner`（跨导航修复）。

### 41.2 工具页预返回动画 RepaintBoundary

- 现象：predictive back 缩放动画 120Hz raster 卡顿，trace `saveLayer×52`。
- 修法：`tools.dart` body 包 `RepaintBoundary` 栅格化成单层。真机复核返回动画变顺，
  确认 saveLayer 来自工具页本身（`Card.outlined(Clip.antiAlias)`+`AnimatedCrossFade`），保留。

### 41.3 已下载转圈变长 = 远程探测串行阻塞本地加载

- 现象：进「已下载」转圈 3.5s（本地 4158 部）。`[DLPROBE]` logcat 埋点实锤：
  `checkRemoteAvailability` 耗 3004ms（远程离线超时）才返回，本地加载仅 462ms。
- 根因：`reload()` 里 `await _checkRemoteAvailability()` 串行挡在 `_loadComics()` 前，
  而 local 视图根本不读 `remoteAvailable`。
- 修法：local 视图跳过探测（`remoteAvailable=false`），只 aggregate/remote 视图 await。
  `showSourceSelector` 用 `_hasConfiguredRemoteServer` 兜底，不影响视图选择器。
  **注意**：这不是给探测加缓存（附录踩坑第 1 条），是 local 视图不需要它故不调用。

### 41.4 加载态全屏遮罩改列表区转圈

- 原则（用户定）：任何档位 loading 只在列表区转圈，保留 AppBar/视图选择器，不整页覆盖。
- 修法：`download_page.dart` 的 `build` 去掉整页 loading 三元，始终渲染正常骨架；
  `_buildComics` 在 `comics.isEmpty && loading` 时返回 `SliverFillRemaining` 居中转圈。

### 41.5 图集远程档滚动卡顿 = 老年代 GC 风暴

- 现象：滚远程图集列表 120Hz jank 15~25%，但 build/raster 全低，elapsed 全靠
  vsyncOverhead 撑高（最重 vsync 100~300ms）。用户体感「加载完不卡，卡在加载+显示封面时」。
- 排除：`[ALBUMPROBE]` 埋点证明 widget 构建 + 封面 provider 全 <1ms/秒，Dart 侧无辜。
- 真凶：trace `CollectOldGeneration`/`Scavenge`/`StartConcurrentMark` 大量——老年代 GC 暂停。
  滚动时大量封面字节在堆上快速分配/驱逐，撑爆新生代晋升老年代，触发昂贵 GC 冻结主线程。
- 修法（两段，基建层默认不影响其他子类）：
  1. `base_image_provider.dart` 加 `targetDecodeWidth`（默认 null）；远程封面 override `=> 480`，
     `decode` 走 `getTargetSize` 按宽缩放，避免数千 px 原图全尺寸解码。
  2. `base_image_provider.dart` 加 `cacheRawBytes`（默认 true）；远程封面 override `=> false`，
     不进 50MB 静态堆字节缓存（已有磁盘缓存兜底），消除堆字节驻留这个 GC 压力源。
- 实测（06 改后 vs 05，均 120Hz）：maxVsync 303ms→51ms（最严重尖峰降 1/6）、
  putIfAbsent 195→33、NewGC 153→85。剩余 ~13% jank 是封面解码上屏瞬间的固有成本，
  60Hz 下更轻，收益递减故止。

### 41.6 工具-缓存大小限制不生效

- 现象：工具页「684MB / 524MB」超限不降。
- 根因（双重失效）：`_trimToLimitInner`（`remote_library_data_source.dart`）统计全部缓存，
  但可删文件池只收 `remote_library_covers` 子目录（够不着占大头的 `App.cachePath`）；
  且 trim 只在远程封面下载后触发，平时不跑。
- 修法（保持原 LRU 设计，删最旧到限制内不清空）：可删池扩到 `App.cachePath`+`dataPath/cache`
  全部；加公开入口 `RemoteLibraryDataSource.trimCacheToLimit()`；`tools.dart` 的
  `_refreshCacheSize` 统计前先 trim，改完限制也触发。

### 41.7 profile APK 体积翻倍（非代码问题，记录备查）

- 反复构建后 APK 从 ~45MB 涨到 93.6MB。解剖：真实内容 43MB + 52MB 对齐填充孤儿条目，
  来自 AGP 8.12 zipflinger 增量打包累积空洞。`flutter clean` 全量重打包即压实（→41.1MB）。
- 教训：清产物用 `flutter clean`，别手删 `build/app/intermediates/` 半截目录（破坏增量一致性，
  会报「找不到 launch activity」假错误）。

### 41.8 涉及文件

- `lib/pages/local_comic_detail_page.dart`（41.1）
- `lib/pages/tools.dart`（41.2 RepaintBoundary、41.6 trim 触发）
- `lib/pages/download_page.dart`（41.3 跳过探测、41.4 列表区转圈）
- `lib/foundation/image_loader/base_image_provider.dart`（41.5 两个解码钩子）
- `lib/foundation/remote_library_data_source.dart`（41.5 远程封面 override、41.6 trim 范围+入口）
- `lib/pages/local_library_page.dart`（41.5 排查页，ALBUMPROBE 探针已撤）
- 分析文档：`Z-plan/后续优化-第一轮/运行期卡顿分析-推荐重算与返回动画.md`

### 41.9 后续验证重点

- 详情页：切换不同漫画/换推荐模式，推荐内容应正确刷新（缓存失效正常）。
- 已下载：进入应秒开（local 不再等远程 3s）；切远程/聚合档只列表区转圈。
- 图集远程档滚动：无几百 ms 冻顿；封面清晰度 480px 应无肉眼可见下降。
- 缓存管理：打开/改限制后「当前」应降到限制内。


## 42. V1.9.21（待提交）：本地已下载滚动优化与 root/shizuku 图集封面修复

> 本轮全部用 **profile 包 + DevTools 逐帧 + 临时探针**驱动。改动集中在本地已下载列表滚动性能和
> root/shizuku 权限下图集封面破图的全链路修复。详细坑与修法见 [经验版 §27](./交接文档-经验思路版.md#27-本地已下载列表滚动卡顿)
> 与 [§28](./交接文档-经验思路版.md#28-rootshizuku-下图集封面破图)。

### 42.1 已下载滚动 — 删常驻后台封面缓加载

- 删 `_warmManagedDownloadCache` 及其 `_backgroundCacheTask`、`pauseBackgroundCoverCache` 等死代码。
- 封面缓存完全惰性化，不再脱离页面生命周期跑 root 通道。

### 42.2 已下载滚动 — 按需迁移避开活跃滚动

- `_drainCoverResolveQueue` 活跃滚动时 break（只入队不打通道），`ScrollEndNotification`→140ms idle→重启补齐。

### 42.3 已下载滚动 — 本地封面降采样解码

- `_DownloadedPageComicTile` 开 `optimizeCoverDecode: true`，封面按布局尺寸解码，缓解折返重解码与老年代 GC。

### 42.4 已下载滚动 — 无封面项双层负缓存

- `noCoverSentinel` 持久化标记 + `_coverResolveFailedIds` 页面级拦截。无封面项折返零 root I/O，
  跨会话永久记忆，直到 `reload()` 调 `invalidateNoCoverSentinels()` 清除。

### 42.5 已下载滚动 — trustStorageFromDatabase 假阳性

- 两处 `? true` 改为查内存 Set `sourceDirectoryNames`。db-only 项不入封面队列、不走通道。

### 42.6 图集封面 — PrivilegedStorageAccess 空回退

- `listDirectoryEntries`/`readFileBytes`：dart:io 空列表/空字节时强制回退特权通道。
  修掉 scoped storage 下 `existsSync=true` 但读取静默空的根因。

### 42.7 图集封面 — 解析层回写 + 惰性 provider

- `resolveCoverPathForItem` 解析成功后回写 `item._localCoverPath`，同步路径下次命中。
- `coverImageProviderForItem` 惰性 provider：封面自身惰性拉取，不依赖外部 setState 时序，消除退出再进闪空。
- 外层 source 卡片加 `_LocalRootCollage` 2×2 子项封面拼贴。

### 42.8 图集封面 — 渲染层全链路去裸 FileImage

- 图集列表、详情页主封面、相关推荐、底栏/侧栏面板所有封面渲染统一改为
  `imageProviderForLocalPath`（privileged-aware），去裸 `FileImage`/`Image.file`/`existsSync` 判定。

### 42.9 图集封面 — imageProviderForLocalPath 特权短路

- 特权模式（root/shizuku）跳过 `existsSync` 快路径，直走 `StreamImageProvider`+`_readFileBytes`。
  这是让特权通道真正可用的最终开关。

### 42.10 撤销权限闪退定性

- 撤销 MANAGE_EXTERNAL_STORAGE 时 Android 强制 kill 进程是 OS 级固有行为，非 bug 不修。

### 42.11 涉及文件

- `lib/foundation/local_library.dart`（42.1 后台缓存、42.4 哨兵+refresh、42.5 trustStorage、42.6 空回退、42.7 回写+惰性 provider、42.9 特权短路）
- `lib/pages/download_page.dart`（42.2 滚动暂停、42.3 降采样、42.4 页面负缓存、42.8 渲染层+底栏面板）
- `lib/pages/local_library_page.dart`（42.7 拼贴+惰性 provider）
- `lib/pages/local_comic_detail_page.dart`（42.7 异步兜底、42.8 去 existsSync+惰性回退）
- `lib/foundation/privileged_storage_access_flutter.dart`（42.6 空回退）
- 分析文档：`Z-plan/后续优化-第二轮/本地已下载滚动卡顿-后台缓加载移除.md`、`Z-plan/后续优化-第二轮/全部文件访问权限图集封面破图.md`

### 42.12 后续验证重点

- root/shizuku 各测一遍：已下载列表滚动跟手、退出无 root 提示、折返不卡
- 全部文件访问权限/root/shizuku：图集列表卡片封面、详情主封面、推荐封面、底栏面板封面全部正常
- 图集点进去能阅读；full-access 与 managed download 不回归


## 43. 第三轮：单项源直开、图集计数对齐与开屏剪贴板 ANR 修复

> 详细坑与修法见 [经验版 §29](./交接文档-经验思路版.md#29-单项源直开与图集计数改为了项之和)
> 与 [§30](./交接文档-经验思路版.md#30-开屏剪贴板大文本anr-修复)。

### 43.1 单项目录源卡片直开详情

- `_openItem` 判子项数=1 且能命中缓存时直进详情，跳过中间列表。多子项源不变。
- `_LocalRootCollage` 单封面时铺满卡片，不走 2×2 GridView 留白。

### 43.2 图集计数改为子项之和

- 新增 `cachedAlbumChildrenCount`：对 `_storageEntries` 中 `supportsCollectionShell` 的
  `comicCount` 求和。与列表卡片 subtitle 同源，修复 `cachedAlbumCount` 按 `isAlbum` 过滤
  漏数自定义下载目录型源的问题。「我」页面改用新 getter。

### 43.3 剪贴板大文本 ANR

- 根因：Flutter `Clipboard.getData` 经 `JSONMethodCodec` 编码，大剪贴板文本在原生主线程
  `JSONStringer.string→Formatter.format` 逐字符转义，实测 6.4s 冻结。Dart-timeline perfetto
  trace 对此盲区。
- 修法：新增 `lingxue.picakeep/clipboard` 原生 MethodChannel，`maxChars=10000` 截断 +
  MIME 短路（`image/*` 直接返回 null）。Dart 端 `MissingPluginException` 时回退内置
  `Clipboard.getData` 并 Dart 侧截断。
- 验证：APP_SCOUT_HANG 消失，6389ms→541ms。

### 43.4 涉及文件

- `lib/pages/local_library_page.dart`（43.1 直开+铺满）
- `lib/foundation/local_library.dart`（43.2 cachedAlbumChildrenCount）
- `lib/pages/me_page.dart`（43.2 改用新计数）
- `android/.../MainActivity.kt`（43.3 Clipboard channel）
- `lib/tools/local_app_links.dart`（43.3 原生通道 helper）


- 横屏/桌面：头像菜单仍覆盖卡片，详情页布局不退化。

## 44. 桌面服务端部署：Linux headless + Windows 托盘 + 两端 CLI wrapper + CI 四包

> 详细根因→修法→不要再走的路见 [经验版 §31](./交接文档-经验思路版.md#31-桌面服务端部署linux-headless--windows-托盘--两端-cli-wrapper--ci-打包)。
> 权威打包步骤见 `Z-plan/后续优化-第二轮/打包说明-Linux与Windows.md`。

### 44.1 目标与核心铁律

- 装一个包：有屏开 GUI、无屏（NAS/SSH）起 CLI 服务端，**CLI 服务能力与桌面版完全一致**，管理在网页端。
- **不要 `dart build cli` 编 `bin/picakeep.dart`**——那是空壳服务端（`headless_cli_server.dart` 的 `api/library/items` 写死 `[]`），网页后台能开但漫画全空。两端改用 **GUI 二进制 + `--server`**（`lib/main.dart` 不建窗直起 `PicaKeepAdminServer`，能读资源）。
- 为何不纯 Dart 编完整服务端：`PicaKeepAdminServer` 传递 import 拉进 17 个 flutter-polluted 文件，`dart compile` 编不了；纯 Dart CLI 需 12000+ 行重构（否决）。

### 44.2 Linux（deb + xvfb + wrapper）

- `flutter build linux --release` → `.github/scripts/package_linux_deb.sh <amd64|arm64>`：GUI bundle→`/opt/PicaKeep`，wrapper→`/usr/local/bin/picakeep`（子命令 server/stop/status/logs，`xvfb-run ... --server`），`Depends: libgtk-3-0, xvfb`。
- wrapper 是 dash 脚本（`dash -n` 校验）；`setsid ... </dev/null &` 全分离让 `picakeep server` 干净返回；stop 用 PID 文件 + 按端口 `ss` 双兜底防孤儿死锁。
- **glibc**：在 ubuntu-22.04（2.35）构建，能在 Debian 12/13 跑；不要 ubuntu-24.04（2.39）。
- **tray_manager 的 Linux 编译期依赖（CI 实测踩坑，已修）**：托盘只 Windows 用，但 `tray_manager` 的 Linux CMakeLists 编译期强制要 `ayatana-appindicator3-0.1`，两个 Linux job 曾都挂在 `flutter build linux`（`CMakeLists.txt:32`）。修：CI 两 Linux job `apt-get` 加 `libayatana-appindicator3-dev`，deb `Depends` 加 `libayatana-appindicator3-1`。教训：跨平台插件「运行时只某平台用」≠「其他平台不编译」，加依赖后四平台都要验构建。
- **文件权限**（旧打包流程实测在 NAS 上 `picakeep` 报 Permission denied 的根因）：二进制权限是 `-rw-r--r--` 漏了 `x`，软链也没建。当前脚本三件套不能丢：`chmod +x` GUI 二进制、`chmod +x` wrapper、`dpkg-deb --root-owner-group`（强制属主 root:root，否则 CI runner 构建用户属主会让目标机普通用户读不到 `/opt/PicaKeep` 二进制和 `data/flutter_assets`/web_console → 终端跑不起来或服务端资源空）。
- 用户已真机验证：`picakeep server` 起服务、读到资源、stop 释放端口。

### 44.3 Windows（托盘 + wrapper）

- GUI exe `--server` 不弹窗即可读资源（实测 totalComicCount=395，HasWindow=False），无需 xvfb。
- 托盘（`tray_controller.dart` 新增，`tray_manager`）：仅 Windows，关窗缩托盘（有服务→hide，无服务→destroy）；图标须 `.ico`（`assets/tray_icon.ico`）。用户已验证。
- wrapper `windows/cli/picakeep.{cmd,ps1}`：cmd 转调 ps1，ps1 取上层 `picakeep.exe --server`（server/stop/status/logs）。**ps1 必须带 UTF-8 BOM**，否则中文 Windows 按 GBK 读会语法崩。
- 布局：Inno 把 `{app}\cli`（非 `{app}`）加 PATH→敲 `picakeep` 命中 wrapper 而非 GUI exe。
- 子命令含简写（两端一致）：`server`(s/up/start)、`stop`(x/down)、`status`(st)、`logs`(l/log)、`version`(-v/--version，列 PicaKeep 版本+系统信息)。Windows ps1 用 `switch -Regex`，Linux wrapper 用 `case` 多模式标签，改时两端同步。Linux 的 version 读打包写入的 `/opt/PicaKeep/picakeep.version`。

### 44.4 web_console 资源定位修复

`web_asset_loader.dart` 原用 `Platform.script`（Flutter 二进制里无效）→网页「资源 not found」。改用 `Platform.resolvedExecutable` 父目录派生候选根。真机验证无需环境变量。

### 44.5 CI（本轮收口）

- `.github/workflows/build.yml` push tag `v*` 或手动一次出 4 包：`linux-amd64-deb`(ubuntu-22.04)、`linux-arm64-deb`(ubuntu-22.04-arm)、`windows-x64`(windows-latest)、`windows-arm64`(windows-11-arm)。
- **本轮改动**：`windows-arm64` 之前只 `upload publish/`，现补全 Inno→出 `PicaKeep_ARM64_Setup_v*.exe`，与 x64 一致。
- **删除** `build-arm-installers.yml`（arm64 两 job 已被 build.yml 完全覆盖，消除 ARM 重复维护）。
- 无 Android（不动）、无 macOS（项目无 macos 目录）。x64 用 subosito 固定版 / arm64 用 git master 是有意的（arm runner 上 subosito 支持不全）。

### 44.6 涉及文件

- `lib/main.dart`、`lib/server/local_server_runtime.dart`、`lib/server/web_console/web_asset_loader.dart`
- `lib/tools/tray_controller.dart`（新增）、`lib/components/window_frame.dart`、`pubspec.yaml`、`assets/tray_icon.ico`（新增）
- `.github/scripts/package_linux_deb.sh`（新增）、`windows/cli/picakeep.{cmd,ps1}`（新增）
- `.github/workflows/build.yml`（4 包）、`build-arm-installers.yml`（已删）

### 44.7 待清理（不影响打包，未做）

空壳源码已无 CI 引用，可删：`bin/picakeep.dart`、`bin/picakeep_server_runner.dart`、`lib/server/headless_cli_server.dart`、`tool/{build,install}_cli.{sh,ps1}`；README 的 `picakeep run` 旧描述待更新。


## 45. 新需求第一轮：阅读器退出停止加载、全局搜索占位与服务端持久化

> 详细坑与修法见 [经验版 §32](./交接文档-经验思路版.md#32-退出阅读器停止图片加载与传输) / [§33](./交接文档-经验思路版.md#33-全局搜索缺失项按开关占位或过滤) / [§34](./交接文档-经验思路版.md#34-服务端数据持久化与路径配置飞牛-docker)。

### 45.1 退出阅读器停止图片加载

- 新增 `StreamImageAbortSignal`：`StreamImageProvider` 加载时持有 `StreamSubscription`，收到 abort 后 `cancel()`。
- 阅读器 `ComicReadingPageLogic` dispose 时先触发 abort（区分"退出"与"翻页"），再清缓存。
- 远程 `loadImageWithProgress` 全阶段响应 abort：等待许可/建连/响应头→补 `abort()` 避免孤儿；body 阶段由 stream cancel 触发 `_guardedImageBody` 回收。

### 45.2 全局搜索缺失项按开关控制

- 全局搜索已下载结果读取已有全局设置位 `localLibraryShowAllDatabaseRecordsSettingIndex`。
- 关→过滤 db-only 项；开→显示文字+缺失占位（复用下载页 `DownloadedComicTile` 逻辑）。
- 仅作用于已下载类结果。

### 45.3 服务端数据持久化（PICAKEEP_DATA_DIR）

- 新增 `PICAKEEP_DATA_DIR` 环境变量，`picakeep_server.json`/`library_trash.json`/`web_console.db`/`local_favorite.db`/`history.db` 统一落持久化根。
- 未设置时回退原路径；同时设置 `--config` 时 DATA_DIR 控制数据目录、`--config` 只控配置文件位置。
- `server_config` 新增 `managedDataRoot`，收藏/历史库读写统一走该根。

### 45.4 Linux 目录选择器存储空间快捷导航

- 服务端新增 `GET /api/admin/volumes`，扫 `/vol*` 过滤存在 `/<vol>/1000/` 的卷。
- 管理后台目录选择器弹窗渲染快捷入口，点击直接跳转。仅 Linux 生效。

### 45.5 飞牛 Docker 部署说明

- 新增 `Z-plan/新需求-第一轮/03-飞牛-Docker-持久化部署说明.md`。
- 核心约定：`PICAKEEP_DATA_DIR` 必须挂宿主持久目录；`/vol1 /vol2 /vol3` 必须挂进容器才能被快捷导航扫到。

### 45.6 涉及文件

- `lib/foundation/image_loader/stream_image_provider.dart`、`lib/foundation/remote_library_data_source.dart`（45.1 abort）
- `lib/pages/reader/reading_logic.dart`、`comic_reading_page.dart`、`reading_data.dart`、`image_view.dart`（45.1 reader dispose）
- `lib/pages/local_search_page.dart`（45.2 搜索开关）
- `lib/foundation/app.dart`、`lib/foundation/local_data_source.dart`（45.3 DATA_DIR）
- `lib/server/server_app.dart`、`lib/server/local_server_runtime.dart`、`lib/server/headless_cli_server.dart`（45.3/45.4）
- `lib/main.dart`（45.3 --headless-server 参数）
- `assets/web_console/js/admin.js`（45.4 快捷导航 UI）

### 45.7 仍未完成

- 网页端收藏/DB 路径独立可编辑表单项（当前仅控制台展示 + 快捷导航）
- 飞牛 NAS Docker 实机重启验收
- 详见 `Z-plan/新需求-第一轮/03-后续补齐-收藏DB路径与飞牛实机验收.md`


## 46. 修复第一轮：DB路径修复、快捷导航、管理面板UI与配置持久化

> 修复 V1.9.25 暴露的问题。详细见 [经验版 §35-§38](./交接文档-经验思路版.md#35-收藏历史-db-路径修复与网页配置入口)。

### 46.1 收藏/历史 DB 路径修复（修复 01）

- 解除 `main.dart` 把 db root 误绑到 `PICAKEEP_DATA_DIR`。
- 新增 `managed_data_root_resolver.dart`：手动配置优先 → download.db 上一层锚定 → 回退为空。
- `server_config` 新增 `managedDataRoot`，网页加配置入口（输入框+目录选择器）。

### 46.2 存储空间快捷导航修复（修复 02）

- 快捷项改为 `{label, jumpPath}` 结构：显示「存储空间 N」，点击直达 `/volN/1000`。
- 过滤仅物理卷，排除 `/vol00/vol01/vol02`；不塞不存在库根。

### 46.3 管理面板 UI（修复 03）

- 重写 `admin.css`：8px 圆角、浅色工作台、统一表格/表单/按钮/状态样式，9 个 tab 全覆盖。

### 46.4 服务端配置持久化（修复 04）

- `_resolveEffectiveConfig` 服务端态以文件为权威源，不再用 appdata 空值覆盖。
- 配置文件名 `picakeep_server.json` → `picakeep_server.data`。客户端 GUI 态不受影响。

### 46.5 涉及文件

- `lib/server/managed_data_root_resolver.dart`（新增）、`lib/server/server_config.dart`、`lib/server/local_server_runtime.dart`、`lib/server/server_app.dart`、`lib/server/headless_cli_server.dart`、`lib/main.dart`
- `assets/web_console/css/admin.css`、`assets/web_console/js/admin.js`


## 47. 新需求-第二轮：常量集中化与 server_app 拆分

> 详细见 [经验版 §39-§40](./交接文档-经验思路版.md#39-服务端常量集中化)。本轮共 5 份计划：01 打包脚本入库→废弃、03 重复文件诊断→无需执行、00 为参考文档。
> 实际落地 02（常量集中化）和 04（拆分 server_app）。

### 47.1 服务端常量集中化（02）

- 新增 `defaultServiceAdminPortInt = 9527`，`server_config.dart`/`local_server_runtime.dart` 裸 9527 改为引用。
- `base.dart` settings 槽位加注释关联。严格执行「绝不能碰」清单无过度替换。

### 47.2 拆分 server_app.dart（04）

- 2917 行单一上帝类 → 宿主 252 行 + 9 个 part/extension 文件。
- 使用 `extension on PicaKeepAdminServer` 模式，私有成员跨 part 自然可见，零可见性改动。
- 职责分工：routing / browse / console / trash / library / favorites / files / status / util。

### 47.3 涉及文件

- `lib/foundation/app_runtime_mode.dart`、`lib/server/server_config.dart`、`lib/server/local_server_runtime.dart`、`lib/base.dart`（02）
- `lib/server/server_app.dart` + 9 个新增 part 文件（04）


## 48. 新需求-第三轮：local_library.dart 深度拆分

> 详细见 [经验版 §41](./交接文档-经验思路版.md#41-拆分-local_librarydart-深度)。分两步完成。

### 48.1 第一步（01，纯移动）

- extension+part 模式迁出 35 个安全成员，拆出 4 个 part（manager_settings/query/cover/archive）。
- 宿主 4089→3850。受限于 Dart extension 裸调用 static 限制，仅拆可安全编译的子集。

### 48.2 第二步（02，放宽约束）

- 69 个 private static 提为文件级顶层私有函数，调用点仅去 `LocalLibraryManager.` 前缀。
- 新增 scan+static 两个 part，宿主 3850→1376。
- 验证：analyze 全绿、方法集合零增删、函数体指纹一致、build 通过。

### 48.3 最终结构

| 文件 | 职责 |
|---|---|
| local_library.dart（宿主，1376行） | import + part 声明 + 14 顶层类 + manager 入口 |
| local_library_manager_settings.dart | 配置路径/下载根/权限判定 |
| local_library_query.dart | 刷新/加载/查询 |
| local_library_cover.dart | 封面缓存/解析/哨兵 |
| local_library_archive.dart | 归档/collection shell/单图册 |
| local_library_scan.dart | 源构建与扫描 |
| local_library_static.dart | 69 个顶层私有函数（db/路径/排序/IO） |

### 48.4 涉及文件

- `lib/foundation/local_library.dart`（宿主）+ 6 个 part 文件（新增/改）


## 49. 第三轮修复：analyzer、远程收藏、安卓返回、加密包、配置共用

> 03~07 共 5 个修复，详细见 [经验版 §42-§46](./交接文档-经验思路版.md#42-修复02拆分analyzer误报)。

### 49.1 修复 analyzer 误报（03）

- 02 拆分后 analyzer 报 11 个 undefined，但 Win/Android build 均通过（CFE 正常）。
- 3 个文件对 `local_library_settings.dart` 的 import 调为 package 路径/show。

### 49.2 修复远程收藏跳转（04）

- `RemoteFavoriteItem` 新增 `itemId`/`displayId`，点击优先 `fetchItemDetail(itemId)` 直连。

### 49.3 修复安卓历史返回（05）

- `history_page`/`me_page` 改 `pushInner`，阅读不再压 root Navigator。

### 49.4 修复加密包远程阅读（06）

- 解锁后重新 `fetchItemDetail` 刷新对象，避免旧 pages 为空。

### 49.5 客户端/服务端配置共用（07，A/D 单向播种）

- 桌面同进程 GUI+服务端场景，配置文件空字段用 appdata 非空值单向填入。

### 49.6 涉及文件

- `lib/foundation/privileged_storage_access_flutter.dart`、`lib/server/server_config.dart`、`lib/server/local_resource_scanner.dart`（03）
- `lib/foundation/remote_library_data_source.dart`、`lib/pages/favorites/main_favorites_page.dart`（04）
- `lib/pages/history_page.dart`、`lib/pages/me_page.dart`、`lib/foundation/download.dart`（05）
- `lib/foundation/remote_library_data_source.dart`（06）
- `lib/server/local_server_runtime.dart`（07）

### 49.7 验收清单（04~07 上机实测参考）

- 远程收藏跳转：手机/网页点收藏项→应进详情（不弹不可定位）；Win 本体无回归
- 安卓历史返回：系统返回→回历史页；其他四入口（收藏/本地库/下载/预览）各回各页
- 加密包远程阅读：手机远程解锁后能翻页；网页/Win 无回归
- 配置共用：GUI 配路径→开服务→直接扫到；网页配置重启不丢（04 红线）；headless 无变化
- 详见 `Z-plan/新需求-第三轮/修复04-07-上机实测清单.md`

### 49.8 安卓双桌面图标修复

- 问题：修复 Android 构建后 debug 包出现两个桌面图标
- 根因：main/AndroidManifest 补回 MainActivity launcher 后，默认启用的 LauncherBlue alias 也带 launcher，Android 桌面显示两个入口
- 修法：debug/profile overlay 移除 alias 的 launcher intent-filter；release overlay 移除 MainActivity launcher 保留 alias 图标切换
- 验证：`flutter build apk --debug/profile/release` 均通过，合并 manifest launcher 数符合预期


## 50. 拆分 download_page 逻辑层（08）

> 详细见 [经验版 §47](./交接文档-经验思路版.md#47-拆分-download_page逻辑层)。

### 50.1 范围

仅拆 `DownloadPageLogic`（1012 行/60 方法/33 字段）。`_DownloadPageState`、`extension ReadComic on DownloadedItem`、顶层函数、数据类、`DownloadedComicInfoView` 均不动。

### 50.2 方案

- 2 个 `static const` 提升为文件顶层 `const`。
- extension+part 模式，4 个 part：loading(17)/cover(16)/actions(11)/view(21)。
- 宿主保留：33 个字段 + 构造 + `@override refresh()`。

### 50.3 验证

- token 比对 65 个迁出方法零差异、analyze 全绿、Win/Android 双端 build 通过。
- Android 真机安装启动 + 下载页可见性（标题/排序/搜索/封面节点）正常。

### 50.4 涉及文件

- `lib/pages/download_page.dart` + 4 个 part 文件


## 51. 拆分 local_favorites.dart（10）

- `LocalFavoritesManager`（903 行/51 方法）拆为 4 个 part：folders(17)/comics(12)/query(18)/db(4)。零 static，cache 单例留宿主。
- 涉及文件：`lib/foundation/local_favorites.dart` + 4 个 part

## 52. 拆分 trash.dart（11）

- `TrashManager`（1386 行/32 方法）拆为 4 个 part：delete(11)/restore(7)/remote(8)/io(6)。实例字段 2 个，instance 留宿主。
- 涉及文件：`lib/foundation/trash.dart` + 4 个 part

## 53. 拆分 local_resource_scanner.dart（12）

- `LocalResourceScanner`（2063 行/84 方法）是全项目最大未拆 god class。拆为 6 个 part（managed/custom/metadata/cover/io/text）。零 static，字段仅 1 个。
- `_basename` 路径方法专项核对反斜杠转义，未重演二轮 04 的 bug。
- 涉及文件：`lib/server/local_resource_scanner.dart` + 6 个 part

### 53.1 附带：补齐 widget_test.dart 最小 smoke test

- 修复现有空 `test/widget_test.dart`，使 `flutter test` 能正常运行。


## 54. 在线能力移植：我页 UI 重排 + 账号入口 + 在线搜索入口（01）

> 来自 `Z-plan/新需求-主线-第一轮/`，**主线第一轮 01~09 全部已完成**。
> 目标：从原项目 PicaComic 移植完整的 picacg 在线能力到 PicaKeep。

### 54.1 主要改动

- 我页两列布局：左列 历史/已下载/[账号|资源库]，右列 [图集|图片收藏]/工具
- 我页单列布局：历史→已下载→[账号|资源库]→[图集|图片收藏]→工具
- 新增账号占位页 `lib/pages/accounts/account_page_placeholder.dart`
- 新增在线搜索占位页 `lib/pages/online_search/online_search_page_placeholder.dart`
- mainPage 顶部栏加「在线搜索」入口（`Icons.travel_explore`），与本地搜索（`Icons.search`）并存
- `_MainPaneActionPage` 枚举加 `onlineSearch`，防重复 push

### 涉及文件

- `lib/pages/me_page.dart`、`lib/pages/main_page.dart`
- `lib/pages/accounts/account_page_placeholder.dart`（新增）
- `lib/pages/online_search/online_search_page_placeholder.dart`（新增）

## 55. 在线源地基（02）

> PicaKeep 原本只有本地源和远程源，无在线源概念。本轮打下完整地基。

### 55.1 主要改动

- 双账号架构：`OnlineAccount`（全局 null→临时→正式登录），支持等待函数 `ensureAccount`
- HTTP 代理池独立隔离：`lib/network/` 新增 `PicacgHttpClient`、`HttpProxyManager`、`BaseProxyManager`
- mainPage 新增在线 Tab（第三极，与本地/远程并列），含在线侧 drawer
- "我"页分支在线入口：在线态显示在线头像/昵称，非在线态显示占位
- 新增 `comic_source` 抽象：`lib/comic_source/` 目录（后续挂 picacg/hitomi 等多源）

### 涉及文件

- `lib/foundation/online_account.dart`（新增）
- `lib/network/`（新增目录）：`picacg_http_client.dart`、`http_proxy_manager.dart`、`base_proxy_manager.dart`
- `lib/comic_source/`（新增目录）：`comic_source.dart`、`models.dart`
- `lib/pages/main_page.dart`（改）
- `pubspec.yaml`（新增 dio、cookie_jar 等依赖）

## 56. picacg 单源全链路（03）

> 在 02 地基上打通 picacg（哔咔漫画）完整流通链路。

### 56.1 主要改动

- pica 协议登录：`PicacgSource` 实现 `AccountLoginProvider`，token 持久化到 `secureStorage`
- 分类/排行页：`lib/pages/online_comic/categories_page.dart`
- 搜索：在线搜索页 `lib/pages/online_search/` 接入 picacg 搜索 API
- 漫画详情：`lib/pages/online_comic/comic_detail_page.dart`，含作者/标签/章节/评论列表
- 章节列表与看图：`lib/pages/online_comic/gallery_page.dart`、`manga_reader_page.dart`
- 图片中间层服务端代理：`_picacgImageProxy` handler（`server_app.dart`），绕过跨域
- 在线下载：`lib/foundation/online_download_manager.dart` + `lib/pages/downloading/`
- 在线收藏同步

### 涉及文件

- `lib/comic_source/picacg/`（新增）：`picacg_source.dart`、`models.dart`、`api.dart`
- `lib/pages/online_comic/`（新增）：多个页面
- `lib/pages/online_search/`（新增/改）
- `lib/foundation/online_download_manager.dart`（新增）
- `lib/pages/downloading/`（新增）
- `lib/server/server_app.dart`（新增图片代理路由）

## 57. 在线搜索下载登录修复（04）

- 新手任务 token 自动检测并返回
- 手误关闭在线 Tab 后的恢复（不丢失在线态）
- 登录页登录后不自动跳转修复
- 详情页 WebView 显示兼容
- 涉及文件：`lib/pages/online_comic/`、`lib/pages/accounts/`、`lib/pages/online_search/`

## 58. 搜索结果独立页与详情页账号卡还原（05）

- 搜索页双源切换：本地搜索历史 + 在线搜索分支
- 收藏页在线态标签显示
- 详情页双卡（收藏卡+关注卡）+ 关注/取消关注按钮
- 涉及文件：`lib/pages/online_search/`、`lib/pages/online_comic/`、`lib/pages/favorites/network_favorites_page.dart`

## 59. 搜索栏卡片复用与入口页瘦身（06）

- 入口页去掉在线专用入口，统一走搜索
- 搜索结果卡片支持图片代理 URL
- 涉及文件：`lib/pages/online_search/`、`lib/pages/online_comic/`

## 60. 下载队列页原项目视觉还原与下载速度（07）

- 下载队列页 UI 还原原项目风格
- 下载速度计算与显示修复
- 涉及文件：`lib/pages/downloading/`

## 61. 下载握手失败走系统代理与并发持久化（08）

- 下载 TLS 握手失败时回退系统代理
- 下载并发控制与数据库持久化
- 涉及文件：`lib/foundation/online_download_manager.dart`、`lib/network/`

## 62. 计划外增量修复（09）

- 图片代理兼容（多域名支持）
- 标签从 API 响应补全
- Token 失效自动清理与重登录
- 下载设置独立页 `lib/pages/settings/download_settings.dart`
- UI 细节对齐原项目
- 涉及文件：`lib/foundation/online/`、`lib/pages/downloading/`、`lib/pages/settings/download_settings.dart`（新增）


## 63. jm（禁漫天堂）单源全链路接入（10~19）

> 来自 `Z-plan/新需求-主线-第一轮/` 10~19，10 份计划。部分完成（10/11/13），其余已完成。

### 63.1 jm 全链路（10/16/17）

- 多域名探测+Cookie登录、AES-ECB响应体解密、图片图块重组（scramble算法+isolate）。
- 搜索/详情/阅读/下载/网络收藏全通，复用picacg已建UI框架。
- 详情页还原：作者/标签/章节/封面/描述信息补全、评论列表与标签区优化。
- 涉及文件：`lib/comic_source/built_in/jm.dart`、`lib/network/jm_network/`、`lib/pages/online_comic/`

### 63.2 域名与404修复（12/13）

- 动态API域名获取（`selectDomain`启动探测），URL编造错误纠正，搜索登录全404→200。
- 涉及文件：`lib/network/jm_network/`

### 63.3 下载图块重组与编解码修复（14/15/18）

- 诊断图块重组RangeError→修复边界：width剪裁最小3列、放缩图片尺寸阈值、scrambleY>1拆分。
- WebP-VP8解码器系统性越界根治：uint64→uint32宽度适配、Huffman表索引越界保护、颜色缓存重建完整性。
- 涉及文件：`lib/network/jm_network/`、`lib/comic_source/built_in/jm.dart`

### 63.4 增量修复（11/19）

- 下载404、详情页调整、本地读图兼容。
- 涉及文件：`lib/pages/online_comic/`、`lib/foundation/online_download_manager.dart`


## 66. 通用在线详情页基类搭建（22）

> 来自 `Z-plan/新需求-主线-第二轮/05`。已完成，真机验证通过 ✓

### 66.1 基类架构

- `BaseOnlineComicPage<T>`（~430 行）：13 强制方法（标题/id/作者/标签/推荐/章节/操作按钮等）+ 16 可选重写（分类/状态/评分等），统一 Sliver 结构
- `OnlineComicPageLogic<T>`（~120 行）：复用 `StateController`，加载/错误/数据三态，`retry()` 刷新，首次触发标志+异步加载避免 setState 冲突
- ScrollController 监听 + AppBar 标题随滚动渐显（阈值 136px）
- 涉及文件：`lib/pages/online_comic/base_online_comic_page.dart`、`online_comic_page_logic.dart`

### 66.2 通用 UI 组件

- `online_comic_page_components.dart`（~430 行）：6 组件——`OnlineComicCover` 封面信息区、`OnlineComicActionButton` 动作按钮（busy/badge）、`OnlineComicTagsSection` 标签分组（点击跳搜索+长按菜单）、`OnlineComicEpisodesList` 章节折叠（>20+展开）、`OnlineComicRecommendationGrid` 推荐网格（3 列）、统一错误/加载视图
- 所有组件空数据处理：`SizedBox.shrink()` 或对应区块隐藏
- 涉及文件：`lib/pages/online_comic/online_comic_page_components.dart`

### 66.3 文档与验证

- `docs/新增在线源站接入指南.md`：理念/方法清单/数据要求/6步接入/完整示例/FAQ
- `example_source_page.dart`（JM 模型，0 issue 可编译）
- 真机验证（Redmi K30i）：封面/标题/标签/按钮/章节全部正确，空区自动隐藏
- 核心指标：新增源站开发 2-3 天→0.5 天（-70%），代码复用率 30%→90%+

### 66.4 实现偏差

- 交互回调增加 `BuildContext` 首参（无全局 context）
- 推荐区用轻量载体 `OnlineComicRecommendation` 解耦（非泛型 T）
- 试点 Nhentai→JM（Nhentai 网络层不存在）
- 章节折叠/推荐区新建（现有 JM 详情页无此逻辑）
- 未改动现有 picacg/jm 详情页


## 67. JM 详情页迁基类重做 + 账号态启动重登修复（未提交，本轮）

> 背景：第二轮 01（jm 详情完善）/ 02（picacg 详情完善）的未提交实现代码曾被误操作永久丢失，第一轮全链路移植（V1.9.32/V1.9.33）已提交未受影响。本轮在 05 基类（V1.9.34）上重做 jm 功能 + 新增账号态启动重登修复。**当前全部未提交**，状态：代码完成 + `flutter analyze` 0 error，待真机复核。

### 67.1 背景：为何是"重做"

第二轮 01（jm 详情完善）/ 02（picacg 详情完善）的**未提交实现代码**曾被一次误操作 `git checkout --` 永久丢失。第一轮全链路移植（`V1.9.32` picacg、`V1.9.33` jm）已提交、未受影响。删后恢复路线：
- **05 基类搭建** → 已提交 `V1.9.34`。
- **jm 在基类上重做**（`JmComicPageV2`，等于把丢失的 01 jm 功能搬到 V2 基类）→ 未提交。
- **06 账号态启动重登** → 未提交（本轮新增）。
- **picacg（02）缺口至今未补**：`online_comic_detail_page.dart` 仍停在 `V1.9.32` 第一轮裸状态，收藏图标恒空心、不读账号态。迁移方案见 `docs/新增在线源站接入指南.md` §七。

### 67.2 JM 详情页 V2（基类版）功能与微调

`JmComicPageV2 extends BaseOnlineComicPage<JmComicInfo>`，入口已切（`jm.dart` comicPageBuilder / `local_comic_detail_page` / `online_search_page`），旧 `jm_comic_detail_page.dart` 成死代码。

- 基类动作区双排：上排无底描边圆形图标（从头开始/分享/收藏/赞/评论），下排两大胶囊（下载 tonal / 阅读 filled）。
- 评论头像回归（`getJmAvatarUrl` → `media/users/$photo`，`getComments` 补解析 `photo`）。
- 收藏/点赞图标状态化（logic 加 `liked` 态 + `loadLikeState`/`setLiked`，`onFavorite` 用 `currentFavorite` toggle）。
- 收藏夹选择弹窗（`_JmFolderSelectDialog`，加收藏先拉 `getFolders()`）。
- 信息区精简（只留来源/浏览/评论）、喜欢图标统一 `thumb_up`、图标行跟随主题色、推荐显示全名小字、标签区彩色分类。
- 信息区文字 `SelectionArea` 可长按选中；标题 `titleMedium`；外层 `GestureDetector(translucent, onTap: unfocus)` 点空白清除选区。

### 67.3 账号态启动重登（06，本 bug 根治）

**bug：** 每次进 jm 详情页，已收藏/已点赞恒显示成未收藏/未点赞。

**根因（三者叠加）：** ① 缺启动重登（上游 `init→reLogin` 我们没有）；② 僵尸 cookie（jm cookie 从上游内存态改成 SQLite 持久化，无 `expires` 永不被过期清理，长期带失效 cookie 请求）；③ 游客 200 静默降级（`/album` 失效会话返 200 但缺 `is_favorite`，读自愈只在非 200 触发够不着，缺字段静默 `false`）。

**修复：**
- **P0 启动重登**：`jm_network.dart` 新增 `warmUpOnStartup()`（活域名重选 → 已登录则 `reLoginFromStored` 换新会话，串行、异步、吞异常）；`main.dart` 启动序列接入（在 `ComicSource.init()` 之后）。登录态用网络层自有 `_hasStoredLogin`（读 jm.data token），不引入 ComicSource 依赖。
- **P1 游客 200 兜底**：`getComicInfo` 加 `isRetry` 参数；`/album` 返 200 但 `!containsKey('is_favorite') && !containsKey('liked')` 且本地已登录 → 重登重取一次（`containsKey` 区分"字段缺失"vs"真 false"）。
- **P2 401 文案放宽**：抽 `_looksLikeLoginError`（登入/登录/login/member），`_get`/`_post` 共用。

### 67.4 历史记录勘误

早期实现 `_post` 自愈时曾加 `while(_performingLogin)` 等待——该版引发**登录死锁**（login 置 flag → _post 死等），已移除、改为 login 调 _post 传 `isRetry:true`。`while(_performingLogin)` 现仅 `_get` 保留（与死锁无关）。

### 67.5 接入指南更新

`docs/新增在线源站接入指南.md`：方法清单补 `loadLikeState`/`onShare`/`onEpisodeLongPress` + 受保护辅助表（`refreshFavorite`/`refreshLiked`/`currentFavorite`/`currentLiked`）；§六改掉过时的"仅用于新增源站"；新增 **§七 迁移已有源站**（为 picacg 迁基类铺路，附"绝大多数迁移不用改基类"判定清单）。

### 67.6 其他随轮改动

- **全局字号设置**（设置-外观）：`appearance_settings.dart`（index 119）+ `main.dart` `MediaQuery.withClampedTextScaling` 全局锁定，屏蔽各设备系统缩放差异。
- **我页卡片标题换行修复**：`me_page.dart` `_MePageCard` ListTile 间距收紧。

### 67.7 JM 评论回复功能（已实现但暂不可用）

**实现内容：**
- **网络层**：`jm_network.dart` 新增 `replyComment(aid, content, commentId)`，尝试多种参数组合（`comment_id`/`video_id`/`CID` + `is_reply=1` + `forum_subject=1`），参考 GitHub 多个同类项目（6.5k⭐ Python 爬虫、3.9k⭐ Qt 桌面端等，调研覆盖 7 个项目）
- **UI 层**：`jm_comments_page_v2.dart` 完整回复交互（`_replyTarget` 状态、`_ReplyBar` 提示条、顶层评论回复图标/长按触发、输入框 hint 切换、`_sendComment` 检测回复模式调用 `replyComment`）
- **POST 200 静默降级兜底**：`_post` 在解密前检查 `status:'fail'` + 登录错误文案 → 触发自愈（对齐 `_get` 的游客 200 兜底，修复 POST 写操作的"請先登入會員"自愈缺口）

**真机测试结果：❌ 失败**
- 顶层评论发送成功（验证登录态/自愈正常）
- 回复发送后服务端返回 `{status: fail, msg: 勿重複留言，請重新確認留言內容。, cid: 0}`
- 尝试 4 种参数组合均失败：`aid+comment_id` / `video_id+comment_id` / `aid+CID` / `/ajax/album_comment` 端点（返回 HTML）
- **结论**：JM 移动端 API (`/comment`) 不支持回复功能，或需未知参数/调用流程；6.5k⭐ Python 项目用网页端接口与移动端 API 不同

**当前状态：**
- 代码已实现但**暂不可用**（UI 可见但无法成功发送回复）
- 查看回复功能正常（嵌套显示已有回复）
- 发顶层评论功能正常（已验证）
- 上游 PicaComic 也未实现回复功能（同为移动端）

**后续：** 需抓包网页端真实请求或找官方 API 文档确认移动端回复接口，暂时保留代码待验证。

### 67.8 待真机复核

- [x] 已登录冷启动直接进**已收藏**详情页 → 收藏/点赞图标即高亮（P0/P1 验证）
- [x] 无网/重登失败不卡启动、不崩
- [x] 信息区可长按选中，点空白/章节/按钮处清除选区
- [x] 标题字号合适
- [x] JM 评论回复：顶层评论成功，回复失败（移动端 API 限制）

---

## 68. Picacg 详情页迁基类重做 + 评论/回复全链路（未提交，本轮）

> 承接 §67.1 遗留「picacg（02）缺口至今未补」。当前未提交，状态：代码完成 + `flutter analyze` 0 问题（涉及文件单测），待真机复核。

### 68.1 背景：补回被误删的 02，照 JM 路线重做

§67.1 记过，02（picacg 详情完善）未提交代码被 `git checkout --` 误删，回退到 `V1.9.32` 裸状态（旧 `OnlineComicDetailPage` 独立 StatefulWidget，收藏图标恒空心、无评论/点赞/推荐/creator）。本轮不修旧页，照 §67 的 JM 路线在 05 基类上**重做**为 `PicacgComicPageV2`，旧页废弃。评论/点赞 API 与字段全部当场实读原项目 `PicaComic_初始`（`methods.dart` 的 `loadMoreCommends`/`comment`/`likeOrUnlikeComment`/`getReply`）坐实后照搬级移植。

### 68.2 Picacg 详情页 V2（基类版）

`PicacgComicPageV2 extends BaseOnlineComicPage<PicacgComicItem>`，入口已切（`picacg.dart` comicPageBuilder + `local_comic_detail_page.dart` picacg 分支），旧 `OnlineComicDetailPage` / `picacg_comments_page.dart` 成死代码。

完成度对齐 JM：图标行（从头开始/收藏/赞/评论）+ 下载/阅读大胶囊 + 标签区（ID/作者/汉化组/分类/标签，长按复制·搜索）+ creator 卡片（`buildCustomSection`，头像+昵称+Lv）+ 简介 + 2 列章节网格（倒序/折叠）+ 推荐区。`loadFavoriteState => data.isFavourite`、`loadLikeState => data.isLiked` 补回旧页缺的账号态显示。

### 68.3 与 JM 的关键差异

- **收藏**：picacg 单一 toggle（`favouriteOrUnfavouriteComic`），无收藏夹弹窗（JM 有多夹）。
- **点赞**：picacg `likeOrUnlikeComic` 是 **toggle**（已赞可取消），JM `likeComic` 单向。两者均用基类 `currentFavorite`/`currentLiked` 取反，**不读 `data` 快照**（否则连点两次错）。
- **图片头**：picacg `imageHeaders => null`（JM 需 `getJmImgHeaders()`）。
- **浏览量**：picacg API 不提供，统计行只剩评论数。
- **账号态省心**：picacg `get()/post()` 在 `token==""` 时短路返「未登录」错误（非脏数据 200），无 §67.3 的 jm 游客 200 静默降级窗口，**无需游客降级处理**，基类 error 态兜底即可。

### 68.4 评论 + 回复全链路（picacg 回复可用，区别于 §67.7 的 jm 回复失败）

- **评论页** `PicacgCommentsPageV2`：列表 + 分页（subData=总页数）+ 发送框 + `_CommentCard`（StatefulWidget，点赞乐观更新带失败回滚）。
- **回复页** `PicacgReplyPage`（新建）：顶部固定原评论 + `getReply` 分页回复列表 + 发送框。评论页回复入口（气泡+数字）可点跳转。
- **网络层**：`sendComment(comicId, content)` 走 POST `/comics/$id/comments`（漫画 ID）；`sendReply(commentId, content)` 走 POST `/comments/$commentId`（评论 ID）——**两条不同路径**，照搬原项目 `comment(id, text, isReply)` 的 isReply 分支。
- **picacg 回复是通的**（区别于 §67.7 jm 移动端回复 `cid` 恒 0 失败）——原项目本就支持，照搬即可。
- **计划外修正**：初版回复入口仅 `replyCount > 0` 显示 → 0 回复评论无法被回复；改为恒显入口（对齐原项目整卡可点）。

### 68.5 网络层与模型新增

- `picacg_network.dart` 新增 6 方法：`likeOrUnlikeComic` / `getComments` / `sendComment` / `sendReply` / `likeOrUnlikeComment` / `getReply`。
- `models.dart` 新增 `PicacgComment`（`fromApi` 处理 `_user` 可空 fallback「Unknown」+ 头像 `fileServer/static/path` 拼接 + `isLiked`/`likes` 非 final 供本地切换）。

### 68.6 未做 / 残余

- **未做 onShare**：picacg 分享链接格式未确认，基类 onShare 为 null → 不显示分享图标。
- **真机 UI 未测**：analyze 0 问题，但未 `flutter run` 点测；回复全链路、评论发送/点赞、头像加载、章节网格、推荐跳转待上机确认。
- **旧死代码**：`online_comic_detail_page.dart` / `picacg_comments_page.dart` 确认无回归后可删。

### 68.7 涉及文件

- `lib/network/picacg_network/picacg_network.dart`（+6 方法）
- `lib/network/picacg_network/models.dart`（+`PicacgComment`）
- `lib/pages/online_comic/picacg_comic_page_v2.dart`（新建）
- `lib/pages/online_comic/picacg_comments_page_v2.dart`（新建）
- `lib/pages/online_comic/picacg_reply_page.dart`（新建）
- `lib/comic_source/built_in/picacg.dart`（comicPageBuilder 切 V2）
- `lib/pages/local_comic_detail_page.dart`（入口切 V2 + 清 2 废 import）

### 68.8 待真机复核

- [ ] 进 picacg 详情页：封面信息/图标行/大胶囊/标签区/creator/简介/章节网格/推荐区显示正常
- [ ] 收藏 toggle：点击图标空心↔实心切换；已收藏漫画进页面即高亮
- [ ] 点赞 toggle：点击切换，可重复点取消赞
- [ ] 标签长按弹「复制/搜索」
- [ ] 评论页：列表（头像+昵称+Lv+日期+内容+点赞）、点赞切换、发送成功刷新、滚动分页
- [ ] 回复：点回复入口进回复页、回复列表加载、发回复成功、0 回复评论也能进去发

---

## 69. 主线第三轮：ehentai/exhentai 网络层与数据模型（已完成）

> 承接第三轮主线（`Z-plan/新需求-主线-第三轮/`），本节起为 ehentai/nhentai 全链路接入。

### 69.1 产出

新建 `lib/network/eh_network/` 目录，含4个文件：
- `eh_models.dart`：`Comment` / `Gallery`（18字段含可变 auth map，`toJson`/`fromJson` 往返，**已修 namespace 分桶 bug**）/ `EhGalleryBrief extends BaseComic`（id=link、cover=coverPath、subTitle=uploader）/ `Galleries`（galleries+next）/ `ArchiveDownloadInfo`。
- `eh_main_network.dart`：`EhNetwork` 单例，含双域名 getter / 独立 `CookieJarSql('eh_cookies.db')` / `getCookies` / `cookiesStr` / `request`（4失效分支）/ `apiRequest` / `post` / `getGalleries`（四排版）/ `getGalleryInfo` / `getComments` / `getReaderLink` / `getEhImageUrl`（showKey/mpvKey 状态机+nl 换节点重试最多4次+画廊级加锁串行+ehgt 3并发限流）/ `getImageLinkWithNL` / `search` / `rateGallery` / `favorite` / `unfavorite` / `comment` / `voteComment` / 归档三方法。新增 `ehUA` 静态 getter（取 `appdata.implicitData[3]`，回退 const webUA，全链路 UA 单一来源）。`getUserName()` 请求 forums.e-hentai.org 解析真实用户名。
- `get_gallery_id.dart`：正则 `/g/(\d+)/([a-z0-9]+)/` → `'画廊号-token'`（无 'eh' 前缀）。
- `js.dart`：`getVariablesFromJsCode`（从上游 tools/js.dart 移植）。
- `pubspec.yaml` 新增 `html: ^0.15.4`（PicaKeep 原本无 HTML 解析库）。

### 69.2 Gallery.fromJson namespace 桶 bug（已修复）

原项目第138行：`tags["key"] = ...`（字面量 "key" 而非循环变量），导致所有 namespace 的标签塌进同一个桶。修复为用循环变量正确分桶。真实验证：tags 桶为 `[group, artist, female]` 共3桶（符合预期）。

### 69.3 唯一标识公共契约

- `id` = `gallery.link`（画廊完整 URL）
- `downloadId` = `getGalleryId(link)` 无前缀（`'123-abc'` 格式，与 `DownloadedGallery.id`、`history.dart._extractEhGalleryId` 一致）
- 详情页 tag = `'ehentai_$link'`
- sourceKey/source/key = `'ehentai'` / `'E-Hentai'` / `'ehentai'`

### 69.4 搜索结果页封面 header 改造（同期完成）

- `comic_source.dart` 新增 `typedef ImageHeadersBuilder = Map<String, String>? Function(BaseComic);`，`ComicSource` 新增可选字段 `imageHeadersBuilder`。
- `online_search_result_page.dart` 新增 `_coverProvider(BaseComic)` 三分支：钩子null → `NetworkImage`；钩子返回null/空 → `NetworkImage`；钩子返回非空 header → `StreamImageProvider.withProgress(OnlineImageManager.getImage(url, headers))`。
- picacg/jm 的 `imageHeadersBuilder` 保持 `null`，行为零改动。ehentai 在源注册收口时填入三件套 header。

---

## 70. 主线第三轮：ehentai 账号登录 Webview 抓 Cookie（已完成）

### 70.1 产出

- `pubspec.yaml`：`flutter_inappwebview`（git fork）+ `desktop_webview_window`（git fork）；`web_socket_channel` ^2→^3；`shelf_web_socket` ^2→^3。
- `lib/server/server_app_routing.dart`：`webSocketHandler` 回调签名适配 shelf_web_socket 3.x。
- `lib/pages/online_comic/webview.dart`（新建）：移植上游 `AppWebview`（mobile/inappwebview）+ `DesktopWebview`（desktop）+ `WebviewExtension`。
- `lib/pages/online_comic/eh_login_page.dart`（新建）：手填4字段表单 + 粘贴解析（`parseCookieText` 纯函数）+ "在Webview中登录"（mobile/desktop双路）+ `loginWithCookies`（清旧/双域双写/validateCookies/写token+name/saveData）。
- `lib/comic_source/built_in/ehentai.dart`（最小空壳）：`key/name/account.onLogin`+logout+infoItems，onLogin 跳 EhLoginPage，logout 清两域 cookie + token。
- `comic_source.dart`：`AccountConfig` 加可选字段 `onLogin: Future<void> Function(BuildContext)?`；`builtIn` getter 加入 ehentai。
- `.gitignore` 加 `/nuget.exe` + `/windows/nuget.exe`。

### 70.2 关键约束

- **cookieJar 必须独立**：用 `CookieJarSql('${App.dataPath}/comic_source/eh_cookies.db')`，禁用全局单例（多源串 cookie）。
- **双域名双写**：同一份身份 cookie 必须分别以 `.e-hentai.org` 和 `.exhentai.org` 两 domain 各写一遍。
- **isLoggedIn 只认 data['token'] 非空**：登录成功后写 `data['token']='ok'` 占位。
- **nuget.exe 路径**：DEPS 脚本向上5层落在 `windows/` 目录，必须放 `windows/nuget.exe`，放项目根会造成构建失败（踩过两次）。

---

## 71. 主线第三轮：ehentai 阅读链路 EhReadingData 与图片解密（部分完成，真机未测）

### 71.1 产出

- `eh_main_network.dart` 新增 `getEhImageUrl(Gallery, int page) → Future<(String imageUrl, String? nl)>`：showKey/mpvKey 双路状态机，剥离字节下载只做"解密直链+试探性GET验证可用性+nl换节点重试最多4次"；画廊级 showKey 加锁串行（`_acquireShowKey`，以 `gallery.link` 为键复用同一 future，`whenComplete` 释放锁）；ehgt 限流：`acquireEhgtSlot`/`releaseEhgtSlot`（限 ehgt.org 3并发）。
- `lib/pages/online_comic/eh_reading_data.dart`（新建，`part of pica_reader`）：`class EhReadingData extends ReadingData`，`downloadId` = `getGalleryId(link)` 无前缀；`hasEp=false`；`loadEpNetwork` 返回 `List.filled(int.parse(maxPage), '')`；`loadImageNetwork` 调 `getEhImageUrl(gallery, page+1)` 拿直链 → `OnlineImageManager.getImage` 带 header 三件套 yield。
- `comic_reading_page.dart`：第62行加 `part '../online_comic/eh_reading_data.dart';`，library 头补 3 个 import。

### 71.2 未完成

- 真实联网逐页出图/MPV 画廊/nl 重试/限流的运行期实测（无账号无模拟器）。
- 上游 `settings[29]` 原图开关分支跳过（PicaKeep settings 索引语义不同，避免越界误用）。

---

## 72. 主线第三轮：ehentai 详情页 V2 + 评论页（已完成，真机未测）

### 72.1 产出

- `lib/pages/online_comic/eh_comic_page_v2.dart`（新建）：`EhentaiComicPageV2 extends BaseOnlineComicPage<Gallery>`。
  - `loadData`：调 `EhNetwork().getGalleryInfo(link)`；响应含 'Content Warning' 标记时弹 `AlertDialog` 二次确认，确认后带绕过参数重试。
  - `extractTitle`：含 `settings[78]=='1'` 日文原标题开关分支。
  - `extractCover`：`data.coverPath.replaceFirst('s.exhentai.org', 'ehgt.org')`。
  - `extractTags`：直接喂 `data.tags`（`Map<namespace, List<value>>`）。
  - `extractEpisodes` / `extractViews` / `extractLikes` / `extractRecommendation` / `extractDescription`：全部 null（无章节/无浏览量/点赞用星级走自定义区块/无推荐/简介多为空）。
  - `imageHeaders`：三件套（Cookie + `EhNetwork.ehUA` + Referer）。
  - `onTagTap`：还原 namespace 前缀，uploader 特判，含空格加英文双引号。
  - `onFavorite`：`DraggableScrollableSheet` 双层面板（平台收藏夹0-9 + 本地 LocalFavoritesManager）。
  - `onLike`：`null`（点赞语义由星级承载）。
  - `onDownload`：TODO 07 SnackBar 占位（入口已存在）。
  - `buildCustomSection`：`_EhRatingSection`（StatefulWidget，5星交互 + `rateGallery`，乐观更新）。
- `lib/pages/online_comic/eh_comments_page.dart`（新建）：`EhCommentsPage`，顶层 `showEhComments(context, link, uploader, auth)` 入口；评论列表/发送（延迟重载）/投票（乐观更新+失败回滚，点赞/点踩）；`_EhCommentCard` + `_VoteButton`。

### 72.2 关键设计点

- `onTagTap` 中 `category` 参数直接就是 namespace（由 `OnlineComicTagsSection` 传入），无需自行反查 `data.tags`。
- `use_build_context_synchronously` 无法用行级 ignore 压制（lint 按参数实际位置报告），用 `ignore_for_file:` 解决。
- 预存 analyze 问题90个来自其他计划，06本计划新文件0 error。

---

## 73. 主线第三轮：ehentai 完整在线下载器（部分完成，归档未做）

### 73.1 产出

- `download_model.dart`：`DownloadedGallery` 新增 `int pageCount` 字段（真实页数），`toJson`/`fromJson` 向后兼容（旧数据缺键回退1）；`createReadingPage` 改为 `hasEp:false`（对齐无章节契约）；`parseDownloadedItemRecordData` **前置精准拦截**：id 匹配 `^\d+-[a-z0-9]+$` 且 data 含 galleryTitle/gallery 键 → `DownloadedGallery.fromJson`（修复此前被 `contains('-')` 误判为 `CustomDownloadedItem` 的 bug）。
- `online_download_manager.dart`：新增 `OnlineDownloadTask.ehentai`（持 Gallery，sourceKey='ehentai'）、`enqueueEhentai(Gallery)`、`_runEhentaiTask`（串行逐页，封面 s.exhentai.org→ehgt.org，带鉴权三件套 header，逐页 `getEhImageUrl(gallery, page+1)` 取直链→`_downloadFile` 写盘）；`_downloadFile`/`_downloadFileOnce` 加可选 headers 参数；持久化分支（_saveQueue/loadQueue ehentai 分支）；`loadCompletedDownloads` 加 `DownloadedGallery` 分支；新建 `OnlineDownloadedGallery extends DownloadedGallery`（持 rootPath/localCoverPath，`createReadingPage` 用 `OnlineLocalReadingData(isEhentai:true, chapters:{})`）；`OnlineLocalReadingData` 加 `isEhentai` 标志，三源参数化（isJm/isEhentai/默认picacg）。

### 73.2 未完成

- **归档下载（Original/Resample，type=0/1/2）**：zip整包下载+解压是独立子工程，本轮刻意未覆盖（验收10）。
- 全链路需联网+账号的运行期验收（入队/进度/逐页落盘/离线阅读/历史/收藏/持久化重启）未实测。

### 73.3 parseDownloadedItemRecordData 拦截顺序（重要）

ehentai id 格式 `123-abc` 含 `-`，与 picacg 24位hex、jm 数字不同，会被原 `contains('-')→CustomDownloadedItem` 分支提前拦截。精准拦截必须在 `contains('-')` **之前**，同时满足「正则匹配 + 含 galleryTitle/gallery 键」两个条件，避免误拦截其他类型。

---

## 74. 主线第三轮：ehentai 源注册集成收口（已完成，真机未测）

### 74.1 产出

- `lib/comic_source/built_in/ehentai.dart`（完整重写，04空壳基础上扩充）：
  - `_EhGalleryLoader` 游标缓存器（移植上游 `_EhentaiGalleriesLoader`，page==1 重置游标，无 nextPage 时哨兵 subData=cache.length 使列表停止上拉）。
  - `account`：`reLogin`（getCookies + validateCookies，无重登模型，只恢复+校验）；`logout` 清两域 cookie + token + name；`infoItems` 展示真实用户名 + ipb_member_id/ipb_pass_hash/igneous 完整明文。
  - `searchPageData`：5种分类/评分选项，`loadPage` 用 `_EhGalleryLoader`。
  - `favoriteData`：multiFolder=false（单文件夹），`loadComic` 用 `_EhGalleryLoader` 加载全部收藏。
  - `comicPageBuilder`：`(comic) => EhentaiComicPageV2(comic.id)`。
  - `imageHeadersBuilder`：动态读 `EhNetwork().cookiesStr`/`ehBaseUrl`/`ehUA`，不缓存静态值（保证域名切换立即生效）。
- `lib/pages/accounts/accounts_page.dart`：`_openLogin` 优先检查 `account?.onLogin`，await 等登录完成后刷新账号区。

### 74.2 favoriteData 说明

`NetworkFavoriteLoader` 只接 `int page` 无 folder 参数，多文件夹逻辑需额外状态机，本轮单文件夹简化优先落地；后续扩充 `loadFolders` 即可接入多收藏夹。

---

## 75. 主线第三轮：nhentai 源接入（部分完成，真机未测）

### 75.1 产出（代码层完成，analyze + Windows build 通过）

- `lib/network/cloudflare.dart`（新建）：`CloudflareException` + `CloudflareInterceptor` + `passCloudflare`，从原项目 nhentai_network 提取，作为独立工具模块供 nhentai 使用。
- `lib/network/nhentai_network/`（3文件）：`models.dart`（NhentaiComic/NhentaiComicBrief/NhentaiTag/Comment）、`tags.dart`（nhentai 标签类型定义）、`nhentai_main_network.dart`（NhentaiNetwork 单例，搜索/详情/评论/收藏/下载方法）。
- `lib/pages/online_comic/`（4文件新建）：`nhentai_login_page.dart`、`nhentai_comic_page_v2.dart`（`NhentaiComicPageV2 extends BaseOnlineComicPage<NhentaiComic>`）、`nhentai_reading_data.dart`（part of pica_reader）、`nhentai_comments_page.dart`。
- `lib/comic_source/built_in/nhentai.dart`（新建）：nhentai 源注册，含账号/搜索/收藏/详情页/下载入口。
- `lib/foundation/online_download_manager.dart`：新增 `enqueueNhentai`/`_runNhentaiTask`/序列化/`OnlineDownloadedNhentai` 包装类；`OnlineLocalReadingData` 加 `isNhentai` 标志（四源参数化）。
- 改动：`comic_source.dart`（builtIn 加 nhentai）、`comic_reading_page.dart`（part 追加）、`network_favorites_page.dart`（bug修复）、`local_comic_detail_page.dart`（nhentai 本地回链按钮）。

### 75.2 未完成

- 真机运行时验收（搜索/封面/登录/收藏/阅读/下载）。
- Android + Linux build（仅做了 Windows debug build）。
- ehentai 本地回链（local_comic_detail_page 数据缺口，nhentai 同理待确认）。
- nhentai 账号态游客降级根治（仅登记限制）。

---

## 76. 主线第三轮：Android 构建失败修复（已完成）

### 76.1 问题与修复

**阻断构建根因**：`comic_source.dart` 的 `onLogin` 字段原为 `void Function(BuildContext)?`，`accounts_page.dart:137` 把其返回值赋给 `Object? result` 并用 `is Future` 消费。`// ignore: use_of_void_result` 只压 analyzer lint，CFE 编译器禁止消费 void 表达式，故 analyze 0 error 但 build 失败。

**修复**：
1. `comic_source.dart`：`onLogin` 字段类型 → `Future<void> Function(BuildContext)?`。
2. `accounts_page.dart:129-141`：消费端改为直接 `await customLogin(context)`，去掉 `Object? result`/`is Future`/无效 ignore 注释。
3. **UA 一致性**：`eh_main_network.dart` 新增 `EhNetwork.ehUA` 静态 getter（取 `appdata.implicitData[3]`，回退 `webUA`）；全链路8处 UA 统一改用（网络层5 + 详情页 + 阅读器 + 下载器 + 源注册封面）；ehentai.dart 因 webUA 不再直接引用而 unused 的 `import base.dart` 已删。

**验证**：两轮 `flutter build apk --debug` 均 BUILD SUCCESSFUL（EXIT=0）。

### 76.2 铁律

`flutter analyze` 通过 ≠ 能构建。涉及 void/类型协变/part 库的改动，必须以 `flutter build` 实跑为准，CFE 比 analyzer 严格。

---

## 77. 主线第三轮：WebView 代理 CF 过盾 + 账号信息显示（已完成，真机验证通过）

### 77.1 WebView 代理问题根因与修复

- **根因**：Android 系统 WebView 不读 HTTP 代理环境变量，只听 `ProxyController.setProxyOverride()`；桌面端 DesktopWebview 已设代理，移动端 AppWebview 遗漏（移植不对称）；CF Turnstile 还检测 `navigator.webdriver` 指纹。
- **修复**（6处）：
  - `base.dart:40` `settings[8]` 默认值 `"0"` → `""`（语义：代理地址 host:port，空=跟随系统）。
  - 新建 `lib/tools/system_proxy.dart`：`getSystemProxy()` via MethodChannel 读 Android 系统代理（`http.proxyHost/Port`）。
  - `android/.../MainActivity.kt`：新增 `SYSTEM_PROXY_CHANNEL` + `readSystemHttpProxy()` helper。
  - `lib/pages/online_comic/webview.dart`：`onWebViewCreated` 串行执行（先 await `_resolveProxy()` → `ProxyController.setProxyOverride()` → `loadUrl()`，消除竞态）；加 `initialSettings`（现代 Chrome UA/JS/cookie/domStorage显式启用）；加 `initialUserScripts`（`AT_DOCUMENT_START` 覆盖 `navigator.webdriver=undefined`，补 `window.chrome`/`navigator.plugins`）。
  - 新建 `lib/pages/settings/network_settings.dart`：代理地址配置 UI（host:port 正则校验 + AlertDialog + 可清除 + 持久化）。
  - `settings_page.dart`：插入"网络"分组（index 5，APP与下载之间），后续组编号顺延；加 part 声明。

### 77.2 账号信息显示修复（3处）

- `eh_main_network.dart` 新增 `getUserName() → String?`：请求 `forums.e-hentai.org`，解析 `div#userlinks > p.home > b > a` 得真实用户名（解耦，网络层只返回数据，写入由调用方负责）。
- `eh_login_page.dart`：登录成功后调 `getUserName()`，写真实用户名；拿不到则回退 `'E-Hentai'` 占位。
- `ehentai.dart` `infoItems`：先 `getCookies(true)` 刷新，展示用户名 + ipb_member_id / ipb_pass_hash / igneous 完整明文。

### 77.3 验收（真机 Android 14 + WebView 145）

✅ 设代理后 ehentai 登录页 CF 验证出现"确认"按钮（不转圈）→ 登录成功。✅ 账号页显示真实用户名 + 完整 cookie。

### 77.4 WebView 版本强依赖

Android 10 + 旧 WebView（<143 左右）CF 仍转圈，属 WebView 引擎 JS 兼容性上限，代码层无解。需手动升级系统 WebView（Android 14 建议 ≥143）。`settings[8]` 存量用户残留 `"0"` 等价直连，行为兼容。

---

## 78. 主线第三轮：nhentai 登录秒退与按钮卡转圈修复（已完成，真机待验）

### 78.1 根因

只改 `lib/pages/online_comic/nhentai_login_page.dart`，两个联动 bug 同源：

**Bug 1（秒退）**：移动端 `onTitleChange` 回调里，`_seenLoginPage = true` 与 `_seenLoginPage && hasToken` 在**同一次回调内连续执行**——状态机形同虚设。`hasToken` 读的是 webview 全局 `CookieManager`，只要此前登录过一次就残留 `access_token`/`refresh_token`，于是：进登录页 → title 含 Login → 置 true → 残留 token 使条件成立 → `_done=true` + `App.globalBack()` → **秒退**。

**Bug 2（按钮卡转圈 + 登录态未建立）**：快捷路径已把 `_done=true`，`_verifyAfterClose` 开头 `if (_done) return` 直接跳过，导致 `_logging` 不复位、`logged` 未置 true、token 未写、`Navigator.pop(true)` 未执行。

### 78.2 修复（三处）

| # | 改动 |
|---|------|
| C1 | `_loginWithWebview` 进 webview 前新增 `_clearWebviewTokens()`：删除 webview CookieManager 里 `.nhentai.net` 域的 `access_token`/`refresh_token`，异常只记日志不阻断；每次点登录前重置 `_seenLoginPage = false`。 |
| C2 | 重写移动端 `onTitleChange` 快捷关闭判定：title 含 Login/Register → 只记 `_seenLoginPage=true` 提前 return；已离开登录页 + jar 有 token 才调 `App.globalBack()`；**去掉快捷路径的 `_done=true`**。 |
| C3 | `_verifyAfterClose` 无需改动，C2 去掉 `_done=true` 后它自然成为唯一收尾点，自动关窗/手动返回两路均可走到。 |

### 78.3 关键铁律

- **`_done` 只由 `_verifyAfterClose` 成功分支置位**，快捷路径只关窗，不置 `_done`。
- **成功判定必须「离开登录页」**：`title` 不含 Login/Register 才算登录完成，在登录页当场判 token 会触发秒退。
- C1 的 token 清理针对 **webview CookieManager**，不动 `NhentaiNetwork().cookieJar`（那是登录态权威源）。

### 78.4 残余风险

- `CookieManager.deleteCookie` 对 httpOnly cookie 的生效性取决于平台；C2 的「离开登录页」判据是主防线，C1 是加固。
- 真机验收（登录不秒退、成功登录、手动返回复位）待用户上机。

---

## 79. 主线第四轮：搜索体验增强（标签建议+NH-ID搜索，已完成，真机迭代验收通过）

### 79.1 功能概述

在在线搜索补全三项能力：① 搜索框实时标签/作者建议（eh/nh 源）；② NH ID 直跳单本详情页；③ EH/NH 详情页标签显示中文译名。JM idMatcher 作为附带完成。EH customOptionsBuilder（P1）未执行，另立计划。

### 79.2 架构变更（新增字段）

| 位置 | 变更 |
|---|---|
| `ComicSource`（`comic_source.dart`） | 新增 `RegExp? idMatcher` 字段 |
| `SearchPageData`（`comic_source.dart`） | 新增 `bool enableTagsSuggestions`（默认 false） |
| `nhentai.dart` | `idMatcher: RegExp(r'^(\d+|nh\d+|nhentai\d+)$')`，`enableTagsSuggestions: true` |
| `ehentai.dart` | `enableTagsSuggestions: true` |
| `jm.dart` | `idMatcher: RegExp(r'^(?:jm)?\d+$')` |
| `tags_translation.dart` | 新增 `tagTranslateCategory`/`tagTranslateWithNs`/`_tagCategoryZh`；修复解析器（Map-of-Map-of-String） |

### 79.3 搜索页（`online_search_page.dart`）

- `Column(固定_SearchBarSection + Expanded内容区)`，不整体替换 body（防止搜索框消失）。
- `_onKeywordChanged` → `TagsTranslation.findSuggestions` → `_SuggestionListView`；末尾空格/清空后建议消失。
- 点击建议补全写回：EH 写 `namespace:key`，NH/其他写 plain key，末尾加空格。
- `_tagsReady` 守卫防数据未就绪时误触发建议。
- idMatcher：遍历 `ComicSource.sources` 的 `idMatcher`，输入纯数字/nh前缀时同时出现多源跳转建议（JM+NH 并列，预期行为）。

### 79.4 标签翻译（`tags_translation.dart`）

- `assets/tags.json` 实际格式：`Map<namespace, Map<key, cn_string>>`（Map-of-Map-of-String），不是 List。
- `tagTranslateWithNs(key, ns)`：key 强制 `toLowerCase()`；nhentai fieldName 首字母大写复数 → `plural2Ns` 映射（"Parodies"→"parody" 等）→ 未命中走全表兜底（O(N)，处理 "Tags" 混合类别）。
- `loadTagTranslations()` 移至 `main.dart` `_initializeOnlineFoundation()` 非阻塞 `unawaited` 预热，不再页面级懒加载。

### 79.5 详情页标签翻译

- `OnlineComicTagsSection` 增加 `enableTagTranslation` 参数（默认 false）。
- `EhentaiComicPageV2`/`NhentaiComicPageV2` override 为 `true`。
- JM/picacg 默认 false，零影响。

### 79.6 关键铁律

- `tags.json` 格式是 Map-of-Map-of-String，不是 List，写计划时不能凭上游假设，执行前必须实读 assets。
- nhentai HTML 解析出的 fieldName 是首字母大写复数，不等于 ehentai namespace 格式，翻译查询前必须先 normalize。
- `idMatcher` 独立于 `enableTagsSuggestions`，两者不耦合。
- body 不整体替换：Column（固定搜索框 + 切换内容区），不用 Overlay/DropdownMenu。
- `main.dart` 加调用时必须同步加 import，否则 analyze 报 `undefined_function`。

### 79.7 残余风险

- §3 EH customOptionsBuilder（bitmask category 过滤，P1）未完成，另立计划。
- 真机建议列表性能（低端机键盘弹出+200 条 ListTile 并发）未专项压测。
- NH tags namespace 全为标准小写 ehentai 格式的假设待真机确认。

### 79.8 涉及文件

- `lib/comic_source/comic_source.dart`（SearchPageData + ComicSource 字段）
- `lib/comic_source/built_in/jm.dart`、`nhentai.dart`、`ehentai.dart`（各自 idMatcher/enableTagsSuggestions）
- `lib/pages/online_search/online_search_page.dart`（搜索页重构）
- `lib/tools/tags_translation.dart`（解析修复 + 翻译函数）
- `lib/main.dart`（`loadTagTranslations()` 预热）
- `lib/pages/online_comic/online_comic_page_components.dart`（`enableTagTranslation` 参数）
- `lib/pages/online_comic/base_online_comic_page.dart`、`eh_comic_page_v2.dart`、`nhentai_comic_page_v2.dart`
- `assets/tags.json`、`assets/tags_tw.json`（补录 `bathing room` 等缺项）
- `lib/pages/download_page.dart`、`lib/pages/favorites/local_favorites.dart`、`lib/pages/local_comic_detail_page.dart`（旧类型调用适配）

---

## 80. 主线第三轮补录：nhentai 收藏 CSRF-token 修复（已完成，真机待验）

### 80.1 根因

`getComicInfo` 里从 `<script>` 标签提取 CSRF token 的代码在 try/catch 里**静默忽略失败**，token 始终为空字符串，导致收藏弹出「缺少 CSRF token」toast。Django 同时以 `csrftoken` cookie 下发 token（非 httpOnly），但原代码无 fallback。

### 80.2 修复

| # | 改动 |
|---|------|
| D1 | `NhentaiNetwork` 类新增 `getCsrfToken()`：从 cookieJar 读 `csrftoken` cookie，找不到返回 `''` |
| D2 | `getComicInfo` script 解析 try/catch 后加 `if (token.isEmpty) token = getCsrfToken();` |

### 80.3 关键铁律

- `getCsrfToken()` 返回空字符串而非 null，调用方用 `isEmpty` 判断即可。
- script 解析 CSRF token 不可靠（nhentai 随时可改 script 格式），cookieJar 里的 `csrftoken` cookie 是稳定来源。
- 用户若未登录且从未访问主页，cookie jar 可能没有该 cookie，此时 token 仍为空——收藏本就需要登录，属预期行为。

### 80.4 涉及文件

- `lib/network/nhentai_network/nhentai_main_network.dart`

---

## 81. 主线第三轮补录：nhentai 详情页 ID 显示 + 页数/时间合并行（已完成，真机待验）

### 81.1 目标

1. 信息区顶部加 ID 行（置顶）。
2. 页数与时间两行合并为同一 Wrap 行。

### 81.2 实现

| # | 文件 | 改动 |
|---|------|------|
| E1 | `base_online_comic_page.dart` | 新增 `buildTagsSectionOverride` nullable 钩子（默认 null = 走现有逻辑） |
| E2 | `base_online_comic_page.dart` | build 里改为 `buildTagsSectionOverride(...) ?? OnlineComicTagsSection(...)` |
| E3 | `nhentai_comic_page_v2.dart` | 重写钩子：ID 置顶；普通标签正常渲染（排除 Pages/时间）；末行合并页数+时间 |
| E4 | `nhentai_comic_page_v2.dart` | 新增 `tags_translation.dart` import |

### 81.3 关键铁律

- `buildTagsSectionOverride` 返回 null = 走默认渲染；返回非 null = 完全替代 `OnlineComicTagsSection`。
- 其他源（ehentai/jm/picacg）不重写该钩子，零影响。
- 时间 key 匹配用 `"时间"` 硬编码即可，加 `|| k == 'Time'` 兜底英文环境。

### 81.4 涉及文件

- `lib/pages/online_comic/base_online_comic_page.dart`
- `lib/pages/online_comic/nhentai_comic_page_v2.dart`

---

## 82. 主线第三轮补录：nhentai 评论页 v2 API 迁移 + 发评论可行性诊断

### 82.1 评论页 v2 迁移（已完成，真机待验）

**根因（日志实证）**：旧评论 endpoint `/api/gallery/{id}/comments` 被 nhentai 服务端主动废弃，403 响应体直接给出迁移指引「Use new API .../api/v2/docs」。上游 PicaComic 仍使用旧路径，无参考价值。

| # | 改动 |
|---|------|
| F1 | 路径迁移：`/api/gallery/$id/comments` → `/api/v2/galleries/$id/comments` |
| F2 | 响应解包：从 `json["result"]` 取数组，兼容旧裸数组（`json is Map ? json["result"] : json`） |
| F3 | avatar 双路径兼容：`http` 开头直接用，相对路径才拼前缀 `https://i3.nhentai.net/` |
| F4 | 全字段空安全（`?.toString() ?? ""`，`(post_date as num?)?.toInt() ?? 0`），防单条缺字段崩整页 |

### 82.2 发评论可行性诊断（结论：不可行，维持只读）

三道门槛，第一道是死结：

1. **User Token 死结**：v2 POST 评论只认 `Authorization: User YOUR_TOKEN`（nhentai 自有服务专用），不接受 API Key；App 走 webview cookie 登录，拿不到 User Token。
2. **PoW 工作量证明**：`GET /api/v2/pow?action=comment` 拿 challenge；文档称 difficulty=0 时传空串即可通过（可绕）。
3. **Captcha 人机验证**：需 Turnstile 真人交互，体验极差；另有 5 条/15 分钟限流。

与 §64 JM 移动端发评论死结同类，评论区维持只读。

### 82.3 关键铁律

- **nhentai API 迁移不能照搬上游 PicaComic**——上游滞后，仍用废弃路径 + sessionid，必须以 nhentai v2 官方文档为准。
- v2 评论响应必须先取 `result` 再遍历；直接 `for (c in json)` 对 Map 迭代会抛异常 → catch 到「Failed to Parse Data」→ 评论页空。
- nhentai v2 API 文档只能靠用户在已登录浏览器手动复制，AI 侧 WebSearch/WebFetch/自动浏览器三条路径全被 CF/内容策略拦截。

### 82.4 涉及文件

- `lib/network/nhentai_network/nhentai_main_network.dart`（`getComments` 方法）
- `Z-plan/Dco/docs/nhentai_评论api.md`（参考文档）

---

## 83. 主线第四轮：收藏页网络收藏夹接入（部分完成）

### 83.1 已完成

- `NetworkFavoriteAction` typedef 加 `bool isAdding` 第二参数（原硬编码 `add:true` 导致无法取消）。
- 折叠抽屉顶部插入网络区格子（`_NetworkSourceTile`）+ `_selectedNetworkSource` 状态，点击展开对应源的收藏夹。
- `NetworkFavoriteWidget` 内嵌化：去掉独立 Scaffold，改为 Grid + 长按菜单。
- JM `addOrDelFavorite`：修复硬编码 `add:true` → `add:isAdding`。
- EH `addOrDelFavorite`：加收藏用 gid+token，取消用 `unfavorite2`。
- NH 确认维持 `favoriteData: null`。

### 83.2 未完成

- JM/EH 多文件夹二级 UI 未做（下一计划补足，见 §84）。

### 83.3 涉及文件

- `lib/comic_source/favorite_data.dart`
- `lib/pages/favorites/main_favorites_page.dart`
- `lib/pages/favorites/network_favorites_page.dart`
- `lib/comic_source/built_in/jm.dart`、`ehentai.dart`

---

## 84. 主线第四轮：收藏页全链路修复（折叠+NH308+JM/EH多文件夹，已完成）

### 84.1 四项修复

| # | 问题 | 修法 |
|---|------|------|
| F1 | 折叠无法关闭 | `_toggleFolders` guard 由 `_folders.isEmpty` 改为 `!_hasAnyItems`（新增 getter），展开后再点可收起 |
| F2 | NH 308 重定向 → 登录后直接退出 | `validateStatus` 接受 308；`get()` redirect 改为直接拼 `origin+location`（修 `Uri.replace(path:)` bug）；`getFavorites` 补 lazy init；登录页 `_verifyAfterClose` 写 `source.data['token']='ok'`+`saveData()` |
| F3 | JM 多文件夹 | `multiFolder:true`；`loadFolders` 返回 `Map<String,String>`；`NetworkFavoriteWidget` 支持二级导航（先选文件夹再加收藏） |
| F4 | EH 多文件夹 | `loadFolders` 请求 `favorites.php` 刷新 folderNames 返回 Map |

### 84.2 关键铁律

- `_hasAnyItems` getter 必须同时检查本地和网络收藏夹；只看 `_folders.isEmpty` 在网络源有数据时仍为空导致无法收起。
- NH 308 redirect 的 `location` 是相对路径，`Uri.replace(path: location)` 会丢失 query string；必须直接拼 `origin + location`。
- `getFavorites` 需 lazy init cookieJar，否则未登录时抛 null。

### 84.3 涉及文件

- `lib/pages/favorites/main_favorites_page.dart`（F1/F3/F4）
- `lib/pages/favorites/network_favorites_page.dart`（F3 二级导航）
- `lib/comic_source/favorite_data.dart`（F3/F4 multiFolder）
- `lib/network/nhentai_network/nhentai_main_network.dart`（F2 308/validateStatus/getFavorites）
- `lib/comic_source/built_in/picacg.dart`、`jm.dart`、`ehentai.dart`、`nhentai.dart`
- `lib/network/jm_network/jm_network.dart`（loadFolders）

---

## 85. 主线第四轮：nhentai 收藏功能修复（部分完成，收藏页真机通过）

### 85.1 实际完成

- `getFavorites` 重写为 v2 API：`GET /api/v2/galleries/favorited`，新增 `_parseV2GalleryItem` 统一解析。
- `favoriteComic`/`unfavoriteComic` 重写为 v2 优先 + 旧版 CSRF fallback。
- 新增 `_cdnServer`+`_fetchCdnServer()`：启动时从 `/api/v2/image-server` 动态获取 CDN 域名（原硬编码 `https://t.nhentai.net` 偶发裂图）。
- 封面 URL：`thumbnail` 字段无前导 `/`，补 `/` 后拼 CDN 域名修复裂图。
- 语言检测：改为硬判断 tag ID（`12227`=English / `6346`=日語 / `29963`=中文），不再依赖 tag name 字符串（原方案在 v2 response 里 tag name 缺失时总显示 Unknown）。

### 85.2 未完成

- 详情页收藏按钮真机验证 pending（代码已改，未跑真机确认点击后服务端入库）。
- `isLoggedIn` 持久化悬留（超出原计划范围，留后续）。

### 85.3 涉及文件

- `lib/network/nhentai_network/nhentai_main_network.dart`（`_cdnServer`/`_fetchCdnServer`/`_parseV2GalleryItem`/`getFavorites`/`favoriteComic`/`unfavoriteComic`）

---

## 86. 主线第四轮：nhentai 搜索切 v2 API（已完成，真机通过）

### 86.1 改动

`search(keyword, page, sort)` 从 HTML 页面抓取改为调用 `GET /api/v2/search?query=...&page=...&sort=...`，复用 `_parseV2GalleryItem` 和 `_fetchCdnServer`。顺带清理第113、276行两处 `res.data!` 冗余非空断言 warning。

### 86.2 效果

搜索结果的标签、语言字段现在由 v2 API 直接返回，不再依赖 HTML 解析，不再出现标签/语言缺失。

### 86.3 关键铁律

- nhentai 搜索结果与收藏页统一走 `_parseV2GalleryItem`，CDN 域名统一走 `_cdnServer`；禁止在两处分别写不同的解析逻辑。

### 86.4 涉及文件

- `lib/network/nhentai_network/nhentai_main_network.dart`（`search` 方法）

---

## 87. 主线第四轮：ehentai 下载弹窗+归档下载（部分完成，真机待验）

### 87.1 已完成

- `eh_comic_page_v2.dart`：删除旧 SnackBar 占位，替换为完整下载选项弹窗：普通下载按钮 + 归档 `ExpansionTile`（展开选归档类型）+ cancelUnlock 长按入口。
- `enqueueEhentai(id, galleryData, [int type=0])`：加可选 `type` 参数区分普通(0)/归档(1+)。
- `_runEhentaiTask` 加归档分支：`getArchiveDownloadLink` 取下载 URL → `dio.download` 流式下载 zip → `ZipDecoder`（`archive` 包，已有依赖）解压 → 按序重命名 → 写 `DownloadedGallery`。
- `_saveQueue`/`loadQueue` ehentai 分支加 `downloadType` 序列化持久化，App 重启后队列不丢失类型。

### 87.2 未完成

- 真机验证：归档 zip 下载 → 解压 → 离线阅读全链路待跑。
- `cancelAndReloadArchiveInfo` 返回类型未确认（计划标注了不确定）。

### 87.3 涉及文件

- `lib/pages/online_comic/eh_comic_page_v2.dart`（下载弹窗 UI）
- `lib/foundation/online_download_manager.dart`（`enqueueEhentai`/`_runEhentaiTask`/`_saveQueue`/`loadQueue`）

---

## 88. 主线第四轮：nhentai 收藏页 401 认证重设计（已完成，真机通过）

### 88.1 根因

nhentai v2 收藏 API 要求 `Authorization: User {access_token}` header，但 access_token 有效期短，过期后返回 401。原实现无刷新机制，登录后一段时间收藏页必 401。

### 88.2 修复

- 新增 `_refreshAccessToken()`：`POST /api/v2/auth/refresh`，Body `{"token": refresh_token}`，成功后把新 access_token 写回 cookieJar。
- `getFavorites`：先用 access_token 请求 v2 API；遇 401 自动调 `_refreshAccessToken()` 刷新后重试一次；二次失败才报错。
- 加 `import 'dart:io' show Cookie;`。

### 88.3 关键铁律

- refresh_token 从 cookieJar 读 `refresh_token` cookie；access_token 同理读 `access_token` cookie。
- 刷新后必须把新 token 写回 cookieJar，否则下次请求仍用旧 token。
- 只重试一次；二次 401 说明 refresh_token 也过期或失效，应引导重新登录。

### 88.4 涉及文件

- `lib/network/nhentai_network/nhentai_main_network.dart`（`_refreshAccessToken`/`getFavorites`/`_parseFavoritesV2`）

---

## 89. 主线第四轮：NAS 路径支持完善（根目录 + 挂载路径导航，已完成，Docker 真机通过）

### 89.1 背景

网页后台“选择服务端目录”弹窗在 Linux/Docker 环境下原本只提供 `/vol*/1000` 存储空间入口，用户额外挂载的 `/data/picakeep`、`/cos` 等路径无法从导航中抵达。

### 89.2 改动

- Linux 根标签栏增加“根目录”和“挂载路径”：
  - “根目录”跳转 `/`，列出系统根下顶层目录。
  - “挂载路径”使用虚拟路径 `__mounts__`，进入聚合视图。
  - 原“存储空间 X”继续跳转 `/volX/1000`，不改变既有快捷入口。
- 新增挂载路径虚拟目录：
  - 从 `/proc/mounts` 读取 Docker 用户挂载，过滤 `/proc`、`/sys`、`/dev`、overlay2/merged 等系统噪音。
  - 聚合 `customLibraryRoots` 自定义资源根。
  - 不重复列出已在根标签展示的存储空间。
- 虚拟目录响应补齐 `roots` 字段，保证根标签栏与面包屑导航不丢失。

### 89.3 验收

- `flutter analyze` 0 error。
- Docker 环境网页后台真机测试通过：根列表显示“根目录 + 挂载路径 + 存储空间1/2/3”；点击“挂载路径”能进入聚合列表；面包屑与根标签栏显示正常。

### 89.4 关键铁律

- 虚拟路径必须在 `_normalizeBrowsePath` 前拦截；否则会被当真实目录处理并 404。
- 虚拟目录虽然不是真实目录，但响应结构必须对齐真实目录，尤其要带 `roots` 字段。
- 新增导航能力时不要重写原 `_linuxStorageVolumeRoots` 行为，避免破坏已验证的 `/vol*/1000` 入口。

### 89.5 涉及文件

- `lib/server/server_app_browse.dart`（`_handleAdminBrowse` / `_handleMountsVirtualDirectory` / `_linuxDockerMountRoots` / `_adminBrowseRoots` Linux 分支）

---

## 90. 主线第四轮：工具页补齐原项目功能（在线工具，已完成）

### 90.1 功能概述

工具页补齐原项目常用在线辅助入口，并移除与主导航重复的“图集”工具入口。新增“在线工具”折叠卡与快捷浮动入口弹窗，覆盖：

- EH 订阅
- 图片搜索：搜图bot酱
- 图片搜索：SauceNAO
- 打开链接：识别 EH/NH/JM 链接并跳转详情页
- 禁漫漫画 ID：输入纯数字 ID 跳转 JM 详情页

### 90.2 改动

- `tool_display_config.dart`：
  - 新增在线工具及 5 个子工具 ID。
  - 删除“图集”工具定义，避免与主导航重复。
- `tools.dart`：
  - 新增“在线工具”折叠卡。
  - 搜图入口使用内置 `AppWebview` 打开外部站点。
  - 打开链接弹窗支持 EH/NH/JM 链接解析并跳详情页。
  - JM ID 弹窗校验数字后进入 `JmComicPageV2`。
- `me_page.dart`：
  - 快捷工具按钮不再直接进入工具页，而是弹出底部在线工具列表。
  - 快捷弹窗内行为与工具页保持一致。
- `eh_subscription_page.dart`：
  - 新增 EH 订阅页：读取 `${EhNetwork().ehBaseUrl}/watched`，复用 `EhNetwork.getGalleries` 拉列表，支持刷新、滚动加载更多、未登录跳转登录、封面鉴权三件套与点击进入详情。
  - 订阅管理仍提示在网页端管理。

### 90.3 边界

- 本轮补齐入口、跳转、订阅列表浏览与常用链接解析。
- EH 订阅的新增/编辑/删除管理不在客户端内实现，仍引导到网页端管理。
- Hitomi 链接入口文案保留，但当前源码实际未接 Hitomi 详情页，无法识别时会提示失败。

### 90.4 关键铁律

- 快捷浮动入口和工具页内入口是两种交互：快捷入口应弹底部工具列表，工具页内入口应在当前页展开或进入子功能。
- EH 图片仍需鉴权三件套：`Cookie`、`User-Agent: EhNetwork.ehUA`、`Referer`，否则订阅列表封面可能裂图。
- “打开链接”不能只按字符串拼 ID，必须按域名与 path 结构分流到对应详情页。

### 90.5 涉及文件

- `lib/pages/tool_display_config.dart`
- `lib/pages/tools.dart`
- `lib/pages/me_page.dart`
- `lib/pages/eh_subscription_page.dart`

---

## 91. 主线第五轮：AI 能力层地基（工具注册表 + 四工具，部分完成）

### 91.1 目标与边界

本轮新增纯 Dart、驱动无关的 AI 能力层地基，为后续 App 内嵌 LLM 对话预留 function-calling 工具体系。当前只建设能力层，不接大模型、不接 UI、不接服务端/MCP，也不接入 App 启动流程。

核心目标是让未来“AI 搜漫画 → 候选确认 → 发起下载”“AI 查本地库/收藏/历史”“AI 查询下载状态”可以直接复用同一套工具注册表与工具 schema。

### 91.2 已完成内容

- 新增 `lib/foundation/ai/ai_tool.dart`：定义 `AiTool` 与 `AiToolResult`，提供 `name`、`description`、`parametersSchema`、`execute(args)` 与 `{ok,data?,message?}` JSON 输出形状。
- 新增 `lib/foundation/ai/ai_tool_registry.dart`：`AiToolRegistry` 单例支持注册、批量注册、`toolSchemas()` 和 `dispatch(name,args)`；未知工具名与工具内部异常统一转为 `ok:false`，不向外抛。
- 新增 `lib/foundation/ai/ai_sources.dart`：集中定义 `picacg`、`jm`、`ehentai`、`nhentai` 四源常量与 `normalizeAiSource()`，兼容 `eh/exhentai/e-hentai`、`nh/n-hentai` 简写。
- 新增 `lib/foundation/ai/ai_capabilities.dart`：惰性注册四工具，暴露 `AiCapabilities.registry/ensureRegistered()`；未接 App 启动流程。
- 新增四个工具：
  - `search_online`：按源分派四源搜索，返回 `id/title/author/cover/tags/pageCount?`；不逐条拉详情补字段。
  - `download_comic`：按源 `getComicInfo/getGalleryInfo` 后调用 `OnlineDownloadManager.enqueueXxx`，一次一本直接入队。
  - `search_local`：支持本地库/收藏/历史检索。
  - `get_download_status`：遍历下载队列，返回 `taskId/title/progress/state/message?`。
- 新增 `lib/foundation/local_search_core.dart`，把本地搜索页原私有匹配逻辑抽为可复用函数；`LocalSearchPage` 改为调用同一套匹配核心，避免 AI 工具与 UI 搜索规则分叉。

### 91.3 搜索字段验证结论

- Picacg 搜索结果可直接取 `PicacgComicItemBrief.pages` 作为 `pageCount`。
- E-Hentai 搜索结果可取 `EhGalleryBrief.pages`，但分页模型是 `Galleries.next` 游标，不是普通页码；本轮仅安全暴露第一页。
- JM / nhentai 搜索结果没有稳定 `pageCount`，工具不为了补字段逐条拉详情，缺失字段直接省略。

### 91.4 验收状态

- `flutter analyze --no-fatal-infos`：0 error、0 warning；仅有既有 `eh_comic_page_v2.dart` 的 deprecated info，不是本轮新增。
- `flutter build apk --debug`：成功。
- 未执行真实联网 `download_comic` 入队验收：该工具会真实联网并写下载队列，属于不可自动撤销操作，应由后续带 UI 确认链路或专项验收执行。
- 未执行真实 registry dispatch 全链路验收；当前属于静态分析与构建通过的“部分完成”。

### 91.5 关键铁律

- AI 能力层保持驱动无关：不依赖具体 LLM SDK，不读 API key，不决定 UI 交互。
- `download_comic` 当前语义是“一次一本、直接入队”；未来 UI 必须在调用前做候选清单、对照卡和用户勾选确认。
- 不要从 `BaseComic.description` 反解析结构化字段；结构化页数应从具体模型字段读取，缺失就省略。
- 不要在注册入口自动接入 App 启动流程；当前只是把能力放进库里，未启用 AI 功能。
- 本地检索不要混用 `LocalLibraryManager` 与 `DownloadManager` 两套来源，否则容易重复并偏离现有本地搜索页行为。

### 91.6 残余风险

- 四源真实账号态、网络态、风控态会影响 `search_online/download_comic` 运行结果，尚需后续运行时验收。
- 历史检索依赖 `HistoryManager.isInitialized == true`，未初始化时会返回无历史结果但不崩溃。
- E-Hentai 继续翻页需要把 `next` 游标加入工具 schema 或引入会话态 loader。

### 91.7 涉及文件

- `lib/foundation/ai/ai_tool.dart`
- `lib/foundation/ai/ai_tool_registry.dart`
- `lib/foundation/ai/ai_sources.dart`
- `lib/foundation/ai/ai_capabilities.dart`
- `lib/foundation/ai/tools/search_online_tool.dart`
- `lib/foundation/ai/tools/download_comic_tool.dart`
- `lib/foundation/ai/tools/search_local_tool.dart`
- `lib/foundation/ai/tools/download_status_tool.dart`
- `lib/foundation/local_search_core.dart`
- `lib/pages/local_search_page.dart`

---

## 92. 主线第五轮：客户端远程已下载卡住修复（已完成，Android profile 真机通过）

### 92.1 背景

Flutter 客户端进入「远程 · 已下载」卡死，网页端正常。用户反馈"之前是好的"，但最近服务端资源规模增大后触发了旧有缺陷。

### 92.2 实际三个根因（均由 logcat 日志锁定）

| 根因 | 日志证据 | 量级 |
|------|---------|------|
| `jsonDecode` 在 UI isolate 同步解析 9.8 MB JSON | `fetch+decode=1228ms` | 1228ms ANR |
| `_prepareTileViewModels` 对 4075 个远程 item 做全量本地历史/收藏 DB 查询 | `tileModels=85303ms` | 85 秒 ANR |
| `_downloadedItemTags` fallback 调 `RemoteLibraryComicItem.toJson()` 序列化全部 episodes/pages URL | 修复前两项后仍 ANR 5-6s | ~5 秒 ANR |

"之前正常"不是代码被改坏，而是 4075 个 items 才暴露了既有路径的 ANR 临界点。

### 92.3 修复内容

1. **`_sendRequest` jsonDecode 挪到后台 isolate**：新增顶层函数 `_decodeJsonBody`，替换为 `await compute(_decodeJsonBody, bodyText)`。
2. **`_prepareTileViewModels` 远程档走直接字段访问路径**：直接读 `item.subTitle`/`item.tags`/`item.sourceDisplayName`/`item.comicSize`，跳过通用辅助函数的 `toJson()` fallback 路径和 DB 查询。
3. **`_buildReadingHistoryById`/`_buildFavoriteById` 排除 `RemoteLibraryComicItem`**：远程 item 的 id 是服务端路径 hash，与本地历史/收藏 target 不对应，无意义查询直接跳过。
4. **分段计时诊断日志**：`_sendRequest` 记录 bodyBytes；`_fetchSnapshotFromNetwork` 记录 fetch+decode/map/items；`reload()` 记录 avail/load/tileModels 各阶段耗时（`Log.info + print` 双通道确保 logcat 可见）。

（服务端 `?managedOnly=1`/`?rootId=` 过滤接口及客户端 `fetchManagedDownloadItems()` 调用在上一轮已完成，本轮基于其基础追加 isolate 解码与 ViewModel 快速路径。）

### 92.4 验收

- `flutter analyze --no-fatal-infos` 0 error/warning；6 条既有 eh Radio deprecated info 非本轮新增。
- Android profile APK 真机：进入「远程 · 已下载」不再卡死，`tileModels=0ms`，无 ANR，用户确认"好了"。

### 92.5 关键铁律

- `jsonDecode` 对 MB 级 body 在 UI 线程调用必触发 ANR；凡 JSON 响应体可能超过约 500KB，必须走 `compute()` isolate，且入口函数必须是顶层/静态函数。
- `RemoteLibraryComicItem.toJson()` 会完整序列化 `episodes: episodesData.map((e) => e.toJson())` 含所有 pages URL；远程档批量调用是隐性高成本，必须避免。
- `_prepareTileViewModels` 远程档必须直接访问 `item.subTitle`/`item.tags` 等 getter，不能走 `_downloadedItemTags`/`_downloadedItemAuthor` 通用辅助函数。
- 过滤快照不得写入全量 `_snapshotCache`（资源库/图集视图依赖完整快照）。

### 92.6 涉及文件

- `lib/foundation/remote_library_data_source.dart`（`_decodeJsonBody`/`compute`/`_fetchFilteredSnapshot`/计时日志）
- `lib/pages/download_page_logic_loading.dart`（`reload` 计时日志）
- `lib/pages/download_page_logic_cover.dart`（`_prepareTileViewModels` 远程快速路径）
- `lib/pages/download_page.dart`（`Log` import）

---

## 93. 主线第五轮：AI 对话驱动层接入（LLM 客户端 + 工具调用循环 + 对话 UI，已完成）

### 93.1 目标

让 AI Tab 从"手动工具调试页"升级为"能自然语言对话、能自动调用工具"的完整对话界面。不新增依赖包，全部走 OpenAI 兼容格式（覆盖 DeepSeek/Ollama/Qwen 等）。

### 93.2 新增文件

- `lib/foundation/ai/llm_client.dart`：LLM HTTP 客户端，`LlmMessage`/`LlmToolCall`/`LlmResponse`/`LlmClient.chat()`；从 `appdata.settings[127-131]` 读取 baseUrl/apiKey/modelId/params；解析 OpenAI `content` 或 `tool_calls` 响应；错误统一返回 `LlmResponse(error: "...")`。
- `lib/foundation/ai/ai_conversation.dart`：对话控制器，`AiConversationController extends ChangeNotifier`；`_runLoop(round)` 最多 3 轮工具调用循环；`download_comic` 挂起等待用户确认（`pendingDownload`），其他工具直接 dispatch；system prompt 内置（本地优先、下载必须确认、数据来源透明）。
- `lib/pages/ai/ai_chat_page.dart`：对话 UI，右对齐 user 气泡 + 左对齐 assistant 气泡 + 可折叠 toolCall/toolResult 卡片 + 黄色下载确认卡片 + 清空/工具调试入口。

### 93.3 改动文件

- `lib/pages/ai/ai_page.dart`：原工具调试内容提取为 `AiToolDebugPage`，`AiPage` 改为直接返回 `AiChatPage`。

### 93.4 关键铁律

- `compute()` 入口函数必须是顶层/静态函数且参数可序列化，不能传 closure 或实例方法。
- `PendingDownload` 必须是公开类型，`_PendingDownload` 私有类型在公开字段中触发 lint 警告。
- `download_comic` 是唯一需要挂起等待确认的工具；其他工具直接执行，不阻塞对话流。
- 工具调用上限 3 轮；达上限后明确告知用户，不允许无限循环。

### 93.5 涉及文件

- `lib/foundation/ai/llm_client.dart`（新增）
- `lib/foundation/ai/ai_conversation.dart`（新增）
- `lib/pages/ai/ai_chat_page.dart`（新增）
- `lib/pages/ai/ai_page.dart`（改造）

---

## 94. 主线第五轮：AI 对话两项修复（底栏热更新 + 远程库工具，已完成）

### 94.1 修复 1：底栏 AI Tab 开关后需重启

根因：`main_page.dart` 只监听 `App.serviceConfigVersion` 触发 setState；AI Tab 开关（`showAiTabSettingIndex=120`）修改了 settings 但没有通知此信号。

修复：`ai_settings_page.dart` 的 `_setSetting()` 当 `index == showAiTabSettingIndex` 时额外调用 `App.serviceConfigVersion.value++`，并去掉设置项 subtitle 的"需重建页面生效"提示。

### 94.2 修复 2：AI 无法访问远程库资源

根因：原有 6 个工具仅查本地 SQLite 库/收藏/历史，不调用 `RemoteLibraryDataSource`。

修复：新增 AI 工具 `query_remote_library`，调用 `RemoteLibraryDataSource.instance.fetchManagedDownloadItems()` 查询远程已下载快照，支持关键词过滤标题/作者/标签，返回统一格式；在 `ai_settings.dart` 新增 index 133，`ai_capabilities.dart` 注册，`ai_settings_page.dart` 补开关。

### 94.3 修复 3：上下文说明

对话列表为空时展示引导卡片，说明"当前对话上下文在本次会话中保留，关闭或清空后重置"，消除用户对"AI 有没有做上下文"的疑惑。

### 94.4 涉及文件

- `lib/pages/settings/ai_settings_page.dart`
- `lib/foundation/ai/tools/query_remote_library_tool.dart`（新增）
- `lib/foundation/ai/ai_settings.dart`（index 133）
- `lib/foundation/ai/ai_capabilities.dart`（注册）
- `lib/base.dart`（settings 数组长度补齐）
- `lib/pages/ai/ai_chat_page.dart`（引导卡片）

---

## 95. 主线第五轮：AI 多会话持久化（会话列表 + 侧栏 + 本地存储，已完成）

### 95.1 功能

多条 AI 会话持久化到磁盘（`${App.dataPath}/ai_conversations/{id}.json`），重启后可继续；左上角汉堡菜单打开侧栏切换会话；会话上限 50 条，超出自动删除最旧的；会话标题取第一条用户消息前 15 字。

### 95.2 改动

- `lib/foundation/ai/ai_conversation_store.dart`（新增）：纯 IO 层，管理会话索引（`ai_conversations_index.json`）、读写单条会话文件、删除、记录上次活跃 id。
- `lib/foundation/ai/ai_conversation.dart`：补充 `AiChatMessage.toJson/fromJson`；`AiConversationController` 新增 `conversationId`、`create({loadId})`、`_save()` 自动持久化。
- `lib/foundation/ai/llm_client.dart`：补充 `LlmMessage.fromJson()` 支持反序列化。
- `lib/pages/ai/ai_chat_page.dart`：升级为 `StatefulWidget`，持有 `AiConversationController`；AppBar 加汉堡菜单；`Scaffold` 加 `_ConversationDrawer`（列表 + 新建 + 删除 + 时间格式对齐历史页 `_formatTime`）。

### 95.3 关键约束

- system prompt 不持久化（重建时重新插入），只持久化 user/assistant/tool 消息。
- 过滤快照与会话文件无关，会话存储不涉及缓存污染问题。
- `AiConversationController.create()` 是 async，UI 层需用 `FutureBuilder` 或 `setState` 在 `initState` 完成后触发重绘。

### 95.4 涉及文件

- `lib/foundation/ai/ai_conversation_store.dart`（新增）
- `lib/foundation/ai/ai_conversation.dart`
- `lib/foundation/ai/llm_client.dart`
- `lib/pages/ai/ai_chat_page.dart`

---

## 97. 主线第五轮：远程搜索卡顿真根因修复 + AI 清单底栏样式 + AI 对话保活（已完成）

### 97.1 远程条目搜索卡顿真根因（已下载页）

此前一轮修复（对应经验版 §8）已解决"进入远程已下载"卡死的三个 ANR 根因，但接入 eh/nh 源后，**搜索框输入**仍会卡：真根因是 `download_page.dart` 的 `_translateDownloadedTag` 用双重 `for` 遍历 `tagTranslations.values` 的 `entries` 逐条 `toLowerCase()` 字符串比较，退化为 O(33739) 全表扫描；该实现自 V1.2 起未变，之前不卡是因为 picacg 条目标签数少（3-8个），接入 eh/nh 后单条目标签数暴涨到 8-9 个且常查不到翻译（必须扫完全表才返回原值），"条目数 × 标签数 × 33739次比较"的量级从可接受跳到 ANR 级。

修复：`tagTranslations` 各分类的 key 在加载时已统一转小写存储，本就具备 O(1) 哈希能力；把 `entries` 遍历改为 `map[lowerTag]` 直接查找，复杂度从 O(33739) 降到 O(12)，签名/行为/调用点均不变。

### 97.2 AI 清单页详情底栏样式

`ai_item_list_page.dart` 的 `_showItemDetail` 两个分支（真实 `RemoteLibraryComicItem` 命中 / fallback 轻量展示）原先都缺透明背景与顶部圆角，与已下载页 `_showInfo` 的底栏风格不一致。统一改为 `showModalBottomSheet(isScrollControlled: true, showDragHandle: false, useSafeArea: false, backgroundColor: Colors.transparent)` + 内层 `Material`（surface 底色 + 顶部圆角16 + `clipBehavior: antiAlias`）包裹原内容。

### 97.3 AI 对话页保活（滚动位置 + 输入草稿）

项目 tab 切换是 `NaviPane` + 每个 tab 独立 `Navigator` 的 `pushAndRemoveUntil` 整栈替换（非 `IndexedStack`/`PageView`），`AiChatPage` 每次切回都完全重建，`AutomaticKeepAliveClientMixin` 对此无效。

- **滚动到底部**：新增 `_scrollToBottomAfterFrame()`（`addPostFrameCallback` + `hasClients && maxScrollExtent > 0` 双重 guard 后 `jumpTo`），在 `_loadController`/`_switchConversation`/`_newConversation` 三处 `setState` 之后各调用一次。
- **输入草稿持久化**：`AiConversationController.create()` 每次都 `_internal()` 新建实例，全局无复用，草稿不能挂在 controller 上；改挂在 `_AiChatPageState` 的 `static final Map<String, String> _draftsByConversationId`（按 `conversationId ?? ''` 分 key），`TextField.onChanged` 写入，三处 controller 重建后恢复，`_send()` 成功后清空对应 key。

### 97.4 验收

- `flutter analyze` 全量通过，无新增 error/warning。
- ⚠️ 本轮无运行时设备环境，Bug1 的 Stopwatch 耗时对比、Bug2/Bug3 的实际视觉与交互效果均未上机验证，仅完成静态代码核对。

### 97.5 关键铁律

- 排查性能卡顿不能只看"新功能接入前后有无代码改动"，还要看"新功能是否改变了旧代码路径上的数据规模/密度"——`_translateDownloadedTag` 本身自 V1.2 起没变过，是 eh/nh 标签密度把它从"能忍"推到"ANR"。
- 草稿类"随 UI 状态走"的数据，如果 controller/State 会被整栈重建而不复用，必须挂在更长生命周期的容器上（`static` 字段或独立 store），不能想着挂在会被重建的实例上。

### 97.6 涉及文件

- `lib/pages/download_page.dart`（`_translateDownloadedTag`）
- `lib/pages/ai/ai_item_list_page.dart`（`_showItemDetail`）
- `lib/pages/ai/ai_chat_page.dart`（滚动保活 + 草稿持久化）

---

## 96. 主线第五轮：AI 工具结果漫画清单页与消息级汇总条（已完成）

### 96.1 背景

`query_remote_library` 等工具返回漫画列表（items 数组）后，LLM 把 JSON 转成 markdown 表格直接输出在文字气泡里，难以阅读、无法交互。

### 96.2 最终形态

同一轮对话内的顺序：

```
[工具调用卡]
[工具结果卡]（纯展示，不带按钮）
[AI 文字回复]：简短总结数量/占比，不逐条罗列、不输出表格
[汇总清单卡]（独立一条消息，挂在文字回复下方，点击进入清单页）
```

点击清单卡进入 `AiItemListPage`，网格展示漫画封面卡片（对齐已下载页视觉），点击 tile 弹轻量详情 sheet（封面 + 标题 + 作者 + 标签 + 可用性状态行）。

### 96.3 数据类型

- `lib/foundation/ai/ai_result_item.dart`（新增，纯 Dart 无 Flutter 依赖）：`AiResultItem`（id/title/author/tags/coverUrl/source/availability），`AiResultItem.fromToolData(toolData)` 从 `{'items': [...]}` 形状提取列表。
- `AiChatMessageType` 新增 `resultList`；`AiChatMessage.resultList({items})` 命名构造，`toolData = {'items': items}`，`text` 为简短摘要（如"共 N 条结果"）。

### 96.4 消息级汇总条实现

- `AiConversationController` 新增 `_pendingResultItems` 缓冲区：每次工具结果若 `result.data` 是 `{'items': List<Map>}` 形状就并入缓冲区。
- 当本轮循环以纯文本回复结束（`!response.hasToolCalls`）时，若缓冲区非空，紧跟在刚添加的 assistant 消息之后再添加一条 `resultList` 消息，然后清空缓冲区。
- `_createNew()`（新建/清空会话）时同步清空缓冲区。
- system prompt 新增规则：工具返回漫画列表时，文字回复只需简要总结数量/分类占比，不逐条罗列或输出 markdown 表格。

### 96.5 清单页塌陷 bug 修复

- 根因：`DownloadedComicTile` 的详情模式用 `LayoutBuilder` 取父级高度约束、简明模式用 `Stack`+`Positioned.fill` 撑满父级，两者都要求父级提供明确尺寸约束；`ListView.builder` 的 item 在主轴方向是自适应内容高度的松散约束，给不出这个尺寸，导致 tile 内部布局无法计算而塌陷。
- 修复：`AiItemListPage` 改为 `CustomScrollView` + `SliverGrid(gridDelegate: SliverGridDelegateWithComics())`，与 `history_page.dart` 的已下载库网格写法对齐，由 gridDelegate 计算出明确的 cell 尺寸。

### 96.6 关键铁律

- `DownloadedComicTile` 必须放在提供明确网格尺寸约束的父级（`SliverGrid`/`GridView` 等）中使用，不能套 `ListView.builder`/`Column`/`Wrap` 等松散容器，否则会塌陷。
- 撤回旧按钮块时要确认相关 import（如 `ai_result_item.dart`）是否仍被新组件引用，不能一起删掉。
- 一轮对话内所有工具结果的 items 会汇总进同一张清单卡，不区分来源工具；这是已知简化，不是 bug。

### 96.7 涉及文件

- `lib/foundation/ai/ai_result_item.dart`（新增）
- `lib/pages/ai/ai_item_list_page.dart`（新增，后改用 SliverGrid）
- `lib/foundation/ai/ai_conversation.dart`（`resultList` 类型 + 缓冲区 + system prompt）
- `lib/pages/ai/ai_chat_page.dart`（`_ToolCard` 撤回按钮块，新增 `_ResultListEntryCard`）

---

## 98. 主线第五轮：AI 在线封面修复 + display_result_list 工具 + 对话卡片优化（已完成）

### 98.1 在线封面修复

`search_online_tool.dart` 的 `_baseComicJson`/`_ehBriefJson` 用 `'cover'` 作键名，而 `AiResultItem.fromJson` 读 `'coverUrl'`，导致搜索清单页封面全为空。修复：两处改为 `'coverUrl'`（`query_remote_library_tool.dart` 已正确用 `'coverUrl'`，远程库清单封面正常）。

### 98.2 display_result_list 工具与架构调整

新增 `display_result_list` 工具，让 AI 可以主动把精选/筛选后的漫画条目以清单卡展示，而非用文字逐条罗列。同时移除旧自动 flush `_pendingResultItems` 逻辑（该缓冲区由控制器被动积累，AI 无法控制内容），`resultList` 消息改为只由 `display_result_list` 工具触发。`_ToolCard` 新增"查看 N 条原始结果"按钮（检测 `resultData` 里的 `items` 字段），作为独立于清单卡之外的入口。

### 98.3 对话卡片 UX 优化

- 工具卡片视觉减小（`margin`/`padding` 收紧），减少占屏比例。
- "查看原始结果"按钮降为辅助文字样式（去掉 `FilledButton.tonal` 紫色背景）。
- `_maxRounds` 从 3 增到 5，避免"查本地→在线搜索→筛选展示→文字回复"四轮触发上限。
- `resultList` 消息位置修正为缓冲模式：`display_result_list` 调用时把 items 暂存至 `_pendingDisplayItems`，等 LLM 纯文字收尾后 flush，顺序保证"[工具卡]…[AI文字]→[清单卡]"。
- AI/用户消息气泡支持长按选择复制（`SelectableText`），点空白处清除选区。

### 98.4 涉及文件

- `lib/foundation/ai/tools/search_online_tool.dart`（封面字段修复）
- `lib/foundation/ai/tools/display_result_list_tool.dart`（新增）
- `lib/foundation/ai/ai_conversation.dart`（`_pendingDisplayItems` 缓冲 + `_maxRounds` 5）
- `lib/pages/ai/ai_chat_page.dart`（工具卡 UX + 查看原始结果按钮 + SelectableText）
- `lib/foundation/ai/ai_capabilities.dart`（注册 `display_result_list`）

---

## 99. 主线第五轮：AI 清单页多选下载 + 下载队列/进度页 + AppBar 整理（已完成）

### 99.1 清单页直跳在线详情页

`AiItemListPage` 原点击 tile 走 `findByCandidates` 查远程库，在线搜索结果永远找不到 → fallback 轻量底栏。修复：根据 `item.source` 直接 push 对应在线详情页（picacg→`PicacgComicPageV2`，jm→`JmComicPageV2`，nhentai→`NhentaiComicPageV2`，ehentai→`EhentaiComicPageV2`），`remote`/其他来源保留原有底栏路径。

### 99.2 清单页多选 + AI 下载队列

`AiItemListPage` 升级为 `StatefulWidget`，支持多选模式（长按进入，参考下载页风格：`selecting`/`selected`/`selectedNum`/AppBar 切换）。选中后加入 `AiDownloadQueue`（新增，`ChangeNotifier` 单例，JSON 持久化到 app data）。

### 99.3 AI 下载清单页

新增 `AiDownloadListPage`：
- 待下载区展示 `AiDownloadQueue.items`，点击触发实际入队（`getComicInfo` → `enqueueXxx`），完成后从队列移除。
- 进度区展示 `OnlineDownloadManager.instance.tasks`，任务卡右侧从只读图标改为可操作按钮（暂停/继续/置顶/重试），进度文字附带下载速度。
- AppBar 全局操作：暂停全部/继续全部/清除已完成。

### 99.4 AI 对话页 AppBar 整理

- 去掉"AI 对话"标题文字。
- 删除/工具调试按钮从 `actions` 移到标题位置（左侧，紧随汉堡菜单）。
- 输入框右侧发送按钮上方新增小悬浮按钮，带下载队列数量角标，点击进入 `AiDownloadListPage`。

### 99.5 涉及文件

- `lib/pages/ai/ai_item_list_page.dart`（多选 + 直跳在线详情页）
- `lib/foundation/ai/ai_download_queue.dart`（新增）
- `lib/pages/ai/ai_download_list_page.dart`（新增）
- `lib/pages/ai/ai_chat_page.dart`（AppBar 整理 + 下载清单入口）
- `lib/main.dart`（队列加载）

---

## 100. 主线第五轮：AI 收藏管理工具（已完成）

新增 `manage_favorites` AI 工具，通过 `action` 参数支持以下操作：
- `list_folders`：列出所有收藏夹
- `create_folder`/`delete_folder`/`rename_folder`：收藏夹 CRUD
- `list_comics`：列出收藏夹内的漫画
- `add_comic`/`remove_comic`：添加或移除收藏
- `check_comic`：检查某漫画是否已收藏

内部调用 `LocalFavoritesManager` 的既有方法；`source` 字符串到 `FavoriteType` 的映射通过 switch 完成。`ai_settings.dart` 新增 index 135，设置页补开关。

涉及文件：`lib/foundation/ai/tools/manage_favorites_tool.dart`（新增）、`lib/foundation/ai/ai_capabilities.dart`、`lib/foundation/ai/ai_settings.dart`、`lib/pages/settings/ai_settings_page.dart`

---

## 101. 主线第五轮：AI 助手 Markdown 渲染 + 工具调试入口移至设置页（已完成）

### 101.1 Markdown 渲染

AI 助手回复气泡改用 `flutter_markdown` 的 `MarkdownBody`（`SelectionArea` 包裹，支持长按复制），替换原 `SelectableText`。新增依赖 `flutter_markdown: ^0.7.7`。

### 101.2 工具调试入口移至设置页

- `AiToolDebugPage` 入口从对话页 AppBar 的扳手按钮移到 AI 设置页底部。
- 对话页 AppBar 移除扳手图标。
- `display_result_list` 和 `manage_favorites` 两个工具补齐设置索引（134/135），设置页补开关，解决调试页里"未开启"状态。

涉及文件：`lib/pages/ai/ai_chat_page.dart`、`lib/pages/settings/ai_settings_page.dart`、`lib/foundation/ai/ai_settings.dart`、`lib/base.dart`、`lib/pages/settings/settings_page.dart`、`pubspec.yaml`/`pubspec.lock`

---

## 102. 主线第五轮：AI 工具卡片折叠分组 + 表格气泡宽度 + 滚动行为修复（已完成）

### 102.1 工具卡片折叠分组

同名工具连续多次调用时，将多对 toolCall/toolResult 折叠为一张"工具名 × N"分组卡（`_ToolGroup`），默认收起，可展开查看各次详情。消息列表引入 `_DisplayItem` sealed class（`_SingleItem` / `_ToolGroup`），build 时通过 `_buildDisplayItems()` 计算渲染列表。

### 102.2 表格气泡宽度

助手消息含 markdown 表格时，气泡 `maxWidth` 从限制 70% 扩展到更宽，避免多列表格因宽度不足大量换行。

### 102.3 滚动行为修复

- 修复"加载后不在底部"：`_scrollToBottomAfterFrame()` 移除 `maxScrollExtent > 0` 守卫（ListView 首帧懒加载时高度可能为 0 导致 guard 跳过），首帧无效时追加第二帧重试。
- 修复"切标签后丢失滚动位置"：引入 `AutomaticKeepAliveClientMixin` 保活 widget（切 tab 不重建），同时记忆每个会话的滚动 offset，切回同一会话时恢复上次位置，切换到不同会话时跳至底部。

涉及文件：`lib/pages/ai/ai_chat_page.dart`

---

## 103. 主线第五轮：AI 提示词标签面板 + 来源限定 + 结构化改造（已完成）

### 103.1 提示词标签系统（计划26）

新增独立模块 `lib/foundation/ai/ai_prompt_tags.dart`（普通标签模型/存储/解析/文本剥离），与设置页、聊天 UI 和控制器共同依赖，避免三处各写一套。

核心功能：
- 输入框左侧新增"提示词标签"按钮，点击弹出锚定面板（向上展开，面板保持展开供连续多选）。
- 面板分两区：固定来源标签（`#搜pica`/`#搜jm`/`#搜eh`/`#搜nh`）+ 可编辑普通提示词标签。
- 标签以 `#标签名` 插入输入框，用户气泡中高亮显示；发送给 LLM 的 user 文本剥离已识别标签，标签内容改为独立 system 指令。
- 来源标签是硬约束：`_getEnabledToolSchemas()` 深复制 `search_online` schema 并收窄 `source.enum`；`_runLoop()` dispatch 前硬拦截未授权来源调用，不靠模型自觉遵守。
- 全局"长期生效"开关：开启时标签写入会话持久化元数据，重开会话后恢复；关闭时仅当次生效。
- 设置页支持普通标签新增/编辑/删除/恢复默认。
- 会话文件升级为向后兼容新版本（新增长期标签/来源快照元数据）；旧版缺字段时按空状态加载。

### 103.2 结构化改造与本地限定（计划27）

- 新增固定范围标签 `#搜本地`：选中后硬拦截 `search_online`，仅查本地设备库和已连接远程库快照。
- 标签插入机制从"拼接到输入框文本再靠正则识别"改为"面板点选进结构化选中集合 `_selectedTagChips`，发送时才与手输文本合并"，消除退格键在标签中间误删导致的静默失效。
- 输入框上方新增常驻结构化标签行（显示已选标签/长期生效的持久状态），不再需要展开面板才能看到长期状态。
- `#搜本地` 与来源标签语义互斥（互斥 UI + 执行层以本地限定优先）；保留手输 `#标签名` 作为并行 fallback 路径，两路在发送时合并去重。
- 测试覆盖：`test/ai_prompt_tags_test.dart`（标签解析、来源映射、优先级）+ `test/ai_conversation_prompt_test.dart`（schema 过滤、dispatch 硬拦截、版本兼容加载）。

### 103.3 关键铁律

- `search_online` 的来源 schema 过滤必须深复制嵌套 `Map`/`List`，不得修改注册表返回的共享 const schema，否则约束会全局污染后续对话。
- 来源约束的实现位置：控制器 dispatch 前，而非全局工具注册表，避免把会话态塞进无状态的全局单例。
- `_history` 只保存"基础 system + user/assistant/tool"，临时/长期标签指令由 `_buildRequestMessages()` 按请求注入，不落盘，避免污染消息历史顺序。

### 103.4 涉及文件

- `lib/foundation/ai/ai_prompt_tags.dart`（新增）
- `lib/foundation/ai/ai_conversation.dart`（提示词标签/来源约束/本地限定/schema 过滤/dispatch 硬拦截）
- `lib/foundation/ai/ai_conversation_store.dart`（会话版本升级/元数据序列化）
- `lib/foundation/ai/ai_settings.dart`（新设置索引）
- `lib/base.dart`（默认设置值）
- `lib/pages/ai/ai_chat_page.dart`（标签面板/结构化选中行/来源标签高亮/标签插入改造）
- `lib/pages/settings/ai_settings_page.dart`（标签管理 UI/长期开关）
- `lib/pages/settings/settings_page.dart`（import 补齐）
- `test/ai_prompt_tags_test.dart`（新增）
- `test/ai_conversation_prompt_test.dart`（新增）

---

## 104. 主线第九轮：AI 对话空回复与长期范围标签回显（代码已完成）

### 104.1 空回复气泡兜底（02号计划）

- 根因：`_runLoop()` 在无 tool call 收尾时无条件把 `response.content ?? ''` 写成 assistant 气泡并写入 `_history`，LLM 返回 `null`、空串或纯空白时会产生空块。
- 修复：抽出 `_handleFinalTextResponse(String?)`。文本 `trim()` 后非空才显示并写入 history；无文字且无待展示清单时显示“（AI本轮未返回有效内容）”的轻量提示但不写 history；有待展示清单时不重复显示提示。
- 测试：`simulateFinalTextResponseForTesting` 驱动生产收尾逻辑，覆盖 null/空白/正常文本/清单卡共 5 项；相关 AI 测试共 60 项通过，`flutter analyze` 0 issue。
- 未覆盖：真实 LLM provider/真机触发空回复，仅完成单元测试降级验证。

### 104.2 长期范围标签回显（05号计划）

- 根因：气泡回显原先只读取本轮参数 `selectedSources`/`localOnlyRequested`，长期状态在第二轮未重新选中时不会回显。
- 修复：状态落地后统一读取 `effectiveAllowedSearchSources`/`effectiveLocalOnly`，与 system prompt 注入和 dispatch 硬拦截共用同一最终生效范围；本轮显式覆盖优先于长期状态。
- 约束：范围标签只加入 `mergedRecognizedNames`，不能混入 `mergedPromptTags` 或持久化普通标签集合。
- 测试：覆盖长期来源、本轮覆盖、无范围三种场景；相关测试通过，未做真实 GUI 验证。

### 104.3 涉及文件

- `lib/foundation/ai/ai_conversation.dart`
- `test/ai_conversation_test.dart`
- `test/ai_conversation_prompt_test.dart`

## 105. 主线第九轮：EH 下载续传与并发提速（代码已完成，真机待验）

### 105.1 根因与修复

- 旧代码进入逐页下载时无条件把 `task.currentPage` 置零；即使本地文件已存在，也先调用 `getEhImageUrl` 做 reader/showpage 解密，再由 `_downloadFile` 内部判断跳过，造成续传进度缓慢爬升。
- 逐页模式现在先用 `_ehPageFileExists` 检查 `.jpg/.png/.webp/.jpeg` 且文件大小大于零的本地文件，命中后直接计入完成数，不发网络请求。
- 未完成页使用固定 `const ehConcurrency = 3` 的 `_Semaphore` 并发；进度改为已完成页计数，暂停/取消仍由 `_OnlineDownloadCancelled` 传播。
- `_downloadFileOnce` 的 ehgt 图片字节请求统一包入 `EhNetwork.acquireEhgtSlot/releaseEhgtSlot`，与已有探测请求共用 3 并发闸；非 ehgt 域名自动短路。

### 105.2 边界与约束

- 归档/zip 分支未改；`getEhImageUrl`、showKey/mpvKey 状态机未改；画廊级锁保证并发调用时复用同一 future。
- 不能用 `task.currentPage` 推断已完成页集合；并发后它只是完成数量，磁盘文件才是续传真相。
- 未做 EH 账号/真实画廊下的耗时和暂停续传实测；`flutter analyze` 全量通过，相关全量测试 7/7 通过。

### 105.3 涉及文件

- `lib/foundation/online_download_manager.dart`
- 只读核对：`lib/network/eh_network/eh_main_network.dart`

## 106. 主线第九轮：本地详情在线跳转 ID 与 Empty data 防御（04/06号计划）

### 106.1 根因链

- 历史 `DownloadedJmComic` 记录缺少顶层 `comicId` 和嵌套 `comic.id` 时会得到空 ID；旧 `_onVisitOnline()` 直接 `substring` 后请求 `/album?id=` 空值，最终由网络层抛出通用 `Empty data`。
- 04号先补了 JM 空/非数字 ID 的前置校验、用户提示和 warning 日志，因此计划状态为“部分完成”（未完成真机与脏数据范围确认）。
- 06号继续确认更深层的同源错误：`LocalLibraryComicItem.id` 是内部 `itemId`，不是来源 ID；校验若读错字段会把合法记录误判为非法。

### 106.2 修复契约

- `resolveOnlineRawId`：`LocalLibraryComicItem` 取 `originalId`，其他具体下载项保持读 `comic.id` 的兼容行为。
- `extractJmNumericId`/`extractNhentaiNumericId`：去掉可选前缀后必须为非空纯数字，否则拦截、提示并记录日志；picacg 同步补空值拦截。
- jm/nhentai/picacg 三个分支统一遵循“取正确来源 ID→格式校验→失败拦截或跳转”。

### 106.3 验证与风险

- `flutter analyze` 0 issue；06号新增 8 项单元测试通过。
- 未用用户实际漫画和实体机复现；代码只覆盖 `local_comic_detail_page.dart` 这一个已知调用点，其他直接读取 `comic.id` 的潜在调用方未做全库审计。

### 106.4 涉及文件

- `lib/pages/local_comic_detail_page.dart`
- `test/local_comic_detail_online_id_test.dart`

## 107. 主线第九轮：本地详情在线信息更新与整体覆盖（07号计划）

### 107.1 数据、写回与渲染

- `DownloadedJmComic` 新增 `works`/`actors`；`DownloadedComic` 新增 `chineseTeam`/`categories`；`NhentaiDownloadedComic` 新增 `categorizedTags`，旧记录缺字段时使用空默认值并保持 round-trip。
- `TrashManager.updateSourceDbRowMetadata` 只更新 download.db 行的 `title`/`subtitle`/`json`，复用 `_mutateSourceDbFile` 的特权 IO，不改 `time`/`directory`/`size`。
- 本地详情从 `sourceRowJson` 还原具体下载子类，按来源显示作品/演员、汉化组/分类、分类标签；新增标题菜单的“更新信息”。
- 拉取完整成功后用 `buildUpdatedDownloadedRecord` 做整体元数据覆盖，保留本地 ID、下载时间、目录、大小、章节和文件结构；写库失败不更新内存半成品。

### 107.2 验证与未完成

- `flutter analyze` 0 issue；全量测试 139 项通过，其中新增模型测试 8 项、更新信息测试 13 项。
- 未做真实 jm/nhentai 设备端到端验证；拉取期间 `_isUpdatingInfo` 尚未接到按钮 loading/置灰视觉反馈；EH 更新信息明确排除在本计划之外。

### 107.3 涉及文件

- `lib/foundation/download_model.dart`
- `lib/foundation/trash_delete.dart`
- `lib/pages/local_comic_detail_page.dart`
- `test/download_model_new_fields_test.dart`
- `test/local_comic_detail_update_info_test.dart`

## 108. 主线第十轮：EH 本地解析与标签显示闭环（01/02号计划）

### 108.1 EH 记录解析与大小

- `local_library_static.dart` 的 `_parseDownloadedItem` 统一委托 `parseDownloadedItemRecordJson`，不再维护第二套类型判断；EH 画廊 ID（`数字-令牌`）不再被 `contains('-')` 误判为 `CustomDownloadedItem`。
- `DownloadedGallery.fromJson` 兼容平铺/旧嵌套结构、数字/字符串 `size` 以及 `tagList`/`tags`，两条公开 SQLite 扫描入口均已用临时数据库验证。

### 108.2 标签行数与本地详情翻译

- `DownloadedComicTile` 新增可选 `maxTagRows`；默认不限制，只有下载列表传入 2。限制必须在构建 child 前计算可见前缀，不能用固定高度裁剪 `Wrap`。
- 本地详情引入来源感知标签显示模型：EH/NH 复用在线 `tagTranslateCategory`/`tagTranslateWithNs`，按第一个冒号拆分并保留原始搜索值；NH 只有存在有效分类桶时才抑制 flat 标签；翻译表异步完成后主动重建详情页。

### 108.3 状态与风险

- 01号计划状态为已完成代码验证；02号计划状态为部分完成。`flutter analyze` 通过，定向 42 项与全量 161 项测试通过。
- 未覆盖：截图对应真实 `download` 行、实体机 EH/NH 列表/详情、横屏和系统大字体人工验收。历史字符串 size、翻译异步刷新、NH 空分类桶三个问题均已用测试补齐。

### 108.4 涉及文件

- `lib/components/comic_tile.dart`
- `lib/foundation/download_model.dart`
- `lib/foundation/local_library_static.dart`
- `lib/pages/download_page.dart`
- `lib/pages/local_comic_detail_page.dart`
- `test/comic_tile_tag_rows_test.dart`
- `test/eh_local_type_size_test.dart`
- `test/download_model_new_fields_test.dart`
- `test/local_comic_detail_tag_display_test.dart`

## 109. 第八轮计划补录：45–47号计划

### 109.1 45号：已下载详情页标签翻译与长标签布局

- EH 落盘标签可能带 namespace（如 `female:elf`）和性别符号后缀；`_translateDownloadedTag` 现在先拆 namespace、剥离后缀再查表，未命中时至少保留处理后的可读值。
- 详情弹层标签容器限制 `maxWidth: 140`，文本单行省略，避免长标签把“阅读/查看详情”按钮挤出可见区域。
- 未做不同 DPI/字号下的真机视觉验收，后续调整应优先复用经验版 §78 的后缀处理和布局约束。

### 109.2 46号：多下载工具调用 400

- `_runLoop` 遇到第一个 `download_comic` 不再提前 `return`；待确认下载改为内部队列，逐个确认后才继续工具循环。
- 保持 `pendingDownload` 对外只读 getter 契约不变，确保 assistant 的每个 `tool_call_id` 最终都有配对的 `tool` 消息，避免旧会话 history 永久损坏并反复 400。

### 109.3 47号：多源搜索分发与范围标签回显

- 多来源在线搜索提示词明确要求对每个来源分别调用 `search_online`，不再让模型询问用户选择哪个来源。
- 来源 chip 和 `#搜本地` 进入 `mergedRecognizedNames`，不混入普通标签持久化集合，且不区分长期/临时模式；长期状态后续由第九轮 05号计划改为读取最终生效范围 getter。
- 相关 AI 测试覆盖提示词文本和范围标签回显；真实模型是否始终遵守“分别调用”仍需运行时观察。

## 110. 主线第十轮：本地详情布局、EH 入口与关于页（03号计划）

### 110.1 本地详情信息语义与布局

- 本地详情信息区改为按原始语义角色排序：`ID -> 创作者/画师/上传者 -> 下载时间`；`download.db.time` 明确显示为“下载时间”，不再与源站“时间”混用。
- 页数、语言、源站时间作为同一个响应式信息行，缺失字段省略，常规宽度尽量同行，窄屏/大字/长值允许自然换行。
- 页数优先取本地正文文件数；EH 可回退已持久化的 `pageCount`，不使用在线 `maxPage` 覆盖本地可读页数。NH 分类桶、EH/Pica 源站时间已贯通下载、队列、序列化和在线包装链路。

### 110.2 EH 本地在线详情与更新信息

- EH 本地记录通过 `sourceRowJson` 恢复完整 `link`，只接受合法的 e-hentai/exhentai 画廊 URL；不从 `gid-token` 猜 URL。
- EH 纳入本地详情的“在线详情”和“更新信息”共用可见性判断。更新请求复用在线页的 Content Warning 确认/取消语义。
- 更新成功只覆盖 uploader、标签和源站时间；保留本地标题、副标题、链接、封面、大小、页数、目录、文件结构和下载时间，并沿用失败不写库、成功原子写回契约。

### 110.3 关于页与版本

- “设置 -> 关于”改为显示 `assets/app_icon.png`、PicaKeep 名称、两行简介、项目地址和 GitHub 问题反馈入口。
- 通过 `package_info_plus` 读取实际安装包版本和构建号，显示格式为 `V<version> (<buildNumber>)`；移除关于页对 `V1.9.9` 和 `appVersion` 的依赖。

### 110.4 验证、边界与遗留

- `flutter analyze` 两次通过；定向 EH/详情/关于页测试 41 项通过；全量 `flutter test` 186 项通过；`git diff --check` 通过；Android debug APK 与 Windows 构建通过。
- 360dp、1.8 倍文字缩放的详情 Widget 测试未发现 `RenderFlex overflow`。检测到 Android 14 设备，但设备已有 PicaKeep 下载任务，本轮没有安装 APK 或中断任务。
- 仍待设备空闲后完成：真实 EH Content Warning 的取消/确认与数据库写回、NH/EH/JM/Pica 设备端布局验收、项目/Issues 外部链接启动，以及已安装包中的实际版本显示。

### 110.5 涉及文件

- 主要实现：`lib/pages/local_comic_detail_page.dart`、`lib/pages/online_comic/eh_content_warning.dart`、`lib/pages/settings/settings_page.dart`、`lib/foundation/download_model.dart`、`lib/foundation/online_download_manager.dart`、`lib/network/nhentai_network/models.dart`、`lib/pages/online_comic/eh_comic_page_v2.dart`
- 依赖与资源：`pubspec.yaml`、`pubspec.lock`、`assets/app_icon.png`、`android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java`
- 测试：`test/local_comic_detail_info_layout_test.dart`、`test/local_comic_detail_online_id_test.dart`、`test/local_comic_detail_update_info_test.dart`、`test/eh_content_warning_test.dart`、`test/settings_about_page_test.dart`、`test/download_model_new_fields_test.dart`

## 111. 主线第十一轮：AI 结果清单参数边界与灰块入口修复（01号计划）

### 111.1 根因与数据边界

- 发布版超长纯灰块的直接根因是模型把 `availability` 生成了字符串，而 `AiResultItem.fromToolData()` 对其执行 Map 强转；条目构建异常在纵向无界 `ListView` 中逃逸为高大的 release `ErrorWidget`。图片 URL、缓存、列表 key、滚动位置和第九轮空 assistant 气泡均不是本次根因。
- “查看 N 条原始结果”入口点击时再次使用同一严格解析，因此与清单卡构建一起失效。两处现在统一消费 `AiResultItemDecodeReport` 的安全解码结果。
- 工具入口新增完整 item schema 和严格运行时校验：可无歧义修正的类型漂移本地规范化；不可恢复的 id/title 或顶层结构错误返回脱敏 `retryable` 失败；空数组是非重试零结果。成功工具才允许生成 `resultList`，失败结果交回模型修正。

### 111.2 旧会话与 UI 行为

- 旧会话读取采用宽松兼容，不要求删除或迁移磁盘 JSON；字符串 `availability`、数字 id、列表 author、单字符串/Map tags 均可恢复，无法恢复的单项隔离，不拖垮整批。
- 消息清单、“查看原始结果”和“查看全部”统一使用规范化数据；部分不可用时显示有界提示，全部不可用时显示有界失败态，不再让 `ErrorWidget` 逃逸。
- 下载队列复用 `AiResultItem.toJson()`，保持既有保存形状、`source:id` 去重和入口行为。

### 111.3 验证与边界

- `flutter test` 全量 202 项通过，`flutter analyze` 无问题，`flutter build apk --release` 成功；Android 14 release 模式设备完成旧数据保留下的清单可见、消息“查看全部”和工具卡原始结果入口验收，并保存了脱敏截图证据。
- 设备上实际旧会话清单为 10/12，脱敏自动化 fixture 和两个入口验收为 12/12；未完成截图对应会话完整杀进程冷启动、真实模型随机错误参数后自动重试、另一台 Android 10 设备和真实网络日志导出。原因是当前设备数据与截图场景不完全一致，且不为随机模型行为制造额外 token/网络请求。

### 111.4 涉及文件

- 实现：`lib/foundation/ai/ai_result_item.dart`、`lib/foundation/ai/tools/display_result_list_tool.dart`、`lib/foundation/ai/ai_conversation.dart`、`lib/foundation/ai/ai_download_queue.dart`、`lib/pages/ai/ai_chat_page.dart`
- 测试：`test/ai_result_item_test.dart`、`test/display_result_list_tool_test.dart`、`test/ai_conversation_test.dart`、`test/ai_conversation_store_test.dart`、`test/ai_download_queue_test.dart`、`test/ai_chat_result_list_widget_test.dart`
- 构建与证据：`build/app/outputs/flutter-apk/app-release.apk`；`Z-plan/新需求-主线-第十一轮/01-result-evidence/`（计划目录被忽略，不纳入提交）

## 112. 主线第十二轮：AI 日志凭据脱敏与缓存诊断补全（01号计划）

### 112.1 实现

- `lib/network/app_dio.dart` 的网络日志 request/response/error/retry 路径统一经过 `NetworkLogRedactor`；只处理日志副本，覆盖大小写不敏感的 Authorization、Cookie/Set-Cookie、API key、access/refresh token、password 及明确 URL 认证参数，普通 request/response body、messages、tools 保留。
- `lib/foundation/log_file_service.dart` 的当前导出、历史分享和 ZIP 都先生成临时脱敏副本；失败时中止，不回退原始路径；历史查看页继续读取原文，保持日志查看语义不变。
- `LlmClient.chat()` 的 `AiUsage shapeV=2` 记录 `conversation`、`turn`、`round`、`messageCount/messagesHash`、`toolCount/toolsHash`、prompt/cache hit/cache miss 和 system 前缀计数。canonical JSON 递归排序 Map、保留 List 顺序，SHA-256 短指纹仅用于诊断，不是认证机制。

### 112.2 验证与边界

- 新增日志脱敏/临时副本测试与缓存前缀测试；`flutter test` 全量 210 项通过，`flutter analyze` 通过，`git diff --check` 通过。
- 旧日志中已经出现的 DeepSeek API Key 不能由代码修复失效，必须在 DeepSeek 平台撤销并轮换；本轮未执行外部密钥操作，也未用真实 provider 发起付费 A/B 请求。
- `shapeV=2` 证明客户端请求 shape 是否稳定，不声称 provider 每次实际命中；真实使用仍需观察 `cacheHit/cacheMiss` 与 hash 组合。

## 113. 主线第十二轮：AI 动态指令后移与缓存前缀稳定化（02号计划）

### 113.1 消息与活动轮

- `_buildRequestMessages()` 现在只返回稳定基础 system 与 `_history`；长期/临时提示词标签、local/online scope 和有序来源集合编码在当前新 user 的确定性 JSON `turn_context` 后缀中，UI 气泡与标题继续使用原始显示文本。
- `_ActiveTurnContext` 冻结本轮最终标签、scope 和来源集合；工具子轮及下载确认续跑始终读取同一快照，结束、异常和清空时释放。下一轮重新计算，不批量改写旧 history。
- 动态 tools schema 策略、dispatch 硬拦截、assistant tool_calls 与 tool 消息配对、多下载确认流程均保持不变。

### 113.2 兼容与验证

- 旧会话的普通 user content 原样读取，新发送消息使用新 JSON，可自然共存；基础 system 新增稳定规则说明 `turn_context` 作用域。
- 连续请求测试证明前一个请求 messages 是后一个请求的精确前缀；测试覆盖标签/scope 变化、JSON 转义、SHA shape、旧 history、工具协议回归。
- 本轮未完成真实 DeepSeek provider 缓存命中率 A/B 或真机体验验证；不以单次随机 miss 判断失败。

## 114. 主线第十二轮：AI 日志脱敏与缓存前缀优化验收（03号验收）

### 114.1 总体结论

- 本轮总体为“部分通过”。01 号计划代码主体通过，但旧 DeepSeek Key 尚未确认在 provider 平台撤销/轮换，因此不能标记为完全完成。
- 02 号计划的稳定前缀改造主体成立，但验收发现两个 P1 语义风险，不能把“基础 system + history”通过等同于产品行为完全不回归。
- 03 号验收为只读复核，未修改业务代码、未回退执行者改动、未替用户执行外部密钥操作。

### 114.2 已验证内容

- `flutter test` 全量 210/210 通过，日志脱敏/缓存前缀/prompt/conversation 定向测试 48/48 通过，`flutter analyze` 无问题，`git diff --check` 通过。
- request/response/error/retry 已接入凭据 redactor；当前导出、ZIP 和历史分享使用脱敏副本；`AiUsage shapeV=2` 已记录 conversation/turn/round、messages/tools count/hash。
- `_buildRequestMessages()` 已收敛为基础 system + history；连续普通 send 证明旧 messages 是后续请求前缀；旧纯文本 history 可读取，既有 dispatch 硬拦截、tool-call 配对和下载协议未出现测试回归。

### 114.3 验收发现与风险

- P1：长期来源限制仍存在、当前轮启用仅本地时，`turn_context` 可能同时写 `local_only: true` 和 `online_only: true`，并保留 `online_sources`；这与 controller/tools 的“localOnly 优先、effectiveOnlineOnly 为 false”契约矛盾，模型可能误判范围。
- P1：旧动态 system 中“直接搜索、不询问、不得使用其他来源、多来源分别调用并合并”的关键行为语义没有完整迁入稳定规则或 JSON；dispatch 能阻止越界调用，但不能保证模型不反问或漏掉多来源分发。
- P2：当前定向测试未真正驱动 interceptor 四类回调、两个分享按钮失败流程、同一 send 的多工具/下载确认续跑、旧 history 发送后保存重载、Unicode/空正文和不同 Set 插入顺序。
- P2：`log_file_service.dart` 的单文件导出共用临时目录，快速连续或并发分享时后一次导出可能删除前一次仍在读取的副本。

### 114.4 后续边界

- 下一步应先修正 `localOnly` 与 `onlineOnly` 的互斥编码，恢复范围指令的“直接搜索/不询问/多源分别调用并合并”语义，并补最小 prompt/JSON 测试后重新验收 02。
- 旧 Key 的撤销/轮换必须由用户在 provider 平台完成；代码只能阻止后续日志继续写出，不能让历史凭据失效。
- 临时目录竞争、interceptor/UI 真实回调覆盖和更完整的恢复测试可作为后续补强，但不应在未新开计划前混入本轮业务改动。

### 114.5 关键位置

- 02 P1 范围编码：`lib/foundation/ai/ai_conversation.dart:614`、`:796`
- P2 临时目录竞争：`lib/foundation/log_file_service.dart:345`
- 第十二轮实现与测试：沿用本篇 §112/§113 的文件清单；验收依据为 `Z-plan/新需求-主线-第十二轮/03-验收-AI日志脱敏与缓存前缀优化.md`

## 115. 主线第十二轮：远程服务代理分流与连接态重建（04/05号计划）

### 115.1 诊断结论与代理策略

- 04 号诊断确认远程状态、资源库 JSON、图片和 WebSocket 分属不同网络栈；仓库没有统一 LAN bypass，远程长生命周期 client/连接池和 WebSocket 还存在重启恢复型状态残留风险。高概率组合是代理/TUN 路由未直连私网，再叠加运行态未重建，但未做真实现场矩阵，不能写成抓包实锤。
- 05 号新增统一 `RemoteServiceNetworkPolicy`：按每个实际请求 URI 判定，私网/回环/链路本地/IPv6 私有/`.local`/单标签主机 `DIRECT`；公网按手动代理 > 环境代理 > direct fallback；mDNS 与网段发现强制直连。同步代理回调不做 DNS。
- 远程服务的状态、资源库控制请求、远程图片和 WebSocket 使用同一策略；绝对图片 URL 按自身 host 判定，不因 base URL 的地址类型强制另一 host 直连。

### 115.2 HTTP 与 WebSocket 生命周期

- `RemoteLibraryClient` 保留 `_instances` 和已有对象身份，原位替换 HTTP transport；transport/cache generation 防止旧请求结果回写新缓存，超时、Socket/握手异常使当前 transport 失效，下一次请求重建。非幂等写请求不自动重放。
- WebSocket 每代拥有独立 `HttpClient`，连接、心跳、重连和回调校验 generation 与目标 URI；迟到握手主动回收，旧 subscription 不得关闭或覆盖新连接；补充幂等 `stop()`。
- 代理设置保存/清除和应用回到前台都会触发远程 transport 重建；页面、Provider 和旧业务对象不因清空实例表而失效。

### 115.3 验证与未完成事项

- 已通过 `dart format`、远程策略与 transport 生命周期定向测试、全量 `flutter test` 221 项、`flutter analyze --no-pub` 和 `git diff --check`。
- 尚未完成真实代理/TUN/VPN 的“魔法开/关 × 应用重启/不重启”四组现场矩阵；未新增可控 WebSocket connector 竞态测试，暂不能宣称现场网络和 WebSocket 迟到连接验收全部完成。
- 不在本轮范围：Dio、WebView、平台工程、服务端、第三方 TUN 路由及代理工具本身的 LAN allowlist。

### 115.4 涉及文件

- 实现：`lib/network/remote_service_network_policy.dart`、`lib/foundation/remote_library_data_source.dart`、`lib/foundation/service_data_source.dart`、`lib/foundation/remote_library_event_channel.dart`、`lib/pages/settings/network_settings.dart`、`lib/pages/settings/settings_page.dart`、`lib/main.dart`
- 测试：`test/remote_service_network_policy_test.dart`、`test/remote_service_transport_lifecycle_test.dart`
- 诊断依据：`Z-plan/新需求-主线-第十二轮/04-诊断-远程本地资源代理超时与局域网直连分析.md`、`05-计划-远程本地资源代理分流与连接态重建.md`



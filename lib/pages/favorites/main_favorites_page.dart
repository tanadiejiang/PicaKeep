import 'package:flutter/material.dart';

class MainFavoritesPage extends StatelessWidget {
  const MainFavoritesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("收藏"),
      ),
    );
  }
}

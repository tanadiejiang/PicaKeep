# PK-06：ExHentai 入口与流程核对

2026-09-08：用户重新指定处理 PK-06。本次按现有代码中的 ExHentai 定义核对“里站”，未取得最初反馈的具体失败链接或有权限账号。代码修复与自动化验证完成，真实站点全流程验收仍待完成。

## 入口和链路

| 环节 | 核对结果与处理 |
| --- | --- |
| “工具”及“我”页输入链接 | 已识别 ExHentai `/g/{gid}/{token}/` 并进入 EH V2 详情；并非完全没有里站入口 |
| 登录与搜索站点 | 已有双域 Cookie 登录及 igneous 输入；补充表站/里站下拉选择并持久化 settings[20]，搜索沿现有站点设置选域名 |
| 详情及 Content Warning | 详情请求原本使用输入链接；保留确认及取消行为 |
| 普通阅读与 MPV | 修复 showpage、imagedispatch API 跟随全局设置的错误，按画廊链接选站；表站 API Host 同步为 api.e-hentai.org |
| 换节点重试 | 阅读和下载的 `/s/` 重试及 MPV 重试沿用画廊站点，切换搜索站点不改变在途画廊所属站点 |
| 封面、阅读图片、逐页下载、归档下载 | Cookie 与 Referer 从该画廊所属站点获取，不再读取可能已被另一站请求覆盖的 cookiesStr |
| 平台收藏、取消、评分、评论投票 | 详情操作携带画廊链接；评论正文请求本来已使用链接 |
| 本地收藏 | PK-04 在独立 PR #3；本分支不包含该项实现 |
| 已下载重开 | 原有画廊完整链接及下载 ID 持久化规则保持不变 |
| 剪贴板本地匹配 | local_app_links 是本地查找提示入口，不等同于在线打开；未将其改造为在线路由 |

未增加 `/s/` 单页、MPV、归档链接到画廊的反向解析；这类入口与普通 `/g/` 画廊链接不同，若原反馈指向此类链接，需要补充样本后再确定行为。

## 验证

- eh_site_test：6 项通过，覆盖两站交叉选择、阅读页链接、www/大小写、旧调用回退、相似域名。
- eh_site_entry_test：2 项通过，真实登录页选择回调往返切换并检查持久化文件；真实 CookieJar 的两站不同 Cookie 在交叉切换后保持各自请求头。
- eh_content_warning_test：3 项通过，确认、取消和普通错误。
- eh_download_limit_page_test：4 项通过，本地 HTTP 服务验证 509、200 HTML 与正常图片的既有判定契约。
- 修改代码及新测试定向静态分析通过；未执行真实平台写操作、收费归档下载、APK 构建或真机测试。路由测试不等同于真实 showpage/MPV 网络成功。

## 真机验收步骤

1. 在 EH 登录页选 ExHentai，使用具备里站权限的账号 Cookie 登录，重启后确认选项保留并搜索里站内容。
2. 搜索站点选择表站，从“工具”或“我”的链接入口打开仅里站可见的完整 `/g/` 链接，检查详情、封面、普通阅读与 MPV 阅读。
3. 表站和里站各加入一个逐页下载任务，执行暂停、继续、失败重试；确认请求没有跨站，完成后从已下载重新阅读。
4. 在详情页验证平台收藏/取消、评分、评论操作；本地收藏需同时包含 PR #3。
5. Cookie 失效或账号无权限时确认显示错误，不将站点访问权限不足视为路由修复失败。归档流程涉及站点积分，单独按用户选择验收。

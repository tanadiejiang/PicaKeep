# PicaKeep 交接待办

> 最后更新：2026年9月20日 · 随 V1.9.70 整理
> 本清单只记「还没做完 / 还没验完」的事，已完成内容不在此重复，见三份交接文档（精简版 / 经验思路版 / 完整版）。
> 写法约定：只写"要做什么、为什么、验什么"，**不引用计划文档路径**（计划目录不随版本管理、易失）。

---

## 一、待执行的计划

1. **JM 列表卡片分类标签还原**
   - 现状：JM 的搜索结果、收藏列表、收藏夹列表卡片只显示标题与作者，**没有分类标签**（原项目显示「同人」「單本」这类）。
   - 已查明：原项目与本项目列表用的是**同一个接口**，差别在解析字段 —— 原项目读 `category.title` + `category_sub.title`，本项目三处列表读的是接口并不返回的 `c['tags']`（`_parseStringList` 对缺失字段返回空列表）。
   - 要做：三处列表解析改为读分类字段（防御式：字段缺失/类型异常不崩、不产生纯数字标签），并明确「同人 同人」这种重复标签是否去重（原项目不去重）。

---

## 二、V1.9.70 已落地但审查指出的不足（搜索结果页）

2. **大字号下输入区域高度不足** —— 系统字号 1.8 时 M06 未满足。
3. **桌面切源弹窗几乎撑满窗口** —— 宽屏容器的 500dp 宽度上限没有作用到该弹窗。
4. **「新查询重新计数」用例断言强度不足** —— 现用例在新查询里换了漫画与标签，即使实现错误地一直复用旧 `operationId` 也会通过。修正方式：让新查询返回**同一漫画、同一标签**，只让 `operationId` 变化。
5. **JM 七项排序缺真实请求证据** —— 只有「总排行有结果、日排行为空、无参数错误」这组对照，没有记录七个 `o=` 值与对应响应；按原验收标准 M03 未算完整通过。
6. **窄屏顶栏搜索框偏挤** —— 360dp 实测约剩 240dp，可用但偏窄。

---

## 三、真机验收待办

> 已通过的部分：用户真机确认 **Picacg / JM / EH / NH 四源退出与重登正常**；账号页与容器路径已由截图核对。

7. **M04**：JM 注册外链能否实际打开；EH 注册的四字段 / 快速填写 / 搜索站点选择。
8. **M05**：EH 账号页展开 `cookies` → 点按复制 → 编辑 `igneous` → 重开页面/应用。双域写入与「空值 = 移除」目前**只有本地 jar 层自动化覆盖**，真实站点未验。
9. **M06 / M07**：EH / NH 的 WebView 登录完成、取消、连续标题、关闭；NH 登录 → 退出 → 重新进入登录 → 重启。
10. **M08**：断网、错误账密、资料加载失败、长用户名/Cookie、系统字号 1.8、繁中/英文。
11. **V1.9.70 尚未上机** —— 提交的代码未重新打包或覆盖安装，手机上运行的是更早的 profile 包。

---

## 四、明确未验证的边界（不要当成已通过）

12. 未执行真实四源认证、原生 Windows WebView2、Android/iOS WebView 或实际 NAS 访问；网页假对象测试只证明**接线与顺序**，不证明平台插件在所有环境的行为。
13. 桌面插件 `launch` 是 `void async` —— **不能把包装器 `open` 的完成解释成网页加载完成**，也不能保证捕获其所有原生异步错误。
14. SQLite、源文件与 SharedPreferences 之间**并非跨存储原子事务**；进程强杀与全局后台身份写入仍属已知边界之外。

---

## 五、长期已知缺口（非本轮引入）

15. **NAS 账号管理未实现** —— 入口保留，点击只提示未接入，不写模式文件、不发远程请求。
16. **PK-03 部分远程访问不可达** —— 未启动，仅有静态线索：网页远程代理返回响应流后立即关闭上游客户端。
17. **PK-02 写入端未对齐** —— 下载目录名写入侧不 trim、不处理换行，新下载仍可能生成带前导空格/换行的脏目录名（此前只修了读取端使存量可见）。
